# Implementation Plan: 중간선거_사이클 — 9월 말 진입 전환과 분기 분해

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-22 22:55
**마지막 업데이트**: 2026-09-23 00:05
**관련 범위**: studies/midterm_cycle, report, scripts, tests, docs, `.claude/rules`
**관련 문서**: 루트 `CLAUDE.md`, `src/verify_lab/CLAUDE.md`, `scripts/CLAUDE.md`, `tests/CLAUDE.md`, `.claude/rules/trading.md`, `.claude/rules/research.md`, `.claude/rules/docs.md`, `.claude/plan-config.json`

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: 진입을 **9월 마지막 거래일 종가**로 옮긴다 (청산은 다음해 6월 마지막 거래일 종가 그대로, 개월 수 8 → **9**)
- [x] 목표 2: 측정 축을 **중간선거해 한 칸**으로 좁힌다 (사이클 네 칸 → 하나)
- [x] 목표 3: **분기별 수익**(4분기·1분기·2분기)을 산출물로 낸다
- [x] 목표 4: **보유 중 최악의 «분포»**를 전체 구간과 분기별로 낸다 — 「보통 진입가 대비 얼마나 밀리나」에 답한다
- [x] 목표 5: 문서 세 장(`설계.md`·`결과.md`·`규칙.md`)을 새 수치로 다시 쓴다

## 2) 비목표(Non-Goals)

- **손절선 확정** — 사용자가 「추후 결정」으로 정했다. 손절선 격자(무손절 + −5 ~ −30% 여덟)는 지금 형태 그대로 둔다
- **분할매수 설계** — 같은 이유로 범위 밖이다. 분기 분해가 그 판단의 «재료»이지 설계가 아니다
- **고점 대비 MDD** — 사용자가 「보유 중 최악만」으로 정했다. 진입가 대비 하나만 잰다
- **레버리지 배수·투자금** — `규칙.md` §1.2 의 미결 항목으로 남긴다
- **지수를 OHLCV 로 재수집** — `^GSPC` 의 34.5% 가 고가==저가라 장중이 가짜다. 지수는 종가 기준을 유지한다 (설계 결정 ⑥)
- **한국 시장 교차 재현** — 신호가 미국 선거 달력이다. 이번 범위가 아니다
- **공유 계층(`execution/periods.py`·`trade_fill.py`) 변경** — 컬럼을 더하면 역방향·만기_말일 성적표가 함께 바뀐다. 새 산출물은 이 매매법 패키지가 소유한다

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

**사용자가 규칙을 바꿨다** (2026-09-22).

| # | 지시 | 지금 | 바뀜 |
| --- | --- | --- | --- |
| 1 | 2026년 사이클 진행 | — | `규칙.md` §6 에 반영 (문서) |
| 2 | 측정도 중간선거해만 | 사이클 네 칸 | **한 칸** |
| 3 | 9월 마지막 종가 매수 | 10월 첫 거래일 | **9월 마지막 거래일** |
| 4 | 분기별 수익 통계 | 없음 | **신설** |
| 5 | 분기별·전체 보유 중 최악의 분포 | 최솟값만 | **분포 신설** |
| 6 | 손절·분할매수 | 격자 | **추후 결정** (그대로) |

**진입 변경은 「하루 앞당기기」다.** 실측으로 24개 중간선거해 **전부**에서 9월 마지막 거래일이 10월 첫 거래일의 바로 앞 거래일이다(간격 1거래일, 예외 0). 격자에서 값을 고른 것이 아니라, **보유 구간을 달력 분기(4Q·1Q·2Q)에 정확히 맞추려는 이동**이고 그래서 목표 3·4 가 성립한다.

**「보통 얼마나 밀리나」에 지금 산출물이 답하지 못한다.** 성적표의 `보유 중 최악(%)` 은 구간 **최솟값**(가장 깊은 한 건)이라 SPY 가 −19.87% 로 찍히는데, 그것은 2018년 한 건이다. 체결별 값은 `거래내역.csv` 에 이미 있으나 **분포를 낸 표가 없다.**

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- `.claude/plan-config.json` — 이 저장소의 검증 명령·자동 포맷·근거 승격 목적지 (**값의 SoT**)
- 루트 `CLAUDE.md` — 측정의 원칙 전부, 특히 1·3·4·8·12·16·17, 그리고 「계획서 규약」 절
- `src/verify_lab/CLAUDE.md` — 계층 간 계약(매매 산출물 계약 · `측정.csv` · 검증 계층의 `row_counts` · 출력 계약)
- `.claude/rules/trading.md` — 경계는 폴더가 아니라 행위 · 손절 · 「최악」이 둘인 이유
- `.claude/rules/research.md` — 결과 문서 필수 구조
- `.claude/rules/docs.md` — 데이터 기간 표기 · 과거형이 허용되는 자리 · 용어
- `scripts/CLAUDE.md` · `tests/CLAUDE.md`

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [x] 기능 요구사항 충족 (목표 1~5)
- [x] 회귀/신규 테스트 추가 — 분기 12건 신규 · 달력 38건 갱신
- [x] 코드 리뷰 실행 및 결과 기록 (마지막 Phase 의 Validation 에 적는다)
- [x] 품질 검증 통과 (마지막 Phase 의 Validation 에 passed/failed/skipped 수를 적는다)
- [x] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [x] 필요한 문서 업데이트 — `docs/COMMANDS.md` **변경** · `docs/INDEX.md` **변경** · `scripts/CLAUDE.md` **변경** · 검증 문서 세 장 **재작성** · `.claude/rules/trading.md` **변경**(2026-09-23 사용자 승인)
- [x] 근거 승격 완료 — 설계 결정 ⑫~⑯ 은 `설계.md`, 실측 수치·분포·기각 가설은 `결과.md`, 미결 다섯과 그 재료는 `규칙.md`, **검증 등급을 좁힐 때의 조건 둘**은 `.claude/rules/trading.md` 로 옮겼다
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

- `src/verify_lab/studies/midterm_cycle/constants.py` — 진입 달 9 · 개월 수 9 · 축 한 칸 · 분기 정의 · 새 산출물 이름
- `src/verify_lab/studies/midterm_cycle/cycle_calendar.py` — `month_first_entries` → **그 달 «마지막» 거래일** 진입
- `src/verify_lab/studies/midterm_cycle/quarters.py` — **신규.** 분기 분해와 보유 중 최악 분포 산식
- `src/verify_lab/studies/midterm_cycle/runner.py` — 축 축소 반영
- `src/verify_lab/studies/midterm_cycle/trading.py` — 축 축소 · 분기 표 조립 · 실행 조건 문구
- `scripts/run_midterm_cycle.py` — 새 산출물 저장
- `tests/test_studies_midterm_cycle_calendar.py` · `tests/test_studies_midterm_cycle_runner.py` — 갱신
- `tests/test_studies_midterm_cycle_quarters.py` — **신규**
- `docs/검증/중간선거_사이클/{설계,결과,규칙}.md` — 재작성
- `docs/COMMANDS.md`: **변경 있음** — §「검증 — 중간선거_사이클」 의 제목·규칙 설명·산출물 목록이 전부 10월 기준이다
- `docs/INDEX.md`: **변경 있음** — §3 표의 중간선거_사이클 설명이 10월 진입·네 칸 기준이다
- `.claude/rules/trading.md`: **변경 있음(사용자 승인 필요)** — 「확정 칸으로 좁히는 것을 검증 등급에는 적용하지 않는다」에 예외가 생긴다

### 데이터/결과 영향

- 🔴 **`storage/results/검증/중간선거_사이클/` 이 통째로 덮인다.** 지금 문서의 모든 수치(평균 +15.76% · 승률 100% · 기준선 대비 +9.66%p 등)는 **10월 진입·네 칸 기준**이라 재현되지 않는다
- 산출물 파일 **3개 → 5개**, 행 수 예상: `측정.csv` 20 → **5**, `성적표.csv` 580 → **약 145**, `거래내역.csv` 945 → **약 236**, `분기.csv` **20**(신규), `분기내역.csv` **174**(신규)
- **시세는 재수집하지 않는다.** 데이터 기간이 그대로이므로 값의 변화는 전부 규칙 변경에서 온다

---

## 6) 단계별 계획(Phases)

### Phase 0 — 인바리언트를 테스트로 먼저 고정(레드)

> 진입일 정의·축·산식이 전부 바뀌고 **최종 수치가 달라지는 변경**이므로 이 Phase 를 둔다.

**작업 내용**:

- [x] 아래 「확정한 설계 결정」을 `docs/검증/중간선거_사이클/설계.md` 에 결정 ⑫~⑯ 으로 먼저 기록한다
- [x] 진입일 테스트 — 합성 거래일에서 **그 달 마지막 거래일**이 나오고, 말일이 휴장이면 **앞당겨진다**
- [x] 진입일 회귀 테스트 — 중간선거해 진입일이 **9월 마지막 거래일**이다 (합성 달력으로 고정)
- [x] 축 테스트 — 산출 축이 **중간선거해 하나**이고 `사이클 위치` 컬럼은 남는다
- [x] 분기 경계 테스트 — 4분기·1분기·2분기의 시작·끝이 **그 분기 마지막 거래일**로 잡힌다
- [x] 🔴 **검산 불변조건 둘** — ① `(1+4Q)(1+1Q)(1+2Q) = 1 + 전체 수익률` ② `min(분기 3개의 진입가 대비 최악) = 전체 보유 중 최악`
- [x] look-ahead 감시 — 뒤를 자른 입력의 결과가 전체 입력과 겹치는 범위에서 같다

**Validation**:

- [x] 새 테스트가 **의도한 대로 실패**한다 — `ImportError` 2건으로 수집 단계에서 멈췄다 (레드 확인)

---

### Phase 1 — 달력 교체와 축 축소(그린 유지)

**작업 내용**:

- [x] `cycle_calendar.month_first_entries` → **`month_last_entries`** 로 바꾼다 — 그 달 마지막 거래일, 휴장이면 **앞당김**
- [x] **데이터의 «마지막» 달을 진입에서 뺀다** — 끝이 잘린 달의 「마지막 거래일」은 실제 월말이 아니라 데이터가 끊긴 날이다 (첫 달을 빼던 것과 이유가 대칭으로 뒤집힌다)
- [x] `constants.ENTRY_MONTH` 10 → **9**, `EXIT_MONTH_OFFSET` 8 → **9**
- [x] `constants.CYCLE_POSITIONS` 를 **중간선거해 하나**로 좁히고, 진입 단계에서 그 해만 남긴다
- [x] `_order_by_cycle` 정리 — 축이 하나면 정렬할 것이 없다. **본인 변경으로 생긴 orphan 만** 정리한다
- [x] 모듈 docstring 과 실행 조건 문구(`NOTE_ENTRY`·`NOTE_AXIS`)를 새 규칙으로 고친다
- [x] 기준선이 「중간선거해에 **아무 달** 마지막 거래일 진입 → 9개월 뒤 달 마지막 거래일 청산」으로 유지되는지 확인한다 — **12달 전부를 계속 계산한다**

**Validation**:

- [x] Phase 0 의 진입일·축 테스트가 **그린**이 된다 — 달력 38건 통과
- [x] 전체 실행으로 SPY 진입일 8건이 `1994-09-30 · 1998-09-30 · 2002-09-30 · 2006-09-29 · 2010-09-30 · 2014-09-30 · 2018-09-28 · 2022-09-30` 과 일치한다

---

### Phase 2 — 분기 분해와 보유 중 최악 분포(그린 유지)

**작업 내용**:

- [x] `studies/midterm_cycle/quarters.py` 신설 — 분기 경계 산출 · 분기별 수익 · 분기별·전체 보유 중 최악 분포
- [x] `분기내역.csv` 조립 — 종목 × 진입 연도 × 분기. 컬럼: 시작일 · 시작가 · 종료일 · 종료가 · 수익률(%) · **진입가 대비 최악(%)** · **분기 시작가 대비 최악(%)**
- [x] `분기.csv` 조립 — 종목 × 구간(**전체 · 4분기 · 1분기 · 2분기**). 수익 쪽(표본 · 합계 · 평균 · 중앙값 · 오른 비율 · 최고 · 최악)과 낙폭 쪽(평균 · 중앙값 · 25%분위 · 75%분위 · 가장 얕게 · 가장 깊게 · −5/−10/−15/−20% 이내 건수)
- [x] **무손절 기준으로만 낸다** — 손절이 걸리면 보유가 중간에 끊겨 분기가 온전하지 않다. 그래서 이 두 표에 `손절선(%)` 축이 없다
- [x] `OUTPUT_FILES` 와 `row_counts` 에 두 파일을 등록한다 (파일 이름의 소유자는 그 패키지의 `constants.py`)
- [x] `scripts/run_midterm_cycle.py` 가 두 표를 저장하고 터미널에 발췌한다 — **CLI 는 파일 이름 리터럴을 갖지 않는다**

**Validation**:

- [x] Phase 0 의 분기 경계·검산 테스트가 **그린**이 된다 — 분기 12건 통과
- [x] `분기.csv` 「전체 보유」 행의 `가장 깊게` 가 `성적표.csv` 무손절 행의 `보유 중 최악(%)` 과 **전 종목에서 일치**한다

---

### Phase 3 — 재실행과 산출물 대조(그린 유지)

**작업 내용**:

- [x] 기본값(인자 없이)으로 전체 실행한다 — **좁혀 돌린 결과가 전체 실행을 덮으므로** 문서에 쓸 값은 인자 없는 실행의 것이다
- [x] `summary.json` 의 `datasets`·`rule`·`row_counts`·`notes` 가 새 규칙을 말하는지 확인한다
- [x] 한 번 더 돌려 `diff -r` 로 **재실행 동일성**을 확인한다 (`summary.json` 제외 — `row_counts` 키가 파일 이름이다)
- [x] 새 수치를 표로 정리해 문서에 옮길 준비를 한다 — **도구 출력을 그대로 붙여넣고 눈으로 읽어 다시 타이핑하지 않는다**

**Validation**:

- [x] 두 번 실행한 산출물이 **바이트 단위로 같다** — `diff -r` 차이 0 (2회 확인)
- [x] 행 수: `측정.csv` **5** · `성적표.csv` **145** · `거래내역.csv` **226** · `분기.csv` **20** · `분기내역.csv` **174**

---

### 마지막 Phase — 문서 정리 및 최종 검증

**작업 내용**

> 🔴 **`/commit` 이 «맨 마지막»인 것은 의도다.** 그 스킬은 「후보 뒤에는 아무것도 덧붙이지 말 것」으로
> 끝나므로 **호출하는 순간 그 턴이 거기서 닫힌다.** 중간에 두면 뒤에 적힌 항목이 그 벽 너머에 남는다 —
> 실제로 두 번 그렇게 샜다(`[실측] 2026-09-14` 후보를 계획서에 안 옮김 · `2026-09-16` 옮기고 체크박스를 안 닫음).
> **체크박스와 상태를 먼저 확정하고, 커밋 후보를 마지막에 만든다.**

- [x] 필요한 문서 업데이트 (`docs/COMMANDS.md` **변경 있음** · `docs/INDEX.md` **변경 있음** · `scripts/CLAUDE.md` **변경 있음**)
  - [x] `설계.md` — 결정 ⑫~⑯ 확정, 결정 ①②③④ 의 「탈락안·근거」를 현재 규칙에 맞게 고치되 **왜 그랬는지는 남긴다**
  - [x] `결과.md` — 새 수치로 재작성. §5 의 「사이클 네 칸」이 사라지므로 **그 결론이 10월 진입·네 칸에서 나온 기록임을 §16 기각 가설에 남긴다**. §14 에 이번 진입 변경을 사후 선택 관점에서 적는다
  - [x] `규칙.md` — §1.1 진입 문구, §2 성적, §5 원자료, §6 다음 사이클(2026-09-30 진입 · 2027-06-30 청산)
  - [x] 🔴 **`.claude/rules/trading.md`** — 2026-09-23 사용자 승인. 「검증 등급의 기본도 좁히지 않음」으로 고쳐 쓰고, **좁히려면 지켜야 할 조건 둘**(좁히기 전 값을 `결과.md` 가 수치로 갖는다 · 사라진 축이 답하던 질문을 `설계.md` 가 적는다)을 새로 달았다
- [x] 자동 포맷 적용 (`poetry run black .`) — 3파일 재포맷
- [x] 변경 기능 및 전체 플로우 최종 검증 — 전체 실행 2회, 산출물 대조 3종 통과
- [x] Validation 절에 `/code-review` 와 품질 검증 **실행 결과**를 적는다
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정
- [x] 🔴 **마지막에 `/commit` 을 실행하고 그 후보를 «이 계획서» 의 `#### Commit Messages` 절에 옮긴다** —
      `/commit` 은 계획서를 모르고 「후보 뒤에 아무것도 덧붙이지 말 것」으로 끝나므로,
      **대화에만 내면 그 절이 빈 채로 남는다.** 체크박스 갱신이 diff 에 더 들어가지만
      커밋 메시지의 내용을 바꾸지 않는다. **커밋은 사용자가 한다 — 후보만 낸다**

**Validation**:

> 순서를 지킨다 — 리뷰에서 고치면 코드가 바뀌므로 품질 검증이 마지막 관문이어야 한다.
> **버그가 0 인 회차에서 끝낸다.** 2회차에도 버그가 나오면 사용자에게 보고하고 3회차 여부를 묻는다 —
> 「그 외」는 목록만 남기고 고치지 않는다. `/impl-plan` 의 「코드 리뷰」 절이 SoT 다.

- [x] `/code-review xhigh` **1회차** (발견 12건 — 버그 **0** · 그 외 12 · 조치: **사실 오류 5건만 수정**, 설계 지적 7건은 사용자가 보류)
- [x] `poetry run python validate_project.py` (passed=1323, failed=0, skipped=0)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

1. `검증 / 중간선거_사이클 9월 말 진입 전환과 분기 분해 신설`
2. `검증 / 중간선거_사이클 진입일 9월 마지막 거래일 이동과 산출 축 중간선거해 단일화`
3. `검증 / 보유 구간의 달력 분기 정렬로 4분기 낙폭 집중 확인`
4. `검증 / 중간선거_사이클 — 9월 말 진입·개월 수 9·중간선거해 단일 축과 분기별 수익·보유 중 최악 분포 산출물 둘 추가`
5. `검증 / 분기 분해 모듈 신설과 검증 등급 축 축소 조건 명문화`

## 7) 리스크(Risks)

| 리스크 | 완화 |
| --- | --- |
| 🔴 **기존 문서의 수치가 통째로 재현 불가**가 된다 | 새 수치로 재작성하고, 사라지는 결론(사이클 네 칸 분리)은 §16 기각 가설에 **그때의 기록**으로 남긴다 |
| **끝이 잘린 달의 방향이 뒤집힌다** — 진입이 「첫 거래일」에서 「마지막 거래일」로 바뀌면 빼야 할 달이 첫 달에서 **마지막 달**로 옮겨간다 | Phase 1 에 명시 작업으로 넣었다. 놓치면 데이터가 끊긴 날에 진입하는 가짜 표본이 생기고 **예외는 나지 않는다** |
| **분기 분해의 분모를 하나만 내면 답이 반쪽**이 된다 | 진입가 대비와 분기 시작가 대비를 **둘 다** 낸다 (측정의 원칙 1 — 고르지 않는다) |
| **공유 계층을 건드리면 다른 매매법 성적표가 바뀐다** | 새 산출물을 이 패키지가 소유한다. `execution/` 은 손대지 않는다 |
| **축이 하나로 줄면서 죽은 코드가 남는다** | 본인 변경으로 생긴 orphan 만 정리하고, 사전에 있던 것은 언급만 한다 |
| **좁혀 돌린 실행이 전체 실행을 덮는다** | Phase 3 을 인자 없는 실행으로 마무리한다 |

## 8) 메모(Notes)

### 확정한 설계 결정 (Phase 0 에서 `설계.md` 로 옮긴다)

| # | 결정 | 탈락안 | 근거 |
| --- | --- | --- | --- |
| ⑫ | 진입 **9월 마지막 거래일 종가** · 개월 수 **9** | 10월 첫 거래일 (기존) | 보유가 **4Q·1Q·2Q 에 정확히 맞는다.** 실측으로 24개 중간선거해 **전부**에서 9월 마지막 거래일이 10월 첫 거래일의 바로 앞 거래일이라(간격 1, 예외 0) **격자 탐색이 아니라 하루 이동**이다. 부수 효과로 진입·청산이 둘 다 「그 달 마지막 거래일」(앞당김)이 되어 방향이 같아진다 |
| ⑬ | 산출 축 **중간선거해 하나** | 사이클 네 칸 유지 | 사용자 결정 (2026-09-22). **대가를 명시한다** — 「Best Six Months 라 좋았나, 중간선거 뒤라 좋았나」의 분리가 산출물에서 사라진다. **기준선 컬럼은 남아** 「아무 달 진입 대비 이 달이 특별한가」에는 계속 답한다 |
| ⑭ | `사이클 위치` 컬럼을 **전 행이 같은 값이어도 남긴다** | 컬럼 삭제 | `손절선(%)` 을 전 행이 같아도 남기는 것과 같은 이유다 — 없으면 **산출물만 봐서 무엇을 잰 표인지 판별되지 않는다** (매매 산출물 계약) |
| ⑮ | 분기 분해는 **무손절 기준으로만** 낸다 | 손절선 격자마다 낸다 | 손절이 걸리면 보유가 중간에 끊겨 **분기가 온전하지 않다.** 이 분해의 질문이 「어느 분기가 수익을 만들고 어느 분기에 밀리나」라 끝까지 들고 간 경로를 봐야 한다. 손절선은 아직 미정이기도 하다 |
| ⑯ | 분기별 낙폭을 **두 분모로** 낸다 — 진입가 대비 · 분기 시작가 대비 | 하나만 낸다 | 다른 질문이다. 진입가 대비는 **전체 보유 중 최악과 검산이 맞고**(세 분기의 최솟값 = 전체), 분기 시작가 대비는 **같은 행의 분기 수익과 분모가 같아** 나란히 읽힌다. 하나를 고르면 그 선택이 결론에 숨는다 (측정의 원칙 1) |

### 착수 전에 실측한 것 (근거 승격 대상)

**9월 마지막 거래일 ↔ 10월 첫 거래일 (SPY·`^GSPC`, 간격 전부 1거래일)**

| 연도 | 9월 마지막 | 10월 첫 | | 연도 | 9월 마지막 | 10월 첫 |
| ---: | --- | --- | --- | ---: | --- | --- |
| 1930 | 09-30 | 10-01 | | 1982 | 09-30 | 10-01 |
| 1934 | 09-28 | 10-01 | | 1986 | 09-30 | 10-01 |
| 1938 | 09-30 | 10-03 | | 1990 | 09-28 | 10-01 |
| 1942 | 09-30 | 10-01 | | 1994 | 09-30 | 10-03 |
| 1946 | 09-30 | 10-01 | | 1998 | 09-30 | 10-01 |
| 1950 | 09-29 | 10-02 | | 2002 | 09-30 | 10-01 |
| 1954 | 09-30 | 10-01 | | 2006 | 09-29 | 10-02 |
| 1958 | 09-30 | 10-01 | | 2010 | 09-30 | 10-01 |
| 1962 | 09-28 | 10-01 | | 2014 | 09-30 | 10-01 |
| 1966 | 09-30 | 10-03 | | 2018 | 09-28 | 10-01 |
| 1970 | 09-30 | 10-01 | | 2022 | 09-30 | 10-03 |
| 1974 | 09-30 | 10-01 | | | | |
| 1978 | 09-29 | 10-02 | | | | |

**보유 중 최악의 분포 (10월 진입 기준 · 무손절 · 중간선거해)** — 바뀐 규칙의 대조축이다.

| 종목 | 표본 | 평균 | 중앙값 | 가장 얕게 | 가장 깊게 | −5% 이내 | −10% 이내 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| SPY | 8 | −6.60 | −5.72 | −0.32 | −19.87 | 3/8 | 6/8 |
| DIA | 7 | −5.80 | −2.77 | −0.15 | −18.66 | 4/7 | 6/7 |
| QQQ | 6 | −8.26 | −7.11 | −0.65 | −22.94 | 2/6 | 4/6 |
| S&P 500 지수 | 24 | −5.91 | −3.13 | +1.46 | −37.18 | 16/24 | 20/24 |
| 나스닥 종합 지수 | 13 | −6.75 | −5.50 | +0.27 | −22.95 | 6/13 | 10/13 |

살 수 있는 셋 합산 21건 — 평균 **−6.81** · 중앙값 **−5.61** · 25%분위 −9.96 · 75%분위 −1.65 · −10% 이내 **16/21(76%)** · −15% 이내 18/21 · −20% 이내 20/21.

🔴 **다섯 대상의 최악이 전부 2018년 한 해다** (SPY −19.87 · DIA −18.66 · QQQ −22.94 · S&P 500 −19.61 · 나스닥 −22.95). **종목을 나눠 담아도 밀릴 때는 같이 밀린다** — 투자금 결정의 근거가 된다.

**지수의 보유 중 최악은 종가 기준이라 얕다** — ETF 21건은 **전부 음수**인데 지수 37건 중 **5건이 양수**다(1942 +1.46 · 1958 +0.38 · 1986 +0.05 · 2006 +0.21 · 나스닥 2006 +0.27). 9개월을 들고 한 번도 진입가를 안 건드리는 일은 **종가만 봤을 때 생기는 착시**다.

### 사용자 판단이 필요한 것 (그 지점에서 멈추고 묻는다)

- 🔴 **`.claude/rules/trading.md` 의 「확정 칸으로 좁히는 것을 검증 등급에는 적용하지 않습니다」** — 결정 ⑬ 이 그 문장의 예외가 된다. **규칙 문서는 사용자의 것이므로 고치기 전에 승인을 받는다.** 그때까지는 `설계.md` 결정 ⑬ 이 예외를 기록한다
- 🔴 **코드 리뷰 12건의 조치 여부** — **버그 0** 이라 회차를 끝냈고, 「그 외」는 `/impl-plan` 규칙대로 **고치지 않고 목록만 남긴다.** 아래가 그 목록이다

### 코드 리뷰 1회차 발견 (xhigh · 버그 0 · 그 외 12)

**사실 오류 — 고치면 값이 아니라 «가리키는 곳»이 맞아진다**

| # | 자리 | 무엇 |
| --- | --- | --- |
| 1 | `결과.md` 머리말 | 사라진 네 칸 대조를 **§16 이라 적었는데 §17.1** 이다 (본문 다른 두 곳은 맞다) |
| 2 | `INDEX.md` 두 곳 | 「전체 보유와 4분기의 분포가 숫자까지 같다」에 **예외를 안 적었다** — S&P 500 지수는 −5.69/−34.37 대 −4.12/−22.32 로 다르다(1930-31). `결과.md` §9.2 는 「ETF 셋과 나스닥」으로 범위를 적어 두었다 |
| 3 | `cycle_calendar.py` 두 줄 | 사라진 이름 **`month_first_entries`** 를 아직 가리킨다 (docstring · 주석) |
| 4 | `trading.py` 함수 docstring | **「사이클 위치 네 칸을 돈다」**로 남아 있다 — 실제는 한 칸 |
| 5 | `quarters.py` docstring · `설계.md` ⑯ | 항등식에 **방향 부호가 빠졌다.** 코드는 맞고(`sign` 을 곱한다) 문서만 `bet_down=False` 일 때만 성립하는 형태다 |

**설계 지적 — 고치면 구조가 바뀐다**

| # | 자리 | 무엇 |
| --- | --- | --- |
| 6 | `trading.py` 「전체 보유」 행 | 성적표 무손절 행과 **같은 값을 두 경로로 계산**한다 (절대 원칙 5). 지금은 값이 같지만 보합이 생기면 승률 산식이 갈릴 수 있다 |
| 7 | `분기.csv` 헤더 | 같은 값에 **다른 이름**(`표본` 대 `신호` · `오른 비율(%)` 대 `승률(%)` · `진입가 대비 최악 가장 깊게(%)` 대 `보유 중 최악(%)`) |
| 8 | 분기 표 둘 | **`손절선(%)` 컬럼이 없다** — 매매 산출물 계약은 「전 행이 같은 값이어도 낸다」를 요구한다 |
| 9 | `quarters.py` 가드 | `simulate_scheduled_trade` 의 범위 검사를 **문구까지 복제**했다 |
| 10 | `_quarter_summary_row` | 세 리스트의 길이를 맞추지 않는다 (`zip(strict=True)` 관용에서 벗어남). `DrawdownProfile.sample_count` 가 그 검사를 할 수 있는데 쓰이지 않는다 |
| 11 | 테스트 | `_run_quarters` 의 두 계약(전체 보유 행 ↔ 성적표 대조 · 손절선 루프 «밖») 이 테스트로 안 걸려 있다 — 손절선 루프 안으로 옮기면 20행이 84행이 되는데 **전 테스트가 통과한다** |
| 12 | 두 docstring | **과거형 서술**(`cycle_calendar.py:36` · `constants.py:6-7`) — 패키지 코드에 금지돼 있다 |

**`bet_down=True` 경로는 테스트가 없다**(#5 와 짝) — `BET_DOWN` 이 `False` 로 고정돼 실행되지 않는 길이다.

### 진행 로그 (KST)

- 2026-09-22 22:55: 계획서 작성. 사용자 지시 6건 반영, 설계 결정 ⑫~⑯ 확정, 착수 전 실측 2건 기록
- 2026-09-22 23:10: Phase 0~1 완료. 테스트 레드 확인 후 달력 교체 — SPY 진입일 8건이 계획서 표와 일치
- 2026-09-22 23:20: Phase 2~3 완료. `quarters.py` 신설, 산출물 5종. 검산 3종(전체 보유 ↔ 성적표 5/5 · 분기 곱 58/58 · 분기 최솟값 58/58) 통과, 재실행 diff 0
- 2026-09-22 23:30: 문서 다섯 갱신. 1차 품질 검증에서 **레이블 겹침 2건**(`시작일`·`종료일` 이 배수 검증 둘과 충돌)과 **CLI 의 `.csv` 리터럴**이 걸려, 컬럼을 `분기 시작일`·`분기 종료일` 로 가르고 CLI 산문에서 확장자를 뺐다. 이름이 바뀌었으므로 산출물을 다시 냈다
- 2026-09-22 23:35: 코드 리뷰 1회차(xhigh) — **버그 0 · 그 외 12**. 규칙대로 「그 외」는 고치지 않고 위 표에 남기고 사용자 판단을 기다렸다
- 2026-09-23 00:05: 사용자가 **「규칙 문서 수정 + 사실 오류 5건까지」**로 정했다. `.claude/rules/trading.md` 에 **검증 등급을 좁힐 때의 조건 둘**을 새로 달고, 사실 오류 다섯(결과.md 절 번호 · INDEX.md 예외 누락 2곳 · 사라진 이름 참조 2줄 · 함수 docstring 의 「네 칸」 · 항등식의 방향 부호)을 고쳤다. **설계 지적 7건은 사용자가 보류.** 전부 서술 수정이라 **산출물은 바이트 단위로 불변**임을 재실행으로 확인했고 품질 검증도 그대로 통과했다

---
