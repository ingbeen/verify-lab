# Implementation Plan: 경량화 ⑤ — 매매 문서 6장 정리 · 역방향 결과 재작성 · 측정.csv 어긋남 열 `예`/`아니오`

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: 🟡 Draft

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-26 16:55
**마지막 업데이트**: 2026-09-26 16:55
**관련 범위**: `docs/매매/역방향/{설계,결과,규칙}.md` · `docs/매매/중간선거_사이클/{설계,결과,규칙}.md`, 중간선거 `측정.csv` 를 만드는 코드와 그 테스트
**관련 문서**: `.claude/rules/research.md`, `.claude/rules/trading.md`, `.claude/rules/docs.md`, 루트 `SLIM_SERIES.md`, `docs/plans/AUDIT_slim_trading_docs.md`, `docs/plans/REPORT_lightweight_inventory.md` §3.6-B · §10 · §12

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [ ] 목표 1: 중간선거 `측정.csv` 의 `평균-비율 어긋남` 을 `예`/`아니오` 로 낸다 (보고서 §10 「측정.csv」)
- [ ] 목표 2: 역방향 `결과.md` 의 **지금 구성으로 재현되지 않는 장기 구간 기록**(§5 ~ §10 약 410줄)을 **현 구성 값으로 다시 쓴다** — 재실행 없이 지금 산출물(`통계.csv`·`test.csv`·`excess.csv`·`signals.csv`)에서 (§10 Q5)
- [ ] 목표 3: 매매 문서 6장의 중복·이력·낡은 수치·문서↔코드 불일치를 걷어낸다 (AUDIT_slim_trading_docs §1 ~ §5, 파일은 유지하고 내용을 옮긴다)
- [ ] 목표 4: 개인 운용 서술을 걷어낸다 (§10 Q1 L2 — AUDIT_slim_trading_docs §6)

## 2) 비목표(Non-Goals)

- **파일 병합** — `research.md`·`trading.md` 가 매매 등급에 설계·결과·규칙 3파일을 요구한다. 내용만 옮긴다
- **매매 규칙 자체** — 진입·청산·손절·**배정액·예비금 비율**은 연구 산출물로 남긴다(§10 Q1 L2 「매매 규칙 자체는 남긴다」). 값을 재선정하지 않는다
- **역방향 산출물의 변경** — 역방향은 다시 쓰는 것이 문서뿐이다. 코드 변경이 역방향 산출물을 바꾸면 안 된다(재실행 대조로 확인)
- **규칙 문서(`.claude/rules/`, `CLAUDE.md` 들)** — ④ 에서 끝났다. 매매 문서의 절을 옮기며 그쪽 포인터가 깨지는 줄만 고친다
- 달력 조사 문서·원달러 문서 — ⑥ ⑦

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- 역방향 `결과.md` 는 머리말 재실행 블록 4개와 **옛 장기 구간(1일 ~ 1년) 표**가 본문 대부분이라 지금 코드(1 ~ 21 거래일 · K=10 · 2005)로 재현되지 않는다. 규칙.md 의 현 구성 통계(§2.4)와 서로 다른 구성을 잰다
- 중간선거는 같은 1배 성적을 `결과.md` §5 ~ §12 와 `규칙.md` §2.1 ~ §2.7 이 두 번 담는다
- 규칙.md 에 개인 집행 절이 있다 — 역방향 §1.5 알림 운영부(판정 시각 · GitHub Actions cron), §4.3 MOC 증권사 · §4.4 세후, 중간선거 §6 「다음 사이클」(진입일 · 사전교육 · 증권사 공지 URL)과 결과 §13 「이번 사이클 적용」, §14.4 포트폴리오
- 중간선거 `측정.csv` 의 어긋남 열이 영문 `False` 로 나간다. `src/verify_lab/CLAUDE.md` 는 `판정가능` 에서 같은 이유(영문 True/False 가 CSV 에 그대로 나간다)로 `예`/`아니오` 를 쓴다. 그 열이 있는 살아 있는 산출물은 중간선거 `측정.csv` 하나다
- 조사 근거: `docs/plans/AUDIT_slim_trading_docs.md` 전체, 보고서 §12.1 「⑤」 행 · §12.2 · §12.3 R1

### 이 계획서에서 확정하는 판단

| # | 판단 | 근거 |
| --- | --- | --- |
| 1 | 역방향 결과 §5 ~ §10 은 **현 구성으로 재작성** — 남길 것: 구간과 무관한 신호 수 · 사건 집중 · 신호일 전체 목록(research.md 요구) · 두 시장 동시 발생. 옛 장기 구간 수치는 머리말 재실행 기록에 「그때 무엇을 쟀고 왜 버렸나」 몇 줄로 | §10 Q5 · AUDIT §2.1 |
| 2 | 역방향 규칙 §2.4 는 재작성된 결과 문서를 가리키는 **요약 3줄 + 포인터**로 | AUDIT §7 |
| 3 | 중간선거 규칙 §2 는 **요약 한 표 + 손절 대조(§2.3) + 레버리지 기록(§2.8 ~ §2.13)** 만 — 나머지 1배 성적은 결과 문서가 SoT | AUDIT §1.2 · §7 |
| 4 | 역방향 §1.5 는 **신호 정의와 신호 가격 산식만** 남기고 알림 운영부(여유 폭 · 판정 시각 · 소비 쪽 · cron)를 지운다 | L2 · AUDIT §6 |
| 5 | 역방향 §4.3(MOC 증권사) · §4.4(세후 — 옛 대상) 삭제, §4.2 세제는 「비용은 요청 시에만 · 세금은 넣지 않는다」 포인터로, §4.5 「사용자 판단 영역」은 **④ 가 고친 `trading.md` 가 요구하는 만큼만** | L2 · §10 Q9 ⑶ |
| 6 | 중간선거 규칙 §6 「다음 사이클」 · 결과 §13 「이번 사이클 적용」 · 결과 §14.4 · 켈리 · 「사용자에게 현재 운용 포트폴리오가 없어」 구절 삭제. 규칙 §1.2 「알고 거는 한계」는 **「한계」** 로 중립화 | L2 |
| 7 | 중간선거 결과의 「우연이든 아니든 성과가 좋으면 투자 대상이라 우연확률을 싣지 않는다」는 **실제 이유(칸당 표본이 하한 미달이라 우연확률이 붙지 않는다)** 가 맞는지 산출물로 확인하고 그 이유로 바꾼다 | L2 · AUDIT §6 |
| 8 | 어긋남 값의 표기는 **`판정가능` 과 같은 소유자·같은 상수**를 쓴다 — 매매법이 각자 변환하지 않는다(원칙 13 은 모든 매매법에 요구되는 축이라 공통 계층이 소유한다 — `src/verify_lab/CLAUDE.md` 「어디까지가 공통이고」). 착수 시 `판정가능` 이 어디서 문자열이 되는지 먼저 보고 같은 방식을 따른다 | §10 「측정.csv」 |

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- `.claude/plan-config.json` — 이 저장소의 검증 명령·자동 포맷·근거 승격 목적지 (**값의 SoT**)
- 루트 `CLAUDE.md` — 특히 「계획서 규약 — 이 프로젝트의 설정」 절 (값이 아니라 **판단 근거**) · 측정의 원칙
- `.claude/rules/research.md` (결과 문서 형식 · 관련 파일 표 · 신호일 목록) · `.claude/rules/trading.md` (규칙 문서 형식) · `.claude/rules/docs.md` (데이터 기간 머리말 · 시제)
- `src/verify_lab/CLAUDE.md` — 「매매 산출물 계약」 · 「`측정.csv`」 · `판정가능` 의 값 규약
- `tests/CLAUDE.md` · `docs/MEMORY.md` 「이름만 바꾸는 변경과 값을 바꾸는 변경을 한 커밋에 섞지 않는다」
- 루트 `SLIM_SERIES.md` 「시리즈 공통 주의」

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [ ] 중간선거 `측정.csv` 의 어긋남 열 값이 `{예, 아니오}` 뿐이고 이를 테스트가 고정한다
- [ ] 재실행 결과 **바뀐 산출물이 중간선거 `측정.csv` 하나**이고, 바뀐 것이 그 열의 값뿐이다
- [ ] 역방향 `결과.md` 의 수치 표가 **지금 산출물에서 스크립트로 뽑은 값**이다 (손으로 옮기지 않았다)
- [ ] 두 매매법 문서의 개인 운용 grep 결과가 0 이거나 의도된 줄뿐이다
- [ ] 두 매매법 문서의 모든 상대 링크·문서 내 앵커가 실재한다 (리뷰 R1 포함)
- [ ] `tests/test_research_docs.py` 통과 (머리말 데이터 기간 · 관련 파일 · 한계 · 재현 방법)
- [ ] 코드 리뷰 실행 및 결과 기록 (마지막 Phase 의 Validation 에 적는다)
- [ ] 품질 검증 통과 (마지막 Phase 의 Validation 에 passed/failed/skipped 수를 적는다)
- [ ] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [ ] 필요한 문서 업데이트 — `docs/COMMANDS.md` 변경 없음(명령 불변) · 두 매매법 문서 · `src/verify_lab/CLAUDE.md`(어긋남 값 규약 한 줄) · `SLIM_SERIES.md`
- [ ] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다 (재작성 사유는 역방향 결과 머리말 재실행 기록에, 어긋남 값 규약은 `src/verify_lab/CLAUDE.md` 에, 중간선거 결과의 재실행 기록에 「어긋남 열 표기 변경 — 값 의미 불변」)
- [ ] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

- `docs/매매/역방향/{설계,결과,규칙}.md` · `docs/매매/중간선거_사이클/{설계,결과,규칙}.md`
- `측정.csv` 어긋남 열을 조립하는 코드(착수 시 확인 — `measure/`·`report/` 의 `판정가능` 소유자 쪽 또는 중간선거 runner) · 그 테스트
- `src/verify_lab/CLAUDE.md` — `측정.csv` 또는 `판정가능` 값 규약 줄
- 규칙 문서 중 **두 매매법 문서의 절 번호를 가리키던 줄**(깨질 때만)
- `docs/INDEX.md` 「찾는 것」 — 가리키는 절 제목이 바뀔 때만
- `storage/results/매매/중간선거_사이클/측정.csv`
- `SLIM_SERIES.md` · 보고서 §12 · 이 계획서
- `docs/COMMANDS.md`: **변경 없음**

### 데이터/결과 영향

- **중간선거 `측정.csv` 의 어긋남 열 값만 바뀐다**(`False` → `아니오`, `True` → `예`). 나머지 산출물은 바이트 동일이어야 한다

## 6) 단계별 계획(Phases)

### Phase 0 — 어긋남 값 규약을 테스트로 먼저 고정 (레드)

**작업 내용**:

- [ ] 착수 전: `git status` 깨끗 · 계획서 ④ 커밋 확인
- [ ] `판정가능` 의 `예`/`아니오` 가 어디서 정의·변환되는지 확인하고(`tests/test_layer_contracts.py` 의 정의처 검사 포함), 어긋남 열도 **같은 상수**를 쓰라는 테스트를 추가한다 — 중간선거 `측정.csv` 를 만드는 경로의 결과가 `{예, 아니오}` 인지 합성 시세로 검사

**Validation**:

- [ ] 새 테스트가 **실패**한다 (지금은 불린이 나간다 — 의도한 레드)

---

### Phase 1 — 어긋남 열 변경과 재실행 (그린)

**작업 내용**:

- [ ] 판단 8 대로 변환을 넣는다. 원칙 13 의 판정 함수(`statistics.mean_rate_conflict`)가 불린을 돌려주는 계약은 바꾸지 않는다 — **표시 변환만** 한다
- [ ] 5개 실행 스크립트를 인자 없이 다시 돈다(명령은 `docs/COMMANDS.md`)

**Validation**:

- [ ] Phase 0 테스트 통과
- [ ] `git diff --stat storage/results/` 가 **중간선거 `측정.csv` 하나**이고, `git diff` 가 그 열의 값만 바꿨음을 보여준다 (다른 열·행 순서 불변)

---

### Phase 2 — 역방향 문서 3장 (그린 유지)

**작업 내용**:

- [ ] `결과.md` 재작성(판단 1): 머리말 재실행 블록 4개를 **재실행 기록 표 1장**(날짜 · 무엇 · 영향)으로. §3.2·§3.3 의 낡은 행 수·실행 규모 삭제. §5 ~ §10 을 현 구성(K=10 · 2005 · 역방향 전체 · 1 ~ 21 거래일)으로 — **표는 스크래치 스크립트로 `storage/results/매매/역방향/*.csv` 에서 뽑아 붙인다.** 머리말 데이터 기간은 산출물 `summary.json` 의 `datasets` 에서. §11 은 ① 이 정리한 상태에서 결론 5·한 줄 요약·§11.3 표의 「QQQ와 다르게 움직이는가」를 중립 서술로(보고서 §12.2), 결론 요약 표의 §11 링크 앵커를 현재 제목에 맞춘다(리뷰 R1). §14.5·§15 축약. 관련 파일 표의 없는 `test_studies_runner.py` 정정(② 의 새 이름으로)
- [ ] `설계.md`: AUDIT §2.2 판정대로 — §8 구현 검증·대조 로그 삭제, 결정 ④⑤⑧⑩⑯ 자리표시를 한 줄로, ⑨ 를 ⑰ 탈락안으로 흡수, §9 「착수 전에 물었던 것」 삭제(**`scripts/data/check_pykrx_etf.py` docstring 이 「§9」를 가리키면 그 주석도 고친다**), §12 삭제, §13 판정표 되살리기의 시작연도 오기 정정. AUDIT §5 의 설계 행(EMA·SMA 병기 · QQQ 시작일 1999-03-10 · `069500_adjusted_max.csv` 「삭제됨」은 사실과 다름 · ⑦ 의 `§9.4` 오인용 · ㉕ 「사용자의 실제 매매」 중립화)
- [ ] `규칙.md`: 판단 2 · 4 · 5, 머리말 changelog 블록 · §2.3 변경 기록 blockquote · 「K=20 시절」 값과 서술 · §5.2·§5.4 의 경과 서술을 현재형 3 ~ 5줄로, 「갭손절 0건」 반복 8곳을 결정 ⑤ 한 곳으로, 「현재 운용 중인 국내 칸」 중립화, `run_strategy(TARGETS, hold_limit=1)` 없는 함수 정정, 절 구성을 `trading.md` 의 §1 ~ §5 틀과 대조해 어긋나면 진행 로그에 적는다(번호를 바꾸면 외부 참조를 함께 고친다)

**Validation**:

- [ ] 역방향 결과의 표 수치를 CSV 와 스크립트로 대조해 불일치 0
- [ ] `grep -n -E '운용 중|포트폴리오|context|알림|cron|MOC|세후|K=20 시절|2008 구간' docs/매매/역방향/*.md` 가 0 이거나 의도된 줄뿐

---

### Phase 3 — 중간선거_사이클 문서 3장 (그린 유지)

**작업 내용**:

- [ ] `결과.md`: 머리말 블록 · §13 · §14.4 · §14.5 개인 서술 · 켈리 · 「이번 사이클 나스닥 −2.11%」 · §16 행 수 삭제, 결론 표의 「QQQ 와 분리되지 않는다 · 현재 포트폴리오와 겹친다」를 중립 사실로(보고서 §12.2), 판단 7, 「질문하신 감각으로」 대화체, 「규칙 확정」 블록(규칙.md 소관) 삭제, §17 설계 재서술은 설계 포인터로(§17.1 은 유지 — 사이클 네 칸 분리의 유일한 기록), 붙은 `~`(보고서 §12.1: 결과 65·79·444) 띄우기, 리뷰 잔여 #3(「손절 설계가 통째로 새로 필요하다」·§15 「분할과 손절을 합치면 결론이 바뀔 수 있다」) 현재 사실로, 재실행 기록에 어긋남 열 표기 변경
- [ ] `규칙.md`: 판단 3 · 6, 리뷰 잔여 #2 — **§4 한계에 「사다리 모양(−3 ~ −10% · 1/8 · 3배 · 절반)은 결과를 본 뒤 정했다」와 범위·추가 매수 배수의 탈락안이 없다는 사실을 적는다**, #4(§3 끝 「상세 근거는 설계 §2」가 새 결정 행에 맞지 않음), 투자금 행(켈리 철회) 삭제 · 등급 행 중립화, §1.2 한계 중립화, 증권사 공지 URL · 거래대금 「호가 차이를 확인하고 산다」 삭제
- [ ] `설계.md`: 덮인 결정 ①②④⑩⑪ 을 ⑫⑬ 탈락안으로 병합(약 45줄 감축), ⑧ 의 10월 진입 값 · §3.4·§3.5 의 옛 값 정리, §3.6(결과 §5.1 과 중복) 포인터로, 「trading.md 는 검증 등급에 적용하지 않는다고 적혀 있다 — 이 결정이 예외」(이미 trading.md 가 조건과 사례를 적는다) 삭제, 붙은 `~`(설계 19), `EXIT_MONTH_OFFSET` 등 AUDIT §5 #16 의 코드 불일치

**Validation**:

- [ ] `grep -n -E '포트폴리오|운용|이번 사이클|다음 사이클|켈리|투자금|알고 건다|사전교육|kbsec|kakaopay' docs/매매/중간선거_사이클/*.md` 가 0 이거나 의도된 줄뿐
- [ ] 두 매매법 문서의 링크·앵커 검사 스크립트 실패 0, 한 줄에 붙은 `~` 둘 이상 0

---

### Phase 4 (마지막) — 문서 정리 및 최종 검증

**작업 내용**

> 🔴 **`/commit` 이 «맨 마지막»인 것은 의도다.** 그 스킬은 「후보 뒤에는 아무것도 덧붙이지 말 것」으로
> 끝나므로 **호출하는 순간 그 턴이 거기서 닫힌다.** 중간에 두면 뒤에 적힌 항목이 그 벽 너머에 남는다 —
> 실제로 두 번 그렇게 샜다(`[실측] 2026-09-14` 후보를 계획서에 안 옮김 · `2026-09-16` 옮기고 체크박스를 안 닫음).
> **체크박스와 상태를 먼저 확정하고, 커밋 후보를 마지막에 만든다.**

- [ ] 필요한 문서 업데이트 (`docs/COMMANDS.md` 변경 없음 확인, `src/verify_lab/CLAUDE.md` 값 규약 줄)
- [ ] 규칙 문서·INDEX 가 두 매매법 문서의 **바뀐 절 번호·제목**을 가리키지 않는지 grep 하고 고친다
- [ ] 보고서 §12 백로그 · `SLIM_SERIES.md` 진행 표와 인계 메모 갱신
- [ ] 자동 포맷 적용 (`poetry run black .`)
- [ ] 변경 기능 및 전체 플로우 최종 검증
- [ ] Validation 절에 `/code-review` 와 품질 검증 **실행 결과**를 적는다
- [ ] DoD 체크리스트 최종 업데이트 및 체크 완료
- [ ] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정
- [ ] 🔴 **마지막에 `/commit` 을 실행하고 그 후보를 «이 계획서» 의 `#### Commit Messages` 절에 옮긴다** —
      `/commit` 은 계획서를 모르고 「후보 뒤에 아무것도 덧붙이지 말 것」으로 끝나므로,
      **대화에만 내면 그 절이 빈 채로 남는다.** 체크박스 갱신이 diff 에 더 들어가지만
      커밋 메시지의 내용을 바꾸지 않는다. **커밋은 사용자가 한다 — 후보만 낸다**

**Validation**:

> 순서를 지킨다 — 리뷰에서 고치면 코드가 바뀌므로 품질 검증이 마지막 관문이어야 한다.
> **버그가 0 인 회차에서 끝낸다.** 2회차에도 버그가 나오면 사용자에게 보고하고 3회차 여부를 묻는다 —
> 「그 외」는 목록만 남기고 고치지 않는다. `/impl-plan` 의 「코드 리뷰」 절이 SoT 다.

- [ ] `/code-review xhigh` **1회차** (발견 \_\_건 — 버그 \_\_ · 그 외 \_\_ · 조치: \_\_)
- [ ] `poetry run python validate_project.py` (passed=\_\_, failed=\_\_, skipped=\_\_)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

> **Done 직전에 `/commit` 을 실행해 이 절을 채운다. 그전에는 비워 둔다.**
> 계획서를 쓰는 시점에는 diff 가 없어 여기 적는 것은 전부 추측이고,
> **추측으로 적은 줄은 그대로 나간다.** 형식·문체 규칙은 `/commit` 이 정한다.

- [ ] (미작성 — `/commit` 을 실행하고 후보 5개를 **이 자리에** 번호 붙은 줄로 옮긴 뒤 이 줄을 지운다)

## 7) 리스크(Risks)

- **재작성한 표에 옮겨 적은 숫자가 틀린다** — 전역 규칙 「숫자는 도구 출력을 그대로 붙여넣는다」. 표는 스크립트가 CSV 에서 만들고, 붙인 뒤 다시 스크립트로 대조한다
- **재작성으로 결론이 바뀐다** — 옛 결론(「QQQ 는 잴 표본이 없다」·「KODEX 는 사건 단위로 간신히」)이 현 구성에서도 성립하는지 값으로 확인하고, 달라지면 **달라졌다고 적는다**(과장 금지 · 원칙 2). 결론이 바뀌면 규칙.md 의 근거 서술도 따라 고친다
- **어긋남 표기 변경이 역방향 산출물까지 바꾼다** — 공통 계층을 고치면 그럴 수 있다. Phase 1 의 diff 대조가 잡는다(역방향에는 그 열이 없다 — 2026-09-26 확인)
- **개인 서술을 지우며 규칙의 한계 서술까지 지운다** — 「알고 거는 한계」의 내용(표본 하한 미달 · 근거 두 해 · 회복하지 못한 해 미측정)은 **한계로 남긴다.** 지우는 것은 「건다」는 개인 결정의 말투다
- **한 세션의 맥락** — 편집 대상 약 4,800줄. 역방향(Phase 2)과 중간선거(Phase 3)를 끊어 진행 로그를 남긴다
- **`VIRTUAL_ENV`** — 모든 도구가 실패하면 `env -u VIRTUAL_ENV`

## 8) 메모(Notes)

- 시리즈의 SoT 는 루트 `SLIM_SERIES.md`. 이 계획서는 ⑤ 이며 **커밋 하나**로 끝난다
- AUDIT_slim_trading_docs 의 줄 번호는 `f55617a` 기준이고, ① 이 역방향 결과 §11 · 규칙 §5.5 · 설계 :684 를 이미 고쳤다

### 진행 로그 (KST)

- 2026-09-26 16:55: 계획서 작성 (Draft). 착수는 `SLIM_SERIES.md` 를 받은 세션이 한다
