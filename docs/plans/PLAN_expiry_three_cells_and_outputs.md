# Implementation Plan: 옵션 만기일 — 확정 3칸 축소와 산출물 3파일 구조

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-21 09:18
**마지막 업데이트**: 2026-09-21 10:05
**관련 범위**: studies/option_expiry, execution, scripts, docs
**관련 문서**: `src/verify_lab/CLAUDE.md`, `.claude/rules/trading.md`, `scripts/CLAUDE.md`, `tests/CLAUDE.md`

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: 옵션 만기일의 기본 실행을 **사용자가 실제로 거는 3칸**으로 좁힌다 — SPY 9월 아래 · DIA 9월 아래 · DIA 12월 위
- [x] 목표 2: 산출물을 **3파일**로 만든다 — `성적표.csv` · `거래내역.csv` · `측정.csv`(신설)
- [x] 목표 3: 성적표에 **`손익분기 대비(%p)`** 컬럼을 더한다 (공유 계층이므로 세 매매법 전부)
- [x] 목표 4: 위 셋이 요구하는 **규칙 문서 개정**을 함께 한다 — 코드와 규칙이 어긋난 채로 두지 않는다

## 2) 비목표(Non-Goals)

- **역방향·월말의 범위를 좁히지 않는다.** 파일 3개 구조는 계약으로 세우되 적용은 그 매매법을 손볼 때 한다 (사용자 확정 2026-09-21). 월말은 검증 등급이라 애초에 확정 칸이 없다
- **손절선 −5%를 다시 고르지 않는다.** 격자 20종도 그대로 둔다 — `.claude/rules/trading.md` 가 요구하는 대조축이고, 3칸이면 성적표가 300행이라 부담이 없다
- **비용(수수료·슬리피지·세금)을 넣지 않는다.** 맨몸 성적 그대로다
- **`offsets.py` 를 지우지 않는다.** `runner.py` 에서는 빠지지만 `scripts/data/check_kodex_distribution.py` 가 여전히 호출한다 (직접 호출 1건 확인)
- **시세를 재수집하지 않는다.** 기간이 바뀌면 이 계획서의 대조가 성립하지 않는다

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

**산출물이 사용자가 쓰지 않는 값으로 채워져 있다.** 기본 실행이 120칸(대상 5 × 만기월 12 × 방향 2)을 내고 성적표가 12,000행 · 거래내역이 59,640행이다. 사용자가 실제로 거는 것은 **3칸**이고, 나머지 8개 측정 표는 열지 않는다.

**사용자 결정** (2026-09-21):

| 결정 | 근거 |
| --- | --- |
| 3·6·9·12월 · 한국장 제외 | 이 매매법의 발단이 **네마녀의날**이다 |
| QQQ 9월 제외 | **배당락 변수를 안고 가고 싶지 않다.** 실측으로 8/26건이 보유 구간에 걸리고 회당 +0.049%p 과대평가 |
| SPY 12월 제외 | 평균 +0.712%로 낮다 |
| 최종 3칸 | SPY 9월 아래 · DIA 9월 아래 · DIA 12월 위 |
| 산출물 3파일 | 성적표 · 거래내역 + **AI 가 참고할 값 한 장** |
| 손절선 −5% | 성과가 아니라 이길 때·질 때 수익률을 보고 적정 수준에서 정했다 |

**규칙과 부딪히는 지점이 하나 있고, 사용자가 규칙을 고치기로 했다.** `.claude/rules/trading.md` 와 `docs/매매/옵션_만기일/규칙.md` §1.1 이 "코드가 칸을 고르지 않는다"를 명시하는데, 이번 변경은 코드가 확정 칸만 낸다. **근거를 규칙 문서에 적는 것이 이 계획서의 필수 산출물이다** — 코드와 규칙이 어긋난 채로 두면 다음 세션이 어느 쪽을 따라야 하는지 판별할 수 없다.

**측정 표를 지우면 원칙 둘이 빈다.** 중앙값(측정의 원칙 4)과 평균-비율 어긋남(원칙 13)을 지금은 `통계.csv` 만 담고 있고 성적표에는 없다. `측정.csv` 가 그 자리를 잇는다.

### 검토로 확인된 사실 (2026-09-21 실측)

손절선 −5% · 시기 `전체` 기준이다. 데이터 기간은 SPY **1993-01-29 ~ 2026-08-25**(8,450 거래일) · DIA **1998-01-20 ~ 2026-08-26**(7,195 거래일)이다.

| 칸 | 신호 | 합계 | 평균 | 승률 | 손익분기 승률 | 승률차 | 손익비 | 질 때 표본 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| SPY 9월 아래 | 33 | +31.07% | +0.942% | 72.73% | 43.10% | +29.63%p | 1.320 | 9 |
| DIA 9월 아래 | 28 | +27.98% | +0.999% | 71.43% | 43.01% | +28.42%p | 1.325 | 8 |
| DIA 12월 위 | 28 | +25.08% | +0.896% | 82.14% | 49.16% | +32.98%p | 1.034 | 5 |

**배당락 실측** (설계 결정 ㊱ 의 차이법 — 같은 날짜로 원본가·수정주가 수익률을 각각 계산해 뺀다):

| 칸 | 걸린 건수 | 평균 왜곡 | 판정 |
| --- | ---: | ---: | --- |
| SPY 9월 아래 | 0 / 32 | +0.0000%p | 안 걸림 |
| DIA 9월 아래 | 0 / 27 | +0.0000%p | 안 걸림 |
| DIA 12월 위 | 0 / 28 | +0.0000%p | 안 걸림 |
| *(제외) QQQ 9월 아래* | *8 / 26* | *+0.0490%p* | *과대평가* |

**확정 3칸은 배당락이 하나도 걸리지 않는다.** 제외한 QQQ 만 걸린다.

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- `.claude/plan-config.json` — 이 저장소의 검증 명령·자동 포맷·근거 승격 목적지 (**값의 SoT**)
- 루트 `CLAUDE.md` — 「측정의 원칙」·「후보 판정 기준」·「계획서 규약」
- `src/verify_lab/CLAUDE.md` — 특히 「계층 간 계약」의 **매매 산출물 계약**과 **실행 요약 계약**
- `.claude/rules/trading.md` — 매매 규칙 계층의 예외 규정과 제약
- `.claude/rules/docs.md` · `.claude/rules/research.md` — 문서 작성 규칙
- `scripts/CLAUDE.md` — CLI 계층 책임과 메타 타입 목록
- `tests/CLAUDE.md` — 테스트 작성 규칙

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [x] 기본 실행이 **확정 3칸**만 낸다 (`summary.json` 의 `rule.cells` 로 확인)
- [x] 옵션 만기일 산출물이 **`성적표.csv` · `거래내역.csv` · `측정.csv` 셋**이다
- [x] 세 매매법 성적표에 **`손익분기 대비(%p)`** 가 `손익분기 승률(%)` 바로 오른쪽에 있다
- [x] **`손익분기 대비` 를 더해도 기존 컬럼 값이 하나도 바뀌지 않는다** (세 매매법 기계 대조)
- [x] 회귀/신규 테스트 추가
- [x] 코드 리뷰 실행 및 결과 기록 — **[일시 비활성 2026-09-17]** 돌리지 않으므로 미리 닫아 둔다
- [x] 품질 검증 통과 (마지막 Phase 의 Validation 에 passed/failed/skipped 수를 적는다)
- [x] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [x] 필요한 문서 업데이트(`docs/COMMANDS.md` / CLAUDE.md / plan 등 — 각각 변경 여부 명시)
- [x] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다
      (결정 근거·실측 수치를 `docs/` 의 살아있는 문서로 이관)
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

**공유 계층 (세 매매법 전부에 걸린다)**

- `src/verify_lab/execution/constants.py` — `DISPLAY_BREAKEVEN_MARGIN` 추가
- `src/verify_lab/execution/periods.py` — 컬럼 산출 (`승률 − 손익분기 승률`)

**옵션 만기일**

- `src/verify_lab/studies/option_expiry/constants.py` — `DATASETS` 축소(SPY·DIA), 확정 칸 목록, `OUTPUT_FILES` 재구성
- `src/verify_lab/studies/option_expiry/runner.py` — 상대 거래일 축·보유 길이 축 산출 제거, `측정.csv` 조립
- `src/verify_lab/studies/option_expiry/__init__.py` — 재노출 정리
- `scripts/run_option_expiry.py` — 표시 조정
- `scripts/data/check_expiry_dividend.py` — 배당락 산출을 runner 가 쓰도록 로직 이동 (중복 구현 금지)

**측정 계층**

- `src/verify_lab/measure/` — 배당락 측정 함수의 자리.
  **판단 기준은 「측정의 원칙에 적혀 있는가」 하나다**(루트 `CLAUDE.md` 「어디까지가 공통인가」).
  배당락은 **원칙 14** 에 있고 `.claude/rules/trading.md` 가 **모든 매매법에 「확정 전 필수 항목」**으로 요구하므로 공통 계층이 소유한다.
  쓰는 곳도 이미 둘이다 — `runner.py`(새로) 와 `scripts/data/check_expiry_dividend.py`(기존).
  🔴 **`check_kodex_distribution.py` 의 계단 탐색 방식은 건드리지 않는다** — 그쪽은 「만기 창 전체」를 보는 다른 질문이고, 임계값이 필요한 방식이라 산식이 다르다. **둘을 억지로 합치지 않는다**

**테스트**

- `tests/test_studies_option_expiry_runner.py` — 산출물 구성 변경 반영
- `tests/test_strategy_output_contract.py` — 새 컬럼 계약
- `tests/test_layer_contracts.py` — `OUTPUT_FILES` 계약·`DISPLAY_*` 생존 검사
- `tests/test_index.py` — 문서 목록 (문서를 지우거나 더하면)
- 신규: 배당락 측정 함수 테스트
- **`tests/test_studies_option_expiry_offsets.py` 는 그대로 둔다** — `offsets.py` 를 남기므로

**`측정.csv` 파일명 상수의 자리**

- **`report/constants.py` 에 새 상수를 둔다.** `STATISTICS_FILENAME`(`통계.csv`)은 **지우지 않는다** — 역방향·월말이 계속 그 이름을 낸다
- 🔴 **두 이름이 공존하는 기간이 생긴다.** 매매 산출물 계약의 「세 매매법이 글자 그대로 같은 이름을 쓴다」와 어긋나는 상태이므로, **그 사실과 해소 조건(나머지 둘을 옮길 때)을 계약 문서에 적는다.** 적지 않으면 다음 세션이 둘 중 어느 쪽이 맞는 이름인지 판별할 수 없다

**문서**

- 루트 `CLAUDE.md` — 「값은 지우지 않았습니다」 표의 목적지 갱신
- `src/verify_lab/CLAUDE.md` — 매매 산출물 계약(3파일 · 새 컬럼 · `측정.csv` 규격)
- `.claude/rules/trading.md` — 「코드가 칸을 고르지 않는다」의 예외와 근거
- `docs/INDEX.md` — 산출물 설명
- `docs/COMMANDS.md` — 실행 결과 설명과 대조 절차 (**변경 있음**)
- `scripts/CLAUDE.md` — 메타 타입 목록의 `option_expiry` 행 (표별 산출 행 수·만기일 요일 분포 설명이 낡는다)
- `docs/매매/옵션_만기일/규칙.md` — §1 확정 3칸, §3 에 QQQ·SPY 12월 제외 근거
- `docs/매매/옵션_만기일/결과.md` — 범위 축소 기록
- `docs/매매/옵션_만기일/설계.md` — 확정된 설계 결정 추가
- `docs/MEMORY.md` — 필요 시

### 데이터/결과 영향

- **성적표 12,000행 → 300행** (3칸 × 손절선 20종 × 시기 5행)
- **거래내역 59,640행 → 약 1,780행** (3칸 × 손절선 20종 × 신호 28~33)
- **측정 표 8개 → `측정.csv` 1개** (3행)
- **세 매매법 성적표에 컬럼 1개 추가** — 역방향·월말도 재실행이 필요하다. **값은 바뀌지 않아야 한다**
- 🔴 **이미 발행된 결과 문서의 수치는 그대로 유효하다** — 시세를 재수집하지 않으므로 같은 칸의 값이 변하지 않는다. 사라지는 것은 **칸의 수**다

## 6) 단계별 계획(Phases)

### Phase 0 — 계약을 테스트로 먼저 고정 (레드)

> 최종 결과(산출물 스키마·판정에 쓰이는 값)가 달라지므로 이 Phase 를 둔다.

**작업 내용**:

- [x] `손익분기 대비(%p)` 가 **세 매매법 성적표에** 있고 `손익분기 승률(%)` 바로 뒤라는 것을 `tests/test_strategy_output_contract.py` 에 고정
- [x] 그 값이 `승률(%) − 손익분기 승률(%)` 와 일치한다는 불변조건 테스트 (표본이 없으면 빈칸)
- [x] 옵션 만기일 `OUTPUT_FILES` 가 **정확히 셋**이라는 테스트
- [x] `측정.csv` 의 축이 **성적표의 `시기 = 전체` · 확정 손절선 행과 1:1** 이라는 테스트
- [x] 배당락 측정 함수의 산식 고정 테스트 (합성 데이터 — 배당락이 구간에 있을 때/없을 때, 방향별 부호)

**Validation**: 레드 허용. 이 시점에는 실패하는 것이 정상이다

---

### Phase 1 — 공유 계층: `손익분기 대비(%p)` 추가 (그린 유지)

> **문맥은 「컬럼 하나, 세 매매법」이다.** 옵션 만기일의 범위 축소와 섞지 않는다 —
> 섞으면 산출물 diff 가 「컬럼이 늘어서 바뀐 것」인지 「칸이 줄어서 바뀐 것」인지 구별되지 않는다
> (`docs/MEMORY.md` 「이름만 바꾸는 변경과 값을 바꾸는 변경을 한 커밋에 섞지 않는다」).

**작업 내용**:

- [x] `execution/constants.py` 에 `DISPLAY_BREAKEVEN_MARGIN: Final = "손익분기 대비(%p)"` 추가
- [x] `execution/periods.py` 에서 `DISPLAY_BREAKEVEN_WIN_RATE` **바로 뒤에** 값을 넣는다
- [x] **표에 실리는 반올림 값끼리 뺀다** — 원값으로 계산하면 `승률 72.73 − 손익분기 43.10 = 29.63` 이 표에서 맞지 않는 행이 생긴다 (매매 산출물 계약의 「판정에 넣는 값은 반올림 뒤」와 같은 이유)
- [x] 표본이 없는 구간은 **빈칸** (`0` 이 아니다 — 「잰 적이 없다」와 「차이가 0」은 다르다)
- [x] 🔴 **재실행 «전»에 현재 산출물 전체를 스크래치패드로 복사한다.** 산출물 폴더는 매매법당 하나이고 재실행이 그 자리를 덮으므로, 사본이 없으면 대조할 대상이 사라진다. **이 사본은 Phase 2 에서도 쓴다** — 확정 3칸의 성적이 축소 전과 같은지 보려면 축소 전 12,000행이 필요하다
- [x] 세 매매법 재실행 (`run_option_expiry.py` · `run_reverse.py` · `run_month_end.py`)
- [x] 🔴 **기존 컬럼 전수 대조** — 새 컬럼을 뺀 나머지가 **값 단위로 같은지** 기계로 확인한다. `summary.json` 은 `row_counts` 의 키가 파일 이름이라 대조에서 뺀다

**Validation**:

- [x] 세 매매법 성적표에서 `손익분기 대비` 를 제외한 전 컬럼이 재실행 전과 일치 (불일치 0건)
- [x] 거래내역은 컬럼이 늘지 않으므로 **전 컬럼 일치** (불일치 0건)
- [x] 새 컬럼 값이 `승률 − 손익분기 승률` 과 전 행 일치

---

### Phase 2 — 옵션 만기일: 확정 3칸 축소와 산출물 3파일 (그린 유지)

**작업 내용**:

- [x] `constants.py` 의 `DATASETS` 를 **SPY · DIA** 로 좁힌다
- [x] `all_cells()` 를 **확정 칸 목록**으로 바꾼다 — 함수 docstring 에 「왜 격자를 내지 않는가」와 근거 문서를 적는다
- [x] 🔴 **빈 목록·알 수 없는 칸을 거부하는 가드를 남긴다.** 지금 `all_cells()` 가 갖고 있는 검사다
- [x] `runner.py` 에서 **상대 거래일 축**(`signals` · `expiries`)과 **보유 길이 축**(`trade_summary` · `trade_excess` · `trade_test`)과 **시기 2분할**(`trade_by_month_halves`) 산출을 뺀다
- [x] `trade_signals` 를 뺀다 — 거래내역이 같은 원자료를 담는다
- [x] `_aggregate_by_month` 가 내던 표를 **`측정.csv`** 로 만든다
  - 축: `종목 · 만기월 · 방향` — 확정 칸만
  - 🔴 **값은 1배 롱 기준 그대로 둔다.** 「아래」 칸이라고 부호를 뒤집지 않는다 — 뒤집으면 `기준선 오른 비율` 이 실제로는 내린 비율을 가리켜 **이름이 거짓이 된다.** `방향` 컬럼은 「어느 쪽으로 거는가」를 **표시만** 하고, 읽는 법은 문서가 적는다
  - 배당락 2컬럼(`배당락 걸린 건수` · `배당락 평균 왜곡(%p)`)을 더한다
- [x] 배당락 산식을 `measure/` 로 옮기고 **`check_expiry_dividend.py` 가 그것을 부르게** 한다 — 두 벌을 두면 조용히 갈라진다 (절대 원칙 5)
- [x] `OUTPUT_FILES` 를 셋으로 정리하고 `STATISTICS_FILENAME` 대신 새 상수를 쓴다
- [x] `__init__.py` 재노출 정리 — 쓰지 않게 된 이름을 남기지 않는다
- [x] `scripts/run_option_expiry.py` 의 화면 표를 새 구성에 맞춘다
- [x] **`summary.json` 의 `measure` 칸을 정리한다** — 상대 거래일 축이 사라지면 `max_offset` · `inside_window_days` 가 가리킬 것이 없다. **`expiry_count` · `expiry_weekdays` 는 남긴다**(만기일 달력은 계속 계산하고 휴장 앞당김 비율은 보고 대상이다)
- [x] 🔴 **내 변경으로 생긴 orphan 만 정리한다** — `_aggregate_month_halves` · `_half_block` · `_matched_length_baseline` · `_record_trade_cell` · `trade_headline` 과 그들만 쓰던 import·상수(`BASELINE_WEEKLY` · `BASELINE_MATCHED_LENGTH` · `PERIOD_FIRST_HALF` 등). **사전에 있던 데드 코드는 지우지 않고 언급만 한다**
- [x] `__init__.py` 재노출과 `DISPLAY_*` 사전에서 **죽은 레이블**을 함께 정리한다 — 사전에 키로 얹혀 있으면 미사용 스캔에 잡히지 않는다 (`src/verify_lab/CLAUDE.md` 의 실물 사례)
- [x] 재실행하고 `summary.json` 의 `rule.cells` 가 3칸인지 확인

**Validation**:

- [x] 성적표 300행 · `측정.csv` 3행 · 거래내역 행 수가 `summary.json` 의 `row_counts` 와 일치
- [x] **확정 3칸의 성적이 이번 변경 전과 같다** — Phase 1 사본에서 같은 칸을 뽑아 전 컬럼 대조 (불일치 0건)
- [x] `측정.csv` 의 3행이 성적표의 `시기 = 전체` 행과 **칸(종목·만기월·방향)이 1:1** 이다.
      🔴 **값을 대조하는 것이 아니다** — 성적표는 방향이 반영된 체결 성적이고 `측정.csv` 는 1배 롱 기준이라 평균의 부호가 다르다
- [x] `check_expiry_dividend.py` 를 돌려 `측정.csv` 의 배당락 두 컬럼과 **같은 값**이 나온다 (산식이 한 벌임을 실측으로 확인)

---

### 마지막 Phase — 문서 정리 및 최종 검증

**작업 내용**

> 🔴 **`/commit` 이 «맨 마지막»인 것은 의도다.** 그 스킬은 「후보 뒤에는 아무것도 덧붙이지 말 것」으로
> 끝나므로 **호출하는 순간 그 턴이 거기서 닫힌다.** 중간에 두면 뒤에 적힌 항목이 그 벽 너머에 남는다 —
> 실제로 두 번 그렇게 샜다(`[실측] 2026-09-14` 후보를 계획서에 안 옮김 · `2026-09-16` 옮기고 체크박스를 안 닫음).
> **체크박스와 상태를 먼저 확정하고, 커밋 후보를 마지막에 만든다.**

- [x] `.claude/rules/trading.md` — **「코드가 칸을 고르지 않는다」의 예외**를 적는다. 무엇이 달라졌고 무엇이 그대로인지(격자는 손절선 축에 남는다), 그리고 **근거가 규칙 문서에 있어야 한다는 요구는 그대로**임을 밝힌다
- [x] `src/verify_lab/CLAUDE.md` — 매매 산출물 계약에 **3파일 구성 · `손익분기 대비` · `측정.csv` 규격**을 넣는다. **적용 범위가 옵션 만기일뿐**이라는 사실과 그 이유를 함께 적는다
- [x] 루트 `CLAUDE.md` — 「값은 지우지 않았습니다」 표에서 기준선·우연확률의 목적지를 갱신
- [x] `docs/매매/옵션_만기일/규칙.md` — §1 을 **3칸**으로, §3 에 「확정 / 탈락안 / 근거」 형식으로 **QQQ 9월 제외**(배당락 8/26건 · +0.049%p)와 **SPY 12월 제외**(평균 +0.712%)를 적는다. 기존 §2 성적표를 3칸 기준으로 갱신
- [x] 🔴 같은 문서 §3.1 에 **「이 손절선 격자는 축소 전 실행(후보 24칸·체결 612건)으로 잰 것이고 지금 구성으로는 재현되지 않는다」**를 적는다. 값 −5% 는 재선정하지 않는다
- [x] 같은 문서에 **확정 3칸이 독립 표본이 아니라는 사실**을 남긴다 — 9월 두 칸은 상관 0.965 로 28건 내내 부호가 갈리지 않았다. 투자금 결정이 이 값을 본다
- [x] `scripts/CLAUDE.md` — 메타 타입 목록의 `option_expiry` 행을 새 산출 구성에 맞춘다
- [x] `docs/매매/옵션_만기일/설계.md` — 확정된 설계 결정에 **범위 축소**와 **산출물 3파일**을 더하고, 배당락 실측 수치를 「데이터 실측 기록」에 넣는다
- [x] `docs/매매/옵션_만기일/결과.md` — 상대 거래일 축 산출물이 사라졌다는 사실과 **되살리는 절차**를 적는다 (git 이력)
- [x] `docs/COMMANDS.md` — 실행 결과 설명과 대조 절차 갱신 (**변경 있음**)
- [x] `docs/INDEX.md` — 산출물 설명 갱신
- [x] 자동 포맷 적용 (`poetry run black .`)
- [x] 변경 기능 및 전체 플로우 최종 검증
- [x] Validation 절에 품질 검증 **실행 결과**를 적는다 — **[일시 비활성 2026-09-17]** `/code-review` 는 빼둔다
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정
- [x] 🔴 **마지막에 `/commit` 을 실행하고 그 후보를 «이 계획서» 의 `#### Commit Messages` 절에 옮긴다**

**Validation**:

> 순서를 지킨다 — 리뷰에서 고치면 코드가 바뀌므로 품질 검증이 마지막 관문이어야 한다.

- [x] `/code-review` — **일시 비활성** (토큰 부족, 2026-09-17)
- [x] `poetry run python validate_project.py` (passed=1274, failed=0, skipped=0)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

> **Done 직전에 `/commit` 을 실행해 이 절을 채운다. 그전에는 비워 둔다.**
> 계획서를 쓰는 시점에는 diff 가 없어 여기 적는 것은 전부 추측이고,
> **추측으로 적은 줄은 그대로 나간다.** 형식·문체 규칙은 `/commit` 이 정한다.

1. 측정 / 옵션 만기일 확정 3칸 축소와 산출물 3파일 통합
2. 측정 / 옵션 만기일 기본 실행 범위 축소와 측정 표 8종의 측정.csv 통합
3. 측정 / 확정 칸 기반 실행 전환과 성적표 손익분기 대비 컬럼 추가
4. 측정 / 사용자가 여는 산출물만 남기는 옵션 만기일 산출 구조 개편
5. 측정 / 옵션 만기일 3칸 확정과 배당락 측정의 공통 계층 이관

## 7) 리스크(Risks)

| 리스크 | 완화책 |
| --- | --- |
| **되돌리기 어렵다** — 기준선·우연확률·상대 거래일 축을 다시 보려면 코드를 되살려야 한다 | 두 축 모두 **이미 판정에서 뺀 것**이라 실질 손실이 작다. 되살리는 절차(git 이력)를 결과 문서에 적는다 |
| **사후 선택처럼 보인다** — 코드가 확정 칸만 내면 「왜 이 칸인가」가 코드에서 사라진다 | 규칙.md §3 에 「확정 / 탈락안 / 근거」로 남긴다. **이것이 이 계획서의 필수 산출물**이며 DoD 에 들어 있다 |
| **세 매매법 산출물이 동시에 바뀐다** (`손익분기 대비`) | Phase 1 에서 **컬럼만** 바꾸고 전 컬럼을 기계 대조한다. 옵션 만기일 범위 축소는 Phase 2 로 분리 |
| **`측정.csv` 의 부호를 오독할 수 있다** — 1배 롱 기준인데 「아래」 칸과 나란히 놓인다 | 부호를 뒤집지 않기로 정하고 **읽는 법을 문서와 컬럼 이름에 남긴다.** 성적표는 방향 반영 값이라 두 표의 평균이 다르다는 사실도 적는다 |
| **QQQ 를 다시 넣고 싶어질 수 있다** | `DATASETS` 를 되돌리면 된다. 제외 근거와 실측 수치가 규칙.md 에 남으므로 판단 재료가 사라지지 않는다 |
| 🔴 **손절선 −5% 의 근거를 «재현»할 수 없게 된다** — 규칙.md §3.1 이 「후보 24칸·체결 612건」으로 잰 격자인데 그 24칸을 더는 낼 수 없다 | **값은 재선정하지 않으므로 결론은 바뀌지 않는다**(`.claude/rules/trading.md` 「근거를 다시 쓰는 것이 값을 다시 고르는 것이 되면 안 된다」). 다만 **「이 격자는 축소 전 실행으로 잰 것이고 지금 구성으로는 재현되지 않는다」를 §3.1 에 명시**한다 — 적지 않으면 다음 사람이 돌려 보고 숫자가 안 맞아 혼란한다 |
| **수정주가 파일에 새 의존성이 생긴다** — `측정.csv` 의 배당락 컬럼 때문에 본 실행이 `SPY_adjusted_max.csv` · `DIA_adjusted_max.csv` 를 읽는다 | 두 파일은 이미 있다. **없을 때 조용히 0 으로 채우지 않고 예외를 던진다** — 「안 걸림」과 「못 쟀다」가 구별되지 않으면 `.claude/rules/trading.md` 가 경고한 그 사고다(「못 잰 것」을 0 으로 세지 않는다) |
| 시세 재수집 시 이 성적이 재현되지 않는다 | 데이터 기간을 규칙.md 머리말에 그대로 둔다 (기존 관용) |

## 8) 메모(Notes)

### 확정된 판단 (착수 전 사용자 승인)

- **최종 3칸**: SPY 9월 아래 · DIA 9월 아래 · DIA 12월 위
- **파일 이름**: `측정.csv` (`참고.csv` 는 무엇을 참고하는지 이름에 없어 탈락)
- **적용 순서**: 옵션 만기일 먼저. 역방향은 그 매매법을 손볼 때
- **손절선**: −5% 확정값 유지. 격자 20종도 유지

### 재검토에서 드러난 것 (규칙 문서로 승격할 것)

- **9월 3칸은 독립 표본이 아니다.** 같은 날 같은 방향이고 상관이 SPY↔DIA **0.965**(28건 내내 부호 안 갈림) · QQQ↔SPY 0.891 · QQQ↔DIA 0.824다. 3칸 균등 배분 시 회당 +1.078% · 승률 74.07%(20/27) · 최악 한 해 −5.07%(2001년). **투자금 결정에서 3칸을 3번의 독립 베팅으로 세면 안 된다**
- **DIA 12월 위의 손익비 1.034는 진 거래 5건으로 만든 값**이다. 한 건만 더 크게 져도 1 아래로 간다
- **QQQ 9월의 손절 1건(2001-09-21)은 배당락 오염 구간(2017~) 밖**이다. 규칙.md §3.2 가 적어 둔 「손절 판정이 오염된다」는 두 번째 근거는 **그 칸에는 해당하지 않았다** — 제외 결정은 사용자가 「변수를 안고 가지 않겠다」로 내린 것이고, 근거를 정확히 적는다
- **`scripts/data/check_expiry_dividend.py` 가 이미 있다.** 배당락을 다시 재는 코드를 새로 쓰지 않고 그 산식을 `measure/` 로 옮겨 공유한다

### 진행 로그 (KST)

- 2026-09-21 09:18: 계획서 작성. 착수 전 사용자 승인 대기
- 2026-09-21 09:30: 승인. Phase 0 — 계약 테스트 4묶음 고정(손익분기 대비 · 파일 셋 · 배당락 산식 · 측정 표 조립). 레드 확인
- 2026-09-21 10:05: 마지막 Phase — 문서 9개 갱신(규칙·설계·결과·INDEX·COMMANDS·trading 규칙·패키지 계약·루트 CLAUDE·scripts). **품질 검증 passed=1274 · failed=0 · skipped=0**, 자동 포맷 적용
- 2026-09-21 09:52: Phase 2 완료 — 확정 3칸 축소와 산출물 3파일. **성적표 12,000 → 300행 · 거래내역 59,640 → 1,780행 · 측정.csv 3행**이고 폴더에 CSV 셋뿐이다. **확정 3칸의 성적이 축소 전과 전 컬럼 일치**(불일치 0칸). 배당락은 세 칸 모두 0건이며 `check_expiry_dividend.py` 와 값이 같아 산식이 한 벌임을 확인했다. 전체 테스트 1,274 통과
- 2026-09-21 09:37: Phase 1 완료 — `손익분기 대비(%p)` 추가 후 세 매매법 재실행. **25개 CSV 전 파일에서 새 컬럼 밖의 값이 하나도 바뀌지 않았다**(불일치 0칸)

---
