# Implementation Plan: 옛 산출물 16개를 계층 폴더로 전부 이관

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-11 20:02
**마지막 업데이트**: 2026-09-11 20:35
**관련 범위**: 산출물 배치, utils, tests, 문서
**관련 문서**: `docs/INDEX.md`, `src/verify_lab/CLAUDE.md`, `tests/CLAUDE.md`, `.claude/rules/docs.md`

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: 루트 직하의 옛 산출물 **16개를 전부** `검증`·`매매`·`실측` 계층 폴더로 옮긴다. 이관 후 루트 직하에 산출물 폴더가 **0개**가 된다
- [x] 목표 2: 폴더 이름의 **옛 slug 6개를 새 slug 로 개명**한다 (`index_extreme` ×4 → `reverse`, `expiry_trading` ×2 → `option_expiry`). 같은 매매법이 계층에 따라 다른 이름으로 불리는 상태를 없앤다
- [x] 목표 3: 옛 폴더를 가리키는 **인용 33건(문서 13개)** 을 전부 새 경로·새 이름으로 고친다
- [x] 목표 4: 「**옮기지 않습니다**」를 선언한 규칙 문서·코드 주석·테스트를 전부 철회하고, **자리가 하나**라는 새 규약으로 바꾼다
- [x] 목표 5: 문서에 적힌 `storage/results/...` 경로의 실재를 **기계가 검사**하게 한다 — 이번 이관의 실패 양식을 기존 가드가 못 잡기 때문이다 (근거는 §3)

## 2) 비목표(Non-Goals)

- **옛 산출물 «파일»의 이름을 바꾸지 않는다.** 옛 `stop_loss_grid.csv` 는 17컬럼, 새 계약의 `성적표.csv` 는 25컬럼이다. 이름만 맞추면 `docs/INDEX.md` 가 경고한 「**다른 것이 같아 보인다**」가 그대로 된다. 파일 이름이 두 벌인 것은 이관 후에도 유지되며, 그 사실을 문서에 남긴다
- **옛 산출물의 «내용»을 재실행·재생성하지 않는다.** 시세를 재수집하면 신호 수와 성적이 달라져 결과 문서의 수치가 재현되지 않는다 (루트 `CLAUDE.md` 「재수집은 이미 나온 결과를 바꿉니다」)
- **묘비 인용을 개명하지 않는다.** `docs/research/연속_등락.md` 의 `20260830_181318_index_extreme`(**없음** 표기)은 디스크에 없다. 그 폴더는 `index_extreme` 이라는 이름으로 존재했다가 지워진 것이므로, `reverse` 로 고치면 **있지도 않았던 이름을 기록으로 남기는 것**이 된다
- **`storage/results/meta.json` 을 손대지 않는다.** git 제외 대상이고 실행마다 최근 5개로 재작성된다. 옛 slug 키(`index_extreme_study`)가 들어 있으나 다음 실행이 자연히 걷어낸다
- **git 조작을 하지 않는다.** 이동은 `mv` 로만 하고 `add`·`commit` 은 사용자가 직접 한다 (루트 `CLAUDE.md` 「Git」)

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

**계층 폴더는 커밋 `ae3620f`(2026-09-11) 에서 도입됐고, 그때는 옛 산출물을 「옮기지 않는다」로 확정했다.**
이유는 하나였다 — 살아있는 문서 여럿이 그 이름을 인용하고 있어 옮기면 전부 깨진다는 것.

사용자가 그 결정을 뒤집기로 했다 (2026-09-11). **깨지는 문서를 함께 고치면 그 이유가 해소되기 때문**이며,
두 자리가 공존하는 한 아래 혼동이 계속 남는다는 판단이다.

- 같은 옵션 만기일 매매법의 측정 산출물이 `20260831_175819_option_expiry`, 매매 산출물이
  `20260831_180611_expiry_trading` 이라 **사용자가 둘을 옛것/새것으로 오해**했다.
  이 혼동은 `src/verify_lab/CLAUDE.md` 「매매법 이름 계약」에 **이미 재발 사례로 기록**돼 있다
  (역방향의 `index_extreme` 대 `reverse_trading`)

### 이번 작업의 함정 — 기존 가드가 이 실패를 못 잡는다

**인용 판정기 `utils/result_citations.py` 의 `RESULT_DIR_NAME` 은 `storage/results/` 접두어를 요구하지 않고
폴더 «이름»만 본다.** 그리고 `result_dir_paths` 는 루트 직하와 계층 폴더 **두 자리를 모두** 훑는다.

그래서 **폴더만 옮기고 문서를 한 글자도 안 고쳐도 `validate_project.py` 는 통과한다.**
인용 33건 중 **29건이 전체 경로로 적혀 있어** 그 순간 전부 죽은 경로가 되는데도 그렇다.

이 저장소는 **같은 사고를 이미 겪었다** — `.claude/rules/session-bootstrap.md` §6 에 결과 문서
8개가 존재하지 않는 산출물 폴더를 가리키게 된 기록이 있고, 그 대응으로 `tests/test_result_citations.py`
를 만들었다. 그 테스트는 **이름**을 보므로 이번 실패 양식(이름은 살아 있고 경로만 죽는 것)에는 눈이 없다.

**그래서 검증 수단을 먼저 만든다** (목표 5). 눈으로 33건을 대조하는 것은 검증이 아니다.

### 확정된 이관 대응표 (16개)

| 옛 자리 | 새 자리 | 개명 |
| --- | --- | --- |
| `20260820_135545_index_extreme` | `검증/20260820_135545_reverse` | ○ |
| `20260820_162628_index_extreme` | `검증/20260820_162628_reverse` | ○ |
| `20260820_162937_index_extreme` | `검증/20260820_162937_reverse` | ○ |
| `20260820_170440_index_extreme` | `검증/20260820_170440_reverse` | ○ |
| `20260824_104655_usdkrw_equivalence` | `검증/20260824_104655_usdkrw_equivalence` | |
| `20260831_175819_option_expiry` | `검증/20260831_175819_option_expiry` | |
| `20260906_080320_leverage_tracking` | `검증/20260906_080320_leverage_tracking` | |
| `20260906_080353_futures_leverage` | `검증/20260906_080353_futures_leverage` | |
| `20260909_181948_month_end` | `검증/20260909_181948_month_end` | |
| `20260909_182235_month_end` | `검증/20260909_182235_month_end` | |
| `20260826_112619_usdkrw_grid` | `매매/20260826_112619_usdkrw_grid` | |
| `20260831_180611_expiry_trading` | `매매/20260831_180611_option_expiry` | ○ |
| `20260906_080410_expiry_trading` | `매매/20260906_080410_option_expiry` | ○ |
| `20260824_095039_ecos_probe` | `실측/20260824_095039_ecos_probe` | |
| `20260824_095514_pykrx_etf_probe` | `실측/20260824_095514_pykrx_etf_probe` | |
| `20260824_095821_pykrx_etf_probe` | `실측/20260824_095821_pykrx_etf_probe` | |

**계층 판정의 근거**

- `usdkrw_grid` → **매매**. `summary.json` 최상위 키가 `"strategy"` 이고 체결 624건·벤치마크·일별 곡선을
  내는 매매 실행이다. `docs/strategy/원달러_그리드.md` 가 그 매매 규칙 문서다
- `*_probe` 3개 → **실측**. `scripts/data/check_*.py` 가 `RESULT_LAYER_PROBE` 로 산출한 것과 같은 성격이다
- 나머지 → **검증**. 측정 산출물이다

**개명 후 이름 충돌 없음**을 확인했다 — 네 `reverse` 는 시각이 전부 다르고,
세 `option_expiry`(`175819` 검증 · `180611`·`080410` 매매)와 기존 `매매/20260911_182130_option_expiry`
도 시각이 전부 다르다.

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- 루트 `CLAUDE.md` — 특히 「계획서 규약 — 이 프로젝트의 설정」 절
- `docs/INDEX.md` §5 「데이터와 산출물」 — 이번에 규약을 바꾸는 자리
- `src/verify_lab/CLAUDE.md` — 「데이터 저장 규칙」, 「매매법 이름 계약」
- `.claude/rules/docs.md` — 문서의 두 종류, 계획서 수명
- `.claude/rules/research.md` — 결과 문서 작성 규칙
- `.claude/rules/session-bootstrap.md` §6 — 인용을 기계가 지키는 이유
- `tests/CLAUDE.md` — 테스트 작성 규칙
- `.claude/skills/clean-results/SKILL.md` — 정리 도구의 두 자리 전제

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [x] 루트 직하의 산출물 폴더가 **0개**다 (계층 폴더 3개와 `meta.json`·`.DS_Store` 만 남는다)
- [x] 16개 폴더가 대응표대로 이관됐고, 파일 개수와 바이트가 이관 전과 같다
- [x] 옛 폴더를 가리키는 인용 33건이 전부 새 경로·새 이름을 가리킨다
- [x] 「옮기지 않습니다」 선언이 문서·코드 주석·테스트에서 전부 철회됐다
- [x] 회귀/신규 테스트 추가 — 문서의 `storage/results/...` 경로 실재 검사
- [x] 코드 리뷰 실행 및 결과 기록 (마지막 Phase 의 Validation 에 적는다)
- [x] 품질 검증 통과 (마지막 Phase 의 Validation 에 passed/failed/skipped 수를 적는다)
- [x] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [x] 필요한 문서 업데이트(`docs/COMMANDS.md` / CLAUDE.md / plan 등 — 각각 변경 여부 명시)
- [x] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다
      (결정 근거·실측 수치를 루트 `CLAUDE.md` 의 프로젝트 설정 절이 정한 목적지로 이관.
      `/impl-plan` 스킬 "근거 승격" 참고)
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

**산출물 이동 (16개 폴더)**

- `storage/results/2026*` 16개 → `storage/results/{검증,매매,실측}/` 아래로

**인용 수정 (33건 · 문서 13개)**

| 문서 | 건수 |
| --- | --- |
| `docs/strategy/투자금_결정.md` | 7 |
| `docs/spec/역방향_설계.md` | 4 |
| `docs/strategy/옵션_만기일_매매_규칙.md` | 3 |
| `docs/spec/원달러_그리드_설계.md` | 3 |
| `docs/research/옵션_만기일.md` | 3 |
| `docs/research/역방향.md` | 3 |
| `docs/research/달러_조달_방식.md` | 3 |
| `docs/research/연속_등락.md` | 2 |
| `docs/strategy/원달러_그리드.md` | 1 |
| `docs/research/월말_진입.md` | 1 |
| `docs/research/원달러_ETF_등가성.md` | 1 |
| `docs/research/선물_대_레버리지_ETF.md` | 1 |
| `docs/research/레버리지_ETF_괴리.md` | 1 |

> 루트 `CLAUDE.md` 의 인용 2건은 **이미 `매매/` 아래의 새 폴더**라 변경 대상이 아니다.

**규약 철회 (5곳)**

- `docs/INDEX.md` §5 — 표 2행, 「산출물이 사는 자리가 둘이고 그것이 의도입니다」 문단,
  「파일 이름도 두 벌이 공존합니다」 절의 옛 폴더 문장, 판정 규칙 표의 「옛 폴더·옛 파일명 그대로 둔다」
- `src/verify_lab/CLAUDE.md` 「데이터 저장 규칙」 표
- `src/verify_lab/utils/result_citations.py` — 모듈 docstring 과 `result_dir_paths` docstring
- `tests/test_result_citations.py` — `test_real_repository_keeps_seeing_the_old_layout`
- `.claude/skills/clean-results/SKILL.md` — 「산출물이 사는 자리가 둘이다」 문단

**테스트·판정 추가**

- `src/verify_lab/utils/result_citations.py` — 경로 실재 판정 함수 (소유자)
- `tests/test_result_citations.py` — 그 함수를 부르는 검사

**변경 «없음»을 확인한 것**

- `scripts/maintenance/clean_results.py` — 계층 폴더와 루트 직하를 이미 둘 다 다룬다
  (`parts[0] in RESULT_LAYERS` 분기). 자리가 하나가 돼도 그대로 동작한다
- `tests/test_research_docs.py` — 「관련 파일」 절의 경로 검사는 범위가 달라 손대지 않는다
- 루트 `CLAUDE.md` — 인용 2건이 이미 `매매/` 아래 새 폴더다
- `docs/COMMANDS.md` — 실행 명령어와 CLI 옵션이 바뀌지 않는다

### 데이터/결과 영향

- **산출물의 내용은 한 바이트도 바뀌지 않는다.** 자리와 이름만 바뀐다
- **결과 문서의 수치도 바뀌지 않는다.** 재실행하지 않으므로 대조 대상이 그대로다
- **git 이력과의 문자열 대조가 끊기는 것이 대가다.** `ae3620f` 이전 커밋과 그 시기 계획서는
  옛 이름(`index_extreme`·`expiry_trading`)으로 적혀 있고, 이관 후 문서는 새 이름을 쓴다.
  git 은 rename 을 자동 추적하므로 **파일 이력은 이어지지만 문자열 grep 은 두 이름을 다 쳐야 한다**

## 6) 단계별 계획(Phases)

### Phase 0 — 검증 수단을 먼저 만든다

> **이 Phase 가 없으면 33건을 눈으로 대조하게 된다.** 기존 가드는 이름만 보므로
> 이관의 실패 양식(경로만 죽는 것)에 눈이 없다 (§3).

**작업 내용**:

- [x] **판정 함수는 `utils/result_citations.py` 에 둔다** (테스트가 아니라). 그 모듈이 이미
      `_is_scannable`(`EXCLUDED_PARTS`)과 `TOMBSTONE_WORDS` 를 갖고 저장소 전체 `.md` 를 훑으므로,
      **새로 복제할 것이 없다.** 모듈의 책임(「문서가 인용한 산출물을 찾는다」)도 그대로다 —
      식별자가 «이름»에서 «경로»로 한 겹 늘어날 뿐이다
- [x] `tests/test_result_citations.py` 에 그 함수를 부르는 검사를 추가한다

> **`tests/test_research_docs.py` 의 `_path_exists` 를 재사용하지 않는다.** 그쪽은 범위가 다르다 —
> `docs/research/*.md` 의 「관련 파일」 절 **안에서만**, 글롭을 포함한 **임의의 저장소 경로**를 본다.
> 이번에 필요한 것은 **저장소 전체 문서**에서 **`storage/results/` 경로만** 보는 것이다.
> 그쪽을 넓히면 연구 문서 구조 검사에 딴 관심사가 섞이고, 새로 복제하면 **판정이 세 벌**이 된다.
> `TOMBSTONE_WORDS` 가 이미 두 곳에 있는 것은 그 모듈 주석이 밝힌 기존 상태이며 이번에 늘리지 않는다.
- [x] **현재 상태(이관 전)에서 그린임을 확인**한다 — 검사기가 33건을 실제로 보고 있다는 증거
- [x] 가짜 루트로 **경로 하나를 죽였을 때 실패하는지** 확인한다 (검사기에 이빨이 있는지)

**Validation**:

- [x] 새 테스트가 이관 전 상태에서 통과한다
- [x] 죽은 경로를 심은 가짜 루트에서 실패한다

---

### Phase 1 — 폴더 16개 이동과 개명 (그린 유지)

**작업 내용**:

- [x] 이관 전 **파일 개수와 총 바이트를 기록**한다 (대조 기준)
- [x] 대응표대로 `mv` 로 16개를 옮긴다. 개명 6개를 함께 적용한다
- [x] 이관 후 파일 개수·총 바이트가 **이관 전과 같은지** 대조한다
- [x] 루트 직하에 산출물 폴더가 0개인지 확인한다
- [x] **Phase 0 테스트가 레드로 바뀌는지** 확인한다 — 실패 목록이 곧 Phase 2 의 수정 대상이다

**Validation**:

- [x] 이관 전후 파일 개수·바이트 일치
- [x] `unreferenced_dirs` 가 새로 뱉는 폴더가 없다 (이름이 바뀐 6개는 Phase 2 에서 인용이 따라온다)
- [x] Phase 0 테스트가 죽은 경로를 **목록으로** 보고한다

---

### Phase 2 — 인용 33건 수정 (그린 복귀)

**작업 내용**:

- [x] Phase 1 이 낸 실패 목록대로 문서 13개의 경로를 고친다
- [x] **경로 없이 이름만 적힌 인용 4건**을 따로 대조한다 — Phase 0 검사기는 경로만 보므로 걸리지 않는다
  - `docs/spec/역방향_설계.md` 의 REF/MID/NEW 표 3건
  - `docs/research/연속_등락.md` 의 5장 표 1건
- [x] 묘비 인용 `20260830_181318_index_extreme` 은 **그대로 둔다** (§2)
- [x] `docs/strategy/옵션_만기일_매매_규칙.md` 의 「옛 폴더·옛 파일명 그대로다」 문장을 고친다 —
      폴더는 옮겨졌고 **파일명만** 옛 것이다

**Validation**:

- [x] Phase 0 테스트 그린 복귀
- [x] `missing_citations` 가 빈 목록
- [x] 저장소 전체 `.md` 에 옛 이름 `index_extreme`·`expiry_trading` 이 남은 곳이 **묘비 1곳뿐**

---

### Phase 3 — 「옮기지 않는다」 선언 철회 (그린 유지)

**작업 내용**:

- [x] `docs/INDEX.md` §5 — 자리가 하나가 됐음을 반영한다. **파일 이름이 두 벌인 것은 유지**되므로
      그 절은 이유만 다시 쓴다 (폴더가 아니라 «계층» 때문에 갈린다)
- [x] `src/verify_lab/CLAUDE.md` 「데이터 저장 규칙」 표에서 옛 자리 행을 걷어낸다
- [x] `src/verify_lab/utils/result_citations.py` 의 두 docstring 을 고친다.
      **`result_dir_paths` 의 루트 직하 탐색은 «남긴다»** — 지우면 실수로 루트에 생긴 폴더가
      `unreferenced_dirs` 에 잡히지 않아 **정리 도구의 시야에서 영원히 사라진다**
- [x] `tests/test_result_citations.py` 의 `test_real_repository_keeps_seeing_the_old_layout` 을
      **「루트 직하에 산출물이 없다」**로 뒤집는다. 현재 단언(`at_root <= found`)은 `at_root` 가
      비면 **무조건 참이라 아무것도 지키지 않는다**
- [x] `.claude/skills/clean-results/SKILL.md` 의 두 자리 전제를 고친다

**Validation**:

- [x] 저장소 전체에서 「옮기지 않습니다」·「자리가 둘」·「루트 직하」 선언이 산출물 맥락에 남지 않는다
- [x] `docs/INDEX.md` 가 `tests/test_index.py` 를 통과한다

---

### 마지막 Phase — 문서 정리 및 최종 검증

**작업 내용**

- [x] 필요한 문서 업데이트 (`docs/COMMANDS.md` **변경 없음**)
- [x] 자동 포맷 적용 — `poetry run black .`
- [x] 변경 기능 및 전체 플로우 최종 검증
- [x] 근거 승격 — 「왜 옮겼나」와 「경로는 기계가 지킨다」를 살아있는 문서로 옮긴다
      (목적지: `docs/INDEX.md` §5 · `.claude/rules/session-bootstrap.md` §6)
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정

**Validation**:

> 순서를 지킨다 — 리뷰에서 고치면 코드가 바뀌므로 품질 검증이 마지막 관문이어야 한다.

- [x] `/code-review xhigh` (발견 14건 · 조치: 10건 수정 · 3건 한계로 문서화 · 1건 사용자 git 조치 안내)
- [x] `poetry run python validate_project.py` (passed=1191, failed=0, skipped=0)

**수정한 10건** — 가장 큰 것은 **다른 PC 에서 반드시 깨지는 두 건**이었다.

| 무엇 | 왜 위험했나 |
| --- | --- |
| `storage/results/meta.json` 에 실재를 요구 | **git 제외 대상**이라 아직 실행한 적 없는 PC 에는 없다. 커밋으로 고칠 수 없는 실패가 된다. 후보 조건을 「산출물 폴더 이름이 든 경로」로 좁혀 해결했고 회귀 테스트를 달았다 |
| 결과 문자열이 OS 구분자를 씀 | Windows 에서 `docs\a.md` 가 되어 테스트가 깨진다. `as_posix()` 로 고정 |
| 접두어 `storage/results/` 하드코딩 | 경로는 `common_constants` 로만 참조하는 규칙 위반. `results_dir.relative_to(root)` 로 유도 |
| 접두어 형태와 맨이름 형태의 기준이 달랐다 | 같은 파일에 두 답이 나왔다. 기준을 하나로 합치고 `results_dir` 이 `root` 아래인지 검증 |
| 계층부터 적은 `검증/<폴더>/x.csv` 를 건너뜀 | 계층 폴더 도입 후 **가장 자연스러운 줄임**인데 검사되지 않았다 |
| 훑기 루프와 묘비 판정이 두 벌 | 「판정식 단일화」 위반. `_scannable_lines`·`_is_tombstone` 으로 추출 |
| 스킬 문서가 안 하는 일을 약속 | `clean_results.py` 는 경로 판정을 하지 않는다. 문서를 사실대로 고침 |
| 픽스처 둘이 같은 `docs/` 를 만듦 | 함께 받는 테스트가 나오면 `FileExistsError` |
| 루트 탐색을 남긴 이유가 테스트와 모순돼 보임 | 두 장치가 **이어서** 동작한다는 것으로 다시 씀 |
| `# Given` 표기 누락 | `tests/CLAUDE.md` §2 |

**한계로 문서화한 3건** — 고치는 것이 더 나쁘거나, 형태상 답이 없다.

- 맨이름 형태는 계층 정보가 없어 **같은 이름의 어느 계층에서든 닿으면 통과**한다.
  검사하지 않는 것보다는 낫다 (`_resolves` 의 [주의])
- 묘비 판정이 줄 단위라 `cited_result_dirs` 보다 눈이 좁다 (`_result_path_candidates` 의 [주의])
- 마크다운 링크·코드블록의 경로는 안 본다. 링크 검사는 `tests/test_research_docs.py` 가
  자기 범위에서 이미 하므로 넓히면 판정이 겹친다

#### Commit Messages (Final candidates) — 5개 중 1개 선택

1. 하네스 / 옛 산출물 16개를 계층 폴더로 옮기고 옛 slug 를 새 이름으로 통일한다
2. 하네스 / 산출물이 사는 자리를 하나로 모으고 인용 33건을 새 경로로 고친다
3. 하네스 / 산출물 이관과 함께 문서 경로의 실재를 기계가 검사하게 한다
4. 문서 / 산출물 계층 이관에 맞춰 인용과 「옮기지 않는다」 규약을 철회한다
5. 하네스 / 두 자리로 갈려 있던 산출물을 계층 폴더 하나로 통합한다

## 7) 리스크(Risks)

| 리스크 | 완화 |
| --- | --- |
| **문서를 안 고쳐도 품질 검증이 통과한다** (§3) | Phase 0 에서 경로 실재 검사를 **먼저** 만들고, Phase 1 직후 **레드가 되는지 확인**해 이빨을 증명한다 |
| **경로 없이 이름만 적힌 인용 4건은 검사기가 못 잡는다** | Phase 2 에서 따로 대조한다. 이름만 적힌 인용은 개명 대상 폴더를 가리키므로 놓치면 죽은 이름이 남는다 |
| **묘비와 산 인용이 같은 옛 이름을 쓴다** | 묘비는 개명하지 않기로 확정했다(§2). Phase 2 검증에서 「옛 이름이 묘비 1곳에만 남는다」로 명시적으로 센다 |
| **git 이 이동을 rename 으로 못 잡으면 diff 가 16폴더 전체 삭제+추가로 보인다** | 내용이 안 바뀌므로 git 의 rename 탐지가 걸린다. 커밋은 사용자가 하므로 diff 모양을 미리 알린다 |
| **다른 PC 에서 pull 할 때 대량 rename 이 온다** | `storage/results/` 는 git 동기화 대상이고 내용이 같아 충돌 없이 적용된다. 다만 그 PC 에 미커밋 변경이 있으면 먼저 정리해야 한다 |
| **이관 중 일부만 옮겨진 채 중단** | Phase 1 에서 파일 개수·총 바이트를 이관 전후로 대조한다 |

## 8) 메모(Notes)

### 확정된 결정과 근거

- **개명한다** (사용자 결정, 2026-09-11) — 「한 매매법 = 한 이름」 규약을 완성하기 위해서다.
  이동만 하면 `검증/…_option_expiry` 와 `매매/…_expiry_trading` 이 남아 **혼동의 원인이 그대로다**
- **파일 이름은 안 바꾼다** — 옛 `stop_loss_grid.csv` 17컬럼 대 새 `성적표.csv` 25컬럼.
  실측으로 확인했다
- **`usdkrw_grid` 는 매매** — `summary.json` 최상위 키가 `"strategy"`, 체결 624건
- **`result_dir_paths` 의 루트 직하 탐색은 남긴다** — 자리가 하나가 돼도 지우지 않는다.
  지우면 실수로 루트에 생긴 폴더가 정리 도구의 시야에서 사라진다

### 진행 로그 (KST)

- 2026-09-11 20:02: 계획서 작성. 16개 폴더·33건 인용·13개 문서를 전수 확인한 뒤 착수 대기
- 2026-09-11 20:10: Phase 0 — 경로 판정 추가. **이관 전 그린**(죽은 경로 0 · 검사기가 본 경로 39건)으로
  검사기가 실제로 보고 있음을 먼저 증명
- 2026-09-11 20:15: Phase 1 — 16개 이관. 파일 128개·113,128,039 바이트가 이관 전과 **정확히 일치**,
  루트 직하 0개. **경로 판정이 24건으로 레드 전환** — 이름 판정은 개명한 6개만 보고 이동만 한 10개는 못 봤다
- 2026-09-11 20:22: Phase 2 — 인용 33건 수정. 세 판정(죽은 경로·없는 이름·인용 안 된 폴더) 모두 0
  ※ 첫 시도에서 zsh 가 변수를 단어 분리하지 않아 sed 13건이 전부 실패했고, 파일은 바뀌지 않았다.
    배열로 바꿔 재실행. 이어 `|` 를 sed 구분자와 정규식 교대에 겹쳐 써 두 번째 실패 — 구분자를 `#` 로 교체
- 2026-09-11 20:28: Phase 3 — 「옮기지 않는다」 5곳 철회
- 2026-09-11 20:33: 코드 리뷰 14건 → 10건 수정. **`meta.json` 은 git 제외라 다른 PC 에서 반드시 깨졌을 것**
- 2026-09-11 20:35: 품질 검증 통과 (1191/0/0). Done

---
