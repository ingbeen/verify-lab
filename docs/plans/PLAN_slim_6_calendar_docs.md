# Implementation Plan: 경량화 ⑥ — 달력 조사 문서 9장 → 매매법당 한 장 (옵션_만기일 · 월말_진입 · 만기_말일)

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: 🟡 Draft

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-26 16:55
**마지막 업데이트**: 2026-09-26 16:55
**관련 범위**: `docs/조사/{옵션_만기일,월말_진입,만기_말일}/` 9장, `storage/results/조사/{옵션_만기일,월말_진입,만기_말일}/`, 그 문서들을 가리키는 코드 주석·테스트 주석·규칙 문서·INDEX·COMMANDS 포인터
**관련 문서**: `.claude/rules/research.md` (한 장형), `.claude/rules/docs.md`, 루트 `SLIM_SERIES.md`, `docs/plans/AUDIT_slim_calendar_docs.md`, `docs/plans/REPORT_lightweight_inventory.md` §3.6-C · §10 · §12

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [ ] 목표 1: 세 매매법 밖에서 쓰이는 지식(AUDIT_slim_calendar_docs §2b P1 ~ P8)을 **먼저** 살아있는 자리로 옮기고 포인터를 바꾼다
- [ ] 목표 2: 9장(5,961줄)을 `docs/조사/{옵션_만기일,월말_진입,만기_말일}.md` 한 장씩으로 줄인다 (목표 약 1,450 ~ 1,750줄 — AUDIT §8 (b))
- [ ] 목표 3: 세 벌로 갈린 **복원 절차를 한 벌**(「SHA + 경로 한정 checkout」)로 만들고, 따라 하면 실제로 파일이 나오게 한다
- [ ] 목표 4: 코드가 더 이상 만들지 않는 산출물 폴더 3개를 지운다 (§10 Q7)
- [ ] 목표 5: 문서끼리의 모순 18건(AUDIT §5b)과 개인 운용 서술(§6)을 걷어내고, §10 의 두 사실 결정(Q9 ⑴ ⑵)을 반영한다

## 2) 비목표(Non-Goals)

- **코드를 되살리지 않는다** — 복원 절차를 «검증»할 때도 checkout 은 스크래치의 임시 worktree 가 아니라 `git show <SHA>:<경로>` 로 파일 존재만 확인한다 (작업 트리를 건드리지 않는다)
- **새 측정을 하지 않는다** — 수치는 옛 문서의 것을 그대로 옮긴다. 산출물 폴더를 지우므로 원자료 포인터는 git 이력으로 바꾼다
- **레지스트리 slug 3줄**(`tracks.py`)은 그대로 둔다 — `tests/test_tracks.py` 가 고정한다
- 원달러 문서 · 연속_등락 · 괴리 · 선물 — ⑦. 규칙 문서의 내용 정리 — ④ 에서 끝났다(포인터가 깨지는 줄만 고친다)

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- 세 매매법은 코드가 지워졌고 걸지 않기로 했다(옵션_만기일·월말_진입은 2026-09-22 만기_말일에 흡수, 만기_말일은 2026-09-26 강등 · 커밋 `9126c54`). 문서 9장이 **지워진 코드의 산출물·CLI 를 현재형으로 설명하고**(`--stop-grid`·`run_option_expiry.py`), 재실행 기록·개편 이력이 쌓였고, 같은 실측(SPY↔DIA 0.965 · 코스피↔코스닥 0.8919 · 2배 실물 2.20배 · 배당락 · 분배락 · KRX 수정주가 창)이 세 문서에 반복된다
- 🔴 **복원 절차가 틀렸다** — 옵션·월말의 「`git log -- studies/<slug>` 로 마지막 커밋」은 삭제 커밋 `0347b58` 을 집어 파일이 없다. 실제로 파일이 있는 자리는 **옵션·월말 `577589e`(= `0347b58~1`, 두 패키지 파일 9개 확인 2026-09-26) · 만기_말일 `5022dc9`(= `9126c54~1`)**. 만기_말일 목록은 전용 테스트 `tests/test_studies_expiry_monthend_runner.py` 와 `tests/test_report_writer.py` 를 빠뜨렸다(리뷰 R6)
- **밖에서 이 문서들의 절 번호를 가리키는 곳이 25곳**(AUDIT §2a) — 코드·테스트 주석 9곳 포함, 테스트가 잡지 못한다
- 조사 근거: `docs/plans/AUDIT_slim_calendar_docs.md` 전체, 보고서 §12.2 · §12.3 R4 · R6

### 이 계획서에서 확정하는 판단

| # | 판단 | 근거 |
| --- | --- | --- |
| 1 | 형태는 **매매법당 한 장**(`docs/조사/<이름>.md`) — `research.md`·`docs.md` 의 한 장형 규정 그대로, `원달러_조달.md` 선례 | §10 Q4 |
| 2 | **월말 9월은 사전에 전해 들은 가설**이다 — 「결과를 듣고 온 정보라 사후 선택과 구별되지 않는다」는 서술을 고친다. **12월에 관한 서술은 건드리지 않는다** | §10 Q9 ⑴ |
| 3 | 만기_말일을 내린 기준은 **승률 75% 초과 — 사용자가 고려 중이던 기준이며 게이트(`screening.py`)에는 넣지 않았다**. 확정 칸 승률(SPY 66.67 · DIA 67.86 · KODEX 코스닥150 70.0%)이 못 미쳤다 | §10 Q9 ⑵ |
| 4 | 복원 절차는 **SHA + 경로 한정 checkout** 한 벌 — revert 경로는 문서 재편 뒤 충돌하므로 버린다. 복원 뒤 `validate_project.py` 로 확인한다는 문장을 넣는다(복원 지점 뒤에도 `trade_fill.py`·`measure/constants.py`·`report/constants.py`·`calendar_entry.py` 가 바뀌었다) | AUDIT §3 |
| 5 | 산출물 폴더 3개를 지운다. 한 장 문서의 원자료 포인터는 **「`git show <이 계획서 착수 시점 HEAD>:storage/results/조사/<이름>/<파일>`」** 로 바꾼다 | §10 Q7 |
| 6 | 원칙의 실물 사례로 루트 `CLAUDE.md`·`trading.md` 가 인용하는 **원자료 표**(SPY 3월 · DIA 6월 분할 · 220칸 · 96칸 중 3칸 · 5칸 −8.61 / 3칸 +3.86 · 월말 4월 분배락 · 12월 46년)는 한 장에도 남긴다 | AUDIT §2b P8 |
| 7 | 「9월 마지막 거래일에 진입하는 중간선거_사이클은 별개」는 **중립 한 줄**로 만기_말일 한 장에만 남긴다(연구 사실 — 두 매매법이 같은 날 진입한다) | L2 |
| 8 | 「사용자 결정 / 사용자 확정」 표기는 **결정 출처 날짜**로 남긴다(`docs.md` 가 허용) | AUDIT §6 |

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- `.claude/plan-config.json` — 이 저장소의 검증 명령·자동 포맷·근거 승격 목적지 (**값의 SoT**)
- 루트 `CLAUDE.md` — 특히 「계획서 규약 — 이 프로젝트의 설정」 절 (값이 아니라 **판단 근거**)
- `.claude/rules/research.md` — **한 장형도 필수 구조를 폴더형과 똑같이 지킨다**, 관련 파일 표의 묘비 낱말(`없음`·`삭제`·`제거`)
- `.claude/rules/docs.md` · `.claude/rules/trading.md`
- `tests/test_research_docs.py` 가 `docs/조사/*.md` 를 검사한다 — 머리말 「데이터 기간」 · `## 관련 파일` · `## N. 한계` · `## N. 재현 방법` · 관련 파일의 링크·경로 실재 · 계획서 참조 금지
- 루트 `SLIM_SERIES.md` 「시리즈 공통 주의」

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [ ] P1 ~ P5 가 살아있는 자리에 있고, 옛 문서 절을 가리키던 25곳(AUDIT §2a)이 새 자리를 가리킨다
- [ ] 한 장 3개가 있고 폴더 3개(9장)가 없다. INDEX 가 한 장 3개를 링크한다
- [ ] 복원 절차 세 벌이 한 벌이 됐고, 적힌 SHA·경로마다 `git show <SHA>:<경로>` 가 파일을 낸다 (스크립트로 전수)
- [ ] 산출물 폴더 3개가 없다
- [ ] 판단 2 · 3 이 한 장 문서에 결정 출처 날짜와 함께 있다
- [ ] 모순 18건 처리 표와 개인 운용 grep 결과가 진행 로그에 있다
- [ ] `grep -rn -E '조사/(옵션_만기일|월말_진입|만기_말일)/' --exclude-dir={.git,.venv,plans} .` 가 0
- [ ] 코드 리뷰 실행 및 결과 기록 (마지막 Phase 의 Validation 에 적는다)
- [ ] 품질 검증 통과 (마지막 Phase 의 Validation 에 passed/failed/skipped 수를 적는다)
- [ ] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [ ] 필요한 문서 업데이트 — `docs/COMMANDS.md` **포인터만 변경**(지수 수집 근거 · 「코드가 없는 트랙」 절의 문서 경로 — 절 자체의 정리는 ⑧) · INDEX · 규칙 문서 포인터 · `SLIM_SERIES.md`
- [ ] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다 (P1 ~ P5 이관이 곧 승격이다. 판단 1 ~ 8 은 한 장 문서 본문에)
- [ ] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

- 삭제: `docs/조사/{옵션_만기일,월말_진입,만기_말일}/{설계,결과,규칙}.md` 9장, `storage/results/조사/{옵션_만기일,월말_진입,만기_말일}/` (각 4파일)
- 신설: `docs/조사/옵션_만기일.md` · `docs/조사/월말_진입.md` · `docs/조사/만기_말일.md`
- 지식 이관: `src/verify_lab/data/pykrx_collector.py`(`collect_pykrx_index` docstring — 1001·1028 소급 구간 건수, 반환 컬럼 호환) · `src/verify_lab/measure/distribution.py` docstring · `src/verify_lab/CLAUDE.md` 「베이스라인·통계 계약」(순열 검정 시드 계약) · `docs/매매/역방향/설계.md` 데이터 실측 기록(수정주가 창이 조회 시점마다 미끄러진다)
- 포인터: `src/verify_lab/common_constants.py` · `execution/constants.py` · `tracks.py`(주석) · `tests/test_pykrx_collector.py`(주석) · `src/verify_lab/CLAUDE.md` · `.claude/rules/{trading,docs,research}.md` · `scripts/CLAUDE.md` · 루트 `CLAUDE.md`(원칙의 실물 사례 출처) · `docs/INDEX.md` · `docs/COMMANDS.md`(경로만)
- `SLIM_SERIES.md` · 보고서 §12 · 이 계획서

### 데이터/결과 영향

- 산출물 폴더 3개 삭제(코드가 만들지 않는 폴더라 재실행과 무관). 살아 있는 산출물은 바뀌지 않는다

## 6) 단계별 계획(Phases)

### Phase 1 — 밖에서 쓰이는 지식을 먼저 옮긴다 (그린 유지)

**작업 내용**:

- [ ] 착수 전: `git status` 깨끗 · 계획서 ⑤ 커밋 확인 · **착수 시점 HEAD SHA 를 진행 로그에**(판단 5 의 원자료 포인터에 쓴다)
- [ ] AUDIT §2a 의 25곳을 grep 으로 다시 찾는다(`조사/옵션_만기일/` · `조사/월말_진입/` · `조사/만기_말일/` · `§7.4` · `§7.6` · `결정 ㊱` · `결정 ㊵`·`㊶`·`㊷` 등). ③ ④ ⑤ 가 이미 바꾼 줄이 있을 수 있다
- [ ] P1: 한국 지수 소급 구간 OHLC=0(1001: 4,345건 · 1028: 1,471건 · 2203: 1,369건 — **옛 문서 월설 §7.6·§7.11 의 표에서 그대로 옮긴다**)과 종가 계열로 저장하는 이유를 `collect_pykrx_index` docstring 에(2203 은 이미 있는지 확인). 포인터 `pykrx_collector.py`·`common_constants.py`·`tests/test_pykrx_collector.py`·`execution/constants.py`·`src/verify_lab/CLAUDE.md`·`trading.md`·COMMANDS 를 **그 docstring 또는 `src/verify_lab/CLAUDE.md` 「단일 값 시계열 계층 계약」의 「지수의 저장 형태」 행**으로. `src/verify_lab/CLAUDE.md` 가 이미 자립적으로 갖는 부분은 포인터만 바꾼다
- [ ] P2: pykrx 지수 반환 컬럼이 `KRX_COLUMN_MAP` 과 호환된다는 실측 → 같은 docstring
- [ ] P3: 「KRX 수정주가 창이 조회 시점마다 미끄러진다(2026-08-12 → 2014-05-22, 2026-08-27 → 2014-06-09)」 → `docs/매매/역방향/설계.md` 의 pykrx 결론(최근 3,000 거래일) 옆
- [ ] P4: 배당락 차이법 + 교차검증(계단 89회 = 배당 89건) → `measure/distribution.py` docstring. 포인터(`결정 ㊱`) 교체
- [ ] P5: **순열 검정이 호출마다 시드를 새로 잡아 대상을 추가해도 기존 값이 안 바뀐다** — 지금 어디에도 계약으로 적혀 있지 않다. `permutation_test` 의 실제 동작을 코드로 확인한 뒤 `src/verify_lab/CLAUDE.md` 「베이스라인·통계 계약」 표에 한 행으로
- [ ] `execution/constants.py` 의 「(결정 ㊵)」·「(결정 ㊷)」·「(결정 ㊶)」: 그 결정의 내용을 주석 안에 한 줄로 자립시키고 번호를 뗀다

**Validation**:

- [ ] 이관한 사실마다 옛 문서의 원문 줄과 새 자리를 나란히 진행 로그에 (수치는 복사한 그대로)
- [ ] `poetry run pytest tests/test_pykrx_collector.py tests/test_measure_distribution.py -q` 통과 (주석만 바뀌었으므로 그대로 통과해야 한다)

---

### Phase 2 — 한 장 3개를 쓴다 (그린 유지)

**작업 내용**:

- [ ] 한 장마다 `research.md` 필수 구조: 머리말(작성일 · **데이터 기간** · 「걸지 않기로 한 규칙의 기록」 · 코드 없음) → `## 관련 파일`(삭제된 코드 행은 묘비 낱말 `삭제` 와 복원 SHA) → 목차 → 결론 요약 → 문제 정의 → 대상과 근거 소스 → 용어 → 측정 결과 → **걸지 않기로 한 규칙과 그 근거**(옛 `규칙.md` §1 · §3 의 결정 / 탈락안 / 근거를 표로) → 해석 → 기각된 가설 → 한계 → 재현 방법(판단 4 의 복원 절차)
- [ ] 무엇을 남기고 무엇을 지울지는 AUDIT §1(중복 A ~ O 의 SoT) · §4(코드 삭제 뒤 의미를 잃은 현재형 서술 — 전부 삭제) · §5a(중요하지 않은 수치 — 삭제) 를 따른다. 공통 한계(M)와 공통 사실(A·B·E·G·H)은 **한 장에만** 두고 나머지는 그 한 장을 가리킨다 — 만기_말일 한 장이 허브다
- [ ] 판단 2 · 3 · 6 · 7 · 8 반영. 개인 운용 서술(AUDIT §6 — 2 ~ 3배 · 알림 없음 · 2026 실행 일정 · 자금 조달 · 원금 1억·켈리 · 새벽 5시 MOC · 세전·양도세 개인 사정 · 「투자금을 정할 때」)을 지우거나 중립 서술로. 과제 B 서술(보고서 §12.2 — 옵션 설계 머리 · 결정 ① · 결과의 과제 B 절과 「과제 B 후보」·「부수적 매매법」 표, 월말 설계 과제 표, 월말 결과 「QQQ와의 관계 (과제 B)」)을 걷는다 — 루트 `CLAUDE.md` 의 「과제는 둘이다」 절 인용(리뷰 R4)도 함께
- [ ] AUDIT §5b 모순 18건을 한 장을 쓰며 해소한다(값이 둘이면 **어느 쪽이 산출물·원문 표에 맞는지** 확인해 하나로, 확인할 수 없으면 「확인 불가」로 적는다 — 코드가 없어 재측정할 수 없다)
- [ ] 옛 9장을 지운다(이름으로)

**Validation**:

- [ ] `poetry run pytest tests/test_research_docs.py -q` 통과 (새 한 장 3개가 검사 대상이다)
- [ ] 세 한 장의 줄 수를 진행 로그에 (목표 합 약 1,450 ~ 1,750)
- [ ] `grep -n -E '2~3배|직접 챙김|원금 1억|켈리|새벽 5시|투자금|과제 B|부수적 매매법|--stop-grid|run_option_expiry|run_month_end' docs/조사/{옵션_만기일,월말_진입,만기_말일}.md` 가 0 이거나 의도된 줄뿐

---

### Phase 3 — 복원 절차 검증 · 산출물 폴더 삭제 · 포인터 정리 (그린 유지)

**작업 내용**:

- [ ] 한 장에 적은 복원 SHA·경로를 스크래치 스크립트로 전수 대조 — 경로마다 `git show <SHA>:<경로>` 가 성공해야 한다. 만기_말일 목록에 `tests/test_studies_expiry_monthend_runner.py`·`tests/test_report_writer.py` 포함(리뷰 R6)
- [ ] `storage/results/조사/{옵션_만기일,월말_진입,만기_말일}/` 를 지운다(이름으로). 한 장의 원자료 포인터를 판단 5 로
- [ ] INDEX: 세 행의 문서 링크를 한 장으로, 「찾는 것」의 「9월 만기일·월말에 다시 걸려 할 때」를 만기_말일 한 장의 해당 절 제목으로
- [ ] 규칙 문서·`scripts/CLAUDE.md`·`tracks.py` 주석·COMMANDS 의 옛 폴더 경로를 한 장으로(COMMANDS 는 경로만 — 「코드가 없는 트랙」 절 자체는 ⑧)
- [ ] `docs.md`·`research.md` 가 「한 장짜리의 예」로 드는 문서가 여전히 맞는지 확인

**Validation**:

- [ ] DoD 의 옛 경로 grep 이 0
- [ ] `poetry run pytest tests/test_index.py tests/test_research_docs.py tests/test_tracks.py -q` 통과
- [ ] 링크·앵커 검사 스크립트 실패 0 (편집한 모든 `.md`)

---

### Phase 4 (마지막) — 문서 정리 및 최종 검증

**작업 내용**

> 🔴 **`/commit` 이 «맨 마지막»인 것은 의도다.** 그 스킬은 「후보 뒤에는 아무것도 덧붙이지 말 것」으로
> 끝나므로 **호출하는 순간 그 턴이 거기서 닫힌다.** 중간에 두면 뒤에 적힌 항목이 그 벽 너머에 남는다 —
> 실제로 두 번 그렇게 샜다(`[실측] 2026-09-14` 후보를 계획서에 안 옮김 · `2026-09-16` 옮기고 체크박스를 안 닫음).
> **체크박스와 상태를 먼저 확정하고, 커밋 후보를 마지막에 만든다.**

- [ ] 필요한 문서 업데이트 (`docs/COMMANDS.md` 는 경로 포인터만 바뀌었는지 확인)
- [ ] 5개 실행 스크립트 중 **달력 지식을 옮긴 코드가 닿는 것**(docstring·주석뿐이라 값은 불변이어야 한다)을 다시 돌려 `git diff --stat storage/results/` 가 지운 폴더 3개뿐인지 확인
- [ ] 보고서 §12 백로그 · `SLIM_SERIES.md` 진행 표와 인계 메모 갱신
- [ ] 자동 포맷 적용 (`poetry run black .`)
- [ ] 변경 기능 및 전체 플로우 최종 검증
- [ ] Validation 절에 `/code-review` 와 품질 검증 **실행 결과**를 적는다
- [ ] DoD 체크리스트 최종 업데이트 및 체크 완료
- [ ] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정
- [ ] 🔴 **마지막에 `/commit` 을 실행하고 그 후보를 «이 계획서» 의 `#### Commit Messages` 절에 옮긴다** —
      `/commit` 은 계획서를 모르고 「후보 뒤에 아무것도 덧붙이지 말 것」으로 끝나므로,
      **대화에만 내면 그 절이 빈 채로 남는다.** 체크박스 갱신이 diff 에 더 들어가지만
      커밋 메시지의 내용을 바꾸지 않는다. **커밋은 사용자가 한다 — 후보만 낸다**

**Validation**:

> 순서를 지킨다 — 리뷰에서 고치면 코드가 바뀌므로 품질 검증이 마지막 관문이어야 한다.
> **버그가 0 인 회차에서 끝낸다.** 2회차에도 버그가 나오면 사용자에게 보고하고 3회차 여부를 묻는다 —
> 「그 외」는 목록만 남기고 고치지 않는다. `/impl-plan` 의 「코드 리뷰」 절이 SoT 다.

- [ ] `/code-review xhigh` **1회차** (발견 \_\_건 — 버그 \_\_ · 그 외 \_\_ · 조치: \_\_)
- [ ] `poetry run python validate_project.py` (passed=\_\_, failed=\_\_, skipped=\_\_)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

> **Done 직전에 `/commit` 을 실행해 이 절을 채운다. 그전에는 비워 둔다.**
> 계획서를 쓰는 시점에는 diff 가 없어 여기 적는 것은 전부 추측이고,
> **추측으로 적은 줄은 그대로 나간다.** 형식·문체 규칙은 `/commit` 이 정한다.

- [ ] (미작성 — `/commit` 을 실행하고 후보 5개를 **이 자리에** 번호 붙은 줄로 옮긴 뒤 이 줄을 지운다)

## 7) 리스크(Risks)

- **옮기기 전에 지워 지식을 잃는다** — Phase 1(이관)을 끝내고 Validation 을 통과한 뒤에만 Phase 2 에서 옛 문서를 지운다
- **수치를 다시 타이핑하다 틀린다** — 옛 문서의 표를 **복사**한다. 코드가 없어 다시 잴 수 없으므로 옛 문서가 유일한 근거물이다(모순 18건은 판정 불가면 「확인 불가」로 남긴다)
- **테스트가 못 잡는 죽은 포인터** — 코드·테스트 주석의 문서 포인터는 grep 으로만 잡힌다. DoD 의 경로 grep 과 절 번호 grep(`§7\.`·`결정 ㊱` 등)을 함께 돈다
- **한 장이 research.md 필수 구조를 못 맞춘다** — `test_research_docs.py` 가 잡는다. 「## N. 한계」·「## N. 재현 방법」 제목 형식을 지킨다
- **한 세션의 맥락** — 읽을 것 약 6,000줄. 매매법 하나씩(만기_말일 → 옵션_만기일 → 월말_진입) 써서 진행 로그를 남긴다
- **`VIRTUAL_ENV`** — 모든 도구가 실패하면 `env -u VIRTUAL_ENV`

## 8) 메모(Notes)

- 시리즈의 SoT 는 루트 `SLIM_SERIES.md`. 이 계획서는 ⑥ 이며 **커밋 하나**로 끝난다
- 복원 SHA 는 작성 시점(2026-09-26)에 `git ls-tree -r --name-only 577589e` 로 두 패키지 파일 9개를 확인했다
- 디스크에 git 미추적 `__pycache__` 만 든 옛 폴더(`src/verify_lab/studies/{month_end,option_expiry}/`, `src/verify_lab/strategy/`)가 있다 — git 과 무관하며 지워도 된다(로컬 정리)

### 진행 로그 (KST)

- 2026-09-26 16:55: 계획서 작성 (Draft). 착수는 `SLIM_SERIES.md` 를 받은 세션이 한다
