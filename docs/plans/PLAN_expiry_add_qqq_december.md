# Implementation Plan: QQQ 12월을 옵션 만기일 매매의 대상 칸에 추가한다

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-02 (KST)
**마지막 업데이트**: 2026-09-02
**관련 범위**: `src/verify_lab/strategy/constants.py` · `scripts/strategy/` · `scripts/data/` · `docs/research/` · `docs/spec/` · `docs/COMMANDS.md`
**관련 문서**: `CLAUDE.md`(루트), `.claude/rules/strategy.md`, `.claude/rules/docs.md`, `docs/research/옵션_만기일.md`, `docs/spec/option_expiry.md`

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: **`EXPIRY_CELLS` 에 QQQ 12월(위)을 추가**해 `stop_loss_grid.csv` 와 `trades.csv` 에
      그 칸의 격자와 체결이 실린다. 지금은 7칸뿐이라 사용자가 CSV 에서 QQQ 12월을 볼 수 없다
- [x] 목표 2: **사용자가 같은 산출물 안에서 12월 세 종목을 나란히 판단**할 수 있게 한다 —
      DIA · SPY · QQQ

## 2) 비목표(Non-Goals)

- **판정 기준을 바꾸지 않는다.** QQQ 12월은 등급 0/3 그대로이며, `measure/screening.py` 를 손대지 않는다
- **채택 여부를 코드로 확정하지 않는다.** 대상 칸에 넣는 것은 **산출물에 싣는다**는 뜻이지
  매매 규칙으로 확정한다는 뜻이 아니다. 규칙은 `docs/strategy/` 가 정하며 아직 쓰지 않았다
- **12B장의 7칸 집계표를 고쳐 쓰지 않는다.** 그 표는 손절선 −5% 를 고른 근거이고 7칸 실행의
  기록이다. 재실행하지 않은 값을 8칸 기준으로 갈아 적으면 문서가 거짓말을 한다
  (`.claude/rules/docs.md`). **새 절을 날짜와 함께 덧붙인다**
- **DIA 6월을 빼지 않는다.** 채택 범위는 사용자 판단이고, 산출물은 전 칸을 낸다
- **시세를 재수집하지 않는다.**

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- 사용자가 **QQQ 12월을 뺀 것이 과최적화 아닌가**를 지적했고, 그 지적이 맞다.
  루트 `CLAUDE.md` 「후보 판정 기준」은 **"등급은 얼마나 믿을 만한지 알려주되 떨어뜨리지 않는다"**
  로 되어 있는데, QQQ 12월을 **등급 0/3 을 이유로** 제외한 것은 등급을 게이트로 쓴 것이다
- QQQ 12월은 **1차 게이트를 통과한 「후보」**다 — 적중률 62.96%(≥60%) · 방향 기대값 +0.53%(>0)
- 사용자가 **12월도 QQQ 포함으로 진행**하기로 했고, 먼저 CSV 로 확인하겠다고 했다

### 확인된 사실 (사전 조사, 2026-09-02)

| 항목 | 값 | 확인 방법 |
| --- | --- | --- |
| QQQ 12월 판정 | 적중률 62.96% · 기대값 +0.53% · 합산 +14.29% · 우연확률 **0.5265** · 등급 **0/3** | `candidates.csv` |
| `EXPIRY_CELLS` 를 값으로 고정한 테스트 | **0건** — 깨지는 테스트가 없다 | `grep -rn EXPIRY_CELLS tests/` |
| 소비처 | `expiry_runner.py`(기본 인자) · `run_expiry_trading.py`(`--ticker` choices) · `check_expiry_dividend.py`(순회) | `grep -rn` |
| `--ticker` 인자 영향 | **없음** — choices 가 `dataset_key` 집합이라 `{dia,kodex200,qqq,spy}` 그대로 | `run_expiry_trading.py:62` |
| 같은 27건으로 맞춘 12월 성적 | DIA 81.48% / SPY 70.37% / **QQQ 62.96%** — 표본 기간 탓이 아니다 | 직접 계산 |
| 12월 상관 | QQQ↔DIA **0.418** · QQQ↔SPY 0.766 · SPY↔DIA 0.871 | 직접 계산 |
| 3분할 대가 | 27해 합계 +20.42% → **+18.47%** (−1.95%p) · 최악은 −2.85% → **−2.56%** 로 개선 | 직접 계산 |

### 영향받는 규칙(반드시 읽고 전체 숙지)

- 루트 `CLAUDE.md` (측정의 원칙 1·3·16, **후보 판정 기준 — 등급은 게이트가 아니다**)
- `~/.claude/CLAUDE.md` (전역 — 수술적 변경, 목표 주도 실행)
- `.claude/rules/strategy.md` (매매 규칙 계층의 예외 규정)
- `.claude/rules/docs.md` (**재실행하지 않은 값을 갈아 적지 않는다**, 수치에 데이터 기간 병기)
- `src/verify_lab/CLAUDE.md` (상수 3계층)

---

## 4) 완료 조건(Definition of Done)

- [x] `EXPIRY_CELLS` 가 **8칸**이고 QQQ 12월(위)이 포함된다
- [x] 상수 주석에 **왜 등급 0/3 인 칸을 넣는지**가 남는다 (등급은 게이트가 아니라는 근거)
- [x] `run_expiry_trading.py` 가 성공하고 `stop_loss_grid.csv` 에 **QQQ 12월 행 20개**가 실린다
- [x] **기존 7칸의 값이 한 자리도 바뀌지 않는다** (이전 실행과 전건 대조)
- [x] **표본 보존 확인**: QQQ 12월의 `신호 + 제외 = 진입 수` 가 성립한다
- [x] `check_expiry_dividend.py` 가 8칸을 돌고 QQQ 12월의 배당락 건수가 나온다
- [x] `docs/research/옵션_만기일.md` 에 **새 절**로 추가 기록이 남는다 (12B 의 7칸 표는 그대로)
- [x] `docs/spec/option_expiry.md` 에 결정이 **확정/탈락안/근거** 형식으로 승격된다
- [x] `docs/COMMANDS.md` 의 「7칸」 기술이 갱신된다
- [x] `poetry run black .` 적용
- [x] `poetry run python validate_project.py` 통과 (`failed=0`, `skipped=0`)

---

## 5) 변경 범위(Scope)

| 파일 | 변경 | 이유 |
| --- | --- | --- |
| `src/verify_lab/strategy/constants.py` | `EXPIRY_CELLS` 에 한 줄 추가 + 주석 보강 | 대상 칸 목록 |
| `scripts/strategy/run_expiry_trading.py` | docstring 의 「등급 3/3 7칸」 수정 | 사실과 어긋남 |
| `scripts/data/check_expiry_dividend.py` | (변경 없음 — `EXPIRY_CELLS` 를 순회하므로 자동 반영) | — |
| `docs/research/옵션_만기일.md` | **새 절 추가** (12B.6) | 12B 의 7칸 표는 보존 |
| `docs/spec/option_expiry.md` | 결정 ㊳ 추가 | 근거 승격 |
| `docs/COMMANDS.md` | 「7칸」 → 「8칸」 | SoT |

### 데이터/결과 영향

- **기존 7칸의 값은 바뀌지 않는다.** 칸마다 독립적으로 계산하므로 칸을 더해도 서로 영향이 없다
- `stop_loss_grid.csv` 140행 → **160행**, `trades.csv` 4,020건 → **4,560건**(27건 × 20 추가)

---

## 6) 단계별 계획(Phases)

### Phase 1 — `EXPIRY_CELLS` 에 QQQ 12월을 추가한다 (그린)

- [x] `ExpiryCell(dataset_key="qqq", expiry_month=12, bet_down=False)` 추가
- [x] 주석에 **등급 0/3 인데 넣는 이유**와 **실측 근거**(같은 27건 비교·상관 0.418·3분할 대가)를 남긴다
- [x] `run_expiry_trading.py` docstring 수정

> **검증**: `poetry run python scripts/strategy/run_expiry_trading.py` 성공.
> `stop_loss_grid.csv` 160행 · QQQ 12월 20행. **기존 7칸 140행을 이전 실행과 전건 대조**

### Phase 2 — 배당락 실측을 8칸으로 다시 돌린다 (그린)

- [x] `check_expiry_dividend.py` 실행 — QQQ 12월의 걸린 건수를 확인
- [x] QQQ 는 배당락이 만기 다음날(2017~)이므로 **12월도 걸릴 것으로 본다** — 실측으로 확인

> **검증**: 8행이 나오고 QQQ 12월의 「걸린 건」이 0 이상. 나머지 7칸은 이전 실행과 같은 값

### 마지막 Phase — 문서 정리 및 최종 검증

- [x] `docs/research/옵션_만기일.md` 에 12B.6 신설 (날짜 명시, 7칸 표 보존)
- [x] `docs/spec/option_expiry.md` 결정 ㊳ 추가
- [x] `docs/COMMANDS.md` 갱신
- [x] `poetry run black .`
- [x] `poetry run python validate_project.py` (`failed=0`, `skipped=0`)

---

## 7) 리스크(Risks)

| 리스크 | 영향 | 완화 |
| --- | --- | --- |
| **칸을 더하면 기존 값이 바뀐다** | 문서 수치가 재현되지 않는다 | 칸별 독립 계산이라 이론상 안 바뀜. **전건 대조를 Phase 1 검증으로 못박음** |
| 「대상 칸에 넣음」이 「채택 확정」으로 읽힌다 | 아직 안 한 판단을 코드가 대신한 것이 된다 | 상수 주석과 문서에 **비교축이지 확정이 아님**을 명시 |
| 12B 의 7칸 집계를 8칸으로 갈아 적음 | 손절 −5% 를 고른 근거가 사라진다 | **비목표로 못박음.** 새 절로만 추가 |

---

## 8) 메모(Notes)

### 확정할 설계 결정

| # | 결정 | 확정 | 탈락안 | 근거 |
| --- | --- | --- | --- | --- |
| ㊳ | **QQQ 12월을 대상 칸에 넣는다** | 등급 0/3 이어도 **1차 게이트를 통과한 후보이므로 포함** | 등급 3/3 만 대상으로 유지 | 루트 `CLAUDE.md` 「후보 판정 기준」이 **"등급은 떨어뜨리지 않는다"** 로 정해져 있다. 등급으로 빼면 **60칸에서 통계량 좋은 칸만 고르는 사후 선택**이 된다. 대가도 작다 — 1999~ 27해 기준 12월 3분할이 2분할보다 합계 −1.95%p(회당 −0.07%p)인데 **최악은 −2.85% → −2.56% 로 개선**되고, QQQ↔DIA 상관이 **0.418** 이라 실제로 분산 효과가 있다. 다만 **QQQ 12월 자체에 통계적 근거는 없다**(우연확률 0.5265 · 기준선 대비 +6.33%p) — 「기전이 있어서 넣는 것」이 아니라 「뺄 근거가 사후 선택뿐이라 안 뺀다」이다 |

### 함께 남길 사실 (12월 전체에 걸리는 한계)

**세 종목의 12월 적중률이 62.96 / 70.37 / 81.48% 로 18.52%p 벌어진다.** 9월은 7.41%p 였다.
같은 시장의 세 지수에서 이만큼 갈리면 **「12월 만기 효과」가 시장 전체 현상이 아니라 DIA 에
몰려 있다**는 뜻이다. **12월은 9월보다 근거가 약하다** — QQQ 를 넣든 빼든 남는 사실이다.

### 진행 로그 (KST)

- 2026-09-02 18:04 — Phase 1: `EXPIRY_CELLS` 8칸. 격자 160행·체결 4,560건
- 2026-09-02 18:05 — **기존 7칸 전건 일치 확인** (격자 140행 불일치 0 · 체결 4,020건 불일치 0)
- 2026-09-02 18:06 — Phase 2: 배당락 8칸 재실행. QQQ 12월 **10건** 걸림, 전체 평균 **−0.067%p**
  (「위」 방향이라 **과소**평가 — 회당 +0.53% 는 +0.60% 로 읽어야 한다)
- 2026-09-02 — 문서 승격 완료. `validate_project.py` 통과(`passed=609 failed=0 skipped=0`)

### 실행 중 발견 (사용자에게 알릴 것)

**`trades.csv` 를 Excel 로 열어 저장하면 파일이 바뀐다.** 8/31 산출물의 `trades.csv` 가
9/2 17:19 에 수정돼 있었고, 날짜가 `1998-12-18` → `1998.12.18` 로, 월·일의 **앞 0 이 지워지고**
(`2021.9.17`), 손절선 `-1.0` → `-1` 로 바뀌었다. **값은 안 바뀌지만 대조·재분석이 깨진다.**
`stop_loss_grid.csv` 는 열지 않아 그대로였고, 그래서 격자 대조가 먼저 성립했다.
