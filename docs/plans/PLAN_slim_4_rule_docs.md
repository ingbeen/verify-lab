# Implementation Plan: 경량화 ④ — 규칙·하네스 문서 정리 (중복 → SoT · 이력 · 모순 · 개인 운용 서술)

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: 🟡 Draft

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-26 16:55
**마지막 업데이트**: 2026-09-26 16:55
**관련 범위**: 규칙·계약 문서 전부(루트·src·scripts·tests `CLAUDE.md`, `.claude/rules/`, `docs/MEMORY.md`, `docs/INDEX.md`, `README.md`, `reference/README.md`), `src/verify_lab/tracks.py`·`tests/test_tracks.py` 의 주석
**관련 문서**: `.claude/rules/docs.md`, 전역 `~/.claude/CLAUDE.md`, 루트 `SLIM_SERIES.md`, `docs/plans/AUDIT_slim_rule_docs.md`, `docs/plans/AUDIT_slim_doc_code_mismatch.md` §1 · §4, `docs/plans/REPORT_lightweight_inventory.md` §3 · §10 · §12

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [ ] 목표 1: **매 세션 항상 로드되는 세 문서**(루트 `CLAUDE.md` · `docs/MEMORY.md` · `.claude/rules/session-bootstrap.md`)를 줄인다 — 작성 시점 767줄, 목표 약 420줄
- [ ] 목표 2: 같은 규칙·결정이 여러 곳에 서술된 **중복 28묶음**을 SoT 한 곳 + 포인터로 (AUDIT_slim_rule_docs §1), 전역 `~/.claude/CLAUDE.md` 와 겹치는 10건은 저장소 고유 사실만 남긴다
- [ ] 목표 3: 규칙·계약 문서에서 **과거 상태·변경 이력·계획 단계**와 **쉽게 변하는 수치·목록**을 걷어낸다 (「왜」는 현재형으로 남긴다)
- [ ] 목표 4: 문서끼리의 **모순 26건**(AUDIT_slim_rule_docs §4)과 문서↔코드 불일치 중 이 문서들의 것(AUDIT_slim_doc_code_mismatch §1 · §4)을 없앤다
- [ ] 목표 5: **개인 운용 서술을 걷어낸다**(보고서 §10 Q1 L2) — 등급 정의 문구 중립화 포함

## 2) 비목표(Non-Goals)

- **`docs/COMMANDS.md`** — 계획서 ⑧. 여기서 COMMANDS 의 중복·이력은 건드리지 않는다(중복 묶음의 SoT 를 정할 때 COMMANDS 쪽은 「⑧ 이 줄인다」로 둔다)
- **매매법·조사 문서**(`docs/매매/**`·`docs/조사/**`) — 계획서 ⑤ ⑥ ⑦. 단 규칙 문서가 **그 문서의 절 번호를 인용**하는 곳은 여기서 절 제목이나 문서 단위로 바꿔 둔다(뒤 계획서가 그 문서를 재편하므로)
- **코드 변경** — `tracks.py`·`tests/test_tracks.py` 의 **주석·docstring 만** 고친다(등급 정의 문구)
- **reference/ 의 인용처 없는 두 파일**(`데이터처리_설계원칙.md` · `test_examples/conftest_example.py`)을 지우는 것 — README 서술만 사실대로 고치고, 지울지는 시리즈 「끝 — 정리」에서 사용자에게 묻는다

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- 규칙·하네스 문서 17개가 3,863줄이고, 매 세션 자동 로드분이 767줄이다. 같은 규칙(산출물 한 폴더 · 등급 SoT · 게이트 값 · 수수료 · 기준선)이 **최대 12곳**에 서술돼 있고 서로 어긋난 곳이 있다 — 예: bootstrap 「시세를 재수집하면 안 된다」 대 루트 「막지는 않되 흔적을 남긴다」, docs.md 「`통계.csv` 가 기준선을 담는다」 대 실제 헤더(기준선 없음)
- 개인 운용 서술이 규칙 계층에 남아 있다 — `docs/MEMORY.md` 의 달러 운용·**개인 구글시트 ID**·2 ~ 3배 집행, 루트 「사용자는 이런 단기 매매에 2 ~ 3배를 씁니다」, 투자금·켈리 규칙 흔적, 「매매 = 걸기로 정한 것」 등급 정의, 「사용자의 매매 대원칙」. 계획서 ① 이 루트에 「개인 운용 상태를 전제로 판단하지 않는다」를 넣었는데 **같은 세션에 MEMORY 의 달러 운용이 함께 로드되어 충돌한다**(리뷰 R3)
- 조사 근거: `docs/plans/AUDIT_slim_rule_docs.md`(§1 중복 D1 ~ D28 · G1 ~ G10, §2 이력, §3 수치, §4 모순 C1 ~ C26, §5 개인 운용 P1 ~ P19, §6 통합·축약 후보와 문서별 목표 줄 수)

### 이 계획서에서 확정하는 판단 (보고서 §10 의 결정 + 작성 시점 결정)

| # | 판단 | 근거 |
| --- | --- | --- |
| 1 | 중복 묶음의 SoT 는 **AUDIT_slim_rule_docs §1 의 「SoT 추천」 열**을 따른다 | 보고서 §10 「나머지는 추천안」 |
| 2 | 요청 시 비용에 **세금은 넣지 않는다** — `trading.md` 의 세금 표·「15.4% 적용」을 루트 쪽(「넣을 것이 없다」)에 맞춘다 (C10) | §10 Q9 ⑶ |
| 3 | 원칙 12 의 「판정」을 **「우연확률·`판정가능` 표시」** 로 좁힌다 — 게이트(`1차 판정`)는 표본 하한을 걸지 않는다 (C20) | §10 Q9 ⑷ |
| 4 | 등급 정의를 중립 문구로 — **매매 = 규칙까지 확정한 것 · 검증 = 재는 중 · 조사 = 걸지 않기로 했거나 신호가 없는 성질** (P16). INDEX 상태 어휘와 같은 말을 쓴다 | §10 Q1 L2 |
| 5 | 「손절은 반드시 있다 (사용자의 매매 대원칙)」 → **이 저장소 규칙 문서의 규약**으로 (P17). 규칙 내용은 그대로 | §10 Q1 L2 |
| 6 | `docs/MEMORY.md` 는 **현재형 + `[실측] 날짜` 딱지 허용**으로 `docs.md` 의 시제 표에 명시한다 — 전역 `~/.claude/CLAUDE.md` 가 같은 딱지를 쓰고, 날짜가 「언제 실측했나」의 근거다 | AUDIT_slim_rule_docs §2 「확인 필요」 해소 |
| 7 | MEMORY 의 인계 기록(설정 전달 스킬 폐기)은 **결정 3줄**만 남긴다 — 「전역 설정을 저장소 경유로 나르지 않는다 · 이유는 구조(PUBLIC 저장소가 통로가 된다) · 권한 가드가 조용히 사라진 사례」 (P12) | §10 「나머지는 추천안」 |
| 8 | INDEX 「매매법·조사」 표의 **`등급` 열을 지운다** — 링크 경로(`매매/`·`조사/`)가 이미 등급을 말하고, 대조 테스트가 없어 낡는다 (리뷰 R13) | §12.3 |
| 9 | `.claude/rules/trading.md` 의 배당락 실측 절차를 **코드(`measure/distribution.py` 의 `dividend_impact`, 차이법)** 에 맞춘다 — 지금은 옵션 만기일 설계가 기각한 「계단 + 임계 0.1%」를 처방한다 | AUDIT_slim_calendar_docs §5b #16 — 착수 시 코드를 읽어 확인 |
| 10 | `trading.md` 가 인용하는 역방향 손절 실측(무손절 최악 −17.45% · 합계 +64.70% → +69.08% · 적중률 59.09%·61.36%)은 **K=10·2008 값**이다. **지금 확정 구성(K=10·2005)의 값으로 바꾸되**, 값은 `docs/매매/역방향/규칙.md` 의 해당 표에서 **그대로 옮긴다**(손으로 다시 계산하지 않는다) | AUDIT_slim_trading_docs 「사용자 결정」 5 — §10 「나머지는 추천안」 |

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- `.claude/plan-config.json` — 이 저장소의 검증 명령·자동 포맷·근거 승격 목적지 (**값의 SoT**)
- 루트 `CLAUDE.md` — 특히 「계획서 규약 — 이 프로젝트의 설정」 절 (값이 아니라 **판단 근거**)
- `.claude/rules/docs.md` — 문서 종류 · 「과거형이 허용되는 자리는 둘뿐입니다」 · 결정 출처 날짜는 남긴다
- 전역 `~/.claude/CLAUDE.md` 「문서와 주석은 리팩토링을 견디게 쓴다」 · 「주석은 코드가 못 하는 말만 한다」 · 「날짜·숫자 범위의 `~` 는 양옆에 공백을 둔다」
- 전역 `~/.claude/skills/writing-for-agents` — CLAUDE.md 를 고칠 때 쓰는 스킬 (`/writing-for-agents`)
- 루트 `SLIM_SERIES.md` 「시리즈 공통 주의」

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [ ] 항상 로드 세 문서의 합이 착수 시점 대비 줄었고 그 수치가 진행 로그에 있다 (목표 약 420줄 — 목표를 못 채우면 이유를 적는다)
- [ ] 중복 28묶음 각각에 대해 **서술 자리 1곳 + 포인터**가 됐음을 grep 으로 확인했다 (묶음별 결과를 진행 로그에 표로)
- [ ] 모순 26건이 해소됐다 (건별 처리 표를 진행 로그에)
- [ ] 개인 운용 서술 grep(아래 Phase 3 Validation) 결과가 0 이거나 의도된 것뿐이다
- [ ] 편집한 문서의 모든 상대 링크가 실재한다 (링크 검사 스크립트)
- [ ] 코드 리뷰 실행 및 결과 기록 (마지막 Phase 의 Validation 에 적는다)
- [ ] 품질 검증 통과 (마지막 Phase 의 Validation 에 passed/failed/skipped 수를 적는다)
- [ ] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [ ] 필요한 문서 업데이트 — `docs/COMMANDS.md` 변경 없음 · 규칙 문서 변경 있음 · `SLIM_SERIES.md` 갱신
- [ ] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다 (위 「확정하는 판단」 1 ~ 10 은 각 규칙 문서 본문에 **결정 출처 날짜와 함께** 들어간다. 보고서 §10 의 Q9 ⑶ ⑷ 도 여기서 살아있는 문서로 옮겨진다)
- [ ] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

- 루트 `CLAUDE.md` · `README.md`
- `docs/MEMORY.md` · `docs/INDEX.md`
- `.claude/rules/session-bootstrap.md` · `docs.md` · `research.md` · `trading.md` · `reference.md`
- `src/verify_lab/CLAUDE.md` · `scripts/CLAUDE.md` · `tests/CLAUDE.md` · `reference/README.md`
- `src/verify_lab/tracks.py` · `tests/test_tracks.py` — **주석·docstring 만** (등급 정의 문구, 문서 경로를 담은 주석)
- `SLIM_SERIES.md` · 보고서 §12 · 이 계획서
- `docs/COMMANDS.md`: **변경 없음**

### 데이터/결과 영향

- 없음

## 6) 단계별 계획(Phases)

### Phase 1 — 항상 로드 세 문서 (그린 유지)

**작업 내용**:

- [ ] 착수 전: `git status` 깨끗 · 계획서 ③ 커밋 확인 · `wc -l CLAUDE.md docs/MEMORY.md .claude/rules/session-bootstrap.md` 를 진행 로그에
- [ ] `session-bootstrap.md`: 스스로 선언한 「어디를 봐야 하는지만」으로 — 1·2·3절 유지, 4절은 **저장소 실측 패턴 표 약 8줄 + 전역 포인터**(G1), 5절(VIRTUAL_ENV)은 삭제(`docs/COMMANDS.md` 품질 검증 옆에 이미 있다 — G3), 6절은 1줄 포인터(SoT `src/verify_lab/CLAUDE.md` 「데이터 저장 규칙」 — D1), 7절(Git)은 삭제(G4). 「시세를 재수집하면 안 된다」(C5) 삭제, 「아래 3절」 오기(C11) 정정
- [ ] `docs/MEMORY.md`: 「사용자의 달러 운용」 절 전부 삭제(P8·P9 개인 시트 ID·P10 — 레버리지 사실은 `docs/조사/레버리지_ETF_괴리/설계.md` 가 이미 갖는다), 「기대 이익을 먼저 잰다」는 중립 2줄(P11), 「산출물은 바이트 단위로 같다」 절은 삭제(D1 — SoT 로 이미 있음. 「조용히 값을 바꾸는 자리 셋」 표가 SoT 에 없으면 SoT 로 옮긴 뒤 삭제), BOM 훅·LOOKUP 관용구 삭제(G8·G9), 인계 기록은 판단 7 의 3줄로(P12). 남는 것: 펜스 절단 · 이름·값 분리 커밋 · 공유 계층 테스트 픽스처 · 설정 전달 금지 결정 · 하네스 메모리 차단 훅
- [ ] 루트 `CLAUDE.md`: AUDIT_slim_rule_docs §2·§3·§5·§6 의 루트 항목 — 「후보 판정 기준」 절의 이력(적중률 하한 걷어냄 · 15곳 복제 · 투자금 결정 · 옵션 만기일 칸 수) 제거와 판단 3, 「두 게이트」·「둘을 모두」 표현(C9), 「매매 수수료와 슬리피지」 절을 원칙 10 포인터 3줄로 줄이고 값·세금 표는 `trading.md` 로 일원화(D6 · 판단 2 · 레버리지 행 P6 삭제), 「결과 보고의 원칙」은 `research.md` 포인터 1줄(D25), 「규칙 문서 구성」 표는 **INDEX 「규칙 문서」 표가 SoT** — 루트에는 「작업 대상 경로의 문서를 작업 전에 읽는다」와 포인터만, 「Read 도구 주입」 인용 블록 삭제(G1), 「외부 요청이 실패하면」 절은 저장소 고유 1줄(`load_krx_credentials()`)만 남기고 전역 포인터(G2), `measure/`·`report/`·`studies/` 「아직 비어 있어」(C13) 삭제, 디렉터리 트리의 등급 설명을 판단 4 로, SPY 3월 사례 값(C4)은 옵션 만기일 `결과.md` 의 값(+0.312%)으로, DIA 6월 「적중률 75.86%」 게이트 서술(C22) 정정, 「재수집은 이미 나온 결과를 바꿉니다」의 `§5.3 — 44건` 오인용 정정

**Validation**:

- [ ] 세 문서 줄 수 합과 착수 시점 대비 감소를 진행 로그에
- [ ] `grep -n -E '달러|환전|배정|1RKnL8|2~3배|켈리|투자금' docs/MEMORY.md CLAUDE.md` 가 0 이거나 의도된 줄뿐

---

### Phase 2 — 계약 문서와 규칙 문서 (그린 유지)

**작업 내용**:

- [ ] `src/verify_lab/CLAUDE.md`: AUDIT_slim_rule_docs §2 의 src 행(「어디까지가 공통인가」 이력 문단 · 「기각한 안 (2026-09-11)」 표 — 대상 코드가 없으니 삭제 · 매매 산출물 계약 셀의 이력 · `측정.csv` 절의 「2026-09-21」 이력 · 계획서 Phase 번호 「2-a/2-b/2-c」 · 「적용 범위는 새로 내는 산출물부터」 과도기 규정), §3 의 낡은 수치(「세 매매법」·「여섯 산출 지점」·「8대상」·저장 대상 표 두 종목 — 목록 삭제), AUDIT_slim_doc_code_mismatch §1·§4 의 src 행(없는 `screen_candidates` — ③ 이 고쳤는지 확인, 「네 상수」→ 둘, 계층 도식의 `measure ↓ report` 방향, 「measure·report 가 studies 를 import 하지 않는지 테스트가 검사」 과장, 「이벤트 없는 검증은 measure 미사용」 일반화, `statistics` 파일 이름, `summary.json` 최상위 키 서술, 메타 키 표를 ③ 결과(`<slug>`)와 대조), 「`.claude/rules/python.md`」 링크 → 전역 `~/.claude/rules/python.md` 표기(R5 · C1), 계층 도식의 등급 서술. 목표 약 550줄(착수 807)
- [ ] 🔴 **달력 조사 문서의 절 번호를 가리키는 줄**(`docs/조사/월말_진입/설계.md` §7.6 등)은 계획서 ⑥ 이 그 문서를 한 장으로 합치며 다시 고친다 — 여기서는 **포인터를 문서 단위로만**(「`docs/조사/월말_진입/…`」) 두고 절 번호를 떼어 둔다. 사실 자체(지수 소급 구간 OHLC=0)를 이 계약 문서가 이미 자립적으로 갖는지 확인한다
- [ ] `.claude/rules/trading.md`: §2 이력(옵션 만기일·월말 사례 · 「폴더 경계가 무너졌습니다」 · 「전에는 인용을 허용했고」 · 겸용 시절 실측 · 「이미 낸 성적표의 세후 절은 그대로」 과도기 규정 · 켈리 1/20 · 옛 `strategy/performance.py`), src 와의 중복(D12 ~ D17 — 산식 계약은 src 가 SoT, trading 은 판독 요령 + 포인터), 판단 2(세금 표 정리) · 판단 5 · 판단 9 · 판단 10, 상태 구동 입력의 예(C24 — 원달러 그리드 코드는 없다. **현재 코드에서 상태 구동의 예를 찾아 바꾸고, 없으면 일반 서술로**), 역방향 손절 격자표 서술(C17), 「지수를 필터에서 잃으면」 절(월말 실측)은 문서 단위 포인터로, 「게이트 값 규약」은 `docs.md` 로 옮긴다(D5). 목표 약 270줄(착수 513)
- [ ] `.claude/rules/docs.md`: 옛 폴더명 `spec/`·`research/` → `설계.md`·`결과.md`(C15), 「`통계.csv` 가 기준선을 담는다」 정정(R2 · C2 — 역방향은 `excess.csv`, 확정 칸으로 좁힌 매매법은 `측정.csv`), 계획서 일반론 중 전역과 겹치는 문장 1줄로(G5), 등급 정의 판단 4, 조사 등급 `규칙.md` 예시(`조사/월말_진입/규칙.md` · `조사/원달러_그리드/규칙.md` — 둘 다 ⑥ ⑦ 이 한 장으로 합친다)를 **특정 파일 없이** 「조사로 내린 규칙은 규칙 절을 가진다」로, 판단 6(MEMORY 시제), D5 의 게이트 값 규약 절 받기
- [ ] `.claude/rules/research.md`: 규칙.md 를 `docs/매매/` 에만 두는 서술(C21), 「§12」 없는 절 인용 포함 옵션 만기일 사례 축약, D25 SoT 확인
- [ ] `.claude/rules/reference.md` · `reference/README.md`: 규칙 문구 중복(D10 — 규칙은 reference.md), README 「언제 지우나」 인용처 서술을 사실대로(R7 — 인용처가 없는 두 파일을 밝힌다)
- [ ] `scripts/CLAUDE.md` · `tests/CLAUDE.md`: 존재하지 않는 인자 예시(C7), 「예외는 둘뿐」(→ 실제 수), 「`test_*.py` 는 src 와 1:1」(② 결과에 맞춰), 보합 20% 재현 테스트(없음), 「세 테스트 파일」 등 개수, 이력 인용 블록, 메타 키 표(③ 결과), `merge_run_summary` 서술
- [ ] `README.md`: 「매매 규칙을 설계하지 않습니다」(C14 — 예외 규정 포인터), 「같은 국면」 금지 용어, 판단 4
- [ ] `docs/INDEX.md`: 판단 8(`등급` 열 삭제 — 표 머리말의 등급 SoT 문장은 유지), 편집으로 바뀐 문서 담당 문구 확인
- [ ] `src/verify_lab/tracks.py` · `tests/test_tracks.py` 주석·docstring: 판단 4, 문서 경로를 담은 주석(「확정 규칙은 `docs/매매/중간선거_사이클/규칙.md` §1」 — 경로를 담으면 등급 변경 때 주석도 고쳐야 한다)은 「그 매매법의 `규칙.md` §1」로, 「걸지 않기로 한 날짜·사연」 주석은 현재형 한 줄로

**Validation**:

- [ ] 중복 묶음별 grep 결과 표(묶음 · SoT · 나머지 자리의 상태)를 진행 로그에
- [ ] `grep -rn -E '걸기로 정|사용자의 매매 대원칙|켈리|spec/|research/|세 매매법|8대상|\.claude/rules/python\.md' CLAUDE.md README.md docs/INDEX.md docs/MEMORY.md .claude src/verify_lab/CLAUDE.md scripts/CLAUDE.md tests/CLAUDE.md src/verify_lab/tracks.py tests/test_tracks.py` 가 0 이거나 의도된 줄뿐(전역 경로 `~/.claude/rules/python.md` 는 의도)
- [ ] `poetry run pytest tests/test_index.py tests/test_research_docs.py tests/test_tracks.py -q` 통과

---

### Phase 3 — 모순 · 링크 · 개인 운용 전수 대조 (그린 유지)

**작업 내용**:

- [ ] AUDIT_slim_rule_docs §4 의 C1 ~ C26 을 한 줄씩 처리 결과로 표시한다(해소 / 뒤 계획서 이관 — 이관은 COMMANDS(⑧)·매매법 문서(⑤⑥⑦)의 몫뿐이어야 한다)
- [ ] AUDIT_slim_rule_docs §5 의 P1 ~ P19 를 같은 방식으로 표시한다 (P13 ~ P15 는 INDEX 가 ① 에서 이미 다시 쓰였으니 확인만)
- [ ] 링크 검사 스크립트(스크래치)를 써서 편집한 모든 `.md` 의 상대 링크를 해석해 실재를 확인한다 — 앵커(`#…`)는 대상 파일의 제목에서 만든 앵커와 대조한다
- [ ] 편집한 문서에서 한 줄에 `~` 가 둘 이상 붙어 있지 않은지(취소선 렌더) 스크립트로 확인한다

**Validation**:

- [ ] `grep -rn -E '포트폴리오|운용 중|현재 운용|사용자의 실제|사용자는 이런|2~3배|직접 챙김|이번 사이클' CLAUDE.md README.md docs/INDEX.md docs/MEMORY.md .claude src/verify_lab/CLAUDE.md scripts/CLAUDE.md tests/CLAUDE.md reference/README.md` 가 0 이거나 의도된 줄뿐
- [ ] 링크 검사 실패 0 · 붙은 `~` 0

---

### Phase 4 (마지막) — 문서 정리 및 최종 검증

**작업 내용**

> 🔴 **`/commit` 이 «맨 마지막»인 것은 의도다.** 그 스킬은 「후보 뒤에는 아무것도 덧붙이지 말 것」으로
> 끝나므로 **호출하는 순간 그 턴이 거기서 닫힌다.** 중간에 두면 뒤에 적힌 항목이 그 벽 너머에 남는다 —
> 실제로 두 번 그렇게 샜다(`[실측] 2026-09-14` 후보를 계획서에 안 옮김 · `2026-09-16` 옮기고 체크박스를 안 닫음).
> **체크박스와 상태를 먼저 확정하고, 커밋 후보를 마지막에 만든다.**

- [ ] 필요한 문서 업데이트 (`docs/COMMANDS.md` 변경 없음 확인)
- [ ] 보고서 §12 백로그 갱신 · `SLIM_SERIES.md` 진행 표와 인계 메모 갱신 (⑤⑥⑦ 이 알아야 할 것 — 규칙 문서가 그 문서들을 어떻게 가리키게 됐는지, ⑧ 로 넘긴 COMMANDS 중복)
- [ ] 자동 포맷 적용 (`poetry run black .`)
- [ ] 변경 기능 및 전체 플로우 최종 검증
- [ ] Validation 절에 `/code-review` 와 품질 검증 **실행 결과**를 적는다
- [ ] DoD 체크리스트 최종 업데이트 및 체크 완료
- [ ] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정
- [ ] 🔴 **마지막에 `/commit` 을 실행하고 그 후보를 «이 계획서» 의 `#### Commit Messages` 절에 옮긴다** —
      `/commit` 은 계획서를 모르고 「후보 뒤에 아무것도 덧붙이지 말 것」으로 끝나므로,
      **대화에만 내면 그 절이 빈 채로 남는다.** 체크박스 갱신이 diff 에 더 들어가지만
      커밋 메시지의 내용을 바꾸지 않는다. **커밋은 사용자가 한다 — 후보만 낸다**

**Validation**:

> 순서를 지킨다 — 리뷰에서 고치면 코드가 바뀌므로 품질 검증이 마지막 관문이어야 한다.
> **버그가 0 인 회차에서 끝낸다.** 2회차에도 버그가 나오면 사용자에게 보고하고 3회차 여부를 묻는다 —
> 「그 외」는 목록만 남기고 고치지 않는다. `/impl-plan` 의 「코드 리뷰」 절이 SoT 다.

- [ ] `/code-review xhigh` **1회차** (발견 \_\_건 — 버그 \_\_ · 그 외 \_\_ · 조치: \_\_)
- [ ] `poetry run python validate_project.py` (passed=\_\_, failed=\_\_, skipped=\_\_)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

> **Done 직전에 `/commit` 을 실행해 이 절을 채운다. 그전에는 비워 둔다.**
> 계획서를 쓰는 시점에는 diff 가 없어 여기 적는 것은 전부 추측이고,
> **추측으로 적은 줄은 그대로 나간다.** 형식·문체 규칙은 `/commit` 이 정한다.

- [ ] (미작성 — `/commit` 을 실행하고 후보 5개를 **이 자리에** 번호 붙은 줄로 옮긴 뒤 이 줄을 지운다)

## 7) 리스크(Risks)

- **줄이다가 규칙의 「왜」를 잃는다** — 이력 문장을 지울 때 그 문장이 담던 「그래서 지금 이 구조다 — 되돌리면 무엇이 깨지는가」를 현재형 한 줄로 남긴다(`docs.md` 의 변환 예시). 지운 문장마다 「왜」가 SoT 쪽에 있는지 확인한다
- **중복을 지우며 SoT 쪽에 없는 사실까지 지운다** — 묶음마다 SoT 문장을 먼저 열어 빠진 사실을 옮긴 뒤 나머지를 지운다
- **자동 로드 경로를 깨뜨린다** — `paths:` frontmatter 는 건드리지 않는다. bootstrap 에 `paths:` 가 생기면 `tests/test_index.py` 가 잡는다
- **CLAUDE.md 는 에이전트가 읽는 문서다** — `/writing-for-agents` 스킬의 기준으로 쓴다. 줄이는 것이 목적이지 문체를 바꾸는 것이 목적이 아니다
- **한 세션의 맥락** — 편집 대상이 약 3,900줄이다. Phase 1 → 2 → 3 순서를 지키고 Phase 마다 진행 로그를 남겨, 끊겨도 다음 세션이 이어받게 한다
- **`VIRTUAL_ENV`** — 모든 도구가 실패하면 `env -u VIRTUAL_ENV`

## 8) 메모(Notes)

- 시리즈의 SoT 는 루트 `SLIM_SERIES.md`. 이 계획서는 ④ 이며 **커밋 하나**로 끝난다
- 감사 원문의 줄 번호는 `f55617a` 기준이고 ① 이 루트 `CLAUDE.md`·INDEX·docs.md·research.md·bootstrap·reference 문서를 이미 고쳤다 — 반드시 grep 으로 다시 찾는다
- 이 계획서가 끝나면 뒤 계획서(⑤⑥⑦)는 규칙 문서를 **고치지 않는 것이 원칙**이다 — 매매법 문서를 합치며 규칙 문서의 포인터가 깨지는 경우만 그 줄을 고친다

### 진행 로그 (KST)

- 2026-09-26 16:55: 계획서 작성 (Draft). 착수는 `SLIM_SERIES.md` 를 받은 세션이 한다
