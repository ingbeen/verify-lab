# Implementation Plan: G — 문서·주석이 코드와 다른 말을 하는 것을 고친다

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-14 12:05
**마지막 업데이트**: 2026-09-15 09:26
**관련 범위**: 루트 CLAUDE.md, src/verify_lab/CLAUDE.md, scripts/CLAUDE.md, docs/, 소스 주석
**관련 문서**: `.claude/rules/docs.md`, `.claude/rules/research.md`, 전역 `~/.claude/CLAUDE.md` 「주석은 코드가 못 하는 말만 한다」

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: **문서가 코드와 다른 말을 하는 11건**을 고친다 — 틀린 설명은 없는 설명보다 나쁘다
- [x] 목표 2: **문서·주석에 박힌 「쉽게 변할 수치」**를 걷어내거나 SoT 를 가리키게 한다
- [x] 목표 3: **루트 `CLAUDE.md` 안의 원칙끼리 부딪히는 지점**을 하나로 정리한다
- [x] 목표 4: **소스 주석의 과거 서술**을 어떻게 할지 결정하고 그 결정을 문서에 남긴다
- [x] 목표 5: **코드는 바꾸지 않는다.** 이 계획서는 문서·주석만 다룬다

## 2) 비목표(Non-Goals)

- 코드 변경 — A~F 소관. **이 계획서에서 코드를 고치고 싶어지면 그것은 A~F 의 항목이다**
- `docs/context/` 수정 — `.claude/rules/context.md` 가 **사용자 소유 문서**로 규정했다. 읽기만 한다
- `docs/research/`·`docs/strategy/` 의 **측정 수치** 수정 — 그 수치는 그때의 실행 기록이다.
  `.claude/rules/docs.md` 가 「문서에 적는 값은 **그 문서가 측정할 때의 기간**이다. 지금 파일의
  기간으로 갱신하지 않는다 — 재실행하지 않았는데 기간만 바꾸면 문서가 거짓말을 한다」로 정했다.
  **이 계획서가 다루는 것은 「코드에서 파생되는 수치」**(대상 수·칸 수·행 수)뿐이다
- `docs/plans/` 의 옛 계획서 — 임시 산출물이며 사용자가 주기적으로 지운다

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

전역 `~/.claude/CLAUDE.md` 가 「**틀린 예시는 없는 예시보다 나쁘다** — 읽는 사람이 그것을 믿고 짠다」로 정했고, `.claude/rules/docs.md` 가 「구체적 수치와 가변 정보를 직접 적지 않는다」로 정했다. 2026-09-14 감사에서 아래가 나왔다.

#### G-1. 문서가 코드와 다른 말을 한다 — 11건

| # | 위치 | 문서가 말하는 것 | 코드 |
| --- | --- | --- | --- |
| ① | `src/verify_lab/CLAUDE.md:382` | 「행 수는 언제나 **`신호 수 × 기준 수 × 구간 수`**」 | 익일 시가는 1일 칸만 내므로 `신호 수 × (종가 구간 수 + 익일 시가 구간 수)`. **같은 저장소의 `measure/forward_return.py:21` 이 「기준별 구간 수의 «합»이다 — 곱셈이 아니다」로 정반대를 적는다** |
| ② | `src/verify_lab/CLAUDE.md:471` | ticker/label 계약을 「고쳤다」 | 검증 두 곳 미수정 → **계획서 B 가 코드를 고친다. G 는 B 이후 서술을 맞춘다** |
| ③ | `scripts/strategy/run_month_end_trading.py:10` | 「대상은 검증 계층과 같은 **8개**」 | 기본값 `DATASETS_TRADING` = **6개**. **같은 파일의 `--ticker` help 는 「6대상」** — 한 파일 안에서 모순 |
| ④ | `scripts/strategy/run_option_expiry_trading.py:128` | 「**160행**을 터미널에 쏟으면」 | 7칸 × (19+1) × 구간 5 = **700행** |
| ⑤ | `docs/COMMANDS.md` 149행 | 「검증 #7 대상 **8칸**」 | `check_expiry_dividend.py` 는 `EXPIRY_CELLS` = **7칸** |
| ⑥ | `docs/strategy/옵션_만기일_매매_규칙.md` §1.1 | 확정 규칙 **4칸** | `EXPIRY_CELLS` = **7칸**. 인자 없이 돌리면 규칙이 제외한 세 칸이 성적표에 섞인다 → **결정 필요 (G-4)** |
| ⑦ | 루트 `CLAUDE.md` 원칙 12 | 「칸당 유효 표본 10건 미만이면 **그 축은 쪼개지 않고**」 | 원칙 17 과 코드는 **쪼개고 행을 남긴 뒤 `판정가능=아니오`** → **결정 필요 (G-5)** |
| ⑧ | `measure/screening.py:32` | 「**정렬도 하지 않는다**」 | `groupby(axis_column, sort=True)` 로 축 오름차순 정렬. 같은 파일 Returns 는 「축 오름차순이며 정렬은 하지 않는다」로 **자기모순** |
| ⑨ | `common_constants.py:52-53` | `DOCS_DIR` 주석이 `utils/result_citations.py` 를 사용처로 지목 | 그 모듈은 `root` 를 인자로 받는다 → **계획서 D 가 상수와 주석을 함께 지운다. D 를 했으면 이 항목은 사라진다** |
| ⑩ | `strategy/periods.py:236-242` | `to_summary_frame` docstring 이 `제외` 컬럼을 설명 | 그 컬럼은 제거됐다 → **계획서 D 소관. D 를 했으면 사라진다** |
| ⑪ | `scripts/CLAUDE.md:111` | 「산출물은 `storage/results/<실행시각>_<검증명>/`」 | 실제는 `storage/results/<계층>/<실행시각>_<검증명>/`. **계층 폴더 도입이 반영 안 됨** |

#### G-2. 문서·주석에 박힌 「쉽게 변할 수치」

| 위치 | 박힌 값 | 무엇이 바뀌면 낡나 |
| --- | --- | --- |
| `docs/COMMANDS.md` 149행 | 「대상 8칸」 | `EXPIRY_CELLS` (**이미 낡음** — G-1 ⑤와 같은 건) |
| `docs/INDEX.md:85` | 「월말은 검증이 **여덟**, 매매가 **여섯**」 | `DATASETS` 길이. 🔴 **`src/verify_lab/CLAUDE.md:234` 가 바로 이것을 금지**하며 「실제로 그 숫자가 낡아 「매매 4대상」이라고 적혀 있던 적이 있다」는 사례까지 든다 |
| `scripts/strategy/run_month_end_trading.py:88` | 「9월 **7/8** · 5월 **6/8** · 12월 **6/8**」 | 시세 재수집 시 전부 변동 |
| `src/verify_lab/strategy/constants.py:55-61, 230-237, 339-341` | 「−4% 2건 · −3% 4건」 「7칸 합계 +175.07%」 「표본 29 · 적중률 75.86% · 회당 +0.44%」 | 시세 기간 |
| `measure/statistics.py:247` · `measure/screening.py:39` · `report/constants.py:93` | 「SPY 3월 평균 +0.257%」 「60칸 중 12칸 방향 갈림」 「SPY 11월 66.7%」 | 시세 기간 |
| `tests/test_layer_contracts.py:14` | 「원칙 12가 「10건」을 명시」 | `MIN_SAMPLE_PER_CELL` 값 |

> ⚠️ **전부 지우는 것이 답은 아니다.** 이 수치들은 「왜 이 값을 골랐는가」의 **근거**이기도 하다
> (전역 규칙이 남기라고 한 「왜」). **구분 기준을 Phase 0 에서 정한다.**

#### G-3. 소스 주석의 과거 서술 — 43건 / 살아있는 문서 16건

전역 `~/.claude/CLAUDE.md` 「적지 않는 것」이 **「과거 상태·변경 이력·개발 단계 … 「기존에는 ~였으나」」**를 금지한다. 2026-09-14 집계:

| 대상 | 건수 | 예 |
| --- | --- | --- |
| `src/**/*.py` 주석 | **43건 / 20파일** (`strategy/constants.py` 4 · `run_summary.py` 3 · `option_expiry_runner.py` 3 …) | 「전에는 같은 뜻의 표가 `summary_by_target.csv` … 셋이었고」 |
| 날짜 딱지가 붙은 변경 이력 | `report/tables.py:515` · `measure/screening.py:10` · `strategy/constants.py:331` 등 | 「**2026-09-12 에 걷어냈다**」 |
| 살아있는 문서 | **16건** | `docs/COMMANDS.md` 544행 「(2026-08-30 개정). **그전에는** 손절 3분할과…」 |

🔴 **판단이 갈린다.** 상당수는 「왜 이 구조를 유지하는가 — 바꾸면 무엇이 깨지는가」에 해당해 **전역 규칙이 남기라고 한 「왜」**이기도 하다. 실제로 그 서술이 없으면 되살아날 위험이 있는 결정들이다(재노출·사슬·CLI 조립). **문장 형태만 규칙과 부딪힌다.**

#### G-4·G-5. 결정이 필요한 둘

- **G-4** — 옵션 만기일 확정 규칙 4칸 vs `EXPIRY_CELLS` 7칸
- **G-5** — 루트 `CLAUDE.md` 원칙 12 와 17 의 충돌

둘 다 **문서만 고칠지 코드까지 갈지**가 갈리므로 Phase 0 에서 결정한다.

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- 루트 `CLAUDE.md` — 전체 (**이 계획서가 이 문서 자체를 고칠 수 있다**)
- `.claude/rules/docs.md` — 「수치를 적을 때는 데이터 기간을 함께 적는다」, 「말은 뜻이 드러나게 쓴다」, 「아카이브 폴더를 만들지 않는다」
- `.claude/rules/research.md` — 결과 문서 작성 규칙
- 전역 `~/.claude/CLAUDE.md` — 「주석은 코드가 못 하는 말만 한다」(남기는 「왜」와 적지 않는 것), 「문서와 주석은 리팩토링을 견디게 쓴다」
- `src/verify_lab/CLAUDE.md` · `scripts/CLAUDE.md` · `tests/CLAUDE.md`
- `docs/INDEX.md` §6 — 이 지도의 유지 규칙

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [x] **선행 계획서(A~F) 복기 완료** — 상태와 「후속 계획서 인계」 절을 전부 읽고,
      **이미 해소된 항목**과 **새로 생긴 불일치**를 판정해 진행 로그에 적었다
- [x] Phase 0 재판단 게이트를 G-1 ⑪건 · G-2 · G-3 · G-4 · G-5 에 실행하고 판정을 진행 로그에 표로 남겼다
- [x] G-4 와 G-5 의 **결정을 내리고 근거를 살아있는 문서에 남겼다**
- [x] 「한다」로 판정된 문서·주석이 전부 고쳐졌다
- [x] **문서 관련 테스트가 통과한다** — `tests/test_index.py` · `tests/test_research_docs.py` · `tests/test_result_citations.py`
- [x] 코드 리뷰 실행 및 결과 기록 (코드를 안 바꿨으면 「해당 없음」)
- [x] 품질 검증 통과 (마지막 Phase 의 Validation 에 passed/failed/skipped 수를 적는다)
- [x] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [x] 필요한 문서 업데이트 (`docs/COMMANDS.md` **변경 있음** — G-1 ⑤)
- [x] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다
      (G-3 의 **결정과 그 기준**을 `src/verify_lab/CLAUDE.md` 또는 루트 `CLAUDE.md` 에 남긴다.
      결정만 있고 기준이 없으면 다음 사람이 같은 판단을 반복한다)
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

- 루트 `CLAUDE.md` — 원칙 12 문구 (G-5)
- `src/verify_lab/CLAUDE.md` — 행 수 공식(①), ticker/label 서술(②), G-3 기준 명문화
- `scripts/CLAUDE.md` — 산출물 경로(⑪)
- `docs/COMMANDS.md` — 「8칸」(⑤), 「그전에는…」(G-3)
- `docs/INDEX.md` — 대상 수(G-2)
- `docs/strategy/옵션_만기일_매매_규칙.md` — G-4 결정에 따라
- `src/verify_lab/measure/screening.py` — 모듈 docstring 자기모순(⑧) · 실측 수치(G-2)
- `src/verify_lab/measure/statistics.py` · `report/constants.py` · `strategy/constants.py` — 실측 수치(G-2)
- `scripts/strategy/run_month_end_trading.py` · `run_option_expiry_trading.py` — ③④와 G-2
- `src/verify_lab/**/*.py` — G-3 결정에 따라 (범위가 가장 크다)
- `docs/COMMANDS.md`: **변경 있음** (G-1 ⑤)

### 데이터/결과 영향

- **없다.** 문서·주석만 바꾼다
- 🔴 **단, 문서 편집이 테스트를 깨뜨릴 수 있다:**
  - `docs/` 에 **산출물 폴더 이름**(`<8자리>_<6자리>_<영소문자>`)을 쓰면 `utils/result_citations.py` 가
    **인용으로 잡아** 그 폴더가 영구히 묶인다. 없어진 폴더 이름을 예시로 쓰면 `tests/test_result_citations.py` 가 실패한다
  - `docs/` 에 파일을 추가·삭제하면 `docs/INDEX.md` 등록이 필요하고 `tests/test_index.py` 가 검사한다
  - `docs/research/` 는 `tests/test_research_docs.py` 가 형식을 검사한다

## 6) 단계별 계획(Phases)

### Phase 0 — 재판단 게이트

> **건너뛸 수 없다.** 특히 **A~F 를 먼저 했다면 이 계획서의 항목 상당수가 이미 사라져 있다.**

**작업 내용**:

- [x] 🔴 **선행 계획서 복기 (이 Phase 의 첫 작업)** — G 는 마지막이라 **앞선 여섯을 전부** 읽는다:

  ```bash
  for f in docs/plans/PLAN_[a-g]_*.md; do
    echo "=== $f"; grep -m1 '^\*\*상태\*\*' "$f"
    sed -n '/^### 후속 계획서 인계/,/^### 진행 로그/p' "$f"
  done
  ```

  - **인계 절에 적힌 변경 하나하나가 이 계획서의 대상을 늘리거나 줄인다.**
    ① A~F 가 **이미 고친 문서**는 뺀다 ② A~F 가 **새로 만든 불일치**는 더한다
    (예: E 가 파일을 옮겼는데 `src/verify_lab/CLAUDE.md` 의 계층 그림이 안 따라왔다)
  - **A~F 중 뒤로 넘긴 결정**(B→E 의 `dataset_record`, C→E 의 공통 어휘, G-4·G-5)이
    **결국 어떻게 됐는지** 확인해 문서에 반영할 것이 남았는지 본다
  - 아래 세 항목의 판정은 이 결과에 따라 달라진다
  - **B 를 했으면** ② 는 「코드는 고쳐졌고 문서 서술만 남았다」로 바뀐다
  - **D 를 했으면** ⑨ ⑩ 은 이미 사라졌다
  - **E 를 했으면** ⑪ 의 내용이 또 달라졌을 수 있다
  - A~F 를 하나도 안 했다면 **G 를 먼저 하지 말 것을 권한다** — 코드가 바뀌면 문서를 두 번 고친다.
    그래도 진행한다면 **①③④⑤⑧⑪ 과 G-2 만** 하고 ②⑨⑩ 은 미룬다

- [x] **G-1 항목별 확인** — 11건을 하나씩 현재 파일에서 눈으로 본다:

  ```bash
  sed -n '380,384p' src/verify_lab/CLAUDE.md          # ①
  sed -n '19,23p'  src/verify_lab/measure/forward_return.py
  sed -n '469,473p' src/verify_lab/CLAUDE.md          # ②
  sed -n '8,14p'   scripts/strategy/run_month_end_trading.py   # ③
  sed -n '125,132p' scripts/strategy/run_option_expiry_trading.py  # ④
  grep -n "8칸" docs/COMMANDS.md                       # ⑤
  grep -n "4칸" docs/strategy/옵션_만기일_매매_규칙.md   # ⑥
  grep -n "쪼개지 않고" CLAUDE.md                        # ⑦
  sed -n '28,42p'  src/verify_lab/measure/screening.py # ⑧
  grep -n "DOCS_DIR" src/verify_lab/common_constants.py # ⑨
  grep -n "제외" src/verify_lab/strategy/periods.py     # ⑩
  grep -n "storage/results" scripts/CLAUDE.md          # ⑪
  ```

  - 각각 **한다 / 이미 고쳐짐 / 안 한다**를 적는다

- [x] **G-1 ④·G-2 의 현재 값을 «코드에서» 다시 뽑는다** — 문서에 적을 숫자를 추측하지 않는다:

  ```bash
  poetry run python -c "
  from verify_lab.strategy.constants import EXPIRY_CELLS, EXPIRY_STOP_LEVELS, MONTH_END_STOP_LEVELS
  from verify_lab.studies.month_end.constants import DATASETS, DATASETS_TRADING, ENTRY_CALENDAR_DAYS, EXIT_OFFSETS
  from verify_lab.studies.option_expiry.constants import DATASETS as OE
  from verify_lab.strategy.constants import PERIODS
  print('EXPIRY_CELLS', len(EXPIRY_CELLS), '| STOP_LEVELS', len(EXPIRY_STOP_LEVELS), '| 구간', len(PERIODS))
  print('격자 행', len(EXPIRY_CELLS)*(len(EXPIRY_STOP_LEVELS)+1)*len(PERIODS))
  print('month_end 검증', len(DATASETS), '매매', len(DATASETS_TRADING), '격자', len(ENTRY_CALENDAR_DAYS)*len(EXIT_OFFSETS))
  print('option_expiry DATASETS', len(OE))
  "
  ```

  🔴 **그리고 그 숫자를 문서에 다시 적지 않는 방법을 먼저 고민한다** — 「`EXPIRY_CELLS` 가 SoT 다」로
  가리키는 편이 낫다면 그렇게 한다

- [x] 🟡 **G-4 결정 — 옵션 만기일 4칸 vs 7칸**
  - **(a) 문서를 명확히 한다** ← 권장. 코드 주석(`strategy/constants.py:329-331`)이
    「**통계량으로 칸을 빼지 않는다. 게이트를 넘었으면 함께 둔다 — 빼면 사후 선택이 된다**(결정 ㊳)」로
    근거를 대고 있다. 그 판단이 옳다면 바꿀 것은 **규칙 문서**다 —
    「§1 은 «실제로 거는» 칸이고, 기본 실행은 «게이트를 넘은 전 칸»을 낸다. 성적표에서 §1 의 네 칸만
    골라 읽으려면 `종목`·`만기월`·`방향` 으로 거른다」를 §1 과 §2 에 **명시**한다
  - **(b) 코드를 규칙에 맞춘다** — `EXPIRY_CELLS` 를 4칸으로 줄인다.
    🔴 선택하면 **코드 변경이므로 이 계획서의 비목표를 깬다.** 별도 계획서로 뺀다
  - 어느 쪽이든 **`docs/INDEX.md:110` 의 요약 문장도 함께** 맞춘다

- [x] 🟡 **G-5 결정 — 루트 `CLAUDE.md` 원칙 12 ↔ 17 충돌**
  - 사실관계: 원칙 12 는 「10건 미만이면 **그 축은 쪼개지 않는다**」, 원칙 17 은 「미달이어도 **행을 남긴다**」,
    코드는 **쪼개고 행을 남긴다**(옵션 만기일 `_aggregate_month_halves`·월말 `_split_by_period`).
    즉 **코드는 17 을 따르고 12 의 앞부분을 안 지킨다**
  - **(a) 원칙 12 의 문구를 코드·17 에 맞춘다** ← 권장. 「쪼개지 않는다」 →
    「**쪼개되 칸당 표본이 하한 미만이면 판정에 쓰지 않는다** — 행은 남기고 `판정가능` 으로 표시한다(원칙 17)」
  - **(b) 코드를 12 에 맞춘다** — 하한 미달 축은 아예 안 낸다.
    🔴 **원칙 17 이 「행이 사라지면 그 구간을 못 봤다는 사실 자체를 사용자가 모른다」로 반대한다.** 권장하지 않는다
  - 🔴 **루트 `CLAUDE.md` 는 사용자의 규칙 문서다.** 고치기 전에 이 결정을 **사용자에게 보고**하고
    승인받는다. 승인 없이 고치지 않는다

- [x] 🟡 **G-3 결정 — 과거 서술 43+16건을 어떻게 할 것인가**
  - **(a) 시제만 바꾼다** ← 권장. 정보(왜 이 구조인가)는 남기고 형태만 규칙에 맞춘다.
    「전에는 X 였다」 → 「**이 자리에 X 를 두지 않는다 — 두면 Y 가 깨진다**」
    - 장점: 규칙과 충돌하지 않으면서 되살아남 방지 정보가 남는다
    - 비용: 20파일 43건을 손으로 고쳐야 한다. **한 번에 하지 말고 항목별로 나눈다**
  - **(b) 날짜 딱지만 지운다** — 「2026-09-12 에 걷어냈다」류만 지우고 나머지는 둔다.
    가장 싸고, 전역 규칙의 「개발 단계·변경 이력」 금지에 가장 직접 걸리는 것만 없앤다
  - **(c) 그대로 둔다** — 「왜」의 일부로 인정한다. 그러면 **그 예외를 문서에 적어야 한다**
  - 🔴 **어느 쪽이든 「기준」을 문서에 남기는 것이 이 항목의 산출물이다.**
    결정 없이 넘어가면 다음 사람이 같은 목록을 다시 만든다
  - **범위가 크면 (b) → (a) 순으로 나눠 진행해도 된다.** 그때는 Phase 를 쪼개고
    남은 것을 진행 로그에 적는다 (스킵이 아니라 **범위 축소**이며, 축소한 사실을 남긴다)

- [x] 항목별 판정을 **진행 로그에 표로** 남긴다

---

### Phase 1 — 코드와 다른 말을 하는 문서를 고친다

**작업 내용**:

- [x] **①** `src/verify_lab/CLAUDE.md:382` 의 행 수 공식을
      `measure/forward_return.py` 의 서술과 **같은 말**로 고친다
  - 🔴 두 곳에 같은 공식을 적지 말고, **계약 문서가 모듈을 가리키게** 한다 —
    두 벌이면 또 갈라진다 (`.claude/rules/docs.md` 「코드에서 파생 가능한 정보를 복제하지 않는다」)
- [x] **③** `run_month_end_trading.py` 모듈 docstring 의 「8개」를 고친다
  - 🔴 **숫자를 다른 숫자로 바꾸지 않는다.** 「기본 대상은 `DATASETS_TRADING` 이다」처럼
    **SoT 를 가리키게** 한다. 같은 파일 `--ticker` help 와 한 말이 되게 맞춘다
- [x] **④** `run_option_expiry_trading.py:128` 의 「160행」을 없앤다
      (행 수를 적을 이유가 없다 — 「전 행을 터미널에 쏟으면 읽을 수 없다」로 충분하다)
- [x] **⑤** `docs/COMMANDS.md` 149행 「대상 8칸」 → 「`strategy/constants.EXPIRY_CELLS` 의 전 칸」
- [x] **⑧** `measure/screening.py` 모듈 docstring 의 「정렬도 하지 않는다」와
      `screen_candidates` Returns 의 「축 오름차순이며 정렬은 하지 않는다」를 **사실에 맞게** 고친다
  - 사실: `groupby(sort=True)` 로 **축 오름차순으로 나온다.** 「무엇을 먼저 보여줄지(우선순위 정렬)는
    표시 계층의 몫」이라는 원래 뜻은 살리되 **모순된 문장을 없앤다**
- [x] **⑪** `scripts/CLAUDE.md:111` 의 산출물 경로에 **계층 폴더**를 넣는다
      (`src/verify_lab/CLAUDE.md` 데이터 저장 규칙의 표기와 맞춘다)
- [x] **②·⑨·⑩** — 선행 계획서 상태에 따라 처리 (Phase 0 판정대로)

---

### Phase 2 — 쉽게 변할 수치를 SoT 로 돌린다

> **전부 지우는 것이 답이 아니다.** 「왜 이 값을 골랐는가」의 근거는 남긴다.

**작업 내용**:

- [x] **판단 기준을 먼저 적는다** (진행 로그에):
  - **지운다/가리킨다** — 코드에서 바로 셀 수 있는 값 (대상 수·칸 수·행 수·격자 크기)
  - **남긴다 + 기간을 붙인다** — 「왜 이 파라미터인가」의 근거가 되는 실측치.
    `.claude/rules/docs.md` 가 요구하는 **데이터 기간 표기**를 함께 단다
- [x] `docs/INDEX.md:85` 「검증이 여덟, 매매가 여섯」 → SoT 를 가리키게
      (🔴 `src/verify_lab/CLAUDE.md:234` 가 「여기에 구체적인 대상 수를 적지 않습니다」로 이미 금지했다)
- [x] `scripts/strategy/run_month_end_trading.py:88` 의 「9월 7/8 · 5월 6/8 · 12월 6/8」
  - 화면에 낼 달을 고른 **근거**이므로 지우면 왜 그 셋인지 알 수 없다.
    → **남기되 「어느 실행 기준인지」를 붙인다.** 또는 근거를 결과 문서로 옮기고 여기서는 가리킨다
- [x] `strategy/constants.py`·`measure/statistics.py`·`measure/screening.py`·`report/constants.py` 의 실측 수치
  - 전부 **파라미터를 고른 근거**다. **남긴다.** 다만 「어느 데이터 기간의 값인가」가 없는 것에는 붙인다
  - 🔴 `strategy/constants.py:5-6` 이 이미 「**표본 수는 시세 기간에 묶여 있으므로 그 문서의 머리말이
    기준이다 — 값을 여기 적으면 재수집할 때마다 두 곳이 갈라진다**」고 적어 두었다.
    **그 선언과 실제 주석이 어긋나는지** 확인하고, 어긋나면 선언 쪽이나 주석 쪽 하나로 맞춘다
- [x] `tests/test_layer_contracts.py:14` 의 「10건」 → `MIN_SAMPLE_PER_CELL` 을 가리키게

---

### Phase 3 — G-3(과거 서술) 과 G-4·G-5 결정 반영

**작업 내용**:

- [x] **G-5** — 사용자 승인을 받았다면 루트 `CLAUDE.md` 원칙 12 문구를 고친다.
      **승인 전에는 고치지 않는다.** 미승인이면 이 항목을 진행 로그에 「보류」로 남긴다
- [x] **G-4** — 결정대로 `docs/strategy/옵션_만기일_매매_규칙.md` §1·§2 와 `docs/INDEX.md:110` 를 맞춘다
  - (b)를 골랐다면 **코드 변경이므로 별도 계획서로 뺀다**
- [x] **G-3** — 결정대로 진행한다
  - (b)를 골랐다면: 날짜 딱지(「2026-09-12 에 걷어냈다」류)를 찾아 지운다

    ```bash
    grep -rn "20[0-9][0-9]-[0-9][0-9]-[0-9][0-9] 에 \|에 걷어냈다\|에 없어졌다\|에 뺐다" src/verify_lab/ --include='*.py'
    ```

  - (a)를 골랐다면: 파일 단위로 나눠 진행하고 **어디까지 했는지 진행 로그에 적는다**
  - 🔴 **정보를 지우지 않는다.** 「전에는 X 였다」를 지울 때는 그 문장이 담고 있던
    **「그래서 지금 이 구조다」**가 남아 있는지 확인한다. 남아 있지 않으면 현재 시제로 다시 쓴다
  - `docs/COMMANDS.md` 544행 등 살아있는 문서 16건도 같은 기준으로

---

### 마지막 Phase — 문서 정리 및 최종 검증

**작업 내용**

- [x] **G-3 의 기준을 문서에 남긴다** — 「과거 서술을 어디까지 남기고 어떤 형태로 쓰는가」.
      자리는 `src/verify_lab/CLAUDE.md` (코드 주석 기준) 또는 루트 `CLAUDE.md`
- [x] **G-4·G-5 의 결정과 근거**를 해당 살아있는 문서에 남긴다 (계획서를 링크하지 않는다)
- [x] `docs/COMMANDS.md`: **변경 있음** — 무엇을 고쳤는지 적는다
- [x] 자동 포맷 적용: `poetry run black .`
- [x] 🔴 **문서 테스트를 먼저 돌려 본다** (품질 검증 전에 깨진 것을 잡는다):

  ```bash
  poetry run pytest tests/test_index.py tests/test_research_docs.py tests/test_result_citations.py -v
  ```

- [x] 🔴 **「후속 계획서 인계」 절을 채운다** (§8 Notes) — G 는 마지막이지만
      **A~F 를 전부 돌린 뒤 남은 것**을 여기 적는다: 미해결 항목, 사용자 승인 대기 중인 결정(G-5),
      범위를 축소해 남긴 과거 서술(G-3), **다음 감사 때 다시 봐야 할 것**.
      `docs/plans/` 는 주기적으로 지워지므로 **남길 가치가 있는 것은 살아있는 문서로 승격**하고,
      이 절에는 「어디로 옮겼는지」만 적는다
- [x] 변경 기능 및 전체 플로우 최종 검증
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정

**Validation**:

> 순서를 지킨다 — 리뷰에서 고치면 코드가 바뀌므로 품질 검증이 마지막 관문이어야 한다.

- [x] `/code-review xhigh` — **해당 없음 (주석·docstring 만 변경).** `.py` 30개가 diff 에 들어 있으나
      **실행 로직은 한 줄도 바뀌지 않았다** — docstring 을 뺀 AST 를 `HEAD` 와 파일별로 대조해
      **30개 전부 동일**함을 확인했다 (대조 스크립트는 스크래치패드에 있고 저장소에 넣지 않았다)
- [x] `poetry run python validate_project.py` (passed=1177, failed=0, skipped=0)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

> **Done 직전에 `/commit` 을 실행해 이 절을 채운다. 그전에는 비워 둔다.**
> 계획서를 쓰는 시점에는 diff 가 없어 여기 적는 것은 전부 추측이고,
> **추측으로 적은 줄은 그대로 나간다.** 형식·문체 규칙은 `/commit` 이 정한다.

1. `문서 / 코드와 어긋난 서술 13건 수정`
2. `문서 / 코드와 어긋난 서술 13건 수정과 과거 서술 72건 현재형 전환`
3. `문서 / 틀린 설명 제거와 과거형 허용 경계의 규칙화`
4. `문서 / 쉽게 변할 수치의 SoT 이관과 측정의 원칙 12·17 충돌 해소`
5. `문서 / 규칙·계약 문서와 주석의 현재형 통일 (실행 로직 무변경)`

## 7) 리스크(Risks)

| 리스크 | 완화 |
| --- | --- |
| 🔴 **문서에 산출물 폴더 이름을 예시로 쓰면 인용으로 잡혀** 그 폴더가 영구히 묶이거나, 없는 폴더면 테스트가 실패한다 | 마지막 Phase 에 문서 테스트 3종을 먼저 돌린다. 예시가 필요하면 `<실행시각>_<매매법>` 같은 **자리표시자**를 쓴다 (`utils/result_citations.NOT_A_PATH_CHARS` 가 그 형태를 판정에서 뺀다) |
| **과거 서술을 지우다 「왜 이 구조인가」를 함께 잃는다** | Phase 3 에 「정보를 지우지 않는다」를 명시했다. 시제만 바꾸는 (a) 가 권장안인 이유다 |
| 루트 `CLAUDE.md` 를 승인 없이 고친다 | G-5 에 **사용자 승인 전 수정 금지**를 명시했다 |
| A~F 를 안 한 상태에서 G 를 하면 문서를 두 번 고친다 | Phase 0 첫 항목이 선행 상태를 확인하고, 안 했으면 **범위를 좁히도록** 지시한다 |
| G-3 (a)가 43건이라 한 세션에 안 끝난다 | **범위 축소를 허용**하되 어디까지 했는지 진행 로그에 남긴다. 스킵이 아니라 범위 축소다 |
| 수치를 지우다 **파라미터 선택 근거**를 잃는다 | Phase 2 가 「지운다/남긴다」 기준을 먼저 적게 한다. 코드에서 셀 수 있는 값만 지운다 |

## 8) 메모(Notes)

- **왜 마지막인가**: A~F 가 코드를 바꾸면 문서가 또 어긋난다. **코드가 안정된 뒤 문서를 맞추는 것이 한 번에 끝난다**
- **이 계획서는 코드를 바꾸지 않는다.** 코드를 고치고 싶어지면 그것은 A~F 의 항목이며, 없으면 새 계획서를 만든다
- 2026-09-14 집계 — 문서/코드 불일치 **11건**, 코드 파생 수치 **6곳**, 과거 서술 **소스 43건 · 문서 16건**
- G-3 은 **「하지 않기로 결정하고 기준을 남기는 것」도 완료**다. 결정 없이 넘기는 것만 실패다

### 후속 계획서 인계 (이 계획서를 끝낸 뒤 채운다)

> G 는 A~G 의 마지막이다. **다음 작업(새 계획서 또는 다음 감사)이 이 절을 읽는다.**
> 형식: `무엇이 | 어떤 상태인가 | 어디에 남겼나`
>
> 🔴 **`docs/plans/` 는 주기적으로 삭제된다.** 여기 적은 것 중 계속 참이어야 하는 것은
> **살아있는 문서로 옮기고** 이 절에는 그 위치만 적는다
> (`.claude/rules/docs.md` 「계획서」 절 — 근거 승격).
> 남은 것이 없으면 **「없음」이라고 적는다**.

| 무엇이 | 어떤 상태인가 | 어디에 남겼나 |
| --- | --- | --- |
| 🔴 **과거형이 허용되는 자리 — 이 계획서의 핵심 산출물** | **결정 완료.** 규칙·계약 문서와 코드 주석은 **현재형**, `spec/` 의 탈락안과 `research/` 의 재실행 기록만 **과거형 허용**. 전역 규칙과 이 저장소의 「확정/탈락안/근거」 요구가 **자리로 갈린다**는 것이 근거다 | [.claude/rules/docs.md](../../.claude/rules/docs.md) 「과거형이 허용되는 자리는 둘뿐입니다」 — **SoT**. `docs/**` 에만 로드되므로 [src/verify_lab/CLAUDE.md](../../src/verify_lab/CLAUDE.md) 「주석은 현재형으로 씁니다」가 **포인터 한 줄**로 가리킨다(복제하지 않았다) |
| **루트 `CLAUDE.md` 원칙 12 ↔ 17 충돌** | **해소.** 원칙 12 를 「쪼개지 않는다」 → 「**하한 미만인 칸은 판정에 쓰지 않는다**(행은 남기고 `판정가능` 으로 표시)」로 고쳤고, 원칙 17 에 **「하한 = 원칙 12 의 10건」**을 명시했다. 사용자 승인을 받고 고쳤다 | 루트 `CLAUDE.md` 원칙 12·17 |
| **세 곳이 원칙 17 을 실제보다 강하게 인용하던 것** | **해소.** `measure/constants.py:63-64` · `src/verify_lab/CLAUDE.md` · `tests/test_layer_contracts.py` 가 「원칙 17 이 「원칙 12 의 10건」이라며 선언한다」고 적는데 원칙 17 에 그 말이 없었다. **원칙 17 쪽을 고쳐 세 서술을 사실로 만들었다** — 인용 세 곳은 그대로 둔다 | 루트 `CLAUDE.md` 원칙 17 |
| **옵션 만기일 4칸 vs `EXPIRY_CELLS` 7칸** | **해소(문서 쪽).** 코드가 옳다 — 「통계량으로 칸을 빼지 않는다」(결정 ㊳). §1.1 에 「이 표는 실제로 거는 칸이고 기본 실행은 게이트를 넘은 전 칸을 낸다」와 **거르는 방법**을 명시했다 | [docs/strategy/옵션_만기일_매매_규칙.md](../strategy/옵션_만기일_매매_규칙.md) §1.1·§2 · [docs/INDEX.md](../INDEX.md) |
| 🔴 **감사가 못 본 13번째 불일치** | **해소.** `docs/spec/월말_진입_설계.md` 결정 ⑭(2026-09-11)의 **「두 계층의 대상 범위는 이제 같다 — 둘 다 `DATASETS`(코스피 4 + 코스닥 4)」가 거짓**이었다. §7.16(2026-09-12)이 매매에서 인버스를 빼 `DATASETS_TRADING`=6 이 됐는데 ⑭ 가 안 따라왔다. **⑭ 가 §7.16 을 가리키게** 하고 숫자를 뺐다 | `docs/spec/월말_진입_설계.md` ⑭ |
| **살아있는 코드에서 계획서를 가리키던 4곳** | **해소.** `tests/test_layer_contracts.py`(2) · `tests/test_strategy_output_contract.py`(2) 의 docstring 이 「계획서 B」·「작업 B/C」를 근거로 들고 있었다 — `docs/plans/` 는 주기적으로 지워지므로 **자립하게** 고쳤다 | 그 두 테스트 파일 |
| **코드에서 셀 수 있는 수치를 SoT 로 돌린 것** | `docs/INDEX.md`(월말 대상 수) · `docs/COMMANDS.md`(만기 칸 수) · `scripts/strategy/run_month_end_trading.py`(대상 수) · `run_option_expiry_trading.py`(격자 행 수) · `tests/test_layer_contracts.py`(표본 하한). **파라미터 선택 근거인 실측치는 남겼다** — 각 모듈이 출처 문서를 인용하거나 머리말이 기준을 선언한다 | 각 파일 |
| ⚠️ **일부러 남긴 과거형 — 다시 지우려 들지 말 것** | `docs/spec/역방향_설계.md:177`(결정 ⑦ 의 탈락안 「이전에는 EMA·SMA 병기였다」) · `docs/research/` 의 재실행 기록 전부(선물_대_레버리지 §개정표 · 옵션_만기일 머리말). **위 기준의 「과거형 허용」 두 자리**이며, 지우면 `.claude/rules/docs.md` 가 요구하는 기록이 사라진다 | [.claude/rules/docs.md](../../.claude/rules/docs.md) 의 표 |
| **실행 로직은 한 줄도 안 바뀌었다** | `.py` 30개가 diff 에 있으나 **docstring 을 뺀 AST 가 `HEAD` 와 전부 동일**하다. 산출물 재실행·대조가 필요 없고, **다음 작업은 「G 때문에 수치가 달라졌다」를 의심하지 않아도 된다** | 마지막 Phase Validation |
| 🔴 **넘긴 것 — 산출물 폴더 58개** | **사용자 지시로 `/clean-results` 를 이번에도 건너뛰었다**(2026-09-15). E 가 넘긴 40개 + F 의 18개가 그대로다. **G 는 재실행을 하지 않아 더 늘지 않았고**, 인용 0건이라 품질 검증에 영향이 없다. 그 스킬은 모델이 호출할 수 없어 사용자가 직접 돌린다 | 없음 (작업 항목) |
| **다음 감사 때 다시 볼 것** | ① `docs/INDEX.md` 100·107행과 `docs/spec/월말_진입_설계.md` 가 아직 「8대상」을 적는다 — **그 문서들의 «내용 요약»이라 남겼으나** `DATASETS` 가 바뀌면 낡는다 ② C 가 남긴 「검증은 `시기`, 매매는 `구간`」 헤더 갈림은 **손대지 않았다**(산출물 컬럼이라 문서 변경으로 끝나지 않는다) ③ `scripts/data/check_pykrx_splice.py` 의 `_kst_now()` 가 `common_constants.KST` 를 안 쓰는 것(F 가 넘김) | 이 표 |

### 진행 로그 (KST)

- 2026-09-14 12:05: 계획서 작성. 감사의 「문서/주석/코드 3자 불일치」·「쉽게 변할 수치」·「과거 상태 기재」를 G 로 묶음
- 2026-09-15 09:09: **Phase 0 착수 — 선행 계획서 A~F 복기.** 여섯 전부 ✅ Done.
  인계 절을 전부 읽고 아래를 확인했다.
  - **수치 영향 없음** — A·B·C·D·E·F 가 모두 「CSV 한 셀도 안 바뀌었다」를 대조로 확인했다.
    따라서 G 는 「선행 계획서 때문에 수치가 달라졌다」를 의심하지 않아도 된다
  - **이미 해소된 것 3건** — ② (B 가 `ticker`/`label` 을 고쳤고 서술도 사실과 맞다) ·
    ⑨ ⑩ (D 가 `DOCS_DIR` 과 `제외` docstring 을 코드에서 없앴다)
  - **선행 계획서가 이 계획서에 넘긴 것** — E 의 「`src/verify_lab/CLAUDE.md` 두 문장 반영 완료」,
    F 의 「`validate_project.py` 출력 형식 변경(`개수 미상`·`errors=N`)」, C 의 「축 헤더가
    검증 `시기` 대 매매 `구간` 으로 갈려 있다(④)」
  - **`/clean-results` 는 이번에도 건너뛴다** (사용자 지시 2026-09-15). E·F 가 넘긴 산출물
    58개가 그대로 쌓여 있고 **인용 0건이라 품질 검증에 영향이 없다.** G 는 재실행을 하지
    않으므로 더 늘지 않는다

- 2026-09-15 09:09: **Phase 0 판정표 — G-1 11건**

  | # | 위치 | 판정 | 근거 (실측) |
  | --- | --- | --- | --- |
  | ① | `src/verify_lab/CLAUDE.md:490` | **한다** | 「신호 수 × 기준 수 × 구간 수」 대 `forward_return.py:21` 「기준별 구간 수의 **합**이다 — 곱셈이 아니다」 |
  | ② | ticker/label 서술 | **이미 고쳐짐** | B 가 코드를 고쳤고 `src/verify_lab/CLAUDE.md:579` 서술이 현재 사실과 맞다 |
  | ③ | `run_month_end_trading.py:10` | **한다** | 「8개」 대 기본값 `DATASETS_TRADING` **6개**. 같은 파일 99행 help 는 「6대상」 |
  | ④ | `run_option_expiry_trading.py:121` | **한다** | 「160행」 대 실제 **700행** (7칸 × (19+1) × 구간 5) |
  | ⑤ | `docs/COMMANDS.md:149` | **한다** | 「대상 8칸」 대 `check_expiry_dividend.py:154` 가 도는 `EXPIRY_CELLS` **7칸** |
  | ⑥ | 매매 규칙 4칸 vs 코드 7칸 | **한다 (G-4)** | `EXPIRY_CELLS` 는 DIA12·KODEX200\_9·SPY9·DIA9·SPY12·QQQ9·QQQ12 |
  | ⑦ | 원칙 12 ↔ 17 | **한다 (G-5)** | `_split_by_period` docstring 이 「표본이 하한에 못 미쳐도 행을 지우지 않는다(원칙 17)」 |
  | ⑧ | `measure/screening.py:32`·`:145` | **한다** | `groupby(axis_column, sort=True)`(166행) 라 축 오름차순으로 나온다 |
  | ⑨ | `common_constants.DOCS_DIR` | **이미 사라짐** | `grep` 0건 (D 가 제거) |
  | ⑩ | `strategy/periods.py` 의 `제외` | **이미 사라짐** | `grep` 0건 (D 가 제거) |
  | ⑪ | `scripts/CLAUDE.md:111` | **한다** | `storage/results/<실행시각>_<검증명>/` — 계층 폴더가 빠졌다 |

- 2026-09-15 09:09: 🔴 **감사가 놓친 12번째 불일치를 찾았다 — 세 곳이 원칙 17 을 실제보다
  강하게 인용한다.** `measure/constants.py:63-64` · `src/verify_lab/CLAUDE.md:528` ·
  `tests/test_layer_contracts.py:14` 가 「**원칙 17 이 「원칙 12 의 10건」이라며 같은 값임을
  선언한다**」고 적는데, 현재 원칙 17 본문은 **「하한」이라고만 쓰고 10건도 원칙 12 도 명시하지
  않는다**(`grep -n "10건" CLAUDE.md` 가 원칙 12 의 93행 한 줄만 낸다).
  **⑦ 과 같은 뿌리**라 G-5 에서 함께 닫는다 — 원칙 17 에 그 한 구절을 넣으면 세 서술이 사실이 된다.

- 2026-09-15 09:09: **Phase 0 판정 — G-2·G-3 의 현재 실측**
  - **G-2** — `docs/INDEX.md:85` 「검증이 여덟, 매매가 여섯」은 **`src/verify_lab/CLAUDE.md:307`
    이 금지한 바로 그것**이다(「여기에 구체적인 대상 수를 적지 않습니다 — 실제로 그 숫자가 낡아
    「매매 4대상」이라고 적혀 있던 적이 있습니다」). `run_month_end_trading.py:88`
    의 「9월 7/8 · 5월 6/8 · 12월 6/8」은 `HEADLINE_MONTHS` 를 고른 **근거**다.
    🔴 **계획서가 지목한 `strategy/constants.py` 의 세 자리는 E 가 `<slug>_constants.py` 로
    옮겨 그 파일에 실측 수치가 남지 않았다** — 현재 자리는 `reverse_constants.py`(26-30·41·51-55)
    와 `option_expiry_constants.py`(21·35-39·76-77·86-88) 다
  - **G-3** — 소스 **52건 / 27파일**(계획서의 43건보다 늘었다 — A~F 가 「왜 이 구조인가」를
    과거형으로 적었다), 날짜 딱지 **6건**, 살아있는 문서 **35건 / 16파일**

- 2026-09-15 09:09: **G-3·G-4·G-5 결정 — 사용자 승인 완료.** 셋 다 추천안.
  - **G-5 (a)** — 원칙 12 문구를 코드·17 에 맞추고, **원칙 17 에 「하한은 원칙 12 의 10건」을
    명시**해 위 12번째 불일치를 함께 닫는다. 🔴 **루트 `CLAUDE.md` 수정 승인을 받았다**
  - **G-4 (a)** — 코드가 옳다(결정 ㊳ 「통계량으로 칸을 빼지 않는다」). 규칙 문서에
    「§1 은 실제로 거는 칸 · 기본 실행은 게이트를 넘은 전 칸」을 명시한다
  - **G-3 (a)** — 시제를 현재형으로 바꾸고 날짜 딱지는 지운다. **정보는 지우지 않는다**

- 2026-09-15 09:20: **Phase 1~3 완료.**
  - **Phase 1** — ①③④⑤⑧⑪ 을 고쳤다. ①⑤⑪ 은 값을 다시 적지 않고 **SoT 를 가리키게** 했다
    (`forward_return.py` · `EXPIRY_CELLS` · `src/verify_lab/CLAUDE.md` 데이터 저장 규칙).
    ⑧ 은 사실이 「축 오름차순으로 나온다」이므로 **「우선순위로 줄 세우지 않는다」**로 고쳐
    원래 뜻을 살리면서 자기모순을 없앴다. ②⑨⑩ 은 이미 해소돼 건드리지 않았다
  - **Phase 2** — 「지운다/가리킨다」는 **코드에서 셀 수 있는 값**(대상 수·칸 수·행 수·표본 하한),
    「남긴다」는 **파라미터를 고른 근거인 실측치**로 갈랐다. 후자는 전부 출처 문서를 인용하거나
    모듈 머리말이 기준을 선언하고 있어 **기간 표기를 새로 붙일 곳이 `run_month_end_trading.py` 의
    `HEADLINE_MONTHS` 주석 하나**였다.
    🔴 **계획서가 지목한 `strategy/constants.py` 의 세 자리는 E 가 옮겨 비어 있었다**
  - **Phase 3** — G-5·G-4 반영 후 G-3 을 **소스 52건 + 살아있는 문서 20건**에 적용했다.
    **문서 35건 중 15건은 남겼다** — 「~하기 전에는」(과거 서술이 아니다)과 **`spec/`·`research/` 의
    탈락안·재실행 기록**이며, 후자는 `.claude/rules/docs.md` 가 «요구»하는 것이라 지우면
    다른 규칙을 깬다. 그 경계 자체가 이 계획서의 산출물이다

- 2026-09-15 09:26: **마지막 Phase 완료.**
  - 자동 포맷 — 160 files unchanged
  - 문서 테스트 3종 선행 실행 — **112 passed**
  - 🔴 **실행 로직 변경 0건을 기계로 증명했다** — diff 에 든 `.py` 30개에 대해
    **docstring 을 제거한 AST 를 `HEAD` 와 파일별로 대조**해 전부 동일함을 확인했다.
    그래서 코드 리뷰가 「해당 없음」이고 산출물 재실행·대조도 필요 없다
  - 품질 검증 — **passed=1177, failed=0, skipped=0** (Ruff·PyRight·Pytest 전부 통과, 총 오류/경고 0)
