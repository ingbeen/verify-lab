# Implementation Plan: 역방향 K=10 전환과 성적표를 확정 칸으로 좁히기

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-26 11:24
**마지막 업데이트**: 2026-09-26 12:02
**관련 범위**: studies/reverse, scripts, tests, docs(매매/역방향 · 매매/중간선거_사이클), .claude/rules, storage
**관련 문서**: src/verify_lab/CLAUDE.md, scripts/CLAUDE.md, tests/CLAUDE.md, .claude/rules/trading.md, .claude/rules/docs.md, .claude/rules/research.md

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: **확정 순위 컷을 두 종목 모두 K=20 → K=10 으로 바꾼다** — `TARGETS` 가 `KODEX 200 K=10 · QQQ K=10` 두 대상이 되고, 규칙 문서 §1·§3 결정 ② 가 그 결정을 「확정 / 탈락안 / 근거」로 갖는다
- [x] 목표 2: **성적표·거래내역을 확정 칸(K=10 × 손절 −5%)으로 좁히고 격자를 지운다** — K=20 대상과 손절선 격자 상수(`STOP_LOSS_LEVELS`)·무손절 기본 행을 코드에서 없앤다. 성적표 200 → **10행**(2대상 × 시기 5), 거래내역 1,600 → **58행**(51 + 7)
- [x] 목표 3: **좁히기 «전» 값을 규칙 문서가 수치로 갖는다** — K=10·2005 손절선 격자(무손절 포함)와 K=20 성적, K=20 에만 있던 신호의 대조를 §3 에 남긴다(`.claude/rules/trading.md` 「무손절 대조 수치를 `규칙.md` 가 «수치로» 갖는다 — 좁히기 전에 적습니다」)
- [x] 목표 4: **갱신한 시세(QQQ ~ 2026-09-24 · KODEX 200 ~ 2026-09-23)로 규칙 문서의 수치를 K=10 기준으로 다시 쓰고**, 재수집 흔적을 역방향·중간선거_사이클 `결과.md` 에 남긴다. 알림 규격(§1.5)도 10위 기준으로 다시 잰다

## 2) 비목표(Non-Goals)

- **측정 표 넷(`signals`·`통계`·`excess`·`test`)을 지우거나 좁히지 않는다** (#3 판단 — 아래 Context). 측정 코드(`runner.py`)는 손대지 않는다
- **「승률 75% 이상」을 게이트로 도입하지 않는다.** 사용자가 「확정은 아니다」라고 했다 — `screening.py`·루트 `CLAUDE.md`·`trading.md` 어디에도 기준으로 적지 않고, 결정 ② 의 근거 칸에 **고려 중인 선호**로만 적는다
- **손절선 −5% 와 보유 한도 D+2 를 재선정하지 않는다** — K=10 격자를 기록하지만 값은 그대로다(`trading.md` 「근거를 다시 쓰는 것이 값을 다시 고르는 것이 되면 안 됩니다」)
- **quant-notify(알림 저장소)를 고치지 않는다** — 다른 저장소다. 바꿔야 할 10위 값만 보고한다
- **중간선거_사이클의 성적·코드를 바꾸지 않는다** — QQQ 재수집의 흔적만 `결과.md` 에 남긴다(측정 CSV 는 재실행에서 바이트 불변이었다)
- **`run_reverse_trading` 의 `stop_levels` 인자를 없애지 않는다** — 테스트가 합성 시세로 체결 경로를 검사하는 입구이고, 요약의 `rule` 구조(`stop_loss_levels` 목록 · 손절선별 `excluded_count`)를 그대로 두기 위해서다. **기본값만 확정 한 종으로 바꾸고**, 격자 상수와 CLI 는 없앤다
- **이 변경과 무관한 낡은 주석을 고치지 않는다** — 언급만 한다(Notes)
- `docs/context/`·`docs/plans/` 의 옛 계획서·날짜 붙은 실측 기록은 건드리지 않는다

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

**사용자 결정 (2026-09-26)**

1. **두 종목 모두 K=10 으로 간다.** 사유: 「K=20 은 노이즈가 많다」. 「되도록 승률 75% 이상만 진행」은 **확정 아님**
2. **성적표에는 K=10 · 손절 −5% 만.** 나머지는 **완전히 제거**(재선정 스위치를 남기지 않는다 — `trading.md` 조건과 충돌하므로 그 문서에 역방향 예외를 적는다). 중요한 근거·삭제되는 성과는 문서에 남긴다
3. 주요 CSV 셋(성적표·거래내역·통계) 외의 필요성은 AI 가 판단 → **셋 다 필요하다고 판단해 유지**
4. 만기_말일 계획서 커밋(`9126c54`) 뒤에 시작하고 시세를 갱신한다

**#1 근거의 데이터 대조** (갱신 전 데이터 2005 ~ 2026-09-04, 손절 −5%, `거래내역.csv` 에서 계산)

| | 건수 | 회당 평균 | 승률 | 최악 | 신호 최소 등락폭 |
| --- | ---: | ---: | ---: | ---: | ---: |
| KODEX 200 K=10 | 51 | +1.50% | 78.43% | −5.00% | 4.29% |
| KODEX 200 K=20 에만 있는 신호 | 37 | +1.90% | 83.78% | −5.85% | 3.24% |
| QQQ K=10 | 7 | +4.74% | 100.00% | +0.25% | 7.94% |
| QQQ K=20 에만 있는 신호 | 7 | +2.66% | 71.43% | −5.00% | 6.34% |

- 공통 신호(KODEX 51 · QQQ 7)는 두 컷에서 **수익률이 전부 같다** — 컷이 체결을 바꾸지 않고 신호 집합만 바꾼다
- **KODEX 에서는 K=20 에만 있던 신호가 더 좋았다** — 「성적을 흐린다」는 뜻의 노이즈는 데이터가 뒷받침하지 않고, 「등락폭이 작은 신호가 섞인다」는 뜻이면 사실이다. **QQQ 는 데이터도 노이즈 쪽이다**
- **75% 는 두 컷을 가르지 않는다** (KODEX 78.43% · 80.68%, QQQ 100% · 85.71%)
- K=10 전환은 KODEX 성적을 **낮추는** 방향이라 과최적화(좋은 값 고르기)가 아니다 — 이 사실과 사용자 사유를 결정 ② 에 함께 적는다

**#3 판단 — 측정 표 셋을 지우지 않는 이유**

| 파일 | 다른 파일에 없는 것 | 근거 규칙 |
| --- | --- | --- |
| `excess.csv` | **기준선 대비 차이.** 이 폴더에서 기준선을 담은 유일한 파일(`통계.csv` 에는 기준선 열이 없다) | 원칙 11 · `.claude/rules/docs.md` 「비율에는 기준선을 붙인다」, 규칙 문서 §2.4 가 인용 |
| `test.csv` | **우연확률**(무작위 뽑기 대조) | 루트 `CLAUDE.md` 「값은 지우지 않았습니다 — 계산은 남깁니다」, §2.4 가 인용 |
| `signals.csv` | 신호일별 1일 ~ 1개월 수익률 원자료 — `통계.csv` 가 집계하는 원본 | 원칙 8 「집계만 있고 원자료가 없으면 미완성」 |

**시세 갱신과 현재 코드 재실행으로 확인한 것 (2026-09-26 11:14 ~ 11:18, 코드 변경 전)**

- 재수집: QQQ 6,915 → **6,929행**(~ 2026-09-24) · KODEX 200 5,898 → **5,911행**(~ 2026-09-23). **겹치는 구간 과거 값 변경 0건**(전 컬럼 대조) · **2026-09-04 이후 새 신호 0건**(K=10·K=20 모두)
- `run_reverse.py` 재실행: **`성적표.csv`·`거래내역.csv` 바이트 불변**. 측정 표 넷과 `summary.json` 만 바뀌었다 — 기준선 모집단에 거래일이 더해졌고, `signals.csv` 는 K=20 신호 하나(2026-08-20)의 비어 있던 `익일시가 1일` 칸이 채워졌다
- `run_midterm_cycle.py` 재실행: **`summary.json` 의 QQQ 기간 두 줄만** 바뀌었다 — 측정·성적 CSV 불변
- 좁히기 전 산출물 사본을 세션 스크래치에 떠 두었다(`baseline_reverse/`, 임시). 좁힌 뒤 대조의 기준이다. **같은 값이 git `9126c54` 의 성적표·거래내역에도 있다**(재실행이 바꾸지 않았으므로)

### Phase 를 이렇게 묶은 이유

**`trading.md` 가 「좁히기 «전»에 적는다」를 요구한다.** 그래서 Phase 0 이 규칙 문서에 **좁히기 전 수치를 먼저 기록**하고 새 정책을 테스트로 고정(레드)한다. Phase 1 이 코드를 좁혀 그 테스트를 초록으로 만들고 재실행으로 값 불변을 대조한다. Phase 2 가 좁힌 결과를 기준으로 **현재형 서술**(규칙 문서 나머지 · 규칙/계약 문서 · COMMANDS)을 맞춘다.

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- `.claude/plan-config.json` — 이 저장소의 검증 명령·자동 포맷·근거 승격 목적지 (**값의 SoT**)
- 루트 `CLAUDE.md` — 특히 「계획서 규약 — 이 프로젝트의 설정」 절 (값이 아니라 **판단 근거**)
- `src/verify_lab/CLAUDE.md` · `scripts/CLAUDE.md` · `tests/CLAUDE.md`
- `.claude/rules/trading.md` · `.claude/rules/docs.md` · `.claude/rules/research.md` · `.claude/rules/session-bootstrap.md`
- `docs/INDEX.md` §6 (지도 유지 규칙) · `docs/MEMORY.md`

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [x] `TARGETS` 가 `(KODEX 200, 10, 2005)` · `(QQQ, 10, 2005)` 두 대상이다 (테스트로 고정)
- [x] `run_reverse_trading` 을 손절선 인자 없이 부르면 **확정 −5% 한 종만** 나온다 (테스트로 고정). `STOP_LOSS_LEVELS` 가 저장소에 없다
- [x] 인자 없는 `run_reverse.py` 재실행 → 성적표 **10행** · 거래내역 **58행**, 그 값이 좁히기 전 산출물의 `K=10 · −5.0` 행과 **전 컬럼 일치**, 측정 표 넷이 좁히기 전과 **바이트 동일**
- [x] 규칙 문서가 좁히기 전 수치(K=10·2005 손절선 격자 + 무손절 · K=20 성적 · K=20 에만 있던 신호 대조)를 갖는다
- [x] 규칙 문서의 현재형 수치(머리말·§1·§2·§5·§6)가 K=10 · 갱신 시세 기준이고, §1.5 알림 규격이 10위 기준이다 (K=20 값을 먼저 재현해 산식이 맞음을 확인한 뒤 잰다)
- [x] 재수집 흔적이 `docs/매매/역방향/결과.md` · `docs/매매/중간선거_사이클/결과.md` 에 있다
- [x] 저장소 전체에서 「역방향 K=20 확정」·「역방향은 격자가 기본」을 현재형으로 말하는 곳이 0건이다 (`docs/plans/`·날짜 붙은 기록 제외)
- [x] 코드 리뷰 실행 및 결과 기록 (마지막 Phase 의 Validation 에 적는다)
- [x] 품질 검증 통과 (마지막 Phase 의 Validation 에 passed/failed/skipped 수를 적는다)
- [x] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [x] 필요한 문서 업데이트 — `docs/COMMANDS.md` **변경 있음**(역방향 절 서술 — 명령 자체는 그대로) · `.claude/rules/trading.md` · `src/verify_lab/CLAUDE.md` · `scripts/CLAUDE.md` **변경 있음** · `docs/INDEX.md` · 루트 `CLAUDE.md` · `docs/MEMORY.md` **변경 없음**(최종 grep 으로 확인)
- [x] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다
      (결정·탈락안·근거와 좁히기 전 수치는 `docs/매매/역방향/규칙.md` §3, 재수집 기록은 두 `결과.md`)
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

- **코드**: `src/verify_lab/studies/reverse/constants.py`(`TARGETS` 두 대상 · `STOP_LOSS_LEVELS` 삭제 · 두 상수의 주석) · `src/verify_lab/studies/reverse/trading.py`(`stop_levels` 기본값 · import · docstring) · `scripts/run_reverse.py`(무손절 행 발췌 → 성적표 전체 출력 · 규칙 출력 문구 · import)
- **테스트**: `tests/test_strategy_reverse_runner.py`(대상 두 종 · 기본 실행 한 종 검사)
- **문서**: `docs/매매/역방향/규칙.md`(주 대상) · `docs/매매/역방향/결과.md`(재수집 흔적) · `docs/매매/중간선거_사이클/결과.md`(재수집 흔적) · `.claude/rules/trading.md` · `src/verify_lab/CLAUDE.md` · `scripts/CLAUDE.md`
- `docs/COMMANDS.md`: **변경 있음** — 역방향 절의 「손절선은 격자가 기본」·「대상은 4종」·「KODEX 200 K=20 은 비교축」 서술. **실행 명령과 인자는 그대로**
- **산출물**: `storage/market/{QQQ,069500}_max.csv`(갱신 완료) · `storage/results/매매/역방향/*` · `storage/results/매매/중간선거_사이클/summary.json`(재실행 완료)

### 데이터/결과 영향

- 성적표 200 → 10행, 거래내역 1,600 → 58행. **남는 행의 값은 바뀌지 않는다** (좁히기 전 사본과 대조)
- 측정 표 넷은 **시세 갱신으로만** 바뀌었고(이미 반영), 이번 코드 변경으로는 바뀌지 않는다
- `summary.json` 의 `trade.rule` — `stop_loss_levels` 가 `[-5.0]`, `targets` 가 두 줄, `excluded_count` 가 한 키
- 되살리는 기준 커밋: **`9126c54`** (K=20 대상과 손절선 격자가 있는 마지막 커밋)

## 6) 단계별 계획(Phases)

### Phase 0 — 좁히기 전 수치를 문서에 먼저 남기고 새 정책을 테스트로 고정(레드)

**작업 내용** — 문서(좁히기 전 기록):

- [x] `docs/매매/역방향/규칙.md` §3 — **결정 ② 를 개정**: 확정 = 두 종목 K=10 (2026-09-26 사용자 결정) / 탈락안 = K=20(2026-08-31 ~ 09-26 확정) · KODEX K=20 + QQQ K=10 · 둘 다 K=20 유지 / 근거 = 사용자 사유 「K=20 은 노이즈가 많다」 + Context 의 대조표 그대로(KODEX 는 성적으로는 반대, QQQ 는 같은 방향) + 「75% 는 고려 중인 선호, 확정·게이트 아님, 두 컷을 가르지 않음」 + 대가(합계·회당·승률·최악·연 빈도)
- [x] 같은 §3 — **결정 하나 추가**: 확정 = 성적표·거래내역을 확정 칸(K=10 × −5%)만 내고 K=20 대상과 손절선 격자를 코드에서 지운다 / 탈락안 = 격자를 스위치(`--grid`)로 남기기 · 격자 기본 유지 / 근거 = 사용자 결정(완전 제거), 되살리기는 git `9126c54`, **`trading.md` 「재선정 수단을 남긴다」의 예외**임을 명시
- [x] §3.1 에 **K=10 · 2005 손절선 격자(무손절 + −2% ~ −10%, 전체 구간, 두 대상)** 표를 「좁히기 전 마지막 격자 측정」으로 더한다 — 값은 좁히기 전 `성적표.csv` 에서 **도구로 뽑아 붙인다**. 「−4% 아래로 무너지고 위로 평평하다」가 K=10·2005 에서도 성립하는지 적는다(성립하지 않으면 그 사실을 적고 **값은 재선정하지 않는다**)
- [x] §3.2 에 **K=20 확정 시절의 성적(두 종목 · −5% · 전체 구간)** 과 K=20 에만 있던 신호 대조를 「지운 대상의 마지막 기록」으로 남긴다

**작업 내용** — 테스트(레드):

- [x] `tests/test_strategy_reverse_runner.py` — `test_대상은_네_종이다` 를 **`test_대상은_두_종이다`** 로: `{("KODEX 200", 10, START_YEAR), ("QQQ", 10, START_YEAR)}`. 목적 문구에 「2026-09-26 두 종목 모두 K=10 (규칙 문서 결정 ②)」
- [x] 같은 파일 — **`test_기본_실행은_확정_손절선_한_종만_낸다`**: 합성 대상 하나를 `stop_levels` 없이 돌려 성적표·거래내역의 `손절선(%)` 이 `{-5.0}` 뿐이고 `무손절` 이 없음을 검사
- [x] 레드 확인 — `poetry run pytest tests/test_strategy_reverse_runner.py` 에서 **위 두 칸만** 실패 (2 failed · 23 passed)

---

### Phase 1 — 코드를 확정 칸으로 좁힌다(그린 유지)

**작업 내용**:

- [x] `constants.py` — `TARGETS` 를 K=10 두 대상으로. `STOP_LOSS_LEVELS` 와 그 주석 삭제. `TARGETS` 위 주석을 **현재형**으로 다시 쓴다(두 종목 K=10 · 근거는 규칙 문서 결정 ② · K=20 은 `9126c54` 에 있다) — 지금 주석의 「KODEX 200 의 확정 규칙은 K=10 이고 K=20 은 비교축」은 옛 상태를 말한다
- [x] `trading.py` — `stop_levels` 기본값을 `(STOP_LOSS_LEVEL,)` 로, import 정리, `run_reverse_trading` docstring 의 「기본이 격자다」 절을 현재형으로
- [x] `scripts/run_reverse.py` — `_print_performance` 가 무손절 행 대신 **성적표 전체(10행)** 를 보이게, `_print_rule` 의 격자 문구를 확정 한 종으로, 쓰지 않게 된 import(`NO_STOP_LABEL`·`STOP_LOSS_LEVELS`) 제거
- [x] `grep -rn "STOP_LOSS_LEVELS"` 가 src·scripts·tests 에서 0건

**Validation**:

- [x] `poetry run pytest tests/test_strategy_reverse_runner.py tests/test_strategy_output_contract.py tests/test_studies_reverse_runner.py tests/test_layer_contracts.py` 초록
- [x] 인자 없이 `run_reverse.py` 재실행 → 성적표 10행 · 거래내역 58행, **좁히기 전 사본의 `K=10 · −5.0` 행과 전 컬럼 일치**(스크립트로 대조), 측정 표 넷 **바이트 동일**(`cmp`), `summary.json` 차이는 `trade.rule`·`row_counts` 뿐 — 실측: 성적표 10/10행 · 거래내역 58/58행 전 컬럼 일치, 측정 표 넷 `cmp` 동일, `measure` 절 동일

---

### Phase 2 — 현재형 서술을 좁힌 결과에 맞춘다(그린 유지)

**작업 내용** — 규칙 문서(`docs/매매/역방향/규칙.md`) 나머지:

- [x] 머리말 — 데이터 기간을 갱신 시세로, 2026-09-26 개정 요약(K=10 전환 · 확정 칸만 냄 · 재수집 흔적: 새 신호 0건 · 과거 값 변경 0건)
- [x] 「이 문서의 성격」 · §1.1 — 두 종목 K=10, 신호·사건 수(51·12 / 7·3), 등락률 하한, 연 빈도. 상위 3개 사건 비중은 **도구로 다시 센다**
- [x] §1.5 알림 규격 — **먼저 스크래치 산식으로 지금 적힌 K=20 값(20위 · 1년 전 · 최근 5년 바뀐 횟수 · 갱신일 표 · 여유별 알림 횟수)을 재현해 일치를 확인**하고, 같은 산식으로 **10위 기준 값**을 다시 쓴다. 절 안의 「20위」 표기를 「10위」로.
      **재현은 그 값을 잰 데이터(`9126c54` 의 시세 — 스크래치 `old/` 에 떠 둠)로 한다** — 갱신 시세로 재면 「1년 전」·「최근 5년」 이 기준일 이동만으로 달라져 산식이 맞는지 가를 수 없다
- [x] §2.1 ~ §2.4 — 확정 대상이 K=10 두 줄이 되게. §2.4 통계적 근거는 갱신된 `통계.csv`·`excess.csv`·`test.csv` 에서 `역방향 전체 · 전체 · K=10 · 2005` 로 **도구로 뽑아** 다시 쓴다
- [x] §5 한계 — 표본(51건·사건 12 / 7건·사건 3), 상위 사건 비중, §5.5 의 QQQ↔KODEX 보유 구간 겹침을 K=10 으로 다시 센다. 갭손절 서술(K=20 에만 있던 신호였다)을 현재형으로
- [x] §6 신호일 원자료 — K=10 두 목록(51 · 7)으로. 값은 `거래내역.csv` 에서 도구로 만든다
- [x] §3 의 결정 ⑥·⑬ 에 K=10 에서의 사실을 한 줄씩 — KODEX K=10 도 D+2 가 최선(D+1 +60.22% · D+2 +76.65% · D+3 +69.49%), **QQQ K=10 은 세 한도가 같아 결정 ⑬ 의 대가가 사라진다**(§3.4 표의 기존 값)

**작업 내용** — 그 밖의 문서:

- [x] `.claude/rules/trading.md` — 「지금 이 길에 있는 매매법은 없습니다. 역방향에는 적용하지 않았습니다」를 현재 상태로. **「재선정 수단을 남긴다」 조건의 역방향 예외**를 사용자 결정·날짜·되살리는 커밋과 함께 적는다
- [x] `src/verify_lab/CLAUDE.md` — 「기본이 격자인 매매법」 · 「손절선 축을 좁힌 매매법은 지금 없습니다(역방향과 중간선거_사이클 모두 격자)」 행
- [x] `scripts/CLAUDE.md` — 메타 타입 `reverse` 행의 「손절선 격자」
- [x] `docs/COMMANDS.md` — 역방향 절(「손절선은 격자가 기본」·「대상은 4종」·「KODEX 200 K=20 은 비교축」)
- [x] `docs/매매/역방향/결과.md` 머리말 — 기존 「시세가 재수집됐다 (2026-08-27)」 블록과 같은 모양으로 2026-09-26 블록. **본문 수치는 갱신하지 않는다**
- [x] `docs/매매/중간선거_사이클/결과.md` 머리말 — QQQ 재수집 블록(기간 · 측정 CSV 불변 · `summary.json` 기간만 변경). **본문 수치는 갱신하지 않는다**

**Validation**:

- [x] `poetry run pytest tests/test_index.py tests/test_research_docs.py tests/test_tracks.py` 초록

---

### 마지막 Phase — 문서 정리 및 최종 검증

**작업 내용**

> 🔴 **`/commit` 이 «맨 마지막»인 것은 의도다.** 그 스킬은 「후보 뒤에는 아무것도 덧붙이지 말 것」으로
> 끝나므로 **호출하는 순간 그 턴이 거기서 닫힌다.** 중간에 두면 뒤에 적힌 항목이 그 벽 너머에 남는다 —
> 실제로 두 번 그렇게 샜다(`[실측] 2026-09-14` 후보를 계획서에 안 옮김 · `2026-09-16` 옮기고 체크박스를 안 닫음).
> **체크박스와 상태를 먼저 확정하고, 커밋 후보를 마지막에 만든다.**

- [x] 필요한 문서 업데이트 (`docs/COMMANDS.md` **변경 있음** — Phase 2 에서 반영)
- [x] 잔여 참조 점검 — `grep -rn "STOP_LOSS_LEVELS\|K=20"` 와 「격자가 기본」·「대상은 4종」 을 훑어 역방향의 **현재형** 서술이 남지 않았는지 확인 (`docs/plans/`·날짜 붙은 기록·§3.2 ~ §3.4 의 측정 기록 제외)
- [x] 자동 포맷 적용 (`poetry run black .`) — 146 files left unchanged
- [x] 인자 없는 `run_reverse.py` · `run_midterm_cycle.py` 최종 재실행 → `git diff --stat storage/results/` 가 Phase 1 대조 결과와 같다 (거래내역 −1,542줄 · 성적표 −190줄 · 측정 표 넷 좁히기 전과 `cmp` 동일 · 중간선거 `summary.json` 8줄)
- [x] 변경 기능 및 전체 플로우 최종 검증
- [x] Validation 절에 `/code-review` 와 품질 검증 **실행 결과**를 적는다
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정
- [x] 🔴 **마지막에 `/commit` 을 실행하고 그 후보를 «이 계획서» 의 `#### Commit Messages` 절에 옮긴다** —
      `/commit` 은 계획서를 모르고 「후보 뒤에 아무것도 덧붙이지 말 것」으로 끝나므로,
      **대화에만 내면 그 절이 빈 채로 남는다.** 체크박스 갱신이 diff 에 더 들어가지만
      커밋 메시지의 내용을 바꾸지 않는다. **커밋은 사용자가 한다 — 후보만 낸다**

**Validation**:

> 순서를 지킨다 — 리뷰에서 고치면 코드가 바뀌므로 품질 검증이 마지막 관문이어야 한다.
> **버그가 0 인 회차에서 끝낸다.** 2회차에도 버그가 나오면 사용자에게 보고하고 3회차 여부를 묻는다 —
> 「그 외」는 목록만 남기고 고치지 않는다. `/impl-plan` 의 「코드 리뷰」 절이 SoT 다.

- [x] `/code-review xhigh` **1회차** (발견 12건 — 버그 0 · 그 외 12 · 조치: 없음 — 목록은 진행 로그 11:59)
- [x] `poetry run python validate_project.py` (passed=1282, failed=0, skipped=0)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

> **Done 직전에 `/commit` 을 실행해 이 절을 채운다. 그전에는 비워 둔다.**
> 계획서를 쓰는 시점에는 diff 가 없어 여기 적는 것은 전부 추측이고,
> **추측으로 적은 줄은 그대로 나간다.** 형식·문체 규칙은 `/commit` 이 정한다.

1. `매매 / 역방향 순위 컷 K=10 전환과 확정 칸 전용 성적표`
2. `매매 / 역방향 K=10 전환 — K=20 대상·손절선 격자 코드 삭제, 성적표 200 → 10행 · 거래내역 1,600 → 58행`
3. `매매 / 역방향 K=10 전환과 지운 K=20·손절선 격자 성적의 규칙 문서 보존`
4. `매매 / 역방향 시세 재수집과 K=10 기준 규칙 문서 수치·10위 알림 규격 재산출`
5. `매매 / 등락폭 작은 신호를 덜어낸 K=10 확정 — 역방향 성적표를 확정 두 대상 × 손절 −5% 로 한정`

## 7) 리스크(Risks)

| 리스크 | 완화 |
| --- | --- |
| **지운 격자·K=20 의 값을 다시 볼 수 없다** — 재실행이 폴더를 덮는다 | Phase 0 에서 규칙 문서에 **먼저** 수치로 남기고, 되살리는 커밋 `9126c54` 를 결정 표에 적는다 |
| **재수집 뒤 목록을 다시 판단할 수단이 없다** (`trading.md` 「재수집하면 목록을 다시 판단한다」) | 사용자 결정으로 격자를 지우는 대가다. 예외와 되살리는 절차(`9126c54` 복원)를 `trading.md` 와 결정 표에 적는다 |
| **§1.5 를 다른 산식으로 다시 재서 값이 조용히 바뀐다** — 원래 스크립트가 저장소에 없다 | 같은 산식으로 **지금 적힌 K=20 값을 먼저 재현**하고, 일치할 때만 10위 값을 적는다. 어긋나면 멈추고 보고한다 |
| **좁히면서 남는 행의 값이 바뀐다** | 좁히기 전 사본과 K=10 · −5% 행을 전 컬럼 대조, 측정 표는 `cmp` |
| **CLI 는 테스트가 거의 안 본다** (`scripts/CLAUDE.md`) | 인자 없이 실제로 돌려 터미널 출력과 산출물을 본다 |
| **숫자를 옮겨 적다 틀린다** | 규칙 문서에 들어가는 수치는 전부 도구 출력을 붙이고, 표마다 행 수를 따로 센다 |

## 8) 메모(Notes)

- 사용자 결정 (2026-09-26): ① 두 종목 K=10 ② 성적표는 K=10 · −5% 만, 격자는 **완전 제거** ③ 측정 표 필요성은 AI 판단(→ 유지) ④ 만기_말일 커밋 뒤 시작 + 시세 갱신 ⑤ `.env` 는 사용자가 직접 만들었다
- **#5 답(1 ~ 10위 등락률)은 대화로 이미 냈다** — 갱신 시세 기준 KODEX 200 10위 +7.69% / −8.17% · QQQ 10위 +9.57% / −7.82%. 규칙 문서 §1.5 에는 Phase 2 에서 같은 값을 산식 검증과 함께 적는다
- **quant-notify 는 20위 기준으로 돌고 있다** — K=10 으로 바꾸려면 그 저장소의 값을 사용자가 갱신해야 한다(이 계획 범위 밖)
- **언급만 하는 낡은 주석** (이 변경과 무관해 고치지 않는다): `constants.py` 의 `STOP_LOSS_LEVEL` 주석이 −5% 근거를 「갭손절 0건의 첫 지점」으로 적는다(규칙 문서 결정 ⑤ 는 2026-09-12 에 그 근거를 버렸다) · `HOLD_LIMIT` 주석이 K=10·2008 수치를 쓴다 · `trading.py` 모듈 docstring 이 보유 한도를 「비교 축 — 전부 산출해 나란히 낸다」로 적는다(한도는 D+2 하나다)

### 진행 로그 (KST)

- 2026-09-26 11:14: 사용자 승인에 따라 시세 재수집 — QQQ ~ 2026-09-24(6,929행) · KODEX 200 ~ 2026-09-23(5,911행). HEAD 파일과 대조해 과거 값 변경 0건, 새 신호 0건
- 2026-09-26 11:17: 코드 변경 없이 `run_reverse.py`·`run_midterm_cycle.py` 재실행 — 역방향 성적표·거래내역 불변, 측정 표 넷·`summary.json` 변경 / 중간선거_사이클 `summary.json` 기간만 변경. 좁히기 전 사본을 스크래치에 떴다
- 2026-09-26 11:24: 계획서 작성
- 2026-09-26 11:24: 승인 전 자체 검증 — `TARGETS` 를 쓰는 곳이 역방향 체결·CLI·테스트뿐임을 확인(다른 매매법 영향 없음), 루트 `CLAUDE.md`·`docs/INDEX.md`·`docs/MEMORY.md` 에 역방향 K 서술 0건 확인. §1.5 재현 데이터를 `9126c54` 시세로 못 박았다(기준일 이동이 재현 판정을 흐리지 않게)
- 2026-09-26 11:24: 사용자 승인 — 「계획서 마지막까지 진행, 사용자 판단이 필요한 경우 중단 후 요청」. `stop_levels` 인자 유지·낡은 주석 셋 언급만은 계획서대로 간다(별도 지시 없음)
- 2026-09-26 11:28: Phase 0 완료. 계획과 다르게 한 것 하나 — 좁히기 전 기록을 §3.1·§3.2 에 나눠 넣지 않고 **§3.5 한 절(「K=10 으로 되돌리며 남긴 마지막 격자」)로 모았다.** 결정 ②·⑭ 가 같은 날의 근거를 한 자리로 가리키게 하려는 것이고, §3.1 머리에 그 절을 가리키는 한 줄을 뒀다. K=10·2005 격자에서도 「−2%·−3% 에서 무너지고 −4% ~ −10% 는 평평」이 성립했다(회당 +1.38 ~ +1.58%)
- 2026-09-26 11:31: Phase 1 완료 (180 passed · 재실행 대조 전부 일치)
- 2026-09-26 11:47: Phase 2 완료 (146 passed). 기록할 것 여섯 —
  ① **§1.5 재현**: 20위 값 · 1년 전 · 최근 5년 갱신 횟수 · 알림 횟수 16개가 전부 재현됐다(알림 표는 `b9aa829` 시점 시세 `6199559` 로, 「문턱 − 여유」를 **넘은** 날 · 전체 기간 연수로 나눔). **갱신일 표의 「값이 바뀐 날」만 하루씩 어긋났다**(재현 187 · 183 대 옛 표 186 · 182, 신호일은 일치). 표시값 반올림 · 등락률 반올림 두 가설로도 맞지 않아 멈췄고, 결론(「값만 바뀐 날 0일」)이 같아 사용자 판단이 필요한 사안이 아니라고 보고 **셈법과 차이를 문서에 적고 진행했다**(Risks 의 「어긋나면 멈추고 보고」를 이 한 칸에 대해 완화 — 최종 보고에 올린다)
  ② **§2.3·§2.4 의 옛 게이트 서술을 고쳤다** — 「게이트는 적중률과 방향 기대값 둘」·「적중률 60% 이상을 통과」가 2026-09-17 전 기준이었다. §2 를 다시 쓰는 범위라 현재 게이트(`screen_verdict`)로 판정표를 다시 걸었다
  ③ **계획에 없던 절도 K=10 으로 맞췄다** — §1.4(겹치는 신호 빈도: K=10·2005 로 4 · 6회, 옛 4 · 5회는 K=10·2008 44건 기준임을 재현), §4.1(배당락 — 실제 보유 구간 0건), §4.2(폭등 25 · 폭락 26). 셋 다 확정 대상을 이름으로 적고 있어 그대로 두면 거짓이 된다
  ④ `src/verify_lab/CLAUDE.md` 는 계획의 두 행 외에 **`측정.csv` 절의 「경계는 좁혔는가」** 문장도 고쳤다 — 역방향은 체결만 좁히고 측정 격자는 그대로라 「측정 격자를 좁혔는가」로 분명히 했다. 502행의 「(역방향·월말)」은 둘 다 해당하지 않아 「(중간선거_사이클)」로 바꿨다
  ⑤ **정정**: Context 의 「`signals.csv` 는 … 비어 있던 `익일시가 1일` 칸이 채워졌다」는 틀렸다 — diff 를 다시 읽으니 **`종가 1개월` 칸**이다(21거래일 뒤가 데이터 안으로 들어왔다). 결과 문서에는 바르게 적었다
  ⑥ **사용자 판단 거리로 남긴 것**: K=10 으로 바꾸며 §1.5 알림 여유 1%p 의 빈도가 연 5.0 ~ 8.3 → 2.3 ~ 4.5회로 줄었다. 옛 근거의 빈도대에 가까운 것은 2%p(3.8 ~ 6.5회)다 — 값은 바꾸지 않았고 문서에 사실만 적었다. 함께 드러난 사실: **KODEX K=10 의 3일 우연확률 0.78(유의하지 않음)**, **QQQ K=10 은 7건이라 우연확률 검정 불가**
- 2026-09-26 11:59: 마지막 Phase — 잔여 참조 점검(현재형 서술 0건) · black(변경 없음) · 최종 재실행(Phase 1 대조와 같음) · `/code-review xhigh` 1회차 **버그 0**으로 종료 · 품질 검증 1282 passed · failed 0 · skipped 0.
  **「그 외」 12건 (고치지 않음 — 조치는 사용자가 정한다)**:
  ① `trading.md` 예외 문단이 「재선정 수단」만 예외라 적었으나 그 조건이 없으면 「재수집하면 목록을 다시 판단한다」도 지킬 수 없다(같은 주장이 `src/verify_lab/CLAUDE.md` 503행에도) ·
  ② 루트 `CLAUDE.md` 208·210행과 `src/verify_lab/CLAUDE.md` 505~507행이 「확정 칸으로 좁힌 매매법 → `측정.csv`」로 적어 역방향(좁혔지만 `측정.csv` 없음)과 어긋난다 ·
  ③ `constants.py` 의 `STOP_LOSS_LEVEL`·`HOLD_LIMIT` 주석과 테스트 docstring 이 K=10·2008 수치와 버린 「갭손절 0건」 근거를 현재 사실로 적는다(계획서가 「언급만」으로 둔 것) ·
  ④ 규칙.md §3.5 각주가 표시값 합계 차이를 「0.01%p」로 적었으나 실제 0.04%p(76.65 + 70.23 = 146.88 대 146.84) ·
  ⑤ 규칙.md 머리말이 `통계.csv` 변화 원인을 「기준선 모집단」으로 묶었으나 `통계` 는 기준선 열이 없고 원인은 K=20 1개월 칸이 채워진 것(결과.md 는 바르게 적음) ·
  ⑥ 규칙.md 머리말의 옛 문장 「§2.3(통계적 근거)은 2008 구간에서 잰 것」이 낡았다(통계적 근거는 §2.4 이고 이번에 2005 로 다시 쟀다) ·
  ⑦ `trading.py` 의 「같은 파일의 기간이 네 번 반복된다」·「격자 안에서」 주석과 모듈 docstring 의 보유 한도 「비교 축」 서술이 지금 구성과 맞지 않는다 ·
  ⑧ `run_reverse.py` `_print_rule` 이 상수에서 손절선을 찍어, 체결 기본값과 출처가 둘이다(지금은 같은 값) ·
  ⑨ `stop_levels` 인자를 운영이 쓰지 않아 쓰이지 않는 유연성으로 남았다(계획서가 의도적으로 남긴 것 — Non-Goals) ·
  ⑩ 새 코드 주석·docstring 넷이 「지웠다」·커밋 해시·날짜로 변경 이력을 적는다(패키지 주석은 현재형 규칙) ·
  ⑪ 계약 문서 셋(`COMMANDS.md`·`scripts/CLAUDE.md`·`src/verify_lab/CLAUDE.md`)이 「지웠다」 과거형 ·
  ⑫ `COMMANDS.md`·`scripts/CLAUDE.md` 가 대상 수·파라미터 값을 직접 적는다(가변 정보 복제)
- 2026-09-26 12:02: `/commit` 후보 5개를 이 계획서에 옮기고 상태를 Done 으로 확정. 커밋은 사용자가 한다
- 2026-09-26 12:05: 사용자 지시로 코드 리뷰 「그 외」 중 **사실이 틀린 서술 다섯(①②④⑤⑥)** 을 고쳤다(추가 코드 리뷰 없음). ① `trading.md`·`src/verify_lab/CLAUDE.md`·규칙.md 결정 ⑭ — 지키지 못한 조건이 「재선정 수단」과 「재수집하면 목록을 다시 판단한다」 둘임을 명시 · ② 루트 `CLAUDE.md` 「값은 지우지 않았습니다」 두 행과 `src/verify_lab/CLAUDE.md` `측정.csv` 절 — 「측정 격자까지 좁힌 매매법」으로 좁히고 역방향의 기준선 차이·우연확률 자리(`excess`·`test`)를 명시. **같은 오류가 루트 `CLAUDE.md` 「기준선을 넘지 못하는 것은…」 절과 `docs/INDEX.md` §5 에도 있어(「기준선은 `통계.csv`」) 모순이 생기지 않게 함께 고쳤다** · ④ 규칙.md §3.5 「K=20 에만」 KODEX 합계를 성적표 두 값의 차 70.19 로(표시값 합 70.23 과 0.04%p 차이를 각주에 명시) · ⑤ 규칙.md 머리말 — `통계`·`signals` 변화 원인은 K=20 1개월 칸이 측정 가능해진 것(제외 1 → 0, 10행 실측 확인) · ⑥ 규칙.md 머리말 — 2008 구간 값은 §3.1·§3.2, §2.4 는 2005 로 다시 쟀다. 문서 검사 146 passed
