# Implementation Plan: storage/results 를 git 동기화 대상으로 전환

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: 🔄 In Progress

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-06 21:48
**마지막 업데이트**: 2026-09-07 10:38
**관련 범위**: 하네스 설정, 문서 규칙, 결과·스펙·매매 규칙 문서, **인용 스캐너와 정리 스킬**
**관련 문서**: 루트 `CLAUDE.md`, `.claude/rules/docs.md`, `.claude/rules/research.md`, `.claude/rules/session-bootstrap.md`, `.claude/rules/context.md`, `tests/CLAUDE.md`, `scripts/CLAUDE.md`, `src/verify_lab/CLAUDE.md`, `docs/INDEX.md`, `docs/COMMANDS.md`

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: `storage/results/` 를 git 동기화 대상으로 전환한다. **`meta.json` 만 제외**한다
- [x] 목표 2: 「git 제외라 다른 PC 에 없다」를 전제한 규칙·지도·본문 서술을 **사실에 맞게** 고친다
- [x] 목표 3: 근거가 끊긴 결과·매매 규칙 문서 **8개**를 실재하는 산출물 폴더로 다시 겨눈다
- [x] 목표 4: 같은 현상이 되풀이되지 않게 **인용한 산출물 폴더를 지키는 규칙**을 세운다
- [x] 목표 5: **인용 스캐너를 한 벌 만들어** 두 방향에 쓴다 —
      `validate_project.py` 는 「인용됐는데 없는 폴더」를 잡고, **정리 스킬**은 「있는데 인용되지 않은 폴더」를 뽑는다.
      **두 판정이 갈라지면 정리기가 「지워도 된다」고 한 폴더를 테스트가 요구하는 상태가 되므로 정의는 한 곳에만 둔다**

## 2) 비목표(Non-Goals)

- **검증을 재실행하지 않는다. 시세를 재수집하지 않는다.** 수치가 달라지면 이 작업의 목적(근거 복원)이 무너진다
- **`docs/context/` 를 손대지 않는다.** 사용자 소유 문서이며(`.claude/rules/context.md`), 거기 적힌
  `storage/results/backtest/`·`storage/results/portfolio/` 는 **이전 프로젝트**의 경로라 이 저장소의 정책과 무관하다
- **원달러 그리드 코드를 복원하지 않는다.** 삭제는 2026-08-30 에 확정된 결정이다
- **고아 폴더 2개(`20260906_094153_kosdaq_month_end`·`20260906_094451_kosdaq_month_end`)의 처리를 미리 정하지 않는다.**
  사용자가 B PC 에서 결정한다 (Phase 3)
- 측정 방식·판정 기준·비용 반영은 건드리지 않는다

## 3) 배경/맥락(Context)

### 이 계획서만 받고 시작하는 세션에게 (B PC 진입 절차)

**이 문서 하나로 이어서 진행할 수 있게 쓰여 있다.** 이 파일이 `git pull` 로 넘어와 있다는 것은
**Phase 1·2 가 다른 PC(이하 「A PC」)에서 끝나 커밋됐다**는 뜻이다.

**진입 순서**

1. `docs/INDEX.md` 를 **`Read` 도구로** 연다 — 셸(`cat`·`head`)로 읽으면 그 경로의 규칙이 주입되지 않는다
2. 이 계획서의 아래 「현재 상태」와 **Phase 3** 을 읽는다
3. Phase 3 의 첫 명령(`ls storage/results/`)부터 실행한다

**현재 상태**

| 항목 | 상태 |
| --- | --- |
| Phase 1 (정책 전환: `.gitignore` + 서술 7곳) | **완료 · 커밋됨** |
| Phase 2 (뒤집힌 본문 서술 4곳) | **완료 · 커밋됨** |
| Phase 3 (산출물 합류 + 근거 재조준) | **조사 완료** (2026-09-07, 아래 「Phase 3 실측」). 반영은 남아 있다 |
| Phase 4 (인용 스캐너와 검증 테스트) | 남아 있다 |
| 마지막 Phase (정리 스크립트·스킬·최종 검증) | 남아 있다 |
| `storage/results/` | git 동기화 대상으로 전환됨. **`meta.json` 만 제외** |

**A PC 가 올린 산출물 폴더 11개** — 이 PC 의 `ls storage/results/` 결과와 대조해
**여기 없는 폴더가 이 PC 에만 있는 것**이고, 그것이 Phase 3 의 대상이다.

```
20260906_080223_index_extreme          20260906_094153_kosdaq_month_end
20260906_080311_option_expiry          20260906_094451_kosdaq_month_end
20260906_080320_leverage_tracking      20260906_123715_kosdaq_month_end
20260906_080353_futures_leverage       20260906_201656_kosdaq_month_end_trading
20260906_080354_usdkrw_equivalence
20260906_080406_reverse_trading
20260906_080410_expiry_trading
```

> **A PC 에는 2026-09-06 08:00 이후 실행 결과만 남아 있었다.** 그 전 폴더는 전부 지워진 뒤였고,
> 그래서 문서가 인용하던 옛 폴더 7개가 A PC 에서는 하나도 나오지 않았다.
> **이 PC 가 그 옛 폴더들의 마지막 소재지일 수 있다** — Phase 3 의 첫 명령이 그것을 확인하는 것이다.

**하지 말 것** (Non-Goals 의 재확인)

- **검증을 재실행하지 말 것. 시세를 재수집하지 말 것.** 수치가 달라지면 근거 복원이라는 목적이 무너진다
- `docs/context/` 를 손대지 말 것 — 사용자 소유 문서다
- 고아 폴더 2개를 임의로 지우지 말 것 — **사용자가 이 PC 에서 결정한다**

### 현재 문제점 / 동기

`docs/spec/`·`docs/research/`·`docs/strategy/` 의 **8개 문서가 존재하지 않는 산출물 폴더를 근거로 인용**하고 있다.
원인은 둘이고, 하나만 고치면 재발한다.

| # | 원인 | 근거 |
| --- | --- | --- |
| 1 | `storage/results/` 가 git 제외라 **다른 PC 로 넘어가지 않는다** | `.gitignore:27` |
| 2 | 재실행이 **새 폴더**를 만들고 옛 폴더는 지워지는데, 문서는 옛 폴더명을 그대로 들고 있다 | A PC 디스크에 2026-09-06 폴더만 남아 있음 |

**실측 (2026-09-06 21:48 KST, A PC)**

| 항목 | 값 |
| --- | --- |
| A PC `meta.json` 이 아는 실행 폴더 | 32개, 가장 이른 것이 `20260830_161331` |
| 문서가 인용한 옛 폴더 7개의 `meta.json` 기록 | **0건** — B PC 에서 실행된 것 |
| `storage/results` 원본 / git 저장 추정 | **86M / 약 15M** (zlib 압축 기준) |
| 그중 `20260906_080320_leverage_tracking` | 74M / **12.7M** (`windows_*.csv` 22개) |
| 최대 파일 | 4.2M (GitHub 100MB 제한과 무관) |
| 현재 `.git` | 16M |
| `storage/market`·`storage/series` 참조 66건 | **전부 실재** — 깨진 것은 `storage/results` 뿐 |

**`meta.json` 만 제외하는 이유** — `src/verify_lab/utils/meta_manager.py` 의 `save_metadata()` 가
실행할 때마다 **파일 전체를 다시 쓴다**(`_load_full_metadata()` → `json.dump()`). 산출물 CSV 는 실행 시각으로
폴더가 갈려 절대 충돌하지 않는데 이 파일 하나만 **두 PC 가 각자 실행할 때마다 충돌**한다.
게다가 `MAX_HISTORY_COUNT = 5` 로 순환 저장이라 오래된 이력은 어차피 밀려 사라지고,
값에 `/home/yblee/...` 절대경로가 들어 있어 PC 가 바뀌면 매 줄이 충돌 후보가 된다.
**근거 보존용 파일이 아니므로 제외한다.**

**사용자 확정 사항 (2026-09-06)**

| 항목 | 결정 |
| --- | --- |
| 추적 범위 | `storage/results` **전체**, `meta.json` 만 제외 |
| `windows_*.csv` 22개 | **포함** — 측정의 원칙 8(사용자가 직접 검증할 수 있어야 한다)이 원자료를 요구하고, `docs/research/레버리지_ETF_괴리.md` §8 이 직접 인용한다 |
| 고아 폴더 2개 | **B PC 에서 결정** — 「그대로 둔다」로 확정된 것이 아니다 |
| 진행 순서 | A PC 에서 할 수 있는 것부터. **마무리는 B PC** |

### 근거가 끊긴 문서 8개 (Phase 3 대상)

| 문서 | 위치 | 현재 가리키는 것 | 재생성 |
| --- | --- | --- | --- |
| `docs/research/지수_극단_이벤트.md` | :7 · :132 | `20260820_170440_index_extreme/` | 가능 |
| `docs/research/원달러_ETF_등가성.md` | :9 | `20260824_104655_usdkrw_equivalence/` | 가능 |
| `docs/research/달러_조달_방식.md` | :7 · :84 · :461 | `20260826_112619_usdkrw_grid/` | **불가 (코드 삭제)** |
| `docs/strategy/원달러_그리드.md` | :459 · :468 | `<실행시각>_usdkrw_grid/` · `_usdkrw_grid_robustness/` | **불가 (코드 삭제)** |
| `docs/strategy/역방향_매매_규칙.md` | :8 | `20260831_105540_reverse_trading/` | 가능 |
| `docs/strategy/옵션_만기일_매매_규칙.md` | :8 · :355 | `20260903_101851_expiry_trading/` | 가능 |
| `docs/research/레버리지_ETF_괴리.md` | :6 | "2026-09-05 10:32 실행" — **폴더명 없음** | 가능 |
| `docs/research/선물_대_레버리지_ETF.md` | :9 | "2026-09-05 21:59" — **폴더명 없음** | 가능 |
| `docs/spec/usdkrw_grid.md` | :78 · :130-131 | 프로브 3개 | 스크립트로 가능 |

> 뒤의 둘은 **폴더 이름 자체를 안 적고** 실행 시각만 문장으로 남겨 1차 조사에서 빠졌다.
> Phase 3 에서 나머지와 같은 형식(머리말의 `근거 산출물` 경로)으로 통일한다.

### Phase 3 실측 (2026-09-07 10:00 KST, B PC)

**이 PC 가 옛 폴더의 마지막 소재지가 맞다.** 미추적 폴더 37개가 남아 있고
**문서가 폴더명으로 인용한 8개가 전부 실재**한다. 재생성 불가라 가장 위험했던
`20260826_112619_usdkrw_grid` 도 여기 있다. 서술과 하나씩 대조했다.

| 인용 폴더 | 검산 결과 |
| --- | --- |
| `20260820_170440_index_extreme` | CSV 4개 + `summary.json` — 서술과 일치 |
| `20260824_104655_usdkrw_equivalence` | CSV **5개.** `effective_cost.csv` 가 없다 — 그 산출물이 생기기 전 실행이다 |
| `20260826_112619_usdkrw_grid` | `daily.csv` **5,340행** — 결과 문서 :84 와 정확히 일치 |
| `20260831_105540_reverse_trading` | KODEX 200 K=20 **88건** — 매매 규칙 §1 과 일치 |
| `20260903_101851_expiry_trading` | `trades.csv` **199행** — 매매 규칙 :355 와 정확히 일치 |
| 프로브 3개 | 전부 실재 |

#### 계획의 전제가 무너진 것 — 2026-09-05 실행 3건이 두 PC 어디에도 없다

「근거가 끊긴 문서 8개」 표는 레버리지·선물을 **「재생성 가능」**으로 적었으나,
**그 문서들이 근거로 삼은 실행 자체가 소실**됐다. 대역 후보를 값으로 대조했다.

| 문서 | 잃은 실행 | 대역 | 대조 |
| --- | --- | --- | --- |
| `선물_대_레버리지_ETF.md` | 09-05 21:59 | `20260906_080353_futures_leverage` (추적 중) | 표본 3,817 · 평균 34.46% · ETF 대비 0.05%p · 정수계약 2.575배 — **전부 일치** |
| `레버리지_ETF_괴리.md` | 09-05 10:32 | `20260906_080320_leverage_tracking` (추적 중) | §8.2 의 SSO 10칸 **완전 일치**, QLD 는 소수 둘째 자리만 어긋난다 |
| `옵션_만기일.md` | 09-05 22:00 | `20260831_175819_option_expiry` (미추적) | §0 판정표 **완전 일치**(KODEX 200 9월 우연확률 0.025). 09-06 실행은 0.029 라 어긋난다 |

**QLD 드리프트의 원인은 코드가 아니라 입력이다.** 커밋 `e1be856` 이 `QQQ_max.csv`·`069500_max.csv` 를
7거래일씩 재수집했다. **그 사실과 영향 범위는 이전 세션이 이미 결과 문서 본문에 기록해 뒀다**
(`docs/research/레버리지_ETF_괴리.md` 26~50행, `옵션_만기일.md` 재실행 기록).
**그래서 본문 수치는 고칠 것이 없고 폴더명만 박으면 된다.**

`20260904_091509_leverage_tracking` 은 `breakdown.csv` 1,629행이 재수집 전 값 그대로라
§5~11 의 정확한 원본이지만 **§8.2 축(`1배 수익률 분위`)이 없다.** 두 조건을 다 만족하는 산출물은 없다.

#### 복구 불가로 확정된 것

`docs/strategy/원달러_그리드.md` :468 의 `*_usdkrw_grid_robustness` 폴더는
**어느 PC 에도 없고** 산출 코드도 2026-08-30 에 삭제됐다. 경로를 지우고 사유를 적는 것이 유일한 처리다.

#### 커밋 비용 (git 중복 제거 후 실측)

| 범위 | git 증가분 |
| --- | --- |
| 미추적 37개 전부 | +22.5MB (`.git` 34M → 약 57M) |
| `leverage_tracking` 2개 제외 (35개) | +9.2MB |
| **문서가 인용·조준할 9개** | **+2.2MB** |

덩어리는 `20260904_090836`·`091509_leverage_tracking` 둘이며 `summary.json` 을 뺀 26개 파일이
**바이트 동일**이라 git 은 한 벌(13.3MB)만 저장한다. 나머지 27개는 중간 실행이고 **어떤 문서도 인용하지 않는다.**

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- 루트 `CLAUDE.md` — 특히 「계획서 규약 — 이 프로젝트의 설정」 절과 「측정의 원칙」
- `.claude/rules/docs.md` — 문서 종류와 SoT 역할, 수치에 데이터 기간을 붙이는 규칙
- `.claude/rules/research.md` — 결과 문서의 「관련 파일」 표 규칙
- `.claude/rules/session-bootstrap.md` — §6 이 이번 변경의 직접 대상
- `.claude/rules/context.md` — `docs/context/` 를 손대지 않는 근거
- `.claude/rules/strategy.md` — `docs/strategy/` 문서를 고칠 때
- `tests/CLAUDE.md` — Phase 4 에서 테스트를 추가한다면 §5(파일 격리)와 충돌 여부를 먼저 판단

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [x] `.gitignore` 가 `storage/results/meta.json` 만 제외한다
- [x] 「git 제외」를 전제한 규칙·지도·코드 서술 7곳이 사실에 맞게 고쳐졌다
- [x] 정책이 뒤집힌 본문 서술(결과·스펙 문서)이 고쳐졌다
- [x] 근거가 끊긴 문서 8개가 **실재하는 폴더**를 가리킨다 (복구 불가한 것은 그 사실을 명시)
- [x] 인용 스캐너가 **한 곳에만** 있고, 테스트와 정리 스크립트가 **같은 함수**를 쓴다
- [x] `validate_project.py` 가 「문서가 인용했는데 없는 폴더」를 실패로 잡는다
- [x] `.claude/skills/` 의 정리 스킬로 **인용되지 않은 폴더를 골라 승인 후 지울 수 있다**
- [x] 회귀/신규 테스트 추가 (또는 추가하지 않은 근거를 Notes 에 기록)
- [x] `poetry run python validate_project.py` 통과 — **passed=982, failed=0, skipped=0** (2026-09-07 11:20)
- [x] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [x] 필요한 문서 업데이트(`docs/COMMANDS.md` / CLAUDE.md / plan 등 — 각각 변경 여부 명시)
- [x] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다
      (결정 근거·실측 수치를 루트 `CLAUDE.md` 의 프로젝트 설정 절이 정한 목적지로 이관.
      `/impl-plan` 스킬 "근거 승격" 참고)
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)
- [ ] **인용 폴더 13개가 커밋됐다** — Git 은 사용자가 직접 한다(`.claude/rules/session-bootstrap.md` §7).
      **커밋 전에는 새 테스트가 이 PC 에서만 통과한다** — 다른 PC 는 pull 해도 그 폴더가 없어
      `tests/test_result_citations.py` 가 실패한다. 대상 목록은 `clean_results.py` 의
      「지킨다」 표에서 `미추적` 인 행이며, 계획서가 없어도 그 명령으로 언제든 다시 뽑을 수 있다

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

**Phase 1 — 정책 전환 (A PC · 완료)**

- `.gitignore` — 27줄 `storage/results/` → `storage/results/meta.json`, 주석 개정
- `docs/INDEX.md` — :167 표의 git 열
- `CLAUDE.md` — :417 디렉토리 트리 주석
- `.claude/rules/session-bootstrap.md` — §6 전체 (제목 포함)
- `.claude/rules/research.md` — :82
- `src/verify_lab/CLAUDE.md` — :185 표의 git 열
- `src/verify_lab/common_constants.py` — :31 주석 (**정책을 서술하는 소스 주석**)
- `tests/test_common_constants.py` — :45 docstring (같은 정책을 테스트가 계약으로 들고 있다)

**Phase 2 — 뒤집힌 본문 서술 (A PC · 완료)**

- `docs/research/지수_극단_이벤트.md` — :133
- `docs/research/연속_등락.md` — :114 · :527
- `docs/spec/index_extreme_events.md` — :530
- `docs/strategy/역방향_매매_규칙.md` — :356 (검토 후 판단)

**Phase 3 — 근거 재조준 (B PC)**

- 위 「근거가 끊긴 문서 8개」 표의 파일 전부

**Phase 4 — 인용 스캐너와 검증 테스트 (B PC)**

- `src/verify_lab/utils/result_citations.py` — **신규.** 인용 스캐너
- `tests/test_result_citations.py` — **신규.** 스캐너 단위 테스트 + 실재 검사
- `tests/CLAUDE.md` — §5 에 좁은 예외 문구

**마지막 Phase — 정리 스크립트·스킬·문서 (B PC)**

- `scripts/maintenance/clean_results.py` — **신규.** 정리 대상 목록·삭제 CLI
- `scripts/CLAUDE.md` — 폴더 역할 표에 `maintenance/` 행 추가
- `.claude/skills/clean-results/SKILL.md` — **신규.** 정리 절차 스킬
- `docs/COMMANDS.md` — **변경 있음** (아래)
- `docs/INDEX.md` — §2 규칙 문서 표에 스킬 행 추가

**`docs/COMMANDS.md`: 변경 있다** — 계획서 작성 시점에는 "변경 없음"이었으나
**정리 스크립트가 평상시 반복 실행하는 명령이 되므로** 루트 `CLAUDE.md` 의 실행 명령어 단일 관리 규칙에 걸린다.
:83·:258·:313 등의 기존 `storage/results/<실행시각>_*` 서술은 산출물이 생기는 위치를 말하는 것이라
동기화 여부와 무관하게 그대로 유효하며 손대지 않는다.

### 데이터/결과 영향

- **산출물·시세 파일의 내용은 한 글자도 바뀌지 않는다.** 추적 여부만 바뀐다
- 측정 결과·판정에 영향 없음. 검증을 다시 돌리지 않는다
- 저장소 크기: git 저장 약 **+15M** (현재 `.git` 16M → 약 31M). 클론·최초 fetch 가 그만큼 길어진다
- 이후 전체 재실행을 커밋할 때마다 비슷한 크기가 누적된다 — **재실행 결과를 무조건 커밋하지 않는다**는
  운용 습관이 필요하며, 그 근거를 Phase 1 에서 `.gitignore` 주석과 규칙 문서에 남긴다

## 6) 단계별 계획(Phases)

### Phase 1 — 정책 전환: `.gitignore` 와 규칙·지도 문서 (A PC · 완료)

**작업 내용**:

- [x] `.gitignore` 26~27줄을 고친다 — `storage/results/` 제외를 걷고 `storage/results/meta.json` 만 남긴다.
      **주석에 세 가지를 적는다**: ① 왜 동기화하는가(결과 문서가 근거로 인용한다)
      ② 왜 `meta.json` 만 빼는가(실행마다 전체 재작성 → 두 PC 충돌, 순환 저장이라 보존 가치 없음)
      ③ 재실행 산출물을 무조건 커밋하지 않는다(누적 경고)
- [x] `docs/INDEX.md` :167 — git 열 「제외 (재생성 가능)」 → 동기화. `meta.json` 예외를 같은 행에 적는다
- [x] `CLAUDE.md` :417 — 트리 주석 「검증 실행 결과 (git 제외, 재생성 가능)」 수정
- [x] `src/verify_lab/CLAUDE.md` :185 — 「데이터 저장 규칙」 표의 git 열 수정
- [x] `.claude/rules/session-bootstrap.md` §6 — **절 전체가 「넘어가지 않는다」를 전제**하므로 다시 쓴다.
      제목부터 바꾸고, 「그 PC 에서 검증을 다시 실행해야 한다」를 걷어낸다.
      **다만 「재수집하면 기존 수치가 재현되지 않는다」는 경고는 유지한다** — 그것은 git 정책과 무관하게 참이다
- [x] `.claude/rules/research.md` :82 — 「git 제외라 다른 PC 에 없고 주기적으로 지워진다」가 근거로 성립하지 않는다.
      **「관련 파일」 표에 넣지 않는다는 규칙 자체는 유지**하되(:80 이 이미 "실행 산출물 폴더는 머리말이 담당한다"고
      정했으므로 중복 방지가 진짜 이유다), 근거 문장을 그것으로 바꾼다
- [x] `src/verify_lab/common_constants.py` :31 — 「언제든 재생성 가능하므로 git 에서 제외한다」는
      **경로 상수 옆에 붙은 정책 서술**이다. 코드를 읽는 사람이 가장 먼저 보는 자리이므로 함께 고친다
- [x] `tests/test_common_constants.py` :45 — docstring 이 같은 정책을 「고정하는 계약」으로 적고 있다.
      **테스트가 검사하는 것은 경로 위치뿐**이므로 assert 는 그대로 두고 목적 문장만 고친다

**Validation**:

- [x] `grep -rn "git 제외\|git 에서 제외\|제외 (재생성 가능)" .claude/rules/ CLAUDE.md docs/INDEX.md src/ tests/`
      결과가 **0건**이거나, 남은 것이 `storage/results` 와 무관한 문맥(자격증명 `.env` 등)임을 확인한다
- [x] `git check-ignore -v storage/results/20260906_080223_index_extreme/summary.json` 이 **무시되지 않음**을 보인다
- [x] `git check-ignore -v storage/results/meta.json` 이 **무시됨**을 보인다

---

### Phase 2 — 정책이 뒤집힌 본문 서술 정리 (A PC · 완료)

> 여기서는 **근거 폴더를 다시 겨누지 않는다.** 「git 제외라서 없다」는 서술만 고친다.
> 어느 폴더를 가리킬지는 B PC 의 조사 결과가 있어야 정해진다 (Phase 3).

**작업 내용**:

- [x] `docs/research/지수_극단_이벤트.md` :133 — 「git 에서 제외돼 있으므로, 없으면 재실행」 →
      동기화 대상이 됐음을 반영. 재실행 안내는 남기되 전제를 고친다
- [x] `docs/research/연속_등락.md` :114 — 「git 제외라 남아 있지 않을 수 있고」 부분만 고친다.
      **「코드가 제거돼 재생성할 수 없다」는 그대로 유효**하고, 이 검증의 산출물은 실제로 저장소에 없다
- [x] `docs/research/연속_등락.md` :527 — 「CSV 경로를 적을 수 없다」의 사유 갱신
- [x] `docs/spec/index_extreme_events.md` :530 — 「재생성 가능, git 제외」 수정
- [x] `docs/strategy/역방향_매매_규칙.md` :356 검토 — 「`storage/results/`가 사라져도 이 절만으로
      판단을 재구성할 수 있어야 합니다」는 **동기화 후에도 유효한 원칙**이다. 고칠 필요가 없으면 그대로 두고
      판단 근거를 진행 로그에 남긴다

**Validation**:

- [x] `grep -rn "git 제외\|git 에서 제외\|git 에서 빠져" docs/research/ docs/spec/ docs/strategy/`
      결과에 남은 행이 없거나, 남았다면 그것이 옳은 서술임을 행 단위로 확인한다
- [x] Phase 2 에서 **근거 산출물 경로를 바꾸지 않았음**을 `git diff` 로 확인한다

---

### Phase 3 — 산출물 합류와 근거 재조준 (B PC — 이 계획서를 pull 받은 PC)

> **선행 조건**: Phase 1·2 가 커밋·푸시돼 있고, A PC 의 `storage/results` 가 올라가 있어야 한다.
> 이 계획서가 손에 있다면 그 조건은 이미 충족된 것이다.

**작업 내용**:

- [x] `git pull` 후 아래를 실행해 **이 PC 에만 있는 폴더**를 가려낸다.
      위 「A PC 가 올린 산출물 폴더 11개」에 없는 이름이 그것이다

      ```bash
      ls storage/results/
      git status --short --untracked-files=all -- storage/results
      ```
- [x] **커밋 범위를 사용자에게 확정받는다** (9개 / 35개 / 37개 — 위 「커밋 비용」 표).
      최소 조건은 **문서가 인용·조준하는 9개**이며, 그보다 좁으면 Phase 4 의 테스트가 A PC 에서 실패한다.
      **특히 `20260826_112619_usdkrw_grid`** 는 코드가 삭제돼 재생성이 불가능하므로 반드시 들어간다.
      **커밋 자체는 사용자가 직접 한다** — 대상 목록만 확정해 전달한다
- [x] 고아 폴더 2개(`20260906_094153`·`20260906_094451_kosdaq_month_end`)를 어떻게 할지 **사용자가 결정**한다.
      삭제하기로 하면 무엇을 지우는지 목록을 먼저 보이고 승인받는다
- [x] 폴더명으로 인용한 문서 5개는 **인용 폴더가 전부 실재하므로 본문을 고치지 않는다**(위 실측 표).
      Phase 3 의 반영은 아래 셋뿐이다
- [x] `docs/research/레버리지_ETF_괴리.md`·`docs/research/선물_대_레버리지_ETF.md` 의 머리말을
      **실행 시각 문장이 아니라 폴더 경로**로 통일한다. 겨눌 곳은 위 대역 표의 `20260906_080320`·`20260906_080353` 이고,
      **본문 수치는 갱신하지 않는다** — 재수집 영향은 두 문서가 이미 본문에 기록해 뒀다
      (`.claude/rules/docs.md` 「재실행하지 않았는데 기간만 바꾸면 문서가 거짓말을 한다」)
- [x] `docs/research/옵션_만기일.md` :10 의 「최신 `*_option_expiry` 폴더」 패턴을 **`20260831_175819_option_expiry`** 로 고정한다.
      **패턴 인용은 링크를 살려두고 값을 어긋나게 한다** — 「최신」인 09-06 실행은 우연확률 0.029 라 본문 0.025 와 맞지 않는다
- [x] `docs/strategy/원달러_그리드.md` :468 의 `*_usdkrw_grid_robustness` 경로를 지우고
      **복구 불가 사실과 이유**(2026-08-30 코드 삭제)를 적는다. :459 의 `<실행시각>_usdkrw_grid/` 는
      산출물이 생기는 위치를 말하는 자리표시자라 그대로 둔다

**Validation**:

- [x] `grep -rhoE "storage/results/[0-9]{8}_[0-9]{6}_[a-z_]+" docs/ | sort -u` 의 모든 경로가 실재한다
      (복구 불가로 명시한 것은 경로를 지웠으므로 목록에 없어야 한다)
- [x] 문서에 적힌 CSV 파일명이 해당 폴더에 실재한다

---

### Phase 4 — 인용 스캐너와 검증 테스트 (B PC)

> **판별 로직의 위치는 저장소 구조가 정한다.** `pyproject.toml` 이 `verify_lab` 만 패키지로 잡아
> **테스트는 `src` 밖을 import 할 수 없고**, `scripts/CLAUDE.md` 는 CLI 계층에 **도메인 로직 구현을 금지**한다.
> 테스트와 스크립트가 같은 함수를 쓰려면 **`src/verify_lab/utils/` 하나뿐**이다.
> `utils` 는 「횡단 관심사」이고 이 스캐너는 도메인에 의존하지 않으므로 계층 계약(「`utils`는 도메인
> 로직에 의존하지 않습니다」)을 어기지 않는다. 이미 `meta_manager.py` 가 `storage/results/` 를 다룬다.

**작업 내용**:

- [x] `src/verify_lab/utils/result_citations.py` 를 만든다. 경로는 `common_constants` 상수로만 참조한다
      - `cited_result_dirs(docs_root)` — 문서가 인용한 산출물 폴더 이름 집합
      - `existing_result_dirs(results_dir)` — 디스크에 있는 폴더 이름 집합
      - `missing_citations(...)` — **인용됐는데 없는 것.** 테스트가 쓴다
      - `unreferenced_dirs(...)` — **있는데 인용되지 않은 것.** 정리 스크립트가 쓴다
- [x] **제외 대상을 코드에 명시**한다. 둘 다 이유가 다르므로 상수로 분리하고 주석에 이유를 적는다
      - `docs/plans/` — 임시 산출물이라 곧 삭제된다
      - `docs/context/` — **사용자 소유 문서이고 거기 적힌 `storage/results/backtest/` 는 이전 프로젝트 경로다**
- [x] **자리표시자를 인용으로 세지 않는다.** `<실행시각>_usdkrw_grid/` 같은 표기는
      `COMMANDS.md`·`원달러_그리드.md` 가 「산출물이 생기는 위치」를 말하는 자리라 실재를 요구하면 안 된다
- [x] **인용 형태가 둘이다 — 접두어를 요구하지 않는다.** `storage/results/<이름>` 만 찾으면 안 되고
      **폴더 이름 단독**(`` `20260820_135545_index_extreme` ``)도 인용으로 센다.
      **`docs/spec/index_extreme_events.md` 가 후자를 쓴다** — 좁은 패턴으로 돌렸다가
      그 spec 이 인용하는 폴더 3개를 지울 뻔했고, 저장소 전수 grep 가드가 잡았다 (2026-09-07 실측).
      **넓게 잡는 쪽이 안전하다** — 헛detection 은 폴더를 더 지키게 할 뿐이다
- [x] **묘비 규칙을 둔다.** 인용과 **같은 줄**에 `없음`·`삭제`·`제거` 가 있으면 실재를 요구하지 않는다.
      낱말은 `tests/test_research_docs.py` 의 `TOMBSTONE_WORDS` 와 **같은 어휘를 쓴다** — 두 벌이 되면 갈라진다.
      실물: `docs/research/연속_등락.md` 의 `20260830_181318_index_extreme` 은 어느 PC 에도 없다
- [x] `tests/test_result_citations.py` 를 만든다
      - **가짜 저장소(`tmp_path`) 단위 테스트** — 인용 추출, 자리표시자 제외, 제외 폴더, 양방향 차집합
      - **실재 검사 1건** — 실제 `docs/` 와 `storage/results/` 를 읽어 `missing_citations()` 가 비었는지 본다
- [x] `tests/CLAUDE.md` §5 에 **좁은 예외 문구**를 넣는다. §5 의 취지는 「테스트가 실제 파일/실제 storage에
      영향을 주지 않아야 한다」이므로 **읽지도 쓰지도 않고 실재 여부만 보는 검사**는 취지와 충돌하지 않는다.
      예외를 「존재 확인」으로 한정해 적고, 파일을 열거나 쓰는 것은 계속 금지로 둔다

**Validation**:

- [x] `poetry run pytest tests/test_result_citations.py -v` 통과 (표적 테스트 — 전체 품질 검증은 마지막 Phase)
- [x] 인용 폴더 하나를 임시로 옮겨 `missing_citations()` 가 그것을 잡는지 확인하고 **되돌린다**

---

### 마지막 Phase — 정리 스크립트·스킬과 최종 검증 (B PC)

**작업 내용**

- [x] `scripts/maintenance/clean_results.py` 를 만든다. **스캐너를 호출만 하고 판별을 다시 구현하지 않는다**
      - 기본 실행: 「지키는 것 / 정리 대상」을 폴더 용량·git 추적 여부와 함께 표로 출력
      - `--delete`: **미추적 폴더만** 지운다. **추적 중인 폴더는 지우지 않고 `git rm -r` 명령을 출력만 한다** —
        전역 규칙이 「Git 처리는 사용자가 직접」이므로 스크립트가 커밋 이력을 건드리지 않는다
      - git 조회는 **읽기 전용**(`git ls-files`)만 쓴다
- [x] `.claude/skills/clean-results/SKILL.md` 를 만든다. **`writing-for-agents` 스킬을 따른다**
      - 절차: 목록 출력 → **인용 조준이 최신인지 먼저 확인** → 사용자에게 목록·용량 제시 → 승인 후 삭제 → 품질 검증
      - **「정리 대상」에 방금 쓴 문서의 근거가 섞여 있으면 멈춘다**는 경고를 넣는다.
        오늘 실측에서 조준 전 3개 폴더가 정리 대상으로 잘못 올라왔다
- [x] `scripts/CLAUDE.md` 폴더 역할 표에 `maintenance/` 행을 더한다
- [x] `docs/COMMANDS.md` 에 정리 명령을 등록한다 (**변경 있음** — Scope 참고)
- [x] `docs/INDEX.md` §2 표에 프로젝트 스킬 행을 더한다
- [x] 자동 포맷 적용 — `poetry run black .`
- [x] 변경 기능 및 전체 플로우 최종 검증
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정

**Validation**:

- [x] `poetry run python scripts/maintenance/clean_results.py` 가 인용 폴더를 「지키는 것」으로,
      나머지를 「정리 대상」으로 가른다 (조준이 끝난 뒤라 레버리지·선물·옵션 만기일 폴더가 「지키는 것」에 들어와야 한다)
- [x] `poetry run python validate_project.py` (passed=**982**, failed=**0**, skipped=**0**)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

1. 하네스 / 문서가 인용한 산출물을 스캐너 한 벌로 지키고 정리 스킬을 붙임
2. 하네스 / 결과 문서의 근거 산출물을 재조준하고 실재 검사와 정리 도구를 세움
3. 하네스 / 인용되지 않은 검증 산출물을 골라 지우는 스킬과 validate_project 검사 추가
4. 하네스 / 산출물 근거 재조준 + 인용 스캐너·정리 스킬로 재발 방지
5. 하네스 / 소실된 실행의 대역을 확정하고 산출물 인용을 기계로 지키게 함

## 7) 리스크(Risks)

| 리스크 | 크기 | 완화 |
| --- | --- | --- |
| 저장소가 약 15M 커진다 | 중 | 최대 파일 4.2M 로 GitHub 제한과 무관. **재실행 산출물을 무조건 커밋하지 않는다**는 경고를 `.gitignore` 주석과 규칙에 남긴다 |
| B PC 에 옛 폴더가 없으면 근거를 못 살린다 | 중 | 특히 `usdkrw_grid` 는 재생성 불가. 그 경우 **복구 불가 사실을 문서에 명시**하는 것이 유일한 처리이며 Phase 3 에 넣어 뒀다 |
| 재조준 과정에서 문서 수치와 새 산출물이 어긋난다 | 중 | 옛 폴더가 없으면 **대조 후 달라진 것을 적는다**. 수치를 조용히 갈아끼우지 않는다 (`.claude/rules/docs.md`) |
| 실재 검사 테스트가 `tests/CLAUDE.md` §5 와 충돌 | 중 | §5 의 취지는 「실제 storage 에 **영향을 주지 않는다**」이므로 **존재 확인만** 하는 예외를 좁게 적는다. 파일을 열거나 쓰는 것은 계속 금지 |
| 인용 폴더가 커밋 안 된 채 테스트가 들어가면 **A PC 에서만 실패**한다 | 중 | Phase 3 의 커밋 범위에 **인용 9개를 반드시 포함**한다. Phase 4 를 Phase 3 뒤에 둔 이유다 |
| 정리 스크립트가 조준 전에 돌아 **살려야 할 폴더를 지운다** | 중 | 오늘 실측에서 조준 전 3개가 정리 대상으로 잘못 올라왔다. 스킬 절차의 첫 단계를 「조준이 최신인지 확인」으로 두고, `--delete` 는 **미추적만** 지운다 |
| 스캐너 판정과 정리기 판정이 갈라진다 | 중 | 같은 함수를 쓰게 강제한다(목표 5). 스크립트에 판별을 다시 구현하지 않는 것을 Phase 체크 항목으로 둔다 |
| 두 PC 가 같은 파일을 동시에 고쳐 충돌 | 소 | 산출물 폴더는 실행 시각으로 갈려 충돌하지 않는다. `meta.json` 은 제외했다. 문서는 Phase 로 PC 를 갈라 뒀다 (A PC 는 Phase 1·2, B PC 는 Phase 3·4) |
| Phase 2 에서 근거 경로까지 손대 Phase 3 과 겹친다 | 소 | Phase 2 Validation 에 `git diff` 확인을 넣어 뒀다 |

## 8) 메모(Notes)

- **`docs/context/` 의 `storage/results/backtest/`·`storage/results/portfolio/` 는 이 저장소의 경로가 아니다.**
  이전 프로젝트의 산출물을 가리키며, 사용자 소유 문서라 손대지 않는다 (`.claude/rules/context.md`)
- `docs/research/옵션_만기일.md` :10 은 타임스탬프 대신 **`최신 *_option_expiry 폴더` 패턴**을 써서
  이번 사고를 혼자 피했다. 다만 어느 실행이 그 수치를 냈는지는 알 수 없으므로,
  동기화 후에는 **정확한 폴더명이 더 낫다** — Phase 3 에서 함께 볼 것
- `storage/results/20260906_080407_expiry_trading` 은 `meta.json` 에만 있고 디스크에 없다.
  같은 날 `080410` 이 3초 뒤에 다시 돌아 대체한 것으로 보인다. **문서가 인용하지 않으므로 이번 범위 밖**이다

### 진행 로그 (KST)

- 2026-09-06 21:48: 계획서 작성. `.gitignore`·규칙 5곳·본문 4곳·문서 8개·재발 방지의 5묶음으로 범위 확정.
  사용자 결정 반영 — 전체 추적 / `meta.json` 제외 / `windows_*.csv` 포함 / 고아 폴더는 다른 PC 에서 결정
- 2026-09-06 21:52: 자체 검증에서 **범위 누락 2건**을 찾아 Phase 1 에 넣었다 —
  `src/verify_lab/common_constants.py` :31 주석과 `tests/test_common_constants.py` :45 docstring 이
  같은 정책을 서술하고 있었다. **문서만 훑으면 코드에 박힌 정책 서술을 놓친다**는 것이 이번 교훈이라,
  Phase 1 Validation 의 grep 범위를 `src/`·`tests/` 까지 넓혔다
- 2026-09-06 21:55: **Phase 1 완료.** `.gitignore` 전환 + 7곳 갱신. 검증 셋 다 통과 —
  잔존 grep 3건은 전부 정당한 문맥(`session-bootstrap.md` §6 의 「전에는 git 제외라」 과거 서술 1건,
  `.env` 자격증명 2건)이고, `git check-ignore` 가 산출물은 `exit=1`(추적) · `meta.json` 은 `exit=0`(제외)이다
- 2026-09-06 21:56: **Phase 2 완료.** 네 곳을 고쳤고 **`docs/strategy/역방향_매매_규칙.md` :356 은
  고치지 않기로 판단**했다 — 「`storage/results/`가 사라져도 이 절만으로 판단을 재구성할 수 있어야 합니다」는
  git 정책을 말하는 문장이 아니라 **문서 자립 원칙**이고, 동기화 후에도 참이다
  (`.claude/rules/strategy.md` 「핵심 결론은 요약해 복사한다 — 그쪽이 개정돼도 이 문서가 자립해야 한다」와 같은 취지).
  Validation 2건 통과 — 본문 grep 0건, `git diff` 에서 근거 산출물 경로 변경 0건
- 2026-09-06 21:57: 표적 테스트로 상태 확인 — `test_index.py`·`test_research_docs.py`·`test_common_constants.py`
  **91 passed**. 전체 품질 검증은 규칙대로 마지막 Phase(B PC)에서 돌린다
- 2026-09-07 11:45: **정리 스크립트의 위험도 판정이 거꾸로 붙어 있어 고쳤다.** 사용자 지적으로 드러났다.
  전역 규칙의 「Git 처리는 사용자가 직접」을 `git rm` 까지로 읽어 **추적 중인 폴더의 삭제를 거부**하고
  명령만 출력하게 만들었는데, **파일을 지우는 것은 git 조작이 아니다.** 그 결과
  **되돌릴 수 있는 쪽(추적중)을 거부하고 되돌릴 수 없는 쪽(미추적)만 지우는** 상태였다.
  이제 `--delete` 는 둘 다 지우고, **삭제를 이력에 기록하는 커밋만** 사용자에게 남긴다.
  스킬·`COMMANDS.md` 의 같은 서술도 함께 고쳤다.
  **교훈은 규칙의 경계를 넓게 읽으면 안전장치가 위험한 쪽에 붙는다는 것이다**
- 2026-09-07 11:20: **Phase 4·마지막 Phase 완료.** 스캐너(`utils/result_citations.py`) 한 벌을
  테스트와 정리 스크립트가 함께 쓰고, `/clean-results` 스킬이 절차를 소유한다.
  품질 검증 **passed=982 · failed=0 · skipped=0**.
  **가드가 실제로 작동하는지 실측했다** — 인용 폴더 하나를 잠시 옮기니 테스트가 폴더명을 찍으며
  실패했고 되돌리니 통과했다. 스킬은 **user-invoked**(`disable-model-invocation: true`)로 두었다 —
  삭제가 되돌릴 수 없으므로 모델이 자율로 부를 자리가 아니고, 상시 컨텍스트 비용도 들지 않는다.
  **남은 것은 커밋 하나뿐이며 그것은 사용자의 몫이다**
- 2026-09-07 11:10: **Phase 3 반영 완료 — 문서 5곳을 고치고 미추적 24개를 지웠다** (277M → 112M).
  **조사 단계에서 스캐너 설계 요건 둘이 실측으로 나왔다.**
  ① **인용 형태가 둘이다.** 좁은 패턴(`storage/results/<이름>`)으로 정리 대상을 뽑았더니
  `docs/spec/index_extreme_events.md` 가 **폴더 이름 단독**으로 인용하는 3개
  (`20260820_135545`·`162628`·`162937_index_extreme` — 반올림 자릿수 전환 대조의 REF/MID/NEW)가
  삭제 목록에 올라왔다. **저장소 전수 grep 가드가 잡았다.** 스캐너는 접두어를 요구하면 안 된다
  ② **묘비 규칙이 필요하다.** 넓은 패턴으로 다시 돌리니 **9번째 끊긴 인용**이 나왔다 —
  `docs/research/연속_등락.md` 의 `20260830_181318_index_extreme` 은 어느 PC 에도 없다.
  그 문서 §3.2 는 「이 검증의 산출물은 저장소에 없다」고 적고 있었는데 **5장 것은 실재**해서
  그것도 함께 고쳤다. 없는 인용은 같은 줄에 `없음` 을 달아 묘비로 표기했다
  ③ **§12B 가 두 실행에 걸쳐 있다는 것도 이때 드러났다** — `옵션_만기일.md` 가 손절 격자를
  「최신 `*_expiry_trading`」으로 가리켰는데 12B.1~5 는 08-31 실행(140행), 12B.7 은 700행 실행이다.
  둘 다 폴더명으로 고정했다. **패턴 인용이 감추고 있던 것이다**
- 2026-09-07 10:38: **Phase 3 조사 완료 + 사용자 요구로 Phase 4 를 재설계했다.** 셋을 고쳤다.
  ① **계획의 전제 하나가 무너졌다** — 레버리지·선물을 「재생성 가능」으로 적었으나 **그 문서들이 근거로 삼은
  2026-09-05 실행 3건이 두 PC 어디에도 없다.** 대역 후보를 값으로 대조해 Context 에 표로 남겼다.
  다행히 **본문 수치는 고칠 것이 없다** — 재수집 영향을 이전 세션이 이미 결과 문서에 기록해 뒀다.
  ② **`docs/COMMANDS.md` 가 「변경 없음」에서 「변경 있음」으로 바뀌었다** — 정리 스크립트가 평상시
  반복 실행하는 명령이 되어 실행 명령어 단일 관리 규칙에 걸린다.
  ③ **재발 방지 후보 A/B/C 를 걷어내고 확정 설계로 바꿨다** — 사용자가 「스킬로 정리 + validate_project 로 검증」을
  지정했다. **판별 로직의 위치는 취향이 아니라 구조가 정했다**: `pyproject.toml` 이 `verify_lab` 만
  패키지로 잡아 테스트가 `src` 밖을 import 할 수 없고, `scripts/CLAUDE.md` 가 CLI 에 도메인 로직을
  금지하므로 **둘이 공유할 수 있는 자리가 `src/verify_lab/utils/` 하나뿐**이다.
  Phase 를 「스캐너+테스트」와 「스크립트+스킬+문서」로 갈랐다 — 앞이 뒤의 선행 조건이다
- 2026-09-06 22:02: **B PC 단독 진행이 가능하도록 자립화했다.** Context 맨 앞에 「이 계획서만 받고 시작하는
  세션에게」를 넣어 진입 순서·현재 상태·A PC 가 올린 폴더 11개 목록·하지 말 것을 담았고,
  Phase 표기를 「이 PC / 다른 PC」에서 **A PC / B PC** 로 바꿨다 — 읽는 쪽이 바뀌면 두 말이 뒤집힌다.
  Phase 3 첫 항목에 실행할 명령을 직접 넣었다

---
