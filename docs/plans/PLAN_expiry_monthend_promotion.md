# Implementation Plan: 만기_말일 매매 승격과 두 매매법 조사 강등

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-22 12:41
**마지막 업데이트**: 2026-09-22 15:05
**관련 범위**: tracks, studies/expiry_monthend, studies/option_expiry, studies/month_end, scripts, tests, docs
**관련 문서**: 루트 `CLAUDE.md`, `src/verify_lab/CLAUDE.md`, `scripts/CLAUDE.md`, `tests/CLAUDE.md`, `.claude/rules/{trading,docs,research}.md`, `docs/INDEX.md`, `docs/MEMORY.md`

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: **`만기_말일` 을 매매 등급으로 승격**하고 그 근거물인 `규칙.md` 를 갖춘다 — 확정 규칙은 **9월 · 셋째 금요일 종가 매수 → 그 달 마지막 거래일 종가 매도 · 아래 방향 · SPY · DIA · KODEX 코스닥150 · 손절 −5%** 다
- [x] 목표 2: **`옵션_만기일` · `월말_진입` 을 조사 등급으로 강등**하고 실행 코드를 제거한다 — 두 매매법의 확정 칸이 `만기_말일` 의 C1·C4 에 그대로 포함되므로 코드가 중복이다
- [x] 목표 3: **`만기_말일` 의 기본 실행을 확정 칸으로 좁힌다** — 산출물이 사용자가 실제로 보는 범위만 담고, 격자는 스위치로 되살린다
- [x] 목표 4: 두 매매법이 사라지면서 **공유 계층의 3벌 중복(`_aggregate_by_month` 등)이 1벌이 되는 것**을 확인하고, 규칙 문서의 낡은 현재형 서술을 정리한다

## 2) 비목표(Non-Goals)

- **손절선 −5% 를 재선정하지 않는다.** 두 매매법이 이미 확정한 값을 그대로 쓴다 (`.claude/rules/trading.md` 「근거를 다시 쓰는 것이 값을 다시 고르는 것이 되면 안 된다」)
- **12월을 다시 재지 않는다.** 사용자가 배당 불확실성을 이유로 제외하기로 정했다
- **다른 달·다른 조합을 새로 탐색하지 않는다.** 9월·12월은 사후 선택이고 그 한계는 그대로 남는다
- **종합지수를 대상에 넣지 않는다.** 설계 결정 ④ 가 상관 0.8924 를 근거로 이미 배제했다
- **인버스 실물 축을 `만기_말일` 로 옮기지 않는다.** 코스닥150 −2배는 ETN 뿐이고 2022-10 상장이라 표본 3건이며, 배당락은 이번 실측에서 **10/10 전수 측정**으로 이미 답이 나왔다
- **`measure/calendar_entry.py`·`calendar_exit.py` 를 `studies/expiry_monthend/` 로 되돌리지 않는다.** 깨지지 않은 코드를 옮기지 않는다 (전역 「수술적 변경」)
- **비용(수수료·슬리피지·세금)을 반영하지 않는다.** 맨몸 성적이 기본이다
- **투자금 결정을 이 계획서에서 하지 않는다.** 새 매매법의 투자금은 `docs/조사/투자금_결정/규칙.md` §1 의 절차로 별도 판단한다
- **git 작업을 하지 않는다.** 커밋 후보만 낸다

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- `만기_말일` 검증이 **두 매매법을 2×2 로 교차**해 재 봤고, **C2(만기→말일)가 9월에서 네 조합 중 유일하게 ETF 4종 전부 게이트를 넘었다.** 「이 조합만 다른 해」도 C4 대비 2~3배 많아 **표본이 실재하는 조합**이다
- 사용자가 결정을 넷 내렸다 — **12월 제외**(배당 불확실성) · **C2 채택** · **KODEX 200 제외**(코스닥150 과 상관 0.8919 라 분산이 아님) · **코스닥150 채택**
- 그 결과 `옵션_만기일`(C1)과 `월말_진입`(C4)은 **걸지 않게 된다.** 두 매매법의 확정 칸이 `만기_말일` 의 조합에 그대로 포함되므로 실행 코드가 중복이고, `src/verify_lab/CLAUDE.md` 가 **「`_aggregate_by_month` 3벌이 98% 동일하고 지금은 위험이 문서로만 막혀 있다」**고 적어 둔 상태다
- `만기_말일` 은 **`규칙.md` 가 없다.** 매매 승격의 실체가 그 문서이므로 승격 전에 먼저 써야 한다

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- `.claude/plan-config.json` — 이 저장소의 검증 명령·자동 포맷·근거 승격 목적지 (**값의 SoT**)
- 루트 `CLAUDE.md` — 「측정의 원칙」·「후보 판정 기준」·「계획서 규약」
- `src/verify_lab/CLAUDE.md` — 「매매법 이름 계약」·「매매 산출물 계약」·「`측정.csv`」·「실행 요약」
- `scripts/CLAUDE.md` — CLI 계층 책임, 메타 타입 목록, 실행 명령어 등록
- `tests/CLAUDE.md` — 테스트 작성 규칙
- `.claude/rules/trading.md` — 「확정된 칸만 내는 것은 사용자의 결정」·손절 대원칙·배당 필수 항목
- `.claude/rules/docs.md` — 등급 축 배치, 과거형이 허용되는 자리, 데이터 기간 표기
- `.claude/rules/research.md` — 결과 문서 필수 구조, 관련 파일 표
- `docs/INDEX.md` — 이름표와 문서 지도 (등록 누락 시 `tests/test_index.py` 실패)
- `docs/MEMORY.md` — 「이름만 바꾸는 변경과 값을 바꾸는 변경을 한 커밋에 섞지 않는다」

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [x] 기능 요구사항 충족 — 등급 3줄 전환 · `규칙.md` 신설 · 확정 칸 축소 · 두 매매법 코드 제거
- [x] 회귀/신규 테스트 추가 (확정 칸 계약 · 산출물 파일 이름 · 계약 테스트의 매매법 목록 축소)
- [x] 코드 리뷰 실행 및 결과 기록 (마지막 Phase 의 Validation 에 적는다)
- [x] 품질 검증 통과 (마지막 Phase 의 Validation 에 passed/failed/skipped 수를 적는다)
- [x] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [x] 필요한 문서 업데이트(`docs/COMMANDS.md` / CLAUDE.md / INDEX.md / MEMORY.md — 각각 변경 여부 명시)
- [x] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다
      (결정 근거·실측 수치를 `docs/` 의 살아있는 문서로 이관)
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

**코드**

- `src/verify_lab/tracks.py` — 3줄 (`expiry_monthend` 승격, `option_expiry`·`month_end` 강등 + `kind` 전환)
- `src/verify_lab/studies/expiry_monthend/constants.py` — 확정 칸 신설, 산출물 이름 `통계.csv` → `측정.csv`
- `src/verify_lab/studies/expiry_monthend/{runner,trading}.py` — 칸 기반 순회
- `scripts/run_expiry_monthend.py` — 기본값 축소, `--grid` 신설
- **삭제**: `src/verify_lab/studies/option_expiry/`, `src/verify_lab/studies/month_end/`, `scripts/run_option_expiry.py`, `scripts/run_month_end.py`

**테스트**

- **삭제**: `tests/test_studies_option_expiry_{offsets,runner}.py`, `tests/test_studies_month_end_runner.py`, `tests/test_strategy_month_end_runner.py`
- **수정**: `tests/test_layer_contracts.py`, `tests/test_strategy_output_contract.py`, `tests/test_index.py`, `tests/test_tracks.py`
- **추가**: `tests/test_studies_expiry_monthend_runner.py` 에 확정 칸 계약

**문서**

- **신설**: `docs/매매/만기_말일/규칙.md`
- **이동**: `docs/검증/만기_말일/` → `docs/매매/만기_말일/` · `docs/매매/옵션_만기일/` → `docs/조사/옵션_만기일/` · `docs/매매/월말_진입/` → `docs/조사/월말_진입/`
- **수정**: `docs/INDEX.md`, `docs/MEMORY.md`, 루트 `CLAUDE.md`, `src/verify_lab/CLAUDE.md`, `scripts/CLAUDE.md`, `.claude/rules/{trading,docs}.md`, 세 매매법의 `설계.md`·`결과.md`·`규칙.md` 안의 상호 링크
- `docs/COMMANDS.md`: **변경 있음** — `run_option_expiry.py`·`run_month_end.py` 항목 삭제, `run_expiry_monthend.py` 의 기본값 설명 갱신

**산출물**

- **이동**: `storage/results/검증/만기_말일/` → `storage/results/매매/만기_말일/` · `storage/results/매매/{옵션_만기일,월말_진입}/` → `storage/results/조사/{옵션_만기일,월말_진입}/`

### 데이터/결과 영향

- **Phase 2 까지는 값이 바뀌지 않는다.** 폴더만 옮겨지고 파일 내용은 바이트 동일해야 한다
- **Phase 3 에서 `만기_말일` 산출물이 줄어든다** — `통계.csv` 48행 → `측정.csv` 4행 · `성적표.csv` 800행 → 35행 · `거래내역.csv` 3,872행 → 158행 (예상치이며 실행으로 확정한다)
- **Phase 4 에서 두 매매법의 산출물은 «다시 생성되지 않는다»** — 조사 폴더에 마지막 실행 결과가 그대로 남고, 재현은 git 이력에서 코드를 꺼내야 한다. `원달러_그리드` 가 같은 상태다
- **시세를 재수집하지 않는다.** 재수집하면 모든 수치가 바뀐다

## 6) 단계별 계획(Phases)

### Phase 0 — 계약을 테스트로 먼저 고정(레드)

> 확정 칸 축소는 **최종 산출물이 달라지는 변경**이고, 등급 전환은 **산출물이 쌓이는 자리가 달라지는 변경**이다. 둘 다 어긋나도 예외가 나지 않으므로 먼저 고정한다.

**작업 내용**:

- [x] `tests/test_studies_expiry_monthend_runner.py` 에 **확정 칸 계약** 추가 (레드 허용)
      — `TRADING_CELLS` 가 **4칸**이고 전부 `c2` · 9월 · 아래 방향이며, 대상이 SPY · DIA · `229200` · `2203` 이다.
      **격자 상수(`DATASETS` 6 · `COMBOS` 4 · `MONTHS` 둘)를 지우지 않는다**는 계약도 함께 고정했다
- [x] 같은 파일에 **산출물 이름 계약** 추가 — `OUTPUT_FILES` 의 값이 `MEASURE_FILENAME`(`측정.csv`) 이다
- [x] 같은 파일의 `test_방향_컬럼을_두지_않는다` 를 **`test_방향이_식별_컬럼으로_붙는다`** 로 바꿨다 (아래 진행 로그의 설계 조정)
- [x] `tests/test_tracks.py` 의 `TestGradeSemantics` 를 새 결정으로 교체 — `test_만기_말일이_매매_등급이다` ·
      `test_걸지_않기로_한_매매법은_조사_등급이다`(두 slug). **`kind` 전환은 기존 `test_조사_등급은_종류도_조사다` 가 이미 강제**하므로 추가하지 않았다
- [x] **역방향·만기_말일 산출물의 기준 사본**을 스크래치패드에 떴다 (83파일). Phase 2·3·4 의 바이트 대조에 쓴다

**Validation**:

- [x] 의도한 레드를 확인했다 — `tests/test_tracks.py` **3 failed / 45 passed**(등급 3건) ·
      `tests/test_studies_expiry_monthend_runner.py` 는 `TRADING_CELLS` 미구현으로 **수집 단계 ImportError**

---

### Phase 1 — `규칙.md` 작성 (문서만, 코드 변경 없음)

> **승격의 근거물이 먼저다.** 등급을 먼저 바꾸면 근거 없이 승격된 순간이 생긴다.
> 지금 폴더(`docs/검증/만기_말일/`)에 만들고 Phase 2 에서 함께 옮긴다.

**작업 내용**:

- [x] `docs/검증/만기_말일/규칙.md` 신설 — 구성은 `.claude/rules/trading.md` 「문서 구성」의 §1~§5
- [x] **§1 확정 규칙** — 9월 · 셋째 금요일 종가 매수 → 그 달 마지막 거래일 종가 매도 · **아래 방향** · SPY · DIA · KODEX 코스닥150 · 손절 **−5%**(진입가 기준 고정) · 알림 없음.
      **2026년 실행 날짜를 달력 코드로 산출해 적는다**(추정 금지). **집행 상품과 배수 전제**(미국 3배 가능 · 코스닥은 −2배 ETF 가 없어 **ETN**)를 명시한다
- [x] **§2 이 규칙의 성적** — 확정 손절선 20행과 무손절 15행을 산출물에서 그대로 옮긴다. **데이터 기간을 대상별로 적는다**
- [x] **§3 확정된 결정** — 「확정 / 탈락안 / 근거」 형식으로 넷:
      ① **12월 제외** (배당 권리로 부분 설명되나 지수에서도 같은 모양이 나와 원인 미확정 · 12월 미국은 네 조합 전부 게이트 아래)
      ② **C2 채택** (9월 4/4 통과 · 「이 조합만 다른 해」가 C4 대비 2~3배 · **C4 는 12월 한국에서 독립 관측 0**)
      ③ **KODEX 200 제외** (코스닥150 과 상관 0.8919 라 분산이 아님. **남길 쪽을 성과로 고른 대가**를 함께 적는다 — 표본 10 대 23 · 코스닥 성과의 92%가 뒤 8해 · ETN 집행)
      ④ **손절선 −5% 를 재선정하지 않음** (두 매매법이 확정한 값)
- [x] **§4 한계** — 사후 선택된 달 · 조합 붕괴 · 표본 10~33건 · 미국 둘 상관 0.965 · 코스닥 ETF 표본이 전부 2016년 이후 · 비용 미반영 · ETN 신용위험
- [x] **§5 원자료** — 신호일 목록의 자리(`storage/results/<등급>/만기_말일/거래내역.csv`)와 **배당락 검토 결과**(SPY 33/33 · DIA 28/28 · 코스닥150 10/10, 걸림 전부 0, 평균 왜곡 0.0014%p 이하)
- [x] `docs/검증/만기_말일/결과.md` 의 「관련 파일」 표에서 **「도출된 매매 규칙 | 없음」 행을 `규칙.md` 로** 바꾼다
- [x] `docs/INDEX.md` §3 에 `규칙.md` 등록

**Validation**:

- [x] `poetry run pytest tests/test_index.py tests/test_research_docs.py` 통과 (링크 실재·등록 대조)

---

### Phase 2 — 등급 전환과 폴더 이동 (값 불변)

> 🔴 **이 Phase 는 값을 바꾸지 않는다.** `docs/MEMORY.md` 의 「이름만 바꾸는 변경과 값을 바꾸는 변경을 한 커밋에 섞지 않는다」를 그대로 따른다 — 섞으면 diff 가 「재실행이 숫자를 바꿨는가」를 말하지 못한다.

**작업 내용**:

- [x] `src/verify_lab/tracks.py` 3줄 —
      `expiry_monthend` → `GRADE_TRADING` · `option_expiry` → `GRADE_SURVEY` + `KIND_PROPERTY` · `month_end` → `GRADE_SURVEY` + `KIND_PROPERTY`.
      **강등 두 줄에는 「실행 코드를 지웠다」는 이유를 `원달러_그리드` 와 같은 형식으로 주석에 남긴다**
- [x] 문서 폴더 이동 3건 (`git mv` 가 아니라 파일 이동 — git 작업은 사용자가 한다)
- [x] 산출물 폴더 이동 3건
- [x] **저장소 전수 링크 수정** — `docs/매매/옵션_만기일/` · `docs/매매/월말_진입/` · `docs/검증/만기_말일/` 을 가리키는 모든 경로. 대상은 세 매매법 문서끼리의 상호 링크, `docs/INDEX.md`, 루트·`src`·`scripts` 의 `CLAUDE.md`, `.claude/rules/*.md`, 코드 주석
- [x] `docs/INDEX.md` §3 이름표의 등급 3줄과 문서 경로 갱신

**Validation**:

- [x] `poetry run pytest tests/test_tracks.py tests/test_index.py tests/test_research_docs.py` 통과
- [x] 세 스크립트를 **인자 없이** 재실행하고 `git status` 로 확인 — **새 자리의 파일이 옛 자리의 파일과 바이트 동일**해야 한다 (`summary.json` 만 예외: `row_counts` 의 키가 파일 이름이라 이름이 바뀌면 내용이 바뀐다 — 이 Phase 에서는 파일 이름을 바꾸지 않으므로 이것도 동일해야 한다)

---

### Phase 3 — `만기_말일` 확정 칸 축소 (값 변경)

> `.claude/rules/trading.md` 「확정된 칸만 내는 것은 사용자의 결정」의 두 조건을 지킨다 —
> **근거는 `규칙.md` §3 이 갖고**(Phase 1 에서 작성), **재수집하면 목록을 다시 판단한다**(그 문장을 `규칙.md` §4 에 적는다).
> **손절선 축은 좁히지 않는다** — 확정 −5% 와 무손절 대조 두 종이 그대로 나오므로 그쪽 조건 셋은 해당이 없다.

**작업 내용**:

- [x] `constants.py` 에 **`Cell`** 과 **`TRADING_CELLS`**(4칸) 신설 — 대상 · 조합 · 달 · 방향을 한 줄에 담는다.
      **`DATASETS`(6)·`COMBOS`(4)·`MONTHS`(9·12)는 그대로 둔다** — 지우면 재판단 수단이 사라진다
- [x] `OUTPUT_FILES` 의 값을 `STATISTICS_FILENAME` → **`MEASURE_FILENAME`** 으로 바꾸고, 모듈 주석의 「좁히지 않은 격자라 `통계.csv` 다」를 현재 사실로 고친다
- [x] **`측정.csv` 에 `방향` 컬럼을 앞쪽 식별 컬럼으로 넣는다** — `src/verify_lab/CLAUDE.md` 「`측정.csv`」 계약이 `종목 · <매매법 축> · 방향` 을 요구한다. 성적표의 `시기 = 전체` 행과 **1:1 로 조인**되어야 하기 때문이다.
      🔴 **값은 1배 롱 기준 그대로 두고 부호를 뒤집지 않는다** — 뒤집으면 `기준선 오른 비율` 이 실제로는 내린 비율을 가리켜 이름이 거짓이 된다
- [x] `runner.py`·`trading.py` 가 **칸 목록을 받아** 순회하게 바꾼다. 측정은 칸의 (대상 · 조합 · 달) 세 축만 쓰고 방향을 모른다
- [x] `scripts/run_expiry_monthend.py` — **기본값을 `TRADING_CELLS` 로.** 축을 넓히는 수단은 이미 있는 `--ticker`·`--combo`·`--month` 셋이고 **격자 스위치를 새로 두지 않는다**(진행 로그의 설계 조정)
- [x] `scripts/CLAUDE.md` 메타 타입 목록의 `expiry_monthend` 행을 **확정 칸**이 드러나게 갱신
- [x] `docs/COMMANDS.md` — 기본값 설명 갱신 (확정 칸 넷 · 축을 넓히는 세 인자)

**Validation**:

- [x] Phase 0 의 확정 칸·파일 이름 계약 테스트가 **그린**
- [x] 인자 없이 재실행해 행 수를 확인하고 **이 계획서의 예상치와 대조** — 어긋나면 원인을 Notes 에 적는다
- [x] **축을 넓혀 재실행하면 격자가 되살아나는지** 확인 — `--ticker` 여섯 · `--combo` 넷 · `--month` 둘로 돌려 Phase 2 시점의 측정 48행이 나오는지 본다 (파일 이름은 `측정.csv` 로 바뀐 채이고, 성적표는 방향이 「아래」로 고정돼 400행이다)
- [x] **역방향 산출물이 바이트 불변**인지 확인 (공유 계층을 건드렸다면 그 신호가 여기 나온다)

---

### Phase 4 — 두 매매법 코드 제거

**작업 내용**:

- [x] `src/verify_lab/studies/option_expiry/` · `src/verify_lab/studies/month_end/` 삭제
- [x] `scripts/run_option_expiry.py` · `scripts/run_month_end.py` 삭제
- [x] 전용 테스트 4개 삭제, 계약 테스트 2개(`test_layer_contracts.py` · `test_strategy_output_contract.py`)의 매매법 목록을 **역방향 · 만기_말일 둘로** 축소
- [x] `scripts/CLAUDE.md` 메타 타입 목록에서 `option_expiry` · `month_end` 행 삭제 + **왜 빠졌는지**를 `원달러_그리드` 와 같은 형식의 인용 블록으로 남긴다
- [x] `docs/COMMANDS.md` 에서 두 스크립트 항목 삭제
- [x] 두 `결과.md` 에 **복원 절차**를 적는다 (`조사/연속_등락/결과.md` §8 과 같은 형식) — 코드를 되살리려면 git 이력의 어느 지점을 보는가
- [x] **`_aggregate_by_month` 3벌 중복이 1벌이 된 것을 확인**하고, `src/verify_lab/CLAUDE.md` 의 「지금은 위험이 문서로만 막혀 있습니다」 문단을 현재 사실로 고친다

**Validation**:

- [x] `poetry run pytest tests/` 전체 그린 (삭제한 모듈을 import 하는 곳이 남아 있으면 여기서 드러난다)
- [x] 역방향·만기_말일 산출물이 **바이트 불변**

---

### 마지막 Phase — 문서 정리 및 최종 검증

**작업 내용**

> 🔴 **`/commit` 이 «맨 마지막»인 것은 의도다.** 그 스킬은 「후보 뒤에는 아무것도 덧붙이지 말 것」으로
> 끝나므로 **호출하는 순간 그 턴이 거기서 닫힌다.** 중간에 두면 뒤에 적힌 항목이 그 벽 너머에 남는다 —
> 실제로 두 번 그렇게 샜다(`[실측] 2026-09-14` 후보를 계획서에 안 옮김 · `2026-09-16` 옮기고 체크박스를 안 닫음).
> **체크박스와 상태를 먼저 확정하고, 커밋 후보를 마지막에 만든다.**

- [x] 필요한 문서 업데이트 (`docs/COMMANDS.md` **변경 있음** — Phase 3·4 에서 반영)
- [x] **규칙 문서의 현재형 서술 정리** — `.claude/rules/trading.md` · `src/verify_lab/CLAUDE.md` · `docs/MEMORY.md` · 루트 `CLAUDE.md` · `docs/INDEX.md` 에서 두 매매법을 「지금 이렇게 한다」로 적은 문장.
      🔴 **과거 실측 기록과 설계 결정은 그대로 둔다** — `.claude/rules/docs.md` 「과거형이 허용되는 자리」가 그 경계다
- [x] 자동 포맷 적용 (`poetry run black .`)
- [x] 변경 기능 및 전체 플로우 최종 검증
- [x] Validation 절에 `/code-review` 와 품질 검증 **실행 결과**를 적는다
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정
- [x] 🔴 **마지막에 `/commit` 을 실행하고 그 후보를 «이 계획서» 의 `#### Commit Messages` 절에 옮긴다**

**Validation**:

> 순서를 지킨다 — 리뷰에서 고치면 코드가 바뀌므로 품질 검증이 마지막 관문이어야 한다.
> **버그가 0 인 회차에서 끝낸다.** 2회차에도 버그가 나오면 사용자에게 보고하고 3회차 여부를 묻는다 —
> 「그 외」는 목록만 남기고 고치지 않는다. `/impl-plan` 의 「코드 리뷰」 절이 SoT 다.

- [x] `/code-review xhigh` **1회차** (발견 13건 — 버그 6 · 그 외 7 · 조치: 버그 6 전부 + 그 외 6건.
      **고치지 않고 목록만 남긴 것 1건**은 아래 「남긴 지적」이다)
- [x] `poetry run python validate_project.py` (passed=1239, failed=0, skipped=0)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

> **Done 직전에 `/commit` 을 실행해 이 절을 채운다. 그전에는 비워 둔다.**
> 계획서를 쓰는 시점에는 diff 가 없어 여기 적는 것은 전부 추측이고,
> **추측으로 적은 줄은 그대로 나간다.** 형식·문체 규칙은 `/commit` 이 정한다.

1. `매매 / 만기_말일 승격과 옵션_만기일·월말_진입 조사 강등`
2. `매매 / 만기_말일 9월 아래 확정 칸 축소와 두 매매법 코드 제거`
3. `매매 / 세 매매법을 만기_말일 하나로 통합하고 확정 규칙 문서 신설`
4. `매매 / 확정 칸 기반 기본 실행 전환과 중복 실행 코드 정리`
5. `매매 / 만기_말일 매매 등급 전환과 산출물 측정.csv 단일화`

## 7) 리스크(Risks)

| 리스크 | 완화 |
| --- | --- |
| **링크가 조용히 깨진다** — 세 폴더가 동시에 옮겨지고 상호 참조가 많다 | `tests/test_research_docs.py` 가 마크다운 링크와 인라인 경로의 실재를 검사한다. Phase 2 Validation 에서 먼저 돌린다 |
| **폴더 이동이 값 변경으로 오인된다** | Phase 2 를 값 불변으로 고정하고 바이트 대조를 Validation 에 넣었다. `summary.json` 은 파일 이름을 바꾸는 Phase 3 에서만 바뀐다 |
| **코드 제거로 재현 수단이 사라진다** | 두 매매법의 확정 칸은 `만기_말일` 의 C1·C4 에 포함되고, 그 밖의 축(만기월 12개·진입일 격자)은 **지금 코드로도 이미 안 나온다.** 복원 절차를 두 `결과.md` 에 적는다 |
| **공유 계층을 건드려 역방향 성적이 바뀐다** | Phase 0 에서 기준 사본을 뜨고 Phase 3·4 Validation 에서 바이트 대조한다 |
| **`규칙.md` 의 2026년 날짜를 추정으로 적는다** | 달력 코드(`measure/calendar_entry.py`·`calendar_exit.py`)로 산출해 적는다 |
| **강등한 두 매매법의 `kind` 를 안 바꾼다** | 코드가 없는데 `매매법` 으로 두면 성적표 계약을 요구받는다. Phase 0 테스트로 고정한다 |

## 8) 메모(Notes)

### 사용자 결정 (2026-09-22 대화)

1. **12월 제외** — 배당 불확실성. 12월 미국은 네 조합 전부 게이트 아래였고, 12월 한국의 말일 청산 우위는 `월말_진입` 규칙 §3.2 가 95%를 연말 배당 권리로 분해했으나 **지수에서도 같은 모양이 나와 원인이 확정되지 않았다**(`만기_말일` 결과 §9.2)
2. **C2(만기→말일) 채택** — 9월 ETF 4/4 통과. **C4 는 12월 한국에서 「이 조합만 다른 해」가 0**
3. **KODEX 200 제외** — 코스닥150 과 상관 0.8919
4. **대상은 SPY · DIA · KODEX 코스닥150**, 코스닥150 지수는 **참고(판정 안 함)**

### 🔴 사용자에게 알릴 것 — 2026년 9월 매매가 이미 진행 중일 수 있다

오늘이 **2026-09-22** 이고, 2026년 9월 셋째 금요일은 **2026-09-18**(지남)이며 그 달 마지막 거래일은 **2026-09-30**(아직)이다. 확정 규칙대로면 **지금이 보유 구간 안**이다.
다만 `storage/market/` 의 시세는 **2026-08-25 ~ 09-08 에서 끝나** 2026년 신호가 표본에 없다. 성적표의 마지막 신호일은 **2025-09-19** 다.
**이 계획서는 시세를 재수집하지 않는다** — 재수집하면 모든 수치가 바뀌고 `규칙.md` 가 그 시점부터 재현되지 않는다. 올해 집행 여부는 사용자가 정한다.

### 근거 승격 목적지

| 옮길 것 | 목적지 |
| --- | --- |
| 결정 넷과 그 근거 | `docs/매매/만기_말일/규칙.md` §3 |
| 배당락 검토 결과 (대조/신호 · 걸린 건수 · 평균 왜곡) | 같은 문서 §5 |
| 코스닥150 을 남긴 대가 (표본 10 · 뒤 8해 편중 · ETN) | 같은 문서 §3·§4 |
| 두 매매법의 코드 복원 절차 | `docs/조사/{옵션_만기일,월말_진입}/결과.md` |
| 「3벌 중복이 1벌이 됐다」 | `src/verify_lab/CLAUDE.md` |

### 남긴 지적 (코드 리뷰 1회차 · 고치지 않음)

**조치 여부는 사용자가 정합니다** (`/impl-plan` 「그 외」는 목록만 남긴다).

- `studies/expiry_monthend/runner.py` — 방향 컬럼을 `insert(3, ...)` 로 **자리 번호를 박아** 넣는다.
  지금은 맞지만 `measure.statistics.summarize` 가 앞에 컬럼을 하나 더 내면 **방향이 통계 컬럼 사이로
  들어간다** — CSV 는 그대로 쓰이고 1:1 조인도 유지돼 표시 순서 테스트만 잡는다.
  `block.columns.get_loc(COL_MONTH_NUMBER) + 1` 이 의도를 그대로 적는다

### 진행 로그 (KST)

- 2026-09-22 12:41: 계획서 작성
- 2026-09-22 12:46: 자체 검증에서 누락 1건 발견 — `측정.csv` 계약이 `방향` 컬럼을 요구한다(성적표와 1:1 조인). 착수 전에 Phase 3 에 반영
- 2026-09-22 12:46: Phase 0 착수
- 2026-09-22 12:55: **설계 조정 1건** — `--grid` 스위치를 두지 않기로 했다.
  `측정.csv` 의 `방향` 컬럼은 성적표와 1:1 로 읽히기 위한 것인데, 격자 스위치가 두 방향을 함께 내면
  **같은 (종목·조합·달)이 값이 똑같은 두 행으로 실려** 그 1:1 이 깨진다.
  축을 넓히는 수단은 **이미 있는 `--ticker`·`--combo`·`--month` 셋**이고, 방향은 「아래」로 고정한다 —
  방향을 다시 고를 재료는 `측정.csv` 의 **오른 비율·내린 비율**(1배 롱 기준)이 그대로 준다.
  두 선례(`옵션_만기일`·`월말_진입`)도 칸 격자 복원 스위치 없이 같은 구조다.
- 2026-09-22 12:55: Phase 0 완료 (의도한 레드 확인) · Phase 1 착수
- 2026-09-22 13:05: Phase 1 완료 — `규칙.md` 신설(§1~§5) · 결과.md 관련 파일 표 갱신 · INDEX 등록. `test_index`·`test_research_docs` **87 passed**
- 2026-09-22 13:05: Phase 2 착수
- 2026-09-22 13:20: Phase 2 완료 — `tracks.py` 3줄 · 문서/산출물 폴더 3쌍 이동 · 경로 참조 **26파일 141건** 치환 · INDEX 등급 3줄.
  `test_tracks`·`test_index`·`test_research_docs` **135 passed**. 네 스크립트 재실행 후 기준 사본과 대조해 **79파일 바이트 동일**이고
  `meta.json` 하나만 달랐다(실행 이력이라 git 제외 대상). **값이 바뀌지 않았음을 확인했다**
- 2026-09-22 13:20: Phase 3 착수
- 2026-09-22 13:35: Phase 3 완료 — `Cell`·`TRADING_CELLS`(4칸)·`trading_axes()`(곱집합 가드) 신설 · `측정.csv` 로 이름 교체 ·
  `방향` 식별 컬럼 추가 · 방향을 「아래」로 고정 · CLI 기본값 축소. 산출물이 **측정 4행 · 성적표 35행 · 거래내역 158행**으로
  계획서 예상치와 일치했고, 축을 넓혀 돌리면 **측정 48행 · 성적표 400행**으로 격자가 되살아났다.
  역방향 산출물 **7파일 바이트 동일**. 전체 테스트 **1336 passed**
- 2026-09-22 13:35: **버그 1건을 잡아 고쳤다** — 조합을 하나만 돌리면 「이 조합만 다른 해」가 신호 수와 같아진다
  (SPY 9월 실제 15해 대 33). 견줄 조합이 없어 **잴 수 없는 값**이므로 두 컬럼을 비우게 했고 테스트로 고정했다.
  **좁히기가 만든 결함이라 Phase 3 안에서 처리했다**
- 2026-09-22 13:35: Phase 4 착수
- 2026-09-22 13:45: **Phase 4 에서 계획서가 예상하지 못한 의존 둘이 드러나 사용자 판단을 요청하고 중단했다.**
  ① **공유 달력 계층의 테스트가 두 매매법의 상수를 픽스처로 쓴다** — `test_measure_calendar_entry.py` 가
  `US_MONTHLY_EXPIRY`·`KR_MONTHLY_EXPIRY` 를, `test_measure_calendar_exit.py` 가 옵션 만기일 상수를,
  `test_measure_calendar_month.py` 가 `HORIZON_MONTH_END` 를 가져온다. 달력을 공유 계층으로 올릴 때
  **만기 규칙 값과 구간 표지는 일부러 매매법에 남겼기** 때문이고(`src/verify_lab/CLAUDE.md`),
  지우면 **국내 만기(둘째 목요일) 규칙값이 저장소에서 사라진다** — 만기_말일은 양 시장 모두 셋째 금요일을 쓴다.
  ② **실측 프로브 넷이 두 매매법을 import 한다** — `check_expiry_dividend` · `check_kodex_distribution` ·
  `check_month_end_dividend` · `check_month_end_leverage`. 특히 **`check_month_end_leverage` 는 2배 실물 환산**을
  재는 도구이고, **만기_말일의 코스닥 칸이 ETN 2배 집행**이라 그 질문이 그대로 살아 있다
- 2026-09-22 14:10: **사용자가 「최대한 코드가 깔끔해지는 방향」으로 정해 전부 삭제했다.** 프로브 넷을 남기지 않은 근거는
  ① 잰 사실이 이미 문서에 있고(`trading.md` 의 분배락 달력 · `규칙.md` §1.2 의 배수 환산)
  ② 저장소에 같은 선례가 둘이며(`연속_등락`·`원달러_그리드`)
  ③ 2배 환산을 다시 재야 하면 **만기_말일 기준으로 새로 쓰는 편이** 월말용 244줄을 고쳐 쓰는 것보다 깔끔하다는 것이다.
  공유 달력 계층의 테스트는 **자기 픽스처를 갖게** 했다 — 공유 계층의 검사가 소비자 패키지에 매달려 있던 것이
  애초에 뒤집힌 의존이었고, 이번에 바로잡혔다
- 2026-09-22 14:20: Phase 4 완료 — 패키지 2 · 실행 스크립트 2 · 실측 프로브 4 · 전용 테스트 4 삭제.
  계약 테스트 둘을 두 매매법 기준으로 줄였고(`test_strategy_output_contract.py` 2,050 → 1,671줄),
  `test_layer_contracts.py` 의 `TestStopGridWiring` 과 해소된 레이블 겹침 둘을 걷어냈다.
  **`_aggregate_by_month`·`_dividend_row` 3벌 중복이 1벌이 됐다.** 전체 테스트 **1239 passed** ·
  역방향 산출물 **7파일 바이트 동일**
- 2026-09-22 14:25: 마지막 Phase 착수
- 2026-09-22 14:45: 규칙 문서의 현재형 서술 정리 — `.claude/rules/trading.md` · 루트 `CLAUDE.md` ·
  `src/verify_lab/CLAUDE.md` · `scripts/CLAUDE.md` · `docs/INDEX.md`. **과거 실측 기록과 설계 결정은 그대로 뒀다**
  (`.claude/rules/docs.md` 「과거형이 허용되는 자리」). `docs/MEMORY.md` 에 이번에 알아낸 것 둘을 적었다 —
  **공유 계층의 테스트는 자기 픽스처를 갖는다** · **문서 절을 스크립트로 자를 때는 코드 펜스를 인식한다**
- 2026-09-22 14:55: `/code-review xhigh` 1회차 — **발견 13건 (버그 6 · 그 외 7)**.
  버그 여섯을 전부 고쳤다: ① 품질 게이트 red(고아 import·정렬) ② 좁혀 돌린 실행에서 **사후 선택 경고가
  `summary.json` 에서 통째로 빠짐**(`set(months) == set(MONTHS)` 게이트) ③ 방향 고정 사실이 요약에 없어
  판정이 오도됨 ④ `trading_axes` 의 달 검증 누락 ⑤ 조합 «부분집합»에서도 붕괴 수가 전체 격자 값처럼 나감
  ⑥ `DEFAULT_BET_DOWN` 이 runner 에 있어 배치 계약 위반 + **import 시점 검증으로 pytest 수집이 죽을 수 있음**.
  그 외에서는 **내 계획서가 하기로 한 문서 정리**와 **내가 만든 테스트 결함**(같은 표를 네 이름으로 두 번 세던
  4-way 검사)을 고쳤고, 자리 번호 지적 하나만 목록으로 남겼다
- 2026-09-22 15:00: 최종 검증 — **Ruff·PyRight 통과 · Pytest 1239 passed / failed=0 / skipped=0**.
  역방향 산출물 **7파일 바이트 동일**, 만기_말일 **측정 4행 · 성적표 35행 · 거래내역 158행**

---
