# Implementation Plan: 옵션 만기일 매매의 손절선을 -5% 로 확정하고 격자를 산출물에서 뺀다

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-02 (KST)
**마지막 업데이트**: 2026-09-02
**관련 범위**: `src/verify_lab/strategy/` · `scripts/strategy/` · `docs/COMMANDS.md` · `docs/research/`
**관련 문서**: `CLAUDE.md`(루트), `.claude/rules/strategy.md`, `.claude/rules/docs.md`, `docs/strategy/역방향_매매_규칙.md`, `docs/research/옵션_만기일.md`

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: **기본 실행이 손절선 −5% 하나만 낸다.** 산출물에서 `손절선(%)` 컬럼이 사라지고
      `trades.csv` 가 신호당 한 행이 된다 (4,560 → 228건)
- [x] 목표 2: **격자를 되살릴 수 있게 남긴다.** `--grid` 옵션으로 기존 격자를 그대로 낸다 —
      `.claude/rules/strategy.md` 가 「손절선 후보를 격자로 전부 돌려 평평한 구간을 찾는다」를
      **절차로** 요구하고, **시세를 재수집하면 그 절차를 다시 밟아야 하기 때문**이다

## 2) 비목표(Non-Goals)

- **손절선 값을 다시 고르지 않는다.** −5% 는 확정됐다. 근거는 「최악 통제가 포화되는 지점」이며
  (KODEX 200 9월 2011-09-08 의 −5.35% 갭 때문에 더 좁혀도 최악이 안 줄고 합계만 깎인다)
  `docs/research/옵션_만기일.md` 12B.3 에 실측으로 남아 있다
- **손절선을 CLI 인자로 열지 않는다.** `--stop -3` 같은 노브를 만들면 값을 옮겨 가며 성적을
  보게 되고 그것이 과최적화다. 역방향 매매 스크립트도 같은 이유로 열지 않았다
- **12B장의 격자 표를 지우지 않는다.** 값 선택의 근거이며 살아있는 기록이다
- **대상 칸을 바꾸지 않는다.** 8칸 그대로다
- **매매 규칙 문서(`docs/strategy/`)를 쓰지 않는다.** 별도 작업이다

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- 사용자가 **손절선을 −5% 로 확정**했고, 산출물의 `손절선(%)` 컬럼이 더 이상 필요 없다
- 지금은 칸마다 20행(무손절 + 19개 손절선)이 나와 `trades.csv` 가 **4,560건**이다.
  그중 사용자가 볼 것은 **−5% 의 228건**뿐이고 나머지는 읽을 때 걸러내야 한다
- `.claude/rules/strategy.md` 는 `strategy/` 에서 **파라미터 확정을 명시적으로 허용**한다
  (「파라미터를 골라 확정 — 나머지 프로젝트: 금지 / `strategy/`: **허용하되 아래 제약**」)
- **역방향 매매가 이미 같은 모양이다** — `run_reverse_trading.py` 는 확정 상수만 쓰고
  격자는 규칙 문서 §3.1 에 기록으로 남아 있다

### 확인된 사실 (사전 조사, 2026-09-02)

| 항목 | 값 | 확인 방법 |
| --- | --- | --- |
| 현재 산출물 | 격자 160행 · 체결 4,560건 | 2026-09-02 실행 |
| −5% 만 남기면 | 격자 8행 · 체결 **228건** (8칸 신호 합) | 27+23+29+33+28+33+27+28 |
| 확정값 −5% 의 근거 | 최악 통제 포화점. 더 좁혀도 −5.35% 갭에 뚫림 | 12B.3 |
| `EXPIRY_STOP_LEVELS` 소비처 | `expiry_runner.run_expiry_trading` 기본 인자 · `run_expiry_trading.py`(로그·summary) | `grep -rn` |
| 손절선을 값으로 고정한 테스트 | **0건** | `grep -rn EXPIRY_STOP_LEVELS tests/` |
| 역방향의 선례 | 손절선·보유 한도를 상수로 두고 인자로 열지 않음 | `run_reverse_trading.py:50-56` |

### 영향받는 규칙(반드시 읽고 전체 숙지)

- 루트 `CLAUDE.md` (측정의 원칙 1 — **다만 `strategy/` 는 명시적 예외**, 원칙 16)
- `.claude/rules/strategy.md` (**파라미터 확정 허용** · 고른 값에는 탈락안과 근거를 남긴다 ·
  손절선 격자로 평평한 구간을 찾는 절차)
- `.claude/rules/docs.md` (재실행하지 않은 값을 갈아 적지 않는다)
- `src/verify_lab/CLAUDE.md` (상수 3계층)

---

## 4) 완료 조건(Definition of Done)

- [x] `EXPIRY_STOP_LEVEL = 0.05` 상수가 있고 **고른 근거가 주석에 남는다**
- [x] 기본 실행의 `trades.csv` 에 **`손절선(%)` 컬럼이 없고** 228건이다
- [x] 기본 실행의 요약표 파일명이 내용과 맞는다 (`stop_loss_grid.csv` → 성적표)
- [x] **값이 바뀌지 않는다** — 새 산출물의 −5% 행이 이전 실행의 −5% 행과 전건 일치한다
- [x] `--grid` 로 격자를 그대로 낼 수 있고, 그때는 `손절선(%)` 컬럼이 있다
- [x] `docs/COMMANDS.md` 가 두 실행 방식을 반영한다
- [x] `docs/research/옵션_만기일.md` 12B 에 **격자가 이제 옵션이라는 사실**이 적힌다
- [x] `poetry run black .` 적용
- [x] `poetry run python validate_project.py` 통과 (`failed=0`, `skipped=0`)

---

## 5) 변경 범위(Scope)

| 파일 | 변경 | 이유 |
| --- | --- | --- |
| `src/verify_lab/strategy/constants.py` | `EXPIRY_STOP_LEVEL` 신설 · `EXPIRY_STOP_LEVELS` 는 격자용으로 주석 보강 | 확정값의 SoT |
| `src/verify_lab/strategy/expiry_runner.py` | 손절선이 하나면 식별 컬럼에서 뺀다 | 컬럼 제거 |
| `scripts/strategy/run_expiry_trading.py` | `--grid` 추가 · 기본은 확정값 · 파일명 분기 | CLI |
| `docs/COMMANDS.md` | 두 실행 방식 | SoT |
| `docs/research/옵션_만기일.md` | 12B 머리말에 한 줄 | 재현 경로 |

### 데이터/결과 영향

- **수치는 하나도 바뀌지 않는다.** 낼 행을 고르는 것뿐이다
- 기본 산출물: `trades.csv` 4,560 → **228건**, 요약 160 → **8행**

---

## 6) 단계별 계획(Phases)

### Phase 1 — 확정 상수와 컬럼 제거 (그린)

- [x] `EXPIRY_STOP_LEVEL: Final = 0.05` 추가, 근거 주석
- [x] `expiry_runner`: 손절선이 하나면 `손절선(%)` 컬럼을 내지 않는다
- [x] `run_expiry_trading.py`: 기본은 확정값 하나, `--grid` 면 격자

> **검증**: 기본 실행 → `trades.csv` 228건 · `손절선(%)` 없음.
> **이전 실행의 −5% 행과 전건 대조**

### Phase 2 — 격자 경로 확인 (그린)

- [x] `--grid` 실행 → 이전과 같은 160행 · 4,560건 · 컬럼 있음

> **검증**: 2026-09-02 실행 산출물과 전건 일치

### 마지막 Phase — 문서 정리 및 최종 검증

- [x] `docs/COMMANDS.md` · `docs/research/옵션_만기일.md` 갱신
- [x] `poetry run black .`
- [x] `poetry run python validate_project.py` (`failed=0`, `skipped=0`)

---

## 7) 리스크(Risks)

| 리스크 | 영향 | 완화 |
| --- | --- | --- |
| 격자를 아예 지우면 재수집 후 절차를 못 밟는다 | 「평평한 구간」을 다시 찾을 수 없다 | **`--grid` 로 남긴다.** 역방향은 지웠다가 K=20 비교축에서 다시 필요했다 |
| `--grid` 가 값을 고르는 노브로 쓰인다 | 과최적화 | **손절선 값 자체는 인자로 열지 않는다.** `--grid` 는 전부 내는 쪽이라 반대 방향이다 |
| 컬럼 제거로 값이 바뀐다 | 문서 수치 불일치 | 전건 대조를 Phase 1 검증으로 못박음 |

---

## 8) 메모(Notes)

### 확정할 설계 결정

| # | 결정 | 확정 | 탈락안 | 근거 |
| --- | --- | --- | --- | --- |
| ㊴ | **손절선을 −5% 로 확정하고 기본 산출물에서 격자를 뺀다** | 기본은 −5% 하나 · `--grid` 로 격자 유지 | A: 격자를 통째로 삭제 / B: `--stop` 으로 값을 인자화 | `.claude/rules/strategy.md` 가 `strategy/` 에서 **파라미터 확정을 허용**하고 역방향 매매가 같은 모양이다. **−5% 는 성적이 아니라 「최악 통제 포화점」이라는 구조로 골랐다** — KODEX 200 9월 2011-09-08 의 −5.35% 갭 때문에 더 좁혀도 최악이 안 줄고 합계만 깎인다(12B.3). A 는 **시세를 재수집하면 격자 절차를 다시 밟아야 하는데** 그 경로가 사라진다 — 역방향이 격자를 지웠다가 K=20 비교축에서 다시 필요했던 전례가 있다. B 는 값을 옮겨 가며 성적을 보게 되어 과최적화다 |

### 진행 로그 (KST)

- 2026-09-02 18:22 — Phase 1·2 완료. 기본 `trades.csv` **228건**·`손절선(%)` 컬럼 없음,
  요약은 `summary_by_cell.csv` 8행. **이전 실행의 −5% 행과 전건 일치**(성적 8행·체결 228건 불일치 0)
- 2026-09-02 18:22 — `--grid` 가 이전 격자와 **전건 일치**(160행·4,560건, `diff` 무출력)
- 2026-09-02 — 문서 갱신·`validate_project.py` 통과(`passed=609 failed=0 skipped=0`)
