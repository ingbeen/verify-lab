# Implementation Plan: 중간선거_사이클 매매 승격과 레버리지 사다리 집행안 확정

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-23 13:53
**마지막 업데이트**: 2026-09-23 15:00
**관련 범위**: tracks, studies/midterm_cycle(주석·요약 문자열만), scripts(주석만), tests, docs, storage/results
**관련 문서**: 루트 `CLAUDE.md`, `src/verify_lab/CLAUDE.md`, `scripts/CLAUDE.md`, `tests/CLAUDE.md`, `.claude/rules/{trading,docs,research}.md`, `docs/INDEX.md`, `docs/MEMORY.md`, `docs/조사/투자금_결정/규칙.md`

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: **`중간선거_사이클` 을 매매 등급으로 승격**한다 — `tracks.py` 한 줄, 문서·산출물 폴더 이동. **산출물 CSV 는 바이트 불변**이다(측정 코드를 바꾸지 않는다)
- [x] 목표 2: **`규칙.md` §1 을 확정 규칙(집행안)으로 다시 쓴다** — 아래 「확정 규칙」 그대로
- [x] 목표 3: **레버리지 실측을 «문서에만» 측정 기록으로 남긴다** — 연도별 1 · 2 · 3배, 1배 최저점 빈도, 집행안 성적과 탈락안 대조, 레버리지 상품 분배락. **CSV 에는 넣지 않는다**(사용자 결정)
- [x] 목표 4: **규칙 문서끼리의 충돌을 정리한다** — `.claude/rules/trading.md` 「손절은 반드시 있다」에 기간 손절을 인정하는 조건, `docs/조사/투자금_결정/규칙.md` 「1배 금지」의 적용 범위, `trading.md` 의 「검증 등급을 좁힌 사례」 문단

### 확정 규칙 (사용자 결정 2026-09-23 — `규칙.md` §1 에 옮길 내용)

| 항목 | 값 |
| --- | --- |
| 신호 | 중간선거 해 (연도를 4로 나눈 나머지가 2) |
| 대상 | **SPY · DIA · QQQ 3분할** — 배정액을 셋으로 똑같이 나눈다 |
| 진입 | 9월 마지막 거래일 종가에 **대상별 배정액의 1/2** 을 **2배 상품**(SSO · DDM · QLD) |
| 예비금 | 대상별 배정액의 나머지 1/2. **8등분**(각 배정액의 1/16) |
| 추가 매수 판정 | **그 대상 1배 ETF 종가**가 진입일 1배 종가 대비 **−3 · −4 · −5 · −6 · −7 · −8 · −9 · −10%** 이하로 **처음** 내려간 날 |
| 추가 매수 체결 | 판정일의 **다음 거래일 종가**에 그 단계 몫을 **3배 상품**(UPRO · UDOW · TQQQ). 하루에 여러 단계를 넘으면 합쳐서. 한 단계는 한 번만 |
| 판정 단위 | **ETF 마다 각자** (SPY 로 통일하지 않는다) |
| 기한 | **청산 전까지.** 닿지 않은 단계의 예비금은 청산일까지 현금으로 대기한다 |
| 청산 | 다음해 6월 마지막 거래일 종가에 보유 전부 |
| 손절 | **기간 손절만** — 가격 손절 없음 |
| 방향 · 빈도 | 위 · 4년에 한 번 |
| 투자금 | **규칙 밖** — 사용자가 따로 정한다. 규칙은 비율만 갖는다 |

## 2) 비목표(Non-Goals)

- **레버리지 상품을 측정 대상(`DATASETS`)에 넣지 않는다** — 사용자 결정(「CSV 에서 제거, 문서에만 결과 기재」). 측정 코드는 1배 그대로다
- 1배 분할매수 격자·손절선 격자를 지우지 않는다 — 탈락안의 근거로 산출물에 남는다
- 상장 전 연도의 레버리지 **이론값을 문서에 넣지 않는다** — 사용자 결정(「의미 없음」)
- 시세 재수집 없음 — 다음 표본은 2027-06-30 이후에 생긴다
- 투자금 «금액»을 규칙으로 정하지 않는다. `투자금_결정` 규칙을 이 매매법에 적용하지 않는다
- `docs/context/` 를 고치지 않는다 — 사용자 소유 문서다 (Notes 의 불일치 항목 참고)
- 교차 재현(한국 시장) · 알림 자동화 · 집행 코드 구현 없음

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- `docs/검증/중간선거_사이클/규칙.md` §1.2 의 미결 넷(대상 · 손절선 · 레버리지 · 투자금)이 **2026-09-23 대화에서 모두 정해졌다.** 등급이 검증인 채로는 「확정된 매매법만 참고하라」의 대상이 되지 않는다
- 사용자가 실제로 집행할 방식(2배 절반 + 3배 사다리)이 **문서의 규칙(1배 일시매수)과 다르다.** 문서가 실제 매매와 달라지지 않도록 규칙을 집행안으로 다시 쓴다
- 집행안은 두 규칙 문서와 부딪힌다 — `trading.md` 는 「무손절은 선택지가 아니다」, `투자금_결정` 은 「이벤트형 전반에 1배 금지」. **예외의 조건을 좁게 적지 않으면 다른 매매법에 새어 나간다**
- 9/23 에 정한 「분할매수 하지 않음(일시매수)」이 같은 날 집행안으로 대체됐다 — 결정 표에 그 이력을 남긴다

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- `.claude/plan-config.json` — 이 저장소의 검증 명령·자동 포맷·근거 승격 목적지 (**값의 SoT**)
- 루트 `CLAUDE.md` — 특히 「계획서 규약 — 이 프로젝트의 설정」 절 (값이 아니라 **판단 근거**)
- `src/verify_lab/CLAUDE.md` · `scripts/CLAUDE.md` · `tests/CLAUDE.md`
- `.claude/rules/trading.md` · `.claude/rules/docs.md` · `.claude/rules/research.md`
- `docs/MEMORY.md` — 특히 「이름만 바꾸는 변경과 값을 바꾸는 변경을 한 커밋에 섞지 않는다」
- `docs/조사/투자금_결정/규칙.md`

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [x] 기능 요구사항 충족 — 등급 전환 · 문서 이동 · `규칙.md` §1 ~ §6 개정 · 레버리지 측정 기록 · 규칙 문서 충돌 정리
- [x] 회귀/신규 테스트 추가 (`test_중간선거_사이클이_매매_등급이다`)
- [x] 산출물 CSV 가 이동 전후로 바이트 동일 (`summary.json` 은 `NOTE_AXIS` 의 문서 경로 한 줄만 다름)
- [x] 코드 리뷰 실행 및 결과 기록 (마지막 Phase 의 Validation 에 적는다)
- [x] 품질 검증 통과 (마지막 Phase 의 Validation 에 passed/failed/skipped 수를 적는다)
- [x] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [x] 필요한 문서 업데이트(`docs/COMMANDS.md` 변경 있음 — 경로·절 제목만 / `docs/INDEX.md` 변경 있음 / `.claude/rules/trading.md` 변경 있음 / `docs/MEMORY.md` 변경 없음 / 루트 `CLAUDE.md` · `src/verify_lab/CLAUDE.md` · `scripts/CLAUDE.md` · `tests/CLAUDE.md` 변경 없음)
- [x] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다
      (결정 근거·실측 수치를 `docs/` 의 해당 문서로 이관. `/impl-plan` 스킬 "근거 승격" 참고)
- [x] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

- `src/verify_lab/tracks.py` — `midterm_cycle` 등급 한 줄과 그 주석
- `tests/test_tracks.py` — `TestGradeSemantics` 에 테스트 하나 추가
- `src/verify_lab/studies/midterm_cycle/__init__.py` · `trading.py` — 문서 경로 (주석과 `NOTE_AXIS` 문자열)
- `scripts/run_midterm_cycle.py` — 모듈 설명의 문서 경로
- `docs/검증/중간선거_사이클/{설계,결과,규칙}.md` → `docs/매매/중간선거_사이클/` 로 이동하고 내용 개정
- `docs/INDEX.md` — 이름표의 등급 · 경로 · 설명, §3 문서 행 셋, `투자금_결정` 행의 1배 금지 범위
- `docs/COMMANDS.md` — **변경 있음: 문서 경로와 절 제목(「검증 — 중간선거_사이클」 → 「매매 — …」)만.** 명령어·CLI 옵션 변경 없음
- `.claude/rules/trading.md` — 「손절은 반드시 있다」에 기간 손절 조건, 「검증 등급을 좁힌 사례」 문단의 경로와 시제
- `docs/조사/투자금_결정/규칙.md` — 머리말 대상 줄과 「1배 금지」 적용 범위
- `storage/results/검증/중간선거_사이클/` → `storage/results/매매/중간선거_사이클/` (재실행으로 생성, 옛 폴더 삭제)

### 데이터/결과 영향

- **측정 코드 변경 없음** — CSV 8개 바이트 동일해야 한다. `summary.json` 은 `notes` 의 문서 경로 한 줄만 바뀐다(`trading.py` 의 `NOTE_AXIS`)
- 레버리지 수치는 **문서에만** 있고 저장소를 다시 돌려도 나오지 않는다 — 측정 절차를 문서에 적어 손으로 재현할 수 있게 한다(Risks)

## 6) 단계별 계획(Phases)

### Phase 1 — 레버리지 측정 기록 확정 (저장소 변경 없음 · 스크래치패드)

> 이 Phase 의 숫자가 Phase 2 문서에 **그대로 붙는다.** 눈으로 옮겨 적지 않는다(전역 「집계·추출은 대조 없이 내지 않는다」).
> 공통 조건: 실제 상품 원본가만 · 원화 금액은 달러 수익률 × 원화(환율 변화 무시) · 예비금 이자 0 · 최저는 장중 저가(여러 상품 합산은 같은 날 저가의 합으로 근사)

**작업 내용**:

- [x] 착수 전에 사용자에게 **미커밋 변경 4건(`docs/INDEX.md` · 중간선거 문서 셋)을 먼저 커밋**해 달라고 요청하고 확인한다 — 요청했고 사용자가 커밋 없이 「진행」을 지시했다. **그 4건이 이 계획서의 최종 커밋에 함께 들어간다**
- [x] 산출물 **기준 사본**을 스크래치패드에 뜬다 — `storage/results/검증/중간선거_사이클/` 전체(Phase 3 바이트 대조용) — 9파일(CSV 8 + `summary.json`)
- [x] **연도별 1 · 2 · 3배** — SPY/SSO/UPRO · DIA/DDM/UDOW · QQQ/QLD/TQQQ, 21행. 상장 전은 「상장 전」. 2006 2배의 분배금 착시(원본가 대 수정주가)를 함께 뜬다
- [x] **1배 보유 중 최저점 빈도** — 장중 저가 기준 1% 구간(0 ~ −10%, −10% 넘게)과 누적, S&P 500 지수 24건 병기
- [x] **단계 도달 빈도** — 1배 종가 기준, −3 ~ −10% 단계별 건수(ETF 21건)
- [x] **집행안 성적** (3분할 · 균등 · ETF 각자 · 청산 전까지) — 2006 · 2010 · 2014 · 2018 · 2022 의 6월 말 평가액 · 수익률 · 장중 최저 · 투입액. 같은 표에 **1배 전액**과 **2배 절반만(추가 매수 없음)** 을 나란히
- [x] **집행안 원자료** — 2014 · 2018 의 대상별 체결(판정일 · 체결일 · 단계 · 상품 · 체결가 · 금액 비율)
- [x] **2018 경로** — 분기 말 평가액(12월 말 · 3월 말 · 6월 말)과 **예비금을 다 쓴 뒤의 노출 배수**(평가액 대비 · 원금 대비)
- [x] **탈락안 대조** — 같은 조건에서 하나만 바꾼 값: 배분(기존 −5/−10 · 점증 1:2:…:8 · 누적 맞춤) · 판정 기준(2배 상품 가격) · 판정 단위(SPY 통일) · 기한(12월 말까지) · 1배 전액 · 2배 절반만
- [x] **레버리지 상품 분배락** — 여섯 상품이 각 보유 구간(2배는 진입일부터, 3배는 체결일부터)에서 몇 번 걸렸고 원본가가 얼마나 과소평가하는지(원본가 대 수정주가 수익률 차이). `.claude/rules/trading.md` 「배당·분배락은 확정 전 필수 항목」의 요구다

**Validation**:

- [x] 1배 재계산이 `거래내역.csv` 무손절 21건과 **불일치 0** (실측 0)
- [x] 분기 원자료 63행 · ETF 체결 21건 · 지수 24건을 따로 세어 대조
- [x] 사다리 불변조건 — 대상별 투입액 ≤ 예비금 · 체결일 < 청산일 · 한 단계 한 번 (배분 4 × 판정 기준 3 × 대상 3 × 5해 = 180경로, 위반 0)
- [x] 균등 3분할 결과가 대화에서 낸 값과 같다 (2014 **+14.47%** · 2018 **+7.05%** · 5해 평균 **+14.98%**) — 세 값 모두 같다

---

### Phase 2 — 문서 내용 개정 (위치는 그대로 · 등급 표기는 Phase 3)

**작업 내용**:

- [x] `규칙.md` **§1** — 위 「확정 규칙」 표로 교체. §1.2 「확정하지 못한 것」을 걷어내고 **「알고 거는 한계」**(표본 6 ~ 8건 하한 미달 등 — 사용자 결정 2026-09-23)로 바꾼다
- [x] **기존 절 번호를 바꾸지 않는다**(§1 · §2.1 ~ §2.7 · §3 ~ §6) — `docs/INDEX.md` 와 `결과.md` 가 번호로 가리킨다. 새 절은 §2.8 부터 붙인다
- [x] `규칙.md` **§2** — 기존 1배 성적(§2.1 ~ §2.7)은 유지하되 제목을 「1배 측정」으로 갈라 읽히게 하고, §2.3 은 「가격 손절을 걸지 않은 근거」, §2.7 은 「1배 분할 — 탈락안의 근거」로 자리를 고친다. **신설**: 연도별 1 · 2 · 3배 · 최저점 빈도 · 단계 도달 빈도 · 집행안 성적 · 2018 경로와 노출 · 레버리지 상품 분배락. **신설 절 머리에 「측정 기록 — 저장소를 다시 돌려도 나오지 않는다」와 측정 절차 · 데이터 기간을 적는다**
- [x] `규칙.md` **§3** — 결정 표를 「확정 / 탈락안 / 근거」로: 대상(3분할) · 집행 방식(2배 절반 + 3배 사다리) · 배분(균등) · 판정 기준(1배 종가 · 다음 거래일) · 판정 단위(ETF 각자) · 기한(청산 전까지 — **추천은 12월 말이었고 사용자가 청산 전까지로 정했다**) · 손절(기간 손절만) · 레버리지 결과의 자리(문서만) · 투자금(규칙 밖) · 3배 단독(제외) · 표본 하한 미달 인지 · 분할매수(9/23 「하지 않음」이 같은 날 집행안으로 대체). 근거 수치는 Phase 1 값
- [x] `규칙.md` **§4 한계** — 집행안 근거는 실제 상품 5해 · 추가 매수 발동 2해(2014 · 2018) · 예비금을 다 쓴 뒤 노출 약 2.5배가 청산까지 · 회복하지 못한 해(1930 · 1938형)는 실제 상품 기간에 없다 · 재현 수단 없음 · 예비금 이자 0 · 환율 · 세금 · 수수료 미반영 · 3배 상품 차입 비용은 금리에 따라 커진다 · DDM · UDOW 거래대금이 얇다 · 3분할이 낙폭을 줄이지 않는다(상관 0.85 ~ 0.96)
- [x] `규칙.md` **§5 원자료** — 기존 SPY 8건 유지 + 집행안 체결 원자료(2014 · 2018)
- [x] `규칙.md` **§6 다음 사이클** — 2026-09-30 진입 · 2027-06-30 청산 · 비율표 · 단계별 판정 가격 산식(진입 종가 × 0.97 … × 0.90, ETF 마다) · 사전 준비(해외 레버리지 상품 사전교육 · 기본예탁금 · 종가 체결 방법). **금액 예시(1억 2천만원)를 둘지는 사용자 확인 사항**(Notes) — 답이 없어 기본값(비율만)
- [x] `결과.md` — 측정 본문은 그대로. §1 결론 요약과 한 줄 요약의 「분할하지 않기로(일시매수)」 문장, §13 이번 사이클 적용 표의 분할매수 · 미결 행을 현재 결정으로 고친다
- [x] `설계.md` — **결정 ⑳** 「레버리지 결과는 산출물이 아니라 문서 기록」(확정 / 탈락안: `DATASETS` 에 추가 / 근거 / 대가: 재현 수단 없음). **데이터 실측 §3.8** — 레버리지 상품 원본가의 분배금 착시(2006) · 상장일 때문에 잴 수 있는 해가 2배 5해 · 3배 4해
- [x] `.claude/rules/trading.md` 「손절은 반드시 있다」 — **기간 손절을 손절로 인정하는 조건**: 가격 손절 격자를 전부 돌려 평평한 구간이 없음을 수치로 보였고, 사용자가 「정해진 청산일에 무조건 판다」만 두기로 명시했을 때. 「손절이 무엇을 막았는가」는 그 매매법의 `규칙.md` 가 갖는다. 지금 이 길은 중간선거_사이클 하나. **역방향 · 만기_말일 문구는 건드리지 않는다**
- [x] `docs/조사/투자금_결정/규칙.md` — 머리말 대상 줄과 「1배 금지」 표에 **적용 범위: 며칠짜리 · 회당 기대값이 작은 매매에만**(사용자 결정 2026-09-23). 중간선거_사이클은 이 규칙 자체를 적용하지 않는다
- [x] `docs/INDEX.md` — `투자금_결정` 행의 「1배 집행은 금지다」에 범위를 붙인다

**Validation**:

- [x] 문서의 수치를 Phase 1 출력과 **기계로 대조**(표 셀을 뽑아 출력과 비교) — 불일치 0 (출력 표 145행 전부 글자 그대로 · 손으로 옮긴 수정주가 대조표 5행 일치)
- [x] `규칙.md` 에 현재형으로 남은 낡은 문장 0 — 「분할매수는 하지 않는다」 · 「확정하지 못한」 · 「미결」 · 「아직 걸 수 없다」 · 「투자금 … 개정이 먼저」 (결정 표의 탈락안 이력은 예외). `결과.md` 도 같은 목록으로 본다
- [x] 새로 쓴 문장의 범위 표기 `~` 양옆 공백 확인 (붙은 `~` 0)
- [x] `poetry run pytest tests/test_index.py tests/test_research_docs.py` 통과 (94 passed)

---

### Phase 3 — 등급 전환과 폴더 이동 (값 불변)

**작업 내용**:

- [x] `tests/test_tracks.py` `TestGradeSemantics` 에 `test_중간선거_사이클이_매매_등급이다` 를 **먼저** 추가하고 그 1건만 실패하는 것을 확인한다 (1 failed · 51 passed) — 목적 문구에 「미결 넷이 2026-09-23 에 정해져 `규칙.md` §1 이 확정 규칙이 됐다」
- [x] `src/verify_lab/tracks.py` — `midterm_cycle` 을 `GRADE_TRADING` 으로, 주석을 「걸기로 정한 매매법 · 확정 규칙은 `규칙.md` §1」 현재형으로
- [x] 문서 폴더 이동 — `docs/검증/중간선거_사이클/` → `docs/매매/중간선거_사이클/` (파일 이동 · git 작업은 사용자). 빈 `docs/검증/` 삭제
- [x] 경로 전수 수정 — `docs/INDEX.md` · `docs/COMMANDS.md` · `.claude/rules/trading.md` · 옮긴 문서 셋 · `scripts/run_midterm_cycle.py` · `studies/midterm_cycle/__init__.py` · `trading.py`(`NOTE_AXIS` 포함). **`docs/plans/` 는 제외**
- [x] 등급 서술 수정 — `docs/INDEX.md` 이름표(등급 · 「첫 검증 등급 매매법」 · 「아직 걸 수 없다」 · 「분할하지 않기로(일시매수)」 — **설명을 집행안 기준으로 다시 쓴다**) · `결과.md` 「관련 파일」 표의 「매매 규칙 (아직 확정 전)」 행 · §3 문서 행 셋 · `규칙.md`/`결과.md` 머리말 · `docs/COMMANDS.md` 절 제목 · `trading.md` 「검증 등급을 좁힌 사례」 문단(검증 등급일 때 좁혔고 2026-09-23 매매로 승격 — 현재형 규칙 문장은 유지)
- [x] `scripts/run_midterm_cycle.py` 를 **인자 없이** 재실행 → `storage/results/매매/중간선거_사이클/`
- [x] 기준 사본과 대조 후 옛 폴더 `storage/results/검증/중간선거_사이클/` 과 빈 `storage/results/검증/` 삭제

**Validation**:

- [x] 새 등급 테스트 **레드 → 그린** — `poetry run pytest tests/test_tracks.py tests/test_index.py tests/test_research_docs.py` (146 passed)
- [x] 기준 사본 대비 **CSV 8개 바이트 동일**, `summary.json` 은 `NOTE_AXIS` 경로 한 줄만 다름(`diff` 로 확인 — 206행 하나)
- [x] `grep -r "검증/중간선거_사이클"` 결과가 `docs/plans/` 밖에서 0 (실측 0)
- [x] 옮긴 문서 셋에서 `검증 등급` 을 현재형으로 말하는 문장 0 (예: `결과.md` §12 「검증 등급이고」) — 재실행 기록 · 결정 표의 이력은 예외 (남은 1곳은 `설계.md` 결정 ⑬ 의 인용 블록 — 결정 기록이라 예외)

---

### 마지막 Phase — 문서 정리 및 최종 검증

**작업 내용**

> 🔴 **`/commit` 이 «맨 마지막»인 것은 의도다.** 그 스킬은 「후보 뒤에는 아무것도 덧붙이지 말 것」으로
> 끝나므로 **호출하는 순간 그 턴이 거기서 닫힌다.** 중간에 두면 뒤에 적힌 항목이 그 벽 너머에 남는다 —
> 실제로 두 번 그렇게 샜다(`[실측] 2026-09-14` 후보를 계획서에 안 옮김 · `2026-09-16` 옮기고 체크박스를 안 닫음).
> **체크박스와 상태를 먼저 확정하고, 커밋 후보를 마지막에 만든다.**

- [x] 필요한 문서 업데이트 (`docs/COMMANDS.md` 변경 있음 — 경로·절 제목만, Phase 3 에서 반영)
- [x] 자동 포맷 적용 (`poetry run black .`) — 157 files left unchanged
- [x] 변경 기능 및 전체 플로우 최종 검증
- [x] Validation 절에 `/code-review` 와 품질 검증 **실행 결과**를 적는다
- [x] DoD 체크리스트 최종 업데이트 및 체크 완료
- [x] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정
- [x] 🔴 **마지막에 `/commit` 을 실행하고 그 후보를 «이 계획서» 의 `#### Commit Messages` 절에 옮긴다** —
      `/commit` 은 계획서를 모르고 「후보 뒤에 아무것도 덧붙이지 말 것」으로 끝나므로,
      **대화에만 내면 그 절이 빈 채로 남는다.** 체크박스 갱신이 diff 에 더 들어가지만
      커밋 메시지의 내용을 바꾸지 않는다. **커밋은 사용자가 한다 — 후보만 낸다**

**Validation**:

> 순서를 지킨다 — 리뷰에서 고치면 코드가 바뀌므로 품질 검증이 마지막 관문이어야 한다.
> **버그가 0 인 회차에서 끝낸다.** 2회차에도 버그가 나오면 사용자에게 보고하고 3회차 여부를 묻는다 —
> 「그 외」는 목록만 남기고 고치지 않는다. `/impl-plan` 의 「코드 리뷰」 절이 SoT 다.

- [x] `/code-review xhigh` **1회차** (발견 12건 — 버그 0 · 그 외 12 · 조치: 없음 — 목록은 Notes, 조치 여부는 사용자가 정한다)
- [x] `poetry run python validate_project.py` (passed=1378, failed=0, skipped=0)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

> **Done 직전에 `/commit` 을 실행해 이 절을 채운다. 그전에는 비워 둔다.**
> 계획서를 쓰는 시점에는 diff 가 없어 여기 적는 것은 전부 추측이고,
> **추측으로 적은 줄은 그대로 나간다.** 형식·문체 규칙은 `/commit` 이 정한다.

1. 매매 / 중간선거_사이클 승격
2. 매매 / 중간선거_사이클 승격과 레버리지 사다리 집행 규칙 확정
3. 매매 / 중간선거_사이클 3분할 · 2배 절반 진입 · 3배 균등 사다리 · 기간 손절 규칙 확정과 매매 등급 전환
4. 매매 / 중간선거_사이클 미결 넷(대상 · 손절 · 레버리지 · 투자금) 확정에 따른 검증 → 매매 승격과 문서 · 산출물 폴더 이동
5. 매매 / 중간선거_사이클 레버리지 집행 성적의 문서 측정 기록화와 기간 손절 · 1배 금지 범위 예외 명시

## 7) 리스크(Risks)

- **레버리지 수치를 다시 뽑을 수단이 없다** — 사용자 결정으로 CSV 에 넣지 않으므로 스크래치 스크립트가 사라지면 재계산 경로가 문서의 절차 서술뿐이다. 완화: 신설 절 머리에 측정 절차(판정 · 체결 · 합산 · 저가 근사 · 이자 0 · 환율 무시)와 데이터 기간 · 상품 상장일을 적는다
- **근거가 두 해뿐이다** — 추가 매수가 발동한 해는 2014 · 2018 둘이고 둘 다 6월 전에 회복했다. 완화: §4 한계와 §3 결정 근거에 발동 해 수를 숫자로 적는다
- **예비금을 다 쓰면 노출이 평가액의 약 2.5배로 청산까지 간다** — 기한이 「청산 전까지」라 해가 바뀐 뒤 빠지는 해(1938형)에도 레버리지가 올라간다. 추천(12월 말)과 다른 사용자 결정이다. 완화: §3 에 추천과 결정을 둘 다 적는다
- **`trading.md` 대원칙과 투자금 규칙의 예외가 다른 매매법에 새어 나갈 수 있다** — 완화: 조건을 좁게 적고(격자에 평평한 구간이 없음을 보였을 때 + 사용자 명시 / 며칠짜리 · 회당 기대값 작은 매매) 역방향 · 만기_말일이 기존 규칙 그대로임을 대조한다
- **이동과 내용 변경이 한 커밋에 섞이면 rename 탐지와 「재실행이 숫자를 바꿨나」 신호가 흐려진다** — 완화: Phase 2 뒤에 한 번 나눠 커밋하기를 권한다(Notes — 사용자 확인 사항)
- **`summary.json` 이 경로 문자열 때문에 바뀐다** — 이름만 바꾸는 변경인데 파일 하나의 내용이 달라진다. 완화: 대조에서 그 한 줄만 달라졌음을 `diff` 로 확인하고 진행 로그에 적는다
- **맨몸 성적이다** — 환율 · 세금 · 수수료 · 슬리피지를 넣지 않았다. 레버리지 상품의 차입 비용은 실제 가격에 들어 있다

## 8) 메모(Notes)

### 사용자 결정 (2026-09-23, 대화)

- 대상: 3분할 — 현재 포트폴리오가 없다고 밝혔다(겹침 우려가 사라짐)
- 규칙은 집행안 · 판정 기준은 1배 ETF 종가와 다음 거래일 종가 체결 · 판정은 ETF 각자 · 기한은 청산 전까지(예비금은 6월 말까지 대기)
- 사다리: −3 ~ −10% 1% 간격 8단계 · **균등**
- 레버리지 결과는 CSV 에 넣지 않고 문서에만 · 이론값은 넣지 않는다
- 1배 금지는 단기 · 회당 기대값이 작은 매매에만 적용
- 투자금은 사용자가 나중에 정한다 · 표본 하한 미달을 알고 건다를 기재
- 기간 손절만(6월 30일 무조건 매도)

### 사용자에게 확인할 것 (착수 전)

- **미커밋 변경 4건을 먼저 커밋** (Phase 1 첫 항목)
- **커밋을 Phase 2 뒤에서 한 번 나눌지** — 나누면 이 계획서에 `Commit Messages (Phase 2)` 를 더한다
- **§6 에 1억 2천만원 금액 예시를 둘지** — 투자금이 규칙 밖이라 기본은 비율만 둔다

### 실행 환경

- 셸에 `VIRTUAL_ENV` 가 프로젝트 밖으로 잡혀 있으면 poetry 가 `.venv` 를 쓰지 않는다 — 검증이 전부 실패하면 `env -u VIRTUAL_ENV` 를 붙여 다시 돈다 (`.claude/rules/session-bootstrap.md` 5절)

### 문서 불일치 (고치지 않고 알린다)

- `docs/context/` 가 운용 포트폴리오(Q-2-2XS)를 전제로 쓰여 있고 `결과.md` §14.4 가 그것을 인용한다. 사용자는 2026-09-23 에 「현재 포트폴리오 없음」이라고 밝혔다 — **`docs/context/` 는 사용자 소유라 이 계획서에서 고치지 않는다**

### `/code-review xhigh` 1회차 「그 외」 12건 (고치지 않음 — 사용자 결정 대기)

1. **가격 손절 격자를 집행안(2배 절반 + 3배 사다리)으로는 재지 않았다** — `trading.md` 기간 손절 조건의 「평평한 구간 없음」이 1배 격자(§2.3)로만 보였다. 집행안 최저는 −37.61%
2. **사다리 모양(−3 ~ −10% · 1/8 · 3배 · 절반)이 결과를 본 뒤 정해졌다는 사실이 §4 에 없다** — 범위 · 추가 매수 배수의 탈락안 행도 없다
3. **`결과.md` 의 낡은 문장** — §14.4 · 결론 표의 「현재 포트폴리오와 그냥 겹친다」, 「손절 설계가 통째로 새로 필요하다」, §15 「분할과 손절을 합치면 결론이 바뀔 수 있다」
4. **`규칙.md` §3 끝의 「상세 근거는 `설계.md` §2 가 갖는다」가 새 결정 행에는 맞지 않는다**
5. **§2.8 ~ §2.13 의 재현 수단** — CSV 에 넣지 않고도 재현되게 하는 모듈 · 프로브를 두는 선택지
6. **투자금 규칙 「1배 금지」 범위에 문턱 수치가 없다** · 개정 이력 표(2026-09-10)의 「1배 금지」에는 범위가 없다
7. **새 테스트가 레지스트리 한 줄만 확인한다** — 문서 폴더 등급과 `tracks.py` 등급을 대조하는 검사가 없다. 검증 등급이 0 개가 됐다
8. **[사실 오류]** `규칙.md` §6 「주문일 14번」은 (ETF, 날짜) 주문 14건이고 **서로 다른 날짜는 8일**이다
9. **`trading.md` 「확정 칸만 내는 매매법」 절의 「지금 이 길에 있는 것은 만기_말일 하나」** 와 `src/verify_lab/CLAUDE.md` 「`측정.csv` … 지금은 만기_말일」이 낡았다 — 중간선거_사이클도 매매 등급이고 `측정.csv` 를 낸다
10. **`trading.md` 새 문장의 시제와 수치** — 「그 뒤 매매로 승격했습니다(2026-09-23)」는 규칙 문서의 현재형 원칙에 어긋나고, 129.46 → 50.18% 같은 측정값이 규칙 문서에 박혔다
11. **INDEX 이름표 행이 규칙 값(상품 · −3 ~ −10% · 1/8 · 2.5배)을 복제한다** — INDEX 는 위치만 적는 문서다
12. **`tracks.py` 주석이 `docs/매매/…` 경로를 담아** 다음 등급 변경 때 주석도 고쳐야 한다

### 진행 로그 (KST)

- 2026-09-23 13:53: 계획서 작성 (Draft). Phase 1 의 수치 일부는 대화 중 스크래치패드 스크립트(`midterm_leverage.py` · `midterm_yearly_actual.py` · `midterm_ladder.py` · `midterm_pyramid.py`)로 이미 냈다 — 저장소 밖이라 Phase 1 에서 다시 돌려 붙인다
- 2026-09-23 14:05: 승인 전 자체 검증으로 조정 — Phase 0 의 레드 테스트가 Phase 1 · 2 동안 레드로 남아 「그린 유지」에 어긋나 **테스트 추가를 Phase 3 안으로** 옮겼다(Phase 0 삭제). `규칙.md` 절 번호 유지 · 변경 없는 CLAUDE 문서 명시 · 실행 환경 메모를 더했다
- 2026-09-23 14:35: 사용자 승인(「진행」). 착수 전 확인 셋 중 답이 없던 둘은 기본값 — **커밋은 최종 후보만**(Phase 2 분할 없음) · **§6 은 비율만**(금액 예시 없음). 미커밋 4건은 그대로 두고 진행
- 2026-09-23 15:00: Phase 1 완료 — 스크래치패드 `phase1.py`·`phase1_extra.py` 의 출력(`phase1_out.md`·`phase1_extra_out.md`)을 Phase 2 에 붙인다. 판정 기한을 「체결일이 청산일 전」으로 명시했다(대화 때는 청산일 체결을 허용했으나 5해 모두 해당 체결이 없어 값은 같다)
- 2026-09-23 15:00: Phase 2 완료. 규칙.md 는 스크래치패드 `render_rule.py` 로 조립했다 — 옛 절(§2.1 ~ §2.7 · §3 기존 행 · §5)은 원문 그대로 잘라 붙이고(대조 7조각 빠짐 0), 새 표는 Phase 1 출력을 그대로 꽂았다. §3 에서 옛 「분할매수(일시매수)」 · 「등급(검증 유지)」 두 행을 걷어냈다(등급 행은 Phase 3 에서 새로 넣는다)
- 2026-09-23 15:00: **작성 중 발견 — 「2배 절반 ≈ 1배 전액」은 원본가에서만 성립한다.** 양쪽 다 수정주가(배당 포함)로 재면 2배 절반이 5해 중 4해에서 1.00 ~ 2.95%p 낮다(2010 +0.04). 원본가에서 비슷해 보인 것은 1배 쪽 배당도 빠져 있기 때문이다. 2006 원본가 차이 −5.20%p 중 착시는 절반가량(수정주가 −2.37%p). 규칙.md §1.1 · §2.9 와 설계.md §3.8 의 문장을 이 수치로 고쳤다 — **사용자에게 보고할 사항**
- 2026-09-23 15:00: Phase 3 완료. 경로 36곳(8파일) 교체 · `docs/검증/` 과 `storage/results/검증/` 은 비어 삭제. 재실행 산출물 9파일 중 CSV 8 바이트 동일 · `summary.json` 은 `NOTE_AXIS` 경로 한 줄만 다름
- 2026-09-23 15:00: 범위 밖 발견(고치지 않음) — `설계.md` 결정 ⑬ 인용 블록의 「규칙 문서를 고칠지는 사용자가 정한다」는 `trading.md` 가 2026-09-22 에 이미 고쳐져 낡았다. 결정 기록이라 손대지 않고 보고만 한다
- 2026-09-23 15:00: 마지막 Phase — black 변경 0 · `/code-review xhigh` 1회차 버그 0(그 외 12, Notes) · 품질 검증 passed=1378 failed=0 skipped=0. 근거 승격 확인: 결정과 근거는 `규칙.md` §1 · §2.8 ~ §2.13 · §3, 레버리지 결과의 자리와 대가는 `설계.md` 결정 ⑳, 실측은 `설계.md` §3.8, 규칙 문서 예외는 `trading.md` · `투자금_결정/규칙.md` 로 옮겼다
- 2026-09-23 15:00: **시각 정정** — 위 여섯 줄은 처음에 `date` 로 확인하지 않은 추정 시각(14:50 · 15:40 · 16:05 · 16:25)으로 적었고 그중 셋은 아직 오지 않은 시각이었다. 이 시점에 확인해 15:00 으로 다시 적었다. 확인된 기준점은 착수 14:35(`date`) · Phase 3 재실행 14:48(실행 로그) · 마무리 15:00(`date`) 셋이다
- 2026-09-23 15:00: `/commit` 후보 5개를 아래 절에 옮기고 상태를 Done 으로 확정. 커밋 대상은 unstaged + untracked 전체(staged 없음)이며, 착수 전 미커밋 4건(오늘 오전의 분할매수 · 등급 결정)도 이 변경에 함께 들어간다

---
