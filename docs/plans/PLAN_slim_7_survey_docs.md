# Implementation Plan: 경량화 ⑦ — 원달러 문서 5장 → 3장 · 연속_등락 · 괴리 · 선물 정리

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: 🟡 Draft

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-26 16:55
**마지막 업데이트**: 2026-09-26 16:55
**관련 범위**: `docs/조사/원달러_그리드/`(설계 · 사양서 · 규칙) · `docs/조사/원달러_조달.md` · `docs/조사/원달러_ETF_등가성/` · `docs/조사/연속_등락/` · `docs/조사/레버리지_ETF_괴리/` · `docs/조사/선물_대_레버리지_ETF/`, 이 문서들을 가리키는 코드 주석(등가성 · ECOS · FRED · pykrx 수집기), 등가성 `runner.py` 의 `NOTE_*` 문자열, `storage/results/조사/원달러_그리드/`
**관련 문서**: `.claude/rules/research.md`, `.claude/rules/docs.md`, 루트 `SLIM_SERIES.md`, `docs/plans/AUDIT_slim_other_survey_docs.md`, `docs/plans/REPORT_lightweight_inventory.md` §3.6-D · §3.6-E · §10 · §12

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [ ] 목표 1: `docs/조사/원달러_ETF_등가성/설계.md` 를 새로 만들어 **살아 있는 등가성 코드가 쓰는 설계·데이터 실측·사전 등록 합격선**을 모은다 — 지금은 코드가 지워진 그리드 폴더(설계 · 사양서)에 흩어져 있다
- [ ] 목표 2: 그리드의 결론을 `원달러_조달.md` 부록으로 흡수하고 그리드 `설계.md`·`사양서.md`·`규칙.md` 와 산출물 폴더를 지운다 (원달러 5장 → 3장)
- [ ] 목표 3: `원달러_조달.md` §14 「채택한 규칙」을 **측정이 가리키는 조달 원칙**으로 중립화한다 (개인 운용 값 제거)
- [ ] 목표 4: 연속_등락 · 괴리 · 선물 문서의 이력 · 낡은 수치 · 문서↔산출물 불일치 · 개인 서술을 정리한다
- [ ] 목표 5: 등가성 `summary.json` 에 실리는 낡은 문자열(`NOTE_ALPHA` 「총보수 미확인」, `NOTE_TER` 의 출처 경로)을 고치고 재실행한다

## 2) 비목표(Non-Goals)

- **국내 종목을 코드로만 적은 표를 종목명으로 바꾸는 것**(AUDIT §5-E) — 표기 통일이지 경량화가 아니고 줄 수를 늘린다. 보고서 백로그에 남긴다
- **새 측정** — 선물 결과 §6 처럼 문서가 옛 실행 값을 싣고 있으면 **지금 산출물 값으로 바꾸되 재실행하지 않는다**(산출물은 이미 최신 실행이다)
- **`docs/COMMANDS.md` 의 정리** — ⑧. 여기서는 옮긴 문서를 가리키게 **경로만** 고친다
- 레지스트리 slug(`usdkrw_grid`) — 그대로 둔다
- 달력 조사 문서 — ⑥ 에서 끝났다

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- `원달러_그리드/설계.md` 1,651줄 중 **살아 있는 코드가 가리키는 것은 약 250줄**(§2 데이터 소스 · §3.1 ~ §3.4 ECOS·FRED·2019-03-14 이상치·매매기준율 시차 · 결정 C9·C12·C14 ~ C18)뿐이고 나머지는 지워진 그리드 엔진의 실측·결정이다. 코드 주석 11줄과 COMMANDS 6줄이 그 250줄만 가리킨다
- 🔴 `사양서.md`(사용자 작성 원본)는 **죽은 문서가 아니다** — 살아 있는 등가성 코드 13개 파일의 46줄이 §2.1·§6.5·§6.6·§11.3·§16 을 사전 등록 합격선의 출처로 인용한다. 사용자 결정(§10 Q4): **그 절만 옮기고 원본은 지운다**
- 등가성에는 `설계.md` 가 없다(INDEX 「(없음)」) — 그래서 그 검증의 설계가 코드가 지워진 트랙 폴더에 있는 어색한 상태다
- `원달러_조달.md` §14 는 개인 운용 규칙(배정액 · 분할 기간 · 알림 문구 · 현재 값 · 집행 여부)을 담는다
- 복원 경로가 틀렸다 — 그리드 `규칙.md:14`·`원달러_조달.md:20-21` 의 `execution/grid/`·`scripts/run_usdkrw_grid.py`(실제 `strategy/grid/`·`scripts/strategy/run_usdkrw_grid*.py`, 복원 `96c6a4f^`), 연속_등락 `결과.md` 의 `studies/reverse/consecutive.py`(실제 `studies/index_extreme/consecutive.py`·`scripts/studies/run_index_extreme.py`, 복원 `df85c13^` — 적힌 `git log` 은 0건)
- 선물 `결과.md` §6 의 코스피 행이 재실행 전 값이다(롤 −0.05 · 리밸런싱 −0.81 · 그대로 두기 −9.25 · 잔여 +0.15 대 산출물 −0.06 · −0.90 · −9.30 · 0.17) 외 불일치 다수
- 조사 근거: `docs/plans/AUDIT_slim_other_survey_docs.md` 전체

### 이 계획서에서 확정하는 판단

| # | 판단 | 근거 |
| --- | --- | --- |
| 1 | 새 `원달러_ETF_등가성/설계.md`(약 280줄) = 그리드 설계 §2 · §3.1 ~ §3.4 · 결정 C9·C12·C14 ~ C18 · §6 + **사양서 §2.1·§6.5·§6.6·§11·§16 을 따옴표 인용**(사용자 원문이므로 인용 표시와 출처 「원본 사양서 v2.0, 사용자 작성」). 결정 번호(C14 등)는 **그대로 유지**해 코드 주석의 번호가 살아 있게 한다 | §10 Q4 · AUDIT §9 |
| 2 | `원달러_조달.md` 부록(약 60줄) = 그리드 규칙 요약 · 표준 지표와 벤치마크 표 각 1개 · 기각 근거(급등장에 보유가 0이 된다 — **성질로** 서술) · 복원(`96c6a4f^` 경로 한정) · 원자료는 `git show <착수 HEAD>:storage/results/조사/원달러_그리드/…` | AUDIT §9 · §10 Q7 |
| 3 | §14 는 B안 — **D1(조건부 진입 배제 근거) · D4·D5(백분위 표기 방식) · §14.3 직접 환전 대 261240 보유 비용 비교**만 남긴다. 배정액 · 분할 3 ~ 6개월 권장 · 알림 문구 형식 · 사용자 판단 영역 표 · 현재 값 · 집행 여부 · 「목적이 미국주식 매수 자금이라」 근거는 지운다 | §10 Q1 L2 · AUDIT §3 |
| 4 | 기각 사유 문장의 「사용자의 실제 목적」을 **「익절형 그리드는 급등 국면에 보유가 0이 된다」는 성질**로 다시 쓴다. 과제 B 인용(「프로젝트의 출발 질문인 QQQ와 다르게」)은 지운다 | L2 · 보고서 §12.2 |
| 5 | 그리드 설계 §3.7 의 **실계좌 환전 영수증**(개인 거래)은 옮기지 않는다 — 비용 수치 C35(0.783%)는 **「실측 스프레드」라는 사실과 값만** 등가성 설계 또는 조달 부록에 둔다(출처는 「2026-08-24 실계좌 환전 1건」으로 개인 식별 정보 없이) | L2 · AUDIT §3 |
| 6 | 괴리·선물·등가성·조달의 1인칭 서술(「사용자의 가설은 맞다」·「1 ~ 2년을 끌고 갈 생각」·「제가 지목한 것이고」)은 가설·질문 자체를 서술하는 중립 문장으로 | L2 |
| 7 | 연속_등락 결과 게이트 값 인용(「0.5% 이상 → 1.0% 이상」)은 **상수 이름 `MIN_EXPECTED_VALUE`** 로 — 결과 문서의 재실행 기록이 아니라 현재형 판정 서술이면 `trading.md` 규약대로 값을 적지 않는다 | `trading.md` 「문서는 게이트 값을 적지 않는다」 |

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- `.claude/plan-config.json` — 이 저장소의 검증 명령·자동 포맷·근거 승격 목적지 (**값의 SoT**)
- 루트 `CLAUDE.md` — 특히 「계획서 규약 — 이 프로젝트의 설정」 절 (값이 아니라 **판단 근거**)
- `.claude/rules/research.md` — 🔴 **`docs/조사/레버리지_ETF_괴리/결과.md` 가 이 규칙의 「실물 예시」다**(계획서 ①). 그 문서를 고칠 때 필수 구조의 순서를 깨지 않는다
- `.claude/rules/docs.md` — 설계.md 의 「데이터 실측 기록」·「확정된 설계 결정」 형식
- `docs/MEMORY.md` 「이름만 바꾸는 변경과 값을 바꾸는 변경을 한 커밋에 섞지 않는다」 — 이 계획서의 산출물 변경은 `summary.json` 의 `notes` 문자열뿐이다
- 루트 `SLIM_SERIES.md` 「시리즈 공통 주의」

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [ ] `원달러_ETF_등가성/설계.md` 가 있고, 코드 주석(등가성 · ECOS · FRED · pykrx 수집기)의 「사양서 §…」·「그리드 설계 §…」 포인터가 전부 그 문서를 가리킨다
- [ ] 그리드 `설계.md`·`사양서.md`·`규칙.md` 와 `storage/results/조사/원달러_그리드/` 가 없다
- [ ] `원달러_조달.md` §14 에 판단 3 이 반영됐고, 그리드 부록이 있다
- [ ] 복원 경로가 실재한다 — `git show 96c6a4f^:<경로>`·`git show df85c13^:<경로>` 가 파일을 낸다 (스크립트로 전수)
- [ ] 선물·괴리·등가성 결과의 수치가 지금 산출물과 일치한다 (고친 표는 스크립트로 대조)
- [ ] 재실행 결과 **바뀐 산출물이 등가성 `summary.json` 하나**이고 바뀐 것이 `notes` 문자열뿐이다
- [ ] `grep -rn -E '원달러_그리드/(설계|사양서|규칙)\.md|사양서 §' --exclude-dir={.git,.venv,plans} .` 가 0 (코드 주석은 새 문서의 절 이름·결정 번호를 가리킨다)
- [ ] 코드 리뷰 실행 및 결과 기록 (마지막 Phase 의 Validation 에 적는다)
- [ ] 품질 검증 통과 (마지막 Phase 의 Validation 에 passed/failed/skipped 수를 적는다)
- [ ] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [ ] 필요한 문서 업데이트 — `docs/COMMANDS.md` **경로 포인터만**(ECOS·FRED·수정 종가 근거) · INDEX · `.claude/rules/research.md`(한 장 예시 서술) · `scripts/CLAUDE.md`(그리드 기각 근거 포인터) · `SLIM_SERIES.md`
- [ ] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다 (판단 1 ~ 7 은 새 설계.md · 조달 부록 · 각 결과 문서 본문에. 사양서 원본의 처분 결정은 등가성 설계.md 머리말에 「2026-09-26 사용자 결정」으로)
- [ ] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

- 신설: `docs/조사/원달러_ETF_등가성/설계.md`
- 삭제: `docs/조사/원달러_그리드/{설계,사양서,규칙}.md`, `storage/results/조사/원달러_그리드/`
- 수정: `docs/조사/원달러_조달.md` · `원달러_ETF_등가성/결과.md` · `연속_등락/결과.md` · `레버리지_ETF_괴리/{설계,결과}.md` · `선물_대_레버리지_ETF/{설계,결과}.md`
- 코드 주석: `src/verify_lab/studies/usdkrw_equivalence/**`(약 46줄 + 그리드 설계 포인터) · `src/verify_lab/data/{ecos_collector,fred_collector}.py` · `scripts/data/collect_pykrx.py` · `scripts/run_usdkrw_equivalence.py` · 등가성 테스트 주석 — AUDIT §1-A 목록을 grep 으로 다시
- 문자열: `src/verify_lab/studies/usdkrw_equivalence/runner.py` 의 `NOTE_ALPHA`·`NOTE_TER`
- 포인터: `docs/INDEX.md`(원달러_그리드 행 · 등가성 행 · 「찾는 것」 ECOS·FRED 행) · `docs/COMMANDS.md`(경로만) · `.claude/rules/research.md` · `scripts/CLAUDE.md` · `src/verify_lab/CLAUDE.md`(가리키면)
- `storage/results/조사/원달러_ETF_등가성/summary.json`
- `SLIM_SERIES.md` · 보고서 §12 · 이 계획서

### 데이터/결과 영향

- 등가성 `summary.json` 의 `notes` 두 문자열만 바뀐다. 원달러_그리드 산출물 폴더는 지운다(코드가 만들지 않는다)

## 6) 단계별 계획(Phases)

### Phase 1 — 등가성 설계.md 신설과 코드 포인터 이관 (그린 유지)

**작업 내용**:

- [ ] 착수 전: `git status` 깨끗 · 계획서 ⑥ 커밋 확인 · **착수 시점 HEAD SHA 를 진행 로그에**
- [ ] AUDIT §1-A 의 참조 목록을 grep 으로 다시 뽑는다(`사양서`·`원달러_그리드/설계`·`§3.1`·`C14`·`C15` 등) — 옮길 절을 확정하는 기준이 이 목록이다
- [ ] 판단 1 로 `설계.md` 를 쓴다. 등가성 `결과.md` 가 싣고 있는 설계성 데이터 표(§5 환율 계열 · §11 총보수 대 실효 비용)가 그리드 설계와 같은 표면 **한 곳에만** 두고 다른 쪽은 포인터로(AUDIT §2-A)
- [ ] 코드 주석의 「사양서 §x」·「그리드 설계 §x / 결정 Cx」 포인터를 새 문서로 — **결정 번호와 합격선 절 이름은 유지**
- [ ] `NOTE_ALPHA`(「총보수가 아직 확인되지 않았다」 — 같은 결과 문서 §11 이 0.25%·0.45% 를 싣는다)와 `NOTE_TER`(출처 경로)를 고친다. 등가성 테스트가 이 문자열을 고정하는지 먼저 확인한다

**Validation**:

- [ ] `grep -rn '사양서' src scripts tests` 의 남은 줄이 새 문서를 가리키거나 「원본 사양서의 합격선」이라는 출처 설명뿐
- [ ] `poetry run pytest tests/test_studies_equivalence_*.py -q` 통과

---

### Phase 2 — 원달러_조달.md 흡수·중립화와 그리드 3장 삭제 (그린 유지)

**작업 내용**:

- [ ] 판단 2 의 부록을 쓴다. 표는 그리드 `규칙.md` §2 의 표를 **복사**한다(수치 재타이핑 금지)
- [ ] 판단 3 · 4 · 5 · 6. 머리말의 「(2026-09-16 통합) 전에는」 · D7 결정 경위 중복(본문과 결정표) · 현재 백분위 스냅숏 중복(§10.3 과 §14.5 — 하나만 두거나 지운다. 원문이 「반드시 바뀐다」고 적은 값이다) · 틀린 복원 경로
- [ ] 그리드 `설계.md`·`사양서.md`·`규칙.md` 를 지운다(이름으로). `storage/results/조사/원달러_그리드/` 를 지운다
- [ ] 이 셋을 가리키던 곳: INDEX(원달러_그리드 행의 문서 → `원달러_조달.md`, 등가성 행에 [설계], 「찾는 것」의 ECOS·FRED 행 → 새 설계.md 의 절 제목), `.claude/rules/research.md` 의 한 장 예시 서술(「측정(§1 ~ §13)과 채택한 규칙(§14)」 → 중립화 후의 구성으로), `scripts/CLAUDE.md` 의 그리드 기각 근거 포인터, COMMANDS 의 경로(ECOS·FRED·수정 종가·「원달러 그리드 — 코드 삭제됨」 절의 링크), `docs/MEMORY.md` 가 가리키면 그 줄

**Validation**:

- [ ] DoD 의 경로 grep 0
- [ ] `poetry run pytest tests/test_index.py tests/test_research_docs.py -q` 통과
- [ ] `grep -n -E '미국주식 매수 자금|배정액|자산의 절반|제가 지목|토스|한투|환전 장부|영수증' docs/조사/원달러_조달.md docs/조사/원달러_ETF_등가성/*.md` 가 0 이거나 의도된 줄뿐

---

### Phase 3 — 연속_등락 · 괴리 · 선물 (그린 유지)

**작업 내용**:

- [ ] `연속_등락/결과.md`: §5.1 ~ §5.8(원문이 「재현되지 않는다 — 그때 무엇을 봤는지의 기록」이라 적은 장)을 다중 검정 비율 한 줄로 축약(§5.9·§5.10 은 결론이 인용하므로 유지), 복원 경로 정정(판단 없이 사실 — `df85c13^`), 판단 7. `docs/매매/역방향/설계.md` 가 이 문서 §2 로 결정 ④⑤⑧⑩⑯ 을 옮겼다고 적으므로 §2 는 유지
- [ ] `레버리지_ETF_괴리/{설계,결과}.md`: 설계 §7 「설계 변경 이력」 → 「ETF 목록만 훑으면 ETN 을 놓친다」만 §6 실측 기록으로 옮기고 나머지 삭제, 「[2026-09-05 추가]」, 축 3종 → 4종과 「시기」 → 「금리 환경」(`constants.py` 의 출력 컬럼 이름으로), 가리키는 대상이 없는 「위 폴더는」, 없는 루트 절 인용(「사용자의 실제 집행 조건」), 판단 6, 설계 §1 과 결과의 같은 문장 반복 정리. **결과.md 의 필수 구조 순서는 그대로**(research.md 실물 예시)
- [ ] `선물_대_레버리지_ETF/{설계,결과}.md`: 설계 §8 「설계 변경 이력」 정리(괴리와 같은 방식), 「세 방식」→ 넷 · 「거를 것이 넷」→ 다섯 · 없는 §6.4 · 새어 들어온 「목표 4」, 결과 §6 표 · 238-241 · 436(14.7% → 산출물 14.9%) · 167(069500 행 수·기간이 같은 문서 35행 재수집과 모순)을 **지금 산출물 값으로**(스크립트로 뽑아 대조), 관련 파일 표에 빠진 `tests/test_studies_futures_runner.py`, 「사용자가 그 시장을 매매하지 않아 폐기」 행 삭제, 판단 6, 설계와 결과의 문장 반복(핵심 난점 · 6쌍 종목표) 정리
- [ ] `원달러_ETF_등가성/결과.md`: 「총보수가 아직 확인되지 않았다」(192-193 · 353-354) 정정, COMMANDS 의 없는 절 이름(「검증 실행」) 인용을 문서 단위 포인터로, 「(5개)」 테스트 개수를 글롭으로

**Validation**:

- [ ] 선물·괴리 결과에서 고친 수치를 산출물 CSV 와 스크립트로 대조해 불일치 0
- [ ] `poetry run pytest tests/test_research_docs.py -q` 통과

---

### Phase 4 (마지막) — 재실행 대조 · 문서 정리 · 최종 검증

**작업 내용**

> 🔴 **`/commit` 이 «맨 마지막»인 것은 의도다.** 그 스킬은 「후보 뒤에는 아무것도 덧붙이지 말 것」으로
> 끝나므로 **호출하는 순간 그 턴이 거기서 닫힌다.** 중간에 두면 뒤에 적힌 항목이 그 벽 너머에 남는다 —
> 실제로 두 번 그렇게 샜다(`[실측] 2026-09-14` 후보를 계획서에 안 옮김 · `2026-09-16` 옮기고 체크박스를 안 닫음).
> **체크박스와 상태를 먼저 확정하고, 커밋 후보를 마지막에 만든다.**

- [ ] `scripts/run_usdkrw_equivalence.py` 를 인자 없이 다시 돌리고(명령은 COMMANDS), `git diff --stat storage/results/` 가 **등가성 `summary.json` 과 지운 그리드 폴더뿐**이며 `summary.json` diff 가 `notes` 두 줄뿐인지 확인한다. 괴리·선물은 코드가 안 바뀌었지만 주석을 고쳤다면 함께 돌려 불변을 확인한다
- [ ] 필요한 문서 업데이트 (`docs/COMMANDS.md` 경로만 바뀌었는지 확인)
- [ ] 보고서 §12 백로그(§5-E 종목명 표기 포함) · `SLIM_SERIES.md` 진행 표와 인계 메모 갱신
- [ ] 자동 포맷 적용 (`poetry run black .`)
- [ ] 변경 기능 및 전체 플로우 최종 검증
- [ ] Validation 절에 `/code-review` 와 품질 검증 **실행 결과**를 적는다
- [ ] DoD 체크리스트 최종 업데이트 및 체크 완료
- [ ] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정
- [ ] 🔴 **마지막에 `/commit` 을 실행하고 그 후보를 «이 계획서» 의 `#### Commit Messages` 절에 옮긴다** —
      `/commit` 은 계획서를 모르고 「후보 뒤에 아무것도 덧붙이지 말 것」으로 끝나므로,
      **대화에만 내면 그 절이 빈 채로 남는다.** 체크박스 갱신이 diff 에 더 들어가지만
      커밋 메시지의 내용을 바꾸지 않는다. **커밋은 사용자가 한다 — 후보만 낸다**

**Validation**:

> 순서를 지킨다 — 리뷰에서 고치면 코드가 바뀌므로 품질 검증이 마지막 관문이어야 한다.
> **버그가 0 인 회차에서 끝낸다.** 2회차에도 버그가 나오면 사용자에게 보고하고 3회차 여부를 묻는다 —
> 「그 외」는 목록만 남기고 고치지 않는다. `/impl-plan` 의 「코드 리뷰」 절이 SoT 다.

- [ ] `/code-review xhigh` **1회차** (발견 \_\_건 — 버그 \_\_ · 그 외 \_\_ · 조치: \_\_)
- [ ] `poetry run python validate_project.py` (passed=\_\_, failed=\_\_, skipped=\_\_)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

> **Done 직전에 `/commit` 을 실행해 이 절을 채운다. 그전에는 비워 둔다.**
> 계획서를 쓰는 시점에는 diff 가 없어 여기 적는 것은 전부 추측이고,
> **추측으로 적은 줄은 그대로 나간다.** 형식·문체 규칙은 `/commit` 이 정한다.

- [ ] (미작성 — `/commit` 을 실행하고 후보 5개를 **이 자리에** 번호 붙은 줄로 옮긴 뒤 이 줄을 지운다)

## 7) 리스크(Risks)

- **사양서 원문을 잃는다** — 사용자 작성 원본이다. 옮기는 절은 **따옴표로 원문 그대로**, 지운 원본은 git 이력(착수 HEAD)에 있다는 사실을 새 설계.md 머리말에 적는다
- **코드 주석의 합격선 출처가 흐려진다** — 「사전 등록 합격선」이라는 성질(결과를 보기 전에 정했다)이 새 문서에서도 드러나야 한다. 사양서의 §16 합격선 표를 통째로 인용한다
- **`NOTE_*` 를 고치며 다른 요약 키까지 바뀐다** — 재실행 diff 가 `notes` 두 줄뿐인지 확인한다
- **괴리 결과를 고치며 research.md 실물 예시가 깨진다** — 장 순서·제목을 바꾸지 않는다
- **한 세션의 맥락** — 읽을 것 약 6,700줄. Phase 1 → 2 → 3 을 끊어 진행 로그를 남긴다
- **`VIRTUAL_ENV`** — 모든 도구가 실패하면 `env -u VIRTUAL_ENV`

## 8) 메모(Notes)

- 시리즈의 SoT 는 루트 `SLIM_SERIES.md`. 이 계획서는 ⑦ 이며 **커밋 하나**로 끝난다
- AUDIT_slim_other_survey_docs 의 줄 번호는 `f55617a` 기준이고, ① 이 등가성 결과 「프로젝트 맥락에서」 절을 이미 지웠다. ④ 가 `docs/MEMORY.md` 의 원달러 운용 절을 지웠을 것이다(인계 메모 확인)

### 진행 로그 (KST)

- 2026-09-26 16:55: 계획서 작성 (Draft). 착수는 `SLIM_SERIES.md` 를 받은 세션이 한다
