# Implementation Plan: 만기 매매 성적표에 구간 축을 넣고 DIA 6월을 뺀다

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: 2026-09-03 (KST)
**마지막 업데이트**: 2026-09-03
**관련 범위**: `CLAUDE.md`(루트) · `.claude/rules/strategy.md` · `src/verify_lab/strategy/` · `tests/` · `docs/`
**관련 문서**: `.claude/rules/docs.md`, `docs/research/옵션_만기일.md`, `docs/spec/option_expiry.md`

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [x] 목표 1: **성적표에 구간 축을 넣는다** — 칸마다 `전체 · 앞 절반 · 뒤 절반 · 최근 10년 · 최근 5년`
      다섯 행. 균등 2분할만으로는 **「식고 있는가」를 놓친다**
- [x] 목표 2: **표본이 모자란 구간도 행을 남긴다.** 0건이어도 행이 있고 `판정가능` 이 `아니오` 다 —
      **행이 사라지면 사용자가 그 구간을 못 봤다는 사실 자체를 모른다**
- [x] 목표 3: **DIA 6월을 대상 칸에서 뺀다** (7칸). 시기 축이 무너졌다
- [x] 목표 4: **구간 분해 규칙을 루트 `CLAUDE.md` 측정의 원칙으로 올린다** — 다른 검증에도 쓴다

## 2) 비목표(Non-Goals)

- **시장 국면(상승장·하락장·횡보장) 축을 넣지 않는다.** 실측에서 칸당 하락장 표본이
  **3~6건**이고 고점대비 −20% 구간은 0.0~0.5년짜리로 쪼개져 연 1회 신호가 들어가지 않는다.
  직전 1년 ±20% 로 재면 **신호의 69%가 횡보장으로 뭉쳐** 분해가 되지 않는다.
  숫자를 만들어내는 쪽이므로 넣지 않는다 (원칙 12)
- **3분할을 넣지 않는다.** 33건 3분할이 11건이라 **최근 10년(10건)과 사실상 같은 축**이고,
  5칸이 하한 10건을 못 넘는다
- **KODEX 200 9월을 빼지 않는다.** 사용자가 유지로 결정했다. 우려는 한계에 적는다
- **판정 게이트·등급 기준을 바꾸지 않는다.** `measure/screening.py` 를 손대지 않는다
- **`trades.csv` 에 구간 컬럼을 넣지 않는다.** 한 체결이 여러 구간에 동시에 속한다
  (뒤 절반이면서 최근 10년이면서 최근 5년일 수 있다) — 컬럼 하나로 표현되지 않는다
- **손절선을 바꾸지 않는다.** −5% 확정 그대로다

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- **균등 2분할만으로는 신호가 식는 것을 놓친다.** 실물 사례가 DIA 6월이다 —
  2분할에서는 앞 92.9% / 뒤 60.0% 로 살아 있어 보였는데, **최근 5년만 보면 2/6 이고
  회당이 −1.832%** 였다. 최근 구간을 따로 보지 않으면 크기를 알 수 없다
- 사용자가 구간별 판단을 요청했고, 국면 분해까지 검토했으나 **표본이 허락하지 않는 것을 실측으로 확인**했다
- **최근 5년은 어느 칸에서도 5건이라 검정이 안 붙는다.** 그래도 봐야 한다 —
  DIA 6월이 그 증거다. 대신 **판정에 쓰지 않는다는 것이 표에 남아야** 한다

### 확인된 사실 (사전 조사, 2026-09-03)

| 항목 | 값 | 확인 방법 |
| --- | --- | --- |
| 칸별 표본 | 23~33건 (DIA 6월 제외 후 7칸) | 산출물 |
| 2분할 하한 통과 | **7/7칸** (11~17건) | 직접 계산 |
| 3분할 하한 통과 | **2/7칸** (7~11건) | 직접 계산 |
| 최근 10년 | **7/7칸이 10건** — 하한 경계 | 직접 계산 |
| 최근 5년 | **0/7칸** (전 칸 5건) | 직접 계산 |
| 고점대비 −20% 하락장 표본 | 칸당 **3~6건** · 구간이 SPY 15개·KODEX 25개로 쪼개짐 | 직접 계산 |
| 직전 1년 ±20% | 196건 중 **136건(69%)이 횡보장** | 직접 계산 |
| DIA 6월 시기 축 | 앞 +1.117%(92.9%) → 뒤 **−0.167%**(60.0%) · 최근 5년 **−1.832%**(2/6) | 산출물 |

### 영향받는 규칙(반드시 읽고 전체 숙지)

- 루트 `CLAUDE.md` (측정의 원칙 3·11·12·16, 후보 판정 기준)
- `~/.claude/CLAUDE.md` (전역 — 수술적 변경, 목표 주도 실행)
- `.claude/rules/strategy.md` (이벤트형 성적표 · 시기를 쪼개도 유지되는가)
- `src/verify_lab/CLAUDE.md` (**표본 보존** — 조용히 사라진 표본은 생존편향을 만든다, 상수 3계층)
- `tests/CLAUDE.md` (표본 보존 테스트 필수, Given-When-Then)
- `.claude/rules/python.md` · `.claude/rules/docs.md`

---

## 4) 완료 조건(Definition of Done)

- [x] `EXPIRY_CELLS` 가 **7칸**이고 DIA 6월이 없다
- [x] 성적표에 **`구간` · `판정가능`** 컬럼이 있고 칸마다 **5행**(전체·앞 절반·뒤 절반·최근 10년·최근 5년)이다
- [x] **표본 0건인 구간도 행이 남는다** — 신호 0, 지표는 비어 있고 `판정가능` 이 `아니오`
- [x] **최근 N년의 기준일이 실행 시각이 아니라 데이터 마지막 거래일**이다 (같은 데이터면 같은 결과)
- [x] **표본 보존**: `앞 절반 + 뒤 절반 = 전체` 가 칸마다 성립한다
- [x] **전체 행의 값이 이전 실행과 전건 일치한다** (구간 축이 값을 바꾸지 않았다)
- [x] 루트 `CLAUDE.md` 에 **측정의 원칙 17** 이 신설되고 DIA 6월 실물 사례가 들어간다
- [x] `.claude/rules/strategy.md` 이벤트형 성적표에 구간 축이 반영된다
- [x] `docs/research/옵션_만기일.md` 에 DIA 6월 제거 사유와 구간 축이 기록된다
- [x] `docs/spec/option_expiry.md` 에 결정이 승격된다
- [x] `docs/COMMANDS.md` 가 새 산출물 구성을 반영한다
- [x] 새 테스트가 있다 — 구간 분할·표본 보존·0건 행 유지·기준일 고정
- [x] `poetry run black .` 적용
- [x] `poetry run python validate_project.py` 통과 (`failed=0`, `skipped=0`)

---

## 5) 변경 범위(Scope)

| 파일 | 변경 | 이유 |
| --- | --- | --- |
| `CLAUDE.md`(루트) | 측정의 원칙 17 신설 | 다른 검증에도 적용 |
| `.claude/rules/strategy.md` | 이벤트형 성적표에 구간 축 | 매매 규칙 계층 |
| `src/verify_lab/strategy/constants.py` | 구간 상수 · `EXPIRY_CELLS` 에서 DIA 6월 제거 | SoT |
| `src/verify_lab/strategy/expiry_runner.py` | `_summarize` 를 구간별로 반복 | 핵심 |
| `tests/test_strategy_expiry_periods.py` | **신설** | 계약 고정 |
| `docs/research/옵션_만기일.md` · `docs/spec/option_expiry.md` · `docs/COMMANDS.md` | 기록·승격 | SoT |

### 데이터/결과 영향

- 성적표 8행 → **35행** (7칸 × 5구간). `trades.csv` 228 → **199건** (DIA 6월 29건 제외)
- **전체 행의 수치는 바뀌지 않는다**

---

## 6) 단계별 계획(Phases)

### Phase 0 — 계약을 테스트로 먼저 고정한다 (레드)

- [x] 칸마다 **5행**이 나온다
- [x] `앞 절반 + 뒤 절반 = 전체` (표본 보존)
- [x] 홀수 표본이면 **뒤 절반이 하나 많다**
- [x] **표본 0건 구간도 행이 남고** 신호가 0, `판정가능` 이 `아니오`
- [x] 표본 10건 이상이면 `판정가능` 이 `예`, 미만이면 `아니오`
- [x] **최근 N년 기준일이 데이터 마지막 거래일**이다 — 시세를 잘라내면 경계가 따라 움직인다

> **검증**: `poetry run pytest tests/test_strategy_expiry_periods.py` 가 **의도한 실패**를 낸다

### Phase 1 — 구간 축을 넣는다 (그린)

- [x] 구간 상수와 하한(10건)을 `strategy/constants.py` 에 둔다
- [x] `_Block` 에 진입일을 담고 `_summarize` 가 구간별 행 목록을 돌려준다
- [x] `EXPIRY_CELLS` 에서 DIA 6월 제거

> **검증**: 성적표 35행 · `trades.csv` 199건.
> **전체 행을 이전 실행(DIA 6월 제외)과 전건 대조**

### Phase 2 — 규칙 문서에 올린다 (그린)

- [x] 루트 `CLAUDE.md` 측정의 원칙 17
- [x] `.claude/rules/strategy.md` 이벤트형 성적표

> **검증**: 원칙 17이 **DIA 6월 실물 사례와 「관찰용만으로 칸을 떨어뜨리지 않는다」 조항**을 담는다

### 마지막 Phase — 문서 정리 및 최종 검증

- [x] `docs/research/옵션_만기일.md` · `docs/spec/option_expiry.md` · `docs/COMMANDS.md`
- [x] `poetry run black .`
- [x] `poetry run python validate_project.py` (`failed=0`, `skipped=0`)

---

## 7) 리스크(Risks)

| 리스크 | 영향 | 완화 |
| --- | --- | --- |
| 최근 N년 경계가 실행 시각에 묶인다 | 같은 데이터인데 날짜가 지나면 결과가 바뀐다 | **데이터 마지막 거래일 기준**으로 고정. 테스트로 못박음 |
| 5건짜리 행으로 판정하게 된다 | 잡음으로 칸을 뺀다 | `판정가능` 컬럼 + **원칙 17에 「관찰용만으로 떨어뜨리지 않는다」 명시** |
| 구간 축이 전체 값을 바꾼다 | 문서 수치 불일치 | 전건 대조를 Phase 1 검증으로 못박음 |
| DIA 6월 제거가 사후 선택으로 읽힌다 | 과최적화 | **시기 축 붕괴가 근거**임을 결정에 명시 — 뒤 절반이 −0.167% 로 판정용에서 먼저 무너졌다 |

---

## 8) 메모(Notes)

### 확정할 설계 결정

| # | 결정 | 확정 | 탈락안 | 근거 |
| --- | --- | --- | --- | --- |
| ㊵ | **구간 축을 5구간으로 넣는다** | 전체·앞 절반·뒤 절반·최근 10년·최근 5년 | A: 3분할 추가 / B: 시장 국면(상승·하락·횡보) 추가 | A 는 33건 3분할이 11건이라 **최근 10년과 사실상 같은 축**이고 5/7칸이 하한 미달이다. B 는 실측에서 칸당 하락장이 **3~6건**이고, 고점대비 −20% 구간이 0.0~0.5년으로 쪼개져 연 1회 신호가 안 들어간다. 직전 1년 ±20% 로 재면 **69%가 횡보장으로 뭉친다** |
| ㊶ | **표본이 모자란 구간도 행을 남긴다** | 0건이어도 행 유지 · `판정가능` 컬럼으로 표시 | 하한 미달 행을 빼고 낸다 | **행이 사라지면 사용자가 그 구간을 못 봤다는 사실 자체를 모른다.** 패키지 절대 원칙 「표본 보존」과 같은 취지다 — 조용히 사라진 표본은 생존편향을 만든다 |
| ㊷ | **최근 N년의 기준일은 데이터 마지막 거래일** | 시세 파일의 마지막 날 − N년 | 실행 시각 − N년 | 실행 시각 기준이면 **같은 데이터로 돌려도 날짜가 지나면 결과가 바뀐다.** 재현이 이 프로젝트의 존재 이유다 |
| ㊸ | **DIA 6월을 대상 칸에서 뺀다** | 7칸 | 유지 | **시기 축이 무너졌다** — 앞 +1.117%(92.9%) → 뒤 **−0.167%**(60.0%), 최근 6년 중 4년 손실(최근 5년 −1.832%). `.claude/rules/strategy.md` 의 「시기를 쪼개도 유지되는가」를 통과하지 못한다. **성적이 낮아서가 아니라 판정용 2분할에서 먼저 무너졌기 때문**이다 |

### KODEX 200 9월에 대한 유보 (한계에 남길 것)

사용자가 유지로 결정했다. 다만 아래는 결과 문서 한계에 남긴다.

- **24칸(12달 × 청산요일 2) 중 우연확률 0.05 미만이 1칸이고 우연 기대치가 1.2칸이다** —
  정확히 기대치다. 미국은 36칸 중 6칸으로 기대치(1.8)의 3.3배다
- **청산 요일을 목요일로 바꾸면 우연확률이 0.025 → 0.053 으로 유의성이 사라진다**
- **합계 +16.25% 중 상위 3건이 +13.44%(83%)** 다
- 한국은 9월이 하락 달이 아니다 (평균 +1.21%, 12달 중 6위). **미국만 9월이 꼴찌**(−0.91%)다

### 진행 로그 (KST)

- 2026-09-03 — Phase 0 레드 확인 → Phase 1·2 완료
- 성적표 **35행**(7칸 × 5구간) · `trades.csv` **199건**. **전체 행이 이전 실행과 전건 일치**
- **표본 보존 성립**: 앞 절반 + 뒤 절반 = 전체 (7칸 모두)
- 최근 5년은 전 칸 5건이라 `판정가능=아니오`. **남은 7칸은 뒤 절반·최근 구간에서 무너진 칸이 없다**
- 원칙 17 신설 · `.claude/rules/strategy.md` 반영 · 결정 ㊵㊶㊷㊸ 승격
- `validate_project.py` 통과 (`passed=620 failed=0 skipped=0`)
