# ROADMAP

> 이 문서는 **검증 대상 목록과 진행 상태의 SoT**입니다.
> 무엇을 검증했고, 무엇이 남았고, 각 검증이 어떤 상태인지는 여기서만 관리합니다.
>
> 이 프로젝트가 왜 시작됐는지는 [context/README.md](context/README.md)를 참고하세요.
> 출발점은 "운용 중인 포트폴리오의 낮은 QQQ 상관이 타이밍 신호에서 나왔다"는 분해 결과이며,
> 검증 결과를 해석할 때 **"QQQ와 다르게 움직이는가"** 를 함께 봅니다. 다만 그것은 **부가 질문**이고
> **1차 질문은 「통계적 우위가 있는가」** 입니다 — QQQ와 같이 움직여도 우위가 있으면 유효한 발견입니다.
> 두 과제의 관계는 [../CLAUDE.md](../CLAUDE.md) "이 프로젝트의 과제는 둘이다" 가 SoT입니다.

---

## 검증 대상 목록

각 검증은 독립적입니다. 순서는 우선순위이며, 앞의 검증 결과가 뒤를 바꾸지는 않습니다.

| # | 검증 대상 | 시장·자산 | 상태 | 스펙 | 결과 |
| --- | --- | --- | --- | --- | --- |
| 1 | **지수 극단 이벤트** — 역대급 등락 이후 수익률 | QQQ, KODEX 200 | 완료 | [spec/index_extreme_events.md](spec/index_extreme_events.md) | [research/지수_극단_이벤트.md](research/지수_극단_이벤트.md) |
| 1-B | **연속 등락** — N일 연속 상승·하락 이후 수익률. **검증 #1 에서 분리(2026-08-30)** | QQQ, KODEX 200 | 완료 (기각) | 없음 — 결과 문서가 설계를 흡수했다 | [research/연속_등락.md](research/연속_등락.md) |
| 2 | **비트코인 4년 주기설** — 반감기 기준 주기성 | BTC | 미착수 | — | — |
| 3 | **정배열 매매법** — 이동평균 정배열 진입의 우위 | 미정 | 미착수 | — | — |
| 4 | **탑티어 우량주 반토막 매매법** — 고점 대비 -50% 도달 후 수익률 | 미국 대형주 | 미착수 | — | — |
| 5 | **원달러 ETF 등가성** — 261240 이 「환전 + 달러 예치」의 대체재인가 | USDKRW, 261240, 261250 | 완료 | [spec/usdkrw_grid.md](spec/usdkrw_grid.md) | [research/원달러_ETF_등가성.md](research/원달러_ETF_등가성.md) |
| 6 | **달러 조달 방식** — 미국주식 매수 자금용 달러를 어떻게 사서 보유할 것인가 | USDKRW | 완료 | — | [research/달러_조달_방식.md](research/달러_조달_방식.md) |
| 7 | **옵션 만기일** — 월물 만기 앞뒤 상대 거래일의 수익률과 **만기일 매수 → 다음주 금요일 매도** | QQQ, SPY, **DIA**, KODEX 200 | 완료 (2026-08-30 방향 기대값 축 추가) | [spec/option_expiry.md](spec/option_expiry.md) | [research/옵션_만기일.md](research/옵션_만기일.md) |

상태 값: `미착수` / `대기`(스펙 확정, 구현 전) / `진행중` / `완료` / `보류`

> **#6 은 성격이 다릅니다.** 1~5 가 "떠도는 매매법이 통계적 우위를 갖는가"를 물은 반면
> #6 은 **사용자 자금 운용 방식의 선택**을 물었습니다. 별도 스펙 없이 진행했고
> (그리드 트랙의 산출물과 원달러 종가만 씀), 결론은 **"매매 규칙을 얹지 않는다"** 입니다.

### 2번 이후 검증에서 미리 알아둘 것

- **비트코인 4년 주기설은 "특정 하루"가 아니라 "반감기 후 N개월 구간"** 이라, 지금의 이벤트-일 기반 틀에
  그대로 맞지 않을 수 있습니다. 착수 시점에 `measure/` 확장이 필요한지 먼저 판단합니다.
- **탑티어 우량주 검증은 개별종목**이라 생존편향(상장폐지·피인수 종목 제외로 성과가 부풀려지는 현상)을
  다뤄야 합니다. 지수 검증에는 없던 문제입니다.
- 국내 개별종목을 다룰 때는 **2015년 6월 가격제한폭 15%→30% 확대**가 일간 극단치의 물리적 상한을 바꿉니다.
  지수 ETF에는 사실상 영향이 없지만 개별종목에는 있습니다.

#### 비용을 넣을 때 쓸 사용자의 실제 집행 조건 (2026-08-30 확인)

측정의 원칙 10 에 따라 **맨몸 성적을 먼저** 냅니다. 비용 단계로 갈 때 아래 값을 씁니다.

| 항목 | 값 |
| --- | --- |
| 매매 수수료 | **우대 0.1%** (일반 0.25%) — 편도 |
| 슬리피지 | **편도 0.1%** 로 잡습니다 |
| 왕복 합계 | 우대 **0.4%** · 일반 **0.7%** |
| 레버리지 | 사용자는 이런 단기 매매에 **2~3배**를 씁니다 |

- **ETF 총보수는 이 값에 넣지 않습니다.** 이미 종가에 녹아 있습니다
  (QQQ 0.20% · SPY 0.09% · DIA 0.16%).
- **「아래」 방향 신호를 인버스로 집행하면 비용 구조가 다릅니다** — 그 ETF 의 총보수(0.9%대)와
  일일 리밸런싱 손실이 별도로 붙으므로, 레버리지·인버스는 **배수를 곱하는 것으로 끝나지 않습니다.**
- 검증 #7 의 후보 기대값이 +0.30 ~ +1.06% 이므로 **왕복 비용이 결론을 뒤집을 수 있는 크기**입니다.

---

## Phase 진행 상태

| Phase | 내용 | 상태 |
| --- | --- | --- |
| 0 | **부트스트랩** — 프로젝트 틀, 규칙 문서, 하네스, 유틸 이관, 품질 게이트 | 완료 |
| 1 | **데이터 확보** — QQQ 수집기, KODEX 200 pykrx 실측 및 수집기 | 완료 |
| 2 | **공통 계층** — forward return·베이스라인·통계·리포트 | 완료 |
| 3 | **검증 #1 실행** — 지수 극단 이벤트 | 완료 |
| G0 | **원달러 그리드 — 데이터 확보** — ECOS·FRED 어댑터, 단일 값 시계열 계층, ETF 2종 실측·수집 | 완료 |
| G1 | **원달러 그리드 — 등가성 검증** (검증 #5) | 완료 |
| G2 | **원달러 그리드 — 그리드 엔진** — 격자·범위·자금배분·체결·일별 평가 (환전 경로 단독) | 완료 |
| G3 | **원달러 그리드 — 경로 확장과 비용·세금·이자** — 집행 경로 계층, 거래비용, 이자·세금, ETF 2종, 하단 이탈 B안 | **완료** |
| G4 | **원달러 그리드 — 성과 지표와 벤치마크** — 표준 지표, 그리드 전용 지표, 벤치마크 3종 | **완료** |
| G5 | **원달러 그리드 — 견고성 검사와 결과 문서** — 축별 단독 검사 33회, 국면 분할 3축, 결과 문서 | **완료** |
| 4 | **검증 #7 실행** — 옵션 만기일. 만기 달력·offset 계층 신설, SPY 수집, yfinance 수정주가 파일명 분리 | **완료** |
| 5 | **방향 무관 판정 도입** — 측정의 원칙 10~13 과 후보 판정 기준 신설, 순열 검정에 방향 비율 축 추가, 표시 용어 교체, 검증 #7 만기월 축 재측정 | **완료** |
| 6 | **방향 기대값 축 신설과 판정 재편** — 네 기준 AND 를 「1차 게이트 + 등급」 두 겹으로 나누고 게이트에 기대값을 넣음. 경량화 잔여 코드 제거 | **완료** |
| 7 | **가격 기준 원본가 통일 · 산출물 한글 헤더 · 이동평균 SMA 고정** — 측정의 원칙 14·15 신설, 검증 #1·#7 재측정 | **완료** |
| 8 | **후보 판정 버그 수정과 경량화** — 시기 항목이 보합을 「내림」으로 세던 버그, 총자산 곡선 계약을 상시 보유형으로 한정, CLI 종료 코드·표본 보존 보고, 데드코드 10종·중복 상수 3종 정리 | **완료** |

### Phase 0 — 부트스트랩

- [x] 폴더 구조, 설정 파일(pyproject·pyright·pytest·gitignore), 하네스(`.claude/`)
- [x] 규칙 문서 — 루트 `CLAUDE.md`, 계층별 `CLAUDE.md`, `.claude/rules/`
- [x] 참고 자산 이관 — `reference/`(읽기 전용), `storage/market/QQQ_max.csv`.
      **저장소 밖을 참조하지 않는 상태 확보**
- [x] 문서 지도 [INDEX.md](INDEX.md) + 부패 검사 테스트 `tests/test_index.py`
- [x] **`.claude/rules/` 자동 로드 검증** — `**/*.py`·`docs/**` 양쪽 확인. 아래 실측 기록 참고
- [x] 유틸 이관 마무리 — `src/verify_lab/utils/` 의 패키지명 참조 정리, `common_constants.py` 신설
- [x] `tests/test_index.py` 를 이 프로젝트 환경에서 실행 검증 (이관 시점에는 외부 환경에서만 통과 확인함)
- [x] 스모크 테스트와 `validate_project.py` 통과

#### 실측 기록 — `.claude/rules/` 자동 로드 (2026-08-09, 2026-08-29 보강)

**결과: `paths:` frontmatter 자동 로드는 동작한다. 트리거는 편집이 아니라 `Read` 도구다.**

두 패턴을 각각 실측했다.

| 패턴 | 읽은 파일 | 주입된 것 | 확인에 쓴 질문 |
| --- | --- | --- | --- |
| `**/*.py` | `src/verify_lab/utils/logger.py` | `.claude/rules/python.md` 본문 + `src/verify_lab/CLAUDE.md` 본문 | 반올림 규칙 (백분율 2자리 / 비율 4자리) |
| `docs/**` | `docs/ROADMAP.md` | `.claude/rules/docs.md` 본문 | 스펙과 결과의 분리 (설계 변경 → `spec/`, 데이터 갱신 재실행 → `research/`) |

두 질문 모두 **해당 규칙 문서에만 있고 다른 문서에는 없는 값**을 골랐다.
다른 문서에는 "python.md를 따른다" 같은 포인터만 있고 값이 없다.

| 확인된 것 | 내용 |
| --- | --- |
| 트리거 | 해당 경로 파일의 **`Read` 도구 호출**. 편집까지 갈 필요 없음 |
| 함께 주입되는 것 | 경로에 걸린 `.claude/rules/*.md` + 해당 디렉터리 계층의 `CLAUDE.md` |
| **구멍 1** | **기존 파일을 열지 않고 새 파일부터 만드는 경우** 규칙이 주입되지 않는다. 이때는 직접 읽어야 한다. `measure/`·`report/`·`studies/`가 아직 비어 있어 그 계층의 첫 작업이 여기 해당한다 |
| **구멍 2** | **파일을 셸로 읽는 경우** (`cat`·`head`·`sed -n`). 주입은 `Read` 도구에만 걸린다 — 아래 2026-08-29 실측 |

##### 구멍 2 의 실측 (2026-08-29)

같은 `.py` 파일을 두 방식으로 읽어 대조했다. 판별 질문은 **`.claude/rules/python.md` 에만 있는 값**
(출력 데이터 반올림 자릿수)이고, verify-lab 과 동일한 구성(`paths: **/*.py`)을 갖춘 다른 저장소에서 확인했다.

| 읽은 방식 | 주입된 것 | 검색 없이 답했나 |
| --- | --- | --- |
| 셸 (`cat -n <파일>`) | 없음 | 못 함 — "검색해야만 알 수 있다"고 답했다 |
| **`Read` 도구**로 같은 파일 | `.claude/rules/python.md` + 그 계층의 `CLAUDE.md` | 함 |

**주의할 것은 auto 모드다.** auto 모드는 "파일은 `cat`·`head`·`sed -n` 으로 읽어라"를 지시하므로,
그 모드에서는 경로 규칙이 한 번도 안 걸린 채 작업이 끝날 수 있다.

**대응: 코드 파일은 `Read` 도구로 연다.** 셸은 검색·집계에 쓰고,
그 경로의 규칙이 필요한 파일은 `Read` 로 다시 연다.

> **검증 방법에 대한 메모**: 모델에게 "규칙이 로드됐냐"고 그냥 물으면 안 된다.
> `CLAUDE.md`에 적힌 "자동 로드된다"는 문장을 근거로 추측해 답하기 때문이다.
> 실제로 그런 오답이 한 번 나왔다. **해당 규칙 문서에만 있는 구체적 값**을 묻고,
> "파일을 추가로 열지 말고 추측하지 말라"는 제약을 함께 걸어야 한다.

> 유틸 4종(`logger`·`formatting`·`cli_helpers`·`meta_manager`)의 이관이 끝나 옛 패키지명 참조가
> 남아 있지 않습니다. `common_constants.py`가 신설돼 경로·컬럼 상수를 단일 관리하며,
> 확정된 경로 기준점과 컬럼 스키마의 근거는 [src/verify_lab/CLAUDE.md](../src/verify_lab/CLAUDE.md)
> "데이터 저장 규칙"에 있습니다.

#### 실측 기록 — `poetry run` 이 다른 가상환경을 잡는 경우 (2026-08-10)

**증상**: `validate_project.py` 가 Ruff·PyRight·Pytest를 **모두** 실패로 보고하면서
`Command not found: ruff` 를 함께 출력한다. 실패 개수는 1개씩, Pytest는 `passed=0` 이다.

**원인**: 셸에 `VIRTUAL_ENV` 가 프로젝트 밖 인터프리터로 설정돼 있으면 Poetry 2.x 가
**이미 활성화된 그 환경을 우선**하고, `virtualenvs.in-project = true` 로 만들어진 `.venv` 를 쓰지 않는다.
그 환경에는 개발 의존성이 없으므로 도구를 찾지 못한다.

**확인**: `poetry env info --path` 가 프로젝트의 `.venv` 를 가리키는지 본다.
다른 경로가 나오면 `VIRTUAL_ENV` 를 해제하고 실행한다.

> 코드가 아니라 실행 환경 문제다. **품질 게이트가 통과하는데도 실패로 보이는 유일한 알려진 경로**이므로,
> 이 출력을 보면 코드를 고치기 전에 환경부터 확인한다.

### Phase 1 — 데이터 확보

- [x] `data/` 로더 계층 — 모든 시세 로딩의 단일 통로. 스키마·정렬·중복·이상치를 검사하고
      **보간 없이 즉시 예외**를 던진다. 이상치 판정은 수집기와 같은 함수를 쓴다(판정식 단일화)
- [x] **KRX 계정 확보와 `.env` 설정** — `KRX_ID`·`KRX_PW` 준비 완료 (2026-08-10)
- [x] **자격증명 로더** — `data/krx_credentials.py`. `python-dotenv` 로 `.env` 를 환경 변수에 올린다.
      **`import pykrx` 자체가 로그인을 시도하므로**(실측) 호출 측은 로더를 부른 뒤에 pykrx 를 import 한다.
      순서를 구조로 강제하려고 스크립트에서 import 를 함수 안에 둔다
- [x] **yfinance 수집기** — 저장 규칙 확정, QQQ 재수집 완료 (2026-08-11).
      저장 규칙은 아래 "확정된 원시 시세 저장 규칙", 라이브러리 동작 실측은
      [spec/index_extreme_events.md](spec/index_extreme_events.md) §8.
      **가격 기준은 당시 수정주가였고 2026-08-20 에 원본가로 바뀌었습니다**(§7 결정 ⑰)
- [x] **pykrx 실측** — `scripts/data/check_pykrx_etf.py` 로 KODEX 200(`069500`) 확인 완료 (2026-08-11).
      결과는 [spec/index_extreme_events.md](spec/index_extreme_events.md) §8 "pykrx KODEX 200"
- [x] 실측 결과를 [spec/index_extreme_events.md](spec/index_extreme_events.md) §8 에 반영하고 §9 항목 5개를 해소
- [x] **이어붙이기 실측** — `scripts/data/check_pykrx_splice.py` 로 확인 완료 (2026-08-12).
      **수정주가는 조회 시점 기준 최근 3,000거래일만 존재하며 구간을 나눠 이어붙일 수 없다.**
      결과는 [spec/index_extreme_events.md](spec/index_extreme_events.md) §8 결론 4
- [x] **분배락 대응 방침 확정** (2026-08-12) — 원본가 전 기간을 본검증으로 하고, 수정주가 구간을 대조로 병기한다.
      **2026-08-20 에 대조를 걷어내고 원본가 단일 기준으로 바뀌었습니다**(§7 결정 ⑰ 가 결정 ⑨ 를 대체).
      두 결정의 탈락안과 근거는 [spec/index_extreme_events.md](spec/index_extreme_events.md) §7
- [x] **KODEX 200 수집기** — `data/pykrx_collector.py` + `scripts/data/collect_pykrx.py`.
      두 기준을 한 번의 실행으로 받는다 (2026-08-12). 수집 결과는
      [spec/index_extreme_events.md](spec/index_extreme_events.md) §8 "KODEX 200 수집 결과"
- [x] pykrx 가 반환하는 한글 컬럼을 공통 스키마로 정규화 — **수집 시점에 수행합니다.**
      로딩 시점에 하면 로더가 파일의 출처를 알아야 하고 소스가 늘 때마다 분기가 늘어납니다.
      스키마 계약은 [src/verify_lab/CLAUDE.md](../src/verify_lab/CLAUDE.md) 참고.
      **`get_etf_ohlcv_by_date` 의 가격 dtype 이 `uint32` 라 차분에서 언더플로우가 나므로 정규화 시 `float64` 로 변환합니다**
      (같은 pykrx 라도 `get_market_ohlcv` 는 `int64` 로 옵니다 — 함수마다 다릅니다)

> yfinance의 `069500.KS`는 **사용 불가**로 확인됐습니다. 근거는 스펙 문서에 기록돼 있습니다.
> 국내 데이터는 pykrx로 받으며, 착수 전 `reference/pykrx_실측기록.md`를 읽으세요.
>
> **Phase 2 착수에 필요한 데이터가 모두 확보됐습니다.** QQQ는 `storage/market/QQQ_max.csv`,
> KODEX 200은 `069500_max.csv` 이며 **둘 다 원본가**입니다(§7 결정 ⑰).
> 행 수와 기간은 재수집할 때마다 달라지므로, 최신 값은 파일과 `storage/results/meta.json`을 봅니다.

#### 확정된 원시 시세 저장 규칙 (계층 간 계약)

수집기와 로더가 같은 전제를 공유해야 "받아는 놨는데 읽을 수 없는" 파일이 생기지 않습니다.

| 항목 | 규칙 | 왜 |
| --- | --- | --- |
| 파일명 | **가격 기준 하나에 파일 하나.** 기본 기준은 `<종목>_max.csv`, 같은 종목의 다른 기준은 `<종목>_<기준>_max.csv` | 파일명이 **기간**에 따라 갈리면 로더가 어느 파일을 읽어야 하는지 모호해집니다. 그래서 기간은 파일명에 넣지 않고 항상 전 기간을 받습니다. 반면 **가격 기준이 다르면 내용이 다른 데이터**이므로 한 파일에 섞지 않고 나눕니다 |
| 수집 범위 | **받을 수 있는 전 기간.** 분석 시작일로 잘라 저장하지 않는다 | 원시 시세는 불변 자산이고, 기간 절단은 측정 계층의 판단입니다. 잘라서 저장하면 나중에 구간을 넓힐 때 다시 받아야 합니다 |
| 최근 구간 | **확정되지 않은 최근 며칠은 저장하지 않고, 제외 건수를 결과에 담아 반환한다** | 미국장은 한국 시각 기준 하루가 밀리고 마감 직후 값은 확정값이 아닙니다. 국내는 시차가 없어 전일 종가가 확정이지만, **장중에도 당일 행이 그대로 반환되는 것이 실측**됐으므로(2026-08-12 14:09 실행) 당일은 반드시 뺍니다. 미확정 종가가 남으면 그날이 극단 이벤트로 잡힙니다. 제외 건수를 보고하는 것은 표본 보존 원칙 때문입니다 |
| 저장 시점 | **이상치 검증을 통과한 뒤에만 저장한다** | 예외만 던지고 파일을 남기면 반쪽짜리 원자료가 남습니다 |
| 이상치 판정 | 수집기가 자기 판정을 두지 않고 **로더와 같은 함수**를 쓴다 | 판정이 갈라지면 "수집은 통과했는데 로딩에서 막히는" 데이터가 생깁니다 |
| 날짜 표기 | 파일에는 `YYYY-MM-DD`, 로딩 후에는 `datetime64[ns]` | 이미 저장된 원시 시세의 표기이며, 창 기반 연산이 벡터화되려면 로딩 후에는 datetime이어야 합니다 |
| 가격 표기 | **소수점 4자리까지만.** 원화 정수 가격은 정수 그대로 둔다. 자릿수 값의 SoT 는 `.claude/rules/python.md` 반올림 규칙표이며 코드에서는 `common_constants.PRICE_DECIMALS` 한 곳이 정한다 | 그보다 깊은 자리는 실제 시세에 없는 부동소수점 표현 잡음이라 **차트와 대조할 때 그대로 방해가 됩니다**(`169.300003` vs 차트 `169.30`). 수집기와 결과 출력이 **같은 값**을 써야 하므로 두 계층이 각자 정의하지 않습니다 — 갈라져도 예외가 나지 않습니다 |

현재 저장 대상과 그 가격 기준은 아래와 같습니다.

| 파일 | 가격 기준 | 구간 | 소스 |
| --- | --- | --- | --- |
| `QQQ_max.csv` | **원본가** | 전 기간 | yfinance `history(auto_adjust=False)` |
| `069500_max.csv` | **원본가** | 상장일부터 전 기간 | pykrx `get_etf_ohlcv_by_date` |

> **두 종목 모두 원본가입니다.** 사용자가 결과를 차트와 직접 대조하는 것이 전제인데 보통의 차트는
> 배당 미포함이기 때문이며, 확정 근거는
> [spec/index_extreme_events.md](spec/index_extreme_events.md) §7 결정 ⑰ 입니다.
> 그 대가로 배당락·분배락이 하루짜리 하락으로 남지만, **왜곡이 신호군과 베이스라인에 똑같이 걸려
> 초과분에서는 지워집니다**(같은 절의 전환 실측).
> 결과 문서에는 어느 파일로 계산했는지를 반드시 적습니다.

> 외부 라이브러리를 부를 때는 **결과를 좌우하는 인자를 기본값에 맡기지 않고 명시적으로 넘깁니다.**
> yfinance·pykrx 모두 웹 API 래퍼라 기본값이 버전 사이에 조용히 바뀝니다.
> 어느 인자가 왜 그런지는 [spec/index_extreme_events.md](spec/index_extreme_events.md) §8에 있습니다.

### Phase 2 — 공통 계층

**한 덩어리로 만들지 않고 세 조각으로 나눕니다.** 측정·베이스라인·출력을 한 계획서에 담으면
"무엇이 끝났는지"를 체크리스트로 판정할 수 없을 만큼 커지고, 중간에 설계를 바꾸면 앞선 작업이 흔들립니다.
**각 조각이 계획서 하나이며, 앞 조각이 그린인 상태에서 다음으로 갑니다.**

- [x] **2-a `measure/` forward return** — 종가 기준·익일 시가 기준 2종, 스펙 §4 의 측정 구간 6개
      (**익일 시가 기준은 1일 구간만 냅니다** — 2026-08-20 §7 결정 ⑳.
      **집계·초과분·검정에는 종가 기준만 들어갑니다** — 2026-08-21 §7 결정 ㉓),
      구간 끝이 데이터를 넘어가는 신호의 **제외와 제외 건수 반환** (2026-08-13).
      **look-ahead 감시 테스트를 공통 계약으로 고정**하는 것이 이 조각의 일이었고,
      공용 헬퍼가 `tests/conftest.py` 에 있다. 반환 계약은 아래 절이 SoT다
- [x] **2-b `measure/` 베이스라인 3종과 통계 집계** — 단순 보유·무작위 진입(시드 고정)·조건부(200일선),
      평균·중앙값·승률·표본 수·유의성 (2026-08-13).
      **착수 당시에는 조건부 베이스라인을 SMA·EMA 두 벌로 냈고, 2026-08-30 에 SMA 하나로 고정했다**
      (측정의 원칙 15 — 근거는 아래 계약 절). 계약은 아래 절이 SoT다
- [x] **2-c `report/`** — 터미널 표, CSV 저장(**신호일 전체 목록 필수**), 마크다운 표 (2026-08-13).
      계약은 아래 절이 SoT다

> 조각을 더 쪼개지 않는 이유: 2-a 의 반환 형태가 정해지기 전에는 2-b 의 입력이 정해지지 않고,
> 2-b 의 산출 항목이 정해지기 전에는 2-c 가 무엇을 출력할지 정해지지 않습니다. 순서가 곧 의존 방향입니다.

#### 확정된 forward return 반환 계약 (계층 간 계약)

`measure` 가 내는 결과는 2-b(통계 집계)와 2-c(CSV 출력)의 입력이므로 계층 간 계약입니다.
**신호일 × 기준 × 구간을 한 줄씩 담은 long-form 한 장**입니다.

| 컬럼 | 내용 |
| --- | --- |
| `Date` | 신호일. 시세 스키마의 날짜 컬럼을 그대로 재사용합니다 |
| `Basis` | 수익률 기준점 (`close` / `next_open`) |
| `Horizon` | 측정 구간 (거래일) |
| `ForwardReturn` | 수익률 (비율, 0.03 = 3%). 제외된 칸은 비어 있습니다 |
| `ExcludedReason` | 제외 사유. 유효한 칸은 빈 문자열입니다 |

행 수는 언제나 **`신호 수 × 기준 수 × 구간 수`** 이고, 순서는 신호일 → 기준 → 구간 오름차순입니다.

| 항목 | 확정 | 왜 |
| --- | --- | --- |
| 반환 형태 | **long-form** | wide(신호일 한 줄, 구간별 컬럼)로 두면 제외 건수를 별도 자료구조로 들고 다녀야 하고, 그 둘이 어긋나도 테스트가 잡지 못합니다. long-form 은 행 수가 고정돼 **표본 보존 검사가 행 세기로 끝납니다.** 대가는 신호일 한 줄짜리 표를 `report` 가 pivot 해서 만드는 것이며, 표를 만드는 것은 원래 그 계층의 책임입니다 |
| 두 기준의 산식 | 출구는 `D+h 종가` 로 **같고** 입구만 `D 종가` / `D+1 시가` 로 갈립니다 | 두 값의 차이가 곧 "갭으로 새는 몫"이라는 [spec/index_extreme_events.md](spec/index_extreme_events.md) §7 결정 ⑥ 의 해석이 출구가 같을 때만 성립합니다 |
| 제외 조건 | `D+h` 가 데이터 마지막을 넘으면 **두 기준 모두** 제외 | 구간이 1 이상이라 출구가 있으면 익일 시가도 반드시 있습니다. 기준별로 갈릴 수 없습니다 |
| 제외 표본 | 행을 지우지 않고 값만 비운 뒤 사유를 답니다 | 행이 사라지면 생존편향이 생깁니다. `count_excluded()` 가 (기준, 구간) 칸별 신호 수와 제외 수를 함께 냅니다 |
| 측정 구간의 단위 | 거래일. 날짜 연산이 아니라 **위치 인덱스**로만 계산합니다 | 달력일이 섞이면 휴장 구간에서 구간 길이가 조용히 달라집니다 |
| 사건 단위 묶기·참고용 z-score | `measure` 가 아니라 **`studies`** | 확인된 재사용처가 0건입니다. "두 번째 검증에서 재사용되는지 확인한 뒤 확정"(루트 `CLAUDE.md`) 규칙을 그대로 따릅니다. 둘 다 forward return 계산에 관여하지 않아 이 계약에 영향이 없습니다 |

> **look-ahead 감시 계약**은 "뒤를 잘라낸 입력의 결과가 전체 입력의 결과와 **겹치는 범위에서** 같다" 입니다.
> 비교 대상은 **짧은 입력에서 값이 있는 칸**뿐입니다 — forward return 은 정의상 미래를 보므로
> 뒤를 자르면 그 구간이 제외로 바뀌는 것이 정상이고, 그 칸까지 비교하면 계약이 항상 실패합니다.
> 이벤트 판정(bool)은 빈 칸이 없어 같은 헬퍼로 겹치는 구간 전체가 비교됩니다.
> **이벤트 정의를 추가할 때마다 이 헬퍼로 계약을 겁니다.**

#### 확정된 베이스라인·통계 계약 (계층 간 계약)

세 베이스라인은 따로 계산하지 않고 **전 거래일을 신호로 넣은 forward return 테이블 하나에서
파생**시킵니다. 수익률 산식이 한 곳에만 있어야 판정이 갈라지지 않습니다.

| 베이스라인 | 모집단 | 답하는 질문 |
| --- | --- | --- |
| 단순 보유 | 그 테이블 **전체** | 평소보다 나은가 |
| 조건부 | 종가가 200일 이동평균 아래인 날만 거른 것. **SMA 하나만** (2026-08-30 축소) | 같은 국면 안에서 이 날이 특별한가 |
| 무작위 진입 | 위 모집단에서 **신호 수만큼 비복원 추출**을 반복한 것 | 운이 아닌가 |

| 항목 | 확정 | 왜 |
| --- | --- | --- |
| 초과분의 단위 | **통계량끼리 대응해 뺍니다** (평균−평균, 중앙값−중앙값, 승률−승률) | 베이스라인 셋 중 둘은 하나의 숫자가 아니라 분포이고, 신호일과 베이스라인 날짜가 달라 **신호 하나에 대응하는 베이스라인 값이 없습니다.** 따라서 "중앙값 초과"는 중앙값의 차이이지 차이의 중앙값이 아니며, 결과 문서에 그렇게 적습니다 |
| 무작위 베이스라인의 반복 단위 | 반복마다 **신호 수만큼** 뽑아 통계량을 하나씩 냅니다 | "같은 표본 수에서 우연히 이 값이 나올 빈도"를 답해야 합니다. 뽑은 것을 전부 합치면 전체 기간 평균에 수렴해 단순 보유와 같은 값이 되고 질문에 답하지 못합니다 |
| 조건부 모집단의 크기 | 해당하는 날 **전부**. 표본 수 차이는 표에 적습니다 | 표본 수를 맞추는 역할은 무작위 베이스라인이 이미 합니다. 여기서 또 뽑으면 난수가 한 겹 더 끼고 두 베이스라인의 역할이 겹칩니다 |
| 이동평균 창이 차기 전 | 판정하지 않고 **판정 불가 일수를 함께 반환**합니다 | 창이 차기 전 값은 초기 구간에 끌려다닙니다. 판정하지 못한 일수를 돌려주지 않으면 표본이 조용히 줄어듭니다 |
| 이동평균의 종류 | **SMA 하나만 씁니다** (2026-08-30 확정, 측정의 원칙 15) | 전에는 SMA·EMA 를 나란히 냈습니다. 그런데 **두 축의 답이 경계에서 갈렸고**(검증 #1 에서 SMA 3개월 우연확률 0.0509 대 EMA 유의), 둘을 다 내면 "어느 쪽이 유리한가"를 고를 여지가 생깁니다. 과최적화를 막으려면 **보통 쓰는 것 하나**로 고정해야 합니다. 창 길이도 같은 이유로 관행값을 씁니다 |
| 유의성 검정 | **순열 검정 단독.** t-test 는 쓰지 않습니다 | 검증 #1 의 표본은 신호 7건·실질 사건 3개라 정규성과 독립 가정이 확실히 깨져 있고, 그 상태의 p 값은 실제보다 큰 확신을 줍니다. 순열 검정은 같은 표본 수·같은 기간에서 그 값이 나오는 빈도를 직접 세므로 가정이 필요 없습니다 |
| 검정 대상 통계량 | **평균·중앙값·오른 비율·내린 비율 넷 다** (2026-08-29 확장) | **평균은 드문데 중앙값은 그렇지 않다면 소수 사건이 결과를 만들었다는 신호**이며, 이 프로젝트가 반드시 드러내야 하는 상황입니다. 여기에 방향 비율이 더해진 것은 **평균이 방향을 가리는 칸이 실재**하기 때문입니다 — 검증 #7 의 SPY 3월 만기가 평균 +0.312% 인데 내린 비율 64.7% 였고, 그 평균을 근거로 한 판정이 축을 바꾸자 뒤집혔습니다. **네 통계량을 같은 추출 표본에서 냅니다** — 축마다 따로 뽑으면 같은 시드에서도 판정 근거가 어긋납니다 |
| 표본 부족 판정선 | 유효 표본 **10건 미만이면 검정하지 않고 사유를 남깁니다.** 백분위도 함께 비웁니다 | 스펙 §6 의 "표본이 한 자릿수인 칸에는 검정을 붙이지 않는다"를 코드로 옮긴 것입니다. 백분위는 사실상 검정 통계량이라 남겨두면 유의성으로 읽힙니다 |
| p 값 산식 | `(관측만큼 극단인 횟수 + 1) ÷ (반복 수 + 1)` (양측) | 0 이 나오면 "절대 우연이 아니다"로 읽히지만 실제로는 반복 수가 유한하다는 뜻일 뿐입니다 |
| 오른 비율 | 수익률이 **0보다 큰** 비율. 정확히 0인 날은 들어가지 않습니다 | 스펙 §6 이 "양수 비율"로 적었고, 국내 ETF 에서 등락률 0 이 실제로 발생합니다. **표시 이름은 `승률` 이 아닙니다** — "승"은 오르는 쪽에 걸었다는 전제가 숨어 있어 아래로 거는 신호에서 정반대를 가리킵니다 (`.claude/rules/docs.md` 용어 대응표) |
| 내린 비율 | 수익률이 **0보다 작은** 비율. **`1 − 오른 비율` 이 아닙니다** | 보합이 어느 쪽에도 들어가지 않으므로 여집합으로 만들면 값이 부풉니다 — 실측에서 두 비율의 합이 99.70~99.89% 입니다. **2026-08-29 부터 `studies` 가 방향을 고르지 않습니다** — 두 비율을 언제나 나란히 내고, 어느 쪽으로 거는지는 결과를 읽는 쪽이 정합니다 (측정의 원칙 11). 그전에는 `studies` 가 골라 넘겼습니다 (2026-08-21 §7 결정 ㉒) |

> **표본 부족 정책의 결과를 미리 알아 둡니다.** 검증 #1 의 QQQ 신호는 7건이므로
> **(기준 × 구간) 7칸이 전부 "표본 부족으로 검정 불가"** 가 됩니다. 버그가 아니라 결론의 일부이며,
> 결과 문서에 그대로 적습니다.

#### 확정된 출력 계약 (계층 간 계약)

`report` 는 **계산하지 않습니다.** `measure` 가 낸 값을 사람이 읽는 형태로 바꾸고 저장하기만 합니다.

| 항목 | 확정 | 왜 |
| --- | --- | --- |
| 표시 단위 | **저장 직전 백분율 2자리** (우연확률만 4자리). `measure` 는 비율(0~1) 그대로 | `.claude/rules/python.md` 반올림 규칙표가 "백분율(수익률·승률) → 2자리"로 확정했습니다. `measure` 부터 백분율로 내면 "모든 비율 값은 0~1 소수"라는 표기 규칙이 깨집니다 |
| 터미널과 CSV 의 값 | **같은 표시용 프레임 하나**를 함께 씁니다 | 따로 가공하면 반올림 시점이 갈려 화면에서 본 숫자를 CSV 에서 찾지 못합니다. 사용자가 직접 대조하는 것이 이 프로젝트의 전제입니다 |
| 행 정렬 | **구간 → 기준** | 같은 구간의 두 기준이 위아래로 붙어야 "둘의 차이 = 갭으로 새는 몫"이 눈에 보입니다 |
| 집계 3표의 기준 | **한 기준만 받고, `기준` 컬럼을 내지 않습니다.** 둘 이상이면 `ValueError` | 값이 `종가` 하나뿐이라 고를 것이 없는 필터가 됩니다(2026-08-21 §7 결정 ㉔). 다만 컬럼이 없는 채로 두 기준이 섞여 들어오면 같은 구간이 두 줄로 나오는데 어느 줄이 무엇인지 알 수 없으므로, 조용히 통과시키지 않고 거부합니다 |
| 방향 비율 | **오른 비율과 내린 비율을 그대로 나란히 냅니다.** 어느 쪽이 "이긴 것"인지 정하지 않습니다 (2026-08-29 변경) | 전에는 `studies` 가 방향을 골라 `reverse_rate_column` 인자로 넘겼습니다. 그런데 **방향을 가리지 않기로 하면서**(측정의 원칙 11) 고를 필요가 사라졌고, 하락 방향 신호에서는 같은 값이 두 열에 오는 중복도 있었습니다. `report` 가 방향을 모른다는 성질은 그대로입니다 |
| 신호일 목록 | **신호일 한 줄**에 구간별 수익률을 펼칩니다. 컬럼 이름은 `기준 구간`(예: `종가 1개월`) | long-form 은 한 신호일이 칸 수만큼 여러 행으로 흩어져 눈으로 대조할 수 없습니다. 2-a 가 long-form 을 택하면서 이 pivot 을 `report` 의 몫으로 미뤄 뒀습니다. 검증별 컬럼(순위·사건 번호·z-score)은 `studies` 가 넘겨 날짜 뒤에 붙습니다 |
| 제외·미검정 칸 | 제외는 **빈칸**, 미검정은 **사유 문자열** | 제외된 칸을 0 으로 채우면 "수익률 0%"로 읽힙니다. 반대로 검정 사유는 결론의 일부라 빈칸으로 두면 "아직 안 돌았다"로 읽힙니다 |
| 산출물 | `storage/results/<실행시각>_<검증명>/` 에 CSV 4개(`signals`·`statistics`·`excess`·`test`)와 `summary.json` | 네 표는 컬럼 구성이 완전히 달라 한 파일에 못 섞습니다. 베이스라인 long-form 원자료(전 거래일 × 7칸 = 5만 행)는 재생성 가능하므로 남기지 않습니다. **폴더 생성과 저장은 `report` 가 소유**합니다 — 스크립트마다 두면 경로 규칙이 조용히 갈라집니다 |
| 마크다운 | **내지 않습니다** (2026-08-30 삭제) | 원래는 "표 렌더링까지만" 이었습니다 — 결과 문서 구조의 SoT 가 `docs/research/CLAUDE.md` 하나여야 하므로 코드가 뼈대를 찍지 않는다는 뜻이었습니다. 그런데 **표 렌더링조차 호출된 적이 없습니다.** 결과 문서의 표는 사람이 산출물 CSV 를 보고 직접 씁니다. 필요해지면 git 이력에서 꺼냅니다 |

### Phase 3 — 검증 #1 실행

**Phase 2 와 같이 세 조각으로 나눕니다.** 이벤트 정의의 반환 형태가 정해지기 전에는 실행
스크립트가 무엇을 순회할지 정해지지 않고, 실행 결과가 나오기 전에는 결과 문서에 쓸 것이 없습니다.
순서가 곧 의존 방향입니다.

- [x] **3-a `studies/index_extreme/` 이벤트 정의** — 테스트 A(확장창 순위)와
      신호일 목록 부가 컬럼(당시 순위·사건 번호·참고용 z-score) (2026-08-13).
      **연속 등락 정의는 2026-08-30 에 제거**됐다 (`research/연속_등락.md`).
      **look-ahead 감시 계약을 네 산출 전부에 걸었고**, 스펙 §8 의 기록값을 그대로 재현했다.
      계약은 아래 절이 SoT다
- [x] **3-b 실행 계층** — `studies/index_extreme/runner.py` + `scripts/studies/run_index_extreme.py` (2026-08-14).
      강건성 조합 396개를 한 실행에서 산출하고 국내 두 가격 기준을 같은 실행 안에서 계산한다.
      스펙 §8 의 기록값(신호·사건 수)을 그대로 재현했다.
      계약은 아래 절이 SoT다
- [x] **3-c 결과 문서** — [research/지수_극단_이벤트.md](research/지수_극단_이벤트.md) (2026-08-14).
      실행 산출물 `20260814_164136_index_extreme` 를 읽어 스펙 §12 의 표 7종을 채웠다. 판정은 아래 절이 요약이다

#### 후속 작업 — 결과 문서를 단기 구간으로 개정 (미착수)

- [ ] **결과 문서 §5~§13 을 단기 구간 결과로 다시 쓴다.**
      2026-08-21 에 측정 구간이 **1·2·3·5·10·21 거래일**로 바뀌었고(§7 결정 ㉑),
      `역방향 비율` 이 새 지표로 들어왔다(§7 결정 ㉒). 그래서
      [research/지수_극단_이벤트.md](research/지수_극단_이벤트.md)
      의 3개월·6개월·1년 표는 **현재 코드로 재현되지 않는다.**
      문서 최상단과 §16.5·각 장 머리에 그 사실을 적어 뒀으므로 잘못 읽힐 위험은 막혀 있지만,
      본문은 여전히 장기 구간 기준이다
- [ ] **방향 `역방향 전체` 신호군을 결과 문서에 반영한다.**
      2026-08-23 에 방향 축에 `역방향 전체` 가 추가됐다(§7 결정 ㉕). **사용자가 실제로 하는 매매가
      이것**이므로 결과 문서의 판정도 이 방향을 함께 봐야 한다. 한계 섹션에는 두 가지를 적는다 —
      대칭 모집단이 신호군의 상승:하락 비율과 어긋난다는 것, 그리고 부호를 뒤집은 값이
      인버스 상품의 손익이 아니라는 것
- 대상 산출물: 단기 구간은 `storage/results/20260821_165557_index_extreme/`,
  `역방향 전체` 까지 담은 것은 `storage/results/20260823_131353_index_extreme/`
- **결론 10개 중 4개(1·7·8·10)는 신호 정의 기반이라 그대로 유효하다.** 나머지는 재계산 대상이며,
  어느 것이 그런지는 결과 문서 §1 표의 "구간 의존" 열에 있다

#### 후속 산출 — 매매 규칙 도출 (2026-08-23, 완료)

측정 결과로부터 **역방향 매매 규칙**을 도출해 `docs/strategy/역방향_매매_규칙.md` 에 남겼다.
**이것은 측정이 아니다** — 루트 `CLAUDE.md` 가 "하지 않는 일"로 둔 손절·보유기간 설계이며,
`strategy/` 폴더에만 적용되는 예외다(`.claude/rules/strategy.md`).

- 대상은 테스트 A 역방향 전체 3종 — KODEX 200 K=10 / QQQ K=10 / QQQ K=20, 전부 2008 시작
- 규칙은 **종가 진입 · 단일 손절 -5%(진입가 기준 고정) · 수익 시 즉시 청산 · 보유 한도 D+2** 다
  (2026-08-30 개정 — 그전에는 손절 3분할과 한도 3종이었다)
- 실행 계층은 `src/verify_lab/strategy/` + `scripts/strategy/run_reverse_trading.py` 다.
  **기존 검증 경로는 그대로 두었고**, 재실행 결과가 `20260823_131353_index_extreme` 과 완전히 동일함을 확인했다
- **2026-08-30 에 테스트 B 축을 제거**했다. 신호군이 376 → 96개로 줄었고
  **테스트 A 산출물은 네 파일 전수 대조에서 한 칸도 바뀌지 않았다.**
  방향 개별·시대 구간·장기 구간은 남겼다 — `excess`·`test` 를 지우면
  "평소보다 나은가·우연인가"를 다시 낼 수단이 사라진다

#### 검증 #1 의 결론 (결과 문서가 SoT, 여기는 다음 검증이 알아야 할 것만)

- **QQQ 테스트 A 는 사건 3개라 검정이 붙지 않는다.** 1,080칸 전부 미검정이며, 이것이 결론의 일부다
- **연속 등락은 우연 수준이라 2026-08-30 에 분리·제거했다.** 단순 보유 대비 유의 비율이 5.8% 로
  우연 기대치 5%와 같았고, 사용자 판정 기준을 **576칸 중 6칸(1.0%)** 만 통과했다(역대급 등락은 56.8%).
  기록은 `research/연속_등락.md` 에 있다. **유의 비율을 판정 기준으로 쓰는 방식이 이 검증에서 처음 쓰였고 잘 작동했다**
- **KODEX 200 테스트 A 는 신호 단위로 유의하지만 사건 단위로는 간신히다.** 순열 검정이 신호 수로 계산되므로
  한 사건에서 나온 15건을 15번의 독립 관측으로 센다. **사건 단위 확인을 함께 싣는 것이 필수다**
- **두 시장의 극단 등락 신호가 같은 날 켜진다** — QQQ 신호 7건 전부가 KODEX 200 신호로부터 9일 이내다.
  `docs/context/` 가 찾는 "QQQ와 다르게 움직이는" 신호가 아니다
- **국내 2026년 신호 18건은 forward return 이 미완이다.** 1년 뒤 재실행하면 KODEX 200 테스트 A 결과가 크게 바뀔 수 있다
- **측정 구간이 단기로 바뀌었다 (2026-08-21, §7 결정 ㉑).** 사용자가 재는 대상이 단기 매매로 옮겨갔고,
  `역방향 비율`(§7 결정 ㉒)이 인버스 상품으로 집행되는데 그 상품이 일간 리밸런싱이라 구간이 길면
  원지수 방향과 손익 부호가 어긋나기 때문이다. **신호 판정은 한 칸도 바뀌지 않았다** —
  식별 6컬럼과 신호일 부가 컬럼을 12,679행 전수 대조해 불일치 0건이었고, 겹치는 구간(1일·1주·1개월)의
  값도 그대로였다. **다음 검증도 단기 구간을 기본으로 한다**
- **가격 기준을 원본가 단일로 바꿨다 (2026-08-20, §7 결정 ⑰).** 사용자가 차트와 직접 대조할 수 있어야 하고 보통의 차트는 배당 미포함이기 때문이다.
  **결론은 바뀌지 않았다** — QQQ 테스트 A 신호 7건은 날짜·등락률·사건 번호가 완전히 동일했고,
  절대 수익률만 1년 기준 -0.889%p 낮아졌을 뿐 초과분은 +0.058%p 로 사실상 그대로다.
  **다음 검증도 원본가를 기본으로 한다**

> **3-b 의 범위는 이미 정해져 있습니다.** 강건성 조합(순위 컷 3개 × 시작연도 4개 × 방향 3개,
> 연속 일수 8개 × 시작연도 4개 × 방향 3개 — 방향에는 `역방향 전체` 가 포함됩니다)을
> **한 실행에서 전부 산출**합니다. 결과 문서는
> QQQ 와 KODEX 200 을 한 문서에 담되 표는 스펙 §12 대로 종목별로 나눕니다.
>
> 조합을 한 번에 도는 근거는 측정의 원칙 1(여러 설정값을 나란히 보고)이며, 실행 시간이 걸림돌이
> 아님을 실측으로 확인했습니다 — 순열 검정 1회(12칸, 반복 1,000, 모집단 6,896일)가 **0.77초**라
> 전 조합을 돌려도 10분 내외입니다. 모든 데이터셋을 한 실행에 두는 것은 **나란히 놓고 보는 전제가
> "파라미터가 같았다"** 이기 때문입니다. 따로 돌리면 그 사실을 사람이 확인해야 하는데,
> 확인을 빠뜨려도 표는 정상으로 보입니다.
>
> **가격 기준이 원본가 단일로 바뀌면서 데이터셋이 3종에서 2종으로 줄었습니다**(§7 결정 ⑰).
> 조합은 396개에서 **264개**가 됐습니다.

#### 사용자 직접 대조 진행 상태 (2026-08-20 기준)

절차는 [HANDS_ON.md](HANDS_ON.md) 가 SoT다. 대조 대상 산출물은 **`20260820_170440_index_extreme`**(원본가·가격 4자리·식별 컬럼 6개)이며,
**Phase 3 자체는 완료 상태 그대로다** — 대조는 결과를 사용자가 자기 손으로 확인하는 별도 활동이다.

| 단계 | 확인할 것 | 상태 |
| --- | --- | --- |
| 1 | 실행 완료. "검증 대상 시세" 행 수·기간이 결과 문서 §3.1 과 일치 | **완료** (원본가 실행으로 재확인됨) |
| 2 | `signals.csv` 컬럼 25개(식별 7 + 신호일 6 + 수익률 12) 구조 확인 | **완료** (컬럼 구성은 바뀌지 않았다) |
| 3 | QQQ·K=10·2003 필터가 **7줄**이고 날짜·등락률·**종가**가 차트와 일치 | **완료** (2026-08-20, 가격 4자리 산출물로 사용자가 7줄과 종가를 직접 확인) |
| 4 | 원본 CSV 에서 등락률을 손으로 계산 | **완료** (2026-08-20, 사용자가 2020-03-16 = -11.98% 를 직접 계산해 대조) |
| 5~8 | 수익률 손계산 / 사건 묶기 / 베이스라인·초과분 / 검정 불가 | **완료** (2026-08-20) — 아래 검산 결과 참고 |

**대조 8단계를 모두 통과했다.** 3·4단계는 사용자가 차트와 계산기로 직접 확인했고, 5~8단계는
**전부 파일 안에서 닫히는 검산**이라 원본 CSV 로 다시 계산해 산출물과 대조했다.

| 단계 | 검산 방법 | 결과 |
| --- | --- | --- |
| 3 | `signals.csv` 필터 → 차트 대조 | **7줄**, 종가가 차트와 일치 (사용자 직접) |
| 4 | 등락률 손계산 | 2020-03-16 = **-11.98%** (사용자 직접) |
| 5 | 신호 7건 × 6구간 × 2기준을 `QQQ_max.csv` 로 재계산 | **84칸 전부 일치, 불일치 0개** |
| 6 | 앞 신호와의 달력 간격으로 사건 번호 재계산 | `1,1,1,1,2,2,3` 로 파일과 일치. **신호 7건 = 사건 3개** |
| 7 | `statistics` − `excess` 로 베이스라인 역산 | `54.90 − 38.67 = 16.23%`. 베이스라인 승률 **85.82%** 이므로 7건 연속 플러스는 **34.3%** 확률로 그냥 일어난다 |
| 8 | `test.csv` 의 미검정 사유와 유의 비율 | QQQ 테스트 A **1,080칸 전부** 검정 불가(유효 표본 최대 7건 < 판정선 10건). 테스트 B 는 714칸 중 **31칸(4.3%)** 으로 기대치 36칸보다 적다 |
| 15번째 체크 | KODEX 200·K=10·2011 필터 | **27줄**(2026년 18건), 종가 전부 정수, 사건 7개, 1년 수익률 미완 18건 |

> **"내가 확인했다"의 경계.** 파일 숫자가 실제 시장과 맞는지(차트 대조)는 사용자만 할 수 있고 3단계에서 끝냈다.
> 나머지는 파일 안에서 닫히므로 누가 계산해도 같은 값이 나온다 — 그 사실 자체가 재현성의 증거다.

#### 대조가 진행되던 도중의 기록 (위 표가 최종 결과다)

아래는 **8단계를 마치기 전**에 산출물이 세 번 바뀌면서 남은 과정 기록이다. "재개 지점"을 말하는
문장들은 그 시점의 상태이며, **최종 판정은 위 표의 「8단계 모두 통과」다.**

**대조 도중 가격 기준이 원본가로 바뀌었다** (2026-08-20, §7 결정 ⑰). QQQ 종가와 수익률이 전부 달라져
[HANDS_ON.md](HANDS_ON.md) 의 손계산 예시를 새 값으로 갱신했다. **재개는 3단계(`signals.csv` 확인)부터다** —
1~2단계는 가격 기준과 무관하게 통과한 항목이라 다시 하지 않아도 된다.

**이어서 같은 날 가격 자릿수가 4자리로 바뀌었다** (2026-08-20, §7 결정 ⑱). 3단계에서 `signals.csv` 의
`169.300003` 이 차트 `169.30` 과 어긋나는 것이 드러났고, 그 꼬리가 실제 시세가 아니라 부동소수점 표현
잡음이었기 때문이다. **신호 판정은 하나도 바뀌지 않았고**(신호일 집합 완전 동일) 손계산 예시가 짧아져
검산이 오히려 쉬워졌다. 전환 실측은 [spec/index_extreme_events.md](spec/index_extreme_events.md)
§8 "가격 자릿수 4자리 전환" 에 있다. **3단계는 이 산출물로 통과했고, 재개 지점은 4단계다.**

**이어서 같은 날 익일 시가 기준이 1일 구간만 남았다** (2026-08-20, §7 결정 ⑳). `signals.csv` 가
24컬럼 → **19컬럼**, 집계 3파일이 신호군당 12행 → **7행**이 됐고, 대조 산출물은
**`20260820_174144_index_extreme`** 이다. **남은 칸의 값은 한 칸도 바뀌지 않았고 신호 판정도 그대로다.**
위 2단계의 컬럼 수와 5단계의 84칸(7건 × 6구간 × 2기준)은 **그 시점 산출물에 대한 기록**이며,
지금 다시 세면 19컬럼·49칸이다. 익일 시가 장기 구간이 필요하면 갭 배수를 역산해 되살린다
([HANDS_ON.md](HANDS_ON.md) 5단계). 전환 실측은
[spec/index_extreme_events.md](spec/index_extreme_events.md) §7 "산출물 축소 실측" 에 있다.

> **이번에는 종가까지 대조 대상이다.** 이전에는 QQQ 가 수정주가라 날짜·등락률로만 맞출 수 있었지만,
> 지금은 **종가가 차트 숫자와 그대로 같아야 한다.** 어긋나면 그 자체가 발견이다.

대조 중 확인된 사실을 [HANDS_ON.md](HANDS_ON.md) 에 반영했다.

- **데이터 끝 날짜가 실행일보다 앞선다.** 수집할 때 확정되지 않은 최근 며칠을 버리기 때문이며
  (미국 2일·국내 1일, `RECENT_EXCLUSION_DAYS`), QQQ 8/11 수집 → 8/7, KODEX 200 8/12 수집 → 8/11 로
  계산이 정확히 맞는다. **버그가 아니라 설계다** — 미확정 종가가 남으면 그날이 극단 이벤트로 잡힌다
- **재현성이 세 번째 실행에서도 유지됐다.** 2026-08-14·08-18·08-19 실행의 CSV 4개가 md5 동일이며,
  확인 명령어를 HANDS_ON 에 실었다
- **터미널은 미리보기일 뿐이다.** 화면 발췌 표는 신호군 375개 중 66개이고 신호일 목록이 없다.
  대조 8단계 중 터미널을 쓰는 것은 1단계뿐이다
- **QQQ 수정주가의 조정이 상수 배율임을 실측했다.** 2020-03 구간에서 배율 1.038787 이 인접 3일에
  동일하게 걸렸고, 등락률은 파일·차트 양쪽에서 -9.17% 로 같았다. **등락률 기준 판정의 근거가
  실측으로 뒷받침됐다.** 기록은 [spec/index_extreme_events.md](spec/index_extreme_events.md)
  "데이터 소스 실측 — QQQ 수정주가의 조정 배율", 가리는 절차는 [HANDS_ON.md](HANDS_ON.md) 3단계

미해결로 남긴 것: **터미널 "검증 대상 시세" 표의 헤더 정렬이 깨진다**(`행 수기간` 으로 붙어 나옴).
표시만의 문제로 숫자에는 영향이 없으며, 코드 수정이므로 계획서가 필요하다.

#### 확정된 이벤트 정의 계약 (계층 간 계약)

`studies` 가 공통 계층에 넘기는 것은 **"어느 날이 신호인가"** 하나입니다. 설계 결정의 근거는
[spec/index_extreme_events.md](spec/index_extreme_events.md) §7 결정 ①~⑬ 이 SoT이며,
여기에는 **계층 사이에서 지켜야 할 형태**만 적습니다.

| 항목 | 확정 | 왜 |
| --- | --- | --- |
| 넘기는 값 | 시세와 **같은 인덱스의 bool Series 하나**. 부가 컬럼은 별도 함수로 따로 낸다 | 이 계약을 지키면 `measure`·`report` 를 손대지 않고 검증을 추가할 수 있습니다. 판정과 부가 정보를 한 반환값에 섞으면 공통 계층이 검증별 컬럼을 알아야 합니다 |
| **집계 시작일** | **이벤트 정의 함수가 인자로 받아 안에서 처리한다.** 시작일 이전의 날은 신호로 세지 않되 순위 축적과 연속 길이 누적에는 넣는다 | 호출 측이 시세를 먼저 잘라 넘기면 순위가 그 지점부터 다시 쌓이고 걸쳐 있던 연속이 끊깁니다. **결과는 달라지는데 예외는 나지 않습니다.** 사고를 구조로 막으려고 시작일을 함수가 소유합니다 |
| 확장창 순위 | **자기보다 극단인 날의 수 + 1.** 동률은 같은 순위를 받는다 | 동률에 순번을 매기면 나중 날이 컷 밖으로 밀려 신호 수가 조용히 달라집니다. 국내 원본가는 정수 가격이라 같은 등락률이 실제로 나옵니다 |
| 등락률 산식 | **한 곳에만 둔다.** 두 테스트와 z-score 가 같은 함수를 쓴다 | 판정식 단일화입니다. 같은 값을 두 곳에서 계산하면 두 곳이 갈라지고, 그때 어느 쪽이 맞는지 판별할 방법이 없습니다 |
| 부가 컬럼의 단위 | **내부 토큰과 비율(0~1)로 낸다.** 한글 레이블과 백분율 변환은 하지 않는다 | 표시용 변환은 `report` 의 몫이라는 기존 경계를 그대로 따릅니다. 다만 `report` 는 검증별 컬럼을 모르므로, 그 컬럼을 붙이는 것은 실행 스크립트의 일입니다 |
| 부가 컬럼의 역할 | **해석 보조이며 판정에 쓰지 않는다** | 스펙 §11 이 z-score 를 "판정 기준이 아니라 참고 컬럼"으로 못 박았습니다. 판정에 섞이면 이벤트 정의가 두 개가 됩니다 |
| look-ahead 감시 | **이벤트 정의뿐 아니라 부가 컬럼에도 건다** | 사건 번호와 z-score 도 신호일 목록에 실려 사용자가 읽는 값입니다. 미래를 참조하지 않는다는 절대 원칙에 예외를 두지 않습니다 |

#### 확정된 실행 계층 계약 (계층 간 계약)

실행 계층은 **계산하지 않습니다.** 이벤트 정의·측정·출력을 조합해 돌리고, 어느 행이 어떤 설정의
결과인지를 붙여 쌓기만 합니다. 설계 결정의 근거는
[spec/index_extreme_events.md](spec/index_extreme_events.md) §7 결정 ⑭~⑯ 이 SoT입니다.

| 항목 | 확정 | 왜 |
| --- | --- | --- |
| 로직의 위치 | 조합 순회와 조립은 **`studies/<검증명>/runner.py`**, `scripts/` 는 인자 파싱·호출·표시·저장만 | `scripts/CLAUDE.md` 가 CLI 계층에 도메인 로직을 금지했고, 수집 경로 2곳이 이미 같은 관용입니다. 이 저장소의 테스트는 전부 `src` 모듈에 붙으므로, 조합 순회를 스크립트에 두면 **가장 조용히 틀릴 수 있는 코드가 테스트 밖에 남습니다** |
| 잘라내는 대상 | **언제나 신호 선택이지 시세가 아닙니다.** 시작일은 이벤트 정의 함수에 넘기고, 구간의 끝은 판정이 끝난 뒤 거릅니다 | 시세를 먼저 자르면 확장창 순위가 그 지점부터 다시 쌓이고 걸쳐 있던 연속이 끊깁니다. **결과는 달라지는데 예외가 나지 않습니다.** 끝을 자르는 것은 안전합니다 — 판정이 미래를 보지 않으므로 뒤를 잘라도 앞의 값이 그대로입니다 |
| 베이스라인 모집단 | 전 거래일 forward return 테이블 **하나에서 파생**시키고, **신호군과 같은 기간**으로 자릅니다 | 수익률 산식이 한 곳에만 있어야 판정이 갈라지지 않습니다. 기간을 맞추지 않으면 초과분의 차이에 "그 사이 시장이 어땠는가"가 섞입니다 (결정 ⑭) |
| 식별 컬럼 | **`report` 가 아니라 실행 계층이 붙입니다.** 네 표 모두 같은 순서로 앞에 옵니다 — 종목·테스트·파라미터·시작연도·방향·시대 구간 **6개**입니다 | `report` 는 어떤 검증이 자기를 쓰는지 몰라야 하므로 검증별 컬럼을 알 수 없습니다. 계산이 아니라 조립이라 실행 계층의 몫입니다 |
| 가격 기준의 자리 | **식별 컬럼이 아니라 `summary.json` 의 `datasets` 와 터미널 "검증 대상 시세" 표에 둡니다** (스펙 §7 결정 ⑲) | 가격 기준은 **행 단위가 아니라 데이터셋 단위 속성**입니다. 원본가 단일 기준으로 확정된 뒤로는 값이 하나뿐이라, 행마다 반복하면 고를 것이 없는 필터가 대조에 하나 끼어듭니다. 대신 **산출물의 데이터셋 구분자가 `종목` 하나뿐**이 되므로 티커가 겹치면 행이 조용히 뒤섞입니다 — `DATASETS` 의 `ticker` 중복 금지를 불변조건 테스트로 겁니다 |
| 신호 수·사건 수 | 네 표에 **각각 정확히 한 번씩** 실립니다 (`statistics` 는 신호 수를 `report` 가 이미 내므로 사건 수만 더합니다) | 스펙 §6 이 "신호 건수와 사건 건수를 함께 낸다"를 요구합니다. 같은 수가 한 표에 두 번 들어가면 어느 쪽이 맞는지 판별할 수 없게 됩니다 |
| 신호 0건 신호군 | **집계에서 빼되 뺐다는 사실을 요약에 남깁니다** | 조용히 사라진 표본은 생존편향을 만듭니다. 실무적으로도 빈 표는 아래 계층이 예외로 거부합니다 — `excess` 는 칸 구성이 다르면 실패하고 `save_table` 은 빈 표를 거부합니다 |
| 화면과 CSV | 터미널 출력은 **저장한 표시용 프레임에서 발췌**합니다 | 따로 가공하면 반올림 시점이 갈려 화면에서 본 숫자를 CSV 에서 찾지 못합니다 |
| 종가 반올림 | **데이터셋이 자릿수를 정합니다** (KRX 원화 정수 / 그 외 4자리) | 한 파일에 여러 시장을 쌓으므로 컬럼 하나에 자릿수를 고정하면 원화 가격에 없는 소수 자리가 붙습니다. 소수 자릿수는 **원시 시세를 저장한 값과 같아야** 하므로 `common_constants.PRICE_DECIMALS` 를 그대로 씁니다 |
| 방향 축 | 이벤트 정의가 내는 두 방향에 **`역방향 전체`** 가 하나 더 붙습니다. **이벤트 정의가 아니라 집계 단계의 합성**이며 집계 3파일에만 실립니다 (스펙 §7 결정 ㉕) | 사용자의 실제 매매가 방향 구분 없이 역방향으로 진입하는 것이라, 방향별로만 나뉜 산출물로는 그 성적을 한 줄로 볼 수 없습니다. **합성이므로 어느 날이 신호인지는 바뀌지 않습니다** — 상승 방향 수익률의 부호만 뒤집어 이어 붙이고, 베이스라인은 같은 거래일을 원본과 부호 반전 두 벌로 둔 대칭 모집단을 씁니다. `signals.csv` 에 넣지 않는 것은 같은 날이 두 줄로 실려 차트 대조를 방해하기 때문이며, 방향별 두 블록을 합치면 복원됩니다 |

### Phase G0 — 원달러 그리드 데이터 확보

- [x] **ECOS 어댑터** — 인증키 로딩(`data/ecos_credentials.py`)과 시계열 수집(`data/ecos_collector.py`).
      통계표코드·항목코드는 **프로브로 실측해 확정**했다 (2026-08-24)
- [x] **FRED 어댑터** — `data/fred_collector.py`. 인증키가 필요 없는 공개 CSV 경로를 쓴다
- [x] **일별 단일 값 시계열 계층** — `storage/series/`, `COL_VALUE`, `load_series_csv()`.
      기존 OHLCV 경로는 손대지 않았다
- [x] **ETF 2종 실측·수집** — 261240·261250. 사양서의 1배/2배 전제를 실측으로 확인했고
      **수정주가가 전 기간 가용**함을 확인했다
- 확정 설계와 실측 수치는 [spec/usdkrw_grid.md](spec/usdkrw_grid.md) 가 SoT다

#### 이 단계에서 확정된 계층 계약

| 항목 | 확정 | 왜 |
| --- | --- | --- |
| 데이터 스키마의 구분자 | **폴더.** OHLCV 는 `storage/market/`, 일별 단일 값은 `storage/series/` | 한 폴더에 두 스키마를 섞으면 파일을 열어보기 전에는 어느 로더로 읽어야 할지 알 수 없다. 폴더가 다르면 그 판단이 경로에서 끝난다 |
| 두 로더의 판정 차이 | 시계열 로더는 **구조와 결측만** 본다. 값의 부호와 변동폭은 보지 않는다 | 마이너스 금리와 0% 금리가 실재하고, 원달러는 하루 20% 넘게 움직인 해가 있다. 시세의 판정을 그대로 걸면 정상 데이터가 막힌다 |
| 값 컬럼 이름 | **`Value`** (중립) | 환율은 원, 금리는 백분율이라 단위가 소스마다 다르다. `Close` 나 `Rate` 로 두면 그 이름이 맞지 않는 소스가 들어올 때 컬럼을 다시 갈라야 한다. 단위는 각 수집기의 대상 정의가 소유한다 |
| 휴장일·결측 | 수집기는 **제외하고 건수를 보고**한다. 전일값 이월은 하지 않는다 | 이월은 측정 계층의 판단이다. 수집이 미리 메우면 "원래 값이 없던 날"과 "메운 날"을 구분할 수 없다 |
| 비밀값 취급 | ECOS 인증키는 **환경 변수로 올리지 않고 값으로 돌려주며**, URL·예외 메시지는 마스킹을 거친다 | ECOS 는 키를 URL 경로에 넣는다. `urllib` 예외에는 URL 이 통째로 담겨 그대로 올리면 키가 로그에 남는다. KRX 가 환경 변수를 쓰는 것은 pykrx 의 제약이지 일반 규칙이 아니다 |

#### 다음 단계가 알아야 할 것

- **매매 규칙 계층의 입력 계약을 넓혀야 한다** (`.claude/rules/strategy.md`). 그리드는 `studies` 의
  신호 목록이 아니라 시세 자체를 입력으로 받는 상태 기계다. 근거는 [spec/usdkrw_grid.md](spec/usdkrw_grid.md) §4.1
- **모든 매매법의 공통 산출물은 일별 총자산 곡선이다.** 표준 지표는 곡선 하나만 받는 함수에서 계산한다.
  역방향 매매는 자본 성장 모델이 없어 아직 이 계약 밖에 있다
- **2019-03-14 의 261240 종가 이상치가 등가성 검증의 판정을 좌우한다.** 이틀을 포함하면 β 가 0.92,
  제외하면 1.96 이다. 처리 방식은 그 단계에서 결정한다

### Phase G1 — 등가성 검증 (검증 #5)

- [x] **정렬 계층** — ETF 거래일을 마스터로 세 시계열을 맞추고 **제외·이월 건수를 함께 반환**한다
- [x] **이론값 2종** — `현물+금리차`(사양서 §16.1)와 `현물+달러금리`(§2.1). 하나를 고르지 않고 둘 다 낸다
- [x] **회귀·연도별 괴리·프리미엄** — 사양서 §16.2·§16.3·§16.4
- [x] **NAV 수집** — `pykrx_collector.collect_pykrx_nav`. NAV 는 단일 값 시계열이라 `storage/series/` 에 둔다
- [x] **결과 문서** — [research/원달러_ETF_등가성.md](research/원달러_ETF_등가성.md). 판정은 **조건부 가능**
- 확정 설계와 실측 수치는 [spec/usdkrw_grid.md](spec/usdkrw_grid.md) 가 SoT다

#### 이 단계에서 확정된 계층 계약

| 항목 | 확정 | 왜 |
| --- | --- | --- |
| 이벤트가 없는 검증 | **`measure/` 를 쓰지 않는다.** 자체 회귀 통계를 내고 `report/writer.py` 의 저장만 재사용한다 | `studies` 의 통상 계약은 "어느 날이 신호인가"인데 이 검증에는 신호가 없다. forward return 이 필요 없으므로 공통 계층을 억지로 확장하지 않았다. **저장 규칙은 계산과 달리 검증마다 같아야 하므로 그쪽만 공유한다** |
| 데이터 계열의 시점 정합 | **소스마다 "그 값이 어느 날의 시장인가"를 확인하고 필요하면 옮긴다** | 매매기준율은 고시일 기준이라 하루 늦다. 모르고 쓰면 **예외가 나지 않고 결과만 틀린다** — 상관이 −0.03 으로 나왔다 |
| 축을 고르지 않는 원칙의 적용 범위 | **데이터 계열도 축이다** | 어느 환율 계열을 쓰느냐로 상관이 −0.03 과 0.99 로 갈린다. 하나를 골라 산출했다면 그 선택이 결론에 숨는다 |
| 비용을 재는 방법 | **공시값을 읽지 않고 NAV 로 잰다.** 공시값은 교차확인용 | 공시 총보수에는 기타비용·내부 매매비용이 빠져 있다. NAV 는 그것들이 이미 반영된 값이라 **실제 부담**을 직접 잰다. 다만 **기준선이 상품 구조를 반영해야 한다** — 노출 배수를 빼먹으면 261250 에서 원화금리가 비용으로 둔갑한다 |

#### 다음 단계가 알아야 할 것

- **그리드의 판정·체결 기준 가격은 정규장 종가로 확정됐다** (결정 C17 이 C1 을 대체).
  매매기준율은 전일 시장을 담아 하루 늦다. 환전 스프레드만 매매기준율 기준으로 계산한다.
  근거는 [spec/usdkrw_grid.md](spec/usdkrw_grid.md) §3.4·§4.3
- **이론값은 `현물 + 달러금리` 로 확정됐다.** 261240 의 NAV 안에 원화 담보 이자가 들어 있다.
  그리드의 환전 경로 수익 모델도 이 전제 위에 세운다
- **261250 에 숨은 레버리지 유지비용은 없다.** 겉보기 연 2.12% 는 「2배 노출을 1배 담보로」라는
  구조에서 나오는 원화금리였고, 구조를 반영하면 실효 비용이 −0.05% 다. 다만 **그 구조 자체가
  원화금리만큼 매년 불리하다는 뜻**이므로 트랙 2 성적을 볼 때 이 몫을 분리해서 본다
- **261240 의 연도별 괴리가 연 ±1%p 다.** 슬롯을 수개월 들고 있으므로 그 기간의 손익이
  이론값과 그만큼 어긋날 수 있다. ETF 경로 성적을 해석할 때 이 폭을 함께 본다

### Phase G2~G5 — 원달러 그리드 엔진·비용·지표·견고성 (2026-08-30 코드 삭제)

**그리드는 채택되지 않았고 2026-08-30 경량화에서 구현을 지웠다** — `src/verify_lab/strategy/grid/`
6,042줄, 테스트 7,910줄, 실행 스크립트 892줄. **결론과 실측은 문서에 남아 있다.**

| 남은 곳 | 내용 |
| --- | --- |
| [spec/usdkrw_grid.md](spec/usdkrw_grid.md) | 확정 설계 A1~A4·B1~B2·C1~C118 과 데이터 실측 15개 절. **재현할 코드는 없지만 왜 그렇게 정했는지는 전부 여기 있다** |
| `docs/strategy/원달러_그리드.md` | 규칙·성적·기각 판정(§2.8)·체결 원자료 |
| [research/달러_조달_방식.md](research/달러_조달_방식.md) | 그 성적을 근거로 한 조달 방식 결정 |
| 아래 「G5 이후 — 트랙 종료」 | 왜 기각했는가 |

**지운 이유**: 채택하지 않기로 확정된 트랙이 저장소 코드의 36% 를 차지했고,
그 코드로 다시 돌려도 이미 난 결론만 나온다. **되살리려면 git 이력에서 꺼낸다.**

### Phase 8 — 후보 판정의 시기 항목이 보합을 「내림」으로 세던 버그 (2026-08-30)

`measure/screening.py` 가 시기별 적중률을 **`1 − 오른 비율`** 로 만들고 있었다. 같은 함수의 전체 축은
`내린 비율` 을 직접 읽는데 시기 축만 여집합으로 만든 것이라, **판정식 단일화(절대 원칙 5) 위반**이다.

두 방향 비율은 여집합이 아니다 — **보합(수익률이 정확히 0)이 어느 쪽에도 들어가지 않는다.**
그래서 `1 − 오른 비율` 은 보합을 통째로 「내림」으로 세어 값을 부풀리고, **시기 항목이 관대해진다.**

**영향 실측: 산출물은 한 칸도 바뀌지 않았다.**

| 확인 | 결과 |
| --- | --- |
| 시기 칸 (종목 4 × 만기월 12 × 앞뒤 절반) | 120칸 |
| 보합이 0이 아닌 칸 | **1칸** (KODEX 200 · 목요일 · 2월 앞 절반, 보합 8.34%p) |
| 그 칸의 방향 | **「위」** — 오른 비율을 쓰므로 버그 경로를 타지 않는다 |
| 1차 판정이 바뀐 칸 | **0칸** |
| 「가장 약한 시기」가 바뀐 칸 | **0칸** |

**버그는 실재하지만 이번 데이터에서는 발현되지 않았다.** 「아래」 방향 칸에 보합이 생기면
그때 등급이 부풀려지므로 수정은 그대로 유효하다. 재현 테스트를 `tests/test_measure_screening.py` 에
남겼다 — 보합 20%인 「아래」 칸에서 수정 전 값은 0.70, 실제 내린 비율은 0.50 이었다.

> **이 버그가 오래 숨어 있던 이유**: 테스트 픽스처가 `내린 비율 = 1 − 오른 비율` 로 데이터를
> 만들고 있었다. **픽스처가 버그와 똑같은 가정을 해서** 두 값이 항상 일치했다.
> 합성 데이터를 만들 때 **여집합이 아닌 값을 여집합으로 만들지 않는지** 확인한다.

### Phase 8 — 표준 지표 계층 삭제와 곡선 계약의 범위 축소 (2026-08-30)

**`strategy/performance.py`(193줄)와 그 테스트(528줄)를 지웠다.** G0 의 「다음 단계가 알아야 할 것」이
선언한 **"모든 매매법의 공통 산출물은 일별 총자산 곡선"** 계약을 **상시 보유형 매매법으로 한정**한다.

> 위 G0 절의 문장은 **당시의 판단 기록이라 그대로 둔다.** 현재 규격은 이 절과
> [.claude/rules/strategy.md](../.claude/rules/strategy.md) 가 SoT다.

**왜 축소했나** — 노출 비율을 실측했다.

| 대상 | 전체 거래일 | 노출일 | 노출 비율 |
| --- | --- | --- | --- |
| KODEX 200 K=10 (D+3) | 4,599 | 56 | **1.22%** |
| QQQ K=20 (D+3) | 4,691 | 18 | **0.38%** |
| QQQ K=10 | 4,691 | 7 | **0.15%** |

자본이 99% 이상의 기간 놀고 있다. 이 곡선으로 CAGR 를 내면 44일 성적이 18년 연환산으로 둔갑하고,
MDD 는 44일 안의 낙폭이 18년 최대낙폭으로 보고되며, **Sharpe 는 4,555일의 수익률이 0이라
분모가 무너져 구조적으로 부풀려진다.** rf 를 0으로 두면 Sharpe 가 부푼다는 것은 이미 알고 있었는데,
**노출이 낮으면 rf 를 제대로 넣어도 같은 왜곡이 분모에서 일어난다.**

`docs/spec/usdkrw_grid.md` 결정 B2 가 이미 같은 판단을 적어 뒀다 — "역방향 매매는 '매 신호 같은
금액 투입' 가정이라 자본 성장 모델이 없고, CAGR·MDD 가 정의되지 않는다."

**지운 이유**: 곡선이 필요한 매매법이 저장소에 하나도 없다(그리드는 위에서 삭제됐다).
프로덕션 호출이 0건인 채로 규칙 문서만 그것을 요구하고 있었다.
**상시 보유형 매매법을 새로 만들 때는 git 이력에서 꺼내 되살린다** — 새로 만들면 산식이 갈라진다.

### Phase 8 — 데드코드 제거와 상수 통합 (2026-08-30, 동작 불변)

호출되지 않는 정의 10종을 지우고, 계층마다 따로 정의돼 있던 상수 3종을 `common_constants.py` 로
합쳤다. `pyright: ignore` 3곳은 **주석으로 덮지 않고 타입을 좁혀 없앴다.**
**세 검증과 매매 규칙의 산출물이 전부 md5 일치**함을 재실행으로 확인했다.

#### 미사용 판정에는 「왜 안 쓰이는가」가 함께 필요하다

`ast` 로 정의 689개를 뽑아 참조를 전수 집계하는 방식으로 후보를 찾았다. 그런데 **그 목록을
그대로 지우면 안 된다.** 두 종류가 섞여 있기 때문이다.

| 왜 안 쓰이는가 | 처리 | 실물 사례 |
| --- | --- | --- |
| 필요 없어서 | **지운다** | `SUPPORT_TOTAL_WITHOUT_PERIOD` — 실제로는 `len(checks)` 로 계산하고 있었다 |
| 결론이 난 축의 잔여물 | **지운다** | `HolidayExit` — 결정 ㉘ 이 「휴장 규칙 네 조합」 대조축을 지웠는데 enum 만 남았다 |
| **연결을 빠뜨려서** | **쓰게 만든다** | `DISPLAY_PRICE_BASIS`·`COL_NAV`·`ETF_TARGETS` — 표와 코드가 하드코딩 문자열을 쓰고 있었다 |

세 번째를 지우면 **하드코딩이 정답인 것처럼 굳는다.** `DISPLAY_PRICE_BASIS` 는 실제로 삭제
목록에 올렸다가, ROADMAP 실행 계층 계약이 「가격 기준을 터미널 표에 둔다」고 명시한 것을 보고
되살렸다.

#### 묶음 상수를 쓰면 테스트의 patch 대상이 늘어난다

`ETF_TARGETS = (ETF_BASE, ETF_LEVERAGE)` 는 **모듈 로드 시점에 굳는다.** 테스트가 개별 이름만
`monkeypatch` 하면 목록을 순회하는 코드가 **진짜 파일을 읽는다** — 실제로 그렇게 깨졌다.
묶음 상수를 도입할 때는 그것을 쓰는 테스트의 patch 목록도 함께 늘린다.

#### 픽스처가 버그와 같은 가정을 하면 그 버그는 영원히 안 잡힌다

위 판정 버그 절에 적은 것과 같은 교훈이 정리 과정에서도 나왔다. `HORIZON_LABELS` 를 줄이자
**테스트가 낡은 구간(252일 = 1년)을 예시로 쓰고 있던 것**이 드러났다. 측정 구간이 단기로
확정된 지 오래인데(결정 ㉑) 테스트만 옛 값을 들고 있었다.

---

## G5 이후 — 트랙 종료 (2026-08-27)

**원달러 그리드 트랙은 종료됐고, 이 규칙은 채택되지 않았다.**

성적 때문이 아니다. 사용자의 실제 목적이 매매 수익이 아니라 **미국주식 매수 자금용 달러 확보**임이
드러났고, 그 목적에서 이 규칙이 **정면으로 충돌**하는 것이 실측으로 확인됐다 — 원달러가 급등하면
전량 익절해 달러가 0이 되는데, **달러가 필요한 순간이 바로 그때**다.

| 근거 | 값 |
| --- | --- |
| 미국 증시 저점 부근 5일의 보유 달러 | **0원 · 0원 · 1,475만 · 106만 · 0원** (다섯 날 전부 범위 상단 위라 매수도 불가) |
| 급등 국면 평균 달러 비중 | 2008~2009 17.55% · 2020 17.96% · 2022 11.51% · **2024~2026 9.76%** |
| 매수 규칙 자체는 맞았다 | 실제 조달 단가 **1,155.83원** 대 기간 평균 1,162.93원 (**0.61% 쌈**) |

판정은 [strategy/원달러_그리드.md](strategy/원달러_그리드.md) §2.8, 대안 검토와 최종 결정은
[research/달러_조달_방식.md](research/달러_조달_방식.md), 채택한 규칙은
[strategy/원달러_백분위_알림.md](strategy/원달러_백분위_알림.md) 다.

### 선택지 셋의 처리

| # | 선택지 | 처리 |
| --- | --- | --- |
| **A** | 지정가 대조군 (G6) | **닫힘.** 운용하지 않을 규칙의 체결 가정을 정밀화하는 일이다. 크기는 이미 실측돼 있다(1,957만원 · 매매 기여분의 69.09%) |
| **B** | QQQ 와의 상관 측정 | **이 경로로는 닫힘.** 그리드 총자산 곡선으로 재는 것이 의미를 잃었다. **다만 프로젝트의 출발 질문 자체는 여전히 미답**이며, 다른 매매법에서 답해야 한다 |
| **C** | 다른 검증으로 넘어간다 | **열려 있음.** #2 비트코인 4년 주기설 · #3 정배열 매매법 · #4 탑티어 우량주 반토막이 미착수다 |

### 다음 트랙이 알아야 할 것

- **"조건이 오면 한다"는 규칙은 조건이 안 올 때를 반드시 함께 재야 한다.** 이 트랙에서 세 번
  같은 실패가 나왔다 — 그리드가 급등장에 달러 0, 밴드가 2022-09-28 시작에서 4년간 0회 발동,
  가격 기준 밴드가 다섯 시작 시점 전부에서 매수 0회. **성적표에는 안 나타나고 가용성에만 나타난다**
- **성적이 좋은지와 목적에 맞는지는 다른 질문이다.** 그리드는 G0~G5 다섯 단계를 거쳐
  성적을 정밀하게 쟀지만, **"달러가 필요한 날에 있었는가"는 한 번도 묻지 않았다.**
  새 트랙은 착수 전에 **판정 기준이 사용자의 실제 목적과 일치하는지** 먼저 확인한다
- **원달러 종가 계열이 2026-08-26 까지 갱신됐다** (2026-08-27 재수집, 3거래일 추가,
  과거 값 변경 0건). 그리드 산출물의 5,340 거래일과 어긋나므로 그리드를 다시 돌리면
  기존 문서의 수치가 재현되지 않는다
- **검증 #1 에서 확정된 계층 계약은 그대로 유효하다** — Phase 2·3 의 "계층 간 계약" 다섯 절

---

## 계획서의 수명

`docs/plans/`는 **임시 산출물**입니다. 사용자가 주기적으로 전부 삭제합니다.
Done 처리 전에 남길 가치가 있는 것을 살아있는 문서로 승격합니다.

| 승격할 내용 | 목적지 |
| --- | --- |
| 데이터 소스 실측 결과 (라이브러리 동작, 데이터 품질) | `docs/spec/` 의 해당 검증 스펙 — "데이터 실측 기록" |
| 검증 설계의 확정 결정과 **탈락안·그 이유** | `docs/spec/` 의 해당 검증 스펙 — "확정된 설계 결정" |
| 측정 결과와 판정 | `docs/research/<검증명>.md` |
| Phase 상태, 검증 목록, 계층 간 설계 계약 | 이 문서 |
| 실행 명령어 | `docs/COMMANDS.md` |

판단 기준: **"이 계획서를 지금 지워도 잃는 것이 없는가?"** 아니라면 아직 승격이 안 끝난 것입니다.

**살아있는 문서에서 계획서를 링크하지 않습니다.** 링크는 다음 삭제 주기에 깨집니다.
필요한 내용은 복사해 자립시킵니다.
