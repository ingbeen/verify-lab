# 검증 #8 — 레버리지·인버스 상품 괴리 측정: 확정 설계

> 이 문서는 **설계와 데이터 실측 기록**입니다. 측정 결과와 판정은 `docs/research/레버리지_ETF_괴리.md` 에 있습니다.
> 설계가 바뀌면 여기를 갱신하고 변경 사유를 §7 에 남깁니다.

---

## 1. 무엇을 묻는가

사용자는 지금까지 검증을 1배 기준으로 해왔지만 실제 매매는 **2~3배 상품**으로 집행할 예정이다.
음의 복리·스왑비용·불완전 추종은 이미 이해하고 있으며, 알고 싶은 것은 **그 크기가 실제로 얼마인가**다.
특히 "1~2주 단기는 괜찮을 것 같고, 장기로 가면 괴리가 커질 것 같다"는 가설을 확인하려 한다.

**이것은 매매법 검증이 아니다.** 검증 #6(달러 조달 방식)과 같은 성격이며, 상품의 물리적 성질을 잰다.
루트 `CLAUDE.md` 의 「후보 판정 기준」·「기준선 대비 차이」는 적용되지 않는다.
다만 측정의 원칙 **3(표본 수)·5(표본 독립성)·12(축 분해)·14(원본가)·17(표본 부족 행 보존)** 은 그대로 걸린다.

---

## 2. 측정 방법 — 괴리를 두 조각으로 나눈다

보유 기간 `h` 에 대해 세 값을 낸다.

| 값 | 산식 | 뜻 |
| --- | --- | --- |
| 단순 배수 기대치 `A` | `배수 × 1배 구간 수익률` | "2배면 2배 오르겠지" |
| 이론 경로 `B` | `Π(1 + 배수 × 1배 일간수익률) − 1` | 매일 정확히 배수로 리밸런싱만 하고 비용이 0 인 상품 |
| 실제 `C` | 배수 상품의 구간 수익률 | 실제로 손에 쥐는 것 |

여기서 괴리가 둘로 갈린다.

- **경로 효과 = `B − A`** — 변동성이 만든 몫. 수수료와 무관하며 추세장에서는 양수가 된다
- **상품 비용 = `C − B`** — 총보수·스왑·차입·추적오차
- **총 괴리 = `C − A`** — 두 몫의 합

**`B` 를 중간에 두는 것이 이 설계의 핵심이다.** `A` 와 `C` 만 비교하면 음의 복리와 비용이 한 덩어리로
뭉쳐 "장기에 벌어진다"는 것만 알 뿐 **왜** 벌어지는지를 나눌 수 없다.

보조 지표로 **실현 배수 = `C ÷ 1배 구간 수익률`** 을 낸다. 분모가 0 근처면 값이 폭발하므로
`|1배 수익률| ≥ 1%` 인 구간에서만 내고, 못 낸 칸에 사유를 남긴다.

### 보유 기간 격자

`5 · 10 · 21 · 63 · 126 · 252 · 756` 거래일 (1주·2주·1개월·3개월·6개월·1년·3년).
**모든 거래일을 시작점으로 삼는 롤링 전수**이며, 상장 후 전체 구간은 표본 1건이라 격자에 넣지 않고 따로 낸다.

### 축 분해

전체 평균만 내면 서로 다른 국면이 상쇄된다(측정의 원칙 12). 세 축으로 쪼갠다.

| 축 | 나누는 기준 |
| --- | --- |
| 변동성 | 구간 **안**의 1배 일간 변동성 사분위. 경계는 **구간별로** 매긴다 |
| 방향 | 구간 1배 수익률의 부호로 오름·내림·보합. **보합을 어느 쪽에도 넣지 않는다** |
| 시기 | 저금리(~2021) / 고금리(2022~). **시작일 기준** |

---

## 3. 측정 대상 — 27종 / 22쌍

2026-09-02 기준 **KRX 상장 ETF 1,167종·ETN 373종 전수 조회**와 yfinance 조회로 확정했다.
거래대금은 2026-09-02 하루치이며 각 카테고리 1위를 골랐다.

### 3.1 국내 (9종)

| 지수 | 배수 | 종목 | 코드 | 상장일 | 기초지수 | 총보수·제비용 | 거래대금 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| KOSPI200 | 1x | KODEX 200 | 069500 | 2002-10-14 | 코스피 200 (현물) | 0.150% | 2조 2,219억 |
| KOSPI200 | 2x | KODEX 레버리지 | 122630 | 2010-02-22 | 코스피 200 (**현물**) | 0.640% | 1조 5,633억 |
| KOSPI200 | −1x | KODEX 인버스 | 114800 | 2009-09-16 | 코스피 200 **선물**지수 | 0.640% | 5,029억 |
| KOSPI200 | −2x | KODEX 200선물인버스2X | 252670 | 2016-09-22 | 코스피 200 **선물**지수 | 0.640% | 4,028억 |
| KOSDAQ150 | 1x | KODEX 코스닥150 | 229200 | 2015-10-01 | 코스닥 150 (현물) | 0.250% | 4,741억 |
| KOSDAQ150 | 2x | KODEX 코스닥150레버리지 | 233740 | 2015-12-17 | 코스닥 150 (**현물**) | 0.640% | 5,550억 |
| KOSDAQ150 | −1x | KODEX 코스닥150선물인버스 | 251340 | 2016-08-10 | F-코스닥150 (**선물**) | 0.640% | 1,553억 |
| KOSDAQ150 | −2x | **삼성 인버스 2X 코스닥150 선물 ETN** | 530107 | 2022-10-17 | 코스닥150 선물 **TWAP** 인버스 -2X | 0.400% | 805.6억 |
| KOSDAQ150 | −2x | 미래에셋 인버스 2X 코스닥150 선물 ETN (대조) | 520057 | 2022-10-17 | 같음 | 0.300% | 178.8억 |

**레버리지는 현물지수, 인버스는 전부 선물지수를 추종한다.** 인버스 칸의 「상품 비용」에는
롤오버·베이시스가 섞여 들어오므로 레버리지 칸과 같은 값으로 읽으면 안 된다.

### 3.2 미국 (18종)

| 지수 | 1x | 2x | 3x | −1x | −2x | −3x |
| --- | --- | --- | --- | --- | --- | --- |
| S&P500 | SPY (1993-01-29) | SSO (2006-06-21) | UPRO (2009-06-25) | SH (2006-06-21) | SDS (2006-07-13) | SPXU (2009-06-25) |
| 나스닥100 | QQQ (1999-03-10) | QLD (2006-06-21) | TQQQ (2010-02-11) | PSQ (2006-06-21) | QID (2006-07-13) | SQQQ (2010-02-11) |
| 다우 | DIA (1998-01-20) | DDM (2006-06-21) | UDOW (2010-02-11) | DOG (2006-06-21) | DXD (2006-07-13) | SDOW (2010-02-11) |

**3배 상품은 2009~2010년 상장이라 2008년 금융위기를 겪지 않았다.** 최악의 변동성 국면 표본이
통째로 없으며, 2006년 상장인 2배 상품이 그 구간을 덮는다.

**DDM 은 유동성이 얇다** — 2026-09-02 기준 일 거래대금 약 1,190만 달러로 다른 칸의 1/20 수준이다.
빼지 않고 그대로 재되 이 사실을 함께 읽는다.

---

## 4. 확정된 설계 결정

### ① 가격 기준은 원본가다

**확정**: 원본가(`<종목>_max.csv`). **탈락안**: 수정주가 단독, 두 기준 병기.

측정의 원칙 14 를 그대로 따른다. 사용자에게 "퀀트 백테스트의 일반 관행은 총수익 기준이며
원본가는 장기 수익률을 구조적으로 과소평가한다"는 사실과 **인버스 ETF 의 이자 기반 분배금 문제**를
제시한 뒤 원본가로 결정됐다 (2026-09-03).

**대가를 눈에 보이게 하는 장치를 함께 둔다** — §5 의 배당 보정분이 그것이다.

### ② 1배 기준은 지수가 아니라 1배 ETF 다

**확정**: KODEX 200 · KODEX 코스닥150 · SPY · QQQ · DIA.
**탈락안**: 기초지수 자체, 둘 다 산출.

측정의 원칙 9(실제로 살 수 있는 것으로 잰다)를 따른다. 사용자의 실제 질문이
"1배 대신 2배를 사면 어떻게 되는가"이므로 1배 ETF 가 기준이어야 한다.

### ③ 최장 구간은 3년이고 전체 구간은 사례로 따로 낸다

**확정**: 롤링은 3년(756거래일)까지. 상장 후 전체 구간은 표본 1건이므로 격자에 넣지 않고 별도 표로.
**탈락안**: 5년 롤링 추가.

5년을 넣으면 3배 상품의 독립 표본이 3개, 코스닥150 계열이 2개로 떨어져 숫자는 나오지만
판정에 쓸 수 없다. 3년도 이미 독립 표본이 1~6개다(§6.3).

### ④ 1배 기준 파일을 재수집하지 않는다

**확정**: 069500·SPY·QQQ·DIA 는 기존 파일을 그대로 쓴다.

재수집하면 검증 #1·#7 의 수치가 그 시점부터 재현되지 않는다(루트 `CLAUDE.md` 「재수집은 이미
나온 결과를 바꿉니다」). **대가는 최근 4~5거래일이 측정에서 빠지는 것**이며, 정렬 계층이
공통 거래일만 남기므로 조용히 섞이지 않는다.

### ⑤ 코스닥150 −2배는 ETN 을 쓰고 두 발행사를 나란히 둔다

**확정**: 삼성(530107) 본선 · 미래에셋(520057) 대조.
**탈락안**: 코스닥 −2배를 빼고 진행, 8종 전부 측정.

ETF 로는 존재하지 않는다. 8종 중 삼성이 거래대금 1위이고 2위의 4.5배다. 둘은 **같은 날 상장·
같은 기초지수**라 나란히 놓으면 발행사가 달라서 생기는 차이가 분리된다. 나머지 6종은 거래대금이
두 자릿수 이상 작아 쓰지 않는다.

### ⑥ 공통 계층으로 올리지 않는다

**확정**: `studies/leverage_tracking/` 안에 둔다.

`studies` 의 계약은 `find_events(df) -> pd.Series[bool]` 이지만 이 검증은 이벤트가 없고 입력이
두 종목의 짝이다. 루트 `CLAUDE.md` 의 판단 기준("이것이 「측정의 원칙」에 적혀 있는가")으로 보면
두 종목 페어의 괴리 분해는 원칙에 없고, 확인된 재사용처가 0건이다. 두 번째 검증에서 같은 계산이
필요해지면 그때 `measure/` 로 올린다.

### ⑦ 비중첩 표본 수는 근사가 아니라 정확한 최대값이다

**확정**: 구간 스케줄링 그리디로 «겹치지 않게 고를 수 있는 최대 개수»를 정확히 센다.
**탈락안**: `공통 거래일 ÷ 구간` 의 내림값.

축으로 걸러 시작일이 흩어진 칸에서는 나눗셈이 맞지 않는다. 전체 칸에서는 두 값이 같다는 것을
실측으로 확인했다(불일치 0건).

---

## 5. 원본가 결정의 대가를 재는 법 — 배당 보정분

총수익 기준으로 다시 쟀을 때 총 괴리가 얼마나 달라지는지를 낸다.

```
C' − A' = (C + d_target) − 배수 × (1배 수익률 + d_base)
        = (C − A) + (d_target − 배수 × d_base)
```

이므로 **배당 보정분 = `d_target − 배수 × d_base`** 다. `d` 는 그 구간의 분배 기여이며
**수정주가와 원본가의 일간 수익률 차이**를 쌓아서 구한다.

**인버스에서는 부호가 뒤집힌다.** 배수가 음수면 `− 배수 × d_base` 가 양수가 되므로,
1배가 배당을 많이 줄수록 원본가로 잰 인버스의 괴리는 **과소**평가된다.

> **부호 규약 (2026-09-04 통일).** 여기서 「과소·과대」는 전부 **부호 있는 값** 기준이다.
> 총 괴리 `C − A` 는 음수일 수 있고, 그때 「과소평가」는 «실제보다 더 작은(더 음수인) 값이
> 나온다» 는 뜻이다. 벌어진 **크기**(절대값)로 읽으면 같은 상황이 「과대」가 되므로 규약을
> 하나로 고정했다 — 전에는 이 문서와 코드 모듈 주석이 서로 반대로 적혀 있었다.
> 계산은 바뀌지 않았고 §6 의 수치도 그대로다.

### 이 계층은 `measure/` 에 있다 (2026-09-04 승격)

**구현 위치는 `src/verify_lab/measure/distribution.py` 다.** 원래 이 검증 안에 있었으나
검증 #9(선물 대 레버리지 ETF)가 두 번째 호출처가 되면서 올렸다.

승격 근거는 루트 `CLAUDE.md` 의 판단 기준 — **「이것이 「측정의 원칙」에 적혀 있는가」** 이다.
원칙 14 가 「배당락이 구간에 걸리는 검증은 그 사실과 규모를 결과 문서에 적는다」를 모든 검증에
요구하므로, 그 규모를 재는 산식은 검증마다 다시 구현할 것이 아니라 공통 계층의 것이다.
**옮기기만 했고 로직은 그대로여서 §6 의 수치는 재실행 없이 유효하다.**

ETN 은 분배금을 지급하지 않고 지표가치에서 제비용만 차감하므로 `d_target` 이 0 이다.
수정주가 파일이 없는 것이 정상이며 결측이 아니다.

---

## 6. 데이터 실측 기록

### 6.1 ETN 시세는 pykrx 가 주지 않는다 (2026-09-03)

pykrx 는 ETN 에 **`get_etn_ticker_list` 와 `get_etn_ticker_name` 둘뿐**이다.
ETF 용 `get_etf_ohlcv_by_date` 나 주식용 `get_market_ohlcv` 에 ETN 코드를 넣으면
**예외 없이 빈 결과**가 돌아온다.

ETF 가 `MDCSTAT043~046`(전종목시세·전종목등락률·개별종목시세·전종목기본종목)을 쓰는 것과
나란히 ETN 은 `064~067` 을 쓸 것으로 추정해 호출해 확인했다.

| 통계 코드 | 내용 | 받는 인자 |
| --- | --- | --- |
| `MDCSTAT06601` | **ETN 개별종목 시세 추이** | `isuCd`(ISIN) · `strtDd` · `endDd` |
| `MDCSTAT06501` | ETN 전종목 시세 | `strtDd` · `endDd` · `isuCd` (**`trdDd` 가 아니다**) |
| `MDCSTAT06701` | ETN 전종목 기본종목 (상장일·만기일·기초지수·제비용) | 없음 |

`MDCSTAT06601` 은 OHLC·거래량·거래대금에 더해 **증권당 지표가치(`PER1SECU_INDIC_VAL`)** 와
기초지수 값을 함께 준다. 지표가치는 ETF 의 NAV 에 해당하며 시장가와의 차이가 곧 유동성공급자
호가로 생기는 괴리다.

**티커가 아니라 ISIN 으로 조회한다.** 종목 코드를 그대로 넣으면 빈 결과가 돌아오고 예외가 나지 않는다.

> **이것은 pykrx 가 감싸지 않은 KRX 내부 통계라 예고 없이 바뀔 수 있다.**
> 수집기가 반환 컬럼을 명시적으로 검증하고, 어긋나면 조용히 통과시키지 않고 예외를 던진다.

### 6.2 yfinance 원본가는 분할이 조정돼 있다 (2026-09-03)

레버리지·인버스 ETF 는 역분할이 잦아 **분할이 조정되지 않으면 가격이 수십 배 점프**한다.
그 상태로 계산하면 괴리가 아니라 분할을 재게 된다.

역분할 이력이 있는 TQQQ·SQQQ·SPXU 를 포함해 **15종 전부가 로더의 이상치 검증(일간 ±50%)을
통과**했다. `history(auto_adjust=False)` 는 **배당만 미조정이고 분할은 조정된 값**을 준다.

### 6.3 표본 수 실측 (2026-09-04)

전 154칸(22쌍 × 7구간)에서 **유효 표본이 10건 미만인 칸은 0개**다.
그러나 **비중첩 표본은 3년 구간에서 1~6개**로 떨어진다.

| 구간 | 미국 2배 계열 | 미국 3배 계열 | 국내 KOSPI200 | 코스닥150 −2배 ETN |
| --- | --- | --- | --- | --- |
| 1주 | 1,012~1,015 | 831~863 | 486~834 | 189 |
| 1개월 | 240~241 | 198~205 | 115~198 | 45 |
| 1년 | 20 | 16~17 | 9~16 | 3 |
| **3년** | **6** | **5** | **3~5** | **1** |

**3년 칸은 통계가 아니라 사례에 가깝다.** 표본 수만 보면 3,400~4,300건이지만 그것은 롤링 중첩이며
실제 독립 관측은 한 자릿수다.

### 6.4 251340 상장일 (2026-09-03)

KODEX 코스닥150선물인버스의 상장일은 **2016-08-10** 이다. 처음에 2016-08-26 부터 요청해
11거래일이 빠져 있었고, KRX 기본종목 조회의 `LIST_DD` 와 대조해 발견한 뒤 다시 받았다.
**수집 시작일은 기억이 아니라 `LIST_DD` 로 확인한다.**

---

## 7. 설계 변경 이력

- **2026-09-03 최초 확정.** 사용자 결정 4건(원본가 · 1배 ETF 기준 · 3년까지 + 전체 구간 · 정식 검증).
- **2026-09-04 배당 보정분을 `measure/` 로 승격하고 부호 규약을 통일.** 검증 #9 가 같은 산식을
  쓰게 되면서 `studies/leverage_tracking/distribution.py` 를 `measure/distribution.py` 로 옮겼다.
  **`studies → studies` 는 계층 의존 방향에 없는 경로**라 그대로 import 할 수 없었고,
  복제하면 같은 판정이 두 곳으로 갈라진다(절대 원칙 5). 함께 §5 의 부호 규약을 「부호 있는 값
  기준」으로 고정했다 — 이 문서는 「과소」, 코드 모듈 주석은 「과대」로 서로 반대였다.
  **로직은 건드리지 않았고 기존 테스트가 그대로 통과하는 것이 그 증거다.**
- **2026-09-03 코스닥150 −2배 추가.** 최초 스캔이 **ETF 목록만** 훑어 ETN 상품을 통째로 놓쳤고
  "존재하지 않는다"고 잘못 보고했다. 사용자가 지적해 ETN 373종을 다시 전수 조회했다.
  **국내 상품을 셀 때 ETF 목록만 보면 안 된다** — 레버리지·인버스 ETN 이 245종 있다.
  이 정정으로 20쌍 → 22쌍이 됐고 ETN 수집기 신설이 범위에 들어왔다.
