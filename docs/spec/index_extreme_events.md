# 검증 #1 — 지수 극단 이벤트 통계 검증 (확정 설계)

> 작성일: 2026-08-09 17:00 (KST)
> 상태: 설계 확정, 구현 전
> 결과 문서: 완료 후 `docs/research/RESEARCH_index_extreme_events.md`

이 문서는 검증 #1의 **확정 설계와 데이터 실측 기록의 SoT**입니다.
설계 변경이 필요하면 이 문서를 갱신하고, 변경 사유를 "확정된 설계 결정"에 남깁니다.

---

## 1. 목적

극단적인 등락 이벤트와 연속 등락 이후, 향후 수익률이 통계적으로 유의미한 패턴을 보이는지 검증한다.

**이것은 매매 전략 개발이 아니라 측정이다.** 최적 파라미터를 찾지 않는다.
"유의미한 패턴이 없다"도 유효한 결론이며, 그 결론이 나오면 그대로 보고한다.

---

## 2. 대상 데이터

**지수가 아니라 실제 매매 가능한 상품을 대상으로 한다.** 검증 결과를 그대로 집행할 수 있어야 하므로,
이벤트 판정과 수익률 측정 모두 아래 종목의 실제 체결가로 수행한다.

| 시장 | 종목 | 소스 | 상장일 |
| --- | --- | --- | --- |
| 미국 | **QQQ** (Invesco QQQ Trust) | yfinance | 1999-03-10 |
| 한국 | **KODEX 200** (`069500`) | pykrx `get_etf_ohlcv_by_date` | 2002-10-14 |

- 두 시장 모두 **본검증 대상**이다. 한쪽이 다른 쪽의 교차검증용이 아니다.
- 각 시장을 독립적으로 분석한다. 두 시장의 거래일 달력을 억지로 맞추지 않는다.

### 기간

| 종목 | 데이터 수집 | 순위 축적 | 신호 집계 |
| --- | --- | --- | --- |
| QQQ | 2000-01-01 ~ 현재 | 2000-01-01 ~ 2010-12-31 | 2011-01-01 ~ 현재 |
| KODEX 200 | 2002-10-14 ~ 현재 | 2002-10-14 ~ 2010-12-31 | 2011-01-01 ~ 현재 |

집계 시작일은 **2011년을 기본값으로 하되, 2003·2005·2008년 시작 결과를 함께 산출해 표로 병기한다**
(근거: §7 결정 ②).

KODEX 200은 상장일 제약으로 순위 축적 구간이 QQQ보다 짧다. 신호 집계 시작일은 동일하므로 비교에는
지장이 없으나, **초기 순위 기준선이 QQQ보다 다소 느슨하다**는 점을 결과 해석 시 명시한다.

### 가격 처리

- **한 산출 안에서 가격 기준을 혼용하지 않는다.** 일간 등락률과 forward return 은 언제나 같은 기준으로 계산한다.
- **두 시장 모두 원본가(배당·분배 미조정) 기준이다** (§7 결정 ⑰).

| 종목 | 소스 | 구간 |
| --- | --- | --- |
| QQQ | yfinance `Ticker.history(auto_adjust=False)` | 상장일부터 전 기간 |
| KODEX 200 | pykrx `get_etf_ohlcv_by_date` | 상장일부터 전 기간 |

**원본가를 쓰는 이유는 사용자가 결과를 차트와 직접 대조하기 위해서다**(루트 `CLAUDE.md` 측정의 원칙 8).
보통의 차트는 배당 미포함이므로 수정주가로 저장하면 신호일 목록의 종가가 차트와 어긋나고, 그러면
**원자료를 제공하는 의미가 절반으로 준다.**

그 대가로 **배당락·분배락이 조정되지 않은 채 남아 하루짜리 하락으로 잡힌다.** 실측된 영향은 이렇다.

| 항목 | 영향 |
| --- | --- |
| 일간 등락률 순위 (테스트 A) | **없다.** QQQ 분기 배당은 약 0.15% 라 ±8% 이상인 극단 등락 판정에 닿지 않는다 |
| 연속 등락 판정 (테스트 B) | **있다.** 배당락일이 상승 연속을 끊고 하락 연속을 만든다 |
| 절대 수익률 | 구간이 길수록 커진다. QQQ 1년 기준 약 -0.9%p, KODEX 200 1년 기준 약 -1.5%p |
| **베이스라인 대비 초과분** | **사실상 없다.** 왜곡이 신호군과 베이스라인에 똑같이 걸려 뺄 때 지워진다 |

**절대 수익률을 다른 자료와 나란히 놓을 때는 배당이 빠져 있다는 점을 결과 문서에 명시한다.**

---

## 3. 이벤트 정의

### 테스트 A — 역대급 등락

**정의**: 판정 시점까지 누적된 데이터 기준으로, 일간 등락률이 **역대 상위 10위** 안에 드는 날.

**순위 산정 방식 (확장 창, Expanding Window)**

- 각 종목의 데이터 시작일을 고정 시작점으로 하고, 판정일까지의 모든 데이터로 순위를 매긴다.
- 예: 2015-08-24 판정 → 2000-01-01 ~ 2015-08-24 데이터 내 순위
- **미래 데이터를 절대 참조하지 않는다.** 전체 기간으로 한 번에 순위를 매기는 것은 금지(look-ahead).
- 순위는 시간이 지나며 계속 바뀐다. 판정 당시 1위였던 날이 나중에 3위가 될 수 있으며,
  그 사실 자체를 결과표에 "당시 순위" 컬럼으로 남긴다.

**순위 컷**: 메인은 **K=10**. 강건성 확인을 위해 K=5·20 결과도 함께 산출한다 (§6).

**폭락과 폭등을 분리 집계한다.**

**사건 단위 집계를 함께 낸다.** 30일 이내에 붙어 있는 신호는 같은 사건에서 파생된 것으로 보고
사건 번호를 부여한다. 신호를 버리지는 않되, "신호 N건 = 사건 M개"를 항상 병기한다 (근거: §7 결정 ③).

### 테스트 B — 연속 등락

**정의**: **N일 연속 상승 또는 하락이 달성된 그날.** 그날 종가로 판정이 끝나고 다음 날 시가에 집행할 수 있다.

> 원 요청문의 "연속 구간의 마지막 날"은 채택하지 않는다. 그날이 마지막인지는 **다음 날 주가를 봐야**
> 알 수 있어 미래 참조가 된다. 이 정의로 낸 결과는 실제 매매에 쓸 수 없다 (근거: §7 결정 ④).

**N 범위**: 3, 4, 5, 6, 7, 8, 9, 10 전부 산출한다. 발생 빈도(연간 몇 회)를 먼저 보고하고,
너무 자주 발생해 의미가 없는 N이 있으면 그 판단 근거를 함께 제시한다.

**중복을 허용한다.** 7일 연속 랠리는 N=5·6·7 신호를 모두 만든다. N별로 독립 집계하되,
**"N=5와 N=7은 같은 랠리를 공유하므로 서로 독립된 결과가 아니다"** 를 결과 문서에 명시한다
(근거: §7 결정 ⑤).

**등락률이 정확히 0%인 날**은 연속을 끊되 방향을 부여하지 않는다.

**연속 상승과 연속 하락을 분리 집계한다.**

**집계 기간**: 테스트 A와 동일. 그 이전 구간 결과는 참고용으로 별도 표에 제시한다.

---

## 4. 측정 방법

**측정 구간 (forward return)**: 이벤트 발생 다음 거래일부터 아래 각 시점까지의 수익률.
전부 **거래일** 단위이며 달력일이 아니다 (§7 결정 ㉑).

- 1일, 2일, 3일 — 반전이 즉시 나오는지
- 1주(5거래일), 2주(10거래일) — 반전이 이어지는지
- 1개월(21거래일) — 바깥 경계. 되돌아가는지를 보는 기준

**수익률 기준점**: 이벤트일 종가 기준은 위 구간을 **전부**, 익일 시가 기준은 **1일 구간만** 산출한다
(§7 결정 ⑳). 두 값의 차이가 갭으로 새는 몫이며, 실제 집행 가능 구간은 익일 시가 기준이다.
**집계·초과분·검정은 종가 기준만 쓴다** — 익일 시가는 `signals.csv` 의 원자료로만 남는다 (§7 결정 ㉓).

**측정 구간 끝이 데이터 범위를 넘어가는 신호**는 해당 구간에서 제외하되, 몇 건이 제외됐는지 명시한다.

---

## 5. 베이스라인 (필수 — 3종 모두)

수익률 절대값만으로는 판단할 수 없다. 아래 셋을 모두 산출해 비교한다.

| 베이스라인 | 내용 | 답하는 질문 |
| --- | --- | --- |
| **단순 보유 대비** | 같은 기간 전체의 평균 forward return | "평소보다 나은가" |
| **무작위 진입 대비** | 같은 기간에서 무작위 날짜를 뽑아 forward return 분포 생성 (1,000회 반복, **난수 시드 고정**) | "운이 아닌가" |
| **조건부 대비** | 하락 국면 내에서만 비교 — **종가가 200일 이동평균 아래인 날.** EMA·SMA 둘 다 산출해 병기 | "같은 국면 안에서 이 날이 특별한가" |

세 결과가 어긋나는 지점이 있으면 그 자체를 명시적으로 보고한다.

---

## 6. 통계 처리와 강건성

각 이벤트 × 각 측정 구간마다 아래를 모두 보고한다.

- **평균과 중앙값을 반드시 병기한다.** 평균은 소수의 극단 사례에 왜곡된다.
- 평균과 중앙값이 크게 벌어지면 → 소수 사건이 결과를 만들고 있다는 신호이므로 명시적으로 지적한다.
- 승률 (양수 비율) / **역방향 비율** / 최고값 / 최악값 / 표준편차
- **역방향 비율은 신호가 걸린 방향의 반대로 움직인 비율이다** — 상승 방향 신호(폭등·연속 상승)면
  음수 비율, 하락 방향 신호(폭락·연속 하락)면 양수 비율이다. 후자는 승률과 같은 값이 된다 (§7 결정 ㉒).
  **`100 − 승률` 로 대신할 수 없다** — 보합(정확히 0%)이 어느 쪽에도 들어가지 않기 때문이다
- **표본 수 (절대 생략 금지)** — 신호 건수와 사건 건수를 함께 낸다
- 유의성 검정 (t-test 등, 사용한 방법 명시). **표본이 한 자릿수인 칸에는 검정을 붙이지 않고
  "표본 부족으로 검정 불가"로 표기한다.** 숫자를 만들어내는 것이 더 나쁘다

### 강건성 체크

| 항목 | 방법 | 무너지면 의미 |
| --- | --- | --- |
| 순위 컷 민감도 | K = 5 / 10 / 20에서 방향이 일관되는가 | 특정 값에만 붙은 우연 |
| 집계 시작 민감도 | 2003 / 2005 / 2008 / 2011년 시작에서 일관되는가 | 구간 선택에 의존 |
| 연속 N 민감도 | N 값들에서 방향이 일관되는가 | 동일 |
| 구간별 일관성 | 2010년대 / 2020년대로 나눠 방향이 일관되는가 | 레짐 의존적, 사용 불가 |
| 코스피 하위표본 | 2015년 6월 이후만 별도 집계 | 가격제한폭 30% 확대 효과 |

> **가격제한폭 항목은 실측 후 판단한다.** 2015년 6월 개별종목 제한폭이 15%→30%로 확대됐지만,
> KODEX 200은 지수 ETF라 일간 등락이 ±15%를 넘긴 적이 사실상 없을 것으로 예상된다.
> 실제로 그렇다면 이 체크는 무의미하므로 "해당 없음"으로 기록하고 넘어간다.

---

## 7. 확정된 설계 결정

| # | 항목 | 확정 | 탈락안 | 근거 |
| --- | --- | --- | --- | --- |
| ① | 테스트 A 이벤트 정의 | **역대 상위 10위 유지** (확장창) | 최근 3년 롤링 상위 K위 / 백분위 / 순위 컷 확대 | 표본이 적다는 사실 자체가 측정 결과다. "역대급 폭락은 통계를 낼 만큼 자주 오지 않는다"는 결론을 정의를 바꿔 회피하지 않는다 |
| ② | 집계 시작 시점 | 2011년 기본 + 2003·2005·2008년 병기 | 2011년 단일 | 시작 시점을 앞당겨도 사건 수가 3개로 같다는 것이 §8 실측으로 확인됐다. 이를 표로 보여주는 것이 결론의 일부다 |
| ③ | 사건 단위 집계 | 30일 이내 신호를 같은 사건으로 묶어 **병기** | 신호 단위만 / 클러스터를 버림 | 2008년 4건이 승률을 부풀린다. 신호를 버리지 않되 독립이 아님을 드러낸다 |
| ④ | 테스트 B 신호일 | **N일 연속이 달성된 그날** | "연속 구간의 마지막 날" | 후자는 다음 날을 봐야 판정할 수 있어 미래 참조다. 실제 매매에 쓸 수 없는 결과가 된다 |
| ⑤ | 테스트 B의 N 중복 | 중복 허용 + 비독립성 명시 | 랠리당 첫 신호만 / 정확히 N일에 끝난 경우만 | "5일 연속이면 산다"는 실제 매매 상황과 가장 가깝다. 신호를 임의로 버리지 않는다 |
| ⑥ | 수익률 기준점 (**⑳ 으로 좁혀짐 2026-08-20, ㉓ 으로 다시 좁혀짐 2026-08-21**) | 종가 기준과 익일 시가 기준 **둘 다** → **익일 시가는 1일 구간만** | 하나만 | 둘의 차이가 곧 갭으로 새는 몫이며 그 자체가 정보다. 계산 비용은 거의 같다 |
| ⑦ | 조건부 베이스라인의 하락 국면 | 종가 < 200일 이동평균. EMA·SMA 병기 | 하나만 | 어느 쪽을 쓰느냐로 결론이 갈리면 그 자체가 강건성 문제다 |
| ⑧ | 0% 등락일 | 연속을 끊되 방향 미부여 | 상승으로 간주 / 무시하고 이어감 | 국내 ETF에서 발생 가능하며, 어느 쪽으로 처리하든 임의성이 생긴다. 가장 보수적인 쪽을 택한다 |
| ⑨ | ~~KODEX 200 가격 기준~~ (2026-08-12 확정, **⑰ 로 대체됨 2026-08-20**) | ~~원본가 전 기간 본검증 + 수정주가 구간 대조 병기~~ → **수정주가 대조를 걷어내고 원본가 단일 기준으로 간다.** 아래 근거는 결정 당시의 기록으로 남긴다 | A: 수정주가를 구간별로 나눠 받아 이어붙이기 / B: 원본가 단일 기준 / D: 자체 분배락 조정 | A 는 **실측으로 불가능**이 확정됐다(§8 결론 4). 종료일을 12년 앞당겨도 같은 시작일이 돌아온다 — 최근 3,000거래일 창이 조회 시점에 고정돼 있어 과거 수정주가는 존재하지 않는다. B 는 "가격 기준을 바꾸면 결론이 바뀌는가"를 아예 재지 않고 넘어간다. D 는 2014년 이전 조정폭이 추정값이 되어, 측정만 하고 추정하지 않는다는 이 프로젝트의 성격과 충돌한다. C 는 원칙 1(여러 설정값을 나란히 보고)과 원칙 7(파라미터 민감도)을 그대로 따르며, **병기 가능 구간이 2014-05-22 이후로 제한된다는 사실 자체를 결과에 적는다** |
| ⑩ | 테스트 B "달성된 그날"의 판정식 (2026-08-13 확정) | **연속 길이가 정확히 N 인 날** | 연속 길이 ≥ N 인 모든 날 | **실측으로 갈렸다.** `= N` 은 아래 §8 "테스트 B 발생 빈도" 표를 그대로 재현하지만, `≥ N` 은 N=3 연속 상승이 685건으로 표의 300건과 두 배 넘게 어긋난다. 결정 ⑤ 의 "7일 연속 랠리는 N=5·6·7 신호를 모두 만든다"도 **각 N 마다 한 건씩**이라는 뜻이 된다 |
| ⑪ | 사건 묶기 "30일"의 단위 (2026-08-13 확정) | **달력일 30일.** 간격은 **바로 앞 신호**와 잰다 | 거래일 30일 / 사건 시작일부터 30일 | §4 는 측정 구간을 "1주(5거래일)·1개월(21거래일)"처럼 **거래일일 때 반드시 명시**한다. §3·결정 ③ 은 그냥 "30일"이므로 달력일이며, "같은 충격의 연쇄"도 달력 시간 개념이다. 앞 신호 기준으로 재는 것은 2008년 사건이 9/29 → 10/13 → 10/15 → 10/28 로 한 달 넘게 이어지면서도 한 사건인 이유다. **테스트 A 7건은 어느 기준이든 사건 3개**지만 테스트 B 는 크게 갈린다 — N=3 연속 상승 301건이 달력 기준 41개, 거래일 기준 19개 사건이 된다 |
| ⑫ | 사건 번호를 매기는 범위 (2026-08-13 확정, **⑯ 에서 테스트 A 한정으로 좁힘**) | 같은 파라미터·같은 시작연도 안에서 **폭등·폭락을 합쳐** 번호를 매기고, 방향별 표에는 "그 방향 신호가 걸친 사건 수"를 적는다 | 방향별로 따로 묶기 | §8 의 신호 목록이 이미 이 방식이다 — 2008년 사건에 폭락 2건과 폭등 2건이 **함께** 들어 있어 "7건 = 사건 3개"가 나온다. 방향별로 나누면 폭등 2건 = 2사건·폭락 2건 = 2사건이 되어, 결정 ③ 이 드러내려던 **비독립성이 오히려 숨는다** |
| ⑬ | 참고용 z-score 산식 (2026-08-13 확정) | **당일 등락률 ÷ 직전 60거래일 등락률 표준편차** (평균 차감 없음, 판정일 제외) | (등락률 − 직전 60일 평균) ÷ 표준편차 | §11 의 "직전 60일 **변동성 대비**"를 글자 그대로 읽은 것이다. "변동성 대비"는 중심으로부터의 거리가 아니라 산포 대비 크기를 뜻하고, "직전"은 그날의 등락이 자기 기준선에 섞이지 않도록 판정일을 뺀다는 뜻이다. 판정에 쓰지 않는 해석 보조 컬럼이라 어느 쪽을 택해도 결론은 바뀌지 않는다. 직전 구간의 변동이 전혀 없으면 **값을 비운다** — 0 으로 나눈 무한대는 "극단적으로 큰 등락"이 아니라 계산 불가다 |
| ⑭ | 베이스라인 모집단의 기간 (2026-08-14 확정) | **신호군의 집계 시작연도(및 시대 구간)에 맞춰 자른다.** 200일 이동평균은 전 기간으로 쌓은 뒤 날짜로 거른다 | 전 기간 모집단 한 벌을 모든 신호군이 돌려쓴다 | §5 가 단순 보유를 "**같은 기간** 전체의 평균 forward return", 무작위 진입을 "**같은 기간에서** 무작위 날짜를 뽑아"로 적었다. 전 기간 한 벌을 쓰면 2003년 시작 신호군과 2011년 시작 신호군이 같은 기준과 비교돼 초과분의 차이에 "그 사이 시장이 어땠는가"가 섞이고, 결국 **시작 시점 민감도 체크가 신호가 아니라 시장을 재게 된다.** §8 "베이스라인 계층 연결 확인"이 전 기간을 쓰면서 스스로 "정식 결과가 아니다 — 집계 시작일도 자르지 않았다"고 적어 둔 것과 같은 방향이다. 이동평균을 먼저 자르지 않는 이유는 창이 그 지점부터 다시 차서 판정 시작일이 밀리기 때문이다 |
| ⑮ | 시대 구간(2010년대·2020년대) 산출 범위 (2026-08-14 확정) | **기본 시작연도(2011)에만** 구간 2개를 추가 산출한다. 구간은 **신호 선택의 양끝**만 좁히고 시세를 자르지 않는다 | 모든 시작연도 × 구간 3종 순회 / 3-b 에서 내지 않고 결과 문서가 손집계 | §6 은 이것을 축이 아니라 강건성 "체크"로 두었고 §12 는 결과 문서 필수 표로 지정했다. 모든 시작연도에 곱하면 "2003년 시작 + 2020년대" 같은 해석하기 어려운 조합이 생기고 산출·검정 비용이 3배가 된다. 결과 문서가 `signals.csv` 를 걸러 손집계하면 **반올림 시점·사건 수·검정이 측정 계층 밖에서 만들어져** 나머지 표와 산식이 갈라진다 |
| ⑯ | 사건 번호를 합칠지 여부 (2026-08-14 확정) | **테스트 A 는 폭등·폭락을 합쳐**(⑫ 그대로), **테스트 B 는 방향별로 따로** 매긴다 | 두 테스트 모두 합집합 / 두 기준을 컬럼으로 병기 | **사건 길이 실측이 갈랐다.** 테스트 A(K=10)의 합집합 사건은 최장 29일·평균 11일로 "한 충격"이 성립한다. 반면 테스트 B 는 신호가 촘촘해 30일 체인이 끊기지 않고 이어져, N=3 합집합 사건이 **최장 2,124일(5.8년)·평균 403일** 이 된다. 방향이 반대인 신호까지 한 충격으로 묶어 "같은 사건인가"를 더 이상 구분하지 못한다(방향별로 매기면 최장 395일·평균 96일). 아래 §8 "테스트 B 발생 빈도" 표의 사건 수가 처음부터 방향별 기준이었고, 이 결정이 그 값을 정식화한다 |
| ⑰ | **가격 기준 (2026-08-20 확정, ⑨ 를 대체)** | **두 시장 모두 원본가 단일 기준.** 수정주가 데이터셋을 산출물에서 없앤다 | A: QQQ 수정주가 + 국내 원본가 (직전 상태) / B: 두 기준을 종목마다 병기 / C: 두 시장 모두 수정주가 | **대조 가능성이 갈랐다.** 측정의 원칙 8 은 "사용자가 직접 검증할 수 있어야 한다"이고 사용자는 차트로 대조하는데, **보통의 차트는 배당 미포함이다.** 2026-08-19 대조에서 실제로 막혔다 — 2020-03-12 의 차트 종가 177.32 와 파일 종가 170.699097 이 달라, 배율 1.038787 을 구해 인접 3일을 예측·대조하고 나서야 배당 조정임이 확인됐다. 대조할 때마다 이 계산을 반복해야 하면 원자료를 제공하는 의미가 준다. **B 는 결정 ⑨ 의 확정안이며 "기준을 바꾸면 결론이 바뀌는가"를 잴 수 있다는 장점이 있으나, 그 장점이 실측으로 작다는 것이 확인됐다** — 아래 전환 실측에서 초과분 차이가 1년 기준 +0.058%p 였다. C 는 대조 가능성을 잃으면서 얻는 것이 없다 |
| ⑱ | **가격 자릿수 (2026-08-20 확정)** | **소수가 나오는 시장은 4자리, 원화 정수 가격은 정수.** 저장(수집기)과 출력(신호일 목록)이 같은 값을 쓰며 정의는 `common_constants.PRICE_DECIMALS` 한 곳에 둔다 | A: 6자리 유지 (직전 상태) / B: 절사 / C: 원화까지 4자리로 통일 / D: 저장은 6자리, 출력만 4자리 | **결정 ⑰ 의 목적이 자릿수에서 반쯤 무너져 있었다.** ⑰ 는 "종가가 차트와 그대로 맞게 한다"였는데 `signals.csv` 에는 `169.300003`·`177.320007` 처럼 꼬리가 남아 차트의 `169.30`·`177.32` 와 어긋났다. 그 자리는 실제 시세가 아니라 **소수를 이진수로 담을 때 생기는 표현 잡음**이며, 2026-08-20 대조 3단계에서 사용자가 실제로 걸렸다. A 는 그 잡음을 그대로 남긴다. B 는 저장소의 모든 자릿수 처리가 `round()` 이고 규칙표 제목부터 "반올림 규칙"이라 관용을 이탈할 이유가 없다. C 는 `22475 → 22475.0` 처럼 **실제 호가에 없는 자리**를 만들어 대조를 오히려 나쁘게 하며, 실행 계층 계약의 "원화 가격에 없는 소수 자리를 붙이지 않는다"와 정면으로 충돌한다. D 는 저장과 출력이 갈라져 **없는 정밀도를 만들어 내거나 저장해 둔 값을 버리는데 표는 정상으로 보인다.** 4자리를 고른 근거는 미국 ETF 호가 단위가 0.01 이고 1999~2000년 QQQ 의 1/32 틱(0.03125)까지 담기면서, 그보다 깊은 잡음(1e-6)은 잘라 낸다는 것이다 — 그 절단이 판정을 바꾸지 않음이 아래 실측으로 확인됐다 |
| ⑲ | **`가격기준` 식별 컬럼 (2026-08-20 확정)** | **네 산출물 CSV 에서 뺀다.** 식별 컬럼은 6개(종목·테스트·파라미터·시작연도·방향·시대 구간)이며, 가격 기준은 `summary.json` 의 `datasets` 와 터미널 "검증 대상 시세" 표에 남긴다 | A: 그대로 유지 (직전 상태) / B: CSV 에서 빼되 터미널 발췌 표에는 남기기 / C: `Dataset.price_basis` 필드까지 삭제 | **결정 ⑰ 로 가격 기준이 축에서 상수가 됐다.** 사용자가 "이 프로젝트에서는 무조건 원본가 — 차트와 동일하게"로 확정했고, 그 결과 `signals.csv` 12,679행의 `가격기준` 이 전부 `원본가` 하나다. 대조는 필터를 거는 작업인데(HANDS_ON 2~3단계) **고를 것이 없는 필터가 하나 끼어 있었다.** 가격 기준은 **행 단위가 아니라 데이터셋 단위 속성**이므로 실행 요약이 제 자리다. A 는 상수를 4개 파일 × 수만 행에 반복한다. B 는 화면과 CSV 의 컬럼 구성이 갈라져 "화면에서 본 숫자를 CSV 에서 찾는다"는 출력 계약과 어긋난다. C 는 **"무엇으로 쟀는지"의 기록까지 지운다** — 결과를 나중에 열었을 때 어떤 가격으로 계산했는지 알 수 없게 된다. **대가는 산출물의 데이터셋 구분자가 `종목` 하나뿐이 되는 것**이며, 티커가 겹치면 서로 다른 시세의 행이 같은 신호군으로 읽히는데 표는 정상으로 보인다. 그래서 `DATASETS` 의 `ticker` 중복 금지를 불변조건 테스트로 걸었다. 되돌리기는 `IDENTITY_COLUMNS` 한 줄과 identity dict 두 줄이라 미리 남겨 둘 이유가 없다 |

| ⑳ | **익일 시가 기준의 측정 구간 (2026-08-20 확정, ⑥ 을 좁힘. ㉓ 으로 집계에서 제외됨 2026-08-21)** | **1일 구간만 낸다.** 종가 기준은 §4 의 6구간 전부를 내고, 익일 시가 기준은 1일 하나다. 정의는 `measure/forward_return.NEXT_OPEN_HORIZONS` 한 곳에 둔다 | A: (기준 × 구간) 12칸 전부 유지 (직전 상태) / B: `signals.csv` 컬럼만 빼고 집계 3파일은 유지 / C: 익일 시가 축을 통째로 폐기 | **장기 구간의 익일 시가 값은 종가 값을 한 번 더 적는 것이다.** 두 기준은 출구(`D+h` 종가)가 같고 입구만 다르므로 `익일시가 h = (1 + 종가 h) × D 종가 ÷ D+1 시가 − 1` 이고, **갭 배수 `D 종가 ÷ D+1 시가` 는 구간과 무관한 상수**다. 그래서 신호일 한 줄에서 `(1 + 종가 1일) ÷ (1 + 익일시가 1일)` 로 갭 배수를 역산하면 지운 칸이 그대로 복원된다 — QQQ 2020-03-16 에서 `(1+0.0758)÷(1+0.0380) = 1.0364`, `(1+0.2370)÷1.0364 − 1 = +19.35%` 로 결정 이전 산출물의 `익일시가 1개월` 값과 일치했다. **1일 칸을 남긴 이유가 이것이며**, 그 값 자체가 "갭으로 새는 몫"의 직접 관측치이기도 하다. A 는 `signals.csv` 24컬럼 중 5개를 복원 가능한 중복에 쓰고, 대조는 필터를 걸고 눈으로 읽는 작업이라 읽을 칸이 두 배가 된다. B 는 파일마다 칸 구성이 갈라져 "화면에서 본 숫자를 CSV 에서 찾는다"는 출력 계약과 어긋난다. C 는 갭을 관측할 수단과 복원 근거를 함께 버린다. **대가는 집계값이다** — 평균·중앙값·승률은 개별 값을 복원한 뒤 다시 집계해야 나오므로 `statistics.csv` 의 익일 시가 장기 행은 되살아나지 않는다. 결과 문서 5.5절의 구간별 종가·익일시가 비교표는 **재실행으로 재현되지 않으며**, 결정 이전 산출물의 기록으로 남긴다 |
| ㉑ | **측정 구간 (2026-08-21 확정)** | **1·2·3·5·10·21 거래일** (1일·2일·3일·1주·2주·1개월). 정의는 `measure/forward_return.DEFAULT_HORIZONS` 한 곳에 둔다 | A: 1·5·21·63·126·252 유지 (직전 상태) / B: 1·2·3·5·10 (2주까지) / C: 1·3·5·10·21·63 (3개월 유지) | **사용자가 재려는 것이 단기 매매로 바뀌었고, 재는 방식이 그 결정을 강제했다.** 결정 ㉒ 의 역방향 비율은 인버스 상품으로 집행하는데 그 상품은 **일간 리밸런싱**이라 구간이 길어질수록 원지수 방향과 손익 부호가 어긋난다 — QQQ 테스트 A 신호 14일 × 6구간 84칸에서 "원지수 하락 = −1x 이익"이 어긋난 칸이 **1일 0/14·1주 0/14 인데 1개월~6개월은 각 2/14(14.3%)** 였다(전체 6칸, 전부 2008년 금융위기 구간). 즉 역방향 비율이 유효한 범위까지만 재는 것이 정직하다. 부수 효과로 **제외가 사라진다** — 제외율이 10거래일 이하 0.04%(12,679행 중 5행)·21거래일 0.91% 인 반면 1년은 7.53%(955행)였고, 실제 재실행에서 전체 제외가 **0.23%(76,074칸 중 175칸)** 로 떨어졌다. **구간을 성과로 고르지 않았다** — QQQ 폭등 신호의 역방향 비율은 1일 100% → 1주~3개월 67% → 6개월 33% → 1년 0% 로 짧을수록 좋아 보이지만, 표본이 3건이라 그렇게 고르면 재현되지 않는다(측정의 원칙 1). B 는 바깥 경계가 없어 반전이 이어지는지 되돌아가는지를 볼 기준이 사라진다. C 의 63거래일은 위 불일치 구간이라 역방향 비율과 함께 쓰면 어긋난다. **대가는 장기 결과의 상실이다** — 결과 문서의 3개월·6개월·1년 표는 재실행으로 재현되지 않으며, 되살리려면 `DEFAULT_HORIZONS` 한 줄을 되돌린다(표시 이름은 `HORIZON_LABELS` 에 남겨 뒀다) |
| ㉒ | **역방향 비율 (2026-08-21 확정)** | **신호가 걸린 방향의 반대로 움직인 비율.** 상승 방향 신호(폭등·연속 상승)는 하락 비율, 하락 방향 신호(폭락·연속 하락)는 승률이다. `statistics.csv` 와 `excess.csv` 에 붙는다 | A: 승률만 유지 (직전 상태) / B: `승률` 을 `상승 비율` 로 개명만 / C: 인버스 −1x 를 시뮬레이션해 실제 승률 산출 / D: `승률` 값 자체를 방향별로 뒤집음 | **지수 극단 이벤트는 양방향으로 집행할 수 있다(인버스 ETF).** 그래서 폭등 신호에서 "맞았는가"는 가격이 **내렸는가**인데, 승률은 언제나 `수익률 > 0` 이라 **정반대를 잰다** — QQQ 테스트 A K=10·2003 폭등 1일의 승률 0.00% 는 사실 "3건 전부 다음날 하락"이며 인버스 관점 100% 다. **`100 − 승률` 로 대신할 수 없다**: 승률이 `> 0` 이므로 여집합은 하락 + 보합이고, 직전 산출물 86,833칸 중 표시값이 정확히 `0.00` 인 칸이 **337개**(QQQ 129 / KODEX 200 208) 였다. 그래서 하락 비율을 `< 0` 으로 따로 센다. **C 를 버린 것이 핵심 판단이다** — 인버스 상품은 배수(−1x/−2x/−3x)를 골라야 하고 보수(PSQ 연 0.95%)·차입비용·추적오차가 붙어 **추정이 된다.** 이 프로젝트는 측정만 하고 추정하지 않으며(결정 ⑨ 와 같은 이유), 배수를 다 내면 산출물이 3배가 된다. **그 대가로 역방향 비율은 원지수 방향이지 인버스 손익이 아니다** — ㉑ 의 실측대로 1일·1주는 100% 일치하지만 1개월은 14.3% 어긋나며, 이 사실을 결과 문서에 적는다. A 는 폭등 쪽을 매번 머리로 뒤집게 한다. B 는 이름만 정직해지고 방향 판단은 여전히 사람 몫이다. D 는 같은 컬럼이 방향에 따라 다른 것을 재게 되어 원자료의 부호와 어긋난다. **계층은 나눈다** — `measure` 는 방향을 모르므로 하락 비율까지만 내고, 어느 쪽이 역방향인지는 방향을 아는 `studies` 가 고르며 `report` 는 받은 컬럼을 그린다 |
| ㉓ | **익일 시가를 집계에서 제외 (2026-08-21 확정, ⑳ 을 좁힘)** | **`signals.csv` 의 `익일시가 1일` 은 유지하고, `statistics.csv`·`excess.csv`·`test.csv` 에서는 익일 시가 행을 없앤다.** 집계에 넘길 기준은 `runner.AGGREGATED_BASIS` 한 곳이 정한다 | A: 네 파일 모두 유지 (직전 상태) / B: 익일 시가 축을 통째로 폐기 | **결정 ⑳ 이 남긴 익일 시가 1일은 원자료로서의 값이 크고 집계값으로서의 값이 작다.** 갭으로 새는 몫의 직접 관측치이자 갭 배수 역산 수단이므로 `signals.csv` 에서는 필수지만, 그 한 칸의 평균·승률을 베이스라인과 비교하고 검정까지 붙이면 집계 3파일이 각각 1/7 만큼 부풀면서 `기준` 컬럼을 계속 끌고 다녀야 한다. **⑳ 과 달리 이번 손실은 되돌릴 수 있다** — `signals.csv` 에 원자료가 남으므로 평균·승률을 다시 집계할 수 있다. B 는 갭을 관측할 수단과 복원 근거를 함께 버려 ⑳ 의 취지를 무너뜨린다. **실측**: 재실행에서 `statistics.csv` 1,736 → **1,488행**, `excess.csv`·`test.csv` 5,208 → **4,464행** 이 됐고 `signals.csv` 는 19컬럼과 12,679행을 유지했다 |
| ㉔ | **`기준` 식별 컬럼 (2026-08-21 확정)** | **집계 3파일과 터미널 비교 표에서 뺀다.** 측정 기준은 `summary.json` 의 `parameters.aggregated_basis` 에 남긴다 | A: 그대로 유지 (직전 상태) / B: CSV 에서만 빼고 터미널 표에는 남기기 / C: `ReturnBasis` 열거형까지 삭제 | **결정 ㉓ 으로 `기준` 이 축에서 상수가 됐다** — 집계 3파일의 값이 전부 `종가` 하나다. 대조는 필터를 거는 작업인데 **고를 것이 없는 필터가 하나 끼어 있게 된다.** 결정 ⑲(`가격기준` 컬럼 제거)와 같은 논리이며, 기준은 행 단위가 아니라 **실행 단위 속성**이므로 실행 요약이 제 자리다. B 는 화면과 CSV 의 컬럼 구성이 갈라져 "화면에서 본 숫자를 CSV 에서 찾는다"는 출력 계약과 어긋나므로 `build_comparison_table` 에서도 함께 뺐다. C 는 **"무엇으로 쟀는지"의 기록까지 지운다.** **대가는 두 기준이 섞여 들어와도 표가 정상으로 보인다는 것**이다 — `기준` 이 없으면 같은 구간이 두 줄로 나오는데 어느 줄이 무엇인지 알 수 없다. 그래서 집계 3표가 **기준이 둘 이상이면 `ValueError`** 를 던지게 하고 테스트로 고정했다 |

#### 단기 전환 실측 — 구간을 바꾸면 무엇이 달라지는가 (2026-08-21)

결정 ㉑~㉔ 의 근거다. 같은 시세로 재실행해 직전 산출물과 대조했다.

| 확인 | 결과 |
| --- | --- |
| **신호 판정** | **완전히 동일.** 식별 6컬럼 + 날짜·종가·등락률·당시 순위·사건 번호·z-score 를 12,679행 전수 대조해 불일치 **0건**. 측정 구간은 "신호 다음에 무엇을 재는가"이지 "무엇이 신호인가"가 아니라는 것이 확인됐다 |
| **겹치는 구간의 값** | `종가 1일`·`익일시가 1일`·`종가 1주`·`종가 1개월` 이 **한 칸도 바뀌지 않았다**(불일치 0건). 구간 목록이 바뀌어도 개별 칸의 산식은 그대로다 |
| 산출물 규모 | `signals.csv` 19컬럼·12,679행 유지(구간 이름만 교체). `statistics.csv` 1,736 → **1,488행**, `excess.csv`·`test.csv` 5,208 → **4,464행** |
| 제외 | 전체 76,074칸 중 **175칸(0.23%)**. 직전 구간에서 1년 한 칸만 955행이었던 것과 대비된다 |
| 역방향 비율 | 하락 방향 신호에서 승률과 값이 다른 칸 **0건**(정의상 같아야 한다). 상승 방향에서 두 값이 같은 칸 32개는 전부 승률과 하락 비율이 우연히 같은 경우이며(30개는 정확히 반반이라 합이 100%), 나머지 2개는 보합이 있어 합이 100% 미만이다 |

#### 산출물 축소 실측 — 익일 시가 장기 구간을 뺐을 때 (2026-08-20)

같은 시세·같은 파라미터로 재실행해 결정 이전 산출물(`20260820_170440_index_extreme`)과 대조했다.

| 산출물 | 이전 | 이후 | 확인 |
| --- | --- | --- | --- |
| `signals.csv` | 24컬럼 · 12,679행 | **19컬럼** · 12,679행 | 사라진 컬럼은 `익일시가 1주·1개월·3개월·6개월·1년` 5개뿐이고, 남은 19컬럼은 **전수 일치** |
| `statistics.csv` | 2,976행 | **1,736행** (248 신호군 × 7칸) | 공통 행의 값 불일치 **0칸** |
| `excess.csv` | 8,928행 | **5,208행** | 공통 행의 값 불일치 **0칸** |
| `test.csv` | 8,928행 | **5,208행** | 공통 행의 값 불일치 **0칸** |

**신호 판정은 하나도 바뀌지 않았다.** 신호일 집합·종가·등락률·당시 순위·사건 번호·z-score 가
이전 실행과 완전히 같다. 이 결정은 무엇을 재는지가 아니라 **무엇을 적는지**만 바꾼다.

#### 전환 실측 — 가격 기준을 바꾸면 무엇이 달라지는가 (2026-08-20)

결정 ⑰ 의 근거다. 같은 파라미터로 QQQ 수정주가 결과와 원본가 결과를 대조했다.
**QQQ 데이터가 7거래일 늘어난 것(2026-08-07 → 08-18)이 함께 섞이므로, 2026-08-07 로 잘라 기간 효과를 분리했다.**

| 확인 | 결과 |
| --- | --- |
| 테스트 A 신호 (K=10, 2003년 시작) | **날짜·등락률·사건 번호가 완전히 동일.** 조정 배율이 분자와 분모에 함께 걸려 약분되므로 등락률은 가격 기준에 불변이다 |
| 테스트 B 신호 | 41건이 사라지고 64건이 생겨 **순 +23건.** 새로 생긴 64건은 **전부 연속 하락**이다 |
| 변화의 원인 | **신호가 바뀐 날짜 22개 전부(100%)가 배당락일 직후 10거래일 안**이었다. 설명되지 않는 변화는 0건이다 |
| 절대 수익률 (평균) | 1일 -0.053%p, 1개월 -0.010%p, **1년 -0.889%p.** 구간이 길수록 배당이 더 빠진다 |
| **베이스라인 대비 초과분** | 1년·단순 보유 기준 **+0.058%p** (중앙값 +0.030%p). **절대값은 0.9%p 낮아졌는데 초과분은 사실상 0 이다** |

> **왜곡은 절대값에만 남고 초과분에서는 지워진다.** 신호군과 베이스라인이 같은 시세에서 나오므로
> 뺄 때 상쇄되기 때문이다. 이 검증이 답하려는 질문("신호일이 평소보다 나은가")은 초과분으로 답하므로,
> 원본가 전환이 결론을 흔들지 않는다.


---

## 8. 사전 실측 기록 (QQQ, 2026-08-09)

설계 확정 전 `storage/market/QQQ_max.csv`(2000-01-04 ~ 2026-07-24, 6,678거래일)로 확인한 결과다.
당시 이 파일은 이전 프로젝트에서 이관한 것이었다.

**2026-08-11 에 자체 수집기로 재수집을 마쳤고, 아래 표들은 그대로 유효함이 확인됐다.**
대조 결과는 이 장의 "yfinance 자체 수집기" 절에 있다.

> **아래 표의 가격은 수정주가 기준이다.** 2026-08-20 에 원본가로 전환했으므로(§7 결정 ⑰)
> **종가 열은 현재 산출물과 절대 수준이 다르다.** 다만 **등락률·순위·날짜는 가격 기준에 불변**이며,
> 전환 후 재실행에서 테스트 A 신호의 날짜·등락률·사건 번호가 완전히 동일함이 확인됐다.
> 표를 다시 뽑을 필요가 없는 이유이며, 값은 당시 기록 그대로 남긴다.

### 지금 시점에서 본 역대 상위 10위

| 순위 | 폭등일 | 등락률 | 순위 | 폭락일 | 등락률 |
| --- | --- | --- | --- | --- | --- |
| 1 | 2001-01-03 | +16.84% | 1 | 2020-03-16 | -11.98% |
| 2 | 2000-01-07 | +12.37% | 2 | 2020-03-12 | -9.17% |
| 3 | 2008-10-13 | +12.16% | 3 | 2008-10-15 | -8.96% |
| 4 | 2025-04-09 | +12.00% | 4 | 2000-04-14 | -8.66% |
| 5 | 2000-04-17 | +11.51% | 5 | 2001-09-17 | -8.50% |
| 6 | 2008-10-28 | +11.05% | 6 | 2001-01-02 | -8.46% |
| 7 | 2002-05-08 | +10.70% | 7 | 2000-05-23 | -8.35% |
| 8 | 2001-04-18 | +10.42% | 8 | 2001-03-28 | -8.21% |
| 9 | 2000-05-30 | +10.05% | 9 | 2008-09-29 | -7.94% |
| 10 | 2001-04-05 | +9.57% | 10 | 2000-11-08 | -7.82% |

폭등 10개 중 9개, 폭락 10개 중 8개가 2010년 이전이다. **역대 순위는 시간이 갈수록 진입 장벽이
올라가므로, 최근 구간에서는 신호가 거의 발생하지 않는다.**

### 집계 시작 시점별 신호 수 (K=10)

| 집계 시작 | 순위 축적 | 시작 시점에 상위 10위가 상위 몇 % | 폭등 | 폭락 | 신호 합계 | 서로 다른 사건 |
| --- | --- | --- | --- | --- | --- | --- |
| 2003년 | 3년 | 1.33% | 3 | 4 | 7건 | **3개** |
| 2005년 | 5년 | 0.80% | 3 | 4 | 7건 | **3개** |
| 2008년 | 8년 | 0.50% | 3 | 4 | 7건 | **3개** |
| 2011년 | 11년 | 0.36% | 1 | 2 | 3건 | **2개** |

### 2003년 이후 신호 전체와 이후 수익률 (신호일 종가 기준)

| 사건 | 날짜 | 종류 | 등락률 | 당시 순위 | 종가 | 1일 | 1주 | 1개월 | 3개월 | 6개월 | 1년 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ① 2008 금융위기 | 2008-09-29 | 폭락 | -7.94% | 6위 | 32.56 | +2.9% | -7.8% | -15.8% | -23.5% | -19.6% | +12.2% |
| | 2008-10-13 | 폭등 | +12.16% | 3위 | 30.25 | -4.3% | -5.5% | -14.3% | -15.8% | -7.5% | +21.9% |
| | 2008-10-15 | 폭락 | -8.96% | 1위 | 26.35 | +5.4% | +0.0% | -0.5% | -4.8% | +9.2% | +41.5% |
| | 2008-10-28 | 폭등 | +11.05% | 5위 | 27.43 | -0.3% | +5.9% | -7.9% | -7.0% | +7.9% | +30.6% |
| ② 2020 코로나 | 2020-03-12 | 폭락 | -9.17% | 1위 | 170.70 | +8.5% | +0.2% | +14.7% | +32.3% | +54.2% | +78.9% |
| | 2020-03-16 | 폭락 | -11.98% | 1위 | 162.98 | +7.6% | +0.9% | +24.0% | +41.3% | +63.2% | +90.4% |
| ③ 2025 관세쇼크 | 2025-04-09 | 폭등 | +12.00% | 4위 | 463.25 | -4.3% | -4.7% | +4.7% | +19.1% | +31.4% | +33.1% |

**해석 시 주의**: 1년 수익률은 7건 전부 플러스(승률 100%, 평균 +44.1%)지만 **실질 표본은 사건 3개**다.
그 3개가 전부 "결국 반등한 사건"이었을 뿐이며, 반등하지 않은 사건이 하나만 섞여도 승률은 67%로 떨어진다.
단기(1~3개월)에서는 사건 ①이 전부 마이너스, ②·③이 플러스로 **방향이 정반대**다.

#### 측정 계층 재현과 익일 시가 기준 대조 (2026-08-13)

`measure/forward_return.py` 로 위 표를 다시 계산했다. 데이터는 `storage/market/QQQ_max.csv`
(6,896행, 1999-03-10 ~ 2026-08-07)이며 **종가 7개와 여섯 구간 42칸이 전부 일치**한다(불일치 0).
따라서 앞으로 이 값이 재현되지 않으면 데이터가 아니라 **산식이 틀린 것**이다.

같은 실행에서 **익일 시가 기준**을 함께 냈다. 두 기준은 출구(`D+h` 종가)가 같고 입구만 다르므로,
차이는 전부 **신호일 종가에서 다음 날 시가 사이의 갭**이다. 괄호는 종가 기준 대비 차이(%p)다.

| 신호일 | 1일 | 1주 | 1개월 | 3개월 | 6개월 | 1년 |
| --- | --- | --- | --- | --- | --- | --- |
| 2008-09-29 | +3.2 (+0.3) | −7.6 (+0.2) | −15.5 (+0.2) | −23.3 (+0.2) | −19.4 (+0.2) | +12.5 (+0.3) |
| 2008-10-13 | −7.0 (−2.7) | −8.2 (−2.7) | −16.7 (−2.4) | −18.2 (−2.4) | −10.1 (−2.6) | +18.4 (−3.4) |
| 2008-10-15 | +4.7 (−0.7) | −0.6 (−0.7) | −1.1 (−0.7) | −5.4 (−0.6) | +8.5 (−0.7) | +40.5 (−1.0) |
| 2008-10-28 | −0.4 (−0.2) | +5.8 (−0.2) | −8.0 (−0.1) | −7.2 (−0.1) | +7.8 (−0.2) | +30.4 (−0.2) |
| 2020-03-12 | +2.8 (−5.7) | −5.0 (−5.2) | +8.8 (−6.0) | +25.4 (−6.9) | +46.1 (−8.0) | +69.6 (−9.3) |
| 2020-03-16 | +3.8 (−3.8) | −2.6 (−3.5) | +19.6 (−4.4) | +36.4 (−5.0) | +57.4 (−5.7) | +83.7 (−6.7) |
| 2025-04-09 | −1.6 (+2.6) | −2.1 (+2.6) | +7.6 (+2.9) | +22.3 (+3.3) | +35.0 (+3.6) | +36.8 (+3.7) |
| **7건 평균 차이** | **−1.44** | **−1.35** | **−1.50** | **−1.65** | **−1.93** | **−2.38** |

**차이가 신호마다 크게 갈린다.** 2008-09-29 는 모든 구간에서 +0.2~0.3%p 로 거의 일정한 반면,
2020-03-12·16 은 −5~−9%p, 2025-04-09 는 +2.6~+3.7%p 다. 극단 이벤트 다음 날은 갭이 크고,
그 갭이 한 방향으로 몰리지도 않는다. **어느 기준으로 재느냐가 결론의 크기를 바꾼다**는 뜻이며,
두 기준을 모두 산출하기로 한 §7 결정 ⑥ 의 근거다.

평균 차이가 구간이 길어질수록 벌어지는 것(−1.44%p → −2.38%p)은 갭이 커져서가 아니다.
입구 가격 차이는 고정인데 출구 가격이 커지므로 **같은 갭이 더 큰 비율 차이로 환산**되기 때문이다.

#### 베이스라인 계층 연결 확인 (2026-08-13)

측정 계층이 완성된 뒤 같은 7건을 베이스라인과 나란히 놓아 계층이 맞물리는지 확인한 결과다.
**검증 #1 의 정식 결과가 아니다** — 이벤트 정의(확장창 순위)와 사건 단위 집계가 아직 없고,
집계 시작일도 자르지 않았다. 정식 결과는 `docs/research/` 에 쓴다.

| 구분 | 표본 | 1년 평균 | 1년 중앙값 | 1년 승률 |
| --- | --- | --- | --- | --- |
| 신호 7건 | 7 | **+44.09%** | +33.13% | 100.0% |
| 단순 보유 (전 거래일) | 6,644 | +13.44% | +16.63% | 79.3% |
| 조건부 (SMA200 아래) | 1,742 | +9.65% | +18.94% | 64.0% |

신호군의 1년 평균 +44.09% 가 이 문서 위쪽 표의 **+44.1% 와 일치**한다 — 서로 다른 경로로 낸 값이
같으므로 집계 계층이 옳다. 200일선 아래인 날은 SMA 기준 1,754일·EMA 기준 1,654일이고,
창이 차기 전 199일은 판정하지 않았다.

**평균 초과(+30.6%p)가 중앙값 초과(+16.5%p)의 두 배 가까이 크다.** 소수 사건이 평균을 끌어올리고
있다는 신호이며, 실질 사건이 3개뿐이라는 사실과 같은 방향을 가리킨다.

순열 검정은 **12칸 전부 "표본 부족으로 검정 불가"** 로 나왔다. 유효 표본이 한 자릿수인 칸에는
검정을 붙이지 않는다는 §6 정책이 그대로 작동한 것이다.

#### 이벤트 정의 계층 재현 (2026-08-13)

`studies/index_extreme/` 이 완성된 뒤 **구현한 모듈로** 위 표들을 다시 냈다. 데이터는
`storage/market/QQQ_max.csv`(6,896행, 1999-03-10 ~ 2026-08-07)다.

| 대조 항목 | 결과 |
| --- | --- |
| 2003년 이후 신호 목록 | **날짜 7건·종류·등락률·당시 순위·종가가 전부 일치.** 당시 순위 6·3·1·5·1·1·4위가 그대로 재현됐다 |
| 사건 단위 묶기 | **7건 = 사건 3개** — 2008년 4건이 사건 ①, 2020년 2건이 사건 ②, 2025년 1건이 사건 ③ |
| 집계 시작 시점별 신호 수 (K=10) | **2003·2005·2008년 3/4/7건(사건 3개), 2011년 1/2/3건(사건 2개)** 로 위 표와 완전히 일치 |

따라서 앞으로 이 값들이 재현되지 않으면 데이터가 아니라 **산식이 틀린 것**이다.

같은 실행에서 참고용 z-score 를 함께 냈다. 신호일의 z-score 는 −4.59 ~ +6.97 범위이며,
2025-04-09(+6.97)이 가장 크고 2008-10-15(−3.16)이 가장 작다. **등락률 순위와 z-score 순위가
일치하지 않는다** — 2008-10-15 는 폭락 1위지만 z-score 는 그 시기 변동성 자체가 커서 작다.
판정을 순위로만 하는 이유이자, z-score 를 해석 보조로만 두는 이유다.

### 테스트 B 발생 빈도 (2011년 이후, 15.6년)

**아래는 이벤트 정의 계층이 낸 값이다** (2026-08-13, 위와 같은 파일). 사건 수는 결정 ⑪·⑯ 대로
**달력 30일·방향별** 기준이다 — 테스트 B 를 합집합으로 매기면 사건 하나가 수년에 걸쳐 구분력을
잃는다(결정 ⑯). 2026-08-14 실행 계층이 이 표를 그대로 재현했다.

| N | 연속 상승 | 연간 | 사건 | 연속 하락 | 연간 | 사건 |
| --- | --- | --- | --- | --- | --- | --- |
| 3 | 301건 | 19.3회 | 41 | 186건 | 11.9회 | 69 |
| 4 | 166건 | 10.6회 | 67 | 73건 | 4.7회 | 52 |
| 5 | 96건 | 6.2회 | 62 | 31건 | 2.0회 | 27 |
| 6 | 52건 | 3.3회 | 47 | 13건 | 0.8회 | 13 |
| 7 | 29건 | 1.9회 | 28 | 3건 | 0.2회 | 3 |
| 8 | 15건 | 1.0회 | 15 | 2건 | 0.1회 | 2 |
| 9 | 10건 | 0.6회 | 10 | 2건 | 0.1회 | 2 |
| 10 | 7건 | 0.4회 | 7 | 0건 | 0.0회 | 0 |

테스트 B는 N=3~7 구간에서 표본이 충분하다. 연속 하락은 N≥7에서 표본이 사라진다.

> 이 표는 설계 확정 당시의 값(6,678행 시절)과 몇 칸에서 1건씩 다르다. 차이는 전부 재수집으로
> 늘어난 뒤쪽 10거래일(2026-07-27 ~ 2026-08-07)에서 나온 것이며, 방향과 크기는 그대로다.

**신호가 많은 칸일수록 사건 수가 오히려 적다.** N=3 연속 상승은 301건이지만 사건은 41개이고,
N=4 는 166건에 사건 67개다. 신호가 촘촘할수록 30일 안에서 서로 이어져 한 사건으로 묶이기
때문이며, **N 이 작은 칸의 표본은 겉보기 건수만큼 독립적이지 않다**는 뜻이다.
결과 문서에서 N=3~4 의 승률·평균을 읽을 때 반드시 함께 제시한다.

### 검증 실행 계층 재현과 실행 규모 (2026-08-14)

`scripts/studies/run_index_extreme.py` 로 강건성 조합 전체를 한 번에 돌린 결과다.
데이터는 §2 의 세 파일이며 순열 검정은 반복 1,000회·시드 0 이다.

| 대조 항목 | 결과 |
| --- | --- |
| QQQ 테스트 A(K=10) 신호 수 | **2003·2005·2008년 폭등 3·폭락 4, 2011년 폭등 1·폭락 2** — 위 "집계 시작 시점별 신호 수" 표와 완전히 일치 |
| QQQ 테스트 A 사건 수 | 2003~2008년 시작은 **폭등이 걸친 사건 2개·폭락이 걸친 사건 2개**(합집합 3개), 2011년 시작은 **각 1개**(합집합 2개) |
| QQQ 2003년 신호 목록 | **날짜 7건·종가·등락률·당시 순위(6·3·1·5·1·1·4)·z-score 가 전부 일치.** 구간별 수익률도 위 표와 같다 |
| QQQ 테스트 B 발생 빈도 | **8개 N × 두 방향 16칸의 신호 수와 사건 수가 전부 일치** (상승 301/166/96/52/29/15/10/7, 사건 41/67/62/47/28/15/10/7) |

따라서 이 값들이 재현되지 않으면 데이터가 아니라 **조합을 도는 코드가 틀린 것**이다.

#### 실행 규모

| 항목 | 값 |
| --- | --- |
| 신호군 | **396개** (데이터셋 3 × [파라미터 11 × 방향 2 × 창 6]). 이 중 **21개는 신호 0건**이라 집계에서 빠졌고 요약에 목록이 남는다 |
| 산출물 | `signals.csv` 19,611행, `statistics.csv` 4,500행, `excess.csv`·`test.csv` 각 13,500행 |
| 실행 시간 | **약 3분** (전 조합, 반복 1,000회). 베이스라인 모집단을 신호군과 같은 기간으로 자르면서(결정 ⑭) 모집단이 작아져 사전 추정(10분)보다 짧다 |
| 검정 수행률 | 13,500칸 중 **8,334칸(61.7%)만 검정**됐고 5,166칸은 "표본 부족으로 검정 불가"다. §6 정책이 그대로 작동한 결과이며 결론의 일부다 |

#### 실행에서 드러난 것 — 국내 수정주가 계열은 시작연도 축이 작동하지 않는다

수정주가 파일이 2014-05-22 부터라 **시작연도 2003·2005·2008·2011 의 단순 보유 모집단이 모두
2,999일로 같다.** 네 조합이 사실상 같은 산출이므로 시작 시점 민감도를 이 계열로 판정할 수 없다.

더 중요한 것은 **테스트 A 가 다른 검증이 된다**는 점이다. 확장창 순위를 쌓을 구간이 없어 초반부터
상위 컷이 헐거워지고, K=10 폭등 신호가 원본가 14건 대비 **수정주가 94건**으로 불어난다.
수정주가 대조는 **테스트 B 와 절대 수익률 수준**에서 읽고, 테스트 A 순위 비교에는 쓰지 않는다.

참고로 200일선 아래인 날은 시작연도 2011 기준 QQQ 가 SMA 600일·EMA 531일(모집단 3,922일 중),
KODEX 200 원본가가 SMA 1,631일·EMA 1,609일(3,837일 중)이다. **국내가 하락 국면에 훨씬 오래 머문다.**

#### 강건성 체크 "코스피 하위표본" 판정 — "해당 없음"이 아니다 (2026-08-14)

§6 은 이 항목을 "실측 후 판단"으로 남기며 **KODEX 200 은 지수 ETF 라 ±15%를 넘긴 적이 사실상
없을 것**으로 예상했다. 원본가 5,881일을 세어 보니 예상이 반은 맞고 반은 틀렸다.

| 확인 | 결과 |
| --- | --- |
| 일간 등락 절대값이 15%를 넘은 날 | **1일뿐** — 2026-07-31 (+24.17%) |
| 2015-06-15 **이전** 3,142일의 최대 등락 | **14.97%** |
| 2015-06-15 **이후** 2,739일의 최대 등락 | **24.17%** |

**넘은 날이 1일뿐이라는 사실보다, 확대 이전 최대가 14.97% 라는 사실이 중요하다.** 3,142거래일을
통틀어 가장 큰 등락이 옛 제한폭(±15%)에서 0.03%p 아래에 멈춰 있다. 제한폭이 그 구간의 극단치를
실제로 잘라내고 있었을 가능성이 크다는 뜻이며, 그렇다면 **테스트 A 의 확장창 순위 기준선이
2015년 6월을 경계로 성격이 다르다.** 상장일부터 쌓은 순위의 윗부분이 제도로 눌려 있었기 때문이다.

관측이 한 건뿐이라 "제한폭이 걸렸다"를 단정하지는 않는다. 다만 이 항목을 "해당 없음"으로 지우지 말고
**결과 문서의 한계 섹션에 위 세 수치를 그대로 싣는다.**

### 데이터 소스 실측 — yfinance 자체 수집기 (2026-08-11)

이관본을 버리고 저장소 안의 수집기로 QQQ 를 다시 받아 확인한 결과다. 설치본은 `yfinance 0.2.66`.

| 항목 | 결과 |
| --- | --- |
| 수정주가 | `history()` 의 `auto_adjust` 기본값이 **`True`** 다. 다만 기본값에 기대지 않고 **명시적으로 전달**한다 — 이 값이 바뀌면 분배락일이 인위적 하락으로 잡히고, 그 사고는 소리 없이 일어난다 |
| 오류 전파 | `raise_errors` 기본값이 **`False`** 다. 조회 실패 시 예외 대신 **빈 DataFrame** 이 돌아온다. 명시적으로 `True` 를 넘겨야 실패가 빈 파일로 둔갑하지 않는다 |
| 반환 형태 | 거래소 타임존이 붙은 DatetimeIndex + `Open`·`High`·`Low`·`Close`·`Volume`·`Dividends`·`Stock Splits`. 뒤의 둘은 원시 시세 스키마에 없어 저장하지 않는다 |
| 최근 데이터 지연 | 실행일(2026-08-11) 기준 마지막 제공 행이 **2026-08-10** 이었다. 당일 데이터는 오지 않는다 |
| 수집 결과 | **6,896행, 1999-03-10 ~ 2026-08-07.** 확정 전 구간(최근 2일)을 제외해 1행이 빠졌다 |

**이관본과 자체 수집본의 가격 기준이 같음이 확인됐다.** 재수집 파일 전체로 역대 상위 10위를 다시 뽑았을 때
위 표의 **폭등 10개·폭락 10개 20개 날짜와 등락률이 모두 일치**했고, 최대 일간 변동도
**+16.84% / −11.98%** 로 같다. 신규로 늘어난 10거래일(2026-07-27 ~ 2026-08-07)은 상위 10위 명단을
바꾸지 않았다. 1999-03-10 ~ 1999-12-31 구간에도 상위 10위에 드는 날은 없다.

> 따라서 이 장의 사전 실측 표들은 **재수집 후에도 다시 뽑을 필요가 없다.**
> 집계 시작 시점별 신호 수와 테스트 B 발생 빈도는 측정 계층이 만들어진 뒤 코드가 낸 값으로 갱신한다.

### 데이터 소스 실측 — QQQ 수정주가의 조정 배율 (2026-08-19)

사용자가 `signals.csv` 를 차트와 대조하다 종가 불일치를 발견해 확인한 결과다.
**조정이 상수 배율로 걸린다는 것이 실측으로 확인됐다.**

2020-03-12 에서 차트 종가 **177.32** ÷ 파일 종가 **170.699097** = **배율 1.038787**.
이 배율을 인접 거래일에 적용해 예측한 값이 차트와 모두 일치했다.

| 날짜 | 파일 종가 (수정주가) | 배율 적용 예측 | 차트 실측 |
| --- | --- | --- | --- |
| 2020-03-13 | 185.158295 | 192.34 | 192.34 |
| 2020-03-16 | 162.978577 | 169.30 | 169.30 |
| 2020-03-17 | 175.339111 | 182.14 | 182.14 |

배율 3.88% 는 2020년 3월 이후 지급된 배당의 누적분에 해당한다(QQQ 배당수익률 연 0.5~0.6%).

**따라서 등락률은 조정에 불변이다.** 배율이 분자와 분모에 함께 걸려 약분되기 때문이며,
2020-03-12 등락률이 파일 값·차트 값 양쪽에서 **-9.17%** 로 같았다.
스펙 §2 "가격 처리"가 등락률 기준 판정을 택한 근거가 실측으로 뒷받침됐다.

> **단, 배율은 배당 지급 시점마다 바뀐다.** 며칠 구간에서는 상수지만 1년 구간에는 배당이 네 번
> 끼므로 시작일과 종료일의 배율이 다르다. **수정주가 기준 1년 수익률은 배당 재투자를 포함한 값**이며,
> 차트의 미조정 가격으로 계산한 값보다 배당분만큼 높다. 대조 시 두 소스의 가격을 섞으면
> 그 차이가 오차인지 배당인지 구분할 수 없으므로, **수익률 계산은 저장 파일 안에서만 수행한다.**
> 이 지침은 [HANDS_ON.md](../HANDS_ON.md) 3단계에 절차로 실려 있다.

### 데이터 소스 실측 — yfinance 원본가는 분할 반영·배당 미반영 (2026-08-20)

가격 기준을 원본가로 전환하기 전에 **`auto_adjust=False` 가 분할까지 미조정으로 오는지**를 확인했다.
분할이 미조정이면 1999년 이후 QQQ 분할이 반영되지 않아 과거 가격이 통째로 틀리는데,
**2020년 구간만 보면 이 사고를 잡아내지 못한다** — QQQ 마지막 분할이 2000-03-20 이기 때문이다.

먼저 설치본 소스를 읽어 라이브러리 쪽 가능성을 지웠다. `yfinance 0.2.66` 의
`scrapers/history.py` 에서 가격을 변형하는 곳은 `auto_adjust`/`back_adjust` 둘뿐이고, 그 내용은
`utils.py` 의 `ratio = Adj Close / Close` 스케일링이 전부다. **분할은 `Stock Splits` 이벤트 컬럼으로
붙기만 하고 가격에 곱해지지 않는다.** 따라서 판정은 야후 응답의 성질에 달려 있어 실측이 필요했다.

원본가로 재수집한 파일(6,903행, 1999-03-10 ~ 2026-08-18)을 직전 수정주가 파일과 전 구간 대조했다.

| 확인 | 결과 |
| --- | --- |
| 2020년 4개 지점 | 03-12 **177.32**, 03-13 **192.34**, 03-16 **169.30**, 03-17 **182.14** — 사용자가 차트에서 읽은 값과 일치 |
| **1999-03-10 배율** | 51.0625 ÷ 43.027061 = **1.1868**. 분할 미반영이었다면 2.3~2.5 였을 것이다 |
| **전 기간 배율 범위** | 공통 거래일 6,896일에서 **1.000000 ~ 1.186754**. 2배 근처에 간 적이 없다 |
| **배율이 1.5배 이상 튀는 날** | **0건.** 분할 불연속이 어디에도 없다 |
| 하루 최대 변화 | 0.9905배 (2004-12-17, 분배일) |

배율은 배당이 쌓인 만큼 시간을 거슬러 매끄럽게 커진다 — 2026년 1.000000, 2020년 1.032906,
2010년 1.142777, 1999년 1.186754. 증가 방향으로 어긋나는 날이 3,408건 있으나 **최대 증가폭이 1.3e-06**
으로 소수점 6자리 저장에서 오는 반올림 잡음이며, 1e-4 를 넘는 날은 0건이다.
**이 실측은 가격을 6자리로 저장하던 시점의 것이다** — 같은 날 자릿수가 4자리로 바뀌었으므로(§7 결정 ⑱), 지금 같은 대조를 다시 하면 잡음의 크기가 1e-4 수준으로 커진다.

> **결론: `auto_adjust=False` 는 분할 반영·배당 미반영이다.** 차트와 직접 대조할 수 있고,
> 그 대가로 분배락이 하루짜리 하락으로 남는다.

### 데이터 소스 실측 — yfinance의 KODEX 200은 사용 불가

`yfinance`로 `069500.KS`를 받아 확인한 결과 **분석에 쓸 수 없다.**

| 문제 | 내용 |
| --- | --- |
| 기간 부족 | 2007-01-30부터만 제공 (실제 상장일 2002-10-14) |
| 판정 | 순위 축적 구간이 5년 가까이 잘려 쓸 수 없다. **국내 데이터는 pykrx로 받는다** |

> **정정 (2026-08-11)**: 처음에는 "2026년에 하루 ±8~24% 변동이 수십 회 발생 — 지수 ETF에서 물리적으로
> 불가능한 값"도 사용 불가 근거로 적었으나, **이 근거는 틀렸다.** KRX 원본(pykrx)에서도, 그리고
> KOSPI 200 지수 자체에서도 같은 크기의 변동이 관측된다(아래 "pykrx KODEX 200" 절 결론 3).
> 2026년은 실제로 그런 해였다. **사용 불가 판정은 기간 부족 근거만으로 유지된다.**

### 데이터 소스 실측 — pykrx는 KRX 계정 없이 아무것도 조회하지 못한다 (2026-08-10)

설치된 `pykrx 1.2.8`에서 **함수의 존재 여부와 호출 가능 여부**를 확인한 결과다.

| 항목 | 결과 |
| --- | --- |
| §2가 지정한 `get_etf_ohlcv_by_date` | **존재한다.** 함께 `get_etf_price_deviation`(괴리율), `get_etf_ticker_list`, `get_etf_tracking_error`도 제공된다 |
| 인증 | **필수.** `KRX_ID`/`KRX_PW` 환경 변수가 없으면 `KRX 로그인 실패`로 모든 조회가 실패한다 |
| 자격증명 보관 | `.env` (`.gitignore`에 제외돼 있음을 `git check-ignore`로 확인). **준비 완료** — 키는 `KRX_ID`·`KRX_PW` |

`reference/pykrx_실측기록.md` §0이 개별종목 조회에서 같은 제약을 기록해 뒀고, ETF 함수에서도
동일하게 적용됨을 이 저장소 환경에서 재확인했다.

> **함수가 있다는 것과 반환값이 쓸 만하다는 것은 다른 문제다.** 실제 반환 컬럼 구성과
> 분배락 조정 여부는 아래 "pykrx KODEX 200" 절에서 실측으로 확정했다.

### 데이터 소스 실측 — pykrx KODEX 200 (2026-08-11)

`scripts/data/check_pykrx_etf.py` 를 `069500` / `20021014 ~ 20260811` 로 실행한 결과다.
KRX 를 5회 호출했고 원자료는 `storage/results/` 에 CSV 로 남았다(재생성 가능, git 제외).

#### 함수별 반환

| 함수 | 결과 |
| --- | --- |
| `get_etf_ohlcv_by_date` | **5,881행, 2002-10-14 ~ 2026-08-11.** 상장일부터 전 기간을 한 번에 받는다. 인덱스 `날짜`(`datetime64[ns]`), 컬럼 `NAV`·`시가`·`고가`·`저가`·`종가`·`거래량`·`거래대금`·`기초지수` |
| `get_etf_price_deviation` | 5,881행. 컬럼 `종가`·`NAV`·`괴리율` |
| `get_index_ohlcv_by_date("1028")` | 5,881행. KOSPI 200 가격지수 |
| `get_market_ohlcv(..., adjusted=True)` | **3,000행, 2014-05-21 ~ 2026-08-11.** ETF 티커에도 통하지만 아래 결론 2의 제약이 있다 |
| `get_market_ohlcv(..., adjusted=False)` | **0행 — 실패한다.** `get_stock_ticker_isin: 'NoneType' object is not subscriptable`. ETF 는 주식 ISIN 목록에 없어 원본가 경로가 동작하지 않는다 |

> **dtype 주의**: `get_etf_ohlcv_by_date` 의 가격은 `uint32`, 거래량·거래대금은 `uint64` 다.
> **부호 없는 정수라 뺄셈에서 언더플로우가 난다** — 전일 대비 차분을 정수 그대로 계산하면
> 하락일이 40억 근처의 거대한 양수가 된다. 로딩 시점에 `float64` 로 변환해야 한다.

#### 결론 1 — `get_etf_ohlcv_by_date` 는 원본가다. 분배락이 그대로 남아 있다

두 방향으로 확인했고 서로 일치한다.

| 확인 | 결과 |
| --- | --- |
| 종가와 그날 NAV 의 차이 | 연도별 평균이 **-0.72% ~ -0.01%** 이며 과거로 갈수록 벌어지는 추세가 없다. 종가가 과거를 낮춘 값이라면 2002년 종가가 그날 NAV 보다 수십 % 낮아야 한다. 즉 **그날의 실제 시장 종가**다 |
| 기초지수 대비 초과 하락 | ETF 가 기초지수보다 유독 많이 떨어진 날 상위 20 중 **11일이 4월**이며, 대부분 **4월 27~29일**에 몰려 있다(2003·2004·2007·2008·2011·2016·2017·2019·2020·2021·2022). 그날 ETF 는 기초지수보다 **-1.4 ~ -2.6%p** 더 떨어진다 |

**매년 4월 말에 약 1.5%p 의 인위적 하락이 존재한다.** 이것이 분배락이며 조정돼 있지 않다.
4월 마지막 거래일 자체는 정상(±0.3%p)이므로, 분배락일은 마지막 거래일이 아니라 그 1~2영업일 전이다.

#### 결론 2 — `get_market_ohlcv(adjusted=True)` 는 분배락을 조정하지만 3,000행 상한이 있다

겹치는 3,000일 중 **2,991일의 종가가 다르다.** 배율(수정가 ÷ 원본가)이 단조 증가한다.

| 연도 | 2014 | 2016 | 2018 | 2020 | 2022 | 2024 | 2025 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 배율 중앙값 | 0.795 | 0.820 | 0.849 | 0.888 | 0.924 | 0.963 | 0.986 |

12년간 약 20.5%, **연평균 약 1.7%** 다. 결론 1에서 관측한 4월 분배락 크기(약 1.5%p)와 맞물린다.
서로 다른 두 관측이 같은 값을 가리키므로 **누적 분배금 조정이 맞다.**

**단, 한 번에 3,000행까지만 온다.** 상장일부터가 아니라 2014-05-21부터 시작한다.
구간을 나눠 이어붙여 전 기간을 얻을 수 있는지는 아래 **결론 4** 에서 실측했다 — **얻을 수 없다.**

#### 결론 4 — 수정주가는 **최근 3,000거래일만** 제공된다. 구간을 나눠 이어붙일 수 없다 (2026-08-12)

`scripts/data/check_pykrx_splice.py` 를 `069500` / 시작일 `20021014` / 종료일 `20141231`·`20260812` 로
실행한 결과다. 수정계수는 조회 **시점** 기준이므로 두 호출을 **한 번의 실행 안에서** 수행했다.

| 호출 (시작일은 둘 다 2002-10-14) | 반환 |
| --- | --- |
| 종료일 **2014-12-31** | **150행, 2014-05-22 ~ 2014-12-30** |
| 종료일 **2026-08-12** | **3,000행, 2014-05-22 ~ 2026-08-12** |

**종료일을 12년 앞당겨도 시작일이 2014-05-22 로 같다.** 이것이 결론의 전부다.

요청 구간이 상한을 넘을 때 서버가 **뒤에서 3,000행을 주는 것이 아니라**, 조회 시점 기준
**최근 3,000거래일이라는 고정된 창**과 요청 구간의 교집합을 준다. 종료일을 당기면 창이 과거로
물러나는 것이 아니라 결과만 잘린다(3,000행 → 150행). 창은 하루가 지날 때마다 하루씩 굴러간다 —
2026-08-11 조회는 2014-05-21 부터, 2026-08-12 조회는 2014-05-22 부터 시작했다.

따라서 **분할 호출로 2014-05-22 이전의 수정주가를 얻을 방법이 없다.** 전 기간 5,882거래일 중
**2,882일(2002-10-14 ~ 2014-05-21)이 수정주가로는 존재하지 않는다.**

부수적으로 확인된 것들:

| 항목 | 결과 |
| --- | --- |
| 겹치는 150일의 값 | 시가·고가·저가·종가·거래량·등락률 **여섯 컬럼 전부 일치.** 같은 시점의 두 호출은 같은 가격 축을 쓴다 — 이어붙이기가 실패한 이유는 축이 어긋나서가 아니라 **과거 데이터가 오지 않기 때문**이다 |
| dtype 이 함수마다 다르다 | `get_market_ohlcv` 는 가격·거래량 전부 **`int64`**, `get_etf_ohlcv_by_date` 는 가격 `uint32`·거래량 `uint64` 다. **언더플로우 위험은 후자에만 있다** |
| 두 함수의 거래량이 다르다 | 겹치는 3,000일 중 **2,991일**에서 값이 다르다. 어느 함수로 수집하느냐로 저장되는 거래량이 달라진다 |
| 국내도 당일 데이터가 장중에 나온다 | 14:09(장중) 실행에서 **당일(2026-08-12) 행이 그대로 반환**됐다. 미확정 종가이므로 수집 시 당일은 제외해야 한다 |

#### 결론 3 — 2026년의 극단 등락은 데이터 오류가 아니라 실제 시장 사건이다

ETF 일간 등락 상위 10 중 폭등 6개·폭락 6개가 2026년 날짜다. 이것만 보면 데이터를 의심하게 되지만,
**KOSPI 200 지수 자체가 같은 값을 보인다.**

| 날짜 | ETF 등락 | KOSPI 200 지수 등락 |
| --- | --- | --- |
| 2026-07-31 | +24.17% | **+19.98%** |
| 2026-03-04 | -12.46% | **-11.94%** |
| 2026-07-28 | -11.19% | **-11.55%** |
| 2026-06-23 | -10.47% | **-10.53%** |
| 2026-03-05 | +10.11% | **+9.83%** |

지수 수준 자체가 2025-06 말 414.60 → 2026-06 말 1370.73 (약 3.3배) → 2026-08-11 987.40 으로 움직였다.
ETF 등락과 기초지수 등락의 2026년 상관은 **0.9945** 다.

> **이 발견은 위 "yfinance의 KODEX 200은 사용 불가" 절의 근거 하나를 정정한다.**
> 2026년 ±8~24% 변동은 소스 문제가 아니라 실제로 일어난 일이다.

#### 데이터 품질 (§9 항목 2·3·4)

| 항목 | 결과 |
| --- | --- |
| 결측·거래정지 | 가격 결측 **0건**, 거래량 0인 날 **0일**, 가격 0 이하 **0일**, 중복 날짜 **0건**, 날짜 오름차순 **정상** |
| 괴리율 | 절대값 평균 **0.271%**, 최대 **+3.48%**(2026-07-31). 2%대는 2003~2004년과 2008년에 몰려 있다. **이벤트로 잡힐 만한 극단 괴리는 없다** |
| 초기 유동성 | 거래량 중앙값 최저는 2005~2006년(47만~56만주)이나 **거래대금은 그때도 일 76~94억 원**이다. 가격이 왜곡될 만큼 얇지 않다 — **순위 축적 구간을 잘라낼 이유가 없다** |

#### §9 항목 해소 현황

| # | 항목 | 상태 |
| --- | --- | --- |
| 1 | 분배금 조정 여부 | **해소** — 미조정(결론 1). 대응 방법도 확정됐다 — 원본가 본검증 + 수정주가 대조 병기(§7 결정 ⑨, 근거는 결론 4) |
| 2 | 초기 유동성 | **해소** — 문제 없음 |
| 3 | 괴리율 | **해소** — 문제 없음 |
| 4 | 결측·거래정지일 | **해소** — 없음 |
| 5 | 국내도 사건이 소수인가 | **해소, 단 QQQ와 성격이 다르다** — 사건은 2008·2020·2024-08·2026 으로 QQQ보다 많지만, **2026년 한 해가 상위 10위의 60%를 차지**한다. 게다가 2026년 신호들은 forward return 측정 구간이 아직 끝나지 않았다 |

### 데이터 소스 실측 — KODEX 200 수집 결과 (2026-08-12)

`scripts/data/collect_pykrx.py` 로 당시 확정이던 결정 ⑨ 대로 두 기준을 받은 결과다.
**수정주가는 결정 ⑰ 로 폐기됐고 파일도 지웠다.** 아래는 그 시점의 기록이며, 배율 검증의 근거로 남긴다.

| 파일 | 기준 | 행 수 | 기간 | 최근 제외 |
| --- | --- | --- | --- | --- |
| `storage/market/069500_max.csv` | 원본가 (본검증) | **5,881행** | 2002-10-14 ~ 2026-08-11 | 1행 |
| ~~`storage/market/069500_adjusted_max.csv`~~ (삭제됨) | 수정주가 (대조) | **2,999행** | 2014-05-22 ~ 2026-08-11 | 1행 |

두 파일 모두 `load_market_csv()` 를 예외 없이 통과했다. 저장 dtype 은 가격·거래량 전부 **`int64`**(부호 있는 정수),
날짜는 `YYYY-MM-DD` 이며 중복·결측 0건이다. 제외된 1행은 실행일(2026-08-12) 당일이다.

**두 파일의 배율(수정 종가 ÷ 원본 종가)이 이어붙이기 실측 때와 같은 값을 보인다.**
겹치는 2,999일에서 배율은 2014년 0.795 → 2025년 0.986 → 최근 1.0000 으로 단조 증가하며,
결론 2 가 별도 조회로 측정한 값과 일치한다. 서로 다른 두 실행이 같은 값을 가리키므로 수집 경로가 옳다.

수정계수의 계단(분배락)은 12년간 **28개**다. **최근 수년은 연 1회가 아니라 분기 지급**이며,
4월 말이 가장 크고(약 1.2%p) 나머지 분기는 0.2~0.4%p 다. 결론 1 이 "매년 4월 말"로 관측한 것은
기초지수 대비 초과 하락 상위 20 만 본 결과이며, **작은 분기 분배락이 그 아래에 더 있다.**
원본가 계열의 누적 과소 계상(연 약 1.7%)은 이 전부의 합이다.

### KRX 자격증명을 코드에 연결하는 방법 (구현 시 참고, 2026-08-10)

**`.env`를 만들어 둔 것만으로는 pykrx가 동작하지 않는다.** 설치본 소스를 직접 확인한 결과다.

| 확인한 것 | 결과 |
| --- | --- |
| pykrx가 자격증명을 읽는 방법 | `os.getenv("KRX_ID")` / `os.getenv("KRX_PW")` — **환경 변수만 본다** |
| pykrx가 `.env`를 읽는가 | **읽지 않는다.** 패키지 안에 dotenv 참조가 하나도 없다 |
| `python-dotenv` 설치 여부 | **미설치.** `pyproject.toml` 의존성에도 없다 |
| 세션 만료 | 만료 시 `get_auth_session()`이 환경 변수를 다시 읽어 **자동 재로그인**한다 |

근거 위치는 설치본의 `pykrx/website/comm/auth.py`다.

따라서 구현 시 아래 세 가지가 필요하다.

1. **`python-dotenv`를 의존성에 추가한다.** 지금은 없다
2. **`.env`를 환경 변수로 올리는 로더를 만든다.** 셸 환경 변수보다 `.env`를 우선(`override=True`)해야
   어느 환경에서 실행하든 같은 계정을 쓴다
3. **그 로더를 `pykrx` import보다 먼저 호출한다.** `build_krx_session()`의 기본 인자가
   `login_id: str = os.getenv("KRX_ID")` 형태라 **import 시점에 값이 고정**되기 때문이다.
   `get_auth_session()`은 호출 시점에 다시 읽어 복구하지만, 순서를 지키는 쪽이 안전하다

`reference/pykrx_collect/krx_credentials.py`가 이 세 가지를 이미 만족하는 구현이다.
**읽고 이해한 뒤 새로 작성한다** — reference는 옛 패키지를 참조해 그대로는 동작하지 않는다.

> **로그 주의**: 로그인 성공 시 pykrx가 **로그인 ID를 표준 출력에 찍는다**(비밀번호는 찍지 않는다).
> 실행 로그를 공유하거나 결과 문서에 붙일 때 이 줄이 섞이지 않게 한다.

---

### 데이터 소스 실측 — 가격 자릿수 4자리 전환 (2026-08-20)

§7 결정 ⑱ 의 근거다. 자릿수를 줄이면 판정이 바뀔 수 있으므로 **착수 전에 전 구간으로 먼저 재고**,
전환 뒤 실행 결과로 다시 확인했다.

#### 착수 전 예측 (QQQ 6,903행에 `round(4)` 를 걸어 계산)

| 확인 | 결과 |
| --- | --- |
| 원시 값이 바뀌는 행 | **6,692 / 6,903 (97%)** — yfinance 는 6자리를 거의 다 쓴다 |
| 등락률 최대 절대차 | **1.9e-06** (0.00019%p) |
| 등락률 **부호**가 바뀌는 날 | **0건** → 테스트 B 연속 판정 불변 |
| 등락률이 **정확히 0** 인 날 | 38건 → 38건 (불변) → 결정 ⑧ 영향 없음 |
| **확장창 상위 10위 판정**이 달라지는 날 | **0건** (폭락 65일·폭등 66일 대상) |
| 등락률 표시값(2자리)이 달라지는 날 | **3건** — 2011-02-01, 2012-08-03, 2021-08-26. 셋 다 신호일이 아니다 |
| 등락률 동률쌍 | 51 → **72** 로 는다. 확장창 순위는 동률에 같은 순위를 주므로 신호 수에는 영향이 없다 |

#### 전환 뒤 실행 대조

효과를 분리하려고 **세 실행**을 만들었다. QQQ 재수집은 같은 날 안에 했으므로 컷오프(`오늘 − 2일`)가
같아 **기간이 6,903행 · 1999-03-10 ~ 2026-08-18 로 변하지 않았다** — 자릿수 효과만 남는다.

| 실행 | 저장 자릿수 | 출력 자릿수 | 폴더 |
| --- | --- | --- | --- |
| REF | 6자리 | 6자리 | `20260820_135545_index_extreme` |
| MID | 6자리 | **4자리** | `20260820_162628_index_extreme` |
| NEW | **4자리** | **4자리** | `20260820_162937_index_extreme` |

| 대조 | 결과 |
| --- | --- |
| 산출물 규모 | 셋 다 동일 — `signals` 12,679 / `statistics` 2,976 / `excess` 8,928 / `test` 8,928, 신호군 248개 |
| **REF vs MID** (출력만) | `statistics`·`excess`·`test` 가 **바이트 단위로 완전 일치**하고 `signals.csv` 는 **`종가` 컬럼 하나만** 달라졌다. 출력 자릿수는 표기만 건드리고 측정값에 닿지 않는다 |
| **REF vs NEW — 신호일 집합** | **완전 동일.** 사라진 신호 0건, 새로 생긴 신호 0건 |
| **테스트 A 신호 7건** (K=10, 2003년 시작) | 날짜·등락률·**당시 순위**·사건 번호가 전부 동일. 종가만 `169.300003 → 169.3` 처럼 짧아졌다 |
| `당시 순위` 가 달라진 행 | **61건.** 전부 QQQ 테스트 B 신호이고 고유 날짜 14개, 순위 범위 **589~2,474위**, 변화폭 전부 ±1. 동률이 늘어 꼬리 쪽 번호가 밀린 것이며 **20위 이내는 하나도 바뀌지 않았다** |
| forward return 이 달라진 칸 | **31칸 / 152,148칸 (0.02%).** 차이는 **전부 정확히 ±0.01%p** 로, 백분율 2자리 반올림 경계에서만 뒤집혔다 |
| `statistics.csv` | **달라진 칸 0개 / 56,544개.** 신호군 집계값(평균·중앙값·승률)은 전혀 바뀌지 않았다 |
| `excess.csv` | 3칸 (전부 ±0.01%p). QQQ 테스트 B 뿐이다 |
| `test.csv` | 11칸 — 백분위 ±0.1, p값 ±0.002. QQQ 테스트 B 뿐이다. 베이스라인 모집단이 미세하게 밀린 결과다 |
| **결론 수치** | **완전히 동일.** QQQ 테스트 B·단순 보유 검정 714칸 중 p<0.05 **31칸(4.3%)**, KODEX 200 718칸 중 **41칸(5.7%)**, 전체 검정 칸 5,130개 |

> **설명되지 않는 변화는 0건이다.** 수익률이 달라진 칸 중 두 개가 이를 잘 보여 준다 —
> 2012-08-02 의 `종가 1일` 은 **2012-08-03** 종가를, 2021-08-25 의 `종가 1일` 은 **2021-08-26** 종가를
> 출구로 쓰는데, 그 두 날이 바로 착수 전 예측이 짚은 "등락률 표시값이 달라지는 3건"에 들어 있다.

#### 함께 확인된 것 — 반올림은 짝수 쪽으로 간다

정확히 절반인 값(`...5`)에서 numpy·pandas 는 **짝수 쪽으로 반올림**한다.

```
51.15625 → 51.1562   (51.1563 이 아니다)
50.28125 → 50.2812
```

1999~2000년 QQQ 는 1/32 틱(0.03125 단위)이라 `...5` 로 끝나는 값이 많아 이 경우가 자주 나온다.
위의 실측은 모두 같은 `round(4)` 로 계산했으므로 이 동작을 이미 포함한 값이다.

#### 저장 결과

파일 전체에 **소수 5자리 이상이 0건**이고, 2020-03 구간이 차트 값과 그대로 일치한다.

```
2020-03-12,182.09,190.78,176.94,177.32,137820700
2020-03-13,187.07,194.48,178.13,192.34,134882000
2020-03-16,174.15,184.68,169.16,169.3,92054400
2020-03-17,175.47,184.39,169.78,182.14,121257100
```

**끝자리 0 은 적지 않는다** — `466.0`·`169.3`·`30.6` 처럼 나온다. 결과 문서와 대조 가이드의 표기도
이 값에 맞춘다.


### 데이터 소스 실측 — `가격기준` 컬럼 제거 대조 (2026-08-20)

§7 결정 ⑲ 의 확인이다. 컬럼을 빼면서 값이 함께 바뀌지 않았는지를 재실행으로 대조했다.
같은 시세·같은 시드이므로 **컬럼 하나가 빠진 것 외에는 아무것도 달라지지 않아야 한다.**

| 대조 (`20260820_162937` → `20260820_170440`) | 결과 |
| --- | --- |
| 컬럼 구성 | `signals` 25 → **24**, `statistics` 19 → **18**, `excess` 17 → **16**, `test` 22 → **21**. 빠진 컬럼은 네 파일 모두 `가격기준` 하나뿐이고 새로 생긴 컬럼은 없다 |
| 행 수 | 12,679 / 2,976 / 8,928 / 8,928 — **전부 동일** |
| **남은 컬럼의 값** | 688,200칸을 통째로 비교해 **달라진 칸 0개** |
| 신호군 | 248개, 신호 0건 16개 — 동일 |
| `summary.json` `datasets` | `price_basis = 원본가` 가 **두 데이터셋 모두 남아 있다** |
| `summary.json` `empty_signal_groups` | 키가 `ticker·price_basis·test·…` → `ticker·test·…` 로 바뀌었다. 식별 dict 에서 파생되므로 함께 빠진다 |
| 터미널 "검증 대상 시세" 표 | `가격기준` 열이 **그대로 나온다** |
| 터미널 발췌 표 | `가격기준` 열이 **빠졌다** — CSV 와 컬럼 구성이 일치한다 |

> **`summary.json` 의 `populations` 섹션에는 `price_basis` 가 남아 있다.** 조합 단위 기록이라
> `empty_signal_groups` 와 성격이 같은데 한쪽만 상수를 담고 있다. 이번 변경의 범위 밖이라 그대로 두었으며,
> 조합 단위 기록끼리 맞추려면 여기서도 빼는 것이 일관된다.


## 9. 착수 전 실측이 필요한 항목

아래는 아직 확인되지 않았다. Phase 1에서 실측하고 그 결과를 이 문서에 추가한다.
**결과에 따라 순위 축적 구간 조정이 필요하다고 판단되면 먼저 사용자에게 보고하고 판단을 받는다.
임의로 데이터를 잘라내지 않는다.**

1. **KODEX 200 분배금 조정 여부** — `get_etf_ohlcv_by_date` 반환값이 분배락 조정된 값인가.
   조정되지 않았다면 조정 방법과 그 근거를 문서에 남긴다
2. **초기 유동성** — 2002~2008년 구간의 일별 거래량·거래대금 추이. 거래가 극히 적은 구간의 가격 변동은
   실제 시장 움직임이 아닐 수 있고, 순위 축적 구간을 오염시킨다
3. **괴리율(NAV 대비 시장가)** — 극단적 괴리가 발생한 날이 이벤트로 잡히는지 확인
4. **결측·거래정지일** 유무
5. **국내도 "사건이 소수" 문제가 있는가** — 국내는 2008·2011·2020·2024-08 등 사건이 더 많을 가능성이 있다

---

## 10. 명시적 제외 항목

아래는 이번 검증에 **포함하지 않는다.**

- 쿨다운 처리 (같은 충격의 연쇄를 별개 신호로 센다. 대신 사건 번호로 병기한다 — §7 결정 ③)
- 서킷브레이커·사이드카 보정 (자연스러운 시장 흐름으로 간주)
- 교차검증 / out-of-sample 분리 (구간별 일관성 체크로 대체)
- 코스닥
- 코로나 구간 제외 실험 (정상적인 시장 이벤트이므로 포함. 대신 중앙값과 상위 기여 사건으로 영향을 확인)

---

## 11. 부가 출력 (필수)

- **신호일 전체 목록**: 날짜, 등락률(%), 판정 시점 순위, 사건 번호, 참고용 z-score(직전 60일 변동성 대비)
- **연도별 신호 발생 분포**: 특정 시기에 몰려 있는지 확인 가능해야 함
- **상위 기여 사건**: 결과 평균을 크게 좌우한 상위 3~5개 사건과 그 기여도

> z-score는 **판정 기준이 아니라 해석 보조용 참고 컬럼**이다. 이벤트 판정은 순위 기준으로만 한다.

---

## 12. 보고 형식

결과 문서(`docs/research/RESEARCH_index_extreme_events.md`)에 아래를 모두 담는다.
각 표는 QQQ와 KODEX 200을 별도로 제시한다.

1. 테스트 A 결과표 — 순위 컷별 × 측정 구간별 (평균/중앙값/승률/신호 수/사건 수)
2. 테스트 B 결과표 — N별 × 측정 구간별 (동일)
3. 베이스라인 대비 초과수익표 — 3종 베이스라인 각각
4. 구간별 비교표 — 2010년대 / 2020년대
5. 신호일 전체 목록 및 연도별 분포
6. 강건성 체크 결과
7. **한계 섹션 (필수)** — 표본 수 제약, 사후 파라미터 결정, 데이터 품질, 세금·거래비용·슬리피지 미반영,
   KODEX 200 순위 축적 구간이 QQQ보다 짧은 점, ETF 괴리율·유동성 이슈 등
