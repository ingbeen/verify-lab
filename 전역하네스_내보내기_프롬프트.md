# 전역 하네스 내보내기 프롬프트

> **목적**: 실행한 PC 의 전역 하네스(`~/.claude/`)를 통째로 번들 파일 하나에 담습니다.
> **짝 문서**: `전역하네스_설치_프롬프트.md` (반대편 PC 에서 이어서 실행)
> **두 프롬프트 외에 실행할 것은 없습니다.** 백업 경로·롤백 절차·중단 조건이 모두 안에 들어 있습니다.
>
> **어느 PC 에서 실행해도 됩니다.** 처음에는 하네스가 갖춰진 PC(개인)에서 실행합니다.

---

## 왜 필요한가

**같은 저장소인데 PC 마다 결과가 다릅니다.** 원인은 구조적입니다 — 저장소는 git 으로 동기화되지만
**`~/.claude/` 는 동기화되지 않습니다.**

2026-08-29 커밋 `7165f95` 「계획서 스킬을 전역 impl-plan 으로 이관하고 레포 복제본을 제거」가
그 실물 사례입니다. 이관은 ① 전역에 배치 ② 레포 복제본 삭제 두 단계인데, **②만 git 을 타고 넘어왔습니다.**
그 결과 한쪽 PC 에서는:

- 계획서 선행 게이트가 걸리지 않습니다 (계획서 없이 코드를 고쳐도 프롬프트가 안 뜸)
- Done 조건 위반 저장 차단(`plan_lint`)이 없습니다
- `/impl-plan` 스킬을 호출할 수 없습니다

**목표는 두 PC 의 하네스를 같게 만드는 것**이고, 같아질 수 없는 것은 **명시적으로 남기는 것**입니다.

## 무엇을 담는가 — 「하네스 표면」 5가지

| 담는다 | 왜 |
| --- | --- |
| `~/.claude/CLAUDE.md` | 전역 작업 규칙. 모델의 판단 기준이 여기서 갈린다 |
| `~/.claude/settings.json` | 훅 등록·권한·환경변수 |
| `~/.claude/skills/**` | 절차 스킬 |
| `~/.claude/hooks/**` | 강제 계층 |
| `~/.claude/commands/**` | 슬래시 커맨드 |

| 담지 않는다 | 왜 |
| --- | --- |
| `sessions/` · `projects/` · `history.jsonl` · `file-history/` · `shell-snapshots/` | 세션 이력. 기계 상태이고 PC 마다 달라야 정상 |
| `cache/` · `db/` · `daemon/` · `ide/` · `session-env/` · `paste-cache/` | 런타임 캐시 |
| `plugins/` | 마켓플레이스 복제본. 용량이 크고 다시 받으면 된다 |
| `backups/` | 백업 자체 |

> 참고: 업무 PC 기준 하네스 표면은 15개 파일 · 약 84KB 입니다. 번들은 그 정도 크기가 됩니다.

---

## 붙여넣을 프롬프트

아래 `---- 프롬프트 시작 ----` 부터 `---- 프롬프트 끝 ----` 까지를 복사해서
**하네스가 갖춰진 PC** 의 Claude Code 세션(verify-lab 저장소)에 붙여넣으세요.

---- 프롬프트 시작 ----

verify-lab 저장소 루트에서 작업해줘. 이 PC 의 전역 하네스(`~/.claude/`)를 읽기 전용으로 조사해
저장소에 번들 파일 하나로 담아줘. 반대편 PC 의 하네스를 이 PC 와 같게 만들기 위한 것이다.

### 지켜야 할 것

1. **읽기 전용이다.** `~/.claude/` 아래 어떤 파일도 수정·삭제·이동하지 않는다.
   저장소 루트에 번들 파일 하나를 새로 쓰는 것만 허용된다.
   **백업은 필요 없다** — 이 PC 에서는 아무것도 바뀌지 않는다.
2. **없는 것을 지어내지 않는다.** 파일이 없으면 없다고 적는다.
3. **원문을 가공하지 않는다.** 요약·정리·재작성을 하지 않는다. 바이트 그대로 옮긴다.
4. **커밋·푸시는 하지 않는다.** 사용자가 직접 한다.

### 0단계 — 환경 진단

```bash
cd <verify-lab 저장소 루트> && test -d .git && echo "저장소 OK"
python3 --version || python --version
TZ=Asia/Seoul date '+%Y-%m-%d %H:%M'
```

- [ ] 저장소 루트다 · python 이 있다 (훅 자체가 python 이라 없으면 절차가 성립하지 않는다)

### 1단계 — 번들 생성

아래 스크립트를 임시 파일로 저장하고 **저장소 루트에서** 실행해라.
손으로 번들을 조립하지 마라 — 사람이 옮겨 적으면 그 자리에서 오타가 섞인다.

```python
#!/usr/bin/env python3
"""이 PC 의 전역 하네스(~/.claude/)를 읽기 전용으로 조사해 번들 마크다운 하나로 내보낸다."""

import hashlib
import json
import platform
import sys
from datetime import datetime, timedelta, timezone
from pathlib import Path

HOME = Path.home()
CLAUDE = HOME / ".claude"
OUT = Path.cwd() / "전역하네스_번들.md"
KST = timezone(timedelta(hours=9))

# 하네스 표면. 이것 말고는 담지 않는다 (세션 이력·캐시·플러그인은 PC 마다 달라야 정상).
SINGLE = ["CLAUDE.md", "settings.json"]
TREES = ["skills", "hooks", "commands"]
SKIP_PARTS = {"__pycache__", ".DS_Store"}
SKIP_SUFFIX = (".pyc", ".pyo")

# OS 마다 달라야 정상인 명령. 설치 쪽에서 이 PC 값을 유지할지 판단하는 근거가 된다.
OS_TOKENS = ("terminal-notifier", "wsl_toast", "notify-send", "osascript", "powershell")


def digest(text: str) -> str:
    """본문의 sha256 을 구한다."""
    return hashlib.sha256(text.encode("utf-8")).hexdigest()


def collect() -> list[Path]:
    """하네스 표면에 해당하는 파일을 모은다."""
    found: list[Path] = []
    for name in SINGLE:
        found.append(CLAUDE / name)
    for tree in TREES:
        root = CLAUDE / tree
        if not root.is_dir():
            continue
        for path in sorted(root.rglob("*")):
            if not path.is_file():
                continue
            if SKIP_PARTS & set(path.parts) or path.name.endswith(SKIP_SUFFIX):
                continue
            found.append(path)
    return found


def emit_file(path: Path) -> list[str]:
    """파일 하나를 마커로 감싼 블록으로 만든다. 없으면 없음 블록을 만든다."""
    rel = path.relative_to(HOME).as_posix()
    if not path.is_file():
        return [f"===== MISSING FILE: ~/{rel} =====", ""]
    text = path.read_text(encoding="utf-8", errors="replace")
    lines = text.count("\n") + (0 if text.endswith("\n") else 1)
    size = len(text.encode("utf-8"))
    mode = oct(path.stat().st_mode & 0o777)
    return [
        f"===== BEGIN FILE: ~/{rel} (sha256={digest(text)} lines={lines} bytes={size} mode={mode}) =====",
        text.rstrip("\n"),
        f"===== END FILE: ~/{rel} =====",
        "",
    ]


def hook_inventory() -> list[str]:
    """settings.json 의 훅 등록을 이벤트·매처·명령으로 펼친다. OS 의존 항목을 표시한다."""
    settings = CLAUDE / "settings.json"
    if not settings.is_file():
        return ["| (settings.json 없음) | | | |"]
    try:
        data = json.loads(settings.read_text(encoding="utf-8"))
    except json.JSONDecodeError as exc:
        return [f"| (파싱 실패: {exc}) | | | |"]
    rows: list[str] = []
    for event, groups in (data.get("hooks") or {}).items():
        for group in groups:
            matcher = str(group.get("matcher", "(매처 없음)"))
            for hook in group.get("hooks", []):
                command = str(hook.get("command", ""))
                mark = "OS 의존" if any(t in command for t in OS_TOKENS) else ""
                head = command[:90].replace("|", "\\|").replace("\n", " ")
                rows.append(f"| {event} | {matcher} | `{head}` | {mark} |")
    return rows or ["| (등록된 훅 없음) | | | |"]


def main() -> int:
    """번들 마크다운을 만든다."""
    files = collect()
    present = [p for p in files if p.is_file()]
    missing = [p for p in files if not p.is_file()]
    now = datetime.now(KST).strftime("%Y-%m-%d %H:%M")

    out: list[str] = [
        "# 전역 하네스 번들",
        "",
        f"> **생성 시각(KST)**: {now}",
        f"> **생성한 PC**: {platform.node()} / {platform.system()} {platform.release()} / python {sys.version.split()[0]}",
        f"> **홈 경로**: {HOME}",
        f"> **담은 파일**: {len(present)}개 (누락 {len(missing)}개)",
        "> **기계가 만든 산출물이다.** 손으로 고치지 않는다 - sha256 이 어긋나면 설치가 중단된다.",
        "",
        "---",
        "",
        "## 1. 파일 목록",
        "",
        "| 경로 | 바이트 | 줄 |",
        "| --- | ---: | ---: |",
    ]
    for path in present:
        text = path.read_text(encoding="utf-8", errors="replace")
        out.append(
            f"| ~/{path.relative_to(HOME).as_posix()} | {len(text.encode('utf-8'))} | {text.count(chr(10))} |"
        )
    for path in missing:
        out.append(f"| ~/{path.relative_to(HOME).as_posix()} | **없음** | |")

    out += [
        "",
        "## 2. settings.json 의 훅 등록 (펼친 형태)",
        "",
        "`OS 의존` 으로 표시된 항목은 PC 마다 달라야 정상이다. 설치 쪽에서 이 PC 값을 유지한다.",
        "",
        "| 이벤트 | 매처 | command (앞 90자) | 비고 |",
        "| --- | --- | --- | --- |",
        *hook_inventory(),
        "",
        "---",
        "",
        "## 3. 원문 파일",
        "",
    ]
    for path in files:
        out += emit_file(path)

    OUT.write_text("\n".join(out) + "\n", encoding="utf-8")
    body = OUT.read_text(encoding="utf-8")
    print(f"작성 완료: {OUT}")
    print(f"  담은 파일 {len(present)}개 / 누락 {len(missing)}개")
    print(f"  번들 크기 {len(body.encode('utf-8'))}B / {body.count(chr(10))}줄")
    for path in present:
        print(f"  O  ~/{path.relative_to(HOME).as_posix()}")
    for path in missing:
        print(f"  X  ~/{path.relative_to(HOME).as_posix()}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
```

### 2단계 — 실행 후 보고

1. **스크립트 출력을 그대로 보고해라.** `X`(누락)가 있으면 감추지 말고 맨 앞에 적어라.
2. 번들 **1장(파일 목록)과 2장(훅 등록)** 을 사람이 읽을 수 있게 정리해라.
   3장 원문은 요약하지 말고 그대로 둔다.
3. 아래를 추가로 확인해 보고해라. 번들에 담기지 않지만 **PC 마다 결과를 가르는 것들**이다.
   - `cat ~/.config/git/ignore 2>/dev/null` — 전역 gitignore. 이것 때문에 한쪽에서만 파일이 안 보일 수 있다
   - `ls ~/.claude/plugins/marketplaces/ 2>/dev/null` — 설치된 플러그인 마켓플레이스
   - `python3 --version` · `poetry --version` · `git --version`
4. **커밋하지 마라.** 사용자에게 `전역하네스_번들.md` 의 크기·줄 수와 함께
   "커밋·푸시하면 된다"고 알려라.

### 하지 말 것

- `~/.claude/` 아래 파일 수정·삭제
- 번들 내용을 손으로 편집 (sha256 이 깨져 설치가 중단된다)
- 없는 파일을 「원래 이런 내용이었을 것」으로 재구성
- 저장소의 다른 파일 수정, git 커밋·푸시

---- 프롬프트 끝 ----

---

## 이 뒤에 사용자가 할 일

1. `전역하네스_번들.md` **커밋·푸시**
2. 반대편 PC 에서 **풀**
3. 그 PC 의 Claude 세션에 `전역하네스_설치_프롬프트.md` 를 붙여넣기
4. 설치가 만들어 준 **역방향 번들**(`전역하네스_번들_역방향.md`)이 있으면 커밋·푸시하고,
   이 PC 로 돌아와 다시 `전역하네스_설치_프롬프트.md` 를 실행 — **여기서 두 PC 가 같아집니다**

> **번들이 만들어지지 않았다면**(= 이 PC 에도 하네스가 없었다면) 계획서 하네스는
> 커밋 `7165f95^` 에서 재생성할 수 있습니다. 범용화 항목은
> `docs/plans/PLAN_global_impl_plan_skill.md` Phase 1 에 적혀 있습니다.
