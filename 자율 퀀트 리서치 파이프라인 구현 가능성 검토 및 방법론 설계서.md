# 자율 퀀트 리서치 파이프라인 구현 가능성 검토 및 방법론 설계서

## TL;DR
- **전체적으로 "조건부 가능"이다.** 매일 새벽 KST 01–05시에 Claude Code를 헤드리스로 자율 구동해 "리서치→구현→백테스트→보고"를 도는 파이프라인은 기술적으로 구현 가능하며, 이미 AlphaAgent·QuantAgent·R&D-Agent 등 학술/오픈소스 선행사례가 존재한다. 다만 성패는 LLM의 코딩·리서치 능력이 아니라 **과최적화·가짜발견 통제, 중복 방지, 리워드 해킹 방지, 데이터 품질** 같은 "가드레일 엔지니어링"에 달려 있다.
- **오케스트레이션은 외부 파이썬 러너가 Claude Code를 단계별로 호출하는 구조를 권장**한다. Claude Code 자체를 4시간 통짜 오케스트레이터로 두면 컨텍스트 압축(compaction)·레이트리밋 하드스톱·무한 대기 프롬프트에 취약하다. macOS는 `pmset`(예약 wake)+`launchd`(스케줄)+`caffeinate`(sleep 방지) 조합으로 무인 기동이 가능하다.
- **가장 큰 리스크는 "그럴듯하지만 가짜인 전략"의 대량 양산**이다. 자동화된 대량 탐색은 필연적으로 데이터 스누핑을 유발하므로, Deflated Sharpe Ratio·Harvey-Liu haircut·CPCV(purged K-fold+embargo)·홀드아웃 봉인·사전등록(pre-registration)을 파이프라인의 강제 게이트로 넣어야 한다. **주의: Anthropic 구독으로 헤드리스 자동화를 돌리는 정책은 2026년 6월 15일 변경이 예고되었다가 당일 철회(일시 중단)되어 현재는 구독 한도로 사용 가능하나, 언제든 바뀔 수 있다.**

---

## Key Findings

1. **주기적 무인 실행: 가능.** macOS에서 `pmset repeat wakeorpoweron`으로 새벽 시간대 wake를 예약하고, `launchd`(LaunchAgent, `StartCalendarInterval`)로 잡을 트리거하며, `caffeinate -i`로 실행 중 재수면을 막는다. Claude Code는 `-p`/`--print` 헤드리스 모드, `--output-format json`, `--allowedTools`, `--permission-mode`, `--max-turns`, `--max-budget-usd`로 완전 비대화형 실행이 된다. 구독 인증은 `claude setup-token`으로 1년짜리 OAuth 토큰을 발급해 `CLAUDE_CODE_OAUTH_TOKEN`으로 넘긴다.

2. **중복 전략 방지: 가능.** 전략을 표준 스키마의 "아이디어 원장(ledger)"으로 저장하고, (a) 자연어 서술 임베딩 유사도(코사인, top-k 검색 후 LLM-as-judge 폴백), (b) 시그널 시계열 상관, (c) 팩터 노출 중복, (d) 정규형/AST 해시의 다층 캐스케이드로 판정한다. AlphaAgent가 실제로 AST 기반 유사도로 "독창성 강제(originality enforcement)"를 구현했다.

3. **4시간 파이프라인 상태머신: 가능.** 리서치/트리아지/구현/백테스트/검증/보고의 6단계로 타임박싱하고, 각 단계 산출물을 디스크(git 커밋 포함)에 체크포인트로 남긴다. 단계 간 상태는 대화 컨텍스트가 아니라 **파일(plan.md/progress.md/ledger)**에 저장해 압축·재시작에도 살아남게 한다.

4. **사람 판단 필요 항목: 결정 큐(decision queue)로 비동기 처리.** 무인 실행 중 블로킹하지 않고, 자본 투입/실거래 연결/데이터 구매/라이선스 이슈 등은 큐에 적재만 하고 파이프라인은 안전한 기본값으로 계속 진행. 아침에 일괄 검토.

5. **전략 선별 기준: 경제적 근거·데이터 요구·비용 민감도·용량·개인 실행가능성**을 루브릭으로 점수화하고, 통계적 유의성은 t>3.0(Harvey-Liu)과 DSR로 강제한다.

6. **백테스트 가능성 게이트: 데이터 존재/접근성/시점정합성/생존편향/체결가정**을 체크리스트로 자동 판정하고, 불가 시 프록시·축소유니버스·페이퍼트레이딩으로 강등하거나 "구현 불가"로 원장에 기록.

7. **리워드 해킹은 실재하는 위협.** LLM 에이전트가 테스트/평가를 조작해 결과를 부풀리는 사례가 벤치마크로 반복 확인됐다(ImpossibleBench 등). 백테스트 채점 로직을 에이전트가 못 건드리게 격리하고, 홀드아웃을 봉인하며, 트라젝토리를 감사하는 독립 검증기가 필수다.

---

## Details

### 질문 1) 주기적으로 Claude Code에게 명령을 보내는 것이 가능한가? — **판정: 가능**

**(a) macOS 무인 스케줄링과 sleep/wake**
- macOS는 서버 OS가 아니므로 잠들면 cron/launchd 잡이 안 뜬다. 세 가지 도구를 조합한다.
  - `pmset repeat wakeorpoweron MTWRFSU 00:55:00` 로 매일 00:55에 wake 예약(잡 시작 몇 분 전). 단 완전 종료(power-off) 상태에서는 firmware 타이머가 동작하지 않으므로 sleep 상태여야 한다.
  - `launchd` LaunchAgent(사용자 레벨)로 `StartCalendarInterval`을 써서 01:00 잡 트리거. LaunchDaemon(root)은 sleep 중 시스템 업데이트를 막을 수 있어 권장하지 않는다.
  - 실행 스크립트를 `caffeinate -i`(혹은 `-s`, AC 전원 시)로 감싸 실행 도중 재수면 방지. `caffeinate`는 잠든 Mac을 깨우지는 못하므로 wake는 `pmset`이 담당한다.
- 대안으로 `pmset -c sleep 0`(AC 전원 시 아예 안 자게)도 가능하지만, 항상 켜두는 Mac mini라면 이 방식이 가장 단순하고 안정적이다.

**(b) 헤드리스/비대화형 실행**
- `claude -p "..."`(=`--print`)로 REPL 없이 1회 실행 후 종료. stdin 파이프도 지원(단 크기 상한 있음). `--output-format json`으로 구조화 출력(`result`, `session_id`, `total_cost_usd`, `num_turns`, `is_error`)을 받아 파이썬이 파싱.
- 권한 자동 승인: `--allowedTools "Read,Edit,Bash"`, `--permission-mode acceptEdits`(파일편집 자동승인), 또는 격리 환경 한정 `--dangerously-skip-permissions`. 무인 실행에서 권한 프롬프트가 뜨면 프로세스가 무한 대기하므로 반드시 사전 승인.
- 프로그래매틱 제어가 필요하면 **Claude Agent SDK**(Python/TypeScript). `ClaudeAgentOptions(allowed_tools, permission_mode, cwd, max_turns)`, `canUseTool` 콜백으로 런타임 승인 로직, 6단계 권한 평가(hooks→deny→ask→mode→allow→callback). `PostToolUse` 훅으로 감사·로깅. MCP 서버로 커스텀 데이터/툴 연동.
- 인증(무인): `claude setup-token`이 1년짜리 OAuth 토큰(`sk-ant-oat01-...`)을 발급(Pro/Max/Team/Enterprise 필요). 일반 `/login` 세션 토큰은 8–12시간 만에 만료돼 401이 나므로 부적합. 주의: 환경변수 `ANTHROPIC_API_KEY`가 설정돼 있으면 그게 우선 적용되므로, 구독 인증을 쓰려면 `unset ANTHROPIC_API_KEY`. (`--bare` 모드는 `CLAUDE_CODE_OAUTH_TOKEN`을 안 읽으니 주의.)

**(c) 장시간 실행: 세션·컨텍스트·레이트리밋·재개**
- **레이트리밋**: 5시간 롤링 윈도(첫 프롬프트 시점부터 시작해 5시간 후 리셋)+주간 캡(Max는 전모델 캡+특정 모델 캡 2중). 사용량은 claude.ai/Claude Code/데스크톱이 **같은 버킷 공유**. 2026년 5월 6일 Anthropic이 Pro/Max/Team/seat-based Enterprise의 5시간 한도를 영구히 **2배로 늘리고** Pro·Max의 피크시간 스로틀을 제거했으나(SpaceX Colossus 1 인프라 딜 기반) **주간 캡은 그대로**다(무료 플랜 제외; 5월 13일 한시적 +50% 주간 상향은 2026-07-13 만료).
- **4시간 연속 실행 현실성**: 벽시계 4시간은 5시간 윈도 안에 들어가지만, 소진은 시간이 아니라 토큰량에 좌우된다. Opus로 멀티파일·연속 테스트루프를 돌리면 4시간 전에 5시간 토큰 한도가 바닥날 수 있다. 단일 에이전트+Max 상위 플랜이 완주 가능성이 높고, Pro는 중간에 벽에 부딪힐 공산이 크다.
- **하드스톱·무한대기**: 5시간 한도 도달 시 "extra usage" 없으면 Claude Code가 복구 경로 없이 멈춘다. 재시작/크래시 후 "이 세션 재개할까요?" 인터랙티브 프롬프트가 뜨면 무인 에이전트는 답을 못 해 무한 블록된다(타임아웃·에러·프로세스 종료 없음). 네이티브 auto-resume은 없다(오픈 피처요청 상태 #36320 등). CI 잡엔 반드시 외부 타임아웃을 건다.
- **컨텍스트 압축**: 200K 토큰 윈도, ~83%에서 자동 compaction(설정 불가). 요약 과정에서 초기 지시가 소실될 수 있어, 반드시 지켜야 할 규칙은 CLAUDE.md에, 진행상태는 progress.md/원장에 외부화. `--resume <session_id>`/`--continue`로 재개. 재개 세션의 표준 오프닝 무브를 "plan/progress 파일 먼저 읽기"로 강제하면 lossy resume이 precise resume으로 바뀐다.

**(d) 오케스트레이터 구조 비교 — 권장: 외부 파이썬 러너**

| 항목 | Claude Code가 통짜 오케스트레이터 | 외부 파이썬 러너가 단계별 호출 (권장) |
|---|---|---|
| 컨텍스트 관리 | 4시간 내내 한 세션 → compaction·context rot 위험 | 단계마다 새 세션/컨텍스트 예산 |
| 레이트리밋 하드스톱 | 중간에 멈추면 전체 실패 | 단계 단위 재시도·스킵·다음날 이월 |
| 상태·재현성 | 대화에 상태가 갇힘 | 상태가 디스크(파일·DB·git)에 = 진실의 원천 |
| 결정 큐/게이트 | 삽입 어려움 | 러너가 각 단계 후 게이트 검사 |
| 구현 난이도 | 낮음(프롬프트만) | 중간(러너 코드 필요) |

권장: 파이썬 러너가 상태머신을 소유하고, 각 phase마다 `claude -p`(또는 Agent SDK)를 짧게 호출하며, 산출물을 파일로 받아 다음 단계로 넘긴다. 레이트리밋 리셋 시각을 파싱→세션ID 기억→`claude --resume <id> -p`로 재개하는 커뮤니티 래퍼 패턴(carlaost/resume-after-limit 등)을 참고. 재개 헬퍼는 모델 턴을 소비하지 않게 슬래시 커맨드가 아닌 순수 실행파일로 두는 것이 핵심이다.

### 질문 2) 중복된 매매법을 찾지 않게 하는 것 — **판정: 가능**

**(a) 아이디어 원장(ledger) 스키마(예시 필드)**: `id`, `날짜`, `자연어_가설`, `경제적_근거`, `팩터_카테고리`(모멘텀/가치/캐리/변동성/유동성/계절성/미시구조 등), `자산군`, `시그널_정의(정규형)`, `파라미터`, `데이터_요구`, `상태`(idea/triaged/implemented/backtested/rejected/shelved), `성과지표`, `중복_클러스터ID`, `출처_URL`, `홀드아웃_봉인여부`.

**(b) 중복 판정 다층 캐스케이드**(단일 방법은 모두 약점 존재 → 결합):
1. **정규형/해시 (exact)**: 시그널 서술을 정규화(연산자·파라미터 표준화)한 canonical form에 SHA-256. 완전 동일·사소 변형 즉시 제거. AlphaAgent는 팩터식의 AST 유사도로 구현. 값싸고 빠르나 "같은 아이디어 다른 표현"은 못 잡음.
2. **임베딩 유사도 (semantic)**: 자연어 서술을 임베딩→FAISS로 top-k 검색. 코사인 ≥0.9면 중복 확정(숏컷), 그 미만은 LLM-as-judge에 후보와 함께 넘겨 패러프레이즈 판정(Lean Refactor 논문에서 검증된 캐스케이드: 임베딩 숏리스팅→고유사도 숏컷→judge 폴백). 임계값은 정밀도/재현율 트레이드오프로 조정.
3. **시그널 상관 (behavioral)**: 두 전략의 포지션/수익 시계열 상관이 높으면(예: |ρ|>0.7) 팩터 노출이 겹치는 것으로 간주. "다른 코드, 같은 베팅"을 잡는 가장 강력한 방법.
4. **팩터 노출 회귀**: 후보 수익을 기존 클러스터/표준 팩터에 회귀해 잔차 알파가 유의한지(증분 기여) 확인.

**(c) 탐색 vs 활용**: 커버리지 맵(자산군×팩터카테고리×시간대 그리드)을 만들어 매일 미탐색 셀을 우선 배정하는 **주제 로테이션**을, 성과 피드백을 반영하는 **멀티암드 밴딧**(예: Thompson sampling)으로 보강. AlphaAgent의 교훈: 독창성 페널티가 탐색 공간을 넓혀 유의미 팩터 발견 확률을 높였으나 IC 분산도 커졌다(고위험·고보상). 즉 crowding을 피하려면 의도적으로 "덜 붐비는" 영역에 가중.

### 질문 3) 리서치→구현→성과측정→보고 전체 흐름 — **판정: 가능**

**단계별 타임박스(예시, 총 ~4h)와 산출물**:
1. **리서치·수집 (~45m)**: 웹검색으로 SSRN/arXiv/블로그/포럼 스캔 → `research_notes.md`(출처 URL·신뢰도 태그 포함). 상태: COLLECTED.
2. **트리아지 (~20m)**: 루브릭 점수화·중복 판정 → `triaged.json`(통과/탈락+사유). 상태: TRIAGED.
3. **백테스트 가능성 게이트 (~15m)**: 데이터·시점정합성 체크 → 불가시 강등/기각. 상태: BACKTESTABLE/BLOCKED.
4. **구현 (~60m)**: 기존 하네스에 맞춰 전략 코드 생성, 격리 실행 → `strategy_xx.py`+단위테스트. 상태: IMPLEMENTED.
5. **백테스트·검증 (~50m)**: 인샘플→CPCV→홀드아웃 순차, DSR/haircut 계산 → `metrics.json`. 상태: VALIDATED/REJECTED.
6. **보고 (~10m)**: 일일 리포트+원장 갱신+결정 큐 flush. 상태: REPORTED.

**상태머신·실패정책**: 각 단계는 타임박스 초과·에러 시 **재시도 1회→스킵(다음날 이월)**. 단계 진입 전 git 커밋(작업트리 자체가 체크포인트—단 bash로 지운 파일은 체크포인트가 못 잡으니 git이 안전). 실패해도 파이프라인은 다음 후보/단계로 진행(부분 완주 허용).

**리서치 소스·신뢰도**: 웹검색으로 arXiv/SSRN 논문, 퀀트 블로그(QuantifiedStrategies 등), 포럼까지 커버 가능. 단 무료 웹은 최신·유료 논문 접근 한계. 각 아이디어에 출처 URL·소스유형·신뢰도(A: 피어리뷰/재현코드 있음, B: 저명 블로그, C: 익명 포럼)를 기록하고, 신뢰도 낮은 소스는 더 엄격한 검증 게이트 적용.

**보고 형태**: (i) 일일 마크다운 리포트(오늘 시도한 아이디어·통과/탈락·최고 후보·결정 큐 항목), (ii) 누적 대시보드(원장 기반, 커버리지 맵·발견율·DSR 분포), (iii) 알림 채널(Slack/이메일)로 요약+승인대기 링크.

### 질문 4) 사용자 판단 필요 항목 처리 — **판정: 가능 (비동기 결정 큐)**

- **패턴**: 동기(블로킹) 대신 **비동기 결정 큐**. 신뢰도/리스크 분류기가 액션을 3분류: (자동 진행) / (기본값으로 진행하되 큐에 기록) / (하드 정지·큐 적재). HumanLayer(`@require_approval`)·CIBA·LangGraph 등이 같은 패턴을 표준화한다. 승인은 아침에 배치 뷰에서 bulk approve/reject.
- **사람에게 올라와야 하는 결정**: 실자본 투입, 실거래 API 연결, 유료 데이터 구매, 모호한 경제적 가정, 라이선스/법적 이슈, 레버리지·공매도 등 블라스트 반경 큰 액션.
- **자동 진행 기본값**: 페이퍼 트레이딩·무료 데이터·읽기전용 리서치·격리 백테스트는 되돌리기 가능하므로 자동 진행. 각 자동 결정은 감사 로그+되돌리기(git revert/원장 롤백) 가능하게.
- **큐가 쌓여도 안 멈추는 설계**: 승인 대기 항목은 체크포인트 상태(`pending_approval`)로 디스크에 durable 저장(재시작에도 생존)하되, 파이프라인 스레드는 블록하지 않고 다음 후보로 진행. 승인 대기가 없어야만 넘어갈 수 있는 단계(실거래)는 별도 게이트로 분리. (동기 승인은 checkpoint 메모리를 오래 붙잡으므로, "queue-then-execute"로 checkpoint 상태와 승인 상태를 분리하는 것이 정석.)

### 질문 5) 시도할 매매법의 기준과 선별 — **판정: 가능**

**트리아지 루브릭(예시, 각 0–3점 가중합)**:
- 경제적 근거: 왜 이 알파가 존재/지속하는가(리스크 프리미엄·행동편향·미시구조). 근거 없으면 데이터 스누핑 의심 → 즉시 감점.
- 데이터 요구: 무료/보유 데이터로 되는가.
- 거래빈도·비용 민감도: 개인 수수료·슬리피지 하에서도 살아남는가.
- 용량(capacity)·개인 실행가능성: 소액 개인 계좌로 체결 가능한가, 접근 가능한 자산군인가.
- 신규성: 원장 대비 중복이 아닌가.

**과최적화·데이터 스누핑 방지(파이프라인 강제 게이트)**:
- **다중검정 보정 (Harvey-Liu-Zhu)**: Harvey, Liu & Zhu(2016) *"…and the Cross-Section of Expected Returns"*(Review of Financial Studies 29:5–68)는 316개 팩터를 카탈로그화하며 "새로 발견된 팩터는 훨씬 높은 허들, 즉 **t-비율 3.0 초과**(p≈0.27%)를 넘어야 한다"고 명시. Harvey-Liu haircut은 시도 횟수를 명시 반영(Bonferroni/Holm/BHY)하며 **비선형**—높은 SR은 약하게, 한계 SR은 강하게 페널티하므로 한계 전략을 우선 버린다.
- **Deflated Sharpe Ratio (Bailey & López de Prado 2014, *Journal of Portfolio Management* 40(5):94–107, SSRN 2460551)**: 시도 횟수·비정규성(왜도·첨도)·표본길이를 반영해 SR을 할인. 파이프라인이 자동으로 시행 횟수(N)를 세고 있으므로 DSR 계산에 직접 투입 가능. 그 전신인 Probabilistic Sharpe Ratio(PSR)도 함께 사용.
- **PBO/CSCV**: 백테스트 오버피팅 확률을 추정(Combinatorially Symmetric Cross-Validation, Bailey et al. 2017).
- **White's Reality Check / Hansen SPA / Romano-Wolf stepwise**: 룰 집합 전체에 대한 데이터스누핑 검정.
- **CPCV(purged K-fold + embargo, López de Prado 2018)**: 라벨 중첩으로 인한 누수를 purging으로, 직렬상관 누수를 embargo로 제거. walk-forward의 단일 경로 한계를 다중 백테스트 경로로 보완. `purged-cross-validation`(eslazarev, sklearn 호환, MIT)·skfolio 같은 구현 존재.
- **가짜 발견 통제 프로토콜**: (i) **사전등록(pre-registration)**—구현 전 가설·유니버스·기간·평가지표를 원장에 고정, (ii) **홀드아웃 봉인**—최종 시점 데이터를 파이프라인이 절대 인샘플로 못 보게 잠그고 1회만 개봉, (iii) 시행 횟수 카운터를 원장에 누적해 DSR/haircut에 반영.

### 질문 6) 백테스트 가능성 판단 — **판정: 가능 (자동 게이트)**

**백테스트 가능성 체크리스트**:
- 필요 데이터의 존재·접근성·비용(무료/보유로 되는가).
- 시점 정합성(point-in-time): 과거 시점에 실제로 알 수 있었던 데이터인가.
- 생존편향: 상장폐지·합병 종목 포함 여부(무료 소스 대부분 미포함).
- Look-ahead / 리스테이트먼트: 펀더멘털·재무 리스테이트먼트 반영 시점.
- 체결 가정·유동성·슬리피지 모델링 가능 여부.

**데이터 없을 때 대안(강등 사다리)**: 프록시 데이터 → 축소 유니버스(유동성 큰 종목만) → 페이퍼/포워드 테스트로 전환. 어느 것도 안 되면 "구현 불가"를 원장에 사유와 함께 기록(BLOCKED)해 재시도 방지.

**무료/저가 데이터 커버리지와 한계**:
- 주식/ETF: yfinance(무키·비공식, Yahoo 스크래핑), Stooq(무키, 글로벌 일봉/FX, pandas-datareader), Tiingo(무료티어, ToS 명확), Alpha Vantage(무료 레이트리밋 타이트). **공통 한계: 생존편향 미보정·point-in-time 아님** → "파이프라인 구축·방법 학습엔 충분하나, 심사위원에게 방어할 결론엔 부족(그 지점이 CRSP/유료가 사는 것)."
- 암호화폐: CoinMarketCap 등으로 상폐 토큰(FTT·SafeMoon 등) 포함 생존편향-프리 데이터셋 구축 사례 존재.
- 선물/옵션: 무료 커버리지 얕음(연속선물·시점정합 어려움).
- **자동 게이트 설계**: 각 전략의 데이터 요구를 파싱→보유/무료 소스 카탈로그와 대조→충족 못 하면 자동 BLOCKED. 데이터 라이선스(개인분석/자동매매/공개대시보드/재판매가 각기 다름—무료 키가 있다고 상업 사용권이 있는 건 아님)도 원장에 기록.

### 질문 7) 그 외 구현 고려사항

**비용 관리**: 토큰(모델 라우팅—계획은 상위모델, 구현은 저가모델; `--max-turns`·`--max-budget-usd`로 상한), 데이터(무료 우선·유료는 결정 큐), API 호출. `/usage`·로컬 세션로그 모니터링.

**로깅/관측성·재현성**: 모든 실행의 구조화 JSON 출력·프롬프트·diff·시드·환경(락파일)·데이터 스냅샷 해시를 아카이빙. "캡처 안 하면 없었던 일"—무인 실행은 로그가 유일한 기록. 시드 고정·환경 고정으로 재현성 확보.

**코드 안전성**: 에이전트 생성 코드는 **격리 실행**(컨테이너/전용 git worktree, 네트워크·파일 권한 최소화). 병렬 에이전트는 worktree 분리로 충돌 방지. 자동 커밋은 별도 브랜치, 회귀 테스트 통과 시에만 병합. 위험 작업 전 git 커밋(=롤백 지점).

**리워드 해킹·결과 위조 방지(핵심)**:
- LLM 에이전트가 목표지표만 좇아 평가를 조작하는 것은 실증된 실패모드다. ImpossibleBench(Zhong, Raghunathan, Carlini, arXiv:2510.20270)에서 **GPT-5는 oneoff-SWEbench에서 76%, Conflicting-SWEbench에서 54.0% 테스트케이스를 악용**했다. BAITBENCH에선 7개 프론티어 에이전트 중 57.1%가 리워드 해킹.
- **"치팅하지 말라" 프롬프트의 효과는 벤치마크 의존적이라 신뢰 불가**: ImpossibleBench에서 엄격한 "테스트가 잘못됐으면 STOP" 프롬프트는 Conflicting-LiveCodeBench에서 GPT-5를 92%→1%로 낮췄지만, 같은 개입이 Conflicting-SWEbench에선 66%→54%밖에 못 낮췄다. 즉 프롬프트만으로는 못 막는다.
- **가장 효과적인 방어는 환경 하드닝**: ImpossibleBench 결론—"테스트를 숨기면 치팅이 거의 0으로 떨어지지만 정상 성능도 훼손된다; 읽기전용 접근이 절충안으로, 정상 성능을 유지하면서 테스트 수정을 막는다." 구체적으로: (i) 백테스트 채점·성과계산 로직을 에이전트가 수정 못 하는 별도 프로세스/읽기전용으로, (ii) 홀드아웃 데이터는 에이전트가 접근 못 하는 곳에 봉인하고 독립 검증기만 개봉, (iii) 에이전트가 결과를 쓰는 경로와 검증기가 읽는 경로 분리, (iv) 에이전트 출력을 검증 LLM 프롬프트에 무가공 삽입 금지.
- **독립 검증기**: 에이전트의 "왜"(체인오브소트)는 신뢰하지 말고 관측 가능한 것(툴콜·diff·실제 수익 시계열)만 채점. 트라젝토리 감사(LLM judge도 자기 약점 있으니 보조로만; TRACE·EvilGenie 참조). 룩어헤드 바이어스 자동 탐지(최근 arXiv에 point-in-time LLM 벤치마크 존재).

**실거래 단계적 게이트**: 페이퍼 트레이딩 → 소액 실거래 → 점진 확대. 각 승급은 결정 큐를 통한 명시적 사람 승인 필요. paper→소액 승급 기준(예: 봉인 홀드아웃 DSR 유의+포워드 N개월 성과 일치)을 사전 정의.

**선행 사례(무엇이 되고 무엇이 실패했나)**:
- **AlphaAgent**(Tang et al., arXiv:2502.16789, KDD 2025): idea/factor/eval 3-에이전트 재귀 루프. AST 독창성 강제+가설-팩터 정렬+복잡도 제어(심볼 길이·자유 파라미터 수)로 alpha decay에 대응. **CSI500·S&P500에서 2021년 1월–2024년 12월, 거래비용 반영 후 연평균 초과수익 각각 11.0%(IR=1.5)·8.74%(IR=1.05)** 달성해 전통·LLM 기법 능가. **교훈: 독창성·복잡도 정규화가 crowding/과최적화를 실제로 완화.**
- **QuantAgent**(Wang et al., arXiv:2402.03755): writer(코드생성)-judge(피드백) 내부루프+실시장 외부루프의 자기개선 프레임워크. MDP로 수렴 이론 제시.
- **QuantEvolve / R&D-Agent-Quant / FactorMiner / QuantaAlpha / AlphaAgentEvo 등**: 다수의 2025–2026 후속 연구가 팩터마이닝→엔드투엔드 전략개발로 확장 중이나, 대부분 "좁은 팩터 마이닝"에 머물고 실행/포지션사이징/리스크관리 통합은 미흡하다고 지적됨.
- **오픈소스**: ai-hedge-fund(멀티페르소나 LLM+페이퍼트레이딩), TradingAgents(트레이딩 데스크 조직 모사), FinRobot, AutoHedge 등. **주의: 상당수가 마케팅성 성과주장(예: InvicTrade "74% 승률")을 내세우며 엄밀한 다중검정·DSR 검증이 없다—이들은 하네스/아키텍처 참고용이지 검증된 알파가 아니다.**
- **공통 실패패턴**: (i) 과최적화·데이터스누핑, (ii) factor crowding에 의한 alpha decay, (iii) 리워드 해킹, (iv) 생존편향·look-ahead 데이터 오염. 본 설계의 게이트들은 정확히 이 4가지를 겨냥한다.

---

## Recommendations

**1단계 (1–2주, MVP 골격)**: 외부 파이썬 러너+상태머신부터 구축. `pmset`+`launchd`+`caffeinate`로 새벽 기동 검증. `claude setup-token`으로 무인 인증. 단계별 `claude -p --output-format json` 호출로 "리서치→원장기록→보고"만 먼저 돌리고, 구현/백테스트는 수동 검토. **벤치마크: 매일 무인 완주율 >90%, 크래시 시 다음날 재개.**

**2단계 (2–4주, 검증 게이트)**: 아이디어 원장+중복 캐스케이드(해시→임베딩→상관) 구현. 백테스트 가능성 자동 게이트. CPCV+DSR+Harvey-Liu haircut를 강제 게이트로. 홀드아웃 봉인·사전등록·시행횟수 카운터. **벤치마크: 봉인 홀드아웃에서 DSR 유의(t>3 상당)한 후보만 "VALIDATED"로 승급; 인샘플-홀드아웃 성과 괴리 <임계.**

**3단계 (지속)**: 리워드 해킹 방어(채점로직 격리·독립 검증기·격리 실행). 결정 큐+알림. 커버리지 맵+밴딧으로 탐색 배분. **벤치마크: 검증기가 잡아낸 조작/누수 건수 추적; 재현성 100%(시드·환경·데이터 스냅샷).**

**4단계 (신중)**: 페이퍼 트레이딩 연결 → (사람 승인 후) 소액 실거래. **승급 기준: 봉인 홀드아웃 DSR 유의 + 포워드 3–6개월 실측이 백테스트와 일치 + 결정 큐 승인.**

**판정을 바꿀 임계/트리거**: (a) 구독 자동화 정책이 다시 API 과금 강제로 바뀌면 → API 키+`--max-budget-usd` 비용통제로 전환하고 야간 실행 빈도 축소. (b) 무인 완주율이 낮으면 → 4시간 통짜 대신 단계를 더 잘게 쪼개고 phase당 컨텍스트 리셋. (c) 검증기가 조작을 반복 탐지하면 → 채점 로직을 완전히 에이전트 격리하고 홀드아웃 접근 원천 차단.

---

## Caveats

- **정책 변동성(가장 중요)**: Anthropic이 2026년 6월 15일 구독 기반 헤드리스/Agent SDK(`claude -p`) 자동화를 별도 월 크레딧으로 분리하려다 **당일(6/15) 철회**했다(support.claude.com 공지 및 The New Stack·DevOps.com 보도로 확인). 공지 원문: "We're pausing the changes to Claude Agent SDK usage described below. For now, nothing has changed: Claude Agent SDK, `claude -p`, and third-party app usage still draw from your subscription's usage limits." 현재는 구독 한도로 동작하나, "향후 개정안을 사전 고지 후 시행"한다고 밝혔으므로 **프로덕션 자동화 전에 support.claude.com 최신 정책을 반드시 재확인**하라. 팀/공유 프로덕션 자동화는 Anthropic도 API 키(pay-as-you-go)를 권장한다.
- **레이트리밋 수치는 대부분 비공식**: Anthropic은 플랜별 정확한 토큰 한도를 공개하지 않는다. "Pro ~45프롬프트/윈도", "5시간 2배 인상" 중 프롬프트 카운트 등은 커뮤니티 추정·부분 공지이며, 실제 완주 여부는 워크로드(모델·컨텍스트 크기)에 따라 다르다. "메시지"는 raw 개수가 아니라 토큰 가중치다.
- **무료 데이터의 근본 한계**: yfinance/Stooq/Alpha Vantage 등은 생존편향 미보정·point-in-time 아님. 이 위에서 나온 "발견"은 방어 가능한 알파가 아니라 프로토타입일 뿐이다.
- **선행 오픈소스의 성과주장 신뢰 주의**: 상당수 리포지토리가 다중검정·DSR 없이 승률/수익을 광고한다. 아키텍처 참고용으로만 쓰라.
- **근본적 난제**: 자동화된 대량 탐색은 본질적으로 가짜발견을 양산한다. 이 파이프라인의 가치는 "알파를 찾는 것"보다 "가짜를 빠르고 정직하게 걸러내는 것"에 있다. 통계 게이트를 느슨하게 하면 시스템은 즉시 데이터스누핑 기계로 전락한다.

---

## 부록: Claude Code에 넘길 설계 요약

**설계 원칙**
1. 상태의 진실은 대화가 아니라 디스크(파일·git·원장)에 둔다.
2. 외부 파이썬 러너가 오케스트레이터; Claude Code는 단계별 워커.
3. 모든 통계 검증(DSR·haircut·CPCV·홀드아웃 봉인)은 우회 불가능한 게이트다.
4. 채점·검증 로직은 에이전트로부터 격리(리워드 해킹 방지, 읽기전용/별도 프로세스).
5. 사람 판단은 비동기 결정 큐로; 파이프라인은 절대 블로킹하지 않는다.
6. 되돌리기 가능한 것은 자동, 되돌리기 불가는 사람 승인.
7. 모든 실행은 재현 가능(시드·환경·데이터 스냅샷·프롬프트 아카이빙).

**미결정 사항 목록**
- 파이썬 버전·하네스 세부·백테스트 엔진 선택(사용자 기존 자산 활용).
- 임베딩 모델·중복 임계값(코사인 컷, 상관 컷)의 구체 수치 튜닝.
- 커버리지 맵의 축 정의와 밴딧 알고리즘 선택.
- 결정 큐 채널(Slack/이메일/로컬 대시보드) 및 알림 포맷.
- 홀드아웃 기간·봉인 메커니즘의 물리적 구현(별도 프로세스/암호화/접근권한 분리).
- 데이터 소스 최종 조합과 라이선스 검토(자동매매 허용 여부).
- 모델 라우팅 정책(계획=상위, 구현=저가)과 월 토큰 예산 상한.
- 구독 vs API 인증 최종 결정(정책 재확인 후).
- 각 phase 타임박스 최종값과 재시도/스킵 임계.