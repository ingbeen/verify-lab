# 전역 하네스 설치·동기화 프롬프트

> **짝 문서**: `전역하네스_내보내기_프롬프트.md` (반대편 PC 에서 먼저 실행)
> **전제**: 저장소 루트에 `전역하네스_번들.md` 가 있어야 합니다.
> **두 프롬프트 외에 실행할 것은 없습니다.** 백업 경로·롤백 절차·중단 조건이 모두 안에 들어 있습니다.

---

## 이 프롬프트가 하는 일

받은 번들과 이 PC 의 하네스를 대조해 **합집합으로 맞추고**, 이 PC 에만 있던 것을
**역방향 번들**로 내보냅니다. 그것을 반대편 PC 에서 한 번 더 적용하면 **두 PC 가 같아집니다.**

```
개인 PC: 내보내기  →  [번들]  →  업무 PC: 설치 + 역방향 번들 생성
                                        ↓
개인 PC: 설치      ←  [역방향 번들]  ←
```

**한 방향 복사로는 같아지지 않습니다.** 이 PC 에만 있는 스킬·훅이 반대편에 없으면
거기서는 여전히 다른 결과가 나옵니다. 왕복이 필요한 이유입니다.

## 적용 규칙 — 판정은 자동, **적용은 승인 후**

검사와 분류는 물어보지 않고 끝까지 진행하지만, **실제 변경은 목록을 제시하고 승인받은 뒤에만** 합니다(G5).
전역 하네스는 이 PC 의 모든 저장소에 걸리기 때문입니다.

| 상황 | 처리 |
| --- | --- |
| 번들에만 있는 파일 | **가져온다** |
| 양쪽에 있고 내용이 같음 | 그대로 둔다 |
| 양쪽에 있고 내용이 다름 | **diff 를 보여주고 번들 것으로 맞춘다.** `settings.json` 만 예외(구조 병합) |
| 이 PC 에만 있는 파일 | **지우지 않는다.** 역방향 번들에 담는다 |
| `settings.json` 훅 등록 | **합집합.** 단 `OS 의존` 표시 항목은 이 PC 값을 유지하고 목록에 남긴다 |

**아무것도 삭제하지 않습니다.** 합집합이므로 한쪽에만 있던 것은 양쪽 모두에 생깁니다.

---

## 붙여넣을 프롬프트

---- 프롬프트 시작 ----

반대편 PC 에서 만든 `전역하네스_번들.md` 를 받아왔다. 이 PC 의 하네스를 그것과 같게 맞추고,
이 PC 에만 있는 것은 역방향 번들로 내보내라. 아래 게이트를 **순서대로** 통과시키고,
어긋나면 **적용하지 말고 멈춰서 보고**해라.

### 고정값 (묻지 말고 이대로 쓴다)

| 항목 | 값 |
| --- | --- |
| 받은 번들 | 저장소 루트 `전역하네스_번들.md` |
| 만들 역방향 번들 | 저장소 루트 `전역하네스_번들_역방향.md` |
| 임시 폴더 | `/tmp/harness-sync/` (`new/` 받은 것, `base/` git 원본) |
| **백업 폴더** | **`~/.claude/backups/<오늘날짜>-하네스동기화/`** — 날짜는 `TZ=Asia/Seoul date +%F`. 이미 있으면 `-2`, `-3` |
| 백업 대상 | 이 PC 의 하네스 표면 **전체** (`CLAUDE.md`·`settings.json`·`skills/`·`hooks/`·`commands/`) + 목록 스냅샷 `before.txt` |
| 하네스 표면 | `~/.claude/` 의 `CLAUDE.md` · `settings.json` · `skills/**` · `hooks/**` · `commands/**` (`__pycache__`·`*.pyc` 제외) |

백업 폴더 위치는 이 PC 의 기존 관용을 따른 것이다 (`~/.claude/backups/` 에 이미
`2026-08-26-스킬통합`, `settings.json.bak-20260828-140941` 이 있다).

### G0. 환경 진단

```bash
TZ=Asia/Seoul date '+%Y-%m-%d %H:%M'
python3 --version
test -w ~/.claude && echo "~/.claude 쓰기 OK"
test -f 전역하네스_번들.md && wc -c 전역하네스_번들.md
```

- [ ] `python3` 이 있다 (훅 자체가 python 이다)
- [ ] `~/.claude` 에 쓸 수 있다
- [ ] 번들이 있고 비어 있지 않다

**중단**: 실패하면 무엇이 어떤 메시지로 막았는지 그대로 보고하고 멈춘다. 우회하지 마라.

### G1. 무결성 — 옮기다 잘리지 않았는가

번들의 각 `BEGIN FILE` 헤더에 `sha256=` `lines=` `bytes=` 가 박혀 있다.
블록 본문을 잘라 `/tmp/harness-sync/new/` 에 쓰고 **다시 계산해 대조**해라.
스크립트로 해라 — 눈으로 세지 마라.

- [ ] 모든 파일의 sha256 이 일치한다
- [ ] `lines=` · `bytes=` 가 실제값과 일치한다
- [ ] `BEGIN` 개수와 `END` 개수가 같다 (잘림 감지)
- [ ] 번들 1장의 파일 목록 개수와 실제 추출 개수가 같다

**중단**: 하나라도 어긋나면 전송 중 손상이다. 다시 받아야 한다.

### G2. 4분류 — 무엇이 들어오고 무엇이 나가는가

이 PC 의 하네스 표면을 훑어 받은 것과 대조한다. **이 결과가 G5 승인표의 재료다.**

| 분류 | 뜻 | 처리 |
| --- | --- | --- |
| **들어옴** | 번들에만 있음 | 가져온다 |
| **다름** | 양쪽에 있으나 sha256 이 다름 | diff 를 보여주고 번들 것으로 맞춘다 |
| **같음** | sha256 동일 | 건드리지 않는다 |
| **나감** | 이 PC 에만 있음 | 지우지 않고 역방향 번들에 담는다 |

- [ ] 4분류 표를 파일 단위로 보고했다
- [ ] **`다름` 에 해당하는 파일은 전부 diff 를 보여줬다.** 특히 `CLAUDE.md` 는 전역 판단 기준이라
      한 줄도 넘기지 말고 보여준다
- [ ] `다름` 중 **이 PC 쪽이 더 새 것으로 보이는 파일**이 있으면 지목해 사용자 판단을 받는다
      (수정 시각·내용으로 판단. 애매하면 묻는다)

### G3. 계획서 훅 특별 검증 — git 원본과 대조

계획서 하네스(`plan_gate.py`·`plan_lint.py`·`impl-plan` 스킬)는 이 저장소에 **삭제 직전 원본**이
남아 있다. 이것으로 범용화가 제대로 됐는지 확인할 수 있다.

```bash
mkdir -p /tmp/harness-sync/base
git show 7165f95^:.claude/hooks/plan_gate.py             > /tmp/harness-sync/base/plan_gate.py
git show 7165f95^:.claude/hooks/plan_lint.py             > /tmp/harness-sync/base/plan_lint.py
git show 7165f95^:.claude/skills/verify-plan/SKILL.md    > /tmp/harness-sync/base/SKILL.md
git show 7165f95^:.claude/skills/verify-plan/template.md > /tmp/harness-sync/base/template.md
git show 7165f95^:.claude/settings.json                  > /tmp/harness-sync/base/settings.json
```

| 파일 | 기대되는 차이 |
| --- | --- |
| `plan_lint.py` | `marker_path()` 의 마커 디렉터리명 1곳 (`verify-lab-plan-gate` → 범용명) |
| `plan_gate.py` | 안내 문구의 `/verify-plan` → `/impl-plan` (`GATE_REASON`·`GATE_CONTEXT`) |
| `SKILL.md` | frontmatter `name: impl-plan`, description·when_to_use 중립화, 본문의 프로젝트 고유 표현 중립화 |
| `template.md` | `/verify-plan` → `/impl-plan`, `.claude/hooks/` → `~/.claude/hooks/`, `src/verify_lab/...` → 플레이스홀더 |
| 훅 등록 | `$CLAUDE_PROJECT_DIR/.claude/hooks/` → `$HOME/.claude/hooks/` |

- [ ] 기대 범위를 벗어난 차이가 있으면 **멈추고 diff 전문을 보여준다**
- [ ] **잔존 문자열**: `verify-plan` · `verify-lab` · `verify_lab` · `krx-sprint` 가 남아 있지 않다.
      남아 있으면 **자동으로 고치지 말고** 위치를 보여준다
- [ ] **「근거 승격」 절 보존**: `SKILL.md` 에 근거 승격 관련 절이 있다
      (verify-lab 이 만든 자산이고, 계획서가 유실 위험으로 지목했다)
- [ ] **`plan_gate.py` 의 `from plan_lint import ...` 가 그대로다** (같은 폴더에 있어야 동작한다)
- [ ] **`project_uses_plans()` 가 그대로다** (`docs/plans/` 가 있는 프로젝트에서만 동작)

> 번들에 계획서 하네스가 없으면 이 게이트는 건너뛰고, **G8 에서 재생성 여부를 사용자에게 묻는다.**

### G4. 동작 검증 — 적용 **전에** 임시 경로에서 돌려본다

- [ ] 받은 **모든 `.py` 훅**이 `python3 -m py_compile` 을 통과한다
- [ ] 받은 **모든 스킬·커맨드 마크다운**의 frontmatter 가 파싱된다 (있는 경우)

계획서 훅이 들어왔다면 `/tmp/harness-sync/new/` 에 둔 채로 가짜 stdin 을 먹여 확인해라.
**적용하고 나서 확인하지 마라** — 잘못된 훅을 전역에 두면 모든 레포가 영향을 받는다.

`plan_gate.py` 입력은 `{"cwd", "session_id", "tool_input": {"file_path"}}` 다.
마커 경로는 하드코딩하지 말고 `plan_lint.marker_path("<세션ID>")` 를 import 해서 얻어라
(import 가능성 검증도 겸한다).

| # | 조건 | 기대 |
| --- | --- | --- |
| A | `cwd` 에 `docs/plans/` 가 **없음** | 무출력 — 계획서 규약을 안 쓰는 레포를 방해하지 않는다 |
| B | `docs/plans/` 있음 + `.md` 편집 | 무출력 |
| C | `docs/plans/` 있음 + `src/foo.py` 편집 + 마커 없음 | `permissionDecision: ask` JSON |
| D | C 와 같은 입력 + 마커 생성 후 | 무출력 (세션당 한 번) |

`plan_lint.py` 는 `{"cwd", "session_id", "tool_input": {"file_path" 또는 "command"}}` 다.
**표본 계획서는 `check_plan()` 을 먼저 읽고 만들어라** — 무엇이 위반인지 코드가 정한다.

| # | 조건 | 기대 |
| --- | --- | --- |
| E | Done 조건을 위반한 `docs/plans/PLAN_*.md` 저장 | `{"decision": "block", ...}` |
| F | 위반 없는 계획서 저장 | 무출력 + 마커 생성 |
| G | `docs/plans/` 없는 cwd | 무출력 |

- [ ] A~G 7건 전부 기대와 일치
- [ ] **A 를 실제 레포 경로로 한 번 더 확인**한다 — `docs/plans/` 가 없는 저장소를 `cwd` 로 넣어 조용한지 본다

**중단**: 하나라도 어긋나면 적용하지 않는다. 어떤 케이스가 어떻게 달랐는지 보고한다.

### G5. 변경 목록 제시와 승인 — **여기서 반드시 멈춘다**

**여기까지 아무것도 바뀌지 않았다.** 지금부터가 실제 변경이므로,
**무엇이 어떻게 바뀌는지 전부 표로 제시하고 사용자의 명시적 승인을 받는다.**

- [ ] 아래 형식으로 **번호가 붙은 변경 목록**을 만든다. 하나도 빠뜨리지 않는다

| # | 대상 | 동작 | 지금 | 적용 후 |
| --- | --- | --- | --- | --- |
| 1 | `~/.claude/skills/impl-plan/SKILL.md` | 신규 배치 | 없음 | 번들 것 (N줄) |
| 2 | `~/.claude/hooks/plan_gate.py` | 신규 배치 | 없음 | 번들 것 (N줄, mode 0o755) |
| 3 | `~/.claude/CLAUDE.md` | 내용 교체 | N줄 | 번들 것 (M줄) · 차이 K곳 |
| 4 | `~/.claude/settings.json` | 훅 등록 **추가** | 훅 N개 | 훅 N+2개 — 아래 항목별 표 참조 |
| 5 | `~/.claude/backups/<날짜>-하네스동기화/` | 폴더 생성 | 없음 | 하네스 표면 백업본 |
| 6 | 저장소 `전역하네스_번들_역방향.md` | 파일 생성 | 없음 | 이 PC 에만 있는 N개 파일 |

- [ ] **`settings.json` 은 파일 단위가 아니라 항목 단위로 적는다.** "병합"이라고만 쓰지 말고
      **추가되는 이벤트·매처·명령을 하나씩** 나열한다. 유지되는 `OS 의존` 항목도 함께 적는다
- [ ] **삭제가 하나도 없다는 것**을 표 아래에 명시한다 (합집합 원칙)
- [ ] **되돌리는 방법**(G6 의 롤백 명령)을 표 아래에 함께 적는다
- [ ] **표를 보여준 뒤 멈춘다.** 사용자가 승인하기 전에는 **단 하나도 적용하지 않는다**
- [ ] 사용자가 번호를 지목해 제외하면 **그 항목만 빼고** 진행한다. 나머지를 임의로 조정하지 않는다
- [ ] 승인 없이 지나갈 수 있는 것은 없다 — G6 백업조차 승인 뒤에 한다

**전역 하네스는 이 PC 의 모든 저장소에 걸린다.** 한 번의 잘못된 적용이 전 저장소의
작업 방식을 바꾸므로, 사용자가 목록을 보고 판단할 기회를 반드시 준다.

### G6. 백업 — 되돌릴 수 있게 만든 뒤에 적용한다

```bash
D=$(TZ=Asia/Seoul date +%F); B=~/.claude/backups/$D-하네스동기화
mkdir -p "$B"
cp -R ~/.claude/CLAUDE.md ~/.claude/settings.json ~/.claude/skills ~/.claude/hooks ~/.claude/commands "$B"/ 2>/dev/null
find ~/.claude/CLAUDE.md ~/.claude/settings.json ~/.claude/skills ~/.claude/hooks ~/.claude/commands -type f > "$B"/before.txt 2>/dev/null
ls -la "$B"
```

- [ ] 하네스 표면 5가지가 전부 백업 폴더에 있다
- [ ] `before.txt` 의 줄 수가 실제 파일 수와 같다

**롤백 절차 (문제가 생기면 이대로 되돌린다)**:

```bash
D=<백업한 날짜>; B=~/.claude/backups/$D-하네스동기화
rm -rf ~/.claude/skills ~/.claude/hooks ~/.claude/commands
cp -R "$B"/skills "$B"/hooks "$B"/commands ~/.claude/
cp "$B"/CLAUDE.md "$B"/settings.json ~/.claude/
```

### G7. 적용 — **G5 에서 승인된 항목만**

- [ ] 승인 목록에 있는 것만 적용한다. **제외된 번호는 건드리지 않는다**
- [ ] `들어옴` 파일을 배치한다 (필요한 폴더는 만든다)
- [ ] `다름` 파일을 번들 것으로 맞춘다 — 단 `settings.json` 은 제외(아래)
- [ ] **`settings.json` 은 구조 병합**한다. 통째로 덮지 않는다
  - [ ] 훅 등록의 **합집합**을 만든다 (같은 `command` 는 중복으로 넣지 않는다)
  - [ ] 번들에서 `OS 의존` 으로 표시된 항목(`terminal-notifier`·`wsl_toast`·`notify-send` 등)은
        **이 PC 값을 유지**한다. 가져온 값으로 덮지 않는다
  - [ ] `permissions`·`env` 같은 다른 절도 **키 단위 합집합**으로 처리하고, 충돌하면 멈추고 묻는다
  - [ ] 병합 후 `json.load` 로 파싱된다
  - [ ] **병합 전후 훅 개수 대조**: 기존 + 새로 들어온 = 병합 후 (중복 제외분 명시)
- [ ] 실행 권한이 필요한 훅은 원래 mode 를 반영한다 (번들 헤더에 `mode=` 가 있다)
- [ ] **아무것도 삭제하지 않았다**

**중단**: 기존 항목이 하나라도 사라지면 **즉시 G6 롤백**하고 보고한다.

### G8. 역방향 번들 — 이 PC 에만 있던 것을 내보낸다

G2 의 `나감` 목록과, `settings.json` 에서 **이 PC 에만 있던 훅 등록**을 담아
`전역하네스_번들_역방향.md` 를 만든다.

- [ ] `전역하네스_내보내기_프롬프트.md` 의 스크립트와 **같은 형식**을 쓴다
      (`BEGIN FILE` / `END FILE` 마커 + `sha256` · `lines` · `bytes` · `mode`)
- [ ] 머리말에 **이 PC 이름과 생성 시각**을 적는다
- [ ] `나감` 이 0건이면 파일을 만들지 말고 **"역방향으로 보낼 것이 없다"고 보고**한다
- [ ] 이 번들이 반대편에서 그대로 `전역하네스_설치_프롬프트.md` 에 쓰인다는 점을 감안해,
      파일명은 반대편에서 `전역하네스_번들.md` 로 바꿔 쓰라고 안내한다

### G9. 남은 차이 보고 — 이 절차의 핵심 산출물

**같아진 것보다 같아지지 않은 것이 중요하다.** 아래를 표로 남긴다.

- [ ] `OS 의존` 이라 일부러 다르게 둔 항목 (어느 쪽이 무엇을 쓰는지)
- [ ] **절대 경로가 박힌 항목** — 예: `SessionStart` 훅이 `/Users/<이름>/Workspace/...` 를 가리키면
      반대편 PC 에서는 그 경로가 없어 조용히 통과한다. **무해하지만 동작이 같지는 않다**
- [ ] 번들에 없어 재생성이 필요한 것 (있으면 `7165f95^` 재생성 여부를 묻는다)
- [ ] 하네스 밖에서 결과를 가르는 것들 — 확인해서 다르면 적는다
  - `~/.config/git/ignore` (전역 gitignore. 이 PC 에는 `**/.claude/settings.local.json` 한 줄이 있다)
  - `.claude/settings.local.json` — **저장소마다 있으나 git 에 올라가지 않는다.** 권한 허용 목록이
    PC 마다 달라 승인 프롬프트가 다르게 뜬다
  - `~/.claude/plugins/marketplaces/` 설치 목록
  - `python3` · `poetry` · `git` 버전

### G10. 사후 확인 (사용자만 할 수 있는 것)

- [ ] **사용자에게 세션 재시작을 요청**한다 (스킬·훅은 세션 시작 시 읽힌다)
- [ ] 새 세션에서 스킬 목록이 반대편 PC 와 같은지 확인
- [ ] verify-lab 에서 계획서 없이 `.py` 를 고치려 할 때 권한 프롬프트가 뜨는지 확인
- [ ] `docs/plans/` 가 없는 저장소에서는 아무 일도 없는지 확인

**「됐을 것이다」로 대신하지 마라.** 확인 전에는 완료로 보고하지 않는다.

### G11. 마무리 (각각 사용자 승인 후)

- [ ] `docs/plans/PLAN_global_impl_plan_skill.md` 가 남아 있으면 DoD 106·107 행을 체크한다
      (계획서는 **체크리스트 갱신만** 허용 — 본문 문구는 고치지 않는다). 없으면 건너뛴다
- [ ] `전역하네스_번들.md` 삭제 여부를 묻는다 (역방향 번들은 반대편에서 쓸 때까지 남긴다)
- [ ] `/tmp/harness-sync/` 정리
- [ ] 남은 찌꺼기 3건을 **보고만** 한다 (지우는 것은 별건이다)
      - `.claude/hooks/__pycache__/plan_lint.cpython-312.pyc`
      - `src/verify_lab/strategy/grid/` (untracked `__pycache__` 만 남음)
      - 루트 `CLAUDE.md` 335·337행의 디렉토리 구조 문구 (`hooks/`·`skills/` 설명이 실재와 다름)

### 절대 하지 말 것

- **G5 승인 없이 적용** — 변경 목록을 보여주고 답을 받기 전에는 아무것도 바꾸지 않는다
- 검증에 실패한 채로 적용
- **이 PC 의 하네스 파일 삭제** — 합집합이지 교체가 아니다
- `settings.json` 통째 덮어쓰기
- 잔존 문자열(`verify-lab` 등)을 **임의로 치환** — 범용화 범위는 사용자가 판단한다
- 번들에 없는 파일을 「원래 이랬을 것」으로 재구성
- git 커밋·푸시 (사용자가 직접 한다)

### 보고 형식

1. **게이트별 통과/실패** 한 줄씩 (G0~G8)
2. **G2 4분류 표** — 들어옴/다름/같음/나감 파일 수와 목록
3. **G3 diff 요약** — 기대 범위 안이었는지
4. **G4 동작 검증 A~G 결과표**
5. **G5 에서 승인받은 항목과 제외된 항목**
6. **G7 병합 전후 훅 개수**
7. **G9 남은 차이 표** ← 가장 중요
8. **백업 폴더 경로와 롤백 명령**
9. **사용자가 직접 해야 할 것** (G10·G11, 역방향 번들 커밋 포함)

---- 프롬프트 끝 ----
