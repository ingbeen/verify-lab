# 산출물 정리 작업(A·B·C) 검증 프롬프트

> **이 파일은 임시입니다.** 세 계획서가 전부 Done 이고 아래 검증까지 끝나면 **지웁니다.**
> 남길 가치가 있는 것은 그 전에 살아있는 문서로 승격돼 있어야 합니다.

## 쓰는 법

**새 세션을 열고 아래 블록을 그대로 붙여넣습니다.** 세 작업을 나눠 검증하려면
「검증 범위」 줄에서 필요 없는 계획서를 지우면 됩니다.

---

```
verify-lab 의 산출물 정리 작업(A·B·C)이 계획서대로 끝났는지 검증해줘.

## 검증 범위 — 이 세 계획서를 SoT 로 본다

| 계획서 | 무엇 | 상태 (2026-09-11) |
| --- | --- | --- |
| docs/plans/PLAN_payoff_ratio_grade.md | 작업 A — 손익비를 후보 판정의 «등급» 항목으로 | ✅ Done (커밋됨) |
| docs/plans/PLAN_naming_unification.md | 작업 B — 매매법 이름과 산출물 «자리» 통일 | ✅ Done (미커밋) |
| docs/plans/PLAN_output_layout.md | 작업 C — 성적표·거래내역의 «이름과 컬럼» 통일 | 🟡 **착수 전** |

**C 가 착수 전이므로 4절의 항목은 「미구현」이 정답이다.** 그것을 실패로 적지 않는다 —
확인할 것은 「C 가 전제한 사실이 지금도 맞는가」다.

## 검증 규율

- **계획서의 서술을 믿지 말고 저장소의 «현재 상태»와 대조한다.** 체크박스가 [x] 라는 것은
  「했다고 적혀 있다」이지 「되어 있다」가 아니다.
- **숫자는 도구 출력을 그대로 붙여넣는다.** 눈으로 읽어 다시 타이핑한 값은 근거가 아니다.
- 확인하지 못한 항목은 **「미확인」으로 남긴다.** 통과로 넘기지 않는다.
- 코드 파일은 `Read` 도구로 연다 — 셸로 읽으면 그 경로의 규칙이 주입되지 않는다.

## 1. 기계로 확인할 것

1. `poetry run python validate_project.py` 를 돌려 `failed=0` · `skipped=0` 인지 본다.
   (셸에 VIRTUAL_ENV 가 남아 있으면 `env -u VIRTUAL_ENV` 를 붙인다)
2. 세 계획서가 Done 4조건을 만족하는지 본다 — DoD 전부 [x] · Validation 에 품질 검증 줄과
   `/code-review` 기록 · 미완료 항목 없음.
3. 옛 이름이 남아 있지 않은지 전수 검색한다:
   `index_extreme` · `expiry_trading` · `expiry_runner` · `kosdaq_month_end_trading` ·
   `STUDY_NAME` · `STRATEGY_NAME` · `simulate_expiry_trade` · `run_strategy`
   → 남아 있다면 **살아있는 코드인지, 옛 산출물을 가리키는 인용인지, 「전에는 이랬다」는
      이력 서술인지** 가른다. **뒤 둘은 정상이다** — 기존 산출물 폴더 29개는 옮기지 않기로
      했고, 개정 근거를 남긴 주석·문서가 옛 이름을 인용한다.
   → **살아있는 코드에 남아 있으면 실패다.** 판별은 `grep --include='*.py' src scripts tests` 로
      하고 **주석·docstring 과 실행되는 식별자를 구별해** 적는다.
4. `summary_by_cell` · `summary_by_target` · `performance.csv` · `trades.csv` 는
   **작업 C 의 대상이라 아직 남아 있는 것이 정상이다.**

## 2. 작업 A — 손익비가 «등급»인가 (게이트가 아닌가)

- `src/verify_lab/measure/screening.py` 에서 **1차 게이트 조건식에 손익비가 없는지** 확인한다.
  게이트는 적중률·방향 기대값 두 축이어야 한다.
- 등급 분모가 **4** 인지, 시기를 못 잰 칸에서 **3** 인지 확인한다.
- 손익비 산식이 **저장소에 한 벌만** 있는지 확인한다 — `measure/statistics.py` 밖에서
  이길때÷질때를 다시 계산하는 코드가 있으면 안 된다.
- 손익분기 승률의 분모가 **전체 표본**인지 확인한다 (`(결정된 거래 ÷ 표본) ÷ (1 + 손익비)`).
  `1 ÷ (1 + 손익비)` 로 되어 있으면 **보합이 있는 칸에서 값이 과대평가된다.**
- **전승 칸**(진 거래 0건)이 손익비 빈칸 + 등급 **충족**으로 나오는지,
  **전부 보합인 칸**은 손익비 항목을 아예 **묻지 않는지** 확인한다.

## 3. 작업 B — 이름과 자리

- 매매법 slug 가 코드·산출물 폴더·문서에서 하나로 맞는지 확인한다.
  확정 이름표는 PLAN_naming_unification.md §5 에 있다.
- 🔴 **매매 계층 구성은 「두 벌」이 아니다** (2026-09-11 개정, 안 C). 초안의 「월말에 체결부가
  있다」가 실측에서 틀려 **공유 로직을 매매법-중립 모듈로 뺐다.** 확인할 것은 아래 셋이다.
  - `strategy/trade_fill.py`(체결 판정식)와 `strategy/periods.py`(구간 5행 산식)가 있고,
    매매법마다 `<slug>_runner.py` **하나**씩인지
  - **매매법 모듈끼리 서로 import 하지 않는지** — 전에는 월말 → 옵션 만기일 → 역방향 사슬이었다
  - `<slug>_trading.py` 가 **하나도 없는지.** 있으면 실패다 — 월말은 더할 체결 로직이 없어
    만들면 빈 껍데기이거나 판정식 복제다. 근거는 `src/verify_lab/CLAUDE.md` 「매매 계층 구성 계약」
- 폴더 이름을 만드는 상수가 **CLI 가 아니라 패키지**에 있는지 확인한다.
- 새 산출물이 `storage/results/{검증, 매매, 실측}/<시각>_<slug>/` 에 생기는지
  **스크립트를 실제로 한 번 돌려** 확인한다.
- 🔴 `src/verify_lab/utils/result_citations.py` 가 **새 경로와 옛 경로를 모두** 인식하는지
  확인한다. `tests/test_result_citations.py` 가 통과하는 것만으로 끝내지 말고,
  **기존 29개 폴더**(루트 직하)가 여전히 인용으로 잡히는지 직접 본다.
  → 초안은 「정규식을 고쳐야 한다」고 적었으나 **정규식은 고칠 필요가 없었다** —
    한글은 상위 폴더 이름에만 들어가고 폴더 이름 자체는 계속 ASCII 다. 고친 것은 **탐색 깊이**다.
- 🔴 `scripts/maintenance/clean_results.py` 를 **실제로 한 번 돌려** 본다 (목록만, `--delete` 없이).
  중첩 폴더의 **용량이 `0.0MB` 로 나오면 실패다** — 경로를 이름으로 되조립하면 예외 없이 그렇게 된다.
  `git ls-files` 호출에 **`-z` 가 붙어 있는지** 확인한다 — 없으면 `core.quotepath` 기본값 때문에
  한글 계층 폴더가 escape 되어 **그 아래 전부가 미추적으로 잡히고 「되돌릴 수 없음」 경고가 거짓이 된다.**
- `create_run_directory` 가 **계층 이름과 slug 모양을 둘 다 거부**하는지 확인한다.
  slug 에 대문자·숫자가 섞이면 폴더는 생기는데 인용 스캐너가 영원히 못 찾는다.
- `docs/INDEX.md` 가 갱신되고 문서 링크가 전부 해석되는지 확인한다.
- `docs/COMMANDS.md` 의 실행 명령이 새 스크립트 이름인지 확인한다.

## 4. 작업 C — 파일명과 컬럼

- 세 매매법의 산출물이 `성적표.csv` · `거래내역.csv` 인지 확인한다.
- 세 성적표의 **헤더를 나란히 출력해** 공통 컬럼의 이름과 순서가 같은지 대조한다.
  기준 표는 PLAN_output_layout.md §5 에 있다.
- **역방향 성적표에 구간 5행**(전체·앞 절반·뒤 절반·최근 10년·최근 5년)이 있는지 확인한다.
- **역방향 거래내역에 청산일·청산가**가 있는지 확인한다.
- 구현할 수 없는 칸이 **0 이 아니라 빈칸**인지 확인한다 —
  특히 월말의 지수 대상 2개(고가·저가가 없어 장중손절 판정 불가).
- 손절선이 **세 매매법 모두** 성적표에 있는지 확인한다.

## 5. 회귀 — 값이 바뀌지 않았는지

**이 셋이 이 검증의 핵심이다.** 형식만 바꾸기로 한 작업이므로 값이 달라졌다면 사고다.

1. **1차 판정(후보/제외)이 바뀌지 않았는가** — 작업 A 는 게이트를 건드리지 않기로 했다.
2. **합계·평균·승률이 바뀌지 않았는가** — 시세를 다시 받지 않기로 했다.
   옛 산출물과 새 산출물을 같은 칸끼리 대조한다.
   → **B 는 새 산출물을 남기지 않았다.** 스크립트 9개를 돌려 경로만 확인하고 그 9폴더를 지웠다
     (재실행은 C 끝에 한 번). 그래서 **지금 `storage/results/{검증,매매,실측}/` 가 비어 있는 것이
     정상**이고 대조는 C 의 재실행 뒤에 한다. B 단계에서 볼 것은 **경로가 맞게 생기는가**뿐이다.
3. **결과 문서가 인용한 산출물 폴더가 전부 실재하는가** — `tests/test_result_citations.py`.

값이 달라진 칸이 있으면 **문서를 고치기 전에 원인을 찾는다.**

## 6. 보고 형식

- 항목마다 **통과 / 실패 / 미확인** 중 하나로 적고, 실패는 **무엇이 어떻게 다른지**를
  도구 출력과 함께 적는다.
- 계획서와 실제가 어긋난 곳은 **어느 쪽이 맞는지 판단하지 말고 둘 다 보여준다.**
- 마지막에 **「이 세 계획서를 지금 지워도 잃는 것이 없는가」**에 답한다.
  아니라면 무엇이 아직 승격되지 않았는지 적는다.
```

---

## 검증이 끝나면

1. 세 계획서의 근거가 살아있는 문서로 승격됐는지 확인하고 `docs/plans/` 를 비웁니다
   (`.gitkeep` 은 남깁니다 — 계획서 게이트 훅이 폴더 존재로 규약 채택을 판정합니다).
2. 인용되지 않는 산출물 폴더를 `/clean-results` 로 정리합니다.
3. **이 파일을 지웁니다.**

## 이 작업의 배경 (검증자가 알아야 할 최소한)

세 작업은 **하나의 정리**를 셋으로 나눈 것입니다.

| 작업 | 무엇을 | 왜 |
| --- | --- | --- |
| **A** | 손익비를 후보 판정의 **등급** 항목으로 | 게이트 두 축을 통과하고도 「질 때 크게 지는」 칸이 실측으로 나왔다 |
| **B** | 매매법 이름과 산출물 **자리** | 같은 매매법이 계층마다 다른 이름으로 불려 사용자가 헷갈렸다 |
| **C** | 성적표·거래내역의 **이름과 컬럼** | 같은 뜻의 표가 매매법마다 다른 이름과 다른 컬럼을 가졌다 |

**순서는 A → B → C 이고 재실행은 C 끝에 한 번만** 합니다. 세 작업 모두
**측정값을 바꾸지 않는 것**이 전제입니다 — 바뀌었다면 그 자체가 조사 대상입니다.
