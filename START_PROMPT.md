# 새 세션 시작 프롬프트

> 이 파일은 verify-lab 작업을 이어받는 세션의 **단일 진입점**입니다.
> **작업이 진척되면 이 파일도 함께 갱신합니다.** 낡은 진입점은 없는 것보다 나쁩니다.
>
> **마지막 갱신**: 2026-08-26 (KST) — G5 완료

---

verify-lab 프로젝트를 이어서 진행한다.

**G5 가 끝났다. 원달러 그리드 트랙의 계획된 단계가 전부 끝났고, 다음 작업은 사용자가 정한다.**
아래 **"다음 작업" 절에 남은 선택지 셋**이 정리돼 있다 — 착수 전에 사용자에게 묻는다.

> ⚠️ **G5 가 뒤집은 것을 먼저 읽어라.** 둘이다.
> ① **§13.3 의 「분할매수 후 보유에 진다」는 파라미터에 의존한다** — 여덟 축 중 **셋에서 부호가
> 바뀐다**(N=7 · g=1.6%·2.4% · 차등 ±0.3). **「그러니 그 값을 쓰자」로 옮기면 §15.1 위반이다.**
> ② **사양서 §14 가 「최악, 전 슬롯 물림」으로 지목한 2009~2014 대세 하락이 전략이 가장 크게
> 이긴 구간(+21.46%p)이다.** 전 기간 −309만원은 **하락장 승리와 급등장 패배의 합**이다.
> 아래 "지금까지가 남긴 것" 절이 SoT다.

## 먼저 읽을 것 (순서대로, 전부)

1. `CLAUDE.md` — 프로젝트 규칙. 특히 "이 프로젝트가 무엇인가"와 "측정의 원칙" 8개
2. **`docs/context/README.md`와 그 폴더의 두 문서** — 사용자의 현재 투자 상태이자 이 프로젝트가
   시작된 이유. 결과를 해석할 때 이 맥락이 없으면 쓸모없는 보고가 된다
3. **`docs/spec/usdkrw_grid.md`** — **지금 트랙의 SoT.** 두 부분이 다 중요하다.
   §4 "확정된 설계 결정"(A1~A4·B1~B2·**C1~C118**)이 **다시 논의하면 안 되는 것들**이고,
   §3 "데이터 실측 기록"(**3.1~3.15**)이 함정과 측정값 목록이다.
   **§4.11 (C105~C118)와 §3.15 가 가장 최근에 확정된 것들**이다
4. **`docs/spec/usdkrw_grid_rules.md`** — 사용자가 쓴 매매 규칙 원본 사양서.
   **그대로 구현하지 않는다** — 문서 머리에 §4 가 바꾼 것들의 목록이 있다
5. **`docs/strategy/원달러_그리드.md`** — **G5 결과 문서이자 이 트랙의 최종 산출물.**
   규칙(§1)·성적(§2)·결정(§3)·한계(§4)·체결 원자료(§5)가 한 파일에 있다
6. **`docs/research/원달러_ETF_등가성.md`** — G1 결과. **ETF 경로를 쓸 수 있는지의 판정**이며,
   §8(연도별 괴리 ±1%p)과 §14(한계)가 ETF 성적을 해석할 때의 전제다
7. `docs/ROADMAP.md` — **진행 상태의 SoT.** Phase G0·G1·G2·G3·G4·**G5**(전부 완료) 절의 "계층 계약"과
   "다음 단계가 알아야 할 것"을 본다. 검증 #1 의 계층 간 계약 다섯 절도 그대로 유효하다
8. `src/verify_lab/CLAUDE.md` — 계층 분리, 상수 관리, 측정 계층의 절대 원칙 5가지
9. `.claude/rules/strategy.md` — 매매 규칙 계층의 예외 규정. **G2 에서 입력 계약이 넓어졌다**
10. `.claude/rules/python.md`, `scripts/CLAUDE.md`, `tests/CLAUDE.md`, `docs/research/CLAUDE.md`

문서 지도는 `docs/INDEX.md` 다. **문서를 추가하면 INDEX 에 등록한다** —
`tests/test_index.py` 가 양방향으로 검사하므로 빠뜨리면 `validate_project.py` 가 실패한다.

---

## 현재 상태

### 되어 있는 것

**검증 #1 (지수 극단 이벤트) — 완료.** 코드·결과 문서·사용자 직접 대조까지 끝났다.
거기서 확정된 공통 계층(`measure/`·`report/`)과 계층 계약은 그대로 유효하다.

**역방향 매매 규칙 — 확정.** `docs/strategy/역방향_매매_규칙.md` 가 SoT이며 **현재 운용 중**이다.
이번 트랙과 무관하므로 **손대지 않는다.**

**원달러 그리드 G0 (데이터 확보) — 완료.**

| 파일 | 내용 |
| --- | --- |
| `storage/series/USDKRW_CLOSE.csv` | **정규장 종가.** 판정·체결·평가의 기준 가격 |
| `storage/series/USDKRW.csv` | 매매기준율. **환전 스프레드 계산에만** 쓴다 |
| `storage/series/DTB3.csv`·`CD91.csv` | 미국 3개월 T-bill · CD 91일물 |
| `storage/series/261240_NAV.csv`·`261250_NAV.csv` | ETF 순자산가치 |
| `storage/market/261240_*.csv`·`261250_*.csv` | ETF 시세 (원본가·수정주가 각 2종) |

**원달러 그리드 G1 (등가성 검증 = 검증 #5) — 완료. 판정은 「조건부 가능」.**
261240 은 「환전 + 달러 예치」를 잘 복제한다(상관 0.9860, 추적오차 1.43%). 다만 **연도별 괴리
1.65%p**(합격선 0.3%p)라 상수 비용으로 처리할 수 없고, **연 ±1%p 를 변동 요인으로 감수**한다.

**원달러 그리드 G2 (그리드 엔진) — 완료.**

- `src/verify_lab/strategy/grid/` 6개 모듈 — `lattice`·`price_range`·`allocation`·`execution`·`engine`·`runner`
- `scripts/strategy/run_usdkrw_grid.py` 로 **2005-01-03 ~ 2026-08-21 · 5,340 거래일**을 실제로 돌렸다
- **환전 경로 단독이며 비용·세금·이자가 전혀 없다.** 총자산 1억 → 1.32억 (+32.06%, 연 1.29%)

**원달러 그리드 G3-1 (거래비용) — 완료.**

- `src/verify_lab/strategy/grid/paths/` 신설 — **집행 경로 계층.** 「슬롯 하나를 사고 판다」를
  경로가 책임지고 엔진은 격자·범위·배분·판정만 한다. 지금은 **환전 구현 하나**다
- 환전 스프레드 편도 0.08% + 슬리피지 편도 0.10%, **왕복 0.36%**. 이자를 빼면 총자산 1억 →
  **1.233억**(+23.29%) — **거래비용이 그 총수익의 33.0% 를 가져간다**
- 엔진의 회계 항등식이 **「총자산은 비용만큼만 줄어든다」**로 바뀌었다
- 확정 결정은 스펙 §4.5 (**C46~C56**), 실측은 §3.9 다

**원달러 그리드 G3-2 (이자·세금) — 완료. 곡선의 성격이 통째로 달라졌다.**

- `src/verify_lab/strategy/grid/interest.py` 신설 — 실수령 금리 모델(사양서 §11.2)과 마스터 달력 정렬
- 총자산 1억 → **1.785억** (+78.49%, CAGR 2.75%). **총수익 7,849만원 중 세후 이자가
  5,016만원(63.9%)** 이고 매매·환율 기여는 2,833만원(36.1%)이다
- 하루 처리 순서가 **이자 → 월말 정산 → 매도 → 평가 → 배분 → 매수** 로 바뀌었다.
  **이자일수 −1 을 미래를 보지 않고 만족시키는 유일한 순서**다 (결정 C57)
- 확정 결정은 스펙 §4.6 (**C57~C66**), 실측은 §3.10 이다

**원달러 그리드 G3-3 (ETF 경로 2종) — 완료. 사양서 §15.2 필수 검증 12개가 모두 통과했다.**

- `src/verify_lab/strategy/grid/paths/etf.py` 신설 — 261240·261250 이 **같은 구현**을 쓴다.
  다른 것은 가격 계열뿐이고 노출 2배는 그 가격에 이미 들어 있다
- **판정 가격과 집행 가격이 갈라졌다.** 격자·범위·하향 돌파·목표가는 **원달러 종가**,
  체결·평가는 **경로의 가격**이다 — 그래서 세 경로의 체결 건수가 매수 264 / 매도 252 로 같다
- **경로가 자기 세금과 보유 이자율을 답한다.** 엔진에 경로 분기가 하나도 없다
- 같은 기간(2017~) 성적: **환전 125,998,702 · 261240 125,794,274 · 261250 136,051,353원**
- 확정 결정은 스펙 §4.7 (**C67~C77**), 실측은 §3.11 이다

**원달러 그리드 G3-4 (하단 이탈 B안) — 완료. 사양서 §7 의 A/B 병행이 갖춰졌다.**

- `--lower-breach A|B` 로 갈라 실행한다. **연장 하단은 범위 계층이 가격으로 내고**(A안이면 정식
  하단과 같은 값이라 **엔진에 분기가 없다**), 어느 레벨이 켜지는지는 격자 계층이 답한다
- **연장은 다음 재조정까지 유지된다** — 직전 재조정 이후의 누적 최저 종가이며 재조정일에 초기화된다
- **B안이 발동하는 곳은 2005~2007 하나뿐이다** — 연장 327일 / 연속 구간 7회 / 최대 6칸.
  **ETF 기간(2017~)에는 어느 축에서도 0일**이라 세 대조군에서 **B안 = A안이 원 단위까지 성립**한다
- 확정 결정은 스펙 §4.8 (**C78~C86**), 실측은 §3.12 다

**원달러 그리드 G4-1 (성과 지표 계층) — 완료. 사양서 §13 의 지표가 실행마다 나온다.**

- `src/verify_lab/strategy/performance.py` 신설 — **곡선 하나와 rf 계열만 받는다.**
  `strategy.grid` 를 import 하지 않는 것을 테스트로 고정했다 (결정 B1 을 구조로 지킨다)
- `src/verify_lab/strategy/grid/metrics.py` 신설 — §13.2 의 13개 지표와 §15.3 의 9개 판정
- **rf = CD91 원지표 세후, 달력일 간격만큼** (결정 C87·C88). **Sharpe 가 0.122** 다
- **§15.3 에서 걸리는 것은 MDD 하나뿐**이고 나머지 6개 통과·2개 판정 불가다
- 확정 결정은 스펙 §4.9 (**C87~C96**), 실측은 §3.13 이다

**원달러 그리드 G4-2 (벤치마크 3종) — 완료. §13.3 의 판정이 나왔고 「진다」이다.**

- `src/verify_lab/strategy/grid/benchmarks.py` 신설 — 세 곡선이 **지표 계층을 그대로 통과**한다.
  결정 B1 의 「곡선 하나만 받는 함수」가 여기서 처음 재사용됐다
- **「분할매수 후 보유」는 엔진의 매도 스위치**다 (결정 C101). 매수·배분·판정이 같은 코드를 지나
  **차이가 익절 로직 하나에서 나온다**는 것이 구조로 보장된다
- **네 실행 전부에서 전략이 「분할매수 후 보유」에 진다** — −309만 / −1,723만 / −2,619만 / −4,578만원
- **원화 파킹 100% 는 네 번 다 이기고**, B&H 는 **환전 2005~ 에서만** 이긴다
- 산출물에 `benchmarks.csv` 가 추가됐다 — 거래일 × 네 곡선
- 확정 결정은 스펙 §4.10 (**C97~C104**), 실측은 §3.14 다

**원달러 그리드 G5 (견고성 검사 + 결과 문서) — 완료. 트랙의 계획된 단계가 전부 끝났다.**

- `src/verify_lab/strategy/grid/regimes.py` · `sweep.py` 신설, `scripts/strategy/run_usdkrw_grid_robustness.py` 신설
- **인자가 없는 스크립트 하나로 고유 33회를 돌린다** (약 35초). 축 비교 40줄 · 국면 205줄
- **결론 여섯 개 중 넷이 여덟 축 전부에서 안정**이고 **「분할매수 후 보유를 이긴다」가 셋에서 뒤집힌다**
- **사양서 §15.3 #8 이 통과로 판정됐다** — 남은 「판정 불가」는 261250 β 하나뿐이다
- **`docs/strategy/원달러_그리드.md` 신설** — 규칙(§1)과 성적(§2)을 한 파일에 뒀다 (결정 A3)
- 확정 결정은 스펙 §4.11 (**C105~C118**), 실측은 §3.15 다

**품질 게이트** — `validate_project.py` 가 `passed=1034 failed=0 skipped=0` 으로 통과한다 (2026-08-26 기준).
숫자가 줄었으면 무언가 깨진 것이다.

### 안 되어 있는 것

**계획된 단계는 없다.** 남은 것은 전부 **사용자가 할지 말지 정하는 선택지**이며 아래 "다음 작업" 절에 있다.

`docs/plans/` 에 G2 계획서 5건과 G3-1~G3-4·G4-1·G4-2·G5 계획서 7건이 Done 상태로 남아 있다.
근거 승격은 전부 끝났으므로 **지금 지워도 잃을 것이 없다** — 지울 때 `.gitkeep` 을 되살린다.

---

## 지금까지가 남긴 것 — 다음 단계가 반드시 알아야 한다

### G5 가 뒤집은 것 ① — §13.3 의 판정이 파라미터에 의존한다

여덟 축을 한 번에 하나씩 옮겨 고유 33회를 돌렸다. **환전 2005~ 의 48개 판정 중 44개가 안정**이고
넷이 뒤집히는데, 그중 셋이 **같은 결론**이다.

| 축 | 「분할매수 후 보유를 이긴다」 | 이기는 지점의 차이 |
| --- | --- | --- |
| **룩백 N** | **N=7 에서만 참** | +445,669원 |
| **익절폭 g** | **1.6% · 2.4% 에서 참** | +5,724,898 / +3,521,574원 |
| **자금 차등** | **±0.3 에서만 참** | +1,266,030원 |
| 최소폭 · 상한 · RP 하한 · 파킹 하한 · 스프레드 | 전 축값 거짓 | — |

> **「그러니 g=1.6% 를 쓰자」로 옮기면 사양서 §15.1 의 「결과를 보고 파라미터 변경 금지」를 어긴다.**
> 뒤집히는 네 지점의 절대금액이 **총자산의 0.4~3.2%** 라, 그 흔들림은 "그 값이 좋다"가 아니라
> **"표본이 그 지점을 특정할 만큼 크지 않다"** 는 신호다 (`.claude/rules/strategy.md`).

**나머지 결론 셋은 여덟 축 전부에서 흔들리지 않는다** — 원화 파킹(+1,651만~+2,391만원)과
B&H(+175만~+1,117만원)를 이기고, MDD 가 −10%보다 얕고(−4.23~−9.60%),
세후 이자가 총수익의 절반을 넘는다(56.5~69.8%). **네 번째 뒤집힘**은 이탈 보너스 30% 판정이며
**N=1 에서만 걸린다**(31.84%).

### G5 가 뒤집은 것 ② — 사양서 §14 의 「최악 국면」이 실제로는 최선이었다

| 구간 (연속 분할) | 성격 | 전략 | 분할매수 | **차이** |
| --- | --- | --- | --- | --- |
| **2008** | 급등 | +9.48% | +29.71% | **−20.24%p** |
| **2009~2014** | **대세 하락** | **+10.70%** | **−10.76%** | **+21.46%p** |
| 2017~2018 | 횡보 | +1.83% | −5.87% | +7.70%p |
| 2024~2026 | 급등 | +8.51% | +15.65% | −7.14%p |

**§14 가 "최악 — 전 슬롯 물림" 으로 지목한 2009~2014 가 전략이 가장 크게 이긴 구간이다.**
원달러가 1,570 → 1,008 로 빠진 6년인데 분할매수는 −10.76% 손실이고 전략은 +10.70% 이익이다.

> **이유는 매매가 아니라 안 산 것이다.** 투입률이 낮아 자금 대부분이 원화로 대기했고 그 구간이
> 고금리 시기였다. **그 승리는 매매의 성과가 아니라 투입률의 성과**이며, 같은 이유로 급등장에서
> 크게 진다. **전 기간 −309만원은 그 둘의 합**이고 평균 하나로는 이 구조가 통째로 안 보인다.

**사양서 8구간과 연속 분할이 다른 것을 보여준다** — 8구간의 2008~2009 −9.82%p 가 연속 분할에서
**2008 단독 −20.24%p** 로 갈린다. 2009년의 하락이 2008년의 급등을 절반쯤 상쇄했기 때문이다.

**ETF 표본에는 그 승리 구간이 없다.** 261240·261250 은 2016-12 상장이라 2009~2014 가 통째로
빠져 있고, 261250 은 횡보(2017~2018)에서만 이기고 나머지 셋에서 전부 진다.

### G5 가 닫은 것 — §15.3 #8 은 통과다

N=1/3/5/7 에서 같은 기간(2017~) 세 경로를 세 기준으로 줄 세웠고 **네 번 다 순위가 같다.**

| 기준 | 순위 (N 네 값 전부) |
| --- | --- |
| 종료 총자산 · Sharpe | 261250 > 환전 2017~ > 261240 |
| **Calmar** | **환전 2017~** > 261250 > 261240 |

**남은 「판정 불가」는 261250 β 하나뿐**이고 그것은 등가성 검증이 답하는 항목이다.
**금액 순위와 Calmar 순위가 서로 다른 것은 별개의 사실**이며 §1.1 이 예고한 그대로다.

### G5 가 답하지 못한 것 — §14 축2 는 판정 불가다

금리차 부호 구간이 **39개인데 31개가 20거래일 미만**이라 독립 사건이 6개뿐이다.
**동률(금리차 0)도 실재한다** — 환전 2005~ 에 13일, 261250 에 11일이다.
사양서의 「(+) 구간만 보면 261250이 과대평가된다」는 방향만 시사한다 —
261250 − 261240 이 (+) 구간에서 +4.06 / +3.11%p 이지만 **(−) 구간 하나에서도 +2.30%p** 다.
**부호별 우열에 통계적 주장을 하지 않는다.**


### §13.3 의 판정은 「진다」다 — 그러나 노출이 4배 다르다

| 실행 | 전략 | 분할매수 후 보유 | 차이 | 전략 투입률 / 벤치마크 투입률 |
| --- | --- | --- | --- | --- |
| 환전 2005~ | 178,494,280원 | **181,588,353원** | **−3,094,074원** | 35.87% / **90.18%** |
| 환전 2017~ | 125,998,702원 | **143,223,758원** | **−17,225,056원** | 19.29% / **83.03%** |
| 261240 | 125,794,274원 | **151,985,641원** | **−26,191,367원** | 19.45% / **84.29%** |
| 261250 | 136,051,353원 | **181,831,735원** | **−45,780,382원** | 18.89% / **84.37%** |

**§13.3 은 "여기서 못 이기면 익절 로직을 제거하는 것이 맞다" 고 적었고 네 번 다 못 이겼다.**
ETF 의 유예 세금(673만~1,236만원)을 물려도 뒤집히지 않는다.

**그러나 같은 것을 재고 있지 않다.** 벤치마크의 MDD 가 −14.41 ~ **−34.36%** 이고 전략은
−3.19 ~ −8.10% 다. 사양서 §1.1 이 "노출이 다르면 CAGR 비교가 무의미하고 **리스크 조정 지표로만**
판단한다" 고 규정한 상황이며, **Calmar 는 전략이 4/4, Sharpe·Sortino 는 3/4** 로 이긴다.
**어느 하나로 결론을 정하지 않는다 — 둘 다 사실이다.**

> **「익절 로직을 제거한다」는 곧 「38회에 나눠 산 B&H 로 바꾼다」는 뜻이다.** 팔지 않으면
> 같은 레벨을 다시 살 수 없어 21년 8개월 동안 **매수가 38번뿐**이고 거래비용이 전략의 34분의 1이다.
> 첫 자금 소진이 **2008-12-18**(전략은 2014-04-10)이고 최장 보유가 **7,896일(21.6년)** 이다.
> 그 선택은 성적이 아니라 **감내 가능성**의 문제다.

### 2017~ 에서는 B&H 에도 진다

| 실행 | 전략 − B&H |
| --- | --- |
| 환전 2005~ | **+6,182,627원** |
| 환전 2017~ · 261240 · 261250 | **−7,904,432 / −17,878,491 / −23,583,175원** |

§13.3 이 "그리드는 **추세장에서 B&H 에 구조적으로 진다.** 이길 수 있는 유일한 환경은 횡보다"
라고 예고한 그대로다. §18.1 이 기간 편향을 경고했는데 **실측은 그보다 나쁘다.**

### 「원화 파킹 100%」와 rf 는 다른 값이다

| | 정의 | 환전 2005~ 종료 |
| --- | --- | --- |
| **rf** (결정 C87) | CD91 **원지표** × (1−15.4%), 하한 없음 | 166,319,600원 (+66.32%) |
| **원화 파킹 100%** (결정 C100) | `max(CD91 − 0.30%p, 0.50%)` 에 15.4% | **157,553,680원 (+57.55%)** |

**8,765,920원(8.77%p) 차이**이며, 상품 스프레드 0.30%p 가 하한 0.50% 보다 크게 작용한다.
그래서 이 벤치마크의 **Sharpe 가 −2.109 로 음수**인데 오류가 아니라 "무위험 금리보다 낮게
굴렸다"는 정확한 측정이다. **두 이름을 섞지 않는다.**

### 이탈 보너스 Red Flag 가 「통과」로 바뀌었지만 그렇게 읽으면 안 된다

| 분모 | 값 | §15.3 판정 |
| --- | --- | --- |
| **총수익 대비** | **24.94%** | **통과** (이자 없을 때 68.24%) |
| **매매 기여분 대비** | **69.1%** | — |

**보너스 자체는 1,589만 → 1,957만원으로 오히려 늘었다**(이자가 총자산을 키워 슬롯이 커졌다).
비중이 내려간 것은 **분모에 이자가 들어왔기 때문**이고, **이자는 종가 체결과 아무 관계가 없는
수익원**이다. 매매가 만든 2,833만원 중 1,957만원이 여전히 지정가 운용이었다면 못 얻었을 몫이다.
**분모 하나만 보면 이 위험이 숨는다** — G5 해석에서 매매 기여분 기준을 함께 쓴다.

### 남은 Red Flag 는 MDD 하나다 (실측은 스펙 §3.8~§3.11)

| 구간 | MDD | 투입률 | 해석 |
| --- | --- | --- | --- |
| 환전 2005~ | **−8.10%** | 35.87% | **이자를 붙여도 달라지지 않았다** (−8.62% → −8.10%). 이자는 노출을 키우지 않는다 |
| 2017~ 세 경로 | **−3.19 ~ −6.13%** | 19% 안팎 | 그 기간 원달러가 대체로 올라 **하향 돌파 자체가 드물었다** |

**원인은 미실현 누락이 아니라 투입률**이며, §17.2 의 기대(−15~−25%)는 훨씬 높은 투입률을
전제한 것으로 보인다. CAGR·총수익률·자산곡선 단조 증가는 통과했고
**Sharpe·Sortino 는 성과 계층이 서야 판정할 수 있다.**

> **261240 의 MDD 는 하루짜리 평가 이상치가 1.93%p 를 만든다.** 2019-03-14 를 빼면
> −5.08% → **−3.15%** 다. 그날 **체결은 0건**인데 종가 +21.98% 가 평가 신고점을 만들어
> 이후 낙폭의 기준이 됐다. 261250 은 그날이 신고점이 아니라 영향이 0 이다.

### 이자가 수익의 3분의 2다 — 사양서 §17.1 이 맞았다

| 항목 | 금액 | 총수익 대비 |
| --- | --- | --- |
| **세후 이자** | **50,163,901원** | **63.9%** |
| 매매 + 환율 레벨 변화 | 28,330,379원 | 36.1% |

- **원화 파킹이 달러 RP 의 3.75배**다 (세전 4,677만 대 1,248만원). 투입률이 낮아서다
- **달러 이자는 §17.1 기대(연 0.7~1.2%)에 미달하는 연 0.42%** 다. 원인은 투입률이지
  이자일수 −1 이 아니다 — 그 몫은 RP 이자의 **0.9%(약 11만원)** 뿐이다
- **RP 하한이 거래일의 48.8% 에 걸리는데도** 0.10~0.70% 사이 총수익률 차이가 4%p 안쪽이다.
  하한이 걸리는 구간이 저금리 시기라 절대 금액이 작다. **두 하한 축 모두 결론을 흔들지 않는다**

### 비용은 궤적을 바꾸지 않고 크기만 깎았다

- **거래비용 7,689,533원인데 총자산은 8,779,054원 줄었다**(이자 없는 기준). 차액은
  **같은 예산으로 더 적은 달러를 사서** 이후 환율 상승분을 덜 먹은 몫이다
- 스프레드 축 네 값에서 **체결 건수가 전부 같다** — 매수 636 / 매도 624 / 미청산 12 / 자금 부족 17일
- **비용률·이자율을 0으로 되돌리면 G2 결과가 원 단위까지 재현된다** (132,064,560원). 회귀 안전망이다

### 사양서 §15.2 필수 검증 — **12개 전부 통과**

#1 무한자금 없음 · #2 미실현 반영 · #3 대기 원화 이자 · #4 ETF 캐리 이중계산 없음 ·
#5 ETF 총보수 이중차감 없음 · #6 정수 주식 수 · #7 재조정 시 슬롯 불변 · #8 하향 돌파 ·
#9 세금 경로별 차등 · #10 RP 이자일수 −1 · #11 격자 고정 · #12 중복 슬롯 없음.

### 세 경로가 같은 기간에서 거의 같다 — 다만 구성이 다르다

| 경로 | 종료 총자산 (2017~) | 실현손익 | 이자(세전) | 세금 | MDD |
| --- | --- | --- | --- | --- | --- |
| 환전 | 125,998,702원 | 10,230,989원 | **20,724,002원** | 3,166,078원 | −3.19% |
| 261240 | 125,794,274원 | **12,774,558원** | 17,375,807원 | **4,982,740원** | −5.08% |
| 261250 | **136,051,353원** | 24,107,012원 | 18,453,392원 | 7,209,768원 | −6.13% |

**261240 − 환전 = −0.20%p** 로 사양서 §17.3 의 기대(연 0.5%p 이내)에 들어온다. 다만
**총액이 비슷할 뿐 수익의 구성이 완전히 다르다** — 환전은 달러 RP 이자와 비과세를 얻고 왕복
0.36% 를 물며, ETF 는 캐리가 종가에 내재돼 실현손익이 크지만 차익에 15.4% 를 문다.
**두 힘이 우연히 상쇄된 것이지 같은 상품이라서가 아니다.**

> **261250 은 이 기간에 Calmar 도 더 좋았다** (0.54 대 0.48). §17.2 의 기대와 반대인데,
> **ETF 데이터에 §14 축1 이 "최악, 전 슬롯 물림"으로 지목한 2009~2014 대세 하락이 없다.**
> 기대가 틀렸다고 읽지 말고 표본이 그 국면을 못 담는다고 읽는다.

### 회전과 보유기간이 사양서 기대를 벗어난다

- **회전**: 전체 29.22회/년(기대 5~15 **초과**) · 슬롯당 1.00회/년(**미달**).
  결정 C10 이 둘 다 산출하게 한 이유가 드러났고 **어느 해석도 §17.2 를 만족하지 않는다**
- **최장 보유 15.8년.** 2009년 3월(원달러 고점) 매수분이 2024-12 에야 팔렸고 얻은 것이 28,322원이다.
  §13.2 의 "5년 물리면 전략 재설계" 기준과 §17.2 의 기대(3~6년)를 크게 넘는다.
  **평균 262일 대 중앙값 20일** — 소수 사건이 평균을 만든다

### 하단 이탈 B안은 발동할 곳이 거의 없다 — 그리고 대가는 MDD 가 아니다

| 경로 | A안 종료 총자산 | B안 종료 총자산 | 연장일 / 최대 칸 |
| --- | --- | --- | --- |
| 환전 2005~ | 178,494,280원 | **179,714,437원** | **327일 / 6칸** |
| 환전 2017~ · 261240 · 261250 | — | **A안과 원 단위까지 같다** | **0일 / 0칸** |

- **연장은 2005~2007 에만 있고 연속 구간 7회뿐**이다. 2008년 이후 19년간 0일이고
  **ETF 기간(2017~)에는 N·g·최소폭 어느 축에서도 0일**이다 (최소폭 15% 의 28일이 유일한 예외)
- **§7 의 손익계산서**: 평균단가 0.695% 개선 · MDD **0.000%p** 악화 → 실패 조건에 안 걸린다
- **그러나 MDD 가 그대로인 것은 B안이 안전해서가 아니다.** 최대 낙폭 구간(2013~2014)에 연장이
  한 번도 없어 두 곡선이 그 구간에서 비례했을 뿐이고, **§14 축1 이 최악으로 지목한
  2009~2014 대세 하락에서 연장이 발동한 표본이 없다**
- **진짜 대가는 현금 소진 시점이다** — 첫 자금 부족일이 **2014-04-10 → 2007-10-31** 로 6년 반
  앞당겨졌다. **MDD 와 소진 시점은 같은 것을 재지 않는다**
- **연장이 직접 만든 몫(34건 1,769,819원)보다 총자산 차이(1,220,158원)가 작다.** 연장이 활성
  레벨을 늘려 기존 슬롯까지 줄이고 원화현금이 줄어 **파킹 이자가 259,514원 줄기** 때문이다.
  **B안은 노출을 늘리는 대신 대기자금 이자를 내주는 거래**다

### Sharpe 가 0.122 다 — 사양서가 미리 예고한 정상 결과다

| rf 정의 | Sharpe |
| --- | --- |
| rf = 0 (**사양서 §13.1 이 금지**) | 0.885 |
| CD91 원지표 세후, 거래일당 하루치 (**틀린 방식**) | 0.369 |
| **CD91 원지표 세후, 달력일 간격만큼** (확정) | **0.122** |

- §13.1 이 "rf를 올바르게 적용하면 Sharpe가 0 근처로 떨어질 수 있으며 **오류가 아니라 정확한 측정**"
  이라고 적었고 그대로 실현됐다. **네 경로 전부 §15.3 의 `Sharpe > 1.0` 에 안 걸리는 것이
  rf 를 제대로 적용했다는 방증**이다 (0.122 / 0.319 / 0.216 / 0.418)
- **세후 CD91 에 그냥 넣어뒀다면 21년 뒤 166,319,585원**이다. 전략은 178,494,280원이라
  **초과분이 12.17%p(1,217만원)** 다. 이 값이 §13.3 벤치마크의 자리를 임시로 대신한다
- **변동성이 1.88~3.48% 로 매우 낮다.** 자금의 60~80%가 원화로 대기하므로 당연하고,
  **Sharpe 의 분모가 작아 rf 차이가 그대로 증폭된다**

### §15.3 Red Flag 에서 걸리는 것은 MDD 하나뿐이다

네 경로 전부 같다 — **6개 통과 · 1개 걸림 · 2개 판정 불가**(N 축은 G5, 261250 β 는 등가성 검증).
걸린 MDD 의 원인은 미실현 손실 누락이 아니라 **투입률**(19~36%)이며, 「자산곡선 단조 증가」가
통과하는 것이 미실현이 들어 있다는 증거다.

### 범위는 대부분의 달에 가만히 있다가 가끔 크게 점프한다

재조정 260회 중 **하단이 안 움직인 달이 174회(67.2%)**, 상단은 182회(70.3%)다. 움직일 때는
하단 최대 4.03% · 상단 최대 16.74% 다. min/max 창이라 **최솟값 자체가 창에서 빠질 때만** 바뀌기
때문이며, 이것이 §13.2 가 「재조정 시 범위 변화량」을 필수 지표로 둔 윈도우 엣지 효과의 실물이다.

---

## 다음 작업 — 사용자가 정한다

### 로드맵에서의 위치

| 단계 | 내용 | 상태 |
| --- | --- | --- |
| G0~G2 | 데이터 확보 · 등가성 검증 · 그리드 엔진 | 완료 |
| G3-1~G3-4 | 거래비용 · 이자·세금 · ETF 경로 2종 · 하단 이탈 B안 | 완료 |
| G4-1·G4-2 | 지표 계층 · 벤치마크 3종 | 완료 |
| **G5** | **견고성 검사(축 8개 33회) · 국면 분할 3축 · 결과 문서** | **완료** |
| — | **계획된 단계가 남아 있지 않다** | — |

### 남은 선택지 셋 — 착수 전에 사용자에게 묻는다

**어느 것도 자동으로 시작하지 않는다.** 셋 다 성격이 다르고 비용도 다르다.

| # | 선택지 | 무엇에 답하나 | 규모 |
| --- | --- | --- | --- |
| **A** | **지정가 대조군 (G6)** | **성적의 매매 기여분 69%가 종가 단일가 체결 가정에 걸려 있다.** 레벨가·목표가로만 체결하면 얼마가 남는가 | 엔진의 매수·매도 계획을 함께 고쳐야 한다. **환전 경로 한정** — ETF 는 원달러 레벨가에 대응하는 지정가가 없다 |
| **B** | **QQQ 와의 상관 측정** | **이 프로젝트의 출발점에 아직 답하지 못했다.** `docs/context/` 의 두 문서가 "QQQ와 다르게 움직이는 매매법을 찾자"에서 시작했는데, 그리드 곡선이 QQQ와 어떻게 움직이는지는 **한 번도 재지 않았다** | 총자산 곡선과 QQQ 수익률의 상관·국면별 분해. **새 매매 규칙이 필요 없다** |
| **C** | **다른 검증으로 넘어간다** | ROADMAP 검증 대상 중 **#2 비트코인 4년 주기설 · #3 정배열 매매법 · #4 탑티어 우량주 반토막**이 미착수다 | 새 트랙. `studies/` 의 이벤트 정의부터 |

> **B 를 권한다.** A 는 그리드의 정밀도를 올리는 일이고 C 는 새 트랙인데, **B 는 이미 만든
> 곡선 하나로 프로젝트의 출발 질문에 답한다.** 결과 문서 §2.7 이 "아직 답하지 못했습니다" 로
> 열어 둔 자리이며, 그 답이 없으면 **그리드를 채택할지 판단할 근거가 하나 빈다.**

### 사용자에게 남은 판단 — 코드 작업이 아니다

- **이 규칙을 운용할 것인가.** 결과 문서 §2.7 이 판정을, §4 가 한계를 정리했다.
  **채택 판단에 필요한 것은 성적이 아니라 감내 가능성**이다 — 익절 로직을 빼면(=분할매수 후 보유)
  MDD −34.36% 와 최장 보유 21.6년을 견뎌야 하고, 그대로 두면 이자 위주의 연 2.75% 를 받는다
- **`docs/plans/` 를 비울 것인가.** 계획서 12건이 Done 이고 **근거 승격이 전부 끝났다.**
  지울 때 **`.gitkeep` 을 되살린다** — 디렉토리가 사라지면 계획서 게이트 훅이 조용히 꺼진다

### 다시 논의하지 말 것 — 확정된 결정

전문은 `docs/spec/usdkrw_grid.md` §4 에 있다. 남은 조각에 직접 걸리는 것만 옮긴다.
**§4.11 의 C105~C118 이 가장 최근에 확정된 것들**이다.

| # | 확정 |
| --- | --- |
| **C105** | **축 검사는 환전 2005~ 에 돌리고 N 축만 네 실행 전부에** 돌린다. 고유 33회 |
| **C106** | **환전 스프레드는 사양서 §12 에 없는 추가 축**이다. 표에 출처를 표기한다 |
| **C107** | **§14 축1 은 사양서 8구간과 연속 분할을 함께** 낸다. 원문은 겹치고 빈다 |
| **C108** | **연속 분할의 경계는 연 단위다.** 고점·저점으로 자르면 §15.1 의 「사후 구간 선택 금지」에 걸린다 |
| **C109** | **구간을 자를 때 직전 거래일을 앵커로 포함한다.** 없으면 경계일 수익률이 조용히 사라진다 |
| **C110** | **§14 축2 의 부호는 원지표 DTB3 − CD91 이고 동률은 별도 칸**이다 |
| **C111** | **§14 축2 는 연속 구간 단위**로 내고 구간 수·길이 분포를 항상 병기한다 |
| **C112** | **금리 원지표를 `RateSeries` 가 함께 낸다.** `rp` 에서 T-bill 을 역산할 수 없다 |
| **C113** | **`GridOutputs` 가 `rates` 를 싣는다.** 국면 지표가 그 실행이 쓴 rf 를 쓴다 |
| **C114** | **한 스크립트 한 실행에서 축과 국면을 함께** 낸다. 대조의 전제가 "같은 코드·같은 데이터"다 |
| **C115** | **견고성 스크립트에 인자가 없다.** 골라 돌리면 안 돌린 축이 산출물에서 안 보인다 |
| **C116** | **사양서 원본을 보존하고** 결과 문서를 자립적으로 새로 썼다 (결정 A3 의 실행 방법) |
| **C117** | **지정가 대조군을 G5 에 넣지 않았다.** 크기는 실측됐고 대조군이 더하는 것은 복리 효과다 |
| **C118** | **결론 뒤집힘은 불리언의 「전 축값에서 같은가」로만** 낸다. 크기로 줄 세우면 좋은 값 고르기가 된다 |
| **C97** | **B&H 의 첫날 매수에는 슬리피지가 붙지 않는다.** 돌파 판정이 없는 집행이라 결정 C60 과 같은 논리다 |
| **C98** | **벤치마크는 실행 중인 경로를 따라간다.** 엔진에도 벤치마크에도 경로 분기가 없다 |
| **C99** | **벤치마크 3종을 매 실행에 병기한다.** §13.3 의 「필수 병기」를 실행 습관에 맡기지 않는다 |
| **C100** | **「원화 파킹 100%」는 실수령 파킹 금리다.** rf(원지표 세후)와 **다른 값**이며 이름을 나눈다 |
| **C101** | **「분할매수 후 보유」는 엔진의 매도 스위치다.** 매수 규칙이 같음이 구조로 보장된다 |
| **C102** | **이자일수·월말 판정은 엔진이 공개한 함수를 벤치마크가 그대로 부른다.** 복제하면 조용히 갈라진다 |
| **C104** | **곡선 비교표(`benchmarks.csv`)를 남긴다.** 요약값 하나로는 "질 때 얼마나 지는가"에 답할 수 없다 |
| **C11** | **「분할매수 후 보유」 벤치마크는 그리드와 같은 레벨·같은 금액으로 사되 매도만 하지 않는다.** 매수 규칙이 같아야 익절만 분리된다 |
| **C87** | **rf = CD91 원지표 × (1 − 15.4%).** 세전도, 실수령 파킹도 아니다 — 곡선이 세후라 같은 기준이어야 한다 |
| **C88** | **rf 는 `연율/365 × 달력일 간격` 으로 붙는다.** 거래일 균등 분배면 Sharpe 가 3배가 된다 |
| **C89** | **Sortino 의 하방편차 분모는 전체 관측 수다.** 하방 개수로 나누면 방향이 직관과 반대로 틀린다 |
| **C91** | **계산 불가는 `None` 이다.** 0·무한대·`NaN` 은 전부 다른 뜻으로 읽히거나 조용히 통과한다 |
| **C92** | **연환산은 250 거래일**(지표), **일할은 365 달력일**(이자·rf). 두 계수는 서로 다르다 |
| **C93** | **최장 보유기간에 미청산 슬롯을 포함한다.** 아직 안 팔린 것이 더 위험하다 |
| **C94** | **표준 지표 계층은 `strategy.grid` 를 import 하지 않는다.** 테스트로 고정돼 있다 |
| **C95** | **§15.3 의 판정 불가를 통과로 적지 않는다.** 아홉 개를 전부 남긴다 |
| **C78** | **B안의 연장은 다음 재조정까지 유지된다** — 직전 재조정 이후의 누적 최저 종가이며 재조정일에 초기화된다 |
| **C79** | **연장 경계는 당일 종가를 감싸는 레벨까지**다. 체결 결과는 같고 활성 레벨 수가 한 칸 달라진다 |
| **C80** | **연장 구간의 배수는 하단부 배수(1+차등)** 다. 위치가 음수면 3구간 판정이 이미 하단부로 보낸다 |
| **C81** | **위치·배수는 정식 범위로 잰다.** 연장 하단으로 재면 3구간 경계가 통째로 이동한다 |
| **C82** | **연장 하단은 범위 계층이 가격으로, 격자 스냅은 격자 계층이** 한다. 범위 산출에 익절폭 g 를 끌어들이지 않는다 |
| **C83** | **하단 이탈 A/B 는 인자로 가른다.** 축별 단독 검사 25회에 들어가지 않는다 |
| **C84** | **평균단가 = 투입 원화 ÷ 취득 단위** (비용 포함, 미청산 포함). 엔진이 매수마다 누적한다 |
| **C85** | **MDD 는 성과 계층(G4)까지 임시 계산으로 낸다.** §3.12 의 값이 G4 의 회귀 대조군이다 |
| **C86** | **결정 C39 는 B안에서도 성립한다.** 연장 레벨도 하향 돌파로만 사므로 판정식이 그대로다 |
| **C67** | **판정 가격과 집행 가격을 나눈다.** 격자·범위·하향 돌파·목표가는 원달러 종가, 체결·평가는 경로의 가격 |
| **C69** | **보유 중 이자율을 경로가 답한다.** ETF 는 언제나 0 — 캐리가 종가에 내재돼 있다 |
| **C70** | **ETF 실효 총비용을 차감하지 않는다.** 비용은 위탁수수료 + 슬리피지뿐이다 |
| **C71** | **정수 주식 수의 잔액은 현금으로 남는다.** 다음 레벨로 넘기지 않는다 |
| **C73** | **2019-03-14 이상치를 그대로 둔다.** 체결 기여는 0 이지만 MDD 를 1.93%p 악화시킨다 |
| **C74** | **ETF 경로의 거래일은 그 종목의 개장일로 좁힌다.** 다만 월평균·범위는 원달러 전 기간으로 계산한 뒤 좁힌다 |
| **C75** | **경로는 한 번에 하나씩 실행한다.** 비교는 곡선 하나만 받는 성과 계층의 일이다 |
| **C57** | **이자를 하루 처리 순서의 맨 앞에 둔다** — 이자 → 월말 정산 → 매도 → 평가 → 배분 → 매수. 이 순서라야 이자일수 −1 이 **미래를 보지 않고** 성립한다 |
| **C58** | **이자 정산은 「다음 달 첫 거래일」에 전월분을 인출한다.** 「마지막 거래일」은 다음 행을 봐야 알 수 있어 look-ahead 감시가 깨진다 |
| **C60** | **월말 RP 이자 환전에는 환전 스프레드만 붙는다.** 슬리피지는 돌파 판정과 종가의 차이라 정기 환전에는 성격이 없다 |
| **C61** | **원화 파킹 이자는 15.4% 과세하되 이자일수 −1 을 적용하지 않는다.** −1 은 RP 의 상품 특성이라 **연속 잔고인 원화에 옮길 근거가 없다** |
| **C62** | **이자 세율은 상수다.** 법정 세율이라 검사 축이 아니다. **ETF 차익도 같은 15.4%** 라 상수 하나가 둘 다 덮는다 |
| **C63** | **원천징수의 과세 기준은 세전 이자다.** 환전 비용은 과세 대상에서 빼지 않는다 |
| **C64** | **미인출 이자는 총자산에 들어가되 매수에는 쓸 수 없다.** 실제 계좌의 미지급 이자가 그 성질이다 |
| **C65** | **금리 원지표가 없는 날은 전일값을 이월하고 건수를 반환한다.** 달력 첫날 이전에 값이 없으면 **거부한다** |
| **C66** | **파킹 이자의 기준 잔고는 전 거래일 마감 원화현금이다.** 첫 거래일은 이자가 없다 |
| **C46** | **설정 객체는 용도별로 나눈다.** `CostConfig` 를 `GridConfig` 가 들고 있으며, 이자·세금도 같은 형태로 붙인다 |
| **C47** | **경로 3종은 집행 경로 인터페이스로 넣는다.** 엔진은 격자·범위·배분·판정만 하고 「슬롯 하나를 사고 판다」는 경로가 책임진다 |
| **C48** | **비용은 슬롯 금액(예산) 안에서 나간다.** 실제 지출 = 예산이고 비용만큼 적게 산다 |
| **C49** | **환전 스프레드는 원화 금액에 곱한다.** 매매기준율 계열을 읽지 않는다 — 어느 쪽에 곱하든 차이가 명목의 0.000262% 다 |
| **C50** | **슬리피지는 체결가를 밀지 않고 비용 금액으로 처리한다.** 밀면 이탈 보너스의 기준선이 흔들린다 |
| **C51** | **이탈 보너스는 비용 전 명목으로 잰다.** 지정가 운용도 같은 비용을 물기 때문이다 |
| **C52** | **평가에 비용을 적용하지 않는다.** 매일 차감하면 미실현 손실이 과대계상된다 |
| **C54** | **엔진은 경로가 돌려준 지출액으로 현금을 움직인다.** 배정된 예산을 그대로 빼지 않는다 |
| **C35** | **환전 스프레드 편도 0.08%** (C2 의 0.10% 를 대체). 실계좌 실측에서 **기본 스프레드가 1.0% 가 아니라 0.783%** 임이 확인됐다. 우대율은 90% 유지. 왕복 총계 **0.36%** |
| **C36** | **g 검사 범위는 4개 그대로.** 이론 최적이 0.72% 로 내려왔지만 g=0.8% 가 이론 수익의 99.0% 를 유지한다. 다만 **4개가 전부 최적의 위쪽**이라 단조 감소만 보인다 |
| **C18** | ETF 비용의 판정 기준은 **측정한 실효 총비용**(261240 +0.10%, 261250 −0.05%). 공시 총보수는 교차확인용 |
| **C12** | §16 이론값의 금리는 **원지표**(DTB3·CD91). RP 모델은 실수령 금리라 등가성 검증에 넣지 않는다 |
| **C7** | RP 이자는 일별 발생분을 **총자산에 즉시 반영**, 세금은 지급(월말 인출) 시 원천징수 |
| **C8** | 종료 시점 미청산 슬롯은 **세전 평가.** 환전 경로가 비과세라 ETF 가 유리해 보이는 편향은 결과 문서 한계에 적는다 |
| **C14** | 이자는 **365일 일할**, 마스터 달력은 원달러 고시일. 미국 휴일은 T-bill 전일값 이월 |
| **C6** | 하단 이탈 B안은 당일 종가를 포함하는 칸까지 연장, 배수는 하단부 1.5 고정, 재조정에서 비활성 |
| **C19** | 일별 시가평가는 **정규장 종가**. 매매기준율로 평가하면 매수 즉시 익절폭의 41% 가 유령 손익으로 붙는다 |
| **B1** | 매매법의 공통 산출물은 **일별 총자산 곡선**. 표준 지표는 곡선 하나만 받는 함수에서 계산한다 |

### 이 트랙에서 이미 밟은 함정 — 다시 밟지 말 것

**전부 예외가 나지 않고 결과만 조용히 틀리는 종류다.**

| 함정 | 무슨 일이 났나 |
| --- | --- |
| **매매기준율의 1일 고시 시차** | 매매기준율(D)은 **D−1 의 시장**을 담는다. 모르고 쓰면 ETF 와의 상관이 **−0.03** 으로 나온다 |
| **이론값 식** | 261240 은 `현물 + 달러금리` 를 복제한다(알파 −0.01%). 사양서 §16.1 의 `현물 + 금리차` 는 **+2.13%** 로 어긋난다 |
| **NAV 분배금 조정** | NAV 를 원본 그대로 쓰면 분배락이 비용으로 잡혀 **값이 두 배**가 된다 |
| **레버리지 상품의 담보 구조** | 2배 상품은 2배 노출을 **1배 담보**로 굴린다. 반영하지 않으면 **원화금리 전액이 비용으로 둔갑**한다 |
| **사양서 산식을 문자 그대로 옮기기** | 최소폭 판정 `상단/하단 − 1 < 폭` 은 폭이 정확히 임계값일 때 **오발동**한다(검사 범위 4개 중 2개에서 실제 발생). 격자 경계의 `ceil(log(·))` 도 g=0.008 에서 **80개 중 28개**가 밀린다. 곱셈·직접 비교로 바꾼다 (결정 C22·C28·C33) |
| **최소폭 강제를 예외 장치로 오해하기** | 기본 설정에서 재조정 260회 중 **153회(59%)** 가 강제된 범위이고 **2013~2021 은 108회 전부**다. 「원달러 변동폭에 대한 사전 지식」이 그 10년의 범위를 통째로 정했다 |
| **g 축을 단일 축으로 읽기** | g 를 올리면 슬롯 상한이 지배한다(1.2% 에서 66%, 2.4% 에서 92% 발동). g 축 결과에는 **익절폭·상한 발동·차등 소멸·현금 잔류**가 함께 들어 있다 |
| **연환산 계수** | 일할은 365 달력일, 연환산은 250 거래일이라 실제 거래일 밀도(약 261일)와 몇 % 어긋난다. 250 은 사양서가 정한 값이라 **오차가 사실**이다 |
| **미래를 요구하는 규칙을 계산식으로 풀기** | 「이자일수 = 보유일수 − 1」은 매도일을 알아야 하고 「매월 마지막 거래일」은 다음 행을 봐야 한다. **둘 다 처리 순서로 풀린다** — 이자를 매도·매수 앞에 두고, 월말을 「다음 달 첫 거래일」로 바꾼다 (결정 C57·C58) |
| **지표의 분모를 하나만 내기** | 이자가 붙자 이탈 보너스 비중이 68.2% → 24.9% 로 떨어져 Red Flag 를 통과했다. **이자는 종가 체결과 무관한 수익원**이라 그 통과가 위험을 숨긴다. 분모를 여러 개 내야 무엇을 재고 있는지가 보인다 |
| **체결이 없으면 영향도 없다고 읽기** | 2019-03-14 은 **체결 0건**인데 평가가 +21.98% 튀며 신고점을 만들어 **261240 의 MDD 를 1.93%p 악화**시켰다. 이상치는 체결을 거치지 않고도 지표에 들어온다 |
| **총액이 비슷하면 같은 상품이라고 읽기** | 환전과 261240 은 같은 기간 총자산이 0.2%p 차이지만 **구성이 완전히 다르다** — 이자·세금·비용이 서로 반대 방향으로 상쇄됐을 뿐이다. 어느 한쪽 가정이 바뀌면 갈라진다 |
| **비율 지표로 규모 차이를 재려 하기** | B안의 MDD 가 A안과 **소수 여섯째 자리까지 같은데**(−8.103851%) 현금 소진은 **6년 반 앞당겨졌다.** 최대 낙폭 구간에 연장이 없어 두 곡선이 그 구간에서 비례했기 때문이다. **MDD 만 보면 B안의 대가가 통째로 안 보인다** |
| **「규정이 있으니 구현할 게 있다」고 읽기** | 결정 C6 의 「다음 재조정에서 비활성화」는 **매일 재계산하면 죽은 문장**이 된다. 종가가 반등하는 순간 저절로 꺼져 그 규정이 아무 일도 하지 않는다. 실측 차이는 **연장일 327일 대 261일** — 규정이 무엇을 막으려는지 먼저 따져야 구현이 정해진다 |
| **`floor(log(·))` 로 감싸는 칸 찾기** | `active_level_indices` 가 밟았던 결정 C22 의 함정이 그대로 재현된다. 가격이 레벨가와 정확히 같으면 한 칸 밀리고, **연장 하단이 한 칸 밀리면 슬롯 금액의 분모가 조용히 어긋난다.** 직접 비교로 보정한다 |
| **rf 를 거래일마다 하루치씩 주기** | 두 거래일 사이가 주말이면 3일인데 하루치만 주면 rf 가 3분의 2로 깎여 **Sharpe 가 0.122 대신 0.369 로 나온다.** 착수 전 실측에서 실제로 한 번 틀렸다. 이 달력의 평균 간격은 **1.48일**이다 (결정 C88) |
| **두 연환산 계수를 섞기** | 지표는 **250 거래일**, 이자·rf 는 **365 달력일**이다(결정 C9·C14). 임시 계산이 회전을 달력 연수로 나눠 28.9회/년이 나왔고 정식 계산은 **29.22회/년**이다. 판정은 안 바뀌었지만 **어느 계수를 썼는지 적지 않으면 다음 사람이 재현하지 못한다** |
| **계산 불가를 0 이나 `NaN` 으로 답하기** | Calmar 0 은 "위험 대비 수익이 없다", 보유기간 0 은 "하루 만에 판다"로 읽힌다. **`NaN` 은 더 나쁘다** — 비교·정렬·집계를 전부 조용히 통과해 요약에서 빈칸으로만 보인다. 실제로 표본 표준편차(ddof=1)가 수익률 1개에서 `NaN` 을 뱉는 것을 구현 중에 잡았다 |
| **판정 불가를 통과로 적기** | §15.3 의 아홉 개 중 둘(N 축·261250 β)은 한 실행으로 알 수 없다. 빼 버리면 **"검사했는데 괜찮았다"와 "애초에 검사하지 않았다"가 구분되지 않는다** (결정 C95) |
| **금액 비교로 노출 차이를 지우기** | 「분할매수 후 보유」가 네 번 다 이기는데 **투입률이 83~90% 대 19~36%** 다. §13.3 은 금액으로 묻고 §1.1 은 노출이 다르면 리스크 조정 지표로만 보라고 한다 — **어느 하나만 인용하면 반대 결론이 나온다.** Calmar 는 전략 4/4, Sharpe 는 3/4 다 |
| **rf 와 「원화 파킹 100%」를 같은 것으로 읽기** | rf 는 CD91 **원지표** 세후(하한 없음)이고 벤치마크는 **실수령 파킹 금리**(하한 있음, 스프레드 −0.30%p)다. 21년 기준 **8,765,920원(8.77%p) 다르며** 그래서 그 벤치마크의 Sharpe 가 −2.109 다 |
| **가공된 값에서 원지표를 역산하려 하기** | `rp = max(T-bill − 계단 스프레드, 하한)` 은 **되돌릴 수 없다.** 하한이 걸리는 날이 거래일의 48.8% 라 그 구간은 정보가 통째로 사라진다. 읽는 쪽이 파일을 다시 열면 달력 정렬과 이월이 두 곳으로 갈린다 (결정 C112) |
| **구간을 날짜로만 자르기** | 앵커가 없으면 **구간 첫날의 수익률이 어느 구간에도 안 들어간다.** 예외가 나지 않고 구간이 많을수록 잃는 몫이 커진다. 「구간별 수익률 개수의 합 = 전체 개수」를 테스트로 고정했다 (결정 C109) |
| **부호 구간 39개를 독립 표본 39개로 읽기** | **31개가 20거래일 미만**인 전환기의 깜빡임이고 60거래일을 넘는 것은 6개뿐이다. **1일짜리 구간은 수익률이 0개라 지표가 정의되지 않는다** — 표준편차가 `NaN` 을 뱉는다 (결정 C111) |
| **결론이 안 흔들린다고 바로 읽기** | 「분할매수 후 보유를 이긴다」가 **여덟 축 중 셋에서 부호가 바뀐다.** 나머지 다섯 축만 보고 「안정」으로 적으면 판정이 뒤집힌 사실이 통째로 사라진다 |
| **뒤집힌 축값을 「좋은 값」으로 옮기기** | N=7·g=1.6% 에서 이긴다는 사실은 **§15.1 의 「결과를 보고 파라미터 변경 금지」에 걸린다.** 차이가 총자산의 0.4~3.2% 라 **표본이 그 지점을 특정하지 못한다**는 뜻이다 |
| **사양서의 국면 예상을 사실로 읽기** | §14 가 "최악 — 전 슬롯 물림" 으로 지목한 2009~2014 가 **전략이 가장 크게 이긴 구간(+21.46%p)** 이다. 예상은 예상이고 실측이 사실이다 |
| **국면 표를 하나만 내기** | 사양서 8구간의 2008~2009 −9.82%p 가 연속 분할에서 **2008 단독 −20.24%p** 로 갈린다. 2009년의 하락이 2008년의 급등을 절반쯤 상쇄한 것이며, **한 표만 보면 그 상쇄가 안 보인다** |
| **벤치마크에 그리드와 같은 비용을 무는 것이 당연하다고 읽기** | 슬리피지는 **돌파 판정과 종가의 차이**를 흡수한 값인데 B&H 에는 돌파 판정이 없다. 결정 C60(월말 이자 환전)이 이미 같은 이유로 갈라 놨다 (결정 C97) |

### 남아 있는 미해결

> **전문은 `docs/spec/usdkrw_grid.md` §5 가 SoT다.** §5.1 이 열려 있는 것, §5.2 가 해소된 것이며,
> 해소된 항목도 취소선으로 남아 있다 — "이미 논의했다" 는 사실이 같은 길을 다시 걷지 않게 한다.

| 항목 | 상태 |
| --- | --- |
| **§13.3 의 판정이 파라미터에 의존한다** | **여덟 축 중 셋(N·g·차등)에서 부호가 바뀐다.** 뒤집히는 지점의 절대금액이 45만~572만원(총자산의 0.4~3.2%)이다. **닫히지 않는다** — §15.1 이 결과를 보고 파라미터를 바꾸는 것을 금지했고, 흔들림은 **표본이 그 지점을 특정하지 못한다**는 신호다 |
| **격자 이탈 보너스가 매매 기여분의 69.1%** | **종가 단일가 체결이 실제로 가능한지에 성적의 상당 부분이 달려 있다.** 크기는 1,957만원으로 실측됐고 **지정가 대조군은 만들지 않았다** (결정 C117) — 다음 단계 선택지 A 다 |
| **슬리피지 편도 0.10% 가 실측되지 않았다** (결론은 흔들지 않는다) | 환전 왕복비용의 **56%** 이며 §6.6 의 가정이다. **다만 스프레드 0.039~0.100% 전부에서 결론이 같다** — 크기는 이 가정에 달려 있고(폭 420만원) 판정은 아니다 |
| **21년 백테스트에 오늘의 우대율을 소급하고 있다** | 토스증권은 2021년 출범이다. **환전 경로 비용이 전 기간에 걸쳐 낙관적**이며 결과 문서 §4.1 에 적혀 있다 |
| **전략이 하락장에서 크게 이기고 급등장에서 크게 진다** | 2009~2014 **+21.46%p**, 2008 **−20.24%p**. 전 기간 −309만원은 그 둘의 합이다. **닫히지 않는다 — 구조다.** 다만 **매매의 성과가 아니라 투입률의 성과**다 |
| **ETF 표본에 전략의 최선 국면이 없다** | 2009~2014 가 통째로 빠져 있다. §5.1 이 「최악 국면이 없다」로 적었던 것이 **「최선 국면이 없다」로 뒤집혀 확인됐다** — 같은 사실의 두 면이다 |
| **§14 축2 의 독립 사건이 6개뿐이다** | 부호 구간 39개 중 31개가 20거래일 미만이다. **부호별 우열에 통계적 주장을 하지 않는다** |
| **하단 이탈 B안의 표본이 2005~2007 하나뿐이다** | 연장 327일이지만 **연속 구간은 7회**이고 전부 그 구간에 몰려 있다. **B안의 우열에 통계적 주장을 하지 않는다** |
| **2017~ 에서는 B&H 에도 진다** | 환전 2005~ 만 이긴다(+618만원). §13.3 이 "추세장에서 구조적으로 진다" 고 예고했고 그 구간이 상승장이다 |
| **QQQ 와 다르게 움직이는지 아직 모른다** | **이 프로젝트의 출발 질문인데 이 트랙이 한 번도 재지 않았다.** 결과 문서 §2.7 이 "아직 답하지 못했습니다" 로 열어 뒀다 — 다음 단계 선택지 B 다 |
| DTB3 가 할인율이라 달러금리를 과소평가한다 (연 0.1% 수준) | **알려진 한계.** FRED 가 할인율만 제공한다 |

**G5 에서 해소된 것 넷.**
① **§15.3 #8 이 통과다** — N 네 값에서 세 기준 모두 경로 순위가 그대로다. 남은 판정 불가는 261250 β 하나뿐이다.
② **최소 범위폭이 결론을 만들지 않는다** — §3.5 가 가장 중요한 견고성 검사로 지목했는데
15/20/25/30% 전부에서 결론이 같고 종료 총자산의 폭이 325만원(1.8%)이다.
③ **비용 가정도 결론을 만들지 않는다** — 스프레드 세 값 전부에서 같다.
④ **슬롯 상한 축의 세 칸이 같게 나오는 것은 버그가 아니다** — 기본 g 에서 8/10/12% 가 전부 미발동이다.

**그 앞에서 해소된 것 다섯**(G4 까지): §17.2 의 「연 회전 5~15회」가 어느 해석도 아니라는 것(전체
29.22 / 슬롯당 1.00회/년) · 하단 이탈 B안이 결정 C39 를 깨지 않는다는 것 · 이탈 보너스 분모 셋과
rf 정의 확정 · 벤치마크 3종이 「그래서 매매할 가치가 있나」에 답했다는 것 ·
「원화 파킹 100%」와 rf 가 8.77%p 다른 값이라는 것.

---

## 검증 #1 에서 확정돼 계속 유효한 것

**다시 논의하지 말 것.** 근거는 ROADMAP 의 계층 간 계약 다섯 절에 있다.

- **공통 계층(`measure/`·`report/`)은 검증이 바뀌어도 바뀌지 않는다**
- **`studies` 가 공통 계층에 넘기는 것은 bool Series 하나**다. 다만 **검증 #5 는 이벤트가 없어
  `measure/` 를 쓰지 않았고**, **그리드는 상태 구동이라 `studies` 를 아예 거치지 않는다**
- **조합 순회와 조립은 `runner.py`, `scripts/` 는 인자 파싱·호출·표시·저장만**
- **가격 기준은 원본가, 소수 4자리.** 자릿수는 `common_constants.PRICE_DECIMALS` 한 곳에서 정한다.
  단 **그리드용 ETF 는 수정 종가**를 본검증 기준으로 쓴다 (사양서 §11.3)
- **look-ahead 감시 테스트를 산출 전부에 건다** (`tests/conftest.py` 의 공용 헬퍼).
  **상태 구동이라도 예외가 아니다** — 상태는 판정일까지의 체결로만 쌓인다
- **표본 부족 판정선은 유효 표본 10건 미만.** 순열 검정 단독, 반복 1,000회·시드 0
- **유의 비율로 판정한다.** 개별 칸의 p값을 근거로 쓰지 않는다

---

## 반드시 지킬 것

- **모든 코드 변경 전에 계획서를 쓴다.** 예외는 오타·주석·로그 메시지뿐이다.
  절차의 SoT 는 `/verify-plan` 스킬이다. `docs/plans/` 가 비어 있으면 `.gitkeep` 이 있어야
  계획서 게이트 훅이 살아 있다
- **스크립트는 AI 가 직접 실행한다** — 데이터 수집도 포함이다.
  다만 **같은 데이터를 이유 없이 다시 받지 않으며**, 덮어썼으면 새 데이터 기준일을 결과 문서에 남긴다
- **공통 계층을 미리 일반화하지 않는다.** 두 번째 사용처에서 재사용되는지 확인한 뒤 확정한다
- **`reference/` 는 읽기 전용이다.** 읽고 이해한 뒤 `src/verify_lab/` 에 새로 작성한다
- **`docs/context/` 는 사용자 소유 문서다.** 오탈자를 발견해도 고치지 말고 언급만 한다
- **Git 처리는 사용자가 한다.** 커밋·푸시하지 않는다
- 설명은 초보자도 이해할 수 있는 쉬운 한국어로. 전문 통계 용어 대신 구체적인 날짜·가격·표를 쓴다

---

## 실행 환경 주의

품질 검증이 **세 항목 모두 실패**하면서 `Command not found` 가 보이면 코드 문제가 아니다.
`poetry env info --path` 가 프로젝트 `.venv` 를 가리키는지 먼저 확인한다.
증상과 대처는 `docs/ROADMAP.md` Phase 0 의 실측 기록에 있다.

- **pykrx 는 로그인 시 로그인 ID 를 표준 출력에 찍는다.** 로그를 문서에 붙일 때 그 줄을 뺀다
- **ECOS 는 인증키가 URL 경로에 들어간다.** 요청 URL 을 로깅·기록할 때 반드시 마스킹한다
- **`storage/results/` 는 git 에서 제외**돼 세션이 바뀌면 없을 수 있다.
  필요하면 `docs/COMMANDS.md` 로 다시 돌린다. 저장된 데이터가 그대로면 결과도 그대로다
- **`collect_yfinance.py` 에 `--adjusted` 를 붙이지 않는다.** 같은 파일명에 덮어써져 본검증 데이터를 잃는다
- `data.krx.co.kr` 이 일시적으로 DNS 해석에 실패하는 일이 있다. 잠시 뒤 재시도하면 된다
