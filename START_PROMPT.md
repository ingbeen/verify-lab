# 새 세션 시작 프롬프트

> 이 파일은 verify-lab 작업을 이어받는 세션에 붙여넣을 프롬프트입니다.
> **작업이 진척되면 이 파일도 함께 갱신합니다.** 낡은 진입점은 없는 것보다 나쁩니다.

---

verify-lab 프로젝트를 이어서 진행한다. 설계는 확정돼 있고, **데이터 확보(Phase 1)·공통 계층(Phase 2)·
이벤트 정의(3-a)·실행 계층(3-b)까지 끝났다.** 측정은 이미 다 돌아갔고 산출물도 나와 있다.
남은 것은 **3-c 결과 문서 하나**다. **코드를 새로 쓸 일은 없다** — 3-c 는 산출물을 읽고 해석해
`docs/research/RESEARCH_index_extreme_events.md` 를 쓰는 일이다.
아래 "다음 작업"에 범위·검증 기준·이미 확정된 것이 정리돼 있다.

## 먼저 읽을 것 (순서대로, 전부)

1. `CLAUDE.md` — 프로젝트 규칙. 특히 "이 프로젝트가 무엇인가"와 "측정의 원칙" 8개 항목
2. **`docs/context/README.md`와 그 폴더의 두 문서** — 내 현재 투자 상태이자 이 프로젝트가 시작된 이유.
   가장 중요한 문서다. 검증 결과를 해석할 때 이 맥락이 없으면 쓸모없는 보고가 된다
3. `docs/ROADMAP.md` — **진행 상태의 SoT.** 무엇이 끝났고 무엇이 남았는지는 여기가 기준이다.
   **계층 간 계약 다섯 절**("확정된 forward return 반환 계약"·"확정된 베이스라인·통계 계약"·
   "확정된 출력 계약"·"확정된 이벤트 정의 계약"·"확정된 실행 계층 계약")이 **산출물의 규격**이므로
   반드시 읽는다. Phase 1의 "확정된 원시 시세 저장 규칙"도 함께 본다
4. `docs/spec/index_extreme_events.md` — 첫 검증의 확정 설계. **이미 논의로 확정된 내용이므로
   설계를 다시 제안하지 말 것.** 특히 §7 "확정된 설계 결정" 16건, §8 "사전 실측 기록"과
   "검증 실행 계층 재현", 그리고 §12 "보고 형식"
5. **`docs/research/CLAUDE.md`** — 결과 문서의 형식 SoT. 이번 작업이 쓸 문서의 규격이다
6. `src/verify_lab/CLAUDE.md` — 계층 분리, 상수 관리, **측정 계층의 절대 원칙 5가지**,
   그리고 확정된 경로 기준점·컬럼 스키마 규약
7. `.claude/rules/python.md` — 코딩 표준. `.py` 파일을 읽으면 자동 주입되지만,
   **기존 파일을 열지 않고 새 파일부터 만들 때는 주입되지 않으니 직접 열 것**
8. `scripts/CLAUDE.md`, `tests/CLAUDE.md`

나머지 문서와 `reference/`의 참고 자료가 어디 있는지는 `docs/INDEX.md`에 지도로 정리돼 있다.
**문서를 추가하면 INDEX에 등록해야 한다** — `tests/test_index.py`가 검사하므로 빠뜨리면
`validate_project.py`가 실패한다.

## 현재 상태

**되어 있는 것**

- 폴더 구조, 설정 파일, 하네스(`.claude/`의 훅·규칙·`/verify-plan` 스킬), 규칙 문서 전체
- 설계·맥락 문서 — `docs/context/`, `docs/ROADMAP.md`, `docs/spec/index_extreme_events.md`
- 문서 지도 `docs/INDEX.md`와 부패 검사 `tests/test_index.py`
- **`poetry install` 완료** — 프로젝트 내부 `.venv` 사용
- **`src/verify_lab/common_constants.py`** — 경로 상수와 시세 스키마 컬럼 상수
- **`src/verify_lab/utils/` 4종** — 로거·포맷팅·CLI 헬퍼·메타 관리
- **`src/verify_lab/data/loader.py`** — 모든 시세 로딩의 단일 통로.
  스키마·정렬·중복·이상치를 검사하고 **보간 없이 즉시 예외**를 던진다.
  이상치 판정 함수는 수집기도 같이 쓴다(판정식 단일화)
- **수집 경로 2종** — `yfinance_collector.py`(미국)와 `pykrx_collector.py`(국내), 그리고 각각의 CLI.
  둘 다 결과를 좌우하는 인자를 **명시적으로** 넘기고, 검증을 통과한 데이터만 저장한다
- **`src/verify_lab/data/krx_credentials.py`** — `.env` → 환경 변수. `import pykrx` 자체가 로그인을
  시도하므로 **수집 모듈이 그 순서를 소유한다**(`_import_pykrx_stock`). 순서는 테스트로 고정돼 있다
- **데이터 확보 완료** — 아래 세 파일. 재수집하면 행 수·기간이 달라지므로 최신 값은 파일과
  `storage/results/meta.json`을 본다

  | 파일 | 가격 기준 | 대략 |
  | --- | --- | --- |
  | `storage/market/QQQ_max.csv` | 수정주가, 전 기간 | 6,896행 |
  | `storage/market/069500_max.csv` | **원본가**, 상장일부터 전 기간 | 5,881행 |
  | `storage/market/069500_adjusted_max.csv` | **수정주가**, KRX 가 주는 최근 구간 | 2,999행 |

- **`src/verify_lab/measure/forward_return.py`** — 신호일별 구간 수익률 2기준.
  반환은 **신호일 × 기준 × 구간 long-form 한 장**이고, 구간 끝이 데이터를 넘는 칸은
  값만 비우고 **행과 사유를 남긴다**(`count_excluded()` 로 칸별 신호 수·제외 수를 함께 낸다).
  계약과 그 근거는 ROADMAP "확정된 forward return 반환 계약"이 SoT다
- **`src/verify_lab/measure/baseline.py`** — 조건부 모집단(종가 < 200일 이동평균) 판정. SMA·EMA 둘 다.
  창이 차기 전은 판정하지 않고 **판정 불가 일수를 함께 반환**한다
- **`src/verify_lab/measure/statistics.py`** — 칸별 집계(`summarize`), 초과분(`excess`),
  **순열 검정**(`permutation_test`). 유효 표본이 10건 미만이면 검정하지 않고 사유를 남긴다
- **`src/verify_lab/measure/constants.py`** — 측정 계층이 공유하는 결과 스키마 상수
- **`src/verify_lab/report/`** — 표시용 표 5종(`tables.py`), 결과 폴더 생성과 저장(`writer.py`),
  한글 레이블·단위(`constants.py`). **비율 → 백분율 변환과 반올림이 여기서 일어난다**
- **look-ahead 감시 계약의 공용 헬퍼** — `tests/conftest.py` 의 `assert_stable_under_truncation`.
  **이벤트 정의를 추가할 때마다 이 헬퍼로 계약을 건다**
- **`src/verify_lab/studies/index_extreme/`** — 검증 #1 의 이벤트 정의 5개 모듈.
  `find_extreme_move_events`(테스트 A)·`find_consecutive_events`(테스트 B)가 bool Series 를 내고,
  `expanding_rank`·`signed_run_length`·`assign_event_ids`·`reference_zscore` 가 신호일 목록의
  부가 컬럼을 낸다. **집계 시작일은 함수가 인자로 받아 안에서 처리한다** — 시세를 먼저 자르면
  순위 축적이 무너지는데 예외가 나지 않기 때문이다. 계약은 ROADMAP "확정된 이벤트 정의 계약"이 SoT
- **`studies/index_extreme/runner.py` 와 `scripts/studies/run_index_extreme.py`** — 강건성 조합
  **396개**(데이터셋 3 × 파라미터 11 × 방향 2 × 창 6)를 한 실행에서 산출하고 국내 두 가격 기준을
  같은 실행 안에서 계산한다. 전 조합이 **약 3분**에 끝난다. 계약은 ROADMAP "확정된 실행 계층 계약"이 SoT
- 테스트와 품질 게이트 — `validate_project.py`가 `failed=0 skipped=0`으로 통과하는 상태

**안 되어 있는 것**

- `docs/research/RESEARCH_index_extreme_events.md` 가 아직 없다. **이것 하나뿐이다**

`docs/plans/`의 계획서는 전부 Done이고 근거 승격이 끝났다. **지금 지워도 잃는 정보가 없다** —
결정 근거와 실측 수치는 스펙·ROADMAP·COMMANDS로 옮겨져 있다. 비울 때는 `.gitkeep`을 되살린다
(디렉토리가 사라지면 계획서 게이트 훅이 스스로 꺼진다).

## 국내 데이터에서 반드시 알고 있어야 할 것

여기서 착오가 나면 결과가 조용히 기울어진다. 근거는 전부 스펙 §7 결정 ⑨ 와 §8 결론 4 에 있다.
**이 사실들을 다시 조사하지 말 것. 실측으로 확정됐다.**

- **국내는 파일이 둘이고 가격 기준이 다르다.** KRX 가 수정주가를 **조회 시점 기준 최근 3,000거래일만**
  제공하기 때문이다. 구간을 나눠 이어붙이는 방법은 **실측으로 불가능이 확정**됐다 —
  종료일을 12년 앞당겨도 같은 시작일이 돌아온다
- **원본가가 본검증이다.** 테스트 A의 확장창 순위는 상장일부터 쌓아야 하므로 수정주가 계열로는 불가능하다.
  수정주가는 겹치는 구간에서 **"가격 기준을 바꾸면 결론이 바뀌는가"를 대조**하는 데 쓴다
- **원본가에는 분배락이 남아 있다.** 12년간 계단 28개이며 최근 수년은 분기 지급이다.
  이 왜곡은 **신호군과 베이스라인에 똑같이 걸리므로 초과분 비교에는 거의 영향이 없고**,
  절대 수익률(연 약 1.7%)에만 남는다. **QQQ와 절대값을 나란히 놓을 때 이 차이를 명시할 것**
- **2026년이 국내 등락 상위 10위의 60%를 차지하고, 그 신호들은 forward return 측정 구간이 아직 끝나지 않았다**
- **수정주가 계열로는 테스트 A 를 비교할 수 없다.** 2014-05-22 부터라 순위를 쌓을 구간이 없어
  초반부터 컷이 헐거워지고, K=10 폭등 신호가 원본가 14건 대비 **수정주가 94건**으로 불어난다.
  같은 이유로 **시작연도 4개의 모집단이 전부 2,999일로 같아** 시작 시점 민감도도 잴 수 없다.
  수정주가 대조는 **테스트 B 와 절대 수익률 수준**에서만 읽는다 (스펙 §8 "검증 실행 계층 재현")

## 다음 작업 — Phase 3-c: 결과 문서

**코드를 새로 쓰지 않는다.** 측정은 이미 끝났고 산출물이 나와 있다. 3-c 는 그것을 읽고
`docs/research/RESEARCH_index_extreme_events.md` 를 쓰는 일이다.
형식의 SoT 는 `docs/research/CLAUDE.md` 이며, `docs/context/` 의 두 문서가 **형식의 실물 예시**다.

문서만 만지므로 계획서 게이트가 걸리지 않는다. 다만 표를 다시 뽑으려고 스크립트를 고쳐야 한다면
그때는 계획서를 먼저 쓴다.

### 먼저 할 일 — 산출물을 다시 만든다

`storage/results/` 는 git 에서 제외돼 있어 세션이 바뀌면 없을 수 있다. 없으면 다시 돌린다
(명령어는 `docs/COMMANDS.md` "검증 실행", 전 조합 약 3분).

산출물은 `storage/results/<실행시각>_index_extreme/` 에 있다.

| 파일 | 내용 |
| --- | --- |
| `signals.csv` | 신호일 한 줄 = 날짜·종가·등락률·당시 순위·사건 번호·z-score + 구간별 수익률 12칸 |
| `statistics.csv` | 신호군 × 칸별 평균·중앙값·승률·최고·최악·표준편차와 신호·제외·표본·사건 수 |
| `excess.csv` | 베이스라인 3종 대비 초과분 (단위는 %p) |
| `test.csv` | 모집단 3종에 대한 순열 검정 (검정하지 않은 칸은 사유가 남아 있다) |
| `summary.json` | 파라미터 전체·시드·모집단 크기·신호 0건이라 빠진 신호군 목록 |

네 표 모두 앞에 **식별 컬럼 7개**(종목·가격기준·테스트·파라미터·시작연도·방향·시대 구간)가 있다.
원하는 조합만 걸러 쓰면 된다.

### 3-c 에서 담을 것 (스펙 §12)

1. 테스트 A 결과표 — 순위 컷별 × 측정 구간별 (평균/중앙값/승률/신호 수/사건 수)
2. 테스트 B 결과표 — N별 × 측정 구간별
3. 베이스라인 대비 초과수익표 — 3종 각각
4. 구간별 비교표 — 2010년대 / 2020년대 (`시대 구간` 컬럼으로 이미 산출돼 있다)
5. 신호일 전체 목록과 **연도별 분포** — 분포와 "상위 기여 사건"은 `signals.csv` 에서 파생시킨다
6. 강건성 체크 결과
7. **한계 섹션 (필수)** — 비우지 않는다

**표는 스펙 §12 대로 종목별로 나눈다.** 해석에는 "통계적으로 유의한가"뿐 아니라
**"QQQ와 다르게 움직이는가"** 를 함께 답한다 — `docs/context/` 가 이 프로젝트의 출발점이다.

### 이미 정해져 있는 것 (다시 설계하지 말 것)

- **설계는 스펙이 확정했다** — `docs/spec/index_extreme_events.md` §3~§7(확정된 결정 16건)·§10~§12
- **계층 간 계약은 ROADMAP 이 SoT다** — forward return 반환·베이스라인과 통계·출력·이벤트 정의·
  **실행 계층** 다섯 절
- **최적 파라미터를 찾지 않는다.** K=5/10/20, 시작연도 4개, N=3~10 을 **모두 나란히 보고**한다.
  "이 설정이 제일 좋다"고 고르는 순간 측정이 아니라 과최적화다
- **"유의미한 패턴이 없다"도 유효한 결론이다.** 유의성이 없으면 없다고 쓴다

### 실측으로 이미 확인된 것 (다시 재지 말 것)

- **QQQ 테스트 A 는 표본이 사건 3개뿐이라 검정이 붙지 않는다.** 2003·2005·2008년 시작이
  폭등 3·폭락 4 = **신호 7건, 사건 3개**, 2011년 시작이 폭등 1·폭락 2 = **신호 3건, 사건 2개**다.
  12칸 전부 "표본 부족으로 검정 불가"로 나오며 **버그가 아니라 결론의 일부**다
- **전체 13,500칸 중 검정된 칸은 8,334칸(61.7%)뿐**이다. 나머지는 표본 부족이다
- **테스트 B 는 신호가 많은 칸일수록 사건 수가 오히려 적다.** QQQ 2011년 이후 N=3 연속 상승은
  301건인데 사건 41개, N=4 는 166건에 사건 67개다. 신호가 촘촘할수록 30일 안에서 이어져 한 사건으로
  묶이기 때문이며, **N 이 작은 칸의 표본은 겉보기 건수만큼 독립적이지 않다.**
  승률·평균과 반드시 함께 제시한다 (근거: 스펙 §8 "테스트 B 발생 빈도")
- **사건 번호의 기준이 테스트마다 다르다** — 테스트 A 는 폭등·폭락을 합쳐, 테스트 B 는 방향별로
  매긴다. 테스트 B 를 합치면 사건 하나가 최장 5.8년에 걸쳐 구분력을 잃는다 (스펙 §7 결정 ⑯)
- **베이스라인은 신호군과 같은 기간이다** (스펙 §7 결정 ⑭). 초과분을 "같은 기간 대비"로 읽어도 된다
- **국내가 하락 국면에 훨씬 오래 머문다.** 2011년 시작 기준 200일선 아래인 날이 QQQ 는 3,922일 중
  SMA 600일인데, KODEX 200 원본가는 3,837일 중 **1,631일**이다

## 반드시 지킬 것

- **모든 코드 변경 전에 계획서를 쓴다.** 예외는 오타·주석·로그 메시지 수정뿐이다
- **`scripts/data/`(데이터 수집)는 내가 직접 실행한다.** 너는 코드만 쓰고 실행 방법을 안내한다.
  `scripts/studies/`(검증 실행)와 `validate_project.py`는 네가 직접 실행해도 된다
- **공통 계층을 미리 일반화하지 않는다.** 두 번째 검증에서 재사용되는지 확인한 뒤 확정한다
- **`reference/`는 읽기 전용이다.** 읽고 이해한 뒤 `src/verify_lab/`에 새로 작성한다
- 설명은 초보자도 이해할 수 있는 쉬운 한국어로. 전문 통계 용어 대신 구체적인 날짜·가격·표를 쓴다

## 실행 환경 주의

품질 검증이 **세 항목 모두 실패**하면서 `Command not found`가 보이면 코드 문제가 아니다.
`poetry env info --path`가 프로젝트 `.venv`를 가리키는지 먼저 확인한다.
증상·원인·대처는 `docs/ROADMAP.md` Phase 0의 실측 기록에 있다.

pykrx는 로그인 시 **로그인 ID를 표준 출력에 찍는다.** 실행 로그를 문서에 붙일 때 그 줄을 뺀다.
