# 새 세션 시작 프롬프트

> 이 파일은 verify-lab 작업을 이어받는 세션의 **단일 진입점**입니다.
> **작업이 진척되면 이 파일도 함께 갱신합니다.** 낡은 진입점은 없는 것보다 나쁩니다.
>
> **마지막 갱신**: 2026-08-24 (KST)

---

verify-lab 프로젝트를 이어서 진행한다.

**지금 진행 중인 것은 「원달러 그리드」 트랙이고, 다음 작업은 G2(그리드 엔진 구현)다.**
데이터 확보(G0)와 등가성 검증(G1)은 끝났다. 무엇을 할지 사용자에게 다시 묻지 않아도 된다 —
**아래 "다음 작업" 절이 착수 지점이다.**

## 먼저 읽을 것 (순서대로, 전부)

1. `CLAUDE.md` — 프로젝트 규칙. 특히 "이 프로젝트가 무엇인가"와 "측정의 원칙" 8개
2. **`docs/context/README.md`와 그 폴더의 두 문서** — 사용자의 현재 투자 상태이자 이 프로젝트가
   시작된 이유. 결과를 해석할 때 이 맥락이 없으면 쓸모없는 보고가 된다
3. **`docs/spec/usdkrw_grid.md`** — **지금 트랙의 SoT.** §4 "확정된 설계 결정"(A1~A4·B1~B2·C1~C18)이
   **다시 논의하면 안 되는 것들**이고, §3 "데이터 실측 기록"이 함정 목록이다
4. **`docs/spec/usdkrw_grid_rules.md`** — 사용자가 쓴 매매 규칙 원본 사양서.
   **그대로 구현하지 않는다** — 위 문서 §4 가 바꾼 부분이 있고, 문서 머리에 그 목록이 있다
5. **`docs/research/원달러_ETF_등가성.md`** — G1 결과. **ETF 경로를 쓸 수 있는지의 판정**이자,
   §11(실효 총비용)이 G3 의 비용 모델에 직접 들어간다
6. `docs/ROADMAP.md` — **진행 상태의 SoT.** Phase G0·G1 절의 "계층 계약"과 "다음 단계가 알아야 할 것"을 본다.
   검증 #1 의 계층 간 계약 다섯 절은 **공통 계층의 규격**이라 그대로 유효하다
7. `src/verify_lab/CLAUDE.md` — 계층 분리, 상수 관리, 측정 계층의 절대 원칙 5가지
8. **`.claude/rules/strategy.md`** — 매매 규칙 계층의 예외 규정. **G2 에서 이 문서를 고쳐야 한다**(아래 참고)
9. `.claude/rules/python.md`, `scripts/CLAUDE.md`, `tests/CLAUDE.md`, `docs/research/CLAUDE.md`

문서 지도는 `docs/INDEX.md` 다. **문서를 추가하면 INDEX 에 등록한다** —
`tests/test_index.py` 가 양방향으로 검사하므로 빠뜨리면 `validate_project.py` 가 실패한다.

---

## 현재 상태

### 되어 있는 것

**검증 #1 (지수 극단 이벤트) — 완료.** 코드·결과 문서·사용자 직접 대조까지 끝났다.
거기서 확정된 공통 계층(`measure/`·`report/`)과 계층 계약은 그대로 유효하다.

**역방향 매매 규칙 — 확정.** `docs/strategy/역방향_매매_규칙.md` 가 SoT이며 **현재 운용 중**이다.
이번 트랙과 무관하므로 **손대지 않는다.**

**원달러 그리드 G0 (데이터 확보) — 완료.**

| 파일 | 내용 |
| --- | --- |
| `storage/series/USDKRW_CLOSE.csv` | **정규장 종가.** 판정·측정의 기준 가격 |
| `storage/series/USDKRW.csv` | 매매기준율. **환전 스프레드 계산에만** 쓴다 |
| `storage/series/DTB3.csv` | 미국 3개월 T-bill |
| `storage/series/CD91.csv` | CD 91일물 |
| `storage/series/261240_NAV.csv`·`261250_NAV.csv` | ETF 순자산가치 |
| `storage/market/261240_*.csv`·`261250_*.csv` | ETF 시세 (원본가·수정주가 각 2종) |

- 새 계층 `storage/series/` = **일별 단일 값 시계열**(`Date,Value`). 로더는 `load_series_csv()`
- 수집기 3종 신설 — `ecos_collector.py`·`fred_collector.py`, `pykrx_collector.collect_pykrx_nav`
- 자격증명 — `.env` 에 `ECOS_API_KEY`·`KRX_ID`·`KRX_PW`. **ECOS 는 키가 URL 경로에 들어가므로 마스킹 필수**

**원달러 그리드 G1 (등가성 검증 = 검증 #5) — 완료. 판정은 「조건부 가능」.**

- `src/verify_lab/studies/usdkrw_equivalence/` 6개 모듈 + `scripts/studies/run_usdkrw_equivalence.py`
- 261240 은 「환전 + 달러 예치」를 잘 복제한다 (상관 0.9860, 추적오차 1.43%)
- 다만 **연도별 괴리 폭 1.65%p**(합격선 0.3%p)라 상수 비용으로 처리할 수 없다
- **ETF 경로를 써도 된다.** 다만 연 ±1%p 를 변동 요인으로 감수한다

**품질 게이트** — `validate_project.py` 가 `passed=457 failed=0 skipped=0` 으로 통과한다 (2026-08-24 기준).
숫자가 줄었으면 무언가 깨진 것이다.

### 안 되어 있는 것

**`src/verify_lab/strategy/grid/` 는 파일 하나도 없다.** 매매 로직 전체가 미착수다 —
격자·범위·자금배분·체결·현금장부·이자 모델·경로별 비용과 세금·그리드 지표·벤치마크.

`docs/plans/` 는 비어 있다. 이전 계획서 19건은 전부 Done 이고 근거 승격이 끝나 삭제됐다.

---

## 다음 작업 — G2 (그리드 엔진)

### 로드맵에서의 위치

| 단계 | 내용 | 상태 |
| --- | --- | --- |
| G0 | 데이터 확보 | 완료 |
| G1 | 등가성 검증 (검증 #5) | 완료 |
| **G2** | **그리드 엔진** — 격자·범위·자금배분·체결·일별 평가. **환전 경로 단독** | **다음** |
| G3 | 경로 확장 — ETF 2종, 비용·세금·이자 모델, 하단이탈 A/B 병행 | |
| G4 | 성과 계층 — 일별 총자산 곡선, 표준 지표, 벤치마크 3종 | |
| G5 | 파라미터 축별 25회 + 국면별 분석 + 결과 문서 | |

### 착수 전에 반드시 할 것 — 규칙 문서 개정 (결정 A1)

`.claude/rules/strategy.md` 가 **"`strategy` 는 `studies` 가 낸 신호 목록을 입력으로 받는다"** 고
규정하고 있는데, **그리드는 이 계약을 지킬 수 없다.** 어느 레벨을 사는지가 "그 레벨을 이미
보유 중인지"에 달려 있어 가격만의 함수가 아니기 때문이다.

입력을 **"신호 목록(이벤트 구동) 또는 시세 자체(상태 구동)"** 로 넓히되,
**매매 규칙을 `studies/` 에 넣지 않는다는 금지는 그대로 유지**한다. 근거는
`docs/spec/usdkrw_grid.md` §4.1 결정 A1 이다.

### G2 의 범위

**환전 경로 하나만** 만든다. ETF 는 G3 다. 비용·세금도 G3 다.

만들 것은 다섯 조각이고 각각이 계획서 하나에 대응한다.

| 조각 | 내용 |
| --- | --- |
| 격자 | `레벨_k = 1000 × (1+g)^k`. **영구 고정**이며 목표가는 `레벨_(k+1)` |
| 범위 | 과거 12N개 **월평균**의 min~max (당월 제외). 최소폭 20% 강제. 월 1회 재조정 |
| 자금배분 | 3구간 차등(1.5/1.0/0.5), 슬롯 상한 8%, **매일 재계산** |
| 체결 | 하향 돌파(전일 종가 > 레벨 AND 당일 종가 ≤ 레벨), 다중 체결 허용, 매도는 종가 ≥ 목표가 |
| 평가 | **일별 시가평가 총자산.** 미실현 손익을 반드시 반영한다 |

세부 규정은 `docs/spec/usdkrw_grid_rules.md` §3~§8 이고,
**바뀐 부분은 `docs/spec/usdkrw_grid.md` §4 가 이긴다.**

### 다시 논의하지 말 것 — 확정된 결정

전문은 `docs/spec/usdkrw_grid.md` §4 에 있다. G2 에 직접 걸리는 것만 옮긴다.

| # | 확정 |
| --- | --- |
| **C17** | **판정·체결 기준 가격은 정규장 종가**(`USDKRW_CLOSE.csv`). 매매기준율은 환전 스프레드 계산에만 쓴다 |
| **C1** | 격자·범위는 **환율로 단일 정의**한다. ETF 는 집행 수단일 뿐이다 |
| **C3** | 초기 자본금 **1억원** |
| **C4** | 슬롯 금액의 분모는 **활성 레벨 전체**(보유분 포함) |
| **C5** | 현금 부족 시 **아래(싼) 레벨부터** 체결 |
| **C6** | 하단 이탈 B안은 당일 종가를 포함하는 칸까지 연장, 배수는 하단부 1.5 고정, 재조정에서 비활성 |
| **C13** | 슬롯 상한 8% 는 **기본 g=0.8% 에서 발동하지 않는다.** 값을 바꾸지 말고 미발동을 결과로 보고한다 |
| **C14** | 이자는 **365일 일할**, 마스터 달력은 환율 고시일 |
| **B1** | 모든 매매법의 공통 산출물은 **일별 총자산 곡선**이다. 표준 지표는 곡선 하나만 받는 함수에서 계산한다 |
| **A4** | 기존 `strategy/constants.py` 는 옮기지 않는다. 그리드는 `strategy/grid/` 서브패키지로 신설 |

### 이 트랙에서 이미 밟은 함정 — 다시 밟지 말 것

**전부 예외가 나지 않고 결과만 조용히 틀리는 종류다.**

| 함정 | 무슨 일이 났나 |
| --- | --- |
| **매매기준율의 1일 고시 시차** | 매매기준율(D)은 **D−1 의 시장**을 담는다. 모르고 쓰면 ETF 와의 상관이 **−0.03** 으로 나온다. 정규장 종가를 쓰면 해결된다 |
| **이론값 식** | 261240 은 `현물 + 달러금리` 를 복제한다(알파 −0.01%). 사양서 §16.1 의 `현물 + 금리차` 는 **+2.13%** 로 어긋난다 |
| **NAV 분배금 조정** | NAV 를 원본 그대로 쓰면 분배락이 비용으로 잡혀 **값이 두 배**가 된다. 조정 배율은 `수정 종가 ÷ 원본가` 로 뽑는다 |
| **레버리지 상품의 담보 구조** | 2배 상품은 2배 노출을 **1배 담보**로 굴린다. 기준선에 반영하지 않으면 **원화금리 전액이 비용으로 둔갑**한다 (−0.05% 가 +2.12% 로) |
| **연환산 계수** | 일할은 365 달력일, 연환산은 250 거래일이라 실제 거래일 밀도(약 261일)와 몇 % 어긋난다. 250 은 사양서가 정한 값이라 **오차가 사실**이다 |

### 남아 있는 미해결

| 항목 | 언제 닫히나 |
| --- | --- |
| 사양서 §17.2 "연 회전 5~15회" 가 전체 건수인지 슬롯당인지 | G5 결과 해석 때. **구현을 막지 않는다** |
| DTB3 가 할인율이라 달러금리를 과소평가한다 (연 0.1% 수준) | **알려진 한계.** FRED 가 할인율만 제공한다 |

---

## 검증 #1 에서 확정돼 계속 유효한 것

**다시 논의하지 말 것.** 근거는 ROADMAP 의 계층 간 계약 다섯 절에 있다.

- **공통 계층(`measure/`·`report/`)은 검증이 바뀌어도 바뀌지 않는다**
- **`studies` 가 공통 계층에 넘기는 것은 "어느 날이 신호인가" bool Series 하나**다.
  다만 **검증 #5 는 이벤트가 없어 `measure/` 를 쓰지 않았다** — 그 판단과 이유는 ROADMAP Phase G1 에 있다
- **조합 순회와 조립은 `runner.py`, `scripts/` 는 인자 파싱·호출·표시·저장만**
- **가격 기준은 원본가, 소수 4자리.** 자릿수는 `common_constants.PRICE_DECIMALS` 한 곳에서 정한다.
  단 **그리드용 ETF 는 수정 종가**를 본검증 기준으로 쓴다 (사양서 §11.3)
- **look-ahead 감시 테스트를 산출 전부에 건다** (`tests/conftest.py` 의 공용 헬퍼)
- **표본 부족 판정선은 유효 표본 10건 미만.** 순열 검정 단독, 반복 1,000회·시드 0
- **유의 비율로 판정한다.** 개별 칸의 p값을 근거로 쓰지 않는다

---

## 반드시 지킬 것

- **모든 코드 변경 전에 계획서를 쓴다.** 예외는 오타·주석·로그 메시지뿐이다.
  절차의 SoT 는 `/verify-plan` 스킬이다. `docs/plans/` 가 비어 있으면 `.gitkeep` 이 있어야
  계획서 게이트 훅이 살아 있다
- **스크립트는 AI 가 직접 실행한다** — 데이터 수집도 포함이다(2026-08-24 개정).
  다만 **같은 데이터를 이유 없이 다시 받지 않으며**, `storage/market/` 을 덮어썼으면
  새 데이터 기준일을 결과 문서에 남긴다
- **공통 계층을 미리 일반화하지 않는다.** 두 번째 사용처에서 재사용되는지 확인한 뒤 확정한다
- **`reference/` 는 읽기 전용이다.** 읽고 이해한 뒤 `src/verify_lab/` 에 새로 작성한다
- **`docs/context/` 는 사용자 소유 문서다.** 오탈자를 발견해도 고치지 말고 언급만 한다
- **Git 처리는 사용자가 한다.** 커밋·푸시하지 않는다
- 설명은 초보자도 이해할 수 있는 쉬운 한국어로. 전문 통계 용어 대신 구체적인 날짜·가격·표를 쓴다

---

## 실행 환경 주의

품질 검증이 **세 항목 모두 실패**하면서 `Command not found` 가 보이면 코드 문제가 아니다.
`poetry env info --path` 가 프로젝트 `.venv` 를 가리키는지 먼저 확인한다.
증상과 대처는 `docs/ROADMAP.md` Phase 0 의 실측 기록에 있다.

- **pykrx 는 로그인 시 로그인 ID 를 표준 출력에 찍는다.** 로그를 문서에 붙일 때 그 줄을 뺀다
- **ECOS 는 인증키가 URL 경로에 들어간다.** 요청 URL 을 로깅·기록할 때 반드시 마스킹한다
- **`storage/results/` 는 git 에서 제외**돼 세션이 바뀌면 없을 수 있다.
  필요하면 `docs/COMMANDS.md` 로 다시 돌린다. 저장된 데이터가 그대로면 결과도 그대로다
- **`collect_yfinance.py` 에 `--adjusted` 를 붙이지 않는다.** 같은 파일명에 덮어써져 본검증 데이터를 잃는다
- `data.krx.co.kr` 이 일시적으로 DNS 해석에 실패하는 일이 있다. 잠시 뒤 재시도하면 된다
