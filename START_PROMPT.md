# 새 세션 시작 프롬프트

> 이 파일은 verify-lab 작업을 이어받는 세션에 붙여넣을 프롬프트입니다.
> **작업이 진척되면 이 파일도 함께 갱신합니다.** 낡은 진입점은 없는 것보다 나쁩니다.

---

verify-lab 프로젝트를 이어서 진행한다. **검증 #1(지수 극단 이벤트)의 코드와 결과 문서는 완료됐다.**

**이번 세션의 일은 새 코드가 아니라 "사용자가 직접 결과를 대조해 보는 것"이다.**
사용자는 소스를 분석해 본 적이 없고, 결과 파일을 직접 보면서 숫자가 맞는지 자기 손으로 확인하려 한다.

**재개 지점은 `signals.csv` 확인부터다.** 대조를 1~3단계까지 했었는데 그 뒤
**가격 기준이 수정주가 → 원본가로 바뀌어 QQQ 수치가 전부 달라졌다**(2026-08-20, 스펙 §7 결정 ⑰).
그래서 신호일 목록부터 다시 본다. **`docs/HANDS_ON.md` 3단계부터 한 단계씩 진행한다.**

대조할 산출물은 **`storage/results/20260820_135545_index_extreme/`** 이다.
`storage/results/` 는 git 에 없으므로 폴더가 없으면 다시 돌린다(약 2분, `docs/COMMANDS.md`).

**진행 방식이 이 세션의 핵심이다 — 아래 "다음 작업"을 반드시 읽고 그대로 지킨다.**

## 먼저 읽을 것 (순서대로, 전부)

1. `CLAUDE.md` — 프로젝트 규칙. 특히 "이 프로젝트가 무엇인가"와 "측정의 원칙" 8개 항목
2. **`docs/context/README.md`와 그 폴더의 두 문서** — 내 현재 투자 상태이자 이 프로젝트가 시작된 이유.
   가장 중요한 문서다. 검증 결과를 해석할 때 이 맥락이 없으면 쓸모없는 보고가 된다
3. `docs/ROADMAP.md` — **진행 상태의 SoT.** 무엇이 끝났고 무엇이 남았는지는 여기가 기준이다.
   **계층 간 계약 다섯 절**("확정된 forward return 반환 계약"·"확정된 베이스라인·통계 계약"·
   "확정된 출력 계약"·"확정된 이벤트 정의 계약"·"확정된 실행 계층 계약")이 **공통 계층의 규격**이므로
   반드시 읽는다. Phase 1의 "확정된 원시 시세 저장 규칙", Phase 3의 "검증 #1 의 결론"도 함께 본다.
   **"2번 이후 검증에서 미리 알아둘 것"이 다음 작업의 출발점이다**
4. **`docs/HANDS_ON.md`** — **이번 세션의 진행 대본이다.** 스크립트 실행부터 신호일·등락률·
   수익률·사건 묶기·베이스라인을 손으로 검산하는 8단계가 순서대로 있고, 체크리스트가 맨 뒤에 있다.
   **손계산 예시 값은 원본가 기준으로 갱신돼 있다** — 여기 적힌 값이 현재 기준이다
5. **`docs/research/RESEARCH_index_extreme_events.md`** — 검증 #1 의 결과 문서.
   **검증 #2 를 설계할 때 참고할 실물 예시이며**, 특히 12장(강건성 체크)·15장(한계)의
   구성과 "유의 비율로 판정하는 방식"을 그대로 재사용할 수 있다
6. `docs/spec/index_extreme_events.md` — 검증 #1 의 확정 설계. **새 검증의 스펙을 쓸 때 형식의 본보기다.**
   §7 "확정된 설계 결정" 17건의 표 형식(확정 / 탈락안 / 근거)을 그대로 따른다.
   **결정 ⑰(가격 기준)과 그 아래 "전환 실측" 절**이 가장 최근 결정이며, 결정 ⑨ 를 대체했다 —
   **한 번 탈락시킨 안을 다시 채택할 때 어떻게 기록하는지의 실물 예시다**
7. `src/verify_lab/CLAUDE.md` — 계층 분리, 상수 관리, **측정 계층의 절대 원칙 5가지**,
   확정된 경로 기준점·컬럼 스키마 규약
8. `.claude/rules/python.md` — 코딩 표준. `.py` 파일을 읽으면 자동 주입되지만,
   **기존 파일을 열지 않고 새 파일부터 만들 때는 주입되지 않으니 직접 열 것**
9. `scripts/CLAUDE.md`, `tests/CLAUDE.md`, `docs/research/CLAUDE.md`

나머지 문서와 `reference/`의 참고 자료가 어디 있는지는 `docs/INDEX.md`에 지도로 정리돼 있다.
**문서를 추가하면 INDEX에 등록해야 한다** — `tests/test_index.py`가 검사하므로 빠뜨리면
`validate_project.py`가 실패한다.

## 현재 상태

**되어 있는 것 — Phase 0~3 전부**

- 폴더 구조, 설정 파일, 하네스(`.claude/`의 훅·규칙·`/verify-plan` 스킬), 규칙 문서 전체
- 설계·맥락 문서 — `docs/context/`, `docs/ROADMAP.md`, `docs/spec/index_extreme_events.md`
- 문서 지도 `docs/INDEX.md`와 부패 검사 `tests/test_index.py`
- **`poetry install` 완료** — 프로젝트 내부 `.venv` 사용
- **`src/verify_lab/common_constants.py`** — 경로 상수와 시세 스키마 컬럼 상수
- **`src/verify_lab/utils/` 4종** — 로거·포맷팅·CLI 헬퍼·메타 관리
- **`src/verify_lab/data/loader.py`** — 모든 시세 로딩의 단일 통로.
  스키마·정렬·중복·이상치를 검사하고 **보간 없이 즉시 예외**를 던진다.
  이상치 판정 함수는 수집기도 같이 쓴다(판정식 단일화)
- **수집 경로 2종** — `yfinance_collector.py`(미국)와 `pykrx_collector.py`(국내), 그리고 각각의 CLI.
  둘 다 결과를 좌우하는 인자를 **명시적으로** 넘기고, 검증을 통과한 데이터만 저장한다
- **`src/verify_lab/data/krx_credentials.py`** — `.env` → 환경 변수. `import pykrx` 자체가 로그인을
  시도하므로 **수집 모듈이 그 순서를 소유한다**(`_import_pykrx_stock`). 순서는 테스트로 고정돼 있다
- **`measure/` 3종** — `forward_return.py`(신호일별 구간 수익률 2기준, long-form 반환),
  `baseline.py`(조건부 모집단 판정, SMA·EMA), `statistics.py`(칸별 집계·초과분·순열 검정),
  그리고 `constants.py`. 계약은 ROADMAP 이 SoT다
- **`report/`** — 표시용 표 5종(`tables.py`), 결과 폴더 생성과 저장(`writer.py`),
  한글 레이블·단위(`constants.py`). **비율 → 백분율 변환과 반올림이 여기서 일어난다**
- **look-ahead 감시 계약의 공용 헬퍼** — `tests/conftest.py` 의 `assert_stable_under_truncation`.
  **이벤트 정의를 추가할 때마다 이 헬퍼로 계약을 건다**
- **`src/verify_lab/studies/index_extreme/`** — 검증 #1 의 이벤트 정의 5개 모듈 + `runner.py`,
  그리고 `scripts/studies/run_index_extreme.py`. 강건성 조합 264개를 한 실행(**약 2분**)에서 산출한다
- **데이터 2파일, 둘 다 원본가** — `QQQ_max.csv`(6,903행, 1999-03-10 ~ 2026-08-18),
  `069500_max.csv`(5,881행, 2002-10-14 ~ 2026-08-11). 재수집하면 행 수·기간이 달라진다
- **검증 #1 결과 문서 완성** — `docs/research/RESEARCH_index_extreme_events.md`.
  **원본가 기준으로 재산출을 마쳤다**
- 테스트와 품질 게이트 — `validate_project.py`가 `passed=279 failed=0 skipped=0`으로 통과

**안 되어 있는 것**

- **검증 #1 직접 대조를 `signals.csv` 부터 다시 한다.** 1~3단계를 2026-08-19 에 통과했지만
  그 뒤 가격 기준이 원본가로 바뀌어 **QQQ 종가·수익률이 전부 달라졌다.** 신호일 목록부터 다시 보는 것이
  이번 세션의 일이다. 단계별 상태는 `docs/ROADMAP.md` Phase 3 의 "사용자 직접 대조 진행 상태"가 SoT다
- **검증 #2(비트코인 4년 주기설)** — 스펙도 데이터도 코드도 없다. 대조가 끝난 뒤 착수한다
- 검증 #3(정배열 매매법)·#4(탑티어 우량주 반토막) — 미착수

`docs/plans/`의 계획서 11건은 전부 Done이고 근거 승격이 끝났다. **지금 지워도 잃는 정보가 없다** —
결정 근거와 실측 수치는 스펙·ROADMAP·COMMANDS·결과 문서로 옮겨져 있다.
**비우는 것은 사용자가 한다.** 비울 때는 `.gitkeep`을 되살린다(디렉토리가 사라지면 계획서 게이트 훅이 스스로 꺼진다).

## 검증 #1 에서 확정돼 다음 검증에도 그대로 가는 것

**다시 논의하지 말 것.** 근거는 ROADMAP 의 계층 간 계약 다섯 절과 결과 문서에 있다.

- **공통 계층(`measure/`·`report/`)은 검증이 바뀌어도 바뀌지 않는다.** 검증마다 새로 쓰는 것은
  `studies/<검증명>/` 의 이벤트 정의와 `runner.py` 뿐이다
- **`studies` 가 공통 계층에 넘기는 것은 "어느 날이 신호인가" bool Series 하나**다.
  부가 컬럼(순위·사건 번호·z-score)은 별도 함수로 따로 낸다
- **집계 시작일은 이벤트 정의 함수가 인자로 받아 안에서 처리한다.** 호출 측이 시세를 먼저 자르면
  결과가 달라지는데 예외가 나지 않는다
- **조합 순회와 조립은 `studies/<검증명>/runner.py`, `scripts/` 는 인자 파싱·호출·표시·저장만.**
  조합 순회를 스크립트에 두면 가장 조용히 틀릴 수 있는 코드가 테스트 밖에 남는다
- **베이스라인 모집단은 전 거래일 forward return 테이블 하나에서 파생시키고, 신호군과 같은 기간으로 자른다**
- **표본 부족 판정선은 유효 표본 10건 미만.** 검정하지 않고 사유를 남긴다
- **순열 검정 단독.** t-test 는 쓰지 않는다. 기본값은 반복 1,000회·시드 0
- **look-ahead 감시 테스트를 산출 전부에 건다** (`tests/conftest.py` 의 공용 헬퍼)
- **가격 기준은 원본가다** (스펙 §7 결정 ⑰). 사용자가 결과를 차트와 직접 대조하는 것이 전제인데
  보통의 차트는 배당 미포함이기 때문이다. 수집기는 `adjusted` 인자를 명시로 받고 **기본값이 원본가**다.
  대가로 배당락일이 하루짜리 하락으로 남지만 **왜곡이 신호군과 베이스라인에 똑같이 걸려 초과분에서는 지워진다**
  (전환 실측: 1년 절대 수익률 -0.889%p, 초과분 +0.058%p). **새 자산군을 붙일 때도 원본가를 기본으로 한다**

## 검증 #1 에서 배운 것 — 검증 #2 에 그대로 쓸 판정 방법

- **유의 비율로 판정한다.** 칸이 수천 개면 5%는 그냥 유의하게 나온다. 검증 #1 에서 QQQ 테스트 B 는
  검정된 714칸 중 31칸(**4.3%**)이 p<0.05 였고, 이것이 "패턴 없음"의 결정적 근거였다.
  반대로 KODEX 200 테스트 A 는 278칸 중 207칸(74.5%)이었다. **개별 칸의 p값을 근거로 쓰지 않는다**
- **신호 수와 사건 수의 차이가 결론을 바꾼다.** 순열 검정은 신호 수로 계산되므로 한 사건에서 나온
  15건을 15번의 독립 관측으로 센다. **사건 단위 확인을 반드시 함께 싣는다**
- **베이스라인의 승률을 먼저 본다.** QQQ 는 아무 날이나 사도 1년 뒤 플러스일 확률이 85.82% 다.
  "신호 7건 전부 플러스"가 우연히도 34.3% 확률로 일어난다. **승률 100%가 특별하다는 뜻이 아니다**
- **시대 구간으로 나눠 보면 레짐 의존이 드러난다.** KODEX 200 N=5 연속 상승 1년 평균이
  2010년대 +1.83%, 2020년대 +54.58% 였다
- **가격 기준을 바꿔도 결론이 흔들리지 않는지 확인할 수 있다.** 검증 #1 에서 수정주가 → 원본가 전환 시
  테스트 A 신호의 날짜·등락률·사건 번호가 **완전히 동일**했다(조정 배율이 등락률에서 약분되기 때문).
  달라진 것은 테스트 B 뿐이었고, **바뀐 날짜 22개 전부가 배당락일 직후 10거래일 안**이었다.
  **"무엇이 왜 바뀌었는가"를 이런 식으로 100% 설명할 수 있으면 변경이 안전하다는 근거가 된다**

## 다음 작업 — 검증 #1 결과를 사용자가 직접 대조한다

**새 코드를 쓰지 않는다. 계획서도 필요 없다.** 이 세션의 산출물은 코드가 아니라
**사용자가 "이 숫자는 내가 확인했다"고 말할 수 있게 되는 것**이다.

### 진행 방식 (이게 제일 중요하다)

- **`docs/HANDS_ON.md` 의 8단계를 순서대로 따라간다.** 대본이 이미 있으니 새로 만들지 않는다.
  **1~2단계(실행·산출물 구조)는 이미 통과했으므로 3단계(신호일 차트 대조)부터 시작한다** —
  다만 가격 기준이 바뀌어 종가가 전부 달라졌으니 3단계도 다시 본다
- **한 번에 한 단계만.** 단계마다 사용자가 직접 해보고 결과를 말할 때까지 기다린다.
  여러 단계를 한꺼번에 설명해 쏟아붓지 않는다
- **사용자 대신 확인해 주지 않는다.** "제가 계산해 보니 맞습니다"가 아니라
  **"이 파일을 열어서 이 줄을 보세요, 무엇이 보이나요"** 로 진행한다.
  직접 확인하는 것이 이 세션의 목적 전체다
- **초보자 기준으로 설명한다.** 사용자는 소스를 분석해 본 적이 없다.
  전문 용어를 쓰면 그 자리에서 한 줄로 풀어 준다
- **막히면 그 단계에서 멈춘다.** 다음으로 넘어가지 말고 왜 안 되는지부터 같이 본다
- **숫자가 어긋나면 그것이 발견이다.** 얼버무리지 말고 어디가 어떻게 다른지 정확히 짚는다.
  코드 버그일 수도 있고 데이터가 갱신된 것일 수도 있다 — **둘을 구분해서 말한다**

### 실행은 AI 가 해도 되고 사용자가 해도 된다

`scripts/studies/run_index_extreme.py` 는 **검증 실행**이라 AI 가 직접 돌려도 된다.
다만 이 세션에서는 **사용자가 자기 터미널에서 직접 돌려 화면을 보는 것**이 목적이므로,
명령어를 안내하고 화면에 무엇이 나오는지 함께 읽는 쪽을 기본으로 한다.

`scripts/data/`(데이터 수집)는 **사용자만 실행한다.**

### 미리 알아둘 것 — 실측으로 확인됐다

- **실행 시간은 2분쯤이다** (데이터셋이 3종 → 2종으로 줄어 짧아졌다)
- **출력이 1,000줄 넘게 쏟아지고, 표 3개는 맨 끝에 나온다.** 앞은 전부 진행 로그다.
  사용자가 "아무것도 안 나온다"고 하면 아직 도는 중이거나 스크롤을 안 내린 것이다
- **로그 접두사가 붙는다** — `2026-08-20 13:55:45.309 DEBUG [src/...py:211] [print_row] :` 뒤부터가 내용이다
- **다시 돌려도 결과가 완전히 같다.** 순열 검정 시드가 0으로 고정돼 있어 같은 데이터면 CSV 가 바이트 단위로 일치한다
  (2026-08-14·08-18·08-19 세 번의 실행에서 실측). **그러므로 값이 달라졌다면 데이터가 바뀐 것**이지
  프로그램이 흔들린 게 아니다. 확인 명령어(`md5`)는 HANDS_ON 에 있다
- **결과 폴더는 실행할 때마다 쌓인다.** 덮어쓰지 않는다
- **264개 조합 중 16개는 신호 0건이라 빠져 248개가 집계된다.** 목록은 `summary.json` 에 있다.
  `signals.csv` 는 12,679행이다
- **`storage/results/` 는 git 에 올라가지 않는다.** 세션이 바뀌면 없을 수 있고, 다시 돌리면 된다
- **터미널 "검증 대상 시세" 표의 헤더 정렬이 깨진다**(`행 수기간` 으로 붙어 나옴). 표시만의 문제로
  숫자에는 영향이 없다. 고치려면 코드 변경이라 계획서가 필요하다 — **아직 미해결로 남아 있다**

### 대조에서 사용자가 만나게 될 헷갈림

미리 알고 있다가 나오면 바로 풀어 준다. 아래 다섯 줄 중 **세 줄은 실제로 나왔던 질문**이다.

| 사용자가 하는 말 | 실제 |
| --- | --- |
| "차트 종가랑 파일 종가가 다른데요" | **이제는 같아야 정상이다.** 두 종목 다 원본가다. 어긋나면 그게 발견이므로 얼버무리지 말 것. 다만 **배당 재투자를 반영한 수정주가 차트**와 비교하면 벌어진다 — 그 경우 배율을 구해 다른 날에도 같은 배율이 걸리는지 확인한다(HANDS_ON 3단계에 절차가 있다) |
| "1개월인데 왜 날짜가 한 달이 아니죠" | 측정 구간은 **거래일 21칸**이지 달력 한 달이 아니다. 휴장일이 껴도 항상 21칸이다 |
| "빈칸이 있는데 버그 아닌가요" | 측정 구간이 아직 안 끝났거나(2026년 신호), 신호가 0건인 조합이다. **0으로 채우면 "수익률 0%"로 읽혀서** 일부러 비워 둔다 |
| "데이터 끝 날짜가 오늘이 아닌데요" | **실제로 나온 질문이다.** 검증은 저장된 CSV 만 읽고, 수집할 때 확정되지 않은 최근 며칠을 버린다(미국 2일·국내 1일). 계산까지 HANDS_ON 에 적어 뒀다 |
| "결과는 터미널에서만 보나요" | **실제로 나온 질문이다.** 터미널은 미리보기고 본체는 폴더의 CSV 5개다. 대조 8단계 중 7단계가 엑셀 작업이다 |

### 이 세션이 끝나면

- 사용자가 체크리스트 15개를 통과했으면 **검증 #1 은 "본인이 확인한" 상태**가 된다.
  통과 상황은 `docs/ROADMAP.md` Phase 3 "사용자 직접 대조 진행 상태" 표에 갱신한다
- 대조 중에 새로 알게 된 것이 있으면 **`docs/HANDS_ON.md` 에 반영**한다
  (설명이 부족했던 단계, 사용자가 걸렸던 지점)
- 숫자가 어긋난 것이 있으면 **원인을 밝히고 `docs/research/` 또는 `docs/spec/` 에 반영**한다
- **그 다음이 검증 #2 착수다** — 아래 절 참고

## 그 다음 작업 — 검증 #2: 비트코인 4년 주기설

**착수 전에 판단부터 한다. 코드를 먼저 쓰지 않는다.**

ROADMAP "2번 이후 검증에서 미리 알아둘 것"이 이 판단을 예고해 뒀다.
**비트코인 4년 주기설은 "특정 하루"가 아니라 "반감기 후 N개월 구간"** 이라,
지금의 이벤트-일 기반 틀에 그대로 맞지 않을 수 있다. 아래 세 가지를 먼저 확인하고
**여러 해석이 가능하면 전부 제시해 사용자 판단을 받는다.**

| 확인할 것 | 왜 걸리는가 |
| --- | --- |
| **이벤트를 "반감기 당일"로 둘 수 있는가** | 당일 하루로 두면 지금 계층이 그대로 동작한다. 구간으로 둬야 한다면 `studies` 의 bool Series 계약이 깨지고 공통 계층 확장 판단이 필요하다 |
| **표본이 몇 개인가** | 반감기는 2012·2016·2020·2024 로 **4개뿐**이다. 검증 #1 의 QQQ 테스트 A(사건 3개)와 같은 상황이며, **표본 부족 판정선 10건에 전부 걸린다.** 그렇다면 "검정 불가"가 결론이 되는데, 그 결론을 내는 것이 이 검증의 목적인지 사용자에게 확인한다 |
| **"거래일"이 무엇인가** | 지금의 측정 구간은 **거래일 위치 인덱스**로 계산한다(1·5·21·63·126·252). BTC 는 24/7 거래라 1년이 365행이다. **같은 상수를 쓰면 252행 = 8.3개월이 되어 조용히 틀린다.** 구간 상수를 자산군별로 둘지 결정해야 한다 |
| **가격 기준이 무엇인가** | BTC 는 배당이 없어 원본가/수정주가 구분 자체가 없다. **결정 ⑰ 의 취지(사용자가 차트와 직접 대조할 수 있어야 한다)는 그대로 지킨다** — 어느 거래소·어느 지수를 쓰느냐가 그 자리를 대신하므로, 사용자가 보는 차트와 같은 소스인지 먼저 확인한다 |

> **공통 계층을 미리 일반화하지 않는다**(루트 `CLAUDE.md`). 검증 #2 가 실제로 무엇을 요구하는지
> 확인한 뒤에 확정하고, 세 번째 검증부터 이득을 본다.

그 뒤 순서는 **스펙(`docs/spec/bitcoin_four_year_cycle.md`) → 데이터 실측(사용자가 실행) →
`studies/bitcoin_cycle/` → `runner.py` + CLI → 결과 문서**다. 각 조각이 계획서 하나다.
스펙 형식은 `docs/spec/index_extreme_events.md` §7 의 표(확정 / **탈락안** / 근거)를 그대로 따른다.

### 문서를 추가하면 INDEX 에 등록한다 (안 하면 품질 게이트가 깨진다)

`tests/test_index.py` 가 `docs/**` 를 **양방향으로** 검사한다 — 등록된 경로가 실재하는지,
실재하는 파일이 등록됐는지. 면제는 `docs/plans/` 뿐이다.

## 반드시 지킬 것

- **모든 코드 변경 전에 계획서를 쓴다.** 예외는 오타·주석·로그 메시지 수정뿐이다.
  절차의 SoT 는 `/verify-plan` 스킬이다
- **`scripts/data/`(데이터 수집)는 내가 직접 실행한다.** 너는 코드만 쓰고 실행 방법을 안내한다.
  `scripts/studies/`(검증 실행)와 `validate_project.py`는 네가 직접 실행해도 된다
- **공통 계층을 미리 일반화하지 않는다.** 두 번째 검증에서 재사용되는지 확인한 뒤 확정한다
- **`reference/`는 읽기 전용이다.** 읽고 이해한 뒤 `src/verify_lab/`에 새로 작성한다
- **`docs/context/` 는 내 소유 문서다.** 오탈자를 발견해도 고치지 말고 언급만 한다
- 설명은 초보자도 이해할 수 있는 쉬운 한국어로. 전문 통계 용어 대신 구체적인 날짜·가격·표를 쓴다

## 실행 환경 주의

품질 검증이 **세 항목 모두 실패**하면서 `Command not found`가 보이면 코드 문제가 아니다.
`poetry env info --path`가 프로젝트 `.venv`를 가리키는지 먼저 확인한다.
증상·원인·대처는 `docs/ROADMAP.md` Phase 0의 실측 기록에 있다.

pykrx는 로그인 시 **로그인 ID를 표준 출력에 찍는다.** 실행 로그를 문서에 붙일 때 그 줄을 뺀다.

`storage/results/` 는 git 에서 제외돼 있어 세션이 바뀌면 없을 수 있다.
검증 #1 의 산출물이 필요하면 `docs/COMMANDS.md` "검증 실행"으로 다시 돌린다(약 2분).
**저장된 시세 파일이 그대로면 결과도 그대로다** — 결과 문서의 값과 일치해야 정상이다.
데이터를 다시 받았다면 기간이 늘어 값이 달라지며, 그때는 결과 문서를 갱신한다.

**`scripts/data/collect_yfinance.py` 를 돌릴 때 `--adjusted` 를 붙이지 않는다.** 붙이면 수정주가가
**같은 파일명에 덮어써져** 본검증 데이터를 잃는다. 기본값이 원본가다.
