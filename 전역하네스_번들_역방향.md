# 전역 하네스 역방향 번들 - 이 파일 하나로 실행한다

> **생성 시각(KST)**: 2026-09-01 13:25
> **만든 PC**: yubeens-Mac-mini.local / Darwin 25.6.0 / python 3.12.13
> **만든 PC 의 홈 경로**: /Users/yubeen
> **담은 파일**: 3개 (누락 0개) - 훅은 하나도 담지 않았다
> **이 파일은 데이터이자 지시서다.** 다른 문서를 찾지 마라 - 필요한 절차가 전부 여기 있다.
> **기계가 만든 산출물이다.** 손으로 고치지 않는다 - sha256 이 어긋나면 적용이 중단된다.

---

## 0. 이 파일이 무엇인가

두 PC 의 전역 하네스(`~/.claude/`)를 같게 맞추는 왕복 절차의 **돌아오는 쪽**이다.
**이 파일을 읽고 있는 PC 가 왕복의 출발지다.**

1. **이 PC 가 먼저** 하네스 표면 18개 파일을 번들로 내보냈다 (2026-08-31 20:06)
2. 위 머리말의 **만든 PC** 가 그것을 받아 적용했다 - 파일 6개를 새로 놓고, 3개를 합치거나 교체했다
3. 그 과정에서 **만든 PC 에만 있거나 만든 PC 것이 더 새로웠던 것**을 담아 되돌려보낸 것이 이 파일이다

이 파일을 적용하면 두 PC 의 하네스가 같아지고, **왕복은 여기서 끝난다.**

### 델타 번들이다 - 전체 하네스 백업이 아니다

**담긴 3개는 「이 PC 에 없거나 이 PC 것이 더 낡은 파일」뿐이다.**
이 PC `~/.claude/` 의 나머지 파일(`settings.json` · 다른 스킬 · 훅 · 커맨드)은
**이미 양쪽이 같으므로 일부러 뺐다.**

- **번들에 없다는 이유로 「이 PC 에만 있는 파일」로 분류하지 마라.** 되돌려보낼 것이 아니다
- **번들에 없는 파일을 지우거나 건드리지 마라**
- **되돌려보낼 것은 없다.** 왕복은 이 적용으로 끝난다

---

## 1. 절차 - 순서대로 한다. 어긋나면 적용하지 말고 멈춰서 보고한다

### A. 무결성 - 옮기다 잘리지 않았는가

아래 5장의 각 `BEGIN FILE` 헤더에 `sha256=` `lines=` `bytes=` `mode=` 가 박혀 있다.
본문(`BEGIN` 다음 줄부터 `END` 앞 줄까지)을 잘라내 **다시 계산해 대조해라.**
본문은 말미 개행이 지워진 채 담겨 있으므로, 개행을 0~4개 붙여보며 sha256 이 맞는 것을 고른다.
**스크립트로 해라 - 눈으로 세지 마라.**

- [ ] 3개 파일의 sha256 이 전부 일치한다
- [ ] `lines=` · `bytes=` 가 실제값과 일치한다
- [ ] `BEGIN` 개수와 `END` 개수가 같다 (잘림 감지)
- [ ] 3장 파일 목록의 개수와 실제 추출 개수가 같다

**중단**: 하나라도 어긋나면 전송 중 손상이다. 적용하지 말고 다시 받아라.

### B. 바탕 판 대조 - 그 사이에 이 PC 가 고쳤는가 (가장 중요)

**이 번들의 파일은 패치가 아니라 전문(全文)이다.** 적용하면 통째로 덮인다.
그래서 **이 PC 의 현재 파일이 번들이 바탕으로 삼은 판 그대로인지** 먼저 확인해야 한다.
그렇지 않으면 그 사이 이 PC 에서 한 수정이 조용히 사라진다.

3장 표의 `바탕 sha256` 은 **이 PC 가 2026-08-31 20:06 에 내보낸 판**의 값이다.
이 PC 의 현재 파일 sha256 을 구해 아래 규칙대로 판정해라.

| 현재 파일의 sha256 | 뜻 | 처리 |
| --- | --- | --- |
| **`바탕 sha256` 과 같다** | 내보낸 뒤 고친 적이 없다 | **안전하다.** 번들 것으로 교체한다 |
| **`담긴 sha256` 과 같다** | 이미 적용돼 있다 | 할 일 없음. 건드리지 않는다 |
| **둘 다 아니다** | **그 사이 이 PC 에서 고쳤다** | **멈춘다.** 덮어쓰면 그 수정이 사라진다. diff 를 전부 보여주고 사용자 판단을 받는다 |

- [ ] 3개 파일 각각을 위 표로 판정해 보고했다
- [ ] 「둘 다 아니다」가 하나라도 있으면 **적용하지 않고** diff 를 보여줬다

### C. 변경 목록 제시와 승인 - 여기서 반드시 멈춘다

**여기까지 아무것도 바뀌지 않았다.** 전역 하네스는 그 PC 의 **모든 저장소**에 걸리므로,
사용자가 목록을 보고 판단할 기회를 반드시 준다.

- [ ] 파일마다 **지금 몇 줄 → 적용 후 몇 줄 · 추가 N줄 · 삭제 N줄**을 표로 적는다
- [ ] **`CLAUDE.md` 는 전역 판단 기준이므로 diff 를 한 줄도 빠뜨리지 말고 보여준다**
- [ ] **삭제되는 내용이 없다는 것**을 확인해 명시한다 (3장 「무엇이 바뀌나」의 삭제 줄 수와 대조)
- [ ] **되돌리는 방법**(D 의 롤백 명령)을 표 아래에 함께 적는다
- [ ] **표를 보여준 뒤 멈춘다.** 승인 전에는 단 하나도 적용하지 않는다
- [ ] 사용자가 파일을 지목해 제외하면 **그것만 빼고** 진행한다. 나머지를 임의로 조정하지 않는다

> `handoff/SKILL.md` 와 `handoff/TEMPLATE.md` 는 **세트다.** 하나만 적용하면
> 스킬이 설명하는 자리가 템플릿에 없거나 그 반대가 된다. 뺄 거면 둘 다 뺀다.

### D. 백업 - 되돌릴 수 있게 만든 뒤에 적용한다

```bash
D=$(TZ=Asia/Seoul date +%F); B=~/.claude/backups/$D-하네스동기화
[ -e "$B" ] && B="$B-2"
mkdir -p "$B"
cp ~/.claude/CLAUDE.md "$B"/ 2>/dev/null
mkdir -p "$B"/handoff && cp ~/.claude/skills/handoff/*.md "$B"/handoff/ 2>/dev/null
ls -la "$B" "$B"/handoff
```

- [ ] 덮어쓸 파일 3개가 전부 백업 폴더에 있다

**롤백 (문제가 생기면 이대로 되돌린다)**:

```bash
D=<백업한 날짜>; B=~/.claude/backups/$D-하네스동기화
cp "$B"/CLAUDE.md ~/.claude/CLAUDE.md
cp "$B"/handoff/*.md ~/.claude/skills/handoff/
```

### E. 적용 - C 에서 승인된 것만

- [ ] 승인 목록에 있는 것만 배치한다. **제외된 파일은 건드리지 않는다**
- [ ] `BEGIN FILE` 헤더의 `mode=` 를 그대로 반영한다
- [ ] **`settings.json` 은 건드리지 않는다.** 이 번들에 들어 있지 않다
- [ ] **아무 파일도 삭제하지 않았다**

**중단**: 기존 내용이 하나라도 사라지면 **즉시 D 의 롤백**을 하고 보고한다.

### F. 보고

1. A 무결성 결과 (3/3 일치했는지)
2. **B 바탕 판 대조 표** - 파일마다 어느 경우였는지
3. C 에서 승인된 항목과 제외된 항목
4. 백업 폴더 경로와 롤백 명령
5. **사용자가 직접 해야 할 것** - **세션 재시작**(스킬은 세션 시작 시 읽힌다), git 커밋

적용 후 `/handoff` 스킬이 인자를 받는지는 **세션을 다시 시작해야** 확인된다.
**「됐을 것이다」로 대신하지 마라.**

---

## 2. 절대 하지 말 것

- **C 승인 없이 적용** - 목록을 보여주고 답을 받기 전에는 아무것도 바꾸지 않는다
- **B 에서 「둘 다 아니다」가 나왔는데 그냥 덮어쓰기** - 그 PC 의 수정이 사라진다
- **번들에 없는 파일을 지우거나 「이 PC 에만 있는 것」으로 분류하기** - 델타 번들이다
- **`settings.json` 수정** - 이 번들의 대상이 아니다
- **4장 훅 등록을 `settings.json` 에 병합하기** - 참고용 목록이다
- **번들 내용을 손으로 편집** - sha256 이 깨져 A 에서 중단된다
- **git 커밋·푸시** - 사용자가 직접 한다

---

## 3. 담은 파일

| 경로 | 바이트 | 줄 | 바탕 sha256 (앞 16자) | 담긴 sha256 (앞 16자) |
| --- | ---: | ---: | --- | --- |
| ~/.claude/CLAUDE.md | 18288 | 222 | `fc63116638da4b20` | `d2ae235d70975c93` |
| ~/.claude/skills/handoff/SKILL.md | 5783 | 75 | `be2d38ae7979f60d` | `7a7fee5125f681d7` |
| ~/.claude/skills/handoff/TEMPLATE.md | 2363 | 65 | `c1e5b4f7fdc3cd0f` | `fa353ac2105339e5` |

**전체 sha256** (B 대조에 쓴다):

- `~/.claude/CLAUDE.md`
  - 바탕 `fc63116638da4b20c98801955ea10542a9639392688eae08b0719ebe27051c74`
  - 담김 `d2ae235d70975c937d7b71d5cd0d08e82d092d762f9672553476cd81cd45cf66`
- `~/.claude/skills/handoff/SKILL.md`
  - 바탕 `be2d38ae7979f60da09fd2f26be768dc04da6d6b477ace2496d2bcdd35ef49ad`
  - 담김 `7a7fee5125f681d78d8c479a0f0e2499d51f197a456f34cd7f6f98738487f592`
- `~/.claude/skills/handoff/TEMPLATE.md`
  - 바탕 `c1e5b4f7fdc3cd0f2d678d175629bc790c42c823e1d406a2b177a4f984dcb183`
  - 담김 `fa353ac2105339e578a392be8ef79bcceaf1d083a341d9de4e793ffae0f8ccb4`

### 왜 담았나 · 무엇이 바뀌나

**`~/.claude/CLAUDE.md`** - 이 PC 에만 있는 「구글 스프레드시트 (Google Sheets MCP)」 절이 들어 있다

  **추가 18줄 · 삭제 0줄.** 「구글 스프레드시트 (Google Sheets MCP)」 절 하나가 통째로 붙는다 (OAuth 키 경로 · `--with "mcp<2"` 제약 · 기본 시트명 `시트1` 함정 · 토큰 7일 만료). 나머지 204줄은 그 PC 것과 한 글자도 다르지 않다

**`~/.claude/skills/handoff/SKILL.md`** - 이 PC 가 더 새 것 - 「이번 지시」(인자 처리) 기능을 갖고 있다. TEMPLATE.md 와 세트다

  **추가 20줄 · 삭제 2줄.** 「호출 - 뒤에 붙인 텍스트가 「이번 지시」다」 절 18줄이 붙고, `argument-hint` 1줄과 「이번 세션에 원하는 것」 설명 1줄이 교체된다

**`~/.claude/skills/handoff/TEMPLATE.md`** - 이 PC 가 더 새 것 - 위 기능이 쓰는 자리. SKILL.md 와 세트다

  **추가 3줄 · 삭제 0줄.** 「이번 지시」 원문 인용 블록 1줄과 「언제 넣나」 표 1행

## 4. 참고 - 만든 PC 에만 있는 훅 등록 (**가져가지 않는다**)

**이 번들은 훅 파일을 하나도 담지 않았고, 훅 등록도 가져가지 않는다.**
전부 만든 PC 전용이기 때문이다 - `block-*.py` 3개는 기행기소 업무용이라 사용자가 제외했고,
나머지는 `OS 의존`(macOS `terminal-notifier`) 이거나 그 PC 의 `절대 경로`다.
**이 표를 `settings.json` 에 병합하지 마라.** 두 PC 가 일부러 다르게 두는 부분을 기록해 둔 것이다.

| 이벤트 | 매처 | command (앞 90자) | 왜 안 가져가나 |
| --- | --- | --- | --- |
| SessionStart | (매처 없음) | `f=/Users/yubeen/Workspace/ghg-docs/_bootstrap/session_start.py; [ -f "$f" ] && python3 "$f` | 절대 경로 - 그 PC 에는 없는 경로다 |
| Stop | (매처 없음) | `p=$(basename "$(jq -r '.cwd // empty' 2>/dev/null)" 2>/dev/null); terminal-notifier -title` | OS 의존 |
| PermissionRequest | (매처 없음) | `j=$(cat); [ "$(printf '%s' "$j" \| jq -r '.tool_name // empty' 2>/dev/null)" = 'AskUserQues` | OS 의존 |
| PreToolUse | AskUserQuestion | `p=$(basename "$(jq -r '.cwd // empty' 2>/dev/null)" 2>/dev/null); terminal-notifier -title` | OS 의존 |
| PreToolUse | Bash | `python3 "$HOME/.claude/hooks/block-db-connect.py"` | 기행기소 업무 전용 - 파일을 담지 않았다 |
| PreToolUse | Bash | `python3 "$HOME/.claude/hooks/block-ghg-prd-write.py"` | 기행기소 업무 전용 - 파일을 담지 않았다 |
| PreToolUse | Bash | `python3 "$HOME/.claude/hooks/block-ghgq-script-registry.py"` | 기행기소 업무 전용 - 파일을 담지 않았다 |

### 이 절차로는 맞출 수 없는 나머지 차이

- **`.claude/settings.local.json`** - 저장소마다 있으나 git 에 올라가지 않는다.
  권한 허용 목록이 PC 마다 달라 **승인 프롬프트가 계속 다르게 뜬다**
- **`~/.config/git/ignore`** - 만든 PC 에는 `**/.claude/settings.local.json` 한 줄이 있다
- **`~/.claude/plugins/marketplaces/`** - 만든 PC 는 `claude-plugins-official`

---

## 5. 원문 파일

===== BEGIN FILE: ~/.claude/CLAUDE.md (sha256=d2ae235d70975c937d7b71d5cd0d08e82d092d762f9672553476cd81cd45cf66 lines=222 bytes=18288 mode=0o644) =====
# 전역 작업 규칙 (모든 프로젝트 공통)

> 개인 작업 철학 — 어느 프로젝트에서든 적용된다. 프로젝트별 규칙·아키텍처·검증 지침은 각 repo의 `CLAUDE.md`를 따른다.

## 사고 절차 (Think Before Coding)

작업을 시작하기 전 항상 다음 원칙을 따른다.
- 최대한 정밀하고 체계적으로 분석한다. (토큰 아낄 필요 없음)
- **가정은 명시적으로 표현한다**: 암묵적 가정 금지. 작업 전 가정·제약을 사용자에게 드러낸다.
- **추측 금지**: 불확실한 사항은 즉시 사용자에게 질문한다. 100% 확신이 없는 작업은 절대 진행하지 않는다. 100% 확신을 가질 때까지 피드백을 받고 진행한다.
- **여러 해석이 가능하면 모두 제시한다**: 임의로 하나를 선택하지 않는다.
- **더 단순한 대안이 있으면 푸시백한다**: 사용자 요청보다 간단한 방법이 있으면 먼저 제안한다.
- **모호하면 멈춘다**: 무엇이 불분명한지 짚고, 답을 받기 전까지 진행하지 않는다.
- **질문은 라운드로 묶는다**: 하나씩 순차로 묻지 않는다. 지금 답할 수 있는 질문(앞선 답에 의존하지 않는 것) 전부를 한 번에 제시하고, 답을 받은 뒤 다음 라운드를 계산한다. 아직 안 정해진 것에 의존하는 질문은 다음 라운드로 미룬다.
- **사실을 찾는 것은 내 일이다**: 파일·명령·조회로 확인할 수 있는 것은 사용자에게 묻지 않는다. 사용자에게 묻는 것은 **결정**뿐이다.

### 질문 형식 표준화

사용자에게 확인이 필요할 때는 아래 형식을 사용한다.
- **현재 확인된 사실**: 조사로 확인한 객관적 정보
- **가능한 선택지**: 선택 가능한 옵션
- **각 선택지의 영향**: 장단점·리스크
- **추천안**: 근거와 함께 제시

## 수술적 변경 (Surgical Changes)

- 직접적으로 지시받은 로직 외의 소스코드는 수정하지 않는다.
- 요청 외 인접 코드·포맷·주석을 임의로 개선하지 않는다. 깨지지 않은 코드는 리팩토링하지 않는다.
- 기존 코드/UI 스타일과 톤앤매너를 매칭한다. 본인 선호와 다르더라도 일관성을 우선한다.
- 본인 변경으로 생긴 orphan(미사용 import·변수·함수)만 정리한다. 사전에 존재하던 데드 코드는 사용자 요청 없이 삭제하지 않고 언급만 한다.
- diff에 포함되는 모든 라인은 사용자 요청과 직접 연결되어야 한다.

### 조회 결과·데이터를 임의로 가공하지 않는다 (마스킹 금지)

**DB 조회 결과·추출 데이터·로그를 마스킹·삭제·요약·집계로 대체하지 않는다.** 사용자가 **명시적으로 요청할 때만** 수행한다.

- 개인정보(이름·아이디·회원ID·연락처)가 보여도 **그대로 둔다.** "개인정보라서 가렸다"는 선의의 판단도 금지다.
- 원본 그대로 두되, 취급 주의가 필요하면 **문서에 경고만 덧붙인다.** 값은 건드리지 않는다.
- 긴 결과를 줄이고 싶어도 **행을 임의로 잘라 집계로 대체하지 않는다.**

**Why:** ① 원본이 **근거물**이다. 가공하는 순간 재현·검증이 불가능해지고, 나중에 "이 값이 맞나"를 되물을 방법이 사라진다. ② **집계를 새로 만들면 계산 오류가 섞인다** — 실제로 겪었다. 원본의 종류 수와 합계를 둘 다 틀리게 옮겼고, 애초에 잘린 목록이라 합계를 낼 수 없는 데이터였다. ③ 무엇을 가렸는지 다음 사람이 모른다.

### 기존 함수 재사용 전 검증 (Existence ≠ Endorsement)

**"이미 구현되어 있으니 쓰면 된다"는 판단은 성립하지 않는다.** 존재한다는 사실이 사용해도 된다는 뜻은 아니다 — **쓰면 안 되는 함수일 수도 있다.**

기존 함수를 재사용하려 할 때는 착수 전 아래를 **반드시 확인**한다.

1. **실제 호출처를 전수 확인한다** (`grep`으로 정의부 제외 카운트).
   - 호출 0건 → 미완성·폐기 예정·의도적 미사용 중 하나일 수 있다. **왜 안 쓰는지**를 먼저 따진다.
   - 간접 호출만 있는 경우(다른 함수 내부에서만 호출) → 그 함수는 **standalone 사용이 검증된 적이 없다**.
2. **같은 목적을 이 프로젝트가 어떤 관용으로 처리하는지 확인한다.**
   기존 관용이 N곳에 일관되게 있다면, 한 곳만 다른 방식을 쓰는 것은 「기존 스타일 매칭」 원칙에 어긋난다.
3. **관용을 이탈하려면 그 이유를 먼저 사용자에게 제시하고 승인받는다.**
4. 조사 결과는 **호출처 개수와 위치를 숫자로** 보고한다. "헬퍼가 있다"가 아니라 "직접 호출 0건, 간접 21곳"처럼.

> 이 검증을 건너뛰면 **데드 코드를 되살리거나, 프로젝트 관용에서 벗어난 코드를 심게 된다.** 둘 다 다음 사람이 치른다.

## 개발 원칙

- **YAGNI**: 필요성이 확인될 때 구현한다.
- **간결성**: 불필요한 추상화를 지양한다.
- **재사용성**: 반복 호출 가능성이 있는 로직은 재사용 가능하게 모듈화한다.
- **영향도 관리**: 공유 모듈을 변경하면 전체 영향도를 먼저 확인한다. 그 프로젝트의 공유 모듈이 무엇인지는 각 repo `CLAUDE.md` 를 본다.
- **자문 체크**: "시니어 엔지니어가 과복잡하다고 볼까?" — 그렇다면 단순화한다.
- **사용자 중심**: 사용자에게 보이는 문자열(에러 메시지·CLI 출력·UI 문구)은 한글로, **무엇이 잘못됐고 무엇을 해야 하는지** 알 수 있게 쓴다. 로그·주석·식별자는 그 프로젝트의 기존 관용을 따른다.

### 만들기 전에 사다리를 오른다

구현을 시작하기 전 아래 순서로 확인하고, **먼저 걸리는 곳에서 멈춘다.**

1. **이게 존재할 필요가 있나?** — 추측성 필요면 만들지 않고 한 줄로 밝힌다.
2. **이미 이 코드베이스에 있나?** — 있으면 재사용한다. 단, 위 「기존 함수 재사용 전 검증」을 먼저 통과해야 한다 — 존재한다는 사실은 써도 된다는 뜻이 아니다.
3. **표준 라이브러리·프레임워크가 해주나?** — 해주면 그걸 쓴다.
4. **이미 설치된 의존성이 해결하나?** — 몇 줄로 되는 일에 새 의존성을 추가하지 않는다.
5. 그 다음에야 **동작하는 최소 코드**를 쓴다.

사다리는 **문제를 이해한 다음에** 오르는 것이지 이해를 대신하지 않는다. 이해를 건너뛰고 나온 작은 diff 는 효율이 아니라 두 번째 버그다.

### 버그 수정은 증상이 아니라 근원을 고친다

티켓은 증상을 말한다. 고치기 전에 **손댈 함수의 호출처를 전부 grep 한다.** 공유 함수 한 곳에 가드를 넣는 것이 호출처마다 넣는 것보다 작은 diff이고, 티켓이 지목한 경로만 고치면 형제 호출처는 깨진 채 남는다.

### 단순화하지 않을 것

신뢰 경계의 입력 검증 · 데이터 손실을 막는 예외 처리 · 보안 조치 · 접근성 · 명시적으로 요청받은 것. 사용자가 전체 버전을 원하면 만들고 재론하지 않는다.

## 목표 주도 실행 (Goal-Driven Execution)

착수 전에 **검증 가능한 성공 기준**을 정의한다. "동작하게 만들기" 같은 약한 기준은 매 단계 사용자 확인을 요구하지만, 강한 기준은 스스로 반복해 도달할 수 있게 한다.

- 모호한 작업을 검증 가능한 목표로 바꾼다.
  - "검증 추가" → "잘못된 입력에서 실패하는 절차를 먼저 만들고, 통과시킨다"
  - "버그 수정" → "버그를 재현하는 절차를 먼저 만들고, 수정 후 재현되지 않음을 확인한다"
- 다단계 작업은 단계마다 검증 방법을 붙여 제시한다.

  ```
  1. [단계] → 검증: [확인 방법]
  2. [단계] → 검증: [확인 방법]
  ```

- **검증 수단은 그 프로젝트가 허용하는 것으로 고른다.** 테스트 코드가 관용인 프로젝트는 테스트로, 테스트를 만들지 않는 프로젝트는 **조회 쿼리·CLI 재실행·HTTP 호출·로그·화면 캡처·수동 실행**으로 한다. 수단이 다를 뿐 "미리 정한 기준으로 확인한다"는 같다. **그 프로젝트가 무엇을 쓰는지는 각 repo `CLAUDE.md` 를 본다.**

## 계획서 선행

- **모든 소스코드 변경은 계획서를 먼저 작성해 승인받은 후 진행한다.** 바로 수정에 들어가지 않는다.
- **예외**(계획서 없이 진행 가능): 오타 수정, 주석 수정, 로그 메시지 수정.
- 계획 수립 시 **작업 지시서 자체의 논리적 모순·비합리적 지시·누락**도 함께 검증한다.
- 계획서 작성 및 **모든 업데이트마다** 아래 「검증 지침」에 따라 자체 검증 후 승인을 요청한다.
- **절차의 SoT 는 `/impl-plan` 스킬이다.** 파일 위치(`docs/plans/PLAN_<short_name>.md`)·필수 구성·Phase 구성·Done 조건·근거 승격이 거기 있다.
- **프로젝트마다 다른 값**(검증 명령·근거 승격 목적지·커밋 기능명)은 각 repo `CLAUDE.md` 의 「계획서 규약 — 이 프로젝트의 설정」 절을 본다.

## 검증 지침

### 계획서 검증

- 최대한 디테일하게 검증한다.
- 작업 지시서 자체에 논리적 오류·누락·비합리적 지시가 없는지 검증한다.
- 전반적 작업 목표에 합리적·논리적으로 부합하는 구현인지 검증한다.
- 오류 발생 가능성이 차단되어 있는지, 동시성 이슈는 없는지 검증한다.
- 반복 호출 가능성이 있는 로직이 재사용 가능하게 모듈화되어 있는지 검증한다.
- **프로젝트의 다른 로직에 나쁜 영향이 없는지 (중요)**.
- 개선·최적화할 내용이 남아있지 않은지 검증한다.
- 위 항목 외에도 동원할 수 있는 모든 검증 방법으로 비판적으로 검증하고, 더 이상 개선 내용이 나오지 않을 때까지 반복한 뒤 최종 보고서로 업데이트한다.

> 성능·인덱스·SlowQuery 검증은 DB를 다루는 프로젝트에만 해당한다. 해당 절차는 경로 한정 규칙(`.claude/rules/`)이 담당하므로 여기에 두지 않는다.

### 소스코드 검증

- 소스코드 생성·수정 시마다 최대한 비판적으로 검증한다.
- 계획서에 명시된 모든 항목을 검증한다.
- 동원 가능한 모든 방법으로 비판적으로 검증하고, 더 이상 개선 내용이 나오지 않을 때까지 반복한 뒤 최종 보고서로 업데이트한다.
- **검증 수단(빌드·curl·시뮬레이터·브라우저 등)은 프로젝트 규칙을 따른다** — 각 repo `CLAUDE.md` 를 본다.
- 검증 결과 리포트는 별도 언급이 없더라도 요청서와 동일 폴더에 유사한 파일명으로 동기화한다. **단, 문서 배치 규칙이 따로 있는 프로젝트는 그 규칙이 우선한다.**

## 문서 참조 방향

- **영구 문서는 임시 문서를 참조하지 않는다.** 참조는 언제나 수명이 긴 쪽으로만 향한다.
  임시 문서(작업 계획서·스크래치 노트·일회성 리포트)는 언젠가 삭제되므로, 영구 문서가 링크하면 그때 통째로 깨진다.
- 임시 문서에만 있는 내용 중 남길 가치가 있으면 **영구 문서로 옮긴 뒤** 임시 문서를 정리한다.
  옮기는 것은 결론뿐 아니라 **근거**다 — "값이 X다"가 아니라 "왜 X인가". 근거를 버리면 나중에 같은 논의를 반복한다.
- 판단 기준: **"이 임시 문서를 지금 지워도 잃는 것이 없는가?"** 아니라면 아직 옮길 것이 남았다.

## 코드 파일에는 이모지를 쓰지 않는다

**SQL·소스코드 등 실행되는 파일(주석 포함)에는 이모지를 넣지 않는다.** 마크다운 문서에는 써도 된다.

- 금지: `⚠️` `🔴` `✅` `❌` 같은 **이모지**(변이선택자 `U+FE0F` 가 붙는 문자 포함).
- 대신 **대괄호 텍스트 표기**를 쓴다 — `[주의]` · `[중요]` · `(정상)` · `(불가)`.
- 허용: `─` `→` `★` `※` `「」` `①` 같은 **활자 기호**. 이모지가 아니라 폭·인코딩이 안정적이다.

**Why:** DB 클라이언트·터미널·로그 뷰어마다 이모지 렌더링과 폭 계산이 달라 **주석 정렬이 깨지고**, 편집기 인코딩이 UTF-8이 아니면 파일이 손상된다. 쿼리를 복사해 다른 도구에 붙일 때 특히 문제가 된다.

## 파일 경로 전달 방식

VSCode Claude Code 확장은 마크다운 링크 목적지를 퍼센트 인코딩하지만 파일을 열 때 디코딩하지 않는다. 그래서 **경로에 한글·공백이 있으면 링크를 클릭해도 아무 반응이 없다**(에러 메시지도 없음). 열리지 않는 링크는 클릭을 유도해놓고 아무 일도 안 일어나므로, 그 경우엔 링크를 아예 만들지 않는다.

- **경로 전체(디렉터리 포함)가 ASCII이고 공백이 없으면 → 마크다운 링크**를 쓴다. 링크 텍스트는 **파일명(필요시 `:라인`)만** 쓴다 — 전체 경로를 두 번 노출하지 않는다.
- **경로 어느 한 구간이라도 한글·공백을 포함하면 → 인라인 코드로 경로만** 쓰고 **마크다운 링크를 만들지 않는다.** 사용자는 이 경로를 복사해 `Ctrl+P`(Windows) / `⌘P`(macOS)로 연다.
- **둘 다 설명 문장 안에 인라인으로 녹인다.** 「근거 파일」 같은 경로 목록 블록을 본문 위·아래에 따로 만들지 않는다. 그 파일을 언급하는 이유(무엇이 어디에 있는지)와 경로가 같은 문장 안에 있어야 한다.
- **링크가 안 열린다는 이유로 기존 한글 파일명을 리네임하지 않는다.**

예시:

> 만나이 산식은 [GhgMemInfoRepository.java:74-80](common/src/main/java/com/myezl/ggaction/common/data/repository/GhgMemInfoRepository.java#L74-L80) 의 `>` 비교 때문에 생일 당일에 1살 적게 나온다. 로컬 전체 기동 절차는 `.claude/로컬개발환경-실행가이드.md` 에 정리돼 있다.

## 외부 도구·라이브러리 확인

- 외부 라이브러리·도구의 최신 사용법·버전 차이·공식 스펙 확인이 필요하면 **Context7 MCP로 먼저 학습**하고, 부족하거나 불명확한 부분은 웹검색으로 근거를 보완한다.

### 다른 레포에서 poetry 를 실행할 때는 `env -u VIRTUAL_ENV` 를 붙인다

세션을 시작한 레포의 가상환경이 `VIRTUAL_ENV` 로 셸에 남아 있어, `cd <다른레포> && poetry run ...` 을
하면 **poetry 가 그 레포가 아니라 세션 시작 레포의 가상환경을 쓴다.**

```bash
cd <다른레포> && env -u VIRTUAL_ENV poetry run python validate_project.py
```

**Why:** 실측으로 겪었다. 워크스페이스 세 레포의 품질 검증을 한 번에 재면서 두 레포가
`ModuleNotFoundError` 로 실패했고, 「이 PC 에 패키지가 설치되지 않았다」고 잘못 판정했다.
`env -u VIRTUAL_ENV` 를 붙여 다시 재니 **한 레포는 처음부터 전부 통과**하고 있었다.

**이 함정은 검증 결과를 조용히 바꾼다.** 실패가 코드 문제인지 실행 방법 문제인지 구분되지 않으므로,
다른 레포의 검증 수치를 근거로 삼기 전에 실행 방식부터 확인한다.

## 구글 스프레드시트 (Google Sheets MCP)

스프레드시트 생성·조회·기입이 필요하면 전역 등록된 `google-sheets` MCP 를 쓴다.
**기행기소 저장소에 한정되지 않는다 — 어느 프로젝트에서든 동일하게 쓴다.**

- **인증은 OAuth 사용자 자격이다.** 생성한 시트는 본인 소유로 내 드라이브에 바로 생긴다.
  클라이언트 `~/.claude/keys/sheets-mcp-oauth.json` · 토큰 `~/.claude/keys/sheets-mcp-token.json`
  (둘 다 권한 600, 저장소 밖이라 커밋되지 않는다)
- **등록 커맨드에 `--with "mcp<2"` 가 반드시 들어간다.** `mcp-google-sheets` 는 MCP SDK 2.x 와
  호환되지 않는다(`FastMCP` → `MCPServer` 개명). 빠지면 `CONNECTION_CLOSED` 로만 보여
  원인을 짐작할 수 없다.
- **새 시트의 기본 이름은 `시트1` 이다**(한국어 계정). `Sheet1` 로 범위를 지정하면
  `Unable to parse range` 가 난다. 쓰기 전에 `list_sheets` 로 실제 이름을 확인한다.
- **동의화면이 「테스트」 상태라 refresh token 이 7일마다 만료된다.**
  만료되면 `sheets-mcp-token.json` 을 지우고 한 번 더 인증한다.
- **기입 후에는 `get_sheet_data` 로 읽어 원본과 대조한다.** 손으로 옮기면 오타가 섞인다.
- 키 파일은 자격증명이다. 내용을 출력·복사·커밋하지 않는다.

## 작업 절차 스킬

절차가 긴 작업은 전역 스킬(`~/.claude/skills/`)로 분리돼 있다.

> **`/handoff` 는 사용자 전용 스킬이다** (`disable-model-invocation: true`). 설계상 **Claude 의 스킬 목록에 뜨지 않는 것이 정상**이며, 사용자가 `/handoff` 를 치면 정상 동작한다. 목록에 없다는 이유로 「호출하면 실패한다」고 보고하지 않는다.

| 스킬 | 언제 |
|---|---|
| `impl-plan` | 계획서(Implementation Plan) 작성·갱신·Done 판정. 코드 변경 전에 쓴다. 훅이 이 스킬을 가리킨다 |
| `diagnosing-bugs` | 원인 분석·버그 진단. 재현 절차 확보 → 가설 순위 제시 → 검증 순서를 강제한다 |
| `grilling` | 계획·설계를 확정하기 전 라운드 인터뷰 |
| `ponytail-review` · `ponytail-audit` | 과설계 점검 (변경분 / 저장소 전체). 목록만 내고 적용하지 않는다 |
| `writing-for-agents` | 스킬·CLAUDE.md 를 쓰거나 고칠 때 |
| `/handoff` | 세션을 다음으로 넘길 때 — 새 세션에 **그대로 붙여넣는 「다음세션 프롬프트」**를 만든다. **사용자만 호출 가능** |

## Git

- 모든 Git 처리는 사용자가 직접 진행하므로, Claude는 Git 관련 어떠한 처리도 하지 않는다.
===== END FILE: ~/.claude/CLAUDE.md =====

===== BEGIN FILE: ~/.claude/skills/handoff/SKILL.md (sha256=7a7fee5125f681d78d8c479a0f0e2499d51f197a456f34cd7f6f98738487f592 lines=75 bytes=5783 mode=0o644) =====
---
name: handoff
description: 현재 대화를 다음 세션에 그대로 붙여넣을 「다음세션 프롬프트」로 압축한다.
argument-hint: "다음 세션에 덧붙일 지시 — 목적·조건·제약 (생략 가능)"
disable-model-invocation: true
---

# 다음세션 프롬프트

**요약문이 아니라 지시서를 쓴다.** 산출물은 사용자가 새 세션 첫 메시지에 **그대로 복사해 붙이면 작동하는 프롬프트**여야 한다. 과거 세션을 서술하지 말고, **다음 세션에게 말한다.**

한글로 쓴다. 형식은 이 폴더의 [TEMPLATE.md](TEMPLATE.md) 를 따른다.

## 호출 — 뒤에 붙인 텍스트가 「이번 지시」다

```
/handoff 한도 대상자는 제외할것.
```

```
/handoff
리워드 금액 파악 하기 위함
리워드는 A조건으로 진행
```

`/handoff` 뒤에 붙은 텍스트 전부가 **「이번 지시」**다. 여러 줄이면 **줄마다 별개 지시**이고, 목적일 수도 조건·제약일 수도 있다. 붙은 텍스트가 없으면 세션 내용만으로 프롬프트를 만든다.

- **세션에서 도출한 과업 위에 얹는다.** 「이번 지시」는 「이번 세션에 원하는 것」 절 **한 곳**에 모두 모은다 — 절마다 흩뿌리면 사용자가 준 지시가 어디로 갔는지 추적할 수 없다. 위 두 번째 예시처럼 **제약만 왔으면 과업은 세션에서 도출한 것이 그대로 남는다.**
- **원문을 그대로 옮긴다.** 「A조건」 같은 축약을 풀어 쓰면 다음 세션이 다른 것을 한다. 정돈된 문장이 필요하면 **원문 아래에 덧붙인다.**
- **「현재 확정된 결론」과 어긋나면 「이번 지시」가 이긴다** — 사용자의 최신 지시이기 때문이다. 뒤집힌 결론은 프롬프트에 **무엇이 왜 바뀌었는지** 남기고, 사용자에게도 한 줄로 알린다.

## 먼저 확인한다

1. **이 세션에 인계할 대화가 있는가.** `/handoff` 가 첫 메시지면 요약할 이력이 없다. 파일에서 역추정한 내용을 「세션 요약」으로 포장하지 않는다 — 인계의 핵심인 **결정의 근거와 짚은 헛다리**는 사라진 대화에만 있어 담을 수 없다. 그 사실을 보고하고 멈춘다.
2. **이미 인계문서 역할을 하는 파일이 있는가.** `git status` 와 루트의 `*.md` 를 본다. 있으면 **새로 만들지 않고 그것을 갱신한다.** 두 벌이 되면 다음 세션이 어느 쪽을 믿을지 모른다.
3. 기존 문서가 있다면 **어디가 낡았는지** 먼저 찾아 보고한다 — 그 이후 생긴 산출물, 바뀐 결론, 완료된 항목.

## 파일

**위치와 추적 여부는 그 프로젝트의 규칙을 따른다.** 각 repo `CLAUDE.md` 를 본다.

| 그 저장소가 | 어떻게 |
|---|---|
| 인계 프롬프트를 **커밋한다**고 정했으면 | 정한 경로·파일명으로 만들고 **추적 대상**으로 둔다. 여러 PC 에서 이어서 작업하는 것이 목적이면 이쪽이다 — **untracked 는 다른 PC 로 넘어가지 않는다** |
| 규칙이 없거나, **문서가 커밋되면 안 되는** 저장소면 | 저장소 루트에 **untracked**. 커밋 대상 경로(`src/`·`docs/`)에는 만들지 않는다 |

- 파일명도 그 프로젝트 규칙을 따른다. 규칙이 없으면 `[한글주제]_다음세션_프롬프트.md`.
- **파일명을 고정해 매번 같은 파일을 갱신한다.** 세션마다 새 파일을 만들면 두 벌이 되고 다음 세션이 어느 쪽을 믿을지 모른다.
- 만든 뒤 `git status` 로 **의도한 추적 상태인지** 확인하고, 경로와 추적 여부를 사용자에게 알린다.

## 각 절을 쓰는 법

**먼저 읽을 것 (순서대로)** — 이 절이 프롬프트의 값어치를 결정한다. 경로만 나열하지 말고 **읽는 순서**와 **각 문서의 함정**을 같이 적는다.

> 나쁨: `조사리포트/INPD-1387_종합리포트.md` 를 읽는다.
> 좋음: `§14-12(최종 확정 결과)를 가장 먼저 읽는다. §1~§9는 1차 조사 기록이고 그때의 「4,997명/988만원」은 과대 계상이라 폐기된 값이다.`

**현재 확정된 결론** — 다음 세션이 **다시 논의하면 안 되는** 것. 숫자는 숫자로 적는다. 확정 근거가 특정 문서에 있으면 절 번호까지 가리킨다.

**남은 일** — `# / 할 일 / 상태` 표. 상태는 `진행 중`·`미착수`·`대기`·`미실행` 처럼 실제 상태를 쓴다. 각 항목에 **필요한 파일 경로**를 붙인다.

**이번 세션에 원하는 것** — 명령문으로 쓴다. 산출물이 있으면 **구성을 번호로 지정**한다.

**이미 밟은 함정** — 이번에 헛다리 짚은 것, 틀렸던 가정, 다시 시도하면 안 되는 접근. **이 절이 없으면 다음 세션은 같은 길을 다시 걷는다.**

**작업 규칙** — 이 건 **특유의** 규칙만 쓴다. 전역 CLAUDE.md 에 이미 있는 규칙을 재기술하지 않는다. 「이 작업에서만 다르게 해야 하는 것」이 있을 때만 이 절을 둔다.

## 담지 않을 것

**이미 다른 산출물에 있는 내용을 복사하지 않는다.** 계획서·리포트·SQL·커밋·조회 결과는 **경로로 참조**한다. 복사하면 두 벌이 되고 한쪽이 낡는다. 이 프롬프트는 **지도이지 사본이 아니다.**

## 값의 처리

- **비밀정보**(토큰·비밀번호·API 키·인증 헤더)는 제거한다.
- **조회 결과의 개인정보**(이름·로그인ID·회원ID·연락처)는 **가공하지 않는다.** 원본 파일을 경로로 참조하고, 취급 주의가 필요하면 경고만 덧붙인다. 전역 「조회 결과를 임의로 가공하지 않는다」가 우선한다.
===== END FILE: ~/.claude/skills/handoff/SKILL.md =====

===== BEGIN FILE: ~/.claude/skills/handoff/TEMPLATE.md (sha256=fa353ac2105339e578a392be8ef79bcceaf1d083a341d9de4e793ffae0f8ccb4 lines=65 bytes=2363 mode=0o644) =====
# 다음세션 프롬프트 템플릿

아래 골격을 따른다. **해당 없는 절은 통째로 뺀다** — 빈 절을 남기지 않는다.
꺾쇠 `<>` 안은 채울 자리다.

---

```markdown
<무슨 작업인지 한 줄>. 이어서 진행한다.
**<지금까지의 상태>이고, 이번 세션의 목표는 「<한 단어~한 구절>」이다.**

## 먼저 읽을 것 (순서대로)

1. `<경로>`
   - **<어느 절을 먼저 볼지>를 가장 먼저** 읽는다.
   - <그 문서의 함정 — 폐기된 값, 낡은 절, 오해하기 쉬운 지점>
2. `<경로>` — <왜 읽는지 한 줄>
3. `<경로>` — <실행 순서나 주의가 있으면 함께>

## 현재 확정된 결론

- <다시 논의하면 안 되는 사실. 숫자는 숫자로>
- <확정 근거가 어디에 있는지 절 번호까지>

## 남은 일

| # | 할 일 | 상태 |
|---|---|---|
| 1 | **<할 일>** — <필요한 파일 경로> | **진행 중** |
| 2 | <할 일> | 미착수 |
| 3 | <할 일> — <조건이나 시점이 있으면 함께> | 대기 |

## 이번 세션에 원하는 것

> <「이번 지시」 원문 그대로 — 여러 줄이면 줄바꿈을 유지한다>

**<명령문으로 한 줄>.** <산출물이 있으면> 구성은 이렇게.

1. **<항목>** — <무엇을 담는지>
2. **<항목>** — <무엇을 담는지>
3. **<항목>** — <무엇을 담는지>

## 이미 밟은 함정 — 다시 밟지 말 것

- <틀렸던 가정과 왜 틀렸는지>
- <시도했다가 버린 접근과 버린 이유>

## 작업 규칙

- <이 작업 특유의 규칙만. 전역 CLAUDE.md 에 있는 것은 쓰지 않는다>
```

---

## 절별 판단 기준

| 절 | 넣는 조건 | 빼는 조건 |
|---|---|---|
| 먼저 읽을 것 | **항상** — 이 절이 프롬프트의 값어치다 | — |
| 현재 확정된 결론 | 다시 논의하면 안 되는 사실이 있을 때 | 아직 아무것도 확정 안 됐을 때 |
| 남은 일 | 항상 | — |
| 이번 세션에 원하는 것 | **항상** — 없으면 지시서가 아니라 요약문이다 | — |
| └ 이번 지시 (원문 인용) | `/handoff` 뒤에 텍스트가 붙었을 때 | 붙은 텍스트가 없을 때 |
| 이미 밟은 함정 | 헛다리를 짚었을 때 | 순탄했을 때 |
| 작업 규칙 | 이 건에서만 다르게 해야 할 것이 있을 때 | 전역 규칙으로 충분할 때 |
===== END FILE: ~/.claude/skills/handoff/TEMPLATE.md =====

