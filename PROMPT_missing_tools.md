# 의뢰 — 권한 규칙은 갔는데 도구가 안 간 것 (mac -> WSL)

> **이 파일 하나로 두 PC 가 각자 할 일을 한다.** 세션에 이 파일 경로만 주면 된다.
>
> **먼저 0 절로 어느 PC 인지 판별하고, 자기 절차만 수행한다.** 남의 절차는 읽지 않는다.
> 수행한 항목은 **이 파일의 체크박스를 채우고, 결과를 4 절에 적는다** — 그 상태를 git 으로
> 넘겨 다음 PC 가 이어받는다.

## 0. 이 PC 가 어느 쪽인가

```bash
python3 -c "import socket, sys; print(socket.gethostname(), '/', sys.platform)"
```

| 결과 | 이 PC 는 | 할 일 |
| --- | --- | --- |
| `darwin` 이 나온다 | **보내는 쪽 (mac)** | **1 절** |
| `linux` 이고 호스트가 `DESKTOP-` 로 시작 | **받는 쪽 (WSL)** | **2 절** |
| 그 밖 | 판단할 수 없다 | 사용자에게 어느 쪽인지 묻고 시작한다 |

## 무엇이 문제인가 (양쪽 공통, 한 번만 읽는다)

`claude-config-import` 로 mac 의 하네스를 WSL 에 적용한 뒤(2026-09-09), **권한 규칙은 왔는데
그 규칙이 가리키는 실행 파일이 받는 쪽에 없는 항목**이 남았다.

| `tools/` 아래 | 번들에 담겼나 | 안에 무엇이 있었나 |
| --- | --- | --- |
| `msg/` | **왔다** (README + `.py` 5개) | venv 밖에 파일이 있었다 |
| `pptx/` | **왔다** (`build_v14.py`) | 같음 |
| `xlsx/` | **안 왔다** | **venv 뿐이었던 것으로 보인다** |

원인은 `export.py` 의 `DENY_SEGMENTS` 에 있는 `venv` 다 — 폴더 안이 전부 걸러지면
**담을 것이 0개가 되어 폴더가 없는 것과 같아진다.** `tools` 자체는 허용 목록에 있고
실제로 `msg`·`pptx` 는 담겼으므로, 남은 것은 mac 에서 이 마지막 한 칸을 확인하는 일이다.

**함께 풀어야 할 것**: `tools/pptx/build_v14.py` 는 **`lxml` 과 `python-pptx` 를 쓰는데**
(import 로 확인) **그것을 돌릴 인터프리터가 번들에 없다.** 권한 규칙에 있는 venv 경로는
`tools/xlsx/venv` 하나뿐이라, **mac 이 그 venv 를 여러 도구의 공용으로 쓴다는 가설**이 선다.
맞다면 venv 하나만 재생성해도 두 도구가 함께 산다.

`tools/msg/` 는 표준 라이브러리만 써서 **이미 동작한다. 할 일이 없다.**

---

## 1. 보내는 쪽 (mac) 에서 할 일

- [ ] **1-1. `tools/` 에 실제로 무엇이 있는지 본다**

  ```bash
  ls -laR ~/.claude/tools/
  find ~/.claude/tools -type f -not -path '*/venv/*'
  ```

  `xlsx/` 안에 venv 말고 파일이 있으면 **위 가설이 틀린 것**이다. 그때는
  `export.py` 의 `should_include()` 로 그 파일을 직접 판정해 왜 빠졌는지 밝힌다.

- [ ] **1-2. 그 venv 의 패키지 목록을 뽑는다**

  ```bash
  ~/.claude/tools/xlsx/venv/bin/python -m pip freeze
  ```

  **이 출력이 곧 받는 쪽의 설치 목록이다.** `lxml` 과 `python-pptx` 가 들어 있는지 본다 —
  있으면 공용 인터프리터 가설이 맞고, 없으면 `pptx` 는 **별도 venv 를 쓰고 있다**는 뜻이라
  그 경로도 찾아야 한다.

- [ ] **1-3. 두 도구를 무슨 인터프리터로 돌리는지 확인한다**

  ```bash
  grep -rn 'tools/xlsx\|tools/pptx\|build_v14' ~/.claude/
  ```

  부르는 곳이 없으면 **대화에서 직접 실행하는 용도**다. 어떤 작업에 썼는지 사용자에게 묻는다.

- [ ] **1-4. 받는 쪽이 만들 venv 경로를 정한다**

  `xlsx/venv` 하나로 갈지, `pptx/venv` 를 따로 둘지 **여기서 정한다.**
  받는 쪽이 추측하면 두 PC 의 구조가 갈린다.

- [ ] **1-5. 결과를 4 절에 적는다** (패키지 목록과 경로를 그대로)

- [ ] **1-6. export 를 고칠지 결정한다**

  「담을 것이 0개가 된 폴더」를 스크립트가 **경고로 알릴지** 정한다. venv 를 담는 선택지는
  없다(바이너리·플랫폼 종속).
  - 고치기로 했다면 `export.py` 를 고치고 `claude-config-export` 스킬 문서에 남긴다
  - 고치지 않기로 했다면 **그 판단과 이유를** 같은 스킬 문서에 남긴다.
    다음 마이그레이션에서 같은 것을 다시 조사하지 않게 하는 것이 목적이다

- [ ] **1-7. 사용자에게 커밋을 요청한다** (git 은 사용자가 직접 한다)

---

## 2. 받는 쪽 (WSL) 에서 할 일

**2-0 게이트: 4 절의 「조사 결과」가 비어 있으면 여기서 멈춘다.** mac 이 1 절을 마치고
커밋한 뒤에야 시작할 수 있다. 비어 있으면 사용자에게 그 사실만 알리고 끝낸다.

- [ ] **2-1. `git pull` 로 조사 결과를 받는다**

- [ ] **2-2. 4 절이 정한 경로에 venv 를 만든다**

  ```bash
  uv venv ~/.claude/tools/<4절이 정한 폴더>/venv --python 3.12
  ```

  **`python3 -m venv` 를 쓰지 않는다** — Debian 계열은 `ensurepip` 가 별도 패키지라 실패한다.
  `uv` 는 이미 설치돼 있다(`~/.local/bin/uv`).

- [ ] **2-3. 4 절의 패키지 목록을 그대로 설치한다**

  ```bash
  uv pip install --python ~/.claude/tools/<폴더>/venv/bin/python <목록>
  ```

  **목록에 없는 것을 추측으로 더하지 않는다.** 다른 것을 깔면 그때 조용히 실패한다.

- [ ] **2-4. 실제로 도는지 검증한다**

  ```bash
  ~/.claude/tools/<폴더>/venv/bin/python -c "import pptx, lxml; print('pptx', pptx.__version__)"
  ```

  권한 규칙이 가리키는 경로와 만든 경로가 **같은지** 확인한다
  (`jq -r '.permissions.allow[] | select(startswith("Bash(/"))' ~/.claude/settings.json`).
  다르면 규칙을 고치거나 경로를 맞춘다.

- [ ] **2-5. 결과를 4 절 아래쪽에 적는다**

---

## 3. 끝나면

**양쪽 체크박스가 모두 채워지면 이 파일을 지운다.** 남길 값은 이미 스킬 문서에 있다 —
함정 자체는 `claude-config-export` 스킬의 「담을 것이 0개가 된 폴더는 통째로 사라진다」,
받는 쪽 절차는 `claude-config-import` 스킬의 6 단계다.

**이 파일에 조사 결과를 남긴 채 방치하지 않는다.** 임시 문서이므로 언젠가 지워지고,
그때 결과도 함께 사라진다.

---

## 4. 조사 결과 (mac 이 채운다)

> 1-5 에서 여기를 채운다. 받는 쪽은 **이 표만 보고** 2 절을 수행한다.

| 항목 | 값 |
| --- | --- |
| `tools/xlsx/` 안에 venv 말고 파일이 있었나 | (예 / 아니오 — 있었다면 무엇) |
| `pip freeze` 목록 | (그대로 붙여넣기) |
| `lxml`·`python-pptx` 가 목록에 있나 | (예 / 아니오) |
| 두 도구를 부르는 곳 | (경로 / 없으면 「대화에서 직접」) |
| **받는 쪽이 만들 venv 경로** | (예: `~/.claude/tools/xlsx/venv`) |
| export 를 고쳤나 | (고침 / 안 고침 — 이유) |

### 받는 쪽 결과 (WSL 이 채운다)

| 항목 | 값 |
| --- | --- |
| 만든 venv 경로 | |
| 설치한 패키지 | |
| 검증 결과 | (import 성공 여부) |
| 권한 규칙과 경로가 맞나 | (예 / 아니오 — 아니면 무엇을 고쳤나) |
