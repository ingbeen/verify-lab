# 전역 하네스 번들

> **생성 시각(KST)**: 2026-08-31 20:06
> **생성한 PC**: DESKTOP-4CN5LK3 / Linux 6.6.87.2-microsoft-standard-WSL2 / python 3.12.3
> **홈 경로**: /home/yblee
> **담은 파일**: 18개 (누락 0개)
> **기계가 만든 산출물이다.** 손으로 고치지 않는다 - sha256 이 어긋나면 설치가 중단된다.

---

## 1. 파일 목록

| 경로 | 바이트 | 줄 |
| --- | ---: | ---: |
| ~/.claude/CLAUDE.md | 16897 | 204 |
| ~/.claude/settings.json | 3713 | 147 |
| ~/.claude/skills/diagnosing-bugs/SKILL.md | 10646 | 147 |
| ~/.claude/skills/grilling/SKILL.md | 2716 | 49 |
| ~/.claude/skills/handoff/SKILL.md | 4561 | 57 |
| ~/.claude/skills/handoff/TEMPLATE.md | 2156 | 62 |
| ~/.claude/skills/impl-plan/SKILL.md | 9179 | 158 |
| ~/.claude/skills/impl-plan/template.md | 5781 | 169 |
| ~/.claude/skills/ponytail-audit/SKILL.md | 2183 | 52 |
| ~/.claude/skills/ponytail-review/SKILL.md | 2588 | 63 |
| ~/.claude/skills/writing-for-agents/SKILL-MECHANICS.md | 2629 | 22 |
| ~/.claude/skills/writing-for-agents/SKILL.md | 11888 | 91 |
| ~/.claude/hooks/plan_gate.py | 3592 | 92 |
| ~/.claude/hooks/plan_lint.py | 11264 | 311 |
| ~/.claude/hooks/toast.log | 19417 | 233 |
| ~/.claude/hooks/wsl_toast.py | 4624 | 127 |
| ~/.claude/commands/commit.md | 2563 | 39 |
| ~/.claude/commands/settings-merge.md | 2391 | 41 |

## 2. settings.json 의 훅 등록 (펼친 형태)

`OS 의존` 으로 표시된 항목은 PC 마다 달라야 정상이다. 설치 쪽에서 이 PC 값을 유지한다.

| 이벤트 | 매처 | command (앞 90자) | 비고 |
| --- | --- | --- | --- |
| Notification | (매처 없음) | `PY=$(command -v python3 \|\| command -v python); "$PY" "$HOME/.claude/hooks/wsl_toast.py"` | OS 의존 |
| PreToolUse | AskUserQuestion | `PY=$(command -v python3 \|\| command -v python); "$PY" "$HOME/.claude/hooks/wsl_toast.py"` | OS 의존 |
| PreToolUse | Edit|Write | `PY=$(command -v python3 \|\| command -v python); "$PY" "$HOME/.claude/hooks/plan_gate.py"` |  |
| PostToolUse | Edit|Write|Bash | `PY=$(command -v python3 \|\| command -v python); "$PY" "$HOME/.claude/hooks/plan_lint.py"` |  |
| Stop | (매처 없음) | `PY=$(command -v python3 \|\| command -v python); "$PY" "$HOME/.claude/hooks/wsl_toast.py"` | OS 의존 |

---

## 3. 원문 파일

===== BEGIN FILE: ~/.claude/CLAUDE.md (sha256=fc63116638da4b20c98801955ea10542a9639392688eae08b0719ebe27051c74 lines=204 bytes=16897 mode=0o644) =====
# 전역 작업 규칙 (모든 프로젝트 공통)

> 개인 작업 철학 — 어느 프로젝트에서든 적용된다. 프로젝트별 규칙·아키텍처·검증 지침은 각 repo의 `CLAUDE.md`를 따른다.

## 사고 절차 (Think Before Coding)

작업을 시작하기 전 항상 다음 원칙을 따른다.
- 최대한 정밀하고 체계적으로 분석한다. (토큰 아낄 필요 없음)
- **가정은 명시적으로 표현한다**: 암묵적 가정 금지. 작업 전 가정·제약을 사용자에게 드러낸다.
- **추측 금지**: 불확실한 사항은 즉시 사용자에게 질문한다. 100% 확신이 없는 작업은 절대 진행하지 않는다. 100% 확신을 가질 때까지 피드백을 받고 진행한다.
- **여러 해석이 가능하면 모두 제시한다**: 임의로 하나를 선택하지 않는다.
- **더 단순한 대안이 있으면 푸시백한다**: 사용자 요청보다 간단한 방법이 있으면 먼저 제안한다.
- **모호하면 멈춘다**: 무엇이 불분명한지 짚고, 답을 받기 전까지 진행하지 않는다.
- **질문은 라운드로 묶는다**: 하나씩 순차로 묻지 않는다. 지금 답할 수 있는 질문(앞선 답에 의존하지 않는 것) 전부를 한 번에 제시하고, 답을 받은 뒤 다음 라운드를 계산한다. 아직 안 정해진 것에 의존하는 질문은 다음 라운드로 미룬다.
- **사실을 찾는 것은 내 일이다**: 파일·명령·조회로 확인할 수 있는 것은 사용자에게 묻지 않는다. 사용자에게 묻는 것은 **결정**뿐이다.

### 질문 형식 표준화

사용자에게 확인이 필요할 때는 아래 형식을 사용한다.
- **현재 확인된 사실**: 조사로 확인한 객관적 정보
- **가능한 선택지**: 선택 가능한 옵션
- **각 선택지의 영향**: 장단점·리스크
- **추천안**: 근거와 함께 제시

## 수술적 변경 (Surgical Changes)

- 직접적으로 지시받은 로직 외의 소스코드는 수정하지 않는다.
- 요청 외 인접 코드·포맷·주석을 임의로 개선하지 않는다. 깨지지 않은 코드는 리팩토링하지 않는다.
- 기존 코드/UI 스타일과 톤앤매너를 매칭한다. 본인 선호와 다르더라도 일관성을 우선한다.
- 본인 변경으로 생긴 orphan(미사용 import·변수·함수)만 정리한다. 사전에 존재하던 데드 코드는 사용자 요청 없이 삭제하지 않고 언급만 한다.
- diff에 포함되는 모든 라인은 사용자 요청과 직접 연결되어야 한다.

### 조회 결과·데이터를 임의로 가공하지 않는다 (마스킹 금지)

**DB 조회 결과·추출 데이터·로그를 마스킹·삭제·요약·집계로 대체하지 않는다.** 사용자가 **명시적으로 요청할 때만** 수행한다.

- 개인정보(이름·아이디·회원ID·연락처)가 보여도 **그대로 둔다.** "개인정보라서 가렸다"는 선의의 판단도 금지다.
- 원본 그대로 두되, 취급 주의가 필요하면 **문서에 경고만 덧붙인다.** 값은 건드리지 않는다.
- 긴 결과를 줄이고 싶어도 **행을 임의로 잘라 집계로 대체하지 않는다.**

**Why:** ① 원본이 **근거물**이다. 가공하는 순간 재현·검증이 불가능해지고, 나중에 "이 값이 맞나"를 되물을 방법이 사라진다. ② **집계를 새로 만들면 계산 오류가 섞인다** — 실제로 겪었다. 원본의 종류 수와 합계를 둘 다 틀리게 옮겼고, 애초에 잘린 목록이라 합계를 낼 수 없는 데이터였다. ③ 무엇을 가렸는지 다음 사람이 모른다.

### 기존 함수 재사용 전 검증 (Existence ≠ Endorsement)

**"이미 구현되어 있으니 쓰면 된다"는 판단은 성립하지 않는다.** 존재한다는 사실이 사용해도 된다는 뜻은 아니다 — **쓰면 안 되는 함수일 수도 있다.**

기존 함수를 재사용하려 할 때는 착수 전 아래를 **반드시 확인**한다.

1. **실제 호출처를 전수 확인한다** (`grep`으로 정의부 제외 카운트).
   - 호출 0건 → 미완성·폐기 예정·의도적 미사용 중 하나일 수 있다. **왜 안 쓰는지**를 먼저 따진다.
   - 간접 호출만 있는 경우(다른 함수 내부에서만 호출) → 그 함수는 **standalone 사용이 검증된 적이 없다**.
2. **같은 목적을 이 프로젝트가 어떤 관용으로 처리하는지 확인한다.**
   기존 관용이 N곳에 일관되게 있다면, 한 곳만 다른 방식을 쓰는 것은 「기존 스타일 매칭」 원칙에 어긋난다.
3. **관용을 이탈하려면 그 이유를 먼저 사용자에게 제시하고 승인받는다.**
4. 조사 결과는 **호출처 개수와 위치를 숫자로** 보고한다. "헬퍼가 있다"가 아니라 "직접 호출 0건, 간접 21곳"처럼.

> 이 검증을 건너뛰면 **데드 코드를 되살리거나, 프로젝트 관용에서 벗어난 코드를 심게 된다.** 둘 다 다음 사람이 치른다.

## 개발 원칙

- **YAGNI**: 필요성이 확인될 때 구현한다.
- **간결성**: 불필요한 추상화를 지양한다.
- **재사용성**: 반복 호출 가능성이 있는 로직은 재사용 가능하게 모듈화한다.
- **영향도 관리**: 공유 모듈을 변경하면 전체 영향도를 먼저 확인한다. 그 프로젝트의 공유 모듈이 무엇인지는 각 repo `CLAUDE.md` 를 본다.
- **자문 체크**: "시니어 엔지니어가 과복잡하다고 볼까?" — 그렇다면 단순화한다.
- **사용자 중심**: 사용자에게 보이는 문자열(에러 메시지·CLI 출력·UI 문구)은 한글로, **무엇이 잘못됐고 무엇을 해야 하는지** 알 수 있게 쓴다. 로그·주석·식별자는 그 프로젝트의 기존 관용을 따른다.

### 만들기 전에 사다리를 오른다

구현을 시작하기 전 아래 순서로 확인하고, **먼저 걸리는 곳에서 멈춘다.**

1. **이게 존재할 필요가 있나?** — 추측성 필요면 만들지 않고 한 줄로 밝힌다.
2. **이미 이 코드베이스에 있나?** — 있으면 재사용한다. 단, 위 「기존 함수 재사용 전 검증」을 먼저 통과해야 한다 — 존재한다는 사실은 써도 된다는 뜻이 아니다.
3. **표준 라이브러리·프레임워크가 해주나?** — 해주면 그걸 쓴다.
4. **이미 설치된 의존성이 해결하나?** — 몇 줄로 되는 일에 새 의존성을 추가하지 않는다.
5. 그 다음에야 **동작하는 최소 코드**를 쓴다.

사다리는 **문제를 이해한 다음에** 오르는 것이지 이해를 대신하지 않는다. 이해를 건너뛰고 나온 작은 diff 는 효율이 아니라 두 번째 버그다.

### 버그 수정은 증상이 아니라 근원을 고친다

티켓은 증상을 말한다. 고치기 전에 **손댈 함수의 호출처를 전부 grep 한다.** 공유 함수 한 곳에 가드를 넣는 것이 호출처마다 넣는 것보다 작은 diff이고, 티켓이 지목한 경로만 고치면 형제 호출처는 깨진 채 남는다.

### 단순화하지 않을 것

신뢰 경계의 입력 검증 · 데이터 손실을 막는 예외 처리 · 보안 조치 · 접근성 · 명시적으로 요청받은 것. 사용자가 전체 버전을 원하면 만들고 재론하지 않는다.

## 목표 주도 실행 (Goal-Driven Execution)

착수 전에 **검증 가능한 성공 기준**을 정의한다. "동작하게 만들기" 같은 약한 기준은 매 단계 사용자 확인을 요구하지만, 강한 기준은 스스로 반복해 도달할 수 있게 한다.

- 모호한 작업을 검증 가능한 목표로 바꾼다.
  - "검증 추가" → "잘못된 입력에서 실패하는 절차를 먼저 만들고, 통과시킨다"
  - "버그 수정" → "버그를 재현하는 절차를 먼저 만들고, 수정 후 재현되지 않음을 확인한다"
- 다단계 작업은 단계마다 검증 방법을 붙여 제시한다.

  ```
  1. [단계] → 검증: [확인 방법]
  2. [단계] → 검증: [확인 방법]
  ```

- **검증 수단은 그 프로젝트가 허용하는 것으로 고른다.** 테스트 코드가 관용인 프로젝트는 테스트로, 테스트를 만들지 않는 프로젝트는 **조회 쿼리·CLI 재실행·HTTP 호출·로그·화면 캡처·수동 실행**으로 한다. 수단이 다를 뿐 "미리 정한 기준으로 확인한다"는 같다. **그 프로젝트가 무엇을 쓰는지는 각 repo `CLAUDE.md` 를 본다.**

## 계획서 선행

- **모든 소스코드 변경은 계획서를 먼저 작성해 승인받은 후 진행한다.** 바로 수정에 들어가지 않는다.
- **예외**(계획서 없이 진행 가능): 오타 수정, 주석 수정, 로그 메시지 수정.
- 계획 수립 시 **작업 지시서 자체의 논리적 모순·비합리적 지시·누락**도 함께 검증한다.
- 계획서 작성 및 **모든 업데이트마다** 아래 「검증 지침」에 따라 자체 검증 후 승인을 요청한다.
- **절차의 SoT 는 `/impl-plan` 스킬이다.** 파일 위치(`docs/plans/PLAN_<short_name>.md`)·필수 구성·Phase 구성·Done 조건·근거 승격이 거기 있다.
- **프로젝트마다 다른 값**(검증 명령·근거 승격 목적지·커밋 기능명)은 각 repo `CLAUDE.md` 의 「계획서 규약 — 이 프로젝트의 설정」 절을 본다.

## 검증 지침

### 계획서 검증

- 최대한 디테일하게 검증한다.
- 작업 지시서 자체에 논리적 오류·누락·비합리적 지시가 없는지 검증한다.
- 전반적 작업 목표에 합리적·논리적으로 부합하는 구현인지 검증한다.
- 오류 발생 가능성이 차단되어 있는지, 동시성 이슈는 없는지 검증한다.
- 반복 호출 가능성이 있는 로직이 재사용 가능하게 모듈화되어 있는지 검증한다.
- **프로젝트의 다른 로직에 나쁜 영향이 없는지 (중요)**.
- 개선·최적화할 내용이 남아있지 않은지 검증한다.
- 위 항목 외에도 동원할 수 있는 모든 검증 방법으로 비판적으로 검증하고, 더 이상 개선 내용이 나오지 않을 때까지 반복한 뒤 최종 보고서로 업데이트한다.

> 성능·인덱스·SlowQuery 검증은 DB를 다루는 프로젝트에만 해당한다. 해당 절차는 경로 한정 규칙(`.claude/rules/`)이 담당하므로 여기에 두지 않는다.

### 소스코드 검증

- 소스코드 생성·수정 시마다 최대한 비판적으로 검증한다.
- 계획서에 명시된 모든 항목을 검증한다.
- 동원 가능한 모든 방법으로 비판적으로 검증하고, 더 이상 개선 내용이 나오지 않을 때까지 반복한 뒤 최종 보고서로 업데이트한다.
- **검증 수단(빌드·curl·시뮬레이터·브라우저 등)은 프로젝트 규칙을 따른다** — 각 repo `CLAUDE.md` 를 본다.
- 검증 결과 리포트는 별도 언급이 없더라도 요청서와 동일 폴더에 유사한 파일명으로 동기화한다. **단, 문서 배치 규칙이 따로 있는 프로젝트는 그 규칙이 우선한다.**

## 문서 참조 방향

- **영구 문서는 임시 문서를 참조하지 않는다.** 참조는 언제나 수명이 긴 쪽으로만 향한다.
  임시 문서(작업 계획서·스크래치 노트·일회성 리포트)는 언젠가 삭제되므로, 영구 문서가 링크하면 그때 통째로 깨진다.
- 임시 문서에만 있는 내용 중 남길 가치가 있으면 **영구 문서로 옮긴 뒤** 임시 문서를 정리한다.
  옮기는 것은 결론뿐 아니라 **근거**다 — "값이 X다"가 아니라 "왜 X인가". 근거를 버리면 나중에 같은 논의를 반복한다.
- 판단 기준: **"이 임시 문서를 지금 지워도 잃는 것이 없는가?"** 아니라면 아직 옮길 것이 남았다.

## 코드 파일에는 이모지를 쓰지 않는다

**SQL·소스코드 등 실행되는 파일(주석 포함)에는 이모지를 넣지 않는다.** 마크다운 문서에는 써도 된다.

- 금지: `⚠️` `🔴` `✅` `❌` 같은 **이모지**(변이선택자 `U+FE0F` 가 붙는 문자 포함).
- 대신 **대괄호 텍스트 표기**를 쓴다 — `[주의]` · `[중요]` · `(정상)` · `(불가)`.
- 허용: `─` `→` `★` `※` `「」` `①` 같은 **활자 기호**. 이모지가 아니라 폭·인코딩이 안정적이다.

**Why:** DB 클라이언트·터미널·로그 뷰어마다 이모지 렌더링과 폭 계산이 달라 **주석 정렬이 깨지고**, 편집기 인코딩이 UTF-8이 아니면 파일이 손상된다. 쿼리를 복사해 다른 도구에 붙일 때 특히 문제가 된다.

## 파일 경로 전달 방식

VSCode Claude Code 확장은 마크다운 링크 목적지를 퍼센트 인코딩하지만 파일을 열 때 디코딩하지 않는다. 그래서 **경로에 한글·공백이 있으면 링크를 클릭해도 아무 반응이 없다**(에러 메시지도 없음). 열리지 않는 링크는 클릭을 유도해놓고 아무 일도 안 일어나므로, 그 경우엔 링크를 아예 만들지 않는다.

- **경로 전체(디렉터리 포함)가 ASCII이고 공백이 없으면 → 마크다운 링크**를 쓴다. 링크 텍스트는 **파일명(필요시 `:라인`)만** 쓴다 — 전체 경로를 두 번 노출하지 않는다.
- **경로 어느 한 구간이라도 한글·공백을 포함하면 → 인라인 코드로 경로만** 쓰고 **마크다운 링크를 만들지 않는다.** 사용자는 이 경로를 복사해 `Ctrl+P`(Windows) / `⌘P`(macOS)로 연다.
- **둘 다 설명 문장 안에 인라인으로 녹인다.** 「근거 파일」 같은 경로 목록 블록을 본문 위·아래에 따로 만들지 않는다. 그 파일을 언급하는 이유(무엇이 어디에 있는지)와 경로가 같은 문장 안에 있어야 한다.
- **링크가 안 열린다는 이유로 기존 한글 파일명을 리네임하지 않는다.**

예시:

> 만나이 산식은 [GhgMemInfoRepository.java:74-80](common/src/main/java/com/myezl/ggaction/common/data/repository/GhgMemInfoRepository.java#L74-L80) 의 `>` 비교 때문에 생일 당일에 1살 적게 나온다. 로컬 전체 기동 절차는 `.claude/로컬개발환경-실행가이드.md` 에 정리돼 있다.

## 외부 도구·라이브러리 확인

- 외부 라이브러리·도구의 최신 사용법·버전 차이·공식 스펙 확인이 필요하면 **Context7 MCP로 먼저 학습**하고, 부족하거나 불명확한 부분은 웹검색으로 근거를 보완한다.

### 다른 레포에서 poetry 를 실행할 때는 `env -u VIRTUAL_ENV` 를 붙인다

세션을 시작한 레포의 가상환경이 `VIRTUAL_ENV` 로 셸에 남아 있어, `cd <다른레포> && poetry run ...` 을
하면 **poetry 가 그 레포가 아니라 세션 시작 레포의 가상환경을 쓴다.**

```bash
cd <다른레포> && env -u VIRTUAL_ENV poetry run python validate_project.py
```

**Why:** 실측으로 겪었다. 워크스페이스 세 레포의 품질 검증을 한 번에 재면서 두 레포가
`ModuleNotFoundError` 로 실패했고, 「이 PC 에 패키지가 설치되지 않았다」고 잘못 판정했다.
`env -u VIRTUAL_ENV` 를 붙여 다시 재니 **한 레포는 처음부터 전부 통과**하고 있었다.

**이 함정은 검증 결과를 조용히 바꾼다.** 실패가 코드 문제인지 실행 방법 문제인지 구분되지 않으므로,
다른 레포의 검증 수치를 근거로 삼기 전에 실행 방식부터 확인한다.

## 작업 절차 스킬

절차가 긴 작업은 전역 스킬(`~/.claude/skills/`)로 분리돼 있다.

> **`/handoff` 는 사용자 전용 스킬이다** (`disable-model-invocation: true`). 설계상 **Claude 의 스킬 목록에 뜨지 않는 것이 정상**이며, 사용자가 `/handoff` 를 치면 정상 동작한다. 목록에 없다는 이유로 「호출하면 실패한다」고 보고하지 않는다.

| 스킬 | 언제 |
|---|---|
| `impl-plan` | 계획서(Implementation Plan) 작성·갱신·Done 판정. 코드 변경 전에 쓴다. 훅이 이 스킬을 가리킨다 |
| `diagnosing-bugs` | 원인 분석·버그 진단. 재현 절차 확보 → 가설 순위 제시 → 검증 순서를 강제한다 |
| `grilling` | 계획·설계를 확정하기 전 라운드 인터뷰 |
| `ponytail-review` · `ponytail-audit` | 과설계 점검 (변경분 / 저장소 전체). 목록만 내고 적용하지 않는다 |
| `writing-for-agents` | 스킬·CLAUDE.md 를 쓰거나 고칠 때 |
| `/handoff` | 세션을 다음으로 넘길 때 — 새 세션에 **그대로 붙여넣는 「다음세션 프롬프트」**를 만든다. **사용자만 호출 가능** |

## Git

- 모든 Git 처리는 사용자가 직접 진행하므로, Claude는 Git 관련 어떠한 처리도 하지 않는다.
===== END FILE: ~/.claude/CLAUDE.md =====

===== BEGIN FILE: ~/.claude/settings.json (sha256=944bb8c97edc58ff37cc1d5c9a7aa546e7afc6677c9a46126a7254905c6f85ab lines=147 bytes=3713 mode=0o644) =====
{
  "permissions": {
    "allow": [
      "WebSearch",
      "WebFetch",
      "Read(**)",
      "Skill(commit:*)",
      "mcp__context7__query-docs",
      "mcp__context7__resolve-library-id",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)",
      "Bash(git show:*)",
      "Bash(git ls-files:*)",
      "Bash(git branch:*)",
      "Bash(ls:*)",
      "Bash(cd:*)",
      "Bash(mkdir:*)",
      "Bash(cat:*)",
      "Bash(echo:*)",
      "Bash(find:*)",
      "Bash(grep:*)",
      "Bash(tree:*)",
      "Bash(wc:*)",
      "Bash(test:*)",
      "Bash(which:*)",
      "Bash(lscpu:*)",
      "Bash(xargs:*)",
      "Bash(xxd:*)",
      "Bash(file:*)",
      "Bash(head:*)",
      "Bash(tail:*)",
      "Bash(sort:*)",
      "Bash(uniq:*)",
      "Bash(diff:*)",
      "Bash(comm:*)",
      "Bash(nl:*)",
      "Bash(basename:*)",
      "Bash(dirname:*)",
      "Bash(pip:*)",
      "Bash(node --version:*)",
      "Bash(poetry:*)",
      "Bash(env -u VIRTUAL_ENV poetry:*)",
      "Bash(timeout 10 poetry run streamlit run:*)",
      "Bash(code:*)",
      "Bash(TZ=*)",
      "Bash(awk:*)",
      "Bash(sdk:*)",
      "Bash(source:*)",
      "Bash(java:*)",
      "Bash(javap:*)",
      "Bash(sg:*)",
      "Bash(curl:*)",
      "Bash(python3:*)",
      "Bash(ss:*)",
      "Bash(bash)",
      "Bash(./gradlew:*)",
      "Bash(SPRING_PROFILES_ACTIVE=*)",
      "Bash(jq:*)",
      "Read(~/.claude/settings.json)",
      "Edit(~/.claude/settings.json)",
      "Edit(.claude/settings.local.json)"
    ],
    "deny": [
      "Read(~/.claude/.credentials.json)",
      "Bash(git push:*)",
      "Bash(git reset:*)",
      "Bash(git clean:*)",
      "Bash(gh pr merge:*)",
      "Bash(gh repo delete:*)",
      "Bash(gh release create:*)",
      "Bash(npm publish:*)"
    ],
    "ask": [
      "Read(//home/yblee/workspace/quant/secrets/**)",
      "Read(//home/yblee/workspace/**/.env)"
    ]
  },
  "model": "opus",
  "outputStyle": "korean",
  "effortLevel": "xhigh",
  "autoCompactEnabled": false,
  "agentPushNotifEnabled": true,
  "hooks": {
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "PY=$(command -v python3 || command -v python); \"$PY\" \"$HOME/.claude/hooks/wsl_toast.py\"",
            "timeout": 20,
            "async": true
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "AskUserQuestion",
        "hooks": [
          {
            "type": "command",
            "command": "PY=$(command -v python3 || command -v python); \"$PY\" \"$HOME/.claude/hooks/wsl_toast.py\"",
            "timeout": 20,
            "async": true
          }
        ]
      },
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "PY=$(command -v python3 || command -v python); \"$PY\" \"$HOME/.claude/hooks/plan_gate.py\"",
            "timeout": 10,
            "statusMessage": "계획서 선행 검사"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "PY=$(command -v python3 || command -v python); \"$PY\" \"$HOME/.claude/hooks/plan_lint.py\"",
            "timeout": 10,
            "statusMessage": "계획서 불변조건 검사"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "PY=$(command -v python3 || command -v python); \"$PY\" \"$HOME/.claude/hooks/wsl_toast.py\"",
            "timeout": 20,
            "async": true
          }
        ]
      }
    ]
  }
}
===== END FILE: ~/.claude/settings.json =====

===== BEGIN FILE: ~/.claude/skills/diagnosing-bugs/SKILL.md (sha256=db5289ed94d07a1cbaffeb9a5d8583ac227ef9ca44e496421f634ce159287d40 lines=147 bytes=10646 mode=0o644) =====
---
name: diagnosing-bugs
description: 어려운 버그·성능 저하의 원인을 재현 절차부터 세워 추적하는 진단 규율. "왜 이래", "왜 안 돼", "원인 분석", "진단", "미지급 원인", "이 회원만", "이 건만", "느려졌다", "값이 이상하다", 특정 건이 기대와 다르게 동작한다는 보고에 사용한다.
---

# 버그 진단

어려운 버그를 위한 규율이다. 단계를 건너뛰려면 왜 건너뛰는지 밝힌다.

착수 전 그 프로젝트의 도메인 문서를 먼저 연다. 어디에 있는지는 그 repo `CLAUDE.md` 가 가리킨다 — 공유 문서함 INDEX·`CONTEXT.md`·`docs/` 등. 증상·키워드로 목적지를 찾는 라우팅 표가 있으면 그것부터 본다.

## 무엇을 가리고 무엇을 가리지 않는가 (착수 전 필독)

이 스킬은 명령·출력·캡처를 그대로 보여주게 한다. 두 가지를 **구분**한다.

| 대상 | 처리 |
|---|---|
| 비밀정보 — 토큰·비밀번호·API 키·인증 헤더·커넥션 문자열 | **`<REDACTED>` 로 치환한다.** 루프는 환경변수 기준으로 짜서 값이 출력에 안 나오게 한다 |
| 조회 결과의 개인정보 — 이름·로그인ID·회원ID·연락처·주소 | **가공하지 않는다. 그대로 둔다.** 전역 「조회 결과·데이터를 임의로 가공하지 않는다」가 우선한다 |

조회 결과를 마스킹·요약·집계로 대체하면 **근거물이 파괴되어 재현과 검증이 불가능해진다.** 취급 주의가 필요하면 값은 그대로 두고 **문서에 경고만 덧붙인다.**

비밀정보를 지운 결과만으로 진단이 불가능하면, 그 사실을 말하고 사용자에게 묻는다.

## Phase 1 — 재현 절차를 세운다

**이 단계가 이 스킬의 전부다.** 이 버그에서 빨간불이 켜지는 절차 하나만 확보하면 원인은 찾아진다. 이분탐색도, 가설 검증도, 계측도 전부 그 절차를 소비할 뿐이다. 절차가 없으면 코드를 아무리 오래 들여다봐도 소용없다.

여기에 **불균형하게 많은 노력을 쏟는다. 공격적으로, 창의적으로, 포기하지 않는다.**

### 수단 — 대략 이 순서로

결정적이고 자동화하기 쉬운 것부터다. **그 프로젝트에서 무엇이 가능한지는 repo `CLAUDE.md` 를 본다.**

1. **실패하는 테스트** — 테스트가 관용인 프로젝트라면 1순위다. 가장 조이기 쉽다 (§Phase 5)
2. **조회 쿼리 / CLI 재실행** — 문제가 되는 대상을 그대로 집어내는 한 줄. 기대값과 실제값이 한 화면에서 대조돼야 한다
3. **저장된 원자료 재계산** — 이미 받아둔 데이터로 계산만 다시 돌린다. 외부 재조회가 없어 결정적이다
4. **HTTP 호출** — `curl` 로 실제 요청을 보내 응답을 본다
5. **차분 비교** — 되는 것과 안 되는 것을 같은 절차에 넣어 나란히 놓고 차이를 찾는다
6. **로그·저널 조회** — 이미 남고 있는 로그에서 해당 시각·해당 대상의 흔적을 뽑는다
7. **화면 재현 / 수동 실행** — 같은 조작·잡을 직접 돌려 증상을 눈으로 확인하고 캡처한다
8. **캡처 리플레이** — 실제 요청·페이로드·수신파일을 저장해 그 경로만 따로 태운다
9. **외부 소스 실측** — 라이브러리나 외부 API 가 조용히 바뀌었는지 실제 호출로 확인한다. **기억이 아니라 실측으로 확정한다**

### 절차를 조인다

절차도 산출물로 취급한다. 일단 하나 생기면 **조인다.**

- 더 빠르게 할 수 있나? (범위를 좁힌다, 불필요한 조인을 뺀다)
- 신호를 더 날카롭게 할 수 있나? ("에러 안 남" 이 아니라 **그 증상 자체**를 집어낸다)
- 더 결정적으로 할 수 있나? (기간·회원·회차를 고정한다)

30초짜리 들쭉날쭉한 절차는 없는 것과 거의 같다. 2초짜리 결정적인 절차는 무기다.

### 재현이 불안정할 때

목표는 깨끗한 재현이 아니라 **재현율을 올리는 것**이다. 조건을 넓혀 같은 증상의 건을 여러 개 모으고, 공통점을 좁힌다. 50% 재현되는 버그는 잡을 수 있고 1% 는 못 잡는다. 잡을 수 있는 선까지 재현율을 올린다.

### 표본을 좁게 잡지 않는다

**1개월 스냅숏·상위 30건 같은 부분 표본으로 결론내지 않는다.** 원인이 표본 바깥에 있으면 그 표본은 아무것도 반증하지 못한다. 이상 징후가 보이면 대조군(정상 건)과 짝지어 **전 기간·전 건**으로 넓혀 원인이 안쪽인지 바깥인지부터 가른다.

### 절차를 도저히 못 세우겠으면

멈추고 그렇게 말한다. 시도한 것을 나열하고, 사용자에게 ① 재현되는 환경 접근 ② 근거물(로그 덤프·수신파일·화면 녹화와 타임스탬프) ③ 임시 계측 허가 중 무엇을 줄 수 있는지 묻는다. **절차 없이 가설로 넘어가지 않는다.**

### 완료 기준 — 빨간불이 켜지는 절차

Phase 1 은 절차가 아래를 전부 만족할 때 끝난다. **이미 최소 1회 실행해 보고**, 실행 내용과 결과를 (비밀정보만 지운 채) 보여준다.

- [ ] **빨간불이 켜진다** — 실제 문제 경로를 타고, **사용자가 말한 바로 그 증상**을 집어낸다. "에러 없이 돈다" 가 아니라 이 버그를 잡아낼 수 있어야 한다
- [ ] **결정적이다** — 돌릴 때마다 같은 결론이 나온다
- [ ] **빠르다** — 분이 아니라 초 단위
- [ ] **혼자 돌릴 수 있다** — 사람이 눌러야 하는 부분이 있으면 번호 매긴 절차로 적어 사용자에게 준다

이 절차가 손에 없는데 코드를 읽으며 이론을 세우고 있다면 **멈춘다. 재현 없이 가설로 직행하는 것이 이 스킬이 막으려는 바로 그 실패다.**

## Phase 2 — 재현하고 최소화한다

절차를 돌려 빨간불을 직접 본다.

- [ ] **사용자가 말한 그 증상**이 나온다. 근처의 다른 실패가 아니다. 다른 버그를 잡으면 다른 수정을 하게 된다
- [ ] 여러 번 돌려도 재현된다
- [ ] 증상을 정확히 포착했다 (에러 메시지·틀린 값·느린 시간) — 뒤 단계에서 수정이 실제로 그것을 없앴는지 확인할 근거다

### 최소화

빨간불이 켜지면, **여전히 빨간불이 켜지는 가장 작은 조건**까지 줄인다. 입력·조건·회원·기간·데이터를 **하나씩** 빼고 매번 다시 돌린다.

이유: 조건이 적을수록 Phase 3 의 가설 공간이 좁아지고, 그 최소 조건이 그대로 회귀 확인 절차가 된다.

**남은 조건 하나를 빼면 빨간불이 꺼질 때** 끝이다.

재현과 최소화가 끝나기 전에는 다음으로 넘어가지 않는다.

## Phase 3 — 가설을 세운다

**검증하기 전에 가설 3~5개를 순위 매겨 만든다.** 하나만 만들면 첫 번째 그럴듯한 생각에 갇힌다.

각 가설은 **반증 가능**해야 한다. 그 가설이 하는 예측을 명시한다.

> 형식: "X 가 원인이면, Y 를 바꿨을 때 증상이 사라진다 / Z 를 바꾸면 더 심해진다."

예측을 못 쓰겠으면 그건 느낌이다. 버리거나 날카롭게 만든다.

**검증 전에 순위 목록을 사용자에게 보여준다.** 사용자는 도메인 지식으로 순위를 즉시 뒤집을 수 있고("그거 지난주에 배포 나갔어"), 이미 배제한 가설을 알고 있다. 값싼 체크포인트이고 시간을 크게 아낀다. 사용자가 자리에 없으면 막히지 말고 내 순위대로 진행한다.

## Phase 4 — 계측한다

각 확인은 Phase 3 의 특정 예측에 대응해야 한다. **한 번에 한 변수만 바꾼다.**

수단 우선순위:

1. **이미 나오고 있는 로그·저널·DB 상태**로 가설을 가른다
2. **차분 조회** — 되는 건과 안 되는 건의 같은 컬럼을 나란히 본다
3. "전부 로그 찍고 grep" 은 하지 않는다

**운영 코드에 디버그 로그를 심지 않는다.** 로컬·개발계 한정으로 불가피하면 `[DEBUG-a4f2]` 처럼 고유 태그를 붙이고, 끝나면 그 태그로 grep 해서 **전부 제거한다.**

**성능 저하는 분기가 다르다.** 로그가 아니라 측정이다. 기준선을 먼저 잡고(실행계획·소요시간·건수) 이분탐색한다. 재보다 먼저 고치지 않는다.

## Phase 5 — 고치고 재확인 절차를 남긴다

**수정 전에 재현 절차를 파일로 고정한다.** 최소화된 조건, 실행 방법, 기대 결과, 현재 결과를 한 파일에 적는다.

- 저장 위치는 **작업 저장소 루트에 untracked**. 커밋 대상 경로(`src/`·`docs/`·`.scratch/`)에 만들지 않는다
- 파일명은 그 프로젝트의 문서명 규칙을 따른다 (규칙이 없으면 `[한글주제]_재현절차.md`)

**테스트 코드 생성은 그 프로젝트의 관용을 따른다.** 테스트를 만들지 않는 프로젝트라면 위 재현 절차 파일이 그 역할을 한다. 테스트를 관용으로 쓰는 프로젝트라면, **올바른 seam 이 있을 때만** 회귀 테스트를 먼저 쓴다. 버그가 여러 호출처의 조합에서 나는데 단일 호출처 테스트밖에 못 두겠다면 그 테스트는 헛된 안심을 준다. **올바른 seam 이 없다는 사실 자체가 발견이다.** 기록한다.

순서:

1. 재현 절차를 고정하고, 지금 빨간불인 것을 보여준다
2. 수정한다 — **증상이 아니라 근원을 고친다.** 손댈 함수의 호출처를 전부 grep 해서 형제 호출처가 같은 버그를 안고 있지 않은지 본다
3. 같은 절차를 다시 돌려 초록불을 확인한다
4. 최소화하기 **전의** 원래 시나리오로도 다시 돌린다

## Phase 6 — 정리

끝났다고 말하기 전에:

- [ ] 원래 증상이 재현되지 않는다 (Phase 1 절차 재실행으로 확인)
- [ ] 재현 절차 파일이 남아 있고, 수정 후 결과가 갱신돼 있다
- [ ] 임시로 심은 `[DEBUG-...]` 계측을 전부 제거했다 (태그로 grep)
- [ ] 임시 스크립트·프로토타입을 정리했다
- [ ] **맞았던 가설이 무엇이었는지** 리포트에 남겼다 — 다음 사람이 같은 길을 다시 걷지 않도록
- [ ] 재사용 가치가 있는 사실(외부 연계 스펙·데이터 특성·라이브러리 실제 동작·타이밍)은 1회성 문서에 가두지 않고 살아있는 문서로 옮겼다 — 어디로 옮기는지는 그 repo 규칙을 따른다
===== END FILE: ~/.claude/skills/diagnosing-bugs/SKILL.md =====

===== BEGIN FILE: ~/.claude/skills/grilling/SKILL.md (sha256=3200c19cda4b96429b26caac80f161bca6c9f0205623ee17ec75a310da5045cf lines=49 bytes=2716 mode=0o644) =====
---
name: grilling
description: 계획·설계·결정을 확정하기 전에 라운드 인터뷰로 끝까지 캐묻는다. 사용자가 생각을 검증받고 싶어 할 때, 계획서를 쓰기 전 설계 결정이 여러 개 남아 있을 때, "어떻게 할까", "이 방향 맞나", "짚어봐 줘" 같은 요청에 사용한다.
---

# 라운드 인터뷰

합의에 도달할 때까지 끝까지 캐묻는다. 결정들을 **트리**로 본다 — 모든 결정에는 그 밑에 매달린 결정들이 있다.

## 프론티어 — 지금 물을 수 있는 것만

**프론티어**는 선행 결정이 이미 정해진 질문들, 즉 **아직 못 들은 답을 추측하지 않고도 지금 물을 수 있는 질문 전부**다.

한 라운드에 **프론티어 전체를 한 번에** 묻는다. 답을 받으면 트리가 갱신되고 — 정해진 결정이 프론티어를 바깥으로 밀어내며 그것에 의존하던 질문들이 열린다 — 프론티어를 다시 계산해 다음 라운드를 던진다.

**이번 라운드의 다른 질문에 답이 달린 질문은 이번 라운드가 아니라 다음 라운드 것이다.** 그걸 지금 물으면 추측 위에 추측을 쌓게 된다.

## 사실을 찾는 것은 내 일이다

프론티어 질문이 환경에서 알아낼 수 있는 사실(파일 내용·호출처 개수·설정값·이력)을 필요로 하면 **내가 직접 조사한다.** 찾아볼 수 있는 것을 사용자에게 묻지 않는다.

- 직접 조사한다. Agent tool 은 사용자가 요청했을 때만 쓴다.
- 조사가 필요한 질문은 조사가 끝날 때까지 그 가지만 기다리게 두고, **나머지 프론티어는 지금 묻는다.**

사용자에게 묻는 것은 **결정**뿐이다.

## 질문 형식

전역 규칙의 4항목 형식을 쓴다. 질문이 여럿이면 번호를 붙여 블록을 반복한다.

```
**Q1. <질문 제목>**

- **현재 확인된 사실**: 조사로 확인한 객관적 정보 (근거 파일·수치)
- **가능한 선택지**: ① ... ② ... ③ ...
- **각 선택지의 영향**: 장단점·리스크
- **추천안**: 근거와 함께

**Q2. <질문 제목>**
...
```

`AskUserQuestion` 도구를 함께 쓸 때는 도구가 질문 4개·선택지 4개까지만 받으므로, **프론티어가 그보다 넓으면 본문에 전체를 적고 도구로는 갈림이 큰 것만 묻는다.** 도구로 다 못 담았다고 질문을 버리지 않는다.

## 끝나는 조건

프론티어가 비면 끝난다 — 트리의 모든 가지를 방문했고, 조용히 가정하고 넘어간 것이 하나도 없는 상태다.

**사용자가 합의에 도달했다고 확인하기 전에는 실행하지 않는다.**
===== END FILE: ~/.claude/skills/grilling/SKILL.md =====

===== BEGIN FILE: ~/.claude/skills/handoff/SKILL.md (sha256=be2d38ae7979f60da09fd2f26be768dc04da6d6b477ace2496d2bcdd35ef49ad lines=57 bytes=4561 mode=0o644) =====
---
name: handoff
description: 현재 대화를 다음 세션에 그대로 붙여넣을 「다음세션 프롬프트」로 압축한다.
argument-hint: "다음 세션에서 무엇을 할 것인가"
disable-model-invocation: true
---

# 다음세션 프롬프트

**요약문이 아니라 지시서를 쓴다.** 산출물은 사용자가 새 세션 첫 메시지에 **그대로 복사해 붙이면 작동하는 프롬프트**여야 한다. 과거 세션을 서술하지 말고, **다음 세션에게 말한다.**

한글로 쓴다. 형식은 이 폴더의 [TEMPLATE.md](TEMPLATE.md) 를 따른다.

## 먼저 확인한다

1. **이 세션에 인계할 대화가 있는가.** `/handoff` 가 첫 메시지면 요약할 이력이 없다. 파일에서 역추정한 내용을 「세션 요약」으로 포장하지 않는다 — 인계의 핵심인 **결정의 근거와 짚은 헛다리**는 사라진 대화에만 있어 담을 수 없다. 그 사실을 보고하고 멈춘다.
2. **이미 인계문서 역할을 하는 파일이 있는가.** `git status` 와 루트의 `*.md` 를 본다. 있으면 **새로 만들지 않고 그것을 갱신한다.** 두 벌이 되면 다음 세션이 어느 쪽을 믿을지 모른다.
3. 기존 문서가 있다면 **어디가 낡았는지** 먼저 찾아 보고한다 — 그 이후 생긴 산출물, 바뀐 결론, 완료된 항목.

## 파일

**위치와 추적 여부는 그 프로젝트의 규칙을 따른다.** 각 repo `CLAUDE.md` 를 본다.

| 그 저장소가 | 어떻게 |
|---|---|
| 인계 프롬프트를 **커밋한다**고 정했으면 | 정한 경로·파일명으로 만들고 **추적 대상**으로 둔다. 여러 PC 에서 이어서 작업하는 것이 목적이면 이쪽이다 — **untracked 는 다른 PC 로 넘어가지 않는다** |
| 규칙이 없거나, **문서가 커밋되면 안 되는** 저장소면 | 저장소 루트에 **untracked**. 커밋 대상 경로(`src/`·`docs/`)에는 만들지 않는다 |

- 파일명도 그 프로젝트 규칙을 따른다. 규칙이 없으면 `[한글주제]_다음세션_프롬프트.md`.
- **파일명을 고정해 매번 같은 파일을 갱신한다.** 세션마다 새 파일을 만들면 두 벌이 되고 다음 세션이 어느 쪽을 믿을지 모른다.
- 만든 뒤 `git status` 로 **의도한 추적 상태인지** 확인하고, 경로와 추적 여부를 사용자에게 알린다.

## 각 절을 쓰는 법

**먼저 읽을 것 (순서대로)** — 이 절이 프롬프트의 값어치를 결정한다. 경로만 나열하지 말고 **읽는 순서**와 **각 문서의 함정**을 같이 적는다.

> 나쁨: `조사리포트/INPD-1387_종합리포트.md` 를 읽는다.
> 좋음: `§14-12(최종 확정 결과)를 가장 먼저 읽는다. §1~§9는 1차 조사 기록이고 그때의 「4,997명/988만원」은 과대 계상이라 폐기된 값이다.`

**현재 확정된 결론** — 다음 세션이 **다시 논의하면 안 되는** 것. 숫자는 숫자로 적는다. 확정 근거가 특정 문서에 있으면 절 번호까지 가리킨다.

**남은 일** — `# / 할 일 / 상태` 표. 상태는 `진행 중`·`미착수`·`대기`·`미실행` 처럼 실제 상태를 쓴다. 각 항목에 **필요한 파일 경로**를 붙인다.

**이번 세션에 원하는 것** — 명령문으로 쓴다. 산출물이 있으면 **구성을 번호로 지정**한다. 사용자가 `/handoff` 에 인자를 줬으면 그 내용이 이 절이 된다.

**이미 밟은 함정** — 이번에 헛다리 짚은 것, 틀렸던 가정, 다시 시도하면 안 되는 접근. **이 절이 없으면 다음 세션은 같은 길을 다시 걷는다.**

**작업 규칙** — 이 건 **특유의** 규칙만 쓴다. 전역 CLAUDE.md 에 이미 있는 규칙을 재기술하지 않는다. 「이 작업에서만 다르게 해야 하는 것」이 있을 때만 이 절을 둔다.

## 담지 않을 것

**이미 다른 산출물에 있는 내용을 복사하지 않는다.** 계획서·리포트·SQL·커밋·조회 결과는 **경로로 참조**한다. 복사하면 두 벌이 되고 한쪽이 낡는다. 이 프롬프트는 **지도이지 사본이 아니다.**

## 값의 처리

- **비밀정보**(토큰·비밀번호·API 키·인증 헤더)는 제거한다.
- **조회 결과의 개인정보**(이름·로그인ID·회원ID·연락처)는 **가공하지 않는다.** 원본 파일을 경로로 참조하고, 취급 주의가 필요하면 경고만 덧붙인다. 전역 「조회 결과를 임의로 가공하지 않는다」가 우선한다.
===== END FILE: ~/.claude/skills/handoff/SKILL.md =====

===== BEGIN FILE: ~/.claude/skills/handoff/TEMPLATE.md (sha256=c1e5b4f7fdc3cd0f2d678d175629bc790c42c823e1d406a2b177a4f984dcb183 lines=62 bytes=2156 mode=0o644) =====
# 다음세션 프롬프트 템플릿

아래 골격을 따른다. **해당 없는 절은 통째로 뺀다** — 빈 절을 남기지 않는다.
꺾쇠 `<>` 안은 채울 자리다.

---

```markdown
<무슨 작업인지 한 줄>. 이어서 진행한다.
**<지금까지의 상태>이고, 이번 세션의 목표는 「<한 단어~한 구절>」이다.**

## 먼저 읽을 것 (순서대로)

1. `<경로>`
   - **<어느 절을 먼저 볼지>를 가장 먼저** 읽는다.
   - <그 문서의 함정 — 폐기된 값, 낡은 절, 오해하기 쉬운 지점>
2. `<경로>` — <왜 읽는지 한 줄>
3. `<경로>` — <실행 순서나 주의가 있으면 함께>

## 현재 확정된 결론

- <다시 논의하면 안 되는 사실. 숫자는 숫자로>
- <확정 근거가 어디에 있는지 절 번호까지>

## 남은 일

| # | 할 일 | 상태 |
|---|---|---|
| 1 | **<할 일>** — <필요한 파일 경로> | **진행 중** |
| 2 | <할 일> | 미착수 |
| 3 | <할 일> — <조건이나 시점이 있으면 함께> | 대기 |

## 이번 세션에 원하는 것

**<명령문으로 한 줄>.** <산출물이 있으면> 구성은 이렇게.

1. **<항목>** — <무엇을 담는지>
2. **<항목>** — <무엇을 담는지>
3. **<항목>** — <무엇을 담는지>

## 이미 밟은 함정 — 다시 밟지 말 것

- <틀렸던 가정과 왜 틀렸는지>
- <시도했다가 버린 접근과 버린 이유>

## 작업 규칙

- <이 작업 특유의 규칙만. 전역 CLAUDE.md 에 있는 것은 쓰지 않는다>
```

---

## 절별 판단 기준

| 절 | 넣는 조건 | 빼는 조건 |
|---|---|---|
| 먼저 읽을 것 | **항상** — 이 절이 프롬프트의 값어치다 | — |
| 현재 확정된 결론 | 다시 논의하면 안 되는 사실이 있을 때 | 아직 아무것도 확정 안 됐을 때 |
| 남은 일 | 항상 | — |
| 이번 세션에 원하는 것 | **항상** — 없으면 지시서가 아니라 요약문이다 | — |
| 이미 밟은 함정 | 헛다리를 짚었을 때 | 순탄했을 때 |
| 작업 규칙 | 이 건에서만 다르게 해야 할 것이 있을 때 | 전역 규칙으로 충분할 때 |
===== END FILE: ~/.claude/skills/handoff/TEMPLATE.md =====

===== BEGIN FILE: ~/.claude/skills/impl-plan/SKILL.md (sha256=cb6a3996f8db9a71961270532151d3366bda3d14531ec8f41c47cce1fcdcd3f4 lines=158 bytes=9179 mode=0o644) =====
---
name: impl-plan
description: 계획서(Implementation Plan) 작성·갱신 절차의 SoT. 코드 변경 전 docs/plans/PLAN_<short_name>.md 를 만들 때, 기존 계획서의 Phase/체크리스트를 갱신할 때, Done 처리 조건을 판단할 때 사용한다.
when_to_use: 사용자가 계획서·plan 작성을 요청할 때. src/·scripts/·tests/ 의 코드를 변경하기 전. docs/plans/ 아래 파일을 읽거나 수정할 때. plan 을 Done 으로 표기해도 되는지 판단할 때.
argument-hint: [short_name]
---

# 계획서(Implementation Plan) 작성 절차

모든 코드 변경은 계획서를 먼저 작성한 뒤 진행한다.
예외는 오타 수정, 주석 수정, 로그 메시지 수정뿐이다.

## 이 저장소의 값을 먼저 읽는다

이 스킬은 여러 저장소가 공유하므로 **프로젝트마다 달라지는 값을 담지 않는다.**
아래 셋은 작업 중인 저장소의 루트 `CLAUDE.md` 「계획서 규약 — 이 프로젝트의 설정」 절이 SoT다.

| 항목 | 쓰이는 곳 |
| --- | --- |
| 검증 명령 (품질 검증·자동 포맷) | 마지막 Phase 의 Validation |
| 근거 승격 목적지 | Done 처리 전 「근거 승격」 |
| 커밋 메시지의 기능명 매핑 | `Commit Messages (Final candidates)` |

그 절이 없는 저장소라면 계획서를 쓰기 전에 사용자에게 값을 확인한다.

## 파일 위치와 네이밍

- 경로: `docs/plans/PLAN_<short_name>.md`
- `<short_name>`은 작업 범위와 목적이 드러나도록 간결히 작성한다.
- 템플릿: 이 스킬 폴더의 `template.md`. 새 계획서는 이 파일을 복사해서 시작한다.

> **`docs/plans/` 폴더는 비어도 유지한다.** 계획서 게이트 훅은 이 폴더의 존재로 규약 채택을 판정하므로,
> 폴더가 사라지면 게이트가 조용히 꺼진다. 계획서를 전부 비울 때도 `.gitkeep` 은 남긴다.

## 날짜/시간 표기 (KST)

- 시간대: KST(Asia/Seoul)
- 형식: `YYYY-MM-DD HH:MM` (예: `2025-12-25 14:30`)
- 적용 대상: `작성일`, `마지막 업데이트`, `진행 로그`
- 현재 시각은 추정하지 말고 `TZ=Asia/Seoul date '+%Y-%m-%d %H:%M'` 으로 확인한다.

## 1) 계획서 필수 구성

- **Goal**: 목표 설정
- **Non-Goals**: 범위 제외 항목
- **Context**: 배경/필요성/영향받는 규칙 + "전체 숙지" 선언
- **Definition of Done**: 완료 조건 체크리스트
- **Scope**: 변경 범위(변경 대상 파일, 데이터/결과 영향)
  - `docs/COMMANDS.md` 업데이트 필요 여부를 반드시 명시한다. 불필요하면 `docs/COMMANDS.md 변경 없음`으로 기록한다.
  - `docs/COMMANDS.md`는 모든 실행 명령어(`poetry run`, `streamlit run` 등)의 단일 SoT다. 실행 방법/CLI 옵션이 바뀌면 반드시 함께 갱신한다.
- **Phases**: 단계별 계획(각 Phase의 할 일 + Validation)
- **Risks**: 리스크와 완화책
- **Notes**: 메모/결정사항/링크/진행 로그

> 주의: Context의 "영향받는 규칙"에는 규칙을 요약·나열하지 말고,
> 참고할 문서(파일) 목록만 나열한 뒤
> "해당 문서들에 기재된 규칙을 모두 숙지하고 준수한다"를 명시한다.

## 2) Phase 구성 원칙

- Phase는 "파일 수"가 아니라 **문맥(context) 기준**으로 구성한다.
- 한 Phase 안에서 "검증/수정/재검증"이 자연스럽게 닫히는 단위로 묶는다.
- 핵심 인바리언트/정책을 테스트로 먼저 고정해야 한다면 Phase 0(레드)를 둔다.
- Phase 1부터는 그린(테스트 통과 상태) 유지가 원칙이다.

### 포맷/린트/테스트 실행 타이밍

- 품질 검증 명령은 **마지막 Phase에서만** 실행한다. 중간 Phase에서는 실행하지 않는다.
- 자동 포맷은 마지막 Phase에서 **적용만** 수행한다. 검사 모드(`--check`)는 쓰지 않는다.

두 명령의 실제 이름은 저장소마다 다르다 — 루트 `CLAUDE.md` 의 프로젝트 설정 절을 본다.

## 3) 스킵(Skipped) 및 완료(Done) 규칙

### 스킵이 "존재하지 않게" 설계한다

스킵은 "아직 구현이 없어서 테스트를 못 만든다"를 의미하는 경우가 많다.
이는 스킵이 아니라 **Phase 분해**로 해결한다.

- Phase 0: 만들 수 있는 테스트(정책/인터페이스/불변조건)부터 최대한 작성
- Phase 1: 필요한 함수/로직 구현으로 Phase 0 테스트 통과
- Phase 2: 부족했던 테스트 추가로 커버리지 완성

즉, 테스트를 스킵으로 미루지 말고 Phase를 나누어 "테스트+구현"으로 완성한다.

### Done 선언 조건 (체크리스트 기반, 서술 금지)

Done은 "말/요약"이 아니라 plan의 **체크리스트 상태로만** 판단한다.
아래를 모두 만족할 때만 `상태: ✅ Done`으로 표기할 수 있다.

1. Definition of Done(DoD) 체크리스트가 모두 `[x]`
2. 마지막 Validation의 품질 검증 결과가 `failed=0` 그리고 `skipped=0`
3. plan 내에 미완료(`- [ ]`) 항목이 남아있지 않음 (Phase/DoD/필수 체크 포함)

> 이 세 조건은 `~/.claude/hooks/plan_lint.py` 가 PLAN 파일 저장 시 기계적으로 검사한다.
> 위반 상태로는 저장이 차단되므로, 규칙을 기억에 의존해 맞추지 않아도 된다.

> **[중요] Validation 줄에는 `validate_project.py` 라는 이름이 그대로 들어가야 한다.**
> 훅은 그 문자열이 있는 체크박스 줄에서만 `failed=`·`skipped=` 를 읽는다.
> 다른 이름으로 바꿔 쓰면 훅이 그 줄을 찾지 못해 **Done 검사가 조용히 건너뛰어진다.**

### 근거 승격 (Done 전 필수, 훅이 검사하지 않음)

`docs/plans/`는 **임시 산출물**이다. 사용자가 주기적으로 전부 삭제하므로,
계획서에만 있는 정보는 그때 사라진다. Done 처리 전에 **남길 가치가 있는 것을 살아있는 문서로 옮긴다.**

- 옮기는 것은 결론뿐 아니라 **근거**다. "값이 17:00이다"가 아니라 **"왜 17:00인가"**를 남긴다.
  근거를 버리면 나중에 같은 논의를 반복하고, 같은 함정을 다시 밟는다
- 목적지는 루트 `CLAUDE.md` 의 프로젝트 설정 절이 SoT다
- **살아있는 문서에서 계획서를 링크하지 않는다.** 링크는 다음 삭제 주기에 깨진다.
  내용을 복사해 자립시킨다
- 판단 기준: **"이 계획서를 지금 지워도 잃는 것이 없는가?"** 아니라면 아직 승격이 안 끝난 것이다

이 항목은 훅이 검사하지 못한다(무엇이 "남길 가치가 있는지"는 기계가 판정할 수 없다).
DoD 체크리스트에 명시적으로 넣어 빠뜨리지 않게 한다.

### 스킵이 남아있는 경우 (불가피한 예외)

스킵이 정말 불가피하면 허용할 수 있으나, `skipped > 0`이면:

- plan 상태를 Done으로 처리할 수 없다.
- DoD 체크박스(특히 테스트/Validation 관련)를 `[x]`로 처리하면 안 된다.
- Validation 결과에는 반드시 `passed/failed/skipped` 수를 기록한다.
- Notes에 스킵 사유/해제 조건/후속 plan 계획을 반드시 기록한다.

## 4) 이미 생성된 계획서 수정 규칙

이미 생성된 plan은 **체크리스트 업데이트와 진행 로그 추가 외에는 수정 금지**한다.
특히 템플릿의 `## 0) 고정 규칙` 섹션은 문구까지 그대로 보존한다.

실측으로 계획의 전제가 무너진 경우는 예외다. 그때는 **무엇이 왜 달라졌는지를 Notes 에 남기고**
사용자에게 조정을 승인받은 뒤 고친다. 조용히 기준을 낮추는 것과 구분된다.

## 5) Commit Messages

- Commit Messages는 "실제로 수행하는 변경" 기준으로 쓴다(추정 금지).
- 형식은 `기능명 / ...` 를 권장한다.
- Phase별 Commit Messages는 기본적으로 작성하지 않는다. plan의 마지막에만
  `Commit Messages (Final candidates)` 5개 후보를 두고, 사용자가 1개를 선택한다.
- 사용자가 "중간 Phase 커밋 단위 분리"를 명시적으로 요청한 경우에만
  해당 Phase에 `Commit Messages (Phase N)`을 추가할 수 있다.

기능명은 변경된 파일 경로를 기준으로 정한다. 경로별 매핑은 루트 `CLAUDE.md` 의 프로젝트 설정 절에 있다.

실제 커밋 메시지 후보 생성은 `/commit` 스킬을 사용한다.

## 6) 하네스가 강제하는 것

계획서 규약은 전역 훅 두 개가 집행한다. 훅은 `~/.claude/hooks/` 에 있고
`docs/plans/` 폴더를 가진 저장소에서만 동작한다.

| 훅 | 시점 | 하는 일 |
| --- | --- | --- |
| `plan_gate.py` | PreToolUse | 문서(`docs/**`, `**/*.md`)를 제외한 파일 편집 시 계획서가 선행되지 않았으면 권한 프롬프트를 띄운다. 차단이 아니라 확인이며 세션당 한 번만 걸린다 |
| `plan_lint.py` | PostToolUse | PLAN 파일 저장 시 Done 3조건과 `## 0) 고정 규칙` 보존을 검사한다. Bash(`sed`·heredoc 등)를 경유한 수정도 검사한다 |

> 훅은 저장소 밖에 있어 **어느 프로젝트의 Ruff·PyRight 검사도 받지 않는다.**
> 훅을 고칠 때는 직접 실행해 확인한다.
===== END FILE: ~/.claude/skills/impl-plan/SKILL.md =====

===== BEGIN FILE: ~/.claude/skills/impl-plan/template.md (sha256=65175f6a80324dc4c74cda1cde7ebaa458838b24089389ca8b0119c1da09a457 lines=169 bytes=5781 mode=0o644) =====
# Implementation Plan: [작업명/기능명]

> 작성/운영 규칙(SoT): `/impl-plan` 스킬(`~/.claude/skills/impl-plan/SKILL.md`)을 반드시 참고하세요.  
> (이 템플릿을 수정하거나 새로운 양식의 계획서를 만들 때도 해당 스킬을 포인터로 두고 준수합니다.)

**상태**: 🟡 Draft / 🔄 In Progress / ✅ Done

---

🚫 **이 영역은 삭제/수정 금지** 🚫

**상태 옵션**: 🟡 Draft / 🔄 In Progress / ✅ Done

**Done 처리 규칙**:

- ✅ Done 조건: DoD 모두 [x] + `skipped=0` + `failed=0`
- ⚠️ **스킵이 1개라도 존재하면 Done 처리 금지 + DoD 테스트 항목 체크 금지**
- 상세: `/impl-plan` 스킬의 "3) 스킵 및 완료 규칙" 참고
- 위 조건은 `~/.claude/hooks/plan_lint.py`가 저장 시 자동 검사합니다

---

**작성일**: YYYY-MM-DD HH:MM
**마지막 업데이트**: YYYY-MM-DD HH:MM
**관련 범위**: (예: 수집, 검증, utils, scripts)
**관련 문서**: (예: scripts/CLAUDE.md, tests/CLAUDE.md)

---

## 0) 고정 규칙 (이 plan은 반드시 아래 규칙을 따른다)

> 🚫 **이 영역은 삭제/수정 금지** 🚫
> 이 섹션(0)은 지워지면 안 될 뿐만 아니라 **문구가 수정되면 안 됩니다.**
> 규칙의 상세 정의/예외는 반드시 `/impl-plan` 스킬을 따릅니다.

- 품질 검증 명령은 **마지막 Phase에서만 실행**한다. 실패하면 즉시 수정 후 재검증한다.
- Phase 0은 "레드(의도적 실패 테스트)" 허용, Phase 1부터는 **그린 유지**를 원칙으로 한다.
- 이미 생성된 plan은 **체크리스트 업데이트 외 수정 금지**한다.
- 스킵은 가능하면 **Phase 분해로 제거**한다.

---

## 1) 목표(Goal)

- [ ] 목표 1:
- [ ] 목표 2:
- [ ] 목표 3:

## 2) 비목표(Non-Goals)

- 범위 밖인 것들을 명확히 적는다.

## 3) 배경/맥락(Context)

### 현재 문제점 / 동기

- (왜 이 작업이 필요한가)
- (무엇이 잘못되어 있고, 어떤 위험이 있는가)

### 영향받는 규칙(반드시 읽고 전체 숙지)

> 아래 문서에 기재된 규칙을 **모두 숙지**하고 준수합니다.

- 루트 `CLAUDE.md` — 특히 「계획서 규약 — 이 프로젝트의 설정」 절
- 작업 도메인 규칙: `scripts/CLAUDE.md` 등
- 테스트를 추가/수정한다면 `tests/CLAUDE.md`

## 4) 완료 조건(Definition of Done)

> Done은 "서술"이 아니라 "체크리스트 상태"로만 판단합니다. (정의/예외는 `/impl-plan` 스킬)

- [ ] 기능 요구사항 충족
- [ ] 회귀/신규 테스트 추가
- [ ] `poetry run python validate_project.py` 통과 (failed=0, skipped=0; passed/failed/skipped 수 기록)
- [ ] 자동 포맷 적용 완료 (마지막 Phase에서 실행)
- [ ] 필요한 문서 업데이트(`docs/COMMANDS.md` / CLAUDE.md / plan 등 — 각각 변경 여부 명시)
- [ ] 근거 승격 완료 — 이 계획서를 지금 삭제해도 잃을 정보가 없다
      (결정 근거·실측 수치를 루트 `CLAUDE.md` 의 프로젝트 설정 절이 정한 목적지로 이관.
      `/impl-plan` 스킬 "근거 승격" 참고)
- [ ] plan 체크박스 최신화(Phase/DoD/Validation 모두 반영)

## 5) 변경 범위(Scope)

### 변경 대상 파일(예상)

- (예) `src/...`
- (예) `tests/...`
- (예) `docs/...`
- `docs/COMMANDS.md`: (변경 있음 / 없음 — 실행 명령어/CLI 옵션 변경 여부 기준, 반드시 명시)

### 데이터/결과 영향

- (예) 출력 스키마 변경 여부
- (예) 기존 결과 비교 필요 여부

## 6) 단계별 계획(Phases)

### (선택) Phase 0 — 인바리언트/정책을 테스트로 먼저 고정(레드)

> 아래 조건 중 하나라도 해당하면 이 Phase를 만든다:
>
> - 핵심 인바리언트/정합성(절대 규칙/정의/중요 로직)의 변경 또는 추가
> - 최종 결과(지표/정의/산식/판단 기준)가 달라질 수 있는 변경
> - 에러 처리 정책 변경(중단 조건/예외 조건/실패 규칙 변경 등)

**작업 내용**:

- [ ] (예) 핵심 정책/정의를 문서에 명확히 기록
- [ ] (예) 만들 수 있는 테스트를 최대한 먼저 추가(레드 허용)
- [ ] (예) 인터페이스/예외/타입/상수 형태를 먼저 고정

---

### Phase 1 — 핵심 구현/수정(그린 유지)

**작업 내용**:

- [ ] ...
- [ ] ...

---

### Phase 2 — (선택) 리팩토링/정리/추가 작업(그린 유지)

**작업 내용**:

- [ ] ...
- [ ] ...

---

### 마지막 Phase — 문서 정리 및 최종 검증

**작업 내용**

- [ ] 필요한 문서 업데이트 (`docs/COMMANDS.md` 포함 여부 명시)
- [ ] 자동 포맷 적용 (루트 `CLAUDE.md` 의 프로젝트 설정 절이 정한 명령)
- [ ] 변경 기능 및 전체 플로우 최종 검증
- [ ] DoD 체크리스트 최종 업데이트 및 체크 완료
- [ ] 전체 Phase 체크리스트 최종 업데이트 및 상태 확정

**Validation**:

- [ ] `poetry run python validate_project.py` (passed=\_\_, failed=\_\_, skipped=\_\_)

#### Commit Messages (Final candidates) — 5개 중 1개 선택

1. 기능명 / 핵심 변경 요약 (정확성/정합성/정책 반영)
2. 기능명 / 버그 수정 + 테스트 보강 (회귀 방지)
3. 기능명 / 정책 변경 반영 및 검증 로직 정리
4. 기능명 / 리팩토링(동작 동일) + 린트/포맷 정리
5. 기능명 / 문서/규칙 업데이트 + 구현 반영

## 7) 리스크(Risks)

- (예) 최종 지표/정의 변경으로 기존 결과와 차이 발생
- (예) 리팩토링 중 회귀 위험

## 8) 메모(Notes)

- 참고 링크/실험 로그/핵심 결정 사항 기록
- 스킵이 존재한다면 반드시 사유/해제 조건/후속 plan 기록

### 진행 로그 (KST)

- YYYY-MM-DD HH:MM: ...
- YYYY-MM-DD HH:MM: ...

---
===== END FILE: ~/.claude/skills/impl-plan/template.md =====

===== BEGIN FILE: ~/.claude/skills/ponytail-audit/SKILL.md (sha256=1aa654bf08596496daced447afdfab413b3acbcfaf5941ebfff489bc2422a59e lines=52 bytes=2183 mode=0o644) =====
---
name: ponytail-audit
description: >
  Whole-repo audit for over-engineering. Like ponytail-review, but scans the
  entire codebase instead of a diff: a ranked list of what to delete, simplify,
  or replace with stdlib/native equivalents. Use when the user says "audit this
  codebase", "audit for over-engineering", "what can I delete from this repo",
  "find bloat", "ponytail-audit", or "/ponytail-audit". One-shot report, does
  not apply fixes.
---

ponytail-review, repo-wide. Scan the whole tree instead of a diff. Rank
findings biggest cut first.

## Tags

Same as ponytail-review:

- `delete:` dead code, unused flexibility, speculative feature. Replacement: nothing.
- `stdlib:` hand-rolled thing the standard library ships. Name the function.
- `native:` dependency or code doing what the platform already does. Name the feature.
- `yagni:` abstraction with one implementation, config nobody sets, layer with one caller.
- `shrink:` same logic, fewer lines. Show the shorter form.

## Hunt

Deps the stdlib or platform already ships, single-implementation interfaces,
factories with one product, wrappers that only delegate, files exporting one
thing, dead flags and config, hand-rolled stdlib.

## Output

One line per finding, ranked: `<tag> <what to cut>. <replacement>. [path]`.
End with `net: -<N> lines, -<M> deps possible.` Nothing to cut: `Lean already. Ship.`

## Boundaries

Scope: over-engineering and complexity only. Correctness bugs, security holes,
and performance are explicitly out of scope. Route them to a normal review pass.

**Lists findings, applies nothing.** One-shot report. Applying any of it is a
separate, separately-approved change.

**Never propose deleting code someone else owns.** Before a `delete:` finding,
check authorship (`git log --diff-filter=A -- <path>`) and name the owner in the
finding. Existing tests, in particular, usually belong to another developer.

Dead-looking code is not always dead: an entity with no live table, a batch with
no run history, a helper with zero call sites each needs its status confirmed
before it is listed as a cut.

Write the findings in Korean; keep tags, paths, and code quotations verbatim.
===== END FILE: ~/.claude/skills/ponytail-audit/SKILL.md =====

===== BEGIN FILE: ~/.claude/skills/ponytail-review/SKILL.md (sha256=ad7d589df93ced0ac93dd646743228964169dbe4e7a741905243258752d928c3 lines=63 bytes=2588 mode=0o644) =====
---
name: ponytail-review
description: >
  Code review focused exclusively on over-engineering. Finds what to delete:
  reinvented standard library, unneeded dependencies, speculative abstractions,
  dead flexibility. One line per finding: location, what to cut, what replaces
  it. Use when the user says "review for over-engineering", "what can we
  delete", "is this over-engineered", "simplify review", or invokes
  /ponytail-review. Complements correctness-focused review, this one only
  hunts complexity.
---

Review diffs for unnecessary complexity. One line per finding: location, what
to cut, what replaces it. The diff's best outcome is getting shorter.

## Format

`L<line>: <tag> <what>. <replacement>.`, or `<file>:L<line>: ...` for
multi-file diffs.

Tags:

- `delete:` dead code, unused flexibility, speculative feature. Replacement: nothing.
- `stdlib:` hand-rolled thing the standard library ships. Name the function.
- `native:` dependency or code doing what the platform already does. Name the feature.
- `yagni:` abstraction with one implementation, config nobody sets, layer with one caller.
- `shrink:` same logic, fewer lines. Show the shorter form.

## Examples

❌ "This EmailValidator class might be more complex than necessary, have you
considered whether all these validation rules are needed at this stage?"

✅ `L12-38: stdlib: 27-line validator class. "@" in email, 1 line, real validation is the confirmation mail.`

✅ `L4: native: moment.js imported for one format call. Intl.DateTimeFormat, 0 deps.`

✅ `repo.py:L88: yagni: AbstractRepository with one implementation. Inline it until a second one exists.`

✅ `L52-71: delete: retry wrapper around an idempotent local call. Nothing replaces it.`

✅ `L30-44: shrink: manual loop builds dict. dict(zip(keys, values)), 1 line.`

## Scoring

End with the only metric that matters: `net: -<N> lines possible.`

If there is nothing to cut, say `Lean already. Ship.` and stop.

## Boundaries

Scope: over-engineering and complexity only. Correctness bugs, security holes,
and performance are explicitly out of scope. Route them to a normal review
pass, not this one.

**Lists findings, applies nothing.** One-shot report. Applying any of it is a
separate, separately-approved change.

**Never propose deleting code someone else owns.** Before a `delete:` finding,
check authorship (`git log --diff-filter=A -- <path>`) and name the owner in the
finding. Existing tests, in particular, usually belong to another developer.

Write the findings in Korean; keep tags, paths, and code quotations verbatim.
===== END FILE: ~/.claude/skills/ponytail-review/SKILL.md =====

===== BEGIN FILE: ~/.claude/skills/writing-for-agents/SKILL-MECHANICS.md (sha256=c768e6307c7c10728c401c213f2c4ba71c542127eeb7ad2956aabd15a0fa0059 lines=22 bytes=2629 mode=0o644) =====
# Skill mechanics

The skill-specific branch of [`writing-for-agents`](SKILL.md): what changes when the document is a skill (frontmatter, the invocation choice, and router skills). Everything else about writing it is the universal reference in `SKILL.md`.

## Invocation

Two choices, trading the two loads:

- A **model-invoked** skill keeps a `description`, so the agent can fire it autonomously, and other skills can reach it. You can still type its name: model-invocation always _includes_ user reach; a description only ever adds agent discovery, never removes the human's. The description is the skill's top-level context pointer, forced to stay loaded at all times: permanent context load in exchange for discoverability. A model-invoked skill whose content is all reference is also one home for shared reference: another skill can invoke it, so reference needed by several skills lives in one place. Mechanics: omit `disable-model-invocation`, and write a model-facing description carrying the trigger branches (the pointer-writing rules in `SKILL.md` apply in full).
- A **user-invoked** skill strips the description from the agent's reach: only the human typing its name can invoke it, and no other skill can. Zero context load, but it spends cognitive load: you are the index that must remember it exists. Mechanics: set `disable-model-invocation: true`; the `description` becomes human-facing: a one-line summary, trigger lists stripped.

Pick model-invocation only when the agent must reach the skill on its own, or another skill must. If it only ever fires by hand, make it user-invoked and pay no context load.

Shared reference that two user-invoked skills both need can live in neither: with no descriptions, neither can fire the other. Push it to a plain file outside the skill system: external reference any skill can point at.

## Splitting by invocation

The invocation cut of splitting (the sequence cut lives in `SKILL.md`): split off a model-invoked skill when you have a distinct leading word that should trigger it on its own (a trigger word you actually use in your prompts), or another skill must reach it. You pay context load for the new always-loaded description, so that independent reach has to be worth it.

## Router skills

When user-invoked skills multiply past what you can remember, that piled-up cognitive load is cured by a **router skill**: one user-invoked skill that names the others and when to reach for each, so the human has one skill to remember instead of many. It can only hint, never fire them: user-invoked skills have no description, so nothing but the human can reach them.
===== END FILE: ~/.claude/skills/writing-for-agents/SKILL-MECHANICS.md =====

===== BEGIN FILE: ~/.claude/skills/writing-for-agents/SKILL.md (sha256=d812e287565ee11de9b74565520b4c31deb4d8b096f3a9ccd577cf739d61256d lines=91 bytes=11888 mode=0o644) =====
---
name: writing-for-agents
description: Writing documents for agents. Use when creating or editing skills, or modifying AGENTS.md or CLAUDE.md.
---

Reference for writing any document an agent consumes: a skill, an `AGENTS.md` / `CLAUDE.md`, a doc reached by a pointer. The packaging differs; the writing does not: the same levers make each one predictable, since the agent takes the same _process_ every run rather than producing the same output.

When the document you're writing is a skill, read [`SKILL-MECHANICS.md`](SKILL-MECHANICS.md) for frontmatter, invocation choice, and router skills.

## House rules (this machine)

The global `~/.claude/CLAUDE.md` outranks anything here when they disagree. Three of its rules bind every document you write:

- **No emoji in executable files** (SQL, source, and their comments). Use bracketed text (`[주의]`, `[중요]`) instead; typographic marks (`─ → ★ ※ 「」 ①`) are fine. Markdown documents may use emoji.
- **Path handling**: an all-ASCII, space-free path gets a markdown link; a path with Korean or spaces gets inline code and **no link** (the VSCode extension percent-encodes link targets without decoding them, so the link silently does nothing).
- **Reference points toward the longer-lived document.** A permanent document never links a temporary one (plans, scratch notes, one-off reports); when a temporary document holds something worth keeping, move it (the reasoning, not just the conclusion) into the permanent one first.

Documents here are written in Korean unless they are near-verbatim copies of an English original.

## Context pointers

A **context pointer** is a reference held in the agent's context that names some out-of-context material and encodes the condition for reaching it. A skill's description is one; a line in `AGENTS.md` naming a doc is the same object. The pointer's _wording_, not its target, decides when the agent reaches the material, and how reliably. A must-have target behind a weakly worded pointer is a variance bug: sharpen the wording first, and inline the material only if sharpening fails.

A pointer does two jobs: state what the material is, and list the **branches** that should trigger reaching it (a branch is a distinct case the document handles, so different runs take different paths through it). Every word of an always-loaded pointer costs on every turn, so it earns even harder pruning than the body:

- **Front-load the leading word**: the pointer is where it does its triggering work.
- **One trigger per branch.** Synonyms that rename a single branch are one branch written twice; collapse them and keep only genuinely distinct branches.
- **Cut identity the body already carries.**

## The two loads

Every document and pointer you add spends one of two budgets:

- **Context load** is the cost of always-loaded material on the agent's window: an `AGENTS.md` line, a skill description, anything sitting in context every turn, spending tokens and attention whether or not it fires.
- **Cognitive load** is the cost on the human: which documents exist and when to reach for each. The human is the index. Not a cost to minimise: it is the price of human agency; spend it where human judgement matters, remove it where it does not.

Material reached only through a pointer escapes context load at the price of the pointer's own line; material with no pointer at all rides entirely on cognitive load.

## Information hierarchy

A document is built from two content types: **steps** (the ordered actions the agent performs) and **reference** (definitions, rules, facts consulted on demand). The two mix freely: all steps (a recipe), all reference (a review's rules, this skill), or both. The core decision is where each piece sits on the **information hierarchy**, a ladder ranked by how immediately the agent needs the material:

1. **In-file step** is the primary tier: what the agent does, in order.
2. **In-file reference** is consulted on demand. Often a legitimately flat peer-set (every rule of a review on one rung), which is a fine arrangement, not a smell.
3. **Disclosed reference** is pushed out into a separate file, reached by a context pointer, loaded only when the pointer fires. Spans a sibling file in the same folder through fully external reference that lives anywhere and any document can point at.

Push too little down and the top bloats; push too much and you hide material the agent actually needs. That tension is the whole decision.

**Progressive disclosure** is the move down the ladder (out of the main file and behind a pointer) so the top stays legible. Not primarily a token optimisation: it is how the hierarchy is protected. Branching is the cleanest disclosure test: inline what every branch needs, and push behind a pointer what only some branches reach. When a document has steps, in-file reference that should be disclosed buries them and turns attending to them into a coin-flip: a variance lever, not just a legibility one.

**Co-location** is the within-file companion: where the ladder decides _how far down_ a piece sits, co-location decides _what sits beside it_ once there. Keep a concept's definition, rules, and caveats under one heading rather than scattered, so reading one part brings its neighbours with it. The test: the document should read like documentation written for the agent. Grouped material reads that way; scattered material does not. (Distinct from duplication: that repeats one meaning in two places; scattering fragments one meaning across many.)

**Sprawl** is the failure mode here: a document simply too long, even when every line is live and unique. Attention thins across the excess, and every extra line is one more to keep relevant. The cure is the ladder: disclose reference behind pointers, and split by branch or sequence so each path carries only what it needs.

## Steps and completion criteria

Every step ends on a **completion criterion**, the condition that tells the agent the work is done. Two properties make it a lever:

- **Clarity**: can the agent tell done from not-done? A vague bound ("understanding reached") invites **premature completion**: ending the step before it is genuinely done, attention slipping to _being done_. The visible steps still ahead (the **post-completion steps**) supply the pull; the criterion's clarity is the resistance. Defend in order: **sharpen the bound first** (local and cheap); only if it is irreducibly fuzzy _and_ you observe the rush, hide the later steps by splitting the sequence. Hiding only works across a real context boundary (a hand-off or a subagent dispatch; an inline call leaves the later steps in context and clears nothing).
- **Demand**: how much it requires. "Every modified model accounted for" forces thorough work where "produce a change list" does not. Demand drives **legwork** (the digging the agent does within the work, latent in the wording rather than written as its own step), and it is not step-bound: "every rule applied" binds a body of flat reference just as "every step done" binds a sequence, which is how an all-reference document still carries an exhaustiveness bar.

The strongest criteria are both checkable and exhaustive.

## When to split

Splitting one document into two spends one of the two loads, so split only when the cut earns it:

- **By sequence**: split a run of steps where the post-completion steps tempt the agent to rush the one in front of it. Keeping them out of view drives more legwork on the current task. Beware the reverse: merging sequences exposes each step's later steps to what follows, inviting premature completion.
- **By invocation**, skill-specific: see [`SKILL-MECHANICS.md`](SKILL-MECHANICS.md).

## Leading words

A **leading word** is a compact concept already living in the model's pretraining that the agent thinks with while running the document (_lesson_, _fog of war_, _tracer bullets_). Repeated as a token, never as a sentence, it accumulates a distributed definition and anchors a whole region of behaviour in the fewest tokens, by recruiting priors the model already holds. Coining your own works if you define it clearly, but a made-up word recruits no priors: you pay in definition tokens what a pretrained word gives free; reach for an existing word first.

It anchors twice. In the body, _execution_: the agent reaches for the same behaviour every time the word appears, and inside flat reference it focuses attention on a class of thing to look for. In a pointer, _invocation_: when the same word lives in your prompts, your docs, and your codebase, the agent links that shared language to the material and reaches it more reliably.

Hunt for opportunities to refactor with leading words. A triad spelled out at three sites, a pointer spending a sentence to gesture at one idea. Each is a passage begging to collapse into a single token:

- "fast, deterministic, low-overhead" → _tight_ (a _tight_ loop).
- "a loop you believe in" → _red_, turning a fuzzy gate into a binary observable state (the loop goes _red_ on the bug, or it doesn't).

You win twice: fewer tokens, and a sharper hook for the agent to hang its thinking on. Assume every document is carrying restatements that leading words retire. Go find them.

**Negation** is the failure mode beside this lever: steering by prohibition drags the forbidden behaviour into context and makes it _more_ available, not less. _Don't think of an elephant_, and the elephant is all there is; the negation is a weak modifier the strongly-activated concept overruns, so the ban half-reads as an instruction to do the thing. Prompt the **positive**: state the target behaviour ("write one-line comments") so the banned one is never spoken. A prohibition earns its place only as a hard guardrail you cannot phrase positively; even then, pair it with the positive target so attention lands on what to do.

## Pruning

- Keep each meaning in a **single source of truth**: one authoritative place, so changing the behaviour is a one-place edit. **Duplication** (the same meaning in more than one place) costs maintenance and tokens, and inflates a meaning's prominence on the ladder past its real rank. (The accidental inverse of a leading word, which repeats a token on purpose, never the meaning.)
- The **environment** is a source of truth too (`package.json` scripts, config files, the directory layout, `--help` output), and a document that restates it is a **cache**: a copy of a lookup, earning its load only when the lookup is expensive. Cache what the agent cannot find by looking: the unwritten convention, the reason behind a choice, the gotcha no config confesses. Leave the one-file, one-command lookups to the environment, where they cannot go stale.
- Check every line for **relevance**: does it still bear on what the document does? A line loses relevance by never bearing on the task (mere exposition, or a branch that should be disclosed) or by going stale as the behaviour or world it describes changes. Shorter documents are easier to keep relevant. Without a pruning discipline the default fate is **sediment**: stale layers that settle because adding feels safe and removing feels risky, until you must core down through them to find what is still live.
- Hunt **no-ops** sentence by sentence: an instruction the model already obeys by default pays load to say nothing. The test (does it change behaviour versus the default?) is model-relative, not reader-relative: two people disagreeing about a no-op disagree about the default, and settle it by running the document, not by debate. When a sentence fails, delete the whole sentence rather than trim words from it. The test also grades leading words: a word too weak to beat the default (_be thorough_ when the agent is already thorough-ish) is a no-op, and the fix is a stronger word (_relentless_), not a different technique.
===== END FILE: ~/.claude/skills/writing-for-agents/SKILL.md =====

===== BEGIN FILE: ~/.claude/hooks/plan_gate.py (sha256=9c3ee5e2ba7f302a566fc38e4afda231eb28ec740e7ddbe3a6ec95fb175fd3e0 lines=92 bytes=3592 mode=0o644) =====
#!/usr/bin/env python3
"""
계획서 선행 게이트 훅 (PreToolUse)

문서(`docs/**`, `**/*.md`)를 제외한 프로젝트 파일을 편집하려 할 때, 이번 세션에 계획서
활동이 없었다면 권한 프롬프트(`ask`)로 승격시킨다. auto 모드에서도 조용히 통과되지 않는다.

오타·주석·로그 메시지 수정은 규칙상 계획서 예외이므로 차단(`deny`)이 아니라 `ask` 를
사용한다. 사용자가 승인하고 `plan_lint.py` 검사를 통과하면 세션 마커가 남아 이후 편집은 통과한다.

출력에는 사용자용 `permissionDecisionReason` 과 모델용 `additionalContext` 를 함께 담는다.
전자만 내보내면 모델에게는 "거부됨" 신호만 전달돼, 계획서 작성으로 넘어가지 않고 편집을
재시도하는 일이 생긴다.

경로 판정·마커 규약·규약 채택 판정은 `plan_lint.py` 가 소유하며 여기서 재사용한다.

입력: PreToolUse 훅 JSON (stdin)
출력: 게이트 대상이면 `permissionDecision: ask` (stdout), 아니면 무출력
"""

import json
import sys

from plan_lint import is_gated, marker_path, project_uses_plans, relative_path

GATE_REASON = (
    "계획서 없이 코드를 변경하려 합니다.\n"
    "프로젝트 규칙상 오타·주석·로그 메시지 수정을 제외한 모든 코드 변경은 "
    "`docs/plans/PLAN_<short_name>.md` 를 먼저 작성해야 합니다.\n"
    "계획서를 작성하려면 `/impl-plan` 스킬을 사용하세요. "
    "예외에 해당하는 사소한 수정이면 이 요청을 승인하면 됩니다."
)

# 모델에게 주입되는 지침. 훅은 사용자의 승인/거부를 알 수 없으므로 양쪽 모두에서
# 유효하게 서술한다.
GATE_CONTEXT = (
    "이 편집은 계획서 선행 게이트에 걸려 사용자 승인 대기 상태가 되었다.\n"
    "- 이 작업이 오타·주석·로그 메시지 수정에 해당하면 그대로 진행한다.\n"
    "- 그 외의 코드 변경이면, 편집을 재시도하기 전에 `/impl-plan` 스킬로 "
    "`docs/plans/PLAN_<short_name>.md` 를 먼저 작성한다.\n"
    "- 거부된 경우 같은 편집을 다시 시도하지 말고 계획서 작성으로 넘어간다. "
    "이 게이트는 세션당 한 번만 걸린다."
)


def main() -> int:
    """
    훅 진입점: stdin JSON을 읽어 계획서 선행 여부를 판정합니다.

    Returns:
        int: 종료 코드 (항상 0. 판정은 stdout JSON으로 전달)
    """
    try:
        payload = json.load(sys.stdin)
    except (json.JSONDecodeError, ValueError):
        return 0

    cwd = str(payload.get("cwd") or "")
    if not project_uses_plans(cwd):
        return 0

    session_id = str(payload.get("session_id") or "")
    if not session_id:
        # 세션을 식별할 수 없으면 마커 추적이 불가능하다. 반복 프롬프트를 피해 통과시킨다
        return 0

    tool_input = payload.get("tool_input") or {}
    rel_path = relative_path(str(tool_input.get("file_path") or ""), cwd)

    if rel_path is None or not is_gated(rel_path):
        return 0

    if marker_path(session_id).exists():
        return 0

    json.dump(
        {
            "hookSpecificOutput": {
                "hookEventName": "PreToolUse",
                "permissionDecision": "ask",
                "permissionDecisionReason": GATE_REASON,
                "additionalContext": GATE_CONTEXT,
            }
        },
        sys.stdout,
        ensure_ascii=False,
    )
    return 0


if __name__ == "__main__":
    sys.exit(main())
===== END FILE: ~/.claude/hooks/plan_gate.py =====

===== BEGIN FILE: ~/.claude/hooks/plan_lint.py (sha256=ce1a2b36e3a1b7bb92891f7b613088f5cf064720fb017b64fa11aaa3ade07860 lines=311 bytes=11264 mode=0o644) =====
#!/usr/bin/env python3
"""
계획서 불변조건 검사 훅 (PostToolUse)

계획서(`docs/plans/PLAN_*.md`)가 저장될 때 Done 처리 규칙을 기계적으로 검사하고,
위반 시 `decision: block`으로 되돌린다. 프롬프트 수준의 경고를 대체하는 결정적 게이트다.

Edit/Write 뿐 아니라 Bash 를 통한 수정(`sed -i`, `cat >`, heredoc 등)도 검사한다.
Bash 는 편집 대상 파일을 입력으로 주지 않으므로, 명령문에 실제로 등장한 계획서 파일명만
골라 검사한다. 무관한 명령이 기존 위반 파일 때문에 차단되는 것을 막기 위함이다.

동시에 `plan_gate.py`가 참조하는 세션 마커를 기록한다. 마커는 "이번 세션에서
검사를 통과한 계획서를 썼거나, 게이트 대상 파일 편집이 이미 승인됐다"는 사실을 나타낸다.
검사에 걸린 계획서로는 마커를 남기지 않는다 — 그러면 게이트의 전제가 무너진다.
Bash 경로에서는 읽기와 쓰기를 구분할 수 없으므로 마커를 남기지 않는다.

입력: PostToolUse 훅 JSON (stdin)
출력: 위반 시 `{"decision": "block", "reason": ...}` (stdout), 정상 시 무출력
"""

import json
import os
import re
import sys
import tempfile
from pathlib import Path

# 계획서 경로 (프로젝트 루트 기준 상대경로)
PLAN_DIR = "docs/plans"
PLAN_PREFIX = "docs/plans/PLAN_"

# 게이트 제외 경로. 나머지는 언어·레이아웃과 무관하게 전부 대상이다.
# 화이트리스트는 새 언어·빌드 파일이 생길 때 조용히 통과시켜 게이트 목적과 실패 방향이 반대다.
# 문서를 제외하는 이유: 계획서가 `docs/plans/` 에 있어 게이트하면 순환이 생기고,
# 규칙상 "코드 변경"에도 해당하지 않는다.
EXCLUDED_DIRS = ("docs/",)
EXCLUDED_SUFFIXES = (".md",)

# Bash 명령문에서 계획서 파일명을 뽑아내는 패턴
PLAN_TOKEN = re.compile(r"PLAN_[A-Za-z0-9_.-]*\.md")

# 계획서 본문 판정용 패턴
STATUS_PREFIX = "**상태**:"
STATUS_DONE = "**상태**: ✅ Done"
FIXED_RULES_HEADER = "## 0) 고정 규칙"
UNCHECKED_BOX = re.compile(r"^\s*- \[ \]", re.MULTILINE)
VALIDATION_LINE = re.compile(r"^\s*- \[[ xX]\].*validate_project\.py.*$", re.MULTILINE)
# 실제 계획서는 `failed=**0**` 처럼 마크다운 강조를 섞어 쓰므로 강조 기호를 허용한다
FAILED_COUNT = re.compile(r"failed=[*_]*(\d+)")
SKIPPED_COUNT = re.compile(r"skipped=[*_]*(\d+)")


def is_done(text: str) -> bool:
    """
    계획서 상태가 Done인지 판정합니다.

    템플릿의 `**상태**: 🟡 Draft / 🔄 In Progress / ✅ Done` 처럼 선택지가 나열된
    줄을 Done으로 오판하지 않도록, 첫 상태 줄이 정확히 Done 표기일 때만 True 입니다.

    Args:
        text: 계획서 전문

    Returns:
        bool: Done 상태 여부
    """
    for line in text.splitlines():
        stripped = line.strip()
        if stripped.startswith(STATUS_PREFIX):
            return stripped == STATUS_DONE
    return False


def check_plan(text: str) -> list[str]:
    """
    계획서 본문의 불변조건을 검사합니다.

    검사 항목:
        1. `## 0) 고정 규칙` 섹션 보존 여부 (상태와 무관하게 항상 검사)
        2. Done 상태에서 미완료 체크박스(`- [ ]`) 잔존 여부
        3. Done 상태에서 Validation 결과의 failed/skipped 가 0인지 여부

    Args:
        text: 계획서 전문

    Returns:
        list[str]: 위반 메시지 목록. 비어 있으면 통과
    """
    violations: list[str] = []

    # 1. 고정 규칙 섹션은 삭제/변형이 금지된다
    if FIXED_RULES_HEADER not in text:
        violations.append(f"`{FIXED_RULES_HEADER}` 섹션이 없습니다. 이 섹션은 삭제/수정이 금지됩니다.")

    if not is_done(text):
        return violations

    # 2. Done 이면 미완료 체크박스가 남아 있을 수 없다
    unchecked = len(UNCHECKED_BOX.findall(text))
    if unchecked > 0:
        violations.append(f"상태가 Done 인데 미완료 체크박스가 {unchecked}개 남아 있습니다. 모두 [x] 로 만들거나 상태를 In Progress 로 되돌리세요.")

    # 3. Done 이면 Validation 결과가 failed=0, skipped=0 이어야 한다
    for line in VALIDATION_LINE.findall(text):
        failed = FAILED_COUNT.search(line)
        skipped = SKIPPED_COUNT.search(line)
        if failed is None or skipped is None:
            violations.append(f"상태가 Done 인데 Validation 줄에 passed/failed/skipped 수가 기록되지 않았습니다: {line.strip()}")
            continue
        if int(failed.group(1)) != 0 or int(skipped.group(1)) != 0:
            violations.append(f"상태가 Done 인데 Validation 결과가 failed=0, skipped=0 이 아닙니다: {line.strip()}")

    return violations


def marker_path(session_id: str) -> Path:
    """
    세션별 게이트 통과 마커 경로를 반환합니다.

    Args:
        session_id: 훅 입력으로 전달된 세션 ID

    Returns:
        Path: 마커 파일 경로
    """
    return Path(tempfile.gettempdir()) / "claude-plan-gate" / session_id


def relative_path(file_path: str, cwd: str) -> str | None:
    """
    편집 대상 파일을 프로젝트 루트 기준 상대경로(POSIX 구분자)로 변환합니다.

    Args:
        file_path: 훅 입력의 절대 경로
        cwd: 훅 입력의 작업 디렉토리

    Returns:
        str | None: 상대경로. 변환 불가 시 None
    """
    if not file_path or not cwd:
        return None
    try:
        return Path(os.path.relpath(file_path, cwd)).as_posix()
    except ValueError:
        # 다른 드라이브 등 상대경로 계산이 불가능한 경우
        return None


def is_gated(rel_path: str) -> bool:
    """
    계획서 게이트의 감시 대상 파일인지 판정합니다.

    제외 목록(문서·마크다운)과 프로젝트 밖 경로를 뺀 나머지가 전부 대상입니다.
    게이트는 차단이 아니라 `ask` 이고 세션당 한 번만 걸리므로, 놓치는 것보다
    넓게 잡는 편이 비용이 낮습니다.

    Args:
        rel_path: 프로젝트 루트 기준 상대경로

    Returns:
        bool: 감시 대상 여부
    """
    if rel_path.startswith("../"):
        # 이 프로젝트 밖의 파일
        return False
    if rel_path.startswith(EXCLUDED_DIRS):
        return False
    return not rel_path.endswith(EXCLUDED_SUFFIXES)


def project_uses_plans(cwd: str) -> bool:
    """
    이 프로젝트가 `docs/plans/` 계획서 규약을 채택했는지 판정합니다.

    두 훅의 나머지 로직은 이 규약(경로·템플릿·Done 조건)을 전제로 하므로,
    채택하지 않은 프로젝트에서는 아무 동작도 하지 않아야 합니다. 이 판정 덕분에
    훅을 전역(`~/.claude/`)에 두어도 다른 언어의 프로젝트를 방해하지 않습니다.

    Args:
        cwd: 훅 입력의 작업 디렉토리

    Returns:
        bool: 규약 채택 여부
    """
    return bool(cwd) and (Path(cwd) / PLAN_DIR).is_dir()


def plans_referenced(command: str, cwd: str) -> list[Path]:
    """
    Bash 명령문이 실제로 언급한 계획서 파일 경로를 수집합니다.

    명령문에 파일명이 등장하지 않으면 빈 목록을 반환합니다. 계획서 폴더를 전수 검사하면
    `ls docs/plans/` 같은 무관한 명령까지 기존 위반 때문에 차단되므로, 언급된 파일만 봅니다.

    Args:
        command: Bash 툴에 전달된 명령문
        cwd: 훅 입력의 작업 디렉토리

    Returns:
        list[Path]: 실재하는 계획서 파일 경로 (중복 제거, 정렬)
    """
    if not command or not cwd:
        return []

    plan_dir = Path(cwd) / PLAN_DIR
    found: set[Path] = set()
    for name in PLAN_TOKEN.findall(command):
        candidate = plan_dir / name
        if candidate.is_file():
            found.add(candidate)
    return sorted(found)


def touch_marker(session_id: str) -> None:
    """
    게이트 통과 마커를 기록합니다. 실패해도 훅 전체를 중단시키지 않습니다.

    Args:
        session_id: 훅 입력으로 전달된 세션 ID
    """
    if not session_id:
        return
    try:
        path = marker_path(session_id)
        path.parent.mkdir(parents=True, exist_ok=True)
        path.touch()
    except OSError:
        pass


def lint_file(path: Path) -> list[str]:
    """
    계획서 파일 하나를 검사하고 파일명이 붙은 위반 목록을 반환합니다.

    Args:
        path: 계획서 파일 경로

    Returns:
        list[str]: `파일명: 위반내용` 형식의 목록. 읽기 실패 시 빈 목록
    """
    try:
        text = path.read_text(encoding="utf-8")
    except OSError:
        return []
    return [f"{path.name}: {item}" for item in check_plan(text)]


def emit_block(violations: list[str]) -> None:
    """
    차단 결정을 stdout에 JSON으로 출력합니다.

    Args:
        violations: 위반 메시지 목록 (비어 있지 않아야 함)
    """
    # Bash 읽기 명령으로도 발동할 수 있으므로 "다시 저장하라"고 단정하지 않는다
    reason = "계획서 불변조건 위반 — 아래 항목을 수정해야 합니다.\n" + "\n".join(f"- {item}" for item in violations)
    json.dump({"decision": "block", "reason": reason}, sys.stdout, ensure_ascii=False)


def main() -> int:
    """
    훅 진입점: stdin JSON을 읽어 계획서를 검사하고 마커를 기록합니다.

    Returns:
        int: 종료 코드 (항상 0. 차단은 stdout JSON으로 전달)
    """
    try:
        payload = json.load(sys.stdin)
    except (json.JSONDecodeError, ValueError):
        return 0

    cwd = str(payload.get("cwd") or "")
    if not project_uses_plans(cwd):
        return 0

    session_id = str(payload.get("session_id") or "")
    tool_input = payload.get("tool_input") or {}

    # Bash 경로: 명령문이 실제로 언급한 계획서만 검사한다. 읽기/쓰기를 구분할 수 없어 마커는 남기지 않는다
    command = str(tool_input.get("command") or "")
    if command:
        violations = [item for path in plans_referenced(command, cwd) for item in lint_file(path)]
        if violations:
            emit_block(violations)
        return 0

    # Edit/Write 경로
    rel_path = relative_path(str(tool_input.get("file_path") or ""), cwd)
    if rel_path is None:
        return 0

    # 게이트 대상 파일 편집이 성사됐다는 것은 게이트를 통과했다는 뜻이다
    if is_gated(rel_path):
        touch_marker(session_id)
        return 0

    if not (rel_path.startswith(PLAN_PREFIX) and rel_path.endswith(".md")):
        return 0

    violations = lint_file(Path(str(tool_input.get("file_path"))))
    if violations:
        # 차단된 계획서로 게이트가 열리면 "유효한 계획서 존재"라는 전제가 무너진다
        emit_block(violations)
        return 0

    # 유효한 계획서를 썼으므로 이후 코드 편집은 게이트를 통과시킨다
    touch_marker(session_id)
    return 0


if __name__ == "__main__":
    sys.exit(main())
===== END FILE: ~/.claude/hooks/plan_lint.py =====

===== BEGIN FILE: ~/.claude/hooks/toast.log (sha256=a8f37af07f5e345521ce7f4c7873945d0cc58b40bc283a437fea0711f910db04 lines=233 bytes=19417 mode=0o644) =====
2026-08-29 19:19:52 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 19:19:56 OK [질문] verify-lab | 세 개의 알림이 순서대로 보였나요?
2026-08-29 19:19:59 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-29 19:21:50 OK [질문] verify-lab | 지금 이 질문이 뜨는 순간 Windows 알림이 함께 떴나요? ('[질문] verify-lab' 제목이었을 겁니다)
2026-08-29 19:21:53 OK [대기] verify-lab | Claude needs your permission to use AskUserQuestion
2026-08-29 19:26:07 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 19:26:29 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-29 19:27:28 OK [대기] verify-lab | Claude needs your permission to use Read
2026-08-29 19:28:50 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-29 19:28:59 OK [완료] quant | 작업이 끝났습니다
2026-08-29 19:30:43 OK [대기] verify-lab | Claude needs your permission to use Read
2026-08-29 19:31:09 OK [질문] quant | 로컬의 poetry 계열 6개를 `Bash(env -u VIRTUAL_ENV poetry:*)` 하나로 통합해 전역에 넣을까요? (규칙 7 - 임의코드 실행 경계)
2026-08-29 19:33:50 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 19:34:36 OK [대기] quant | Claude needs your permission to use Edit
2026-08-29 19:34:51 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-29 19:35:25 OK [대기] quant | Claude needs your permission to use Bash
2026-08-29 19:36:08 OK [완료] quant | 작업이 끝났습니다
2026-08-29 19:36:25 OK [대기] quant | Claude needs your permission to use Bash
2026-08-29 19:36:26 OK [대기] quant | Claude needs your permission to use Bash
2026-08-29 19:36:26 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-29 19:42:00 OK [질문] quant | 승인 프롬프트를 어떤 방식으로 없앨까요?
2026-08-29 19:43:40 OK [완료] quant | 작업이 끝났습니다
2026-08-29 19:45:03 OK [대기] quant | Claude needs your permission to use Edit
2026-08-29 19:46:14 OK [대기] quant | Claude needs your permission to use Bash
2026-08-29 19:47:26 OK [완료] quant | 작업이 끝났습니다
2026-08-29 19:47:49 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-29 19:49:18 OK [완료] quant | 작업이 끝났습니다
2026-08-29 19:52:45 OK [완료] quant | 작업이 끝났습니다
2026-08-29 19:53:24 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 19:53:24 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 19:53:25 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 19:54:17 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 19:56:04 OK [질문] verify-lab | 조사 결과 verify-plan 은 전역이 아니라 verify-lab 전용 스킬이었고, krx-sprint 에 거의 같은 복사본이 따로 있습니다. 어느 방향으로 갈까요?
2026-08-29 19:58:46 OK [완료] quant | 작업이 끝났습니다
2026-08-29 19:59:21 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 20:04:01 OK [완료] quant | 작업이 끝났습니다
2026-08-29 20:05:30 OK [질문] verify-lab | 전역 스킬 이름을 무엇으로 할까요?
2026-08-29 20:07:51 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 20:15:51 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-29 20:16:00 OK [대기] quant | Claude needs your permission to use Bash
2026-08-29 20:19:20 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 20:22:59 OK [완료] quant | 작업이 끝났습니다
2026-08-29 20:24:01 OK [대기] verify-lab | Claude needs your permission to use Edit
2026-08-29 20:24:50 OK [대기] verify-lab | Claude needs your permission to use Edit
2026-08-29 20:25:08 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 20:25:52 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 20:26:35 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-29 21:18:21 OK [대기] verify-lab | Claude needs your permission to use Edit
2026-08-29 21:18:31 OK [대기] verify-lab | Claude needs your permission to use Edit
2026-08-29 21:21:02 OK [대기] verify-lab | Claude needs your permission to use Edit
2026-08-29 21:22:24 OK [대기] verify-lab | Claude needs your permission to use Write
2026-08-29 21:23:23 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-29 21:28:03 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 21:29:18 OK [대기] verify-lab | Claude needs your permission to use Edit
2026-08-29 21:29:23 OK [대기] verify-lab | Claude needs your permission to use Edit
2026-08-29 21:29:56 OK [대기] verify-lab | Claude needs your permission to use Edit
2026-08-29 21:30:06 OK [완료] quant | 작업이 끝났습니다
2026-08-29 21:31:51 OK [대기] verify-lab | Claude needs your permission to use Edit
2026-08-29 21:32:04 OK [대기] verify-lab | Claude needs your permission to use Edit
2026-08-29 21:32:40 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-29 21:34:06 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 21:57:18 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-29 22:00:40 OK [질문] quant | verify-lab · quant 의 docs/plans/ 에 .gitkeep 을 추가할까요?
2026-08-29 22:02:29 OK [대기] quant | Claude needs your permission to use Bash
2026-08-29 22:14:08 OK [대기] krx-sprint | Claude needs your permission to use Edit
2026-08-29 22:32:27 OK [완료] krx-sprint | 작업이 끝났습니다
2026-08-29 22:38:40 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-29 22:42:06 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 07:34:47 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 07:46:30 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 07:47:37 OK [완료] quant | 작업이 끝났습니다
2026-08-30 07:53:01 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 08:01:52 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 08:08:15 OK [완료] quant | 작업이 끝났습니다
2026-08-30 08:08:30 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 08:13:58 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 08:16:09 OK [완료] quant | 작업이 끝났습니다
2026-08-30 08:20:07 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 08:22:20 OK [질문] quant | EMA 코드 경로를 어떻게 할까요?
2026-08-30 08:28:55 OK [완료] quant | 작업이 끝났습니다
2026-08-30 08:32:34 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 08:37:12 OK [질문] quant | SMA 전환 후 `TestEmaContiniuty` 클래스·메서드 이름은 어떻게 할까요?
2026-08-30 08:37:57 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 08:42:23 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 08:42:46 OK [완료] quant | 작업이 끝났습니다
2026-08-30 08:52:43 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 08:54:11 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 08:55:55 OK [완료] quant | 작업이 끝났습니다
2026-08-30 08:56:16 OK [질문] verify-lab | 1차 게이트(화면·문서에 올릴지 가르는 선)를 무엇으로 잡을까요?
2026-08-30 08:57:55 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 09:01:56 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 09:04:03 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 09:05:49 OK [완료] quant | 작업이 끝났습니다
2026-08-30 09:10:32 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 09:14:04 OK [완료] quant | 작업이 끝났습니다
2026-08-30 09:18:45 OK [완료] quant | 작업이 끝났습니다
2026-08-30 09:19:57 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 09:22:04 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 09:22:10 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 09:23:29 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 09:23:52 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 09:24:23 OK [완료] quant | 작업이 끝났습니다
2026-08-30 09:26:26 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 09:28:08 OK [완료] quant | 작업이 끝났습니다
2026-08-30 09:28:09 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 09:32:44 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 09:34:45 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 09:36:45 OK [완료] quant | 작업이 끝났습니다
2026-08-30 09:41:32 OK [질문] quant | EMA 제거 범위를 어디까지 할까요?
2026-08-30 09:43:10 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 09:44:34 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 09:47:10 OK [완료] quant | 작업이 끝났습니다
2026-08-30 10:03:41 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 10:10:03 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 10:13:56 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 10:14:27 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 10:17:58 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 10:21:01 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 10:36:12 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 10:39:13 OK [완료] quant | 작업이 끝났습니다
2026-08-30 10:49:11 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 10:49:45 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 10:50:21 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 10:50:54 OK [완료] quant | 작업이 끝났습니다
2026-08-30 10:58:02 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 10:58:09 OK [질문] verify-lab | 가격 기준을 원본가로 통일하면 한국(KODEX 200) 결과가 바뀝니다. 어떻게 할까요?
2026-08-30 11:00:04 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:01:01 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:01:24 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:03:48 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 11:07:58 OK [완료] quant | 작업이 끝났습니다
2026-08-30 11:08:52 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 11:10:11 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:11:06 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:11:28 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:13:44 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:14:38 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:15:10 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:15:44 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:15:46 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 11:18:25 OK [완료] quant | 작업이 끝났습니다
2026-08-30 11:21:11 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 11:22:00 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:24:26 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 11:26:28 OK [완료] quant | 작업이 끝났습니다
2026-08-30 11:30:46 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 11:31:13 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 11:32:22 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:32:48 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:33:55 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:35:30 OK [질문] quant | sell_buffer 비교 실험과 SPY 고원 추가를 언제 진행할까요?
2026-08-30 11:37:01 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:37:49 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:38:45 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:41:27 OK [완료] quant | 작업이 끝났습니다
2026-08-30 11:45:39 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 11:47:32 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:47:54 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:50:55 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 11:51:49 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 11:54:46 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 11:55:33 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 11:56:27 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 11:56:47 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 11:57:01 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 11:59:08 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 12:02:09 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 12:05:20 OK [완료] quant | 작업이 끝났습니다
2026-08-30 12:07:32 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 12:08:53 OK [질문] verify-lab | 총자산 곡선이 필요한 매매법이 현재 저장소에 0개입니다(그리드는 삭제됨, 역방향 매매는 노출 1% 미만). performance.py 를 어떻게 할까요?
2026-08-30 12:12:07 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 12:16:34 OK [완료] quant | 작업이 끝났습니다
2026-08-30 12:19:43 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 12:21:57 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 12:22:14 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 12:23:37 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 12:24:43 OK [질문] quant | sell_buffer 고원의 저거래 필터 기준을 어느 값으로 통일할까요?
2026-08-30 12:27:49 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 12:28:23 OK [질문] param_plateau | 지적하신 내용을 반영해 추천을 바꿉니다. 저거래 필터를 어떻게 할까요?
2026-08-30 12:31:34 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:00:55 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 16:02:08 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:03:52 OK [완료] quant | 작업이 끝났습니다
2026-08-30 16:07:13 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:13:55 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:14:55 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:15:14 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 16:17:42 OK [완료] quant | 작업이 끝났습니다
2026-08-30 16:19:48 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:20:09 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:24:45 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:28:47 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:35:24 OK [완료] quant | 작업이 끝났습니다
2026-08-30 16:40:01 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:43:00 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:43:31 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:44:36 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:45:00 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:46:40 OK [완료] quant | 작업이 끝났습니다
2026-08-30 16:47:55 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 16:51:46 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 16:53:06 OK [대기] 20260830_164518_index_extreme | Claude needs your permission to use Bash
2026-08-30 17:01:09 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 17:42:39 OK [질문] verify-lab | 연속 등락 결과 문서를 어디에 어떤 형태로 남길까요?
2026-08-30 17:44:31 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 17:48:54 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 17:50:36 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 17:51:52 OK [완료] quant | 작업이 끝났습니다
2026-08-30 17:52:24 OK [질문] verify-lab | 손절 방식을 어떻게 확정할까요? (이 결정으로 계획서를 쓰겠습니다)
2026-08-30 17:57:19 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 17:59:33 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 18:00:06 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 18:04:57 OK [대기] quant | Claude needs your permission to use Bash
2026-08-30 18:06:26 OK [완료] quant | 작업이 끝났습니다
2026-08-30 18:08:20 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 18:30:10 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 19:52:42 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 20:00:15 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 20:01:24 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 20:13:59 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 20:15:43 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 20:20:07 OK [대기] verify-lab | Claude needs your permission to use Edit
2026-08-30 20:22:46 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 20:26:08 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 21:09:11 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 21:10:41 OK [대기] verify-lab | Claude needs your permission to use Bash
2026-08-30 21:13:03 OK [질문] verify-lab | docs/context/README.md 의 "원본을 수정하지 않습니다" 규정을 어떻게 처리할까요?
2026-08-30 21:15:38 OK [대기] verify-lab | Claude needs your permission to use Edit
2026-08-30 21:18:06 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 21:20:04 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 21:22:58 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 21:34:03 OK [완료] verify-lab | 작업이 끝났습니다
2026-08-30 21:35:03 OK [완료] verify-lab | 작업이 끝났습니다
===== END FILE: ~/.claude/hooks/toast.log =====

===== BEGIN FILE: ~/.claude/hooks/wsl_toast.py (sha256=299231a13c88fc5ac852d2d4a0fd4b2ecc33eb758d7b8b92007f277395d4bdaa lines=127 bytes=4624 mode=0o755) =====
#!/usr/bin/env python3
"""WSL2 에서 Windows 토스트 알림을 띄우는 Claude Code hook.

stdin 으로 hook JSON 을 받아 이벤트 종류에 맞는 알림을 Windows 에 띄운다.
승인 요청·질문·작업 완료처럼 사용자 입력을 기다리며 멈추는 지점을 알리는 것이 목적이다.

WinRT ToastNotification API 를 PowerShell 로 호출한다 (BurntToast 등 외부 모듈 불필요).
명령은 UTF-16LE + base64 로 감싸 전달하므로 따옴표·특수문자 이스케이프 문제가 없다.
"""

from __future__ import annotations

import base64
import json
import os
import shutil
import subprocess
import sys
from datetime import datetime
from pathlib import Path
from xml.sax.saxutils import escape

# powershell.exe 를 WSL 경로에서 실행하면 UNC 경고가 나므로 Windows 드라이브에서 실행한다
PS_WORKDIR = "/mnt/c"
PS_FALLBACK = "/mnt/c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe"
# shell:AppsFolder 에 등록된 powershell.exe 의 AppUserModelID
APP_ID = r"{1AC14E77-02E7-4E5D-B744-2EB1AE5198B7}\WindowsPowerShell\v1.0\powershell.exe"
LOG_PATH = Path.home() / ".claude" / "hooks" / "toast.log"
BODY_MAX = 120


def find_powershell() -> str | None:
    return shutil.which("powershell.exe") or (PS_FALLBACK if os.path.exists(PS_FALLBACK) else None)


def build_message(payload: dict) -> tuple[str, str] | None:
    """hook JSON 에서 (제목, 본문) 을 만든다. 알릴 필요가 없으면 None."""
    event = payload.get("hook_event_name", "")
    project = Path(payload.get("cwd") or os.getcwd()).name or "Claude Code"

    if event == "Notification":
        # 승인 요청과 입력 대기 모두 여기로 온다. Claude Code 가 준 문구를 그대로 쓴다
        body = payload.get("message") or "입력을 기다리는 중입니다"
        # AskUserQuestion 승인 요청은 아래 PreToolUse 분기가 이미 알린다.
        # 여기서 거르지 않으면 질문 한 번에 알림이 두 번 울린다 (실측 확인)
        if "AskUserQuestion" in body:
            return None
        return f"[대기] {project}", body

    if event == "PreToolUse" and payload.get("tool_name") == "AskUserQuestion":
        questions = payload.get("tool_input", {}).get("questions") or []
        first = questions[0] if questions else {}
        body = first.get("question") or first.get("header") or "질문이 있습니다"
        return f"[질문] {project}", body

    if event == "Stop":
        return f"[완료] {project}", "작업이 끝났습니다"

    return None


def show_toast(powershell: str, title: str, body: str) -> None:
    if len(body) > BODY_MAX:
        body = body[: BODY_MAX - 1] + "…"
    xml = (
        "<toast><visual><binding template=\"ToastGeneric\">"
        f"<text>{escape(title)}</text><text>{escape(body)}</text>"
        "</binding></visual></toast>"
    )
    script = f"""
$ErrorActionPreference = 'Stop'
[Windows.UI.Notifications.ToastNotificationManager, Windows.UI.Notifications, ContentType = WindowsRuntime] | Out-Null
[Windows.Data.Xml.Dom.XmlDocument, Windows.Data.Xml.Dom, ContentType = WindowsRuntime] | Out-Null
$doc = New-Object Windows.Data.Xml.Dom.XmlDocument
$doc.LoadXml(@'
{xml}
'@)
$toast = New-Object Windows.UI.Notifications.ToastNotification $doc
[Windows.UI.Notifications.ToastNotificationManager]::CreateToastNotifier('{APP_ID}').Show($toast)
"""
    encoded = base64.b64encode(script.encode("utf-16-le")).decode("ascii")
    subprocess.run(
        [powershell, "-NoProfile", "-NonInteractive", "-EncodedCommand", encoded],
        cwd=PS_WORKDIR,
        stdout=subprocess.DEVNULL,
        stderr=subprocess.DEVNULL,
        timeout=15,
        check=False,
    )


def log(line: str) -> None:
    try:
        with LOG_PATH.open("a", encoding="utf-8") as f:
            f.write(f"{datetime.now():%Y-%m-%d %H:%M:%S} {line}\n")
    except OSError:
        pass


def main() -> int:
    try:
        payload = json.loads(sys.stdin.read() or "{}")
    except (json.JSONDecodeError, ValueError):
        return 0

    message = build_message(payload)
    if message is None:
        return 0

    powershell = find_powershell()
    if powershell is None:
        log("SKIP powershell.exe 를 찾지 못했습니다 (WSL 환경이 아닌 것으로 보입니다)")
        return 0

    title, body = message
    try:
        show_toast(powershell, title, body)
    except (OSError, subprocess.SubprocessError) as exc:
        log(f"FAIL {title} | {exc}")
        return 0

    log(f"OK {title} | {body}")
    return 0


if __name__ == "__main__":
    sys.exit(main())
===== END FILE: ~/.claude/hooks/wsl_toast.py =====

===== BEGIN FILE: ~/.claude/commands/commit.md (sha256=ee669c64fbd6ce90673b9661d4dae2d7b55dcab237f74120cd785dfe2306ac11 lines=39 bytes=2563 mode=0o644) =====
---
description: 현재 변경 내용을 읽고 커밋 메시지 후보 5개를 제시 (커밋은 사용자가 직접)
allowed-tools: Bash(git status:*), Bash(git diff:*), Bash(git log:*), Read, Glob, Grep
---

현재 변경 내용을 확인해서 커밋 메시지 후보를 제시해줘. 아래 규칙을 지켜:

1. Git 상태를 바꾸는 작업은 절대 하지 말 것.
   add, commit, push, checkout, restore, stash, reset 등 전부 금지.
   읽기 전용 조회(git status, git diff, git log)만 사용한다. 커밋은 내가 직접 한다.

2. 대상 범위는 staged 변경이 있으면 staged 만(git diff --cached),
   없으면 unstaged + untracked 전체로 잡는다.
   어느 쪽을 대상으로 삼았는지 먼저 밝힐 것.

3. 메시지는 "실제로 수행된 변경" 기준으로만 쓴다(추정 금지).
   파일 목록·파일명만 보고 내용을 추측하지 말고 diff 를 실제로 읽을 것.
   diff 가 크면 파일별로 나눠 읽되, 안 읽은 부분을 읽은 것처럼 쓰지 말 것.
   untracked 파일은 diff 에 안 잡히므로 직접 읽어서 확인한다.

4. 형식은 `기능명 / 변경 요약` 을 기본으로 한다.
   기능명은 아래 순서로 정한다.
   - 현재 프로젝트의 CLAUDE.md 에 기능명(prefix) 컨벤션이 있으면 그것을 그대로 따른다.
   - 없으면 변경된 파일 경로와 내용을 보고 적절한 한글 기능명을 정한다.
     (예: docs/ → `문서 / `, tests/ → `테스트 / `, 수집 로직 → `수집 / `, 검증 로직 → `검증 / `)
   - 여러 영역이 섞였으면 변경의 중심이 되는 영역 하나를 기능명으로 쓴다.

5. `Commit Messages (Final candidates)` 제목 아래 후보 5개를 번호 목록으로 제시한다.
   - 5개는 서로 실질적으로 달라야 한다. 요약 수준(짧게 ↔ 상세)과 강조점(무엇을 ↔ 왜)을 다르게 가져갈 것.
   - 어미나 조사만 바꾼 사실상 동일한 후보는 금지.
   - 나는 그중 1개를 골라 실제 커밋 메시지로 쓴다.

6. 후보 앞에는 변경 요약을 3~6줄로 짧게 붙인다(어떤 파일이 무엇 때문에 바뀌었는지).
   후보 뒤에는 아무것도 덧붙이지 말 것(커밋 명령어 안내, 다음 단계 제안 금지).

7. 서로 무관한 변경 덩어리가 섞여 있으면, 후보를 내기 전에 그 사실을 먼저 알리고
   커밋 분리 기준을 제안한다. 분리 여부는 내가 결정한다.

8. 변경 사항이 전혀 없으면 후보를 만들지 말고 그 사실만 알린다.
===== END FILE: ~/.claude/commands/commit.md =====

===== BEGIN FILE: ~/.claude/commands/settings-merge.md (sha256=5a08b38af92fe816eaffb17fdeae05331d0f9c4054a246e6b2df503304abf02c lines=41 bytes=2391 mode=0o644) =====
---
description: 프로젝트 .claude/settings.local.json 의 permission 을 전역 ~/.claude/settings.json 으로 정리·병합
allowed-tools: Read(~/.claude/settings.json), Edit(~/.claude/settings.json), Edit(.claude/settings.local.json), Bash(jq:*)
---

프로젝트의 .claude/settings.local.json 에 있는 permission 항목들을
전역 ~/.claude/settings.json 으로 정리해서 옮겨줘. 아래 규칙을 지켜:

1. 먼저 두 파일을 모두 읽어라. 전역의 기존 설정(model, hooks, theme 등)은
   절대 건드리지 말고 permissions 만 병합(merge)할 것. (전체 치환 금지)

2. 여러 항목이 공통 접두사를 공유하면 프리픽스 와일드카드 하나로 통합.
   (예: 서로 다른 awk 명령 여러 개 → Bash(awk *))

3. 전역에 이미 있는 더 넓은 규칙으로 커버되는 항목은 삭제.
   (예: 전역에 bare "Read" 가 있으면 로컬의 Read(...) 는 전부 삭제)

4. 특수 상황·일회성·특정 도구/프로젝트 전용 명령은 전역에 넣지 말 것.
   다시 필요하면 그때 승인 프롬프트로 처리하면 됨.
   (예: 특정 스크립트 실행(./xxx.sh), 단발성 설치·네트워크 테스트 등)

5. 파괴적·비가역 "전용" 명령어는 allow 에 절대 넣지 말 것 (와일드카드로
   좁히지도 말고 배제). 이미 있으면 삭제.
   (rm, rmdir, mv, dd, chmod, truncate, 파일 덮어쓰기 리다이렉트 등)

6. git, npm 처럼 '읽기·일반'과 '파괴적' 작업이 섞인 도구는 통짜 와일드카드
   (Bash(git *), Bash(npm *))로 allow 하지 말 것. 파괴적 서브명령까지
   딸려 자동승인되기 때문.
   - allow 에서 빼서 기본적으로 승인 프롬프트가 뜨게 둘 것.
   - 원격 반영 등 절대 자동실행되면 안 되는 것(git push 등)만 deny 로
     하드 차단.
   - (읽기가 너무 자주 프롬프트되면 그 읽기 서브명령만 골라 allow 에
     추가하는 건 나에게 확인받고 진행.)

7. 경계가 애매한 항목(예: python3 *, node -e * 같은 임의코드 실행)은
   임의로 결정하지 말고 나에게 확인받고 진행할 것.

8. 편집 후 jq 로 JSON 유효성 검증하고, 어떤 항목을 무슨 규칙으로
   통합/삭제/배제/차단했는지 매핑 표로 보여줄 것.

9. 옮긴 뒤 로컬 settings.local.json 의 allow 배열은 비울 것.
===== END FILE: ~/.claude/commands/settings-merge.md =====

