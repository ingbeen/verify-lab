# 다음세션 프롬프트

> 이 파일은 verify-lab 세션 인계의 **단일 진입점**입니다. 아래 `---` 아래를 새 세션 첫 메시지에
> 그대로 복사해 붙여넣습니다. 관리 규칙은 루트 `CLAUDE.md` "세션 인계 규칙" 이 SoT입니다.
>
> **마지막 갱신**: 2026-09-03 (KST) — 검증 **#7 에서 매매 규칙을 도출**해 `docs/strategy/` 에 남겼다

---

verify-lab 을 이어서 진행한다.
**검증 #7(옵션 만기일)의 매매 규칙까지 확정됐고, 다음에 무엇을 할지는 정해져 있지 않다.**

## 가장 먼저 알아야 할 것

**아무것도 자동으로 시작하지 않는다.** 먼저 무엇을 할지 사용자에게 묻는다.

미착수 후보는 `docs/ROADMAP.md` "검증 대상 목록" 의 **#2 비트코인 4년 주기설 ·
#3 정배열 매매법 · #4 탑티어 우량주 반토막** 셋이고, 그 밖에 **검증 #1 결과 문서를
단기 구간으로 개정하는 후속 작업**이 미착수로 남아 있다.
**어느 것도 선택되지 않았고 사용자가 다른 것을 가져올 수도 있다.**

## 먼저 읽을 것 (순서대로)

1. `CLAUDE.md` (루트) 와 `docs/INDEX.md`
   - **「이 프로젝트의 과제는 둘이다」 표를 가장 먼저** 읽는다. 과제 A(통계적 우위)가 1차 질문이고
     과제 B(QQQ 와의 분리)는 부가 질문이다 — **B 가 아니라는 이유로 A 를 탈락시키지 않는다**
   - **측정의 원칙은 17개다.** 16(합산 수익률 병기)과 17(구간 분해)이 2026-08-31·09-03 에 추가됐다
   - **후보 판정 기준의 「등급은 떨어뜨리지 않는다」를 반드시 읽는다.** 등급으로 칸을 빼면
     사후 선택이 된다 — 실제로 그렇게 했다가 되돌린 적이 있다
   - 함정: **`strategy/` 는 예외 계층**이다. 손절·익절·자금배분 설계는 **그 폴더 안에서만** 허용되고
     밖에서는 여전히 금지다. 경계는 폴더 단위이며 `.claude/rules/strategy.md` 가 SoT다
2. `docs/ROADMAP.md` — **진행 상태의 SoT**
3. `docs/context/` 두 문서 — 사용자의 현재 운용 상태

## 현재 확정된 매매 규칙 둘

| 규칙 | 문서 | 상태 |
| --- | --- | --- |
| **역방향 매매** | `docs/strategy/역방향_매매_규칙.md` | 운용 중 |
| **옵션 만기일 매매** | `docs/strategy/옵션_만기일_매매_규칙.md` | **2026-09-03 확정** |

**둘 다 §1 이 "확정된 매매법만 참고하라"의 지시 대상**이다. 규칙을 물으면 §1 만 읽으면 된다.

옵션 만기일은 **7칸(9월 넷 · 12월 셋) · 손절 −5% · 연 2회 · 알림 없음(사용자가 직접 챙김)** 이다.
2026년 실행 날짜는 그 문서 §1.6 에 있다.

## 남은 일 — 옵션 만기일 관련

- **없다.** 규칙·문서·테스트·산출물이 전부 닫혔다
- 다만 **2026-09-10(목) KODEX 200 진입**이 가장 가까운 실행일이다. 사용자가 직접 챙긴다
- `docs/plans/` 의 네 계획서(손절 격자 · QQQ 12월 추가 · 손절선 확정 · 구간 축)는
  **전부 Done 이고 근거는 승격이 끝났다.** 지워도 잃는 것이 없다

## 이미 밟은 함정 — 다시 밟지 말 것

- **등급으로 칸을 빼지 않는다.** QQQ 12월을 등급 0/3 이라고 뺐다가 되돌렸다.
  게이트(적중률·기대값)만 칸을 가르고 등급은 알려주기만 한다
- **회당 기대값만 적지 않는다.** 신호가 드문 매매법은 회당이 구조적으로 작다.
  **합산과 표본 수를 같은 표에** 둔다 (원칙 16)
- **구간 분해에서 표본이 모자란 행을 지우지 않는다.** 0건이어도 행을 남기고
  `판정가능` 을 「아니오」로 적는다. 0 으로 채우는 것도 금지다 (원칙 17)
- **`--grid` 없이 손절 격자를 재현하려 하지 않는다.** 기본 실행은 확정값 −5% 하나만 낸다
- **CSV 를 Excel 로 열어 저장하지 않는다.** 날짜 앞 0 이 지워지고 소수 끝자리가 잘려
  재분석·대조가 깨진다. 실제로 겪었다
- **DNS 실패를 "네트워크 차단"으로 오진하지 않는다.** 루트 `CLAUDE.md`
  "외부 요청이 실패하면" 의 3단계를 순서대로 밟는다

## 작업 규칙

- **모든 코드 변경은 계획서 선행.** `docs/plans/PLAN_*.md`. 문서만 고치는 작업은 예외
- **검증 명령은 마지막 Phase 에서만** — `poetry run python validate_project.py`
  (`failed=0 skipped=0` 이 통과 기준). 포맷은 `poetry run black .`
- **Git 은 사용자가 직접 한다.** add·commit·push 를 하지 않는다
- 커밋 메시지는 `기능명 / 내용` 형식이고 기능명은 변경 경로로 정한다 (루트 `CLAUDE.md`)
