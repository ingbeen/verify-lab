# 다음세션 프롬프트

> 이 파일은 verify-lab 세션 인계의 **단일 진입점**입니다. 아래 `---` 아래를 새 세션 첫 메시지에
> 그대로 복사해 붙여넣습니다. 관리 규칙은 루트 `CLAUDE.md` "세션 인계 규칙" 이 SoT입니다.
>
> **마지막 갱신**: 2026-08-27 (KST) — 원달러 그리드 트랙 **종료**(채택 안 함) · 검증 #6 완료

---

verify-lab 을 이어서 진행한다.
**원달러 그리드 트랙은 종료됐고, 다음으로 분석할 매매법은 아직 정해져 있지 않다.**

## 가장 먼저 알아야 할 것

**다음에 무엇을 검증할지가 정해져 있지 않다.** 미착수 후보는 `docs/ROADMAP.md` "검증 대상 목록" 의
**#2 비트코인 4년 주기설 · #3 정배열 매매법 · #4 탑티어 우량주 반토막** 셋이지만,
**어느 것도 선택되지 않았고 사용자가 다른 것을 가져올 수도 있다.**

**아무것도 자동으로 시작하지 않는다.** 먼저 무엇을 할지 사용자에게 묻는다.

## 먼저 읽을 것 (순서대로)

1. `CLAUDE.md` (루트) 와 `docs/INDEX.md`
   - **「이 프로젝트가 무엇인가」와 「측정의 원칙」 8개를 가장 먼저** 읽는다. 이 저장소는 백테스트
     도구가 아니라 **측정소**이며, 그 구분이 모든 판단의 기준이다
   - 함정: **`strategy/` 는 예외 계층**이다. 손절·익절·자금배분 설계는 **그 폴더 안에서만** 허용되고
     밖에서는 여전히 금지다. 경계는 폴더 단위이며 `.claude/rules/strategy.md` 가 SoT다
   - `docs/INDEX.md` 가 저장소 전체의 문서 지도다
2. `docs/ROADMAP.md` — **진행 상태의 SoT**
   - **"G5 이후 — 트랙 종료" 절의 「다음 트랙이 알아야 할 것」을 반드시 읽는다.**
     이번에 배운 것 중 다음 검증에 그대로 적용되는 교훈이 거기 있다
   - 함정: **각 Phase 절의 결론은 체크리스트가 아니라 절 끝의 「다음 단계가 알아야 할 것」에 있다**
3. `docs/context/README.md` 와 그 폴더의 두 문서
   - 이 프로젝트의 출발 질문(「QQQ와 다르게 움직이는가」)이 여기서 나왔고 **아직 미답이다**
   - 함정: **사용자 소유 문서다.** 오탈자를 발견해도 고치지 말고 언급만 한다 (`.claude/rules/context.md`)
4. `docs/research/달러_조달_방식.md` — **가장 최근 결론** (검증 #6)
   - 그리드를 왜 안 쓰는지, 밴드·분할·백분위를 왜 전부 기각했는지가 여기 있다
   - **§10.2 와 §11(기각된 가설)이 핵심**이다
5. 그리드 트랙을 다시 볼 일이 생기면 그때만 — `docs/spec/usdkrw_grid.md`(1,600줄 넘음),
   `docs/strategy/원달러_그리드.md`, `docs/research/원달러_ETF_등가성.md`
   - 함정: **§4 「확정된 설계 결정」(C1~C118)은 다시 논의하지 않는다**
   - 함정: **§5.2 의 취소선 항목은 「지워진 것」이 아니라 「이미 논의했다」는 기록**이다
6. `docs/COMMANDS.md` — 실행 명령어의 단일 SoT. 품질 게이트 기준선이 "품질 검증" 절에 있다

## 현재 확정된 결론

- **원달러 그리드는 채택되지 않았다** (2026-08-27). 성적 때문이 아니라 **사용자의 실제 목적과
  충돌**하기 때문이다 — 원달러 급등 시 전량 익절로 달러가 0이 되는데 달러가 필요한 순간이 그때다.
  판정은 `docs/strategy/원달러_그리드.md` §2.8
- **사용자의 달러 운용 목적은 매매 수익이 아니라 미국주식 매수 자금 확보다.** 이 사실이
  그리드 트랙 5단계를 거치는 동안 문서에 없었고, 그래서 성적만 정밀하게 재고 목적 적합성을
  묻지 않았다. `docs/research/달러_조달_방식.md` §2
- **채택한 것은 「기계적 시간 분할 매수 + 백분위 알림(정보용)」 하나뿐이다** —
  `docs/strategy/원달러_백분위_알림.md` 가 SoT. **분할 기간과 배정액은 미확정**이며 사용자 결정이다
- **백분위에 예측력이 없다는 것이 측정 결과다.** 그래서 알림에 「매우 쌈」 같은 판정 어휘를
  붙이지 않는다 (결정 D4)
- **검증 #1(지수 극단 이벤트)에서 확정된 계층 계약은 그대로 유효하다** —
  `docs/ROADMAP.md` Phase 2·3 의 "계층 간 계약" 다섯 절
- **`docs/strategy/역방향_매매_규칙.md` 가 현재 유일하게 운용 중인 규칙이다**

## 남은 일

| # | 할 일 | 상태 |
|---|---|---|
| 1 | **다음에 분석할 매매법을 정한다** — **아직 정해져 있지 않다.** 후보는 `docs/ROADMAP.md` "검증 대상 목록" 의 #2·#3·#4 지만 사용자가 다른 것을 가져올 수 있다 | **대기** (사용자 결정) |
| 2 | **달러 분할 매수의 기간과 배정액을 정한다** — 코드 작업이 아니다. `docs/strategy/원달러_백분위_알림.md` §1.1 이 비어 있는 자리 | **대기** (사용자 결정) |
| 3 | 백분위 알림 구현 (입력·발송 수단) — **지금은 하지 않는다.** 달러 보유가 0이라 입력할 내역이 없다 | 보류 |

## 이번 세션에 원하는 것

**먼저 사용자에게 「무엇을 할지」를 묻는다. 어느 것도 자동으로 시작하지 않는다.**

1. 미착수 검증 셋(#2·#3·#4)을 나란히 제시하되, **사용자가 다른 것을 원할 수 있음을 전제**한다
2. 정해지면 **계획서부터 쓴다** — `docs/plans/PLAN_<short_name>.md` 이고 절차의 SoT 는
   `/verify-plan` 스킬이다. 코드를 먼저 건드리지 않는다
3. `docs/ROADMAP.md` "2번 이후 검증에서 미리 알아둘 것" 을 착수 전에 본다 —
   비트코인은 이벤트-일 기반 틀에 안 맞을 수 있고, 개별종목은 생존편향과 가격제한폭 변경을 다뤄야 한다

## 이미 밟은 함정 — 다시 밟지 말 것

**사본을 만들지 말고 아래를 열어서 본다.**

- **가장 크게 배운 것 — 성적이 좋은지와 목적에 맞는지는 다른 질문이다.**
  그리드는 G0~G5 다섯 단계로 성적을 정밀하게 쟀지만 **「달러가 필요한 날에 있었는가」를
  한 번도 묻지 않았다.** 새 검증은 착수 전에 **판정 기준이 사용자의 실제 목적과 일치하는지**
  먼저 확인한다 (`docs/ROADMAP.md` "다음 트랙이 알아야 할 것")
- **「조건이 오면 한다」는 규칙은 조건이 안 올 때를 반드시 함께 재야 한다.**
  이 트랙에서 같은 실패가 세 번 나왔고 **셋 다 성적표에는 안 나타나고 가용성에만 나타났다**
  (`docs/research/달러_조달_방식.md` §10.2)
- **파라미터를 바꿔 결론이 흔들리면 그것은 「그 값이 좋다」가 아니라 「표본이 그 지점을 특정할
  만큼 크지 않다」는 신호다.** 흔들린 값을 채택하면 사양서 §15.1 위반이다
- **가공된 값에서 원지표를 역산하지 않는다** (결정 C112), **미래를 요구하는 규칙은 계산식이 아니라
  하루 처리 순서로 푼다** (결정 C57·C58) — 그리드 트랙의 전체 목록은 `docs/spec/usdkrw_grid.md` §3
- **낡은 진입점.** 이 파일이 바로 그 자리다 — **작업이 진척되면 `/handoff` 로 이 파일을 갱신**한다.
  낡은 진입점은 없는 것보다 나쁘다

## 작업 규칙

- **`docs/strategy/역방향_매매_규칙.md` 는 현재 운용 중인 규칙이다. 손대지 않는다**
- **시세를 이유 없이 다시 받지 않는다.** `storage/market/` · `storage/series/` 에 이미 있으면 그것을 읽는다.
  받았다면 새 데이터 기준일과 무엇이 달라졌는지를 해당 결과 문서에 적는다
- **원달러 종가는 2026-08-26 까지 갱신돼 있다** (2026-08-27 재수집, 3거래일 추가, 과거 값 변경 0건).
  그리드 산출물의 5,340 거래일과 어긋나므로 **그리드를 다시 돌리면 기존 문서 수치가 재현되지 않는다**
