# 다음세션 프롬프트

> 이 파일은 verify-lab 세션 인계의 **단일 진입점**입니다. 아래 `---` 아래를 새 세션 첫 메시지에
> 그대로 복사해 붙여넣습니다. 관리 규칙은 루트 `CLAUDE.md` "세션 인계 규칙" 이 SoT입니다.
>
> **마지막 갱신**: 2026-08-26 (KST) — G5 완료 · `START_PROMPT.md` 폐기

---

verify-lab 의 원달러 그리드 트랙을 이어서 진행한다.
**G5 까지 계획된 단계가 전부 끝난 상태이고, 이번 세션의 목표는 「다음 트랙을 정하는 것」이다.**

## 먼저 읽을 것 (순서대로)

1. `CLAUDE.md` (루트) 와 `docs/INDEX.md`
   - **「이 프로젝트가 무엇인가」와 「측정의 원칙」 8개를 가장 먼저** 읽는다. 이 저장소는 백테스트
     도구가 아니라 **측정소**이며, 그 구분이 모든 판단의 기준이다
   - 함정: **`strategy/` 는 예외 계층**이다. 손절·익절·자금배분 설계는 **그 폴더 안에서만** 허용되고
     밖에서는 여전히 금지다. 경계는 폴더 단위이며 `.claude/rules/strategy.md` 가 SoT다
   - `docs/INDEX.md` 가 저장소 전체의 문서 지도다. 여기서 못 찾은 문서는 없다고 봐도 된다
2. `docs/ROADMAP.md` — **진행 상태의 SoT**
   - **「G5 이후 — 남은 선택지」 절을 가장 먼저** 읽는다. 이번 세션이 정할 것이 거기 셋으로 정리돼 있다
   - 함정: **각 Phase 절의 결론은 체크리스트가 아니라 절 끝의 「다음 단계가 알아야 할 것」에 있다.**
     체크박스만 훑으면 그 트랙이 남긴 판단을 통째로 놓친다
3. `docs/spec/usdkrw_grid.md` — **그리드 트랙의 SoT** (1,600줄 넘음. 통독하지 말고 아래 순서로)
   - **§4 「확정된 설계 결정」(A1~A4 · B1~B2 · C1~C118)은 다시 논의하지 않는다.**
     §4.11 (C105~C118) 이 가장 최근에 확정된 것들이다
   - **§3 「데이터 실측 기록」(3.1~3.15)이 이 트랙에서 밟은 함정의 전체 목록**이다. §3.15 가 가장 최근이다
   - 함정: **§5.2 의 취소선 항목은 「지워진 것」이 아니라 「이미 논의했다」는 기록**이다.
     같은 길을 다시 걷지 않으려고 남겨 둔 것이므로 지우거나 무시하지 않는다
4. `docs/strategy/원달러_그리드.md` — **G5 결과 문서이자 이 트랙의 최종 산출물**
   - 규칙(§1) · 성적(§2) · 결정 근거(§3) · 한계(§4) · 체결 원자료(§5)가 한 파일에 있다
   - 함정: **§2.7 은 「채택」이 아니라 「아직 판단 전」이다.** 이 규칙은 운용 대상이 아니다
   - 함정: **시세를 다시 받으면 이 문서의 수치가 재현되지 않는다** (§5.3). 성적이 원달러 종가
     **2026-08-21 · 5,340 거래일**에 묶여 있고, 수집기는 같은 파일명에 덮어쓴다
5. `docs/spec/usdkrw_grid_rules.md` — 사용자가 쓴 **매매 규칙 원본 사양서**
   - **그대로 구현하지 않는다.** 문서 머리에 §4 가 바꾼 것 4건의 표가 있다
     (환율 기준 · 이론값 식 · 환전 비용 2건). 충돌하면 언제나 `usdkrw_grid.md` §4 가 이긴다
6. `docs/research/원달러_ETF_등가성.md` — ETF 경로를 쓸 수 있는지의 판정 (**조건부 가능**)
   - **§8(연도별 괴리 연 ±1%p)과 §14(한계)가 ETF 성적을 해석할 때의 전제**다
7. `docs/context/README.md` 와 그 폴더의 두 문서 — **선택지 B 를 고르면 필수**
   - 이 프로젝트의 출발 질문(「QQQ와 다르게 움직이는가」)이 여기서 나왔다
   - 함정: **사용자 소유 문서다.** 오탈자를 발견해도 고치지 말고 언급만 한다 (`.claude/rules/context.md`)
8. `docs/COMMANDS.md` — 실행 명령어의 단일 SoT. **품질 게이트 통과 기준선이 「품질 검증」 절에 있다**

## 현재 확정된 결론

- **G0~G5 가 전부 완료다.** 원달러 그리드 트랙에 계획된 단계가 남아 있지 않다 —
  `docs/ROADMAP.md` "Phase 진행 상태"
- **확정 결정 C1~C118 은 다시 논의하지 않는다** — `docs/spec/usdkrw_grid.md` §4 가 전문이다
- **결론 여섯 개 중 넷은 여덟 축 전부에서 안정이고, 「분할매수 후 보유를 이긴다」가 셋에서 뒤집힌다** —
  근거는 `docs/spec/usdkrw_grid.md` §3.15, 사람이 읽는 요약은 `docs/strategy/원달러_그리드.md` §2.4
- **열려 있는 미해결과 해소된 것의 목록은 `docs/spec/usdkrw_grid.md` §5.1 · §5.2 가 SoT**다.
  `START_PROMPT.md` 에 있던 사본은 폐기됐으므로 그 목록만 믿는다
- **검증 #1(지수 극단 이벤트)에서 확정된 계층 계약은 그대로 유효하다** —
  `docs/ROADMAP.md` Phase 2·3 의 "계층 간 계약" 다섯 절이 근거다

## 남은 일

| # | 할 일 | 상태 |
|---|---|---|
| 1 | **다음 트랙을 정한다** — A(지정가 대조군 G6) · B(QQQ 상관 측정) · C(다른 검증) 중 하나. 근거는 `docs/ROADMAP.md` "G5 이후 — 남은 선택지" | **대기** (사용자 결정) |
| 2 | **이 규칙을 운용할 것인가** — 코드 작업이 아니다. `docs/strategy/원달러_그리드.md` §2.7 의 판정과 §4 의 한계가 근거이며, **필요한 것은 성적이 아니라 감내 가능성**이다 | **대기** (사용자 결정) |
| 3 | **`docs/plans/` 에 `.gitkeep` 을 되살린다** — 지금 폴더가 비었는데 `.gitkeep` 이 없다. git 은 빈 폴더를 담지 않으므로 **다른 PC 에서 pull 하면 폴더가 사라져 계획서 게이트가 조용히 꺼진다** (`.claude/hooks/plan_lint.py` 의 `project_uses_plans()` 가 디렉토리 존재로 규약 채택을 판정한다) | **미착수** |
| 4 | 검증 #2 비트코인 4년 주기설 · #3 정배열 매매법 · #4 탑티어 우량주 반토막 — `docs/ROADMAP.md` "검증 대상 목록" | 미착수 |

## 이번 세션에 원하는 것

**먼저 사용자에게 「선택지 A·B·C 중 무엇을 할지」를 묻는다. 어느 것도 자동으로 시작하지 않는다.**

1. **셋을 나란히 제시한다** — `docs/ROADMAP.md` "G5 이후 — 남은 선택지" 를 근거로, 각각이
   무엇에 답하는지와 규모(어디를 고쳐야 하는지)까지 함께 적는다
2. **권장안은 B(QQQ 와의 상관 측정)** 다. 권하는 이유는 같은 절의 인용 블록에 적혀 있으니
   여기서 되풀이하지 말고 그 문장을 그대로 사용자에게 보인다
3. **정해진 뒤에 계획서부터 쓴다** — `docs/plans/PLAN_<short_name>.md` 이고 절차의 SoT 는
   `/verify-plan` 스킬이다. 코드를 먼저 건드리지 않는다

## 이미 밟은 함정 — 다시 밟지 말 것

**이 트랙에서 밟은 함정은 전부 문서에 남아 있다. 사본을 만들지 말고 아래를 열어서 본다.**

- **전체 목록**: `docs/spec/usdkrw_grid.md` §3.1~§3.15 (실측 기록)와 §4 각 결정의 "탈락안 · 근거" 칸
- **가장 다시 밟기 쉬운 셋** — 전부 예외가 나지 않고 결과만 조용히 틀린다
  - **뒤집힌 축값을 「좋은 값」으로 옮기기.** N=7 · g=1.6% 에서 이긴다는 사실을 채택하면
    사양서 §15.1 의 「결과를 보고 파라미터 변경 금지」를 어긴다. 흔들림은 "그 값이 좋다"가 아니라
    **"표본이 그 지점을 특정할 만큼 크지 않다"** 는 신호다 (§3.15)
  - **가공된 값에서 원지표를 역산하기.** `rp = max(T-bill − 스프레드, 하한)` 은 되돌릴 수 없고
    하한이 거래일의 48.8% 에 걸려 그 구간은 정보가 통째로 사라진다 (결정 C112)
  - **미래를 요구하는 규칙을 계산식으로 풀기.** 「이자일수 −1」과 「매월 마지막 거래일」은
    둘 다 계산식이 아니라 **하루 처리 순서**로 풀린다 (결정 C57 · C58)
- **낡은 진입점.** 이 파일이 바로 그 자리다 — **작업이 진척되면 `/handoff` 로 이 파일을 갱신**한다.
  낡은 진입점은 없는 것보다 나쁘다

## 작업 규칙

- **`docs/strategy/역방향_매매_규칙.md` 는 현재 운용 중인 규칙이고 이 트랙과 무관하다. 손대지 않는다**
- **시세를 이유 없이 다시 받지 않는다.** `storage/market/` · `storage/series/` 에 이미 있으면 그것을 읽는다.
  덮어쓰면 `docs/strategy/원달러_그리드.md` 의 성적이 그 시점부터 재현되지 않으므로,
  받았다면 새 데이터 기준일과 무엇이 달라졌는지를 그 결과 문서에 적는다
