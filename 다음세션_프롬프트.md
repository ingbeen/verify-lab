# 다음세션 프롬프트

> 이 파일은 verify-lab 세션 인계의 **단일 진입점**입니다. 아래 `---` 아래를 새 세션 첫 메시지에
> 그대로 복사해 붙여넣습니다. 관리 규칙은 루트 `CLAUDE.md` "세션 인계 규칙" 이 SoT입니다.
>
> **마지막 갱신**: 2026-09-04 (KST) — 검증 **#9(선물 대 레버리지 ETF)의 데이터와 구현이 끝났고
> 결과 분석·결과 문서만 남았다**

---

verify-lab 을 이어서 진행한다.
**검증 #9(선물 대 레버리지 ETF)의 데이터 수집과 구현이 전부 끝났고, 남은 것은 결과를 읽고
`docs/research/선물_대_레버리지_ETF.md` 를 쓰는 것 하나다.**

## 가장 먼저 할 것

**검증을 다시 돌릴 필요가 없다.** 산출물이 이미 나와 있다 —
`storage/results/` 의 가장 최근 `*_futures_leverage` 폴더를 읽는다.
다시 돌리고 싶으면 `poetry run python scripts/studies/run_futures_leverage.py` 이며
**수집은 다시 하지 않는다**(시세가 `storage/market/` 에 있고, 재수집하면 이 수치가
그 시점부터 재현되지 않는다).

## 먼저 읽을 것 (순서대로)

1. `CLAUDE.md` (루트) 와 `docs/INDEX.md`
   - **측정의 원칙은 17개다.**
   - **`strategy/` 는 예외 계층**이다. 손절·익절·자금배분 설계는 그 폴더 안에서만 허용된다
2. **[docs/spec/futures_leverage.md](docs/spec/futures_leverage.md)** — 검증 #9 의 확정 설계.
   **§2.2 의 분해 산식과 §4 의 확정 결정 ①~⑨ 를 읽지 않으면 결과를 해석할 수 없다**
3. `docs/ROADMAP.md` — 진행 상태의 SoT
4. `docs/context/` 두 문서 — 사용자의 현재 운용 상태

## 검증 #9 — 무엇이 끝났고 무엇이 남았나

| 단계 | 상태 |
| --- | --- |
| 데이터 수집 | **완료** — 코스피200 38,438행(128계약, 1996-05-03~) · 코스닥150 18,508행(50계약, 2015-11-23~) |
| 구현 | **완료** — 수집기·전용 로더·연속 계열·포지션 엔진·구간 수익률·분해·실행 CLI |
| 품질 검증 | **통과** — Ruff·PyRight·Pytest 788건 (failed=0, skipped=0) |
| 문서 | spec·COMMANDS·ROADMAP·INDEX·scripts/CLAUDE.md **완료** |
| **결과 분석과 `docs/research/` 문서** | **미착수 — 이것이 남은 일 전부다** |

계획서 `docs/plans/PLAN_futures_leverage.md` 는 아직 In Progress 이며,
**결과 문서를 쓰고 나서 Done 처리**한다.

### 결과를 읽을 때 반드시 함께 읽어야 하는 것

- **이것은 매매법 검증이 아니다.** 「후보 판정 기준」·「기준선 대비 차이」가 적용되지 않는다.
  적중률·우연확률을 내지 않으며 **그것이 없다는 이유로 미완성으로 보지 않는다**
- **인버스 넷(114800·252670·251340·530107)은 ETF 자신이 선물지수를 추종한다.**
  「선물 대 ETF」가 「직접 굴리는 선물 대 포장된 선물」이 되고 **롤 비용이 양쪽에 있어
  잔여로 분리되지 않는다.** 레버리지 둘(122630·233740)만 깨끗하게 갈린다
- **`롤·베이시스 몫` 은 양수면 롤 수익**(백워데이션), 음수면 롤 비용(콘탱고)이다.
  이름이 부호를 정하지 않는다
- **3년(756거래일) 칸은 비중첩 표본이 한 자릿수**다. 통계가 아니라 사례에 가깝다
- **월 1회 리밸런싱은 「매월 1일」이 아니라 「진입 후 21거래일마다」**다. 근거는 spec §4 ⑤

### 1차 실행에서 이미 보이는 것 (2026-09-04, 미결제약정 역전 · 1년 보유)

| 지수 | 종목 | 배수 | 롤·베이시스 몫 | 리밸런싱 오차 | 여유현금 이자 | 잔여 | 선물 − ETF |
| --- | --- | --- | --- | --- | --- | --- | --- |
| KOSPI200 | 122630 | 2 | −0.05%p | −0.81%p | **+3.19%p** | +0.15%p | +0.05%p |
| KOSPI200 | 114800 | −1 | −0.07 | +0.16 | **+2.20** | −1.05 | −0.98 |
| KOSPI200 | 252670 | −2 | +0.34 | +0.34 | **+1.83** | +0.50 | −0.18 |
| KOSDAQ150 | 233740 | 2 | **+2.39** | +2.09 | **+2.50** | −0.73 | **+4.05** |
| KOSDAQ150 | 251340 | −1 | +1.89 | +1.64 | **+1.95** | +0.54 | −1.36 |
| KOSDAQ150 | 530107 | −2 | −1.12 | +9.46 | **+2.23** | −2.49 | −0.25 |

**가장 큰 항목이 여유현금 이자(+1.8~+3.2%p/년)다.** 즉 「선물이 싼가」의 답은 대부분
**증권사가 예탁금 이용료를 주느냐**에 달려 있고, 그것은 사용자의 실제 계좌 조건이 정한다.
**이 사실을 결과 문서의 결론에 반드시 넣는다.**

**코스피200 과 코스닥150 의 롤 몫이 크게 다르다** — 코스피200 은 거의 0(−0.05%p/년),
코스닥150 은 +2.39%p/년(백워데이션). 왜 다른지는 결과 문서에서 다뤄야 한다.

**자기자본 소진은 전 칸 0건**이다 — 30년 전 구간에서 2배·−2배 모두 소진 없음.

### 산출물 파일

`storage/results/<실행시각>_futures_leverage/` 에 있다. **CSV 헤더는 한글**이고 비율은 백분율 2자리다.

| 파일 | 내용 |
| --- | --- |
| `comparison.csv` (378행) | 지수 × 배수 × 방식 × 구간 집계. **가장 먼저 볼 표** |
| `decomposition.csv` (84행) | 차이 분해 |
| `roll_events.csv` (328행) | 롤 원자료. **판정일과 집행일이 따로** 있다 |
| `breakeven.csv` · `wipeouts.csv` · `leverage_drift.csv` | 경계·소진·배수 표류 |
| `windows_<지수>_<종목>.csv` | 시작일 전체 목록. **사용자가 차트와 대조하는 자리** |
| `summary.json` | 실행 조건 |

## 현재 확정된 매매 규칙 둘

| 규칙 | 문서 | 상태 |
| --- | --- | --- |
| **역방향 매매** | `docs/strategy/역방향_매매_규칙.md` | 운용 중 |
| **옵션 만기일 매매** | `docs/strategy/옵션_만기일_매매_규칙.md` | 2026-09-03 확정 |

**둘 다 §1 이 "확정된 매매법만 참고하라"의 지시 대상**이다. 규칙을 물으면 §1 만 읽으면 된다.
옵션 만기일은 **7칸(9월 넷 · 12월 셋) · 손절 −5% · 연 2회 · 알림 없음**이고
2026년 실행 날짜는 그 문서 §1.6 에 있다.

## 이미 밟은 함정 — 다시 밟지 말 것

**검증 #9 에서 새로 겪은 것** (전부 `docs/spec/futures_leverage.md` 에 근거가 있다)

- **`MDCSTAT12701` 을 개별 계약 시세로 쓰지 않는다.** 「최근월물 시세 추이」라 하루 한 행만
  주고 원월물이 통째로 빠진다. 개별 계약은 **`MDCSTAT12601`** 이다 (spec §5.2)
- **선물 시세를 `load_market_csv` 로 읽지 않는다.** 날짜 기준 중복 제거가 같은 날짜의 계약을
  첫 개만 남기고 지우는데 **경고만 뜨고 예외가 없다.** `load_futures_csv` 를 쓴다
- **선물 수집을 두 상품 동시에 돌리지 않는다.** 요청이 몰려 KRX 가 JSON 이 아닌 응답을 준다
- **스프레드 종목을 이름으로만 거르지 않는다.** 표기가 시대별로 다르다
  (2001~2005 「스프레드」 / 2010~ 「SP」). 미결제약정 결측을 함께 본다

**앞선 검증에서 겪은 것**

- **등급으로 칸을 빼지 않는다.** 게이트(적중률·기대값)만 칸을 가르고 등급은 알려주기만 한다
- **회당 기대값만 적지 않는다.** 합산과 표본 수를 같은 표에 둔다 (원칙 16)
- **구간 분해에서 표본이 모자란 행을 지우지 않는다.** 0건이어도 행을 남기고
  `판정가능` 을 「아니오」로 적는다. 0 으로 채우는 것도 금지다 (원칙 17)
- **CSV 를 Excel 로 열어 저장하지 않는다.** 날짜 앞 0 이 지워지고 소수 끝자리가 잘린다
- **DNS 실패를 "네트워크 차단"으로 오진하지 않는다.** 루트 `CLAUDE.md` 의 3단계를 밟는다

## 미착수로 남아 있는 다른 검증

`docs/ROADMAP.md` "검증 대상 목록" 의 **#2 비트코인 4년 주기설 · #3 정배열 매매법 ·
#4 탑티어 우량주 반토막** 셋과, **검증 #1 결과 문서를 단기 구간으로 개정하는 후속 작업**.
**검증 #9 결과 문서를 끝낸 뒤에 사용자에게 무엇을 할지 묻는다.**

## 작업 규칙

- **모든 코드 변경은 계획서 선행.** `docs/plans/PLAN_*.md`. 문서만 고치는 작업은 예외
- **검증 명령은 마지막 Phase 에서만** — `poetry run python validate_project.py`
  (`failed=0 skipped=0` 이 통과 기준). 포맷은 `poetry run black .`
- **Git 은 사용자가 직접 한다.** add·commit·push 를 하지 않는다
- 커밋 메시지는 `기능명 / 내용` 형식이고 기능명은 변경 경로로 정한다 (루트 `CLAUDE.md`)
