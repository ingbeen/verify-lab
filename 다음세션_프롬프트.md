# 다음세션 프롬프트

> 이 파일은 verify-lab 세션 인계의 **단일 진입점**입니다. 아래 `---` 아래를 새 세션 첫 메시지에
> 그대로 복사해 붙여넣습니다. 관리 규칙은 루트 `CLAUDE.md` "세션 인계 규칙" 이 SoT입니다.
>
> **마지막 갱신**: 2026-08-28 (KST) — 검증 **#7 옵션 만기일 완료**(판정: 우위 없음)

---

verify-lab 을 이어서 진행한다.
**검증 #7(옵션 만기일)이 완료됐고, 다음으로 분석할 매매법은 아직 정해져 있지 않다.**

## 가장 먼저 알아야 할 것

**다음에 무엇을 검증할지가 정해져 있지 않다.** 미착수 후보는 `docs/ROADMAP.md` "검증 대상 목록" 의
**#2 비트코인 4년 주기설 · #3 정배열 매매법 · #4 탑티어 우량주 반토막** 셋이고, 그 밖에
**검증 #1 결과 문서를 단기 구간으로 개정하는 후속 작업**이 미착수로 남아 있다.
**어느 것도 선택되지 않았고 사용자가 다른 것을 가져올 수도 있다.**

**아무것도 자동으로 시작하지 않는다.** 먼저 무엇을 할지 사용자에게 묻는다.

## 먼저 읽을 것 (순서대로)

1. `CLAUDE.md` (루트) 와 `docs/INDEX.md`
   - **「이 프로젝트의 과제는 둘이다」 표를 가장 먼저** 읽는다. 과제 A(통계적 우위)가 1차 질문이고
     과제 B(QQQ 와의 분리)는 부가 질문이다 — **B 가 아니라는 이유로 A 를 탈락시키지 않는다**
   - **측정의 원칙은 9개다.** 9번(지수가 아니라 ETF 로 잰다)이 2026-08-27 에 추가됐다
   - 함정: **`strategy/` 는 예외 계층**이다. 손절·익절·자금배분 설계는 **그 폴더 안에서만** 허용되고
     밖에서는 여전히 금지다. 경계는 폴더 단위이며 `.claude/rules/strategy.md` 가 SoT다
2. `docs/ROADMAP.md` — **진행 상태의 SoT**
   - 검증 목록 7개 중 **#1·#5·#6·#7 완료, #2·#3·#4 미착수**
   - 함정: **각 Phase 절의 결론은 체크리스트가 아니라 절 끝의 「다음 단계가 알아야 할 것」에 있다**
3. `docs/research/옵션_만기일.md` — **가장 최근 결론** (검증 #7)
   - **§1 결론 요약(근거 9개)과 §12 「기각된 가설」이 핵심**이다. §12 에는 실행에서 무너진
     설계 4개가 이유와 함께 있다
4. `docs/spec/option_expiry.md` — 검증 #7 의 확정 설계
   - 함정: **결정 ⑮ 가 최초 설계(§3.5 베이스라인)를 뒤집었다.** §7.7 이 그 기록이며,
     "베이스라인이 치우쳐 있으면 검정이 통과해도 아무 뜻이 없다"가 이 트랙의 가장 큰 교훈이다
   - §7 에 데이터 실측 7건이 있다 — 만기 달력·QQQ 배당락·재수집·pykrx 수정주가 창·
     KODEX 분배락·수정주가 교차검증·실행 기록
5. `docs/context/README.md` 와 그 폴더의 두 문서
   - 이 프로젝트의 출발 질문(「QQQ와 다르게 움직이는가」)이 여기서 나왔고 **아직 미답이다**
   - 함정: **사용자 소유 문서다.** 오탈자를 발견해도 고치지 말고 언급만 한다 (`.claude/rules/context.md`)
6. `docs/COMMANDS.md` — 실행 명령어의 단일 SoT. 품질 게이트 기준선이 "품질 검증" 절에 있다

## 현재 확정된 결론

- **검증 #7 판정: 옵션 만기일에 방향성 우위는 없다** (2026-08-27). 순열 검정 **315칸 중 평균
  p<0.05 가 20칸, 우연 기대치가 15.8칸**이다. 문헌이 보고한 만기주 초과 +0.33%p 가 재현되지 않는다
  (QQQ **-0.099%p** · SPY **-0.019%p**). 근거는 `docs/research/옵션_만기일.md` §1·§7
- **이 프로젝트의 과제는 둘이다** — A(통계적 우위)가 1차, B(QQQ 와의 분리)는 부가.
  **B 미충족은 탈락 사유가 아니다.** 루트 `CLAUDE.md` 가 SoT
- **측정 대상은 지수가 아니라 ETF 다** (측정의 원칙 9). 실제 매매가 ETF 로 이루어지기 때문
- **수치를 적는 문서는 데이터 기간(시작~종료, 행 수)을 머리말에 적는다.** 종료일만으로는 부족하다.
  `.claude/rules/docs.md` 가 SoT이며, 기존 살아있는 문서 8개에 소급 기재됐다
- **시세가 2026-08-27 에 재수집·확장됐다** — QQQ 6,908행(~08-25) · SPY 8,450행 신규(~08-25) ·
  069500 5,891행(~08-26). 수정주가 3벌도 신규다. 상세는 `docs/spec/option_expiry.md` §7.3
- **`yfinance_collector` 가 수정주가를 `<종목>_adjusted_max.csv` 로 분리 저장한다** (2026-08-27).
  이전에는 원본가 파일을 덮어썼다
- **원달러 그리드는 채택되지 않았다** (2026-08-27). 성적이 아니라 **사용자의 실제 목적과 충돌**해서다.
  판정은 `docs/strategy/원달러_그리드.md` §2.8
- **채택한 것은 「기계적 시간 분할 매수 + 백분위 알림(정보용)」 하나뿐이다** —
  `docs/strategy/원달러_백분위_알림.md` 가 SoT. **분할 기간과 배정액은 미확정**이며 사용자 결정이다
- **`docs/strategy/역방향_매매_규칙.md` 가 현재 유일하게 운용 중인 규칙이다**

## 남은 일

| # | 할 일 | 상태 |
|---|---|---|
| 1 | **다음에 분석할 매매법을 정한다** — 후보는 `docs/ROADMAP.md` "검증 대상 목록" 의 #2·#3·#4 지만 사용자가 다른 것을 가져올 수 있다 | **대기** (사용자 결정) |
| 2 | **검증 #1 결과 문서를 단기 구간으로 개정한다** — `docs/ROADMAP.md` "후속 작업 — 결과 문서를 단기 구간으로 개정". 대상은 `docs/research/지수_극단_이벤트.md` §5~§13 과 `역방향 전체` 방향 축 | 미착수 |
| 3 | **달러 분할 매수의 기간과 배정액을 정한다** — 코드 작업이 아니다. `docs/strategy/원달러_백분위_알림.md` §1.1 이 비어 있는 자리 | **대기** (사용자 결정) |
| 4 | 백분위 알림 구현 (입력·발송 수단) — **지금은 하지 않는다.** 달러 보유가 0이라 입력할 내역이 없다 | 보류 |

## 이번 세션에 원하는 것

**먼저 사용자에게 「무엇을 할지」를 묻는다. 어느 것도 자동으로 시작하지 않는다.**

1. **남은 일 표의 1·2·3 을 나란히 제시**하되, 사용자가 다른 것을 원할 수 있음을 전제한다
2. 정해지면 **계획서부터 쓴다** — `docs/plans/PLAN_<short_name>.md` 이고 절차의 SoT 는
   `/verify-plan` 스킬이다. 코드를 먼저 건드리지 않는다
3. 새 검증이면 `docs/ROADMAP.md` "2번 이후 검증에서 미리 알아둘 것" 을 착수 전에 본다 —
   비트코인은 이벤트-일 기반 틀에 안 맞을 수 있고, 개별종목은 생존편향과 가격제한폭 변경을 다뤄야 한다

## 이미 밟은 함정 — 다시 밟지 말 것

**사본을 만들지 말고 아래를 열어서 본다.**

- **베이스라인이 치우쳐 있으면 검정이 통과해도 아무 뜻이 없다.** 검증 #7 의 최초 설계는 베이스라인을
  「만기 창 밖 거래일」로 뒀는데, 창이 한 달을 거의 다 덮어 **전 거래일의 5%** 밖에 남지 않았다.
  그 치우친 표본과 견주니 **315칸 중 189칸이 p<0.05**, **평균 0.004% 짜리 칸까지 유의**하게 나왔다.
  **유의성보다 그 크기가 말이 되는지를 먼저 본다** (`docs/spec/option_expiry.md` 결정 ⑮·§7.7)
- **집계표에서 축 컬럼이 조용히 사라질 수 있다.** 바깥 Series 로 `groupby` 하면 결과에 축이 남지 않아
  "행이 무엇에 대한 값인지" 알 수 없는 표가 **예외 없이** 나온다. 축은 프레임 안의 컬럼으로 만든 뒤
  이름으로 묶는다. `tests/test_studies_expiry_runner.py` 가 고정한다
- **배당락·분배락이 신호 위치에 달력상 고정돼 있으면 원본가를 쓸 수 없다.** 프로젝트가 원본가를
  정당화한 근거("왜곡이 신호군과 베이스라인에 똑같이 걸려 초과분에서 지워진다")가 그때는 성립하지
  않는다. 새 검증마다 **신호 위치와 배당락 위치가 겹치는지 착수 전에 실측**한다
- **"관행상 그럴 것"이라는 추측을 실측으로 대신하지 않는다.** 국내 분배락이 월말이라 만기(둘째 목요일)와
  안 겹칠 것이라 적었는데 **틀렸다** — 47건 중 42건이 만기 창 안이었다 (`docs/spec/option_expiry.md` §7.5)
- **pykrx 수정주가는 창이 미끄러진다.** 조회 시점 기준 최근 3,000거래일이라 **시작일이 조회할 때마다
  달라진다** (2026-08-12 조회 → 2014-05-22 시작, 2026-08-27 조회 → 2014-06-09 시작). 같은 문서 §7.4
- **시세를 덮어쓰기 전에 백업한다.** QQQ 재수집 때 백업을 안 남겨 **과거 값 변경 건수를 세지 못했다.**
  069500 은 백업이 있어 0건을 확인했다. 같은 문서 §7.3
- **성적이 좋은지와 목적에 맞는지는 다른 질문이다.** 원달러 그리드는 5단계로 성적을 정밀하게 쟀지만
  「달러가 필요한 날에 있었는가」를 한 번도 묻지 않았다. 새 검증은 착수 전에 **판정 기준이 사용자의
  실제 목적과 일치하는지** 먼저 확인한다 (`docs/ROADMAP.md` "다음 트랙이 알아야 할 것")
- **파라미터를 바꿔 결론이 흔들리면 그것은 「그 값이 좋다」가 아니라 「표본이 그 지점을 특정할 만큼
  크지 않다」는 신호다**

## 작업 규칙

- **`docs/strategy/역방향_매매_규칙.md` 는 현재 운용 중인 규칙이다. 손대지 않는다**
- **시세를 이유 없이 다시 받지 않는다.** `storage/market/` · `storage/series/` 에 이미 있으면 그것을 읽는다.
  **받아야 한다면 덮어쓰기 전에 스크래치패드로 백업**하고, 새 기준일과 과거 값 변경 건수를
  해당 결과 문서에 적는다
- **검증 #1 을 다시 돌리면 기존 문서 수치가 재현되지 않는다.** QQQ·069500 이 2026-08-27 에
  확장됐고, `docs/research/지수_극단_이벤트.md` 의 6,903행·5,881행 기준 수치는 **그 문서가 측정할
  때의 값**이다. 재실행하지 않았는데 기간만 갱신하지 않는다
- **남은 일 #2 의 대상 산출물 폴더가 이미 사라졌다.** `storage/results/` 는 git 제외라
  `20260823_131353_index_extreme` 가 남아 있지 않다. 재생성해야 하고, 위 항목 때문에
  **그때와 같은 수치가 나오지 않는다**
