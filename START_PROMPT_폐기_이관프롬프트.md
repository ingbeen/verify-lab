# START_PROMPT.md 폐기 · handoff 전환 — 실행 프롬프트

> **사용법**: verify-lab 세션에서 아래 `---` 사이를 그대로 복사해 붙여넣는다.

---

`START_PROMPT.md`(619줄, git 추적) 를 폐기하고, 앞으로 세션 인계를 `/handoff` 스킬로 일원화하려 한다.
**이 작업의 절대 조건은 「아무것도 잃지 않는 것」이다.**

## 배경 — 왜 바꾸나

- `/handoff` 스킬을 여러 저장소·여러 PC 공용으로 쓰기로 했다. `START_PROMPT.md` 는 이 저장소에만 있는 별도 관용이라 이중 관리가 된다.
- **`/handoff` 산출물은 이 저장소에서 git 추적 대상이어야 한다.** 다른 PC(Windows)에서 이어서 작업하는데, untracked 파일은 넘어가지 않는다. `START_PROMPT.md` 가 두 PC 에서 작동했던 이유가 추적 대상이었기 때문이다.

## 절대 지킬 것

1. **`START_PROMPT.md` 를 먼저 지우지 않는다.** 삭제는 마지막 단계이고, 무손실 검증과 내 승인을 통과한 뒤에만 한다.
2. **git 명령을 실행하지 않는다.** 조회(`git status`·`git log`·`git diff`)만 하고, 커밋·삭제 반영은 내가 직접 한다.
3. 옮기는 것은 **결론뿐 아니라 근거**다. "값이 X다"가 아니라 "왜 X인가"를 함께 옮긴다.

## 1단계 — 분류표를 먼저 만들어 보여줄 것 (아직 아무것도 옮기지 말 것)

`START_PROMPT.md` 의 7개 절을 전부 읽고, **줄 단위로** 아래 세 갈래로 분류한다.

| 분류 | 뜻 | 목적지 |
|---|---|---|
| **A. 승격** | 세션이 바뀌어도 계속 참인 지식. 함정·확정된 결정·환경 제약 | `docs/` 의 살아있는 문서 |
| **B. 인계** | 지금 시점에만 참인 것. 현재 진척·다음에 할 일·미결 | 새 인계 프롬프트 |
| **C. 이미 있음** | 다른 문서에 같은 내용이 이미 있는 것 | 옮기지 않고 **경로로 참조**만 |

절별 분량은 이렇다. 큰 절일수록 안에서 A·B·C 가 섞여 있을 것이다.

```
먼저 읽을 것 (순서대로, 전부)                    26줄
현재 상태                                       108줄
지금까지가 남긴 것 — 다음 단계가 반드시 알아야 한다  236줄
다음 작업 — 사용자가 정한다                      184줄
검증 #1 에서 확정돼 계속 유효한 것                 17줄
반드시 지킬 것                                    15줄
실행 환경 주의                                    11줄
```

**C 로 분류할 때는 반드시 대조해서 확인한다.** "아마 스펙에 있을 것"으로 넘기지 말고, 해당 문서를 열어 같은 내용이 실제로 있는지 확인하고 **문서명과 절 번호를 적는다.** 없으면 C 가 아니라 A 다.

**A 의 목적지는 후보를 제시하고 나에게 묻는다.** 임의로 정하지 않는다. 현재 후보는 `docs/ROADMAP.md`·`docs/spec/usdkrw_grid.md`·`docs/spec/usdkrw_grid_rules.md`·`docs/COMMANDS.md`·`docs/INDEX.md`·`docs/context/` 다.

분류표를 표로 보여주고 **내 승인을 받은 뒤** 2단계로 간다.

## 2단계 — 승격 (A 항목)

- 승인된 목적지로 옮긴다. **덧붙이는 것이지 덮어쓰는 것이 아니다.** 기존 내용을 지우지 않는다.
- 옮긴 뒤 `docs/INDEX.md` 의 해당 요약줄도 함께 본다. 본문만 고치고 요약을 두면 요약이 먼저 읽혀 오판이 남는다.
- 옮긴 항목마다 **원본의 몇 번째 절에서 왔는지**를 작업 기록에 남긴다 (문서 본문이 아니라 나에게 보고할 내용으로).

## 3단계 — verify-lab 의 인계 규칙을 `CLAUDE.md` 에 명시

`/handoff` 스킬은 **"위치와 추적 여부는 그 프로젝트 규칙을 따른다"** 로 되어 있다. 이 저장소 규칙을 정해 적어야 스킬이 그대로 따른다.

`CLAUDE.md` 에 다음 취지의 절을 추가한다 (문구는 기존 톤에 맞춰 다듬어도 된다).

- 세션 인계는 `/handoff` 로 한다. 저장소 루트의 **고정 파일명** 하나를 매번 갱신하며, **git 추적 대상**이다 (여러 PC 에서 이어서 작업하므로).
- 파일명 후보를 제시하고 나에게 확인받을 것. (`다음세션_프롬프트.md` 를 기본안으로 본다)
- 계획서(`docs/plans/`)와는 다른 물건이다 — 계획서는 임시 산출물이고 인계 프롬프트는 상시 유지된다.

## 4단계 — 새 인계 프롬프트 작성 (B 항목)

`/handoff` 스킬의 `TEMPLATE.md` 골격을 따른다. B 로 분류한 내용만 담는다.

- **A 로 승격한 내용을 여기에 다시 쓰지 않는다.** 경로로 참조한다. 복사하면 두 벌이 되고 한쪽이 낡는다.
- 「먼저 읽을 것 (순서대로)」에는 경로만이 아니라 **각 문서의 함정**(폐기된 값·낡은 절·오해하기 쉬운 지점)을 같이 적는다.
- 「이번 세션에 원하는 것」은 명령문으로 쓴다. 지금 다음 작업이 무엇인지는 원본의 「다음 작업」 절을 근거로 삼되, 확정되지 않았으면 그렇게 적는다.

## 5단계 — 참조 정리

- `docs/INDEX.md:21` 이 아직 `START_PROMPT.md` 를 진입점으로 가리킨다. 새 파일로 바꾼다.
- `docs/plans/` 안의 `START_PROMPT` 참조 20여 곳은 **과거 계획서의 기록이므로 손대지 않는다.** 그때 실제로 그 파일을 갱신한 것이 사실이기 때문이다.
- 그 외 참조가 더 있는지 `grep` 으로 전수 확인하고 목록을 보고한다.

## 6단계 — 무손실 검증 (숫자로 보고)

- 원본 619줄이 **A/B/C 어디로 갔는지 빠짐없이 대응**되는지. 분류되지 않고 남은 줄이 있으면 그 자체가 결함이다
- A 로 옮긴 항목이 목적지 문서에서 실제로 검색되는지 (문자열로 확인)
- C 로 분류한 항목이 정말 다른 곳에 있는지 재확인
- 새 인계 프롬프트가 `TEMPLATE.md` 의 절 구성을 따르는지

## 7단계 — 삭제는 마지막, 내 승인 뒤에

- 6단계 결과를 보고하고 **내 승인을 받은 뒤에만** `START_PROMPT.md` 를 지운다.
- `rm` 으로 파일만 지운다. **`git rm` 은 쓰지 않는다.** 커밋은 내가 한다.
- 지운 뒤 `git status` 를 보여줘서 내가 커밋할 것을 확인할 수 있게 한다.

## 마지막 보고에 넣을 것

- 분류 결과 요약 (A/B/C 각 항목 수와 줄 수)
- A 항목별 목적지와 이동 근거
- 새 인계 프롬프트 경로와 추적 여부
- `CLAUDE.md` 에 추가한 규칙
- 정리한 참조 목록
- 내가 직접 해야 할 git 명령

---
