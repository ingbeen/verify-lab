"""원달러 그리드 매매 규칙

**상태 구동 매매법**이다. `studies` 가 낸 신호 목록이 아니라 **시세 자체**를 입력으로 받으며,
어느 레벨을 사는지가 「그 레벨을 이미 보유 중인지」에 달려 있어 가격만의 함수로 표현되지 않는다.
이 입력 계약은 `.claude/rules/strategy.md` 가 SoT다.

규칙의 원본 사양서는 `docs/spec/usdkrw_grid_rules.md` 이고, **그대로 구현하지 않는다** —
착수 전 논의와 실측으로 바뀐 부분이 있으며 `docs/spec/usdkrw_grid.md` §4 가 이긴다.

기존 `strategy/constants.py`(역방향 매매)는 옮기지 않는다. 이동하면 기존 코드와 테스트의
import 경로가 바뀌고 얻는 것은 이름의 대칭뿐이다 (결정 A4).
"""
