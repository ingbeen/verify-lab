"""매매 규칙 계층

측정 결과(`studies`·`measure`)로부터 도출한 **매매 규칙**을 적용해 체결 내역과 성적을 낸다.

이 계층은 루트 `CLAUDE.md` 가 "하지 않는 일"로 규정한 진입·손절·보유기간 설계를 다루는
**명시적 예외**이며, 경계는 폴더 단위다. 허용 범위와 제약은 `.claude/rules/strategy.md` 가 SoT다.

`measure` 와 다른 점은 **경로 의존**이다. 손절은 장중 저가·고가를 봐야 하고 자금 조각마다
청산 시점이 갈리므로, forward return 테이블로는 표현되지 않는다.
"""
