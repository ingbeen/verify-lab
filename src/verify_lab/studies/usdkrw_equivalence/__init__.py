"""검증 #5 — 원달러 ETF 등가성

**261240 이 「환전 + 달러 예치」의 대체재인가**를 잰다. 매매 로직이 전혀 없는 순수 측정이며,
사양서 §16 이 "1단계보다 먼저 실행한다"고 규정한 게이트다. 여기서 "대체 불가" 판정이 나오면
그리드 백테스트의 ETF 경로가 폐기된다.

**이 검증은 이벤트가 없다.** `studies` 의 통상 계약("어느 날이 신호인가")을 따르지 않으므로
`measure` 의 forward return 을 쓰지 않고 자체 회귀 통계를 낸다. 대신 `report/writer.py` 의
산출물 저장은 그대로 재사용한다 — 계산은 다르지만 저장 규칙은 같아야 한다.

확정된 설계와 근거는 `docs/spec/usdkrw_grid.md` 가 SoT다.
"""
