# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/

# 가상환경
.venv/
venv/

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# 자격증명 — 저장소에 절대 포함하지 않는다
.env
.env.*

# 검증 실행 결과 (실행마다 쌓이며 언제든 재생성 가능)
storage/results/

# 수집한 원시 시세(storage/market/)는 git 으로 동기화한다.
# 지수 ETF 몇 종이라 용량이 작고, 같은 데이터로 결과를 재현할 수 있어야 하기 때문이다.
# 개별종목 검증이 들어와 용량이 커지면 그때 이 정책을 다시 판단한다.

# 실행 로그
logs/
